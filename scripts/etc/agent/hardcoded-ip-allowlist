# Files that intentionally include IPv4 literals (CloudFormation templates, etc.)
# Entries are repo-relative paths matched exactly.
scripts/etc/acg-cluster.yaml
scripts/lib/acg/scripts/etc/acg-cluster.yaml
# OrbStack assigns 127.0.0.2 as k3d LoadBalancer alias — no DNS hostname available
scripts/etc/cloudflared/config.yml
