# Active Context — k3d-manager

## Current Status (2026-05-26 — ArgoCD expected drift ignore fix complete on v1.4.10)
- Current branch: `k3d-manager-v1.4.10`
- **COMPLETE:** `scripts/etc/argocd/applicationsets/services-git.yaml` now ignores expected drift for `Secret/order-service-secrets` and `ConfigMap/product-catalog-seed-script` in `shopping-cart-apps`, so ArgoCD no longer fights ESO-owned / generated data objects and `shopping-cart-order` / `shopping-cart-product-catalog` can settle to `Synced / Healthy`; committed as `898927eb` (`fix(argocd): ignore expected order and product-catalog drift`) and pushed to `origin/k3d-manager-v1.4.10`.
- **COMPLETE:** `docs/issues/2026-05-26-argocd-expected-drift-order-and-product-catalog.md` captures the OutOfSync/Healthy drift on `shopping-cart-order` and `shopping-cart-product-catalog`, the exact objects involved, and the recommended `services-git` ignoreDifferences policy. Committed as `450a72e3` (`docs: add argocd expected drift issue`).
- **CONTEXT:** `fix/acg-up-reconcile-order-password` now captures the order-service PostgreSQL reconcile fix and its RCA doc after the remote `ubuntu-k3s` rebuild exposed a PostgreSQL auth mismatch.
- **CONTEXT:** After the remote `ubuntu-k3s` rebuild, `order-service` stayed degraded because `postgresql-orders` was never reconciled back to the Vault-backed password that `order-service-secrets` consumes. The RCA is documented in `docs/issues/2026-05-26-order-service-postgresql-password-reconcile-missing.md`.
- **COMPLETE:** `scripts/plugins/shopping_cart.sh` now reconciles `postgresql-orders` to `_pg_pass_orders` during data-layer bootstrap, force-syncs `order-service-secrets`, and restarts `order-service` if its `DB_PASSWORD` env is stale; `scripts/tests/plugins/shopping_cart.bats` now asserts the helper exists. Committed as `cbf9a980` (`fix(acg-up): reconcile order-service postgres password after sandbox seed`) and pushed to `origin/fix/acg-up-reconcile-order-password`. Validation: `shellcheck -S warning bin/acg-up scripts/plugins/shopping_cart.sh` passed; `git diff --check` passed; `bats scripts/tests/plugins/shopping_cart.bats` passed; `./scripts/k3d-manager _agent_audit` passed.
- **COMPLETE:** `docs/issues/2026-05-26-order-service-postgresql-password-reconcile-missing.md` captures the missing orders-password reconcile step that caused the rebuild-time PostgreSQL auth failure. Committed as `80df2255` (`docs: add order-service password reconcile issue`).
- **CONTEXT:** AWS ACG sandbox expiry forced a remote `ubuntu-k3s` rebuild; the rebuilt cluster surfaced a bootstrap gap where `redis-cart-secret`, `payment-db-credentials`, `payment-encryption-secret`, and MinIO were not fully restored before app startup. See `docs/issues/2026-05-26-acg-sandbox-expiry-remote-cluster-rebuild-shopping-cart-bootstrap-gap.md`.
- **CONTEXT:** The prevention guidance in `docs/issues/2026-05-26-acg-sandbox-expiry-remote-cluster-rebuild-shopping-cart-bootstrap-gap.md` now narrows the recovery path to keep the local `k3d` cluster intact and make `make up` / `bin/acg-up` always re-run Vault secret reconciliation after the remote ACG sandbox expires.
- **COMPLETE:** `bin/acg-up` now re-applies `shopping-cart-infra/data-layer/secrets/` and `data-layer/minio/secret.yaml`, waits for the vault-backed ExternalSecrets to become Ready, and deploys `data-layer/minio/` before the app layer starts so `redis-cart-secret`, `payment-db-credentials`, `payment-encryption-secret`, and `minio-credentials` are restored automatically on a sandbox rebuild; committed as `d51433e8` (`fix(acg-up): sync vault-backed data-layer secrets and MinIO on every make up`) and pushed to `origin/k3d-manager-v1.4.10`. Validation: `shellcheck -S warning scripts/plugins/shopping_cart.sh` passed; `bats scripts/tests/plugins/shopping_cart.bats` passed; `bats scripts/tests/bin/acg_up.bats` passed; `./scripts/k3d-manager _agent_audit` passed; `git diff --check` passed.
- **COMPLETE:** `bin/acg-up` now seeds `secret/minio/credentials` in Vault KV with `root-user=minioadmin` (reusing existing values when present) and a random `root-password`, so `data-layer/minio/secret.yaml` syncs automatically via ESO; committed as `8ec93e68` (`fix(acg-up): seed minio Vault KV credentials for ESO sync`) and pushed to `origin/k3d-manager-v1.4.10`. Validation: `shellcheck -S warning bin/acg-up` passed; `./scripts/k3d-manager _agent_audit` passed; `./scripts/k3d-manager _agent_checkpoint` failed with `.git/index.lock` permission errors and the failure was recorded in `docs/issues/2026-05-26-acg-up-minio-checkpoint-index-lock-permission-error.md` (committed as `4e923d36`).
- **COMPLETE:** `bin/acg-up` decomposition refactor per `docs/plans/v1.4.10-refactor-acg-up-decomposition.md` extracted `shopping_cart_prepare_infra_bootstrap`, `shopping_cart_prepare_cluster_secrets_and_seed`, and `shopping_cart_reconcile_product_catalog` into `scripts/plugins/shopping_cart.sh`, and `bin/acg-up` now calls those helpers around Step 7 / Step 10. Committed as `85aac02a` (`refactor(acg-up): extract shopping-cart bootstrap helpers`) and pushed to `origin/k3d-manager-v1.4.10`. Validation this pass: `shellcheck -S warning bin/acg-up scripts/plugins/shopping_cart.sh` passed; `./scripts/k3d-manager _agent_audit` passed; `bats scripts/tests/bin/acg_up.bats`, `bats scripts/tests/plugins/shopping_cart.bats`, and `bats scripts/tests/plugins/argocd.bats` passed; `./scripts/k3d-manager _agent_checkpoint` still fails with `.git/index.lock` permission errors (tracked in `docs/issues/2026-05-26-agent-checkpoint-index-lock-permission-error.md`).
- **COMPLETE:** `scripts/etc/argocd/applicationsets/services-git.yaml` now sets `project: shopping-cart` for the `services-git` ApplicationSet template so shopping-cart service apps land in the `shopping-cart` ArgoCD project instead of `platform`; committed as `469a86a7` (`fix(argocd): assign shopping-cart services to shopping-cart project in services-git ApplicationSet`) and pushed to `origin/k3d-manager-v1.4.10`. Validation: `./scripts/k3d-manager _agent_audit` passed; `./scripts/k3d-manager _agent_checkpoint` failed with `.git/index.lock` permission errors and the failure was recorded in `docs/issues/2026-05-26-agent-checkpoint-index-lock-permission-error.md` (committed as `3c486d9f`).
- **COMPLETE:** `bin/acg-down` now cleans stale `/tmp/argocd-*.sock`, `/tmp/k3d-config-tmp-*.yaml`, and `/tmp/k3d-hostsfile-*` files before its final Done message; committed as `7f9890e6` (`fix(acg-down): clean up stale argocd sockets and k3d tmp files on teardown`) and pushed to `origin/k3d-manager-v1.4.10`. Validation: `shellcheck -S warning bin/acg-down` passed; `./scripts/k3d-manager _agent_audit` passed; `./scripts/k3d-manager _agent_checkpoint` failed with `.git/index.lock` permission errors and the failure was recorded in `docs/issues/2026-05-26-agent-checkpoint-index-lock-permission-error.md` (committed as `1b5d862e`).
- **CONTEXT:** ubuntu-k3s cluster restarted fresh (2026-05-26); cicd namespace empty; ArgoCD not yet deployed. Next `acg-up` run will apply the corrected ApplicationSet.
- **COMPLETE:** services imagePullSecrets refactor — `bin/acg-up` patches `default` ServiceAccount in `shopping-cart-apps` after `ghcr-pull-secret` creation (SA patch moved outside namespace loop, `--context ubuntu-k3s` added); all 5 `services/shopping-cart-*/kustomization.yaml` have `imagePullSecrets` patches removed; order-service functional patches (probes + RabbitMQ ConfigMap/env) restored after Codex incorrectly deleted them; commits `1f69b38d` (Codex) + `b03de3bd` (Claude fix); shellcheck clean ✓.
- **COMPLETE — shopping-cart-frontend PR #23 POST-MERGE:** `fix/cart-response-unwrap` → `main` merged (SHA `0ca35f0` / 2026-05-25); enforce_admins restored ✓; `docs/next-improvements` branch already existed on shopping-cart-frontend, checked out locally ✓; no version bump (Unreleased CHANGELOG only); retrospective (`2026-05-25-fix-cart-response-unwrap-retrospective.md`) created + committed (`4ebecfc`) + pushed to origin ✓; k3d-manager memory-bank updated ✓
- **COMPLETE — shopping-cart-frontend PR #22 POST-MERGE:** `fix/product-detail-field-mapping` → `main` merged (SHA `bef902a1` / 2026-05-25); enforce_admins restored ✓; `docs/next-improvements` branch already existed, checked out locally ✓; no version bump (Unreleased CHANGELOG); retrospective (`2026-05-25-pr22-product-detail-fix-retrospective.md`) committed (`1581e93`) + pushed to origin ✓
- **COMPLETE — shopping-cart-payment PR #21 POST-MERGE:** `fix/payment-remove-placeholder-secret` → `main` merged (SHA `9f9701bb` / 2026-05-25); enforce_admins restored ✓; `docs/next-improvements` branch already existed, checked out locally ✓; no version bump (Unreleased CHANGELOG); retrospective (`2026-05-25-pr21-placeholder-secret-removal-retrospective.md`) committed (`a36179e`) + pushed to origin ✓
- **COMPLETE:** `shopping-cart-payment` branch `fix/payment-remove-placeholder-secret` now removes `k8s/base/secret.yaml` from `k8s/base/kustomization.yaml` and deletes the placeholder secret manifest so ESO owns `payment-db-credentials` and `payment-encryption-secret`; committed as `f3f3817` and pushed to `origin/fix/payment-remove-placeholder-secret`. Validation passed for `kubectl kustomize k8s/base`.
- **COMPLETE:** `shopping-cart-frontend` branch `fix/product-detail-field-mapping` now maps `quantity`→`stock` and `image_url`→`imageUrl` in `src/services/productService.ts:getProductById`, and adds the `/minio/` nginx proxy in `nginx.conf`; committed as `66b9007` and pushed to `origin/fix/product-detail-field-mapping`. Validation passed for `npm run build` and `npm run lint`.
- **COMPLETE — PR #80 POST-MERGE:** enforce_admins restored on main ✓; retrospective (`docs/retro/2026-05-25-v1.4.9-retrospective.md`) committed (`b285331a`) to k3d-manager-v1.4.10 ✓; standing docs audit (`docs/api/functions.md` — `acg_check_ttl` added) ✓; memory-bank updated ✓; branch cleanup (fetch --prune) ✓
- **MERGED PR #80:** v1.4.9 — ESO ownership fix, lib-acg v0.3.1 subtree pull, acg-down sudo pre-warm; merge SHA `a294dfc2` (2026-05-25); all Copilot findings addressed (9 on k3d-manager, 4 on lib-acg); v1.4.9 tag already exists
- **COMPLETE (operational):** products table on ubuntu-k3s cluster fixed — dropped old INTEGER PK table, restarted product-catalog (triggered `create_all()` with correct UUID schema), re-ran seed job → 1000 products seeded ✓

## Pending — Next Release Cycle
- **Node.js 20 → 22:** All shopping-cart CI workflows use `node-version: '20'`; GitHub Actions has deprecated Node.js 20 runner. Bump to `'22'` in all 5 repos. Issue doc: `shopping-cart-frontend/docs/issues/2026-05-22-nodejs20-deprecation.md`. Write one Codex spec covering all repos, single-pass.
- **`e2e/cart.spec.ts` and `CHANGELOG.md` on `docs/next-improvements`:** After squash-merge of PR #23, both files reverted to pre-fix flat Cart state. The wrapped mock responses (`{ success: true, data: {...} }`) and CHANGELOG cart fix entry are missing — need re-applying on `docs/next-improvements` branch before next PR from that branch.

## In Progress (v1.4.9 — open items carried forward)
- **OPEN — acg-cluster.yaml subtree path restoration** — lib-acg v0.2.0 removed `scripts/etc/acg-cluster.yaml` (CloudFormation template) without updating the reference in `acg.sh` (`_LIB_ACG_ROOT/scripts/etc/acg-cluster.yaml`), breaking `make up` with `Invalid template path`. Restored at subtree path `scripts/lib/acg/scripts/etc/acg-cluster.yaml` in k3d-manager; added to hardcoded-ip-allowlist for pre-commit IP literal check. CHANGELOG updated; PR to be created.
- **IN PROGRESS — shopping-cart-e2e-tests PR #1 open** (`chore/add-apache-license`); adds Apache 2.0 LICENSE + copilot-instructions.md + README/package.json license field; 8 Copilot findings fixed in `ca25f79` (PaymentClient, generateTestId, 60s timeout, localhost scope, MIT→Apache 2.0 in README/package.json); all 8 threads resolved; waiting for 3rd Copilot review. No branch protection on e2e-tests main yet.
- **IN PROGRESS — shopping-cart-infra PR #60 open** (`shopping-cart-infra-v0.5.5`); fixes Keycloak ERR_NAME_NOT_RESOLVED from office laptop — KC_HOSTNAME_URL→https://keycloak.3ai-talk.org, argocd-cm issuer+url→3ai-talk.org, realm argocd redirectUri→/auth/callback; CI green; 2 Copilot findings fixed in `6a9d128`; waiting for 2nd Copilot review. ArgoCD-cm and argocd-server already patched live.
- **REMAINING BLOCKER — ArgoCD Image Updater ghcr-pull-secret:** `ghcr-pull-secret` in `shopping-cart-apps` needs credential rotation — current token gets `permission_denied: The token provided does not match expected scopes` from ghcr.io OAuth2 tag-list API. Rotate with a classic PAT (`read:packages`) and update the secret on `ubuntu-k3s`.

## Planned (v1.4.10)
- **COMPLETE:** acg-up minio Vault seed — `bin/acg-up` now seeds `secret/minio/credentials` in Vault KV with `root-user=minioadmin` (reusing existing values when present) and a random `root-password`, so `data-layer/minio/secret.yaml` syncs automatically via ESO; committed as `8ec93e68` and pushed to `origin/k3d-manager-v1.4.10`. Validation: `shellcheck -S warning bin/acg-up` passed; `./scripts/k3d-manager _agent_audit` passed; `_agent_checkpoint` hit `.git/index.lock` permission errors and the failure was recorded in `docs/issues/2026-05-26-acg-up-minio-checkpoint-index-lock-permission-error.md` (committed as `4e923d36`).
- **COMPLETE:** services-git ApplicationSet project fix — `scripts/etc/argocd/applicationsets/services-git.yaml` now sets `project: shopping-cart` for shopping-cart service apps; committed as `469a86a7` and pushed to `origin/k3d-manager-v1.4.10`. Validation note: `./scripts/k3d-manager _agent_audit` passed; `_agent_checkpoint` hit `.git/index.lock` permission errors and the failure was recorded in `docs/issues/2026-05-26-agent-checkpoint-index-lock-permission-error.md` (committed as `3c486d9f`).
- **COMPLETE:** acg-down /tmp cleanup — `bin/acg-down` now removes stale `/tmp/argocd-*.sock`, `/tmp/k3d-config-tmp-*.yaml`, and `/tmp/k3d-hostsfile-*` files before the final Done message; committed as `7f9890e6` and pushed to `origin/k3d-manager-v1.4.10`. Validation: `shellcheck -S warning bin/acg-down` passed; `./scripts/k3d-manager _agent_audit` passed; `_agent_checkpoint` hit `.git/index.lock` permission errors and the failure was recorded in `docs/issues/2026-05-26-agent-checkpoint-index-lock-permission-error.md` (committed as `1b5d862e`).
- **POINTER:** `shopping-cart-frontend/docs/issues/2026-05-26-cart-page-error-loading-cart-api-route-mismatch-and-response-envelope.md` captures the cart page `Error loading cart` RCA: frontend uses `/api/cart` while basket-service serves `/api/v1/cart`, and the cart client also needs to unwrap the `{ success, data }` response envelope. Follow-up fix pending on the frontend repo.

## Archived
Entries for v1.4.2–v1.4.8 archived to `memory-bank/archive/activeContext-v1.4.2-v1.4.8.md`
