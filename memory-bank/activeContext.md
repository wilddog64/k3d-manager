# Active Context — k3d-manager

## Current Status (v1.11.0 RELEASED 2026-06-28; v1.12.0 — next branch cut)

- **SPEC WRITTEN + ASSIGNED TO CODEX (Claude, 2026-06-29) — expose hub Grafana at grafana.3ai-talk.org so Image Updater dashboard is visible:** `docs/bugs/v1.12.0-bugfix-grafana-image-updater-dashboard-exposure.md` written and handed to Codex on `k3d-manager-v1.12.0`. **Root cause (read-only live investigation, Claude 2026-06-29):** the dashboard is correct and fully populated on the HUB Grafana (`svc/kube-prometheus-stack-grafana` / context `k3d-k3d-cluster`), but `grafana.3ai-talk.org` → cloudflared → `localhost:3001` → the launchd port-forward written at `scripts/lib/providers/k3s-hostinger.sh:604-610` targets the Hostinger APP-cluster Grafana (`svc/acg-kube-prometheus-stack-grafana` / context `ubuntu-hostinger`), where commit `72751fc5` deleted the dashboard ("missing") and whose Prometheus has no `argocd_*` metrics + no Loki datasource ("no data"). Live confirm of the user's `:3001` Grafana: 0/24 dashboards are argocd/image-updater, `argocd_app_info`=0. **Fix = repoint the `:3001` port-forward to the hub Grafana** — 2-value change to the plist-writer call (`svc/acg-kube-prometheus-stack-grafana`→`svc/kube-prometheus-stack-grafana`, `${_HOSTINGER_KUBE_CONTEXT}`→`k3d-k3d-cluster`) + the matching `scripts/tests/lib/provider_contract.bats:556-559` assertions. Targets only `scripts/lib/providers/k3s-hostinger.sh` + `provider_contract.bats`. Commit msg: `fix(observability): point grafana.3ai-talk.org port-forward at hub Grafana so image-updater dashboard is visible`. **AWAITING Codex impl + SHA.** See [[project_oci_argocd_platform_helm]], [[project_infra_placement]].
- **COMPLETE + LIVE DEPLOYED (Codex, 2026-06-29) — Image Updater Grafana logs path fixed end-to-end on hub:** implemented on `k3d-manager-v1.12.0` as commits `04d9dfb1` (`fix(loki): keep hub release name compatible with promtail`) and `c58abaf8` (`fix(grafana): point hub loki datasource at loki-gateway`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 4 task files plus docs: `scripts/etc/argocd/applicationsets/observability.yaml` now forces the Helm release name for the hub Loki app back to `loki` while keeping the ArgoCD Application name `hub-loki`; `scripts/etc/helm/observability/kube-prometheus-stack-values.yaml` now points the hub Grafana Loki datasource at `http://loki-gateway.monitoring.svc.cluster.local`; `scripts/tests/plugins/argocd_loki.bats` now asserts both the forced hub release name and the corrected datasource URL; `docs/bugs/2026-06-29-image-updater-grafana-loki-service-name-mismatch.md` records the hub service-name mismatch root cause; and `docs/issues/2026-06-29-grafana-datasource-provisioning-needs-restart.md` records the live-validation quirk that datasource provisioning required a Grafana rollout restart before the API reflected the new URL. Validation passed with `bats scripts/tests/plugins/argocd_loki.bats scripts/tests/plugins/argocd_metrics_servicemonitor.bats` (`1..16`, all pass), `/private/tmp/k3d-manager-pyyaml/bin/python3` parsing `scripts/etc/argocd/applicationsets/observability.yaml` (`OK`), `shellcheck -S warning scripts/plugins/observability.sh` (clean), and `./scripts/k3d-manager _agent_audit` (exit 0). **LIVE DEPLOY PASS:** after reapplying hub `observability`, hard-refreshing `application/hub-loki` and `application/kube-prometheus-stack`, and restarting `deployment/kube-prometheus-stack-grafana`, hub services converged to `loki`, `loki-gateway`, `loki-canary`, etc.; Grafana `/api/datasources` now reports Loki URL `http://loki-gateway.monitoring.svc.cluster.local`; Grafana `/api/ds/query` for the logs panel `{namespace="cicd",pod=~"argocd-image-updater.*"} |= "Processing results:"` now returns recent `Processing results:` lines; and Grafana `/api/ds/query` for the Prometheus stat panel still returns replica value `1`. **Operator result:** the hub dashboard now has server-side data for both Prometheus and Loki panels. **PR URL:** none; per repo rules no PR was created.
- **COMPLETE + LIVE DEPLOYED (Codex, 2026-06-29) — Image Updater Grafana datasource bindings fixed on hub:** implemented on `k3d-manager-v1.12.0` as commit `4754b893` (`fix(grafana): pin image updater dashboard datasource uids`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 5 task files only: `scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml` now pins all Prometheus panels to datasource uid `prometheus`, pins the logs panel to datasource uid `loki`, and removes the unused datasource templating variables; `scripts/etc/helm/observability/kube-prometheus-stack-values.yaml` now provisions a hub Grafana Loki datasource (`name: Loki`, `uid: loki`, `url: http://hub-loki-gateway.monitoring.svc.cluster.local`); `scripts/tests/plugins/argocd_metrics_servicemonitor.bats` now asserts the fixed explicit datasource UIDs and empty templating list; `scripts/tests/plugins/argocd_loki.bats` now asserts the hub Grafana Loki datasource provisioning; and `docs/bugs/2026-06-29-image-updater-grafana-datasource-uids.md` records the unresolved-datasource-variable root cause. Validation passed with `shellcheck -S warning scripts/plugins/observability.sh` (clean), `bats scripts/tests/lib/observability.bats scripts/tests/plugins/argocd_loki.bats scripts/tests/plugins/argocd_metrics_servicemonitor.bats` (`1..20`, all pass), `/private/tmp/k3d-manager-pyyaml/bin/python3` parsing the dashboard YAML+JSON (`OK`), and `./scripts/k3d-manager _agent_audit` (exit 0). **LIVE DEPLOY PASS:** after pushing and hard-refreshing hub `application/kube-prometheus-stack`, Grafana `/api/datasources` on `k3d-k3d-cluster`/`monitoring` returned both `Prometheus` uid `prometheus` and `Loki` uid `loki`; Grafana `/api/dashboards/uid/argocd-image-updater` returned version `3` with explicit datasource bindings and URL `/d/argocd-image-updater/argocd-apps-and-image-updater`; the underlying Prometheus queries for `argocd-image-updater` replicas and watched-app ArgoCD metrics still returned live values. **PR URL:** none; per repo rules no PR was created.
- **COMPLETE + LIVE DEPLOYED (Codex, 2026-06-29) — Image Updater Grafana moved to hub observability and the stale app-cluster copy was removed:** implemented on `k3d-manager-v1.12.0` as commit `0aa8c443` (`fix(observability): move image updater grafana wiring to hub`), pushed to `origin/k3d-manager-v1.12.0`; follow-up cleanup commit pending memory-bank bookkeeping. Scope held to the observability task files: `scripts/etc/argocd/applicationsets/observability.yaml` now adds a distinct hub `hub-loki` app (same Loki chart/values, unique Application name to avoid colliding with the existing app-cluster `loki` app in `cicd`); `scripts/plugins/observability.sh` now makes `deploy_observability` apply the `ArgoCD Apps & Image Updater` dashboard ConfigMap and the `promtail.yaml` shipper explicitly to hub context `k3d-k3d-cluster`, and `deploy_observability_acg` now deletes any stale `configmap/grafana-dashboard-argocd` from the app cluster before continuing; `scripts/tests/lib/observability.bats` now asserts the hub deploy path and the ACG cleanup path; `scripts/tests/plugins/argocd_loki.bats` now covers the hub Loki app name and hub dashboard wiring; `docs/bugs/2026-06-29-image-updater-grafana-wrong-cluster.md` records the original wrong-cluster root cause; `docs/issues/2026-06-29-deploy-observability-safety-gate-after-function.md` records the separate dispatcher oddity where `./scripts/k3d-manager deploy_observability` completed the work but still exited via the generic safety gate; and `docs/issues/2026-06-29-deploy-observability-acg-default-context-bug.md` records the second live-validation oddity where the default `deploy_observability_acg` invocation forwarded `--help` as the context, so the explicit `ubuntu-hostinger` form was used as the workaround. Validation passed with `shellcheck -S warning scripts/plugins/observability.sh` (clean), `bash -n scripts/plugins/observability.sh`, `bats scripts/tests/lib/observability.bats scripts/tests/plugins/argocd_loki.bats scripts/tests/plugins/argocd_metrics_servicemonitor.bats` (`1..19`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **LIVE DEPLOY PASS:** hub Prometheus proves the queries are live (`kube_deployment_status_replicas_available{namespace="cicd",deployment="argocd-image-updater"} = 1`, `argocd_app_info` returns basket/order/product-catalog, and 30m sync increases are non-empty); rerunning `./scripts/k3d-manager deploy_observability` updated the hub `observability` ApplicationSet, re-applied `configmap/grafana-dashboard-argocd`, started hub `promtail` (`4/4 Running`), and reconciled `application/hub-loki` to `Synced/Healthy`; rerunning `./scripts/k3d-manager deploy_observability_acg ubuntu-hostinger` removed the stale app-cluster dashboard copy, and live readback now shows `kubectl --context ubuntu-hostinger -n monitoring get configmap grafana-dashboard-argocd` → `NotFound` while the hub still has `configmap/grafana-dashboard-argocd`. **Operator implication:** if a dashboard with this title still shows `No data`, it is almost certainly a stale browser tab or the wrong Grafana instance; the valid dashboard is the hub copy. **PR URL:** none; per repo rules no PR was created.
- **COMPLETE + LIVE DEPLOYED (Codex, 2026-06-29) — Grafana now shows Image Updater metrics and logs:** implemented on `k3d-manager-v1.12.0` as commit `44539a24` (`fix(observability): use container log paths for Fluent Bit Loki shipping`). Scope held to the observability files only: `scripts/etc/argocd/applicationsets/observability-acg.yaml` adds a new `loki` app (`grafana-community/helm-charts`, `loki` chart `18.2.0`), `scripts/etc/helm/observability/loki-values.yaml` pins Loki monolithic mode and disables the simple-scalable targets, `scripts/etc/observability/promtail.yaml` now runs Fluent Bit v5.0.8 against `/var/log/containers/*.log` and ships to `loki.monitoring.svc.cluster.local:3100`, `scripts/plugins/observability.sh` applies the shipper on ACG deploy, `scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml` adds a Loki log panel (`Image Updater Processing Results`) alongside the existing Image Updater metrics panels, `scripts/tests/plugins/argocd_loki.bats` covers the new Loki/shipper wiring, and `scripts/tests/plugins/argocd_metrics_servicemonitor.bats` now asserts the Loki panel variables/query. Validation passed with `shellcheck -S warning scripts/plugins/observability.sh` (clean), `bash -n scripts/plugins/observability.sh`, `bats scripts/tests/plugins/argocd_metrics_servicemonitor.bats scripts/tests/plugins/argocd_loki.bats` (`1..12`, all pass), and `/private/tmp/k3d-manager-pyyaml/bin/python3` parsing the new YAML files plus dashboard JSON (clean). **LIVE DEPLOY PASS:** `kubectl --context k3d-k3d-cluster -n cicd annotate application loki argocd.argoproj.io/refresh=hard --overwrite` reconciled the new Loki app to `Synced/Healthy`; `kubectl --context ubuntu-hostinger -n monitoring get pods -l app.kubernetes.io/name=loki` now shows `loki-0 2/2 Running`, `loki-canary` and `loki-gateway` are running, and the restarted Fluent Bit shipper pod is `1/1 Running` with logs showing `/var/log/containers/...` watchers instead of the earlier tag-pattern warnings. The dashboard ConfigMap on `ubuntu-hostinger` contains the Loki log panel and datasource selector, and Grafana sidecar provisioning remains active. **PR URL:** none; per repo rules no PR was created.
- **COMPLETE + LIVE DEPLOYED (Codex, 2026-06-29) — Grafana now surfaces Image Updater readiness + watched-app churn:** implemented on `k3d-manager-v1.12.0` as commit `8de03933` (`feat(grafana): surface image updater readiness and watched-app churn`). Scope held to the 2 dashboard files only: `scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml` now adds Prometheus-backed stat panels for `argocd-image-updater` ready/desired replicas plus watched-app sync/activity tables and a 30m churn panel filtered to `shopping-cart-(basket|order|product-catalog)`; `scripts/tests/plugins/argocd_metrics_servicemonitor.bats` now asserts the deployment-replica queries and watched-app filters. Validation passed with `shellcheck -S warning scripts/plugins/argocd.sh` (clean), dashboard YAML+JSON validation via `/private/tmp/k3d-manager-pyyaml/bin/python3`, `bats scripts/tests/plugins/argocd_metrics_servicemonitor.bats` (`1..7`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **LIVE DEPLOY PASS (2026-06-29):** `kubectl --context ubuntu-hostinger apply -f scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml` created `configmap/grafana-dashboard-argocd`; live readback confirmed dashboard title `ArgoCD Apps & Image Updater` with the new `argocd-image-updater` replica and watched-app queries; Grafana sidecar log on `deploy/acg-kube-prometheus-stack-grafana` (`grafana-sc-dashboard`) logged `Writing /tmp/dashboards/argocd-image-updater.json (ascii)` at `2026-06-29T17:04:48Z`. **Important limit:** this wires the Prometheus-observable parts into Grafana; the `Last cycle:` log summary from `make status` still requires a log pipeline and is not exposed by the current stack. **PR URL:** none; per repo rules no PR was created.
- **BUGFIX COMPLETE + LIVE VALIDATED (Codex, 2026-06-29) — Hostinger ArgoCD drift self-healed on refresh:** `docs/bugs/2026-06-29-argocd-expected-drift-hostinger-apps.md` is implemented on `k3d-manager-v1.12.0` as commit `46735708` (`fix(hostinger): reapply data appset on refresh and clear stale app ownership`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 5 task files only: `scripts/etc/argocd/applicationsets/data-git.yaml` now ignores `.spec.persistentVolumeClaimRetentionPolicy` on StatefulSets; `scripts/lib/providers/k3s-hostinger.sh` now reapplies the hub `data-git` ApplicationSet during Hostinger refresh and strips stale platform tracking-ids from the product-catalog resource set before hard-refreshing the affected apps; `scripts/tests/lib/provider_contract.bats` adds coverage for the data-appset reapply and stale-ownership cleanup; `scripts/tests/plugins/argocd_app_cluster_generator.bats` now asserts the new StatefulSet ignore; and `docs/bugs/2026-06-29-argocd-expected-drift-hostinger-apps.md` records the diagnosis/fix. Validation passed with `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh` (clean), `bats scripts/tests/lib/provider_contract.bats scripts/tests/plugins/argocd_app_cluster_generator.bats` (`1..48`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **LIVE VALIDATION PASS:** `make refresh CLUSTER_PROVIDER=k3s-hostinger` completed successfully; live `kubectl` confirmed the hub `data-git` ApplicationSet and `data-layer` Application now carry the expanded ignore block and `data-layer`, `shopping-cart-product-catalog`, `shopping-cart-namespace`, and `ubuntu-hostinger-platform` all settled to `Synced/Healthy`; final `make status CLUSTER_PROVIDER=k3s-hostinger` also showed `Prometheus: 1/1 available`, Grafana HTTP 200, and `Data layer: 4/4 ready`. **PR URL:** none; per repo rules no PR was created.
- **BUGFIX COMPLETE + LIVE VALIDATED (Codex, 2026-06-29) — Hostinger Grafana no-data observability drift self-healed on refresh + surfaced in `make status`:** `docs/bugs/2026-06-29-hostinger-grafana-no-data-observability-drift.md` is implemented on `k3d-manager-v1.12.0` as commit `b6d16480` (`fix(hostinger): reapply observability on refresh and surface Prometheus OOM`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 10 task files only: `scripts/lib/providers/k3s-hostinger.sh` now re-applies `deploy_observability_acg "${_HOSTINGER_KUBE_CONTEXT}"` during refresh immediately after cluster registration so stale live ApplicationSet branch pins self-heal back to the current branch; `scripts/plugins/observability.sh` now exports `APP_CLUSTER_NAME` and envsubsts it into `observability-acg`; `scripts/etc/argocd/applicationsets/observability-acg.yaml` now passes `prometheus.prometheusSpec.externalLabels.cluster={{.clusterLabel}}`; `scripts/etc/helm/observability/kube-prometheus-stack-acg-values.yaml` raises Prometheus memory limit from `512Mi` to `1Gi`; `bin/cluster-status` now adds an **App Observability** section that reports the app-cluster Prometheus availability and warns explicitly on `OOMKilled` / non-ready backends so blank Grafana panels are called out in `make status`; new `scripts/tests/bin/cluster_status_observability.bats` covers the status section; `scripts/tests/lib/observability.bats` now asserts `APP_CLUSTER_NAME` envsubst; and `scripts/tests/lib/provider_contract.bats` now covers the refresh re-apply path and fixes the existing stale-listener harness to clear after one pass. Validation passed with `bash -n scripts/lib/providers/k3s-hostinger.sh scripts/plugins/observability.sh bin/cluster-status`, `shellcheck -S warning` on those three files (clean), `bats scripts/tests/lib/provider_contract.bats scripts/tests/lib/observability.bats scripts/tests/bin/cluster_status_image_updater.bats scripts/tests/bin/cluster_status_observability.bats` (`1..49`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **LIVE VALIDATION PASS:** the same 2026-06-29 Hostinger refresh/status run showed `Prometheus: 1/1 available` in the new status section and final service health reported Grafana HTTP 200 / Prometheus HTTP 200. **PR URL:** none; per repo rules no PR was created.
- **PHASE 1 COMPLETE (Codex, 2026-06-28) — app-image CVE scan → auto-update via Image Updater:** `docs/plans/v1.12.0-app-image-cve-autoupdate.md` Phase 1 (k3d-manager only) is implemented on `k3d-manager-v1.12.0` as commit `0ed5aa27` (`feat(platform-ops): app-image CVE scan with Image Updater auto-bump + rebuild dispatch`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 4 Phase 1 files only: new `scripts/etc/argocd/platform-ops/app-cve-scan.sh` adds the hub-side app-image trivy scan + notify + GitHub REST `workflow_dispatch` rebuild path using `curl --config` token hardening; new `scripts/etc/argocd/platform-ops/app-cve-scan-cronjob.yaml` adds the self-contained `platform-ops` CronJob + SA with no extra RBAC; `scripts/plugins/argocd.sh` gained the 3 exact spec edits (new CronJob apply, ConfigMap now includes `app-cve-scan.sh`, new manual secret hint for `platform-ops-app-rebuild`); new `scripts/tests/plugins/app_cve_scan.bats` covers the 4 pure-logic cases. Validation passed with `shellcheck -S warning scripts/etc/argocd/platform-ops/app-cve-scan.sh` (clean), `sh -n` + `bash -n` on the new script, `bats scripts/tests/plugins/app_cve_scan.bats` (`1..4`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **NEXT = Phase 2 (spec written, see below); Phase 3 (app-repo `workflow_dispatch`) still deferred.** Builds on v1.6.0 ArgoCD CVE auto-upgrade + v1.6.1 stack-vuln-report.
- **PHASE 2 COMPLETE (Codex, 2026-06-28) — extend Image Updater to all shopping-cart apps:** `docs/plans/v1.12.0-phase2-image-updater-all-apps.md` is implemented on `k3d-manager-v1.12.0` as commit `61fa0a10` (`feat(argocd): extend Image Updater image-list to basket and order app images`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 2 spec files only: `scripts/etc/argocd/applicationsets/services-git.yaml` now generalizes the Image Updater annotations from product-catalog-only to basket+order+product-catalog using the single `app` alias, shared `or (eq .path.basename ...)` guard, `app=ghcr.io/wilddog64/{{.path.basename}}:latest`, `app.update-strategy: digest`, and `write-back-method: argocd`; new `scripts/tests/plugins/argocd_image_updater_annotations.bats` adds the 6 literal `grep -F` assertions from the spec. Validation passed with the YAML parse check via isolated PyYAML venv interpreter (`/private/tmp/k3d-manager-pyyaml/bin/python3 -c "import yaml; list(yaml.safe_load_all(open('scripts/etc/argocd/applicationsets/services-git.yaml')))"`), `bats scripts/tests/plugins/argocd_image_updater_annotations.bats` (`1..6`, all pass), `bats scripts/tests/plugins/argocd_app_cluster_generator.bats` (`1..4`, all pass), `bats scripts/tests/plugins/argocd.bats` (`1..15`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **Live OutOfSync-flap validation after merge remains Claude's job (kubectl), not Codex's DoD.**
- **BUGFIX COMPLETE (Codex, 2026-06-28) — Image Updater installed by bootstrap:** `docs/bugs/v1.12.0-bugfix-image-updater-not-bootstrapped.md` is implemented on `k3d-manager-v1.12.0` as commit `ceaa0157` (`fix(argocd): install Image Updater during bootstrap so it survives cluster rebuilds`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 2 spec files only: `scripts/plugins/argocd.sh` now adds private `_argocd_deploy_image_updater` (env-gated by `ARGOCD_SKIP_IMAGE_UPDATER`, non-fatal `apply -k`, rollout restart, `--no-exit` rollout status) immediately before `_argocd_deploy_applicationsets` and invokes it at the start of the `enable_bootstrap` block in `_argocd_configure_post_deploy`; new `scripts/tests/plugins/argocd_image_updater_install.bats` adds the 5 grep-on-source assertions from the spec. Validation passed with `shellcheck -S warning scripts/plugins/argocd.sh` (clean), `bash -n scripts/plugins/argocd.sh`, `bats scripts/tests/plugins/argocd_image_updater_install.bats` (`1..5`, all pass), `bats scripts/tests/plugins/argocd.bats` (`1..15`, all pass), `bats scripts/tests/plugins/argocd_app_cluster_generator.bats` (`1..4`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **Deferred open question unchanged (NOT in this bugfix):** image-updater reads `pullsecret:shopping-cart-apps/ghcr-pull-secret` but `shopping-cart-apps` ns does not exist on the hub — NOW SPECCED as the follow-up bugfix `docs/bugs/v1.12.0-bugfix-image-updater-ghcr-pull-secret-hub.md` (see entry above). **PR URL:** none; per repo rules no PR was created. See [[project_oci_argocd_platform_helm]].
- **BUGFIX COMPLETE + VERIFIED (Codex impl, Claude verify, 2026-06-28) — Image Updater ghcr-pull-secret resolvable on hub:** `docs/bugs/v1.12.0-bugfix-image-updater-ghcr-pull-secret-hub.md` is implemented on `k3d-manager-v1.12.0` as commit `c008e82f` (`fix(argocd): provision ghcr-pull-secret in cicd so Image Updater can auth to ghcr.io`), pushed to `origin/k3d-manager-v1.12.0`; memory-bank recorded in separate commit `cc02e4e1`. **Claude verification PASS:** SHA confirmed on origin; scope = exactly the 4 spec files; every diff block matches the spec verbatim (credentials → `pullsecret:cicd/ghcr-pull-secret`, rbac resource dropped, `_argocd_ensure_ghcr_pull_secret` inserted before `_argocd_deploy_image_updater` and called before `apply -k`, bare-`kubectl` create); `scripts/lib/system.sh` subtree untouched; all gates re-run by Claude — `shellcheck -S warning` (clean), `bash -n` (exit 0), PyYAML `safe_load_all` (valid), `bats argocd_image_updater_install.bats` (`1..10`), `bats argocd.bats` (`1..15`), `bats argocd_app_cluster_generator.bats` (`1..4`), `_agent_audit` (exit 0). Scope held to the 4 spec files only: `scripts/etc/argocd/image-updater/kustomization.yaml` now drops `pull-secret-rbac.yaml` from `resources` and repoints registry credentials to `pullsecret:cicd/ghcr-pull-secret`; `scripts/etc/argocd/image-updater/pull-secret-rbac.yaml` is deleted; `scripts/plugins/argocd.sh` now adds private `_argocd_ensure_ghcr_pull_secret` immediately before `_argocd_deploy_image_updater` and invokes it before `apply -k`; `scripts/tests/plugins/argocd_image_updater_install.bats` adds the 5 required assertions (10 total). Validation passed with `shellcheck -S warning scripts/plugins/argocd.sh` (clean), `bash -n scripts/plugins/argocd.sh`, YAML parse via `/private/tmp/k3d-manager-pyyaml/bin/python3` `yaml.safe_load_all(...)`, `bats scripts/tests/plugins/argocd_image_updater_install.bats` (`1..10`, all pass), `bats scripts/tests/plugins/argocd.bats` (`1..15`, all pass), `bats scripts/tests/plugins/argocd_app_cluster_generator.bats` (`1..4`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). SECURITY note preserved per spec: bare `kubectl` for the docker-registry secret create so `--docker-password` does not pass through the trace-aware wrapper; upstream sensitive-flag registration remains deferred/out of scope. **PR URL:** none; per repo rules no PR was created. **LIVE VALIDATION PASS (Claude kubectl, 2026-06-29) on hub `k3d-k3d-cluster` ns `cicd`:** pre-state confirmed clean (no `argocd-image-updater` deploy, no `ghcr-pull-secret`); the two fix functions were exercised against the live cluster via a lib-sourced driver with `ARGOCD_NAMESPACE=cicd` (dispatcher blocks `_`-private invocation by design). `_argocd_ensure_ghcr_pull_secret` resolved the PAT through the `gh auth token` fallback (env `GHCR_PAT` unset, Vault port unset), validated it against api.github.com, and created `ghcr-pull-secret` (type `kubernetes.io/dockerconfigjson`, registry `ghcr.io`, username `wilddog64`). `_argocd_deploy_image_updater` re-ensured the secret idempotently then `apply -k`'d the kustomize and the `argocd-image-updater` deploy rolled out `1/1` (pod `2/2` with istio sidecar). Controller logs confirm ghcr.io auth works with `pullsecret:cicd/ghcr-pull-secret`: it fetched the `wilddog64/shopping-cart-product-catalog` digest and committed the live spec update with **`Processing results: applications=6 images_considered=1 images_skipped=5 images_updated=1 errors=0`** — zero `level=error`, zero 401/403/unauthorized/denied; basket/order cleanly skipped (no newer tag), not erroring. See [[project_oci_argocd_platform_helm]].
- **BUGFIX COMPLETE (Codex, 2026-06-29) — ArgoCD metrics scraped via ServiceMonitor + starter Grafana dashboard shipped:** `docs/bugs/v1.12.0-bugfix-argocd-metrics-servicemonitor.md` is implemented on `k3d-manager-v1.12.0` as commit `60cd500c` (`fix(argocd): scrape ArgoCD metrics via ServiceMonitor and ship Grafana dashboard`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 4 spec files only: `scripts/etc/argocd/values.yaml.tmpl` now enables `metrics.serviceMonitor` with `additionalLabels.release: kube-prometheus-stack` for controller, server, repoServer, and applicationSet; `scripts/plugins/argocd.sh` now applies `grafana-dashboard-argocd.yaml` immediately after the PrometheusRule apply in the platform-ops deploy; new `scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml` adds the sidecar-discoverable starter ConfigMap dashboard in namespace `monitoring`; new `scripts/tests/plugins/argocd_metrics_servicemonitor.bats` adds the 5 grep-on-source assertions. Validation passed with `shellcheck -S warning scripts/plugins/argocd.sh` (clean), `bash -n scripts/plugins/argocd.sh`, dashboard YAML + embedded JSON validity via `/private/tmp/k3d-manager-pyyaml/bin/python3` (`yaml.safe_load` + `json.loads`), `bats scripts/tests/plugins/argocd_metrics_servicemonitor.bats` (`1..5`, all pass), `bats scripts/tests/plugins/argocd.bats` (`1..15`, all pass), `bats scripts/tests/plugins/argocd_image_updater_install.bats` (`1..10`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **PR URL:** none; per repo rules no PR was created. **CLAUDE LIVE VALIDATION PASS (2026-06-29):** surgical `helm upgrade argocd argo/argo-cd --version 9.5.15 -n cicd --reuse-values --set <component>.metrics.{enabled,serviceMonitor.enabled,serviceMonitor.additionalLabels.release=kube-prometheus-stack}` for all 4 components created `argocd-{application-controller,applicationset-controller,repo-server,server}-metrics` Services + matching ServiceMonitors; Prometheus discovered all 4 (`targets?state=active` → all `up`); `count(argocd_app_info)` EMPTY→**19**, `count(argocd_app_sync_total)` EMPTY→**23**; dashboard Panel-2 query `sum by (name) (increase(argocd_app_sync_total[5m]))` renders the flapping `shopping-cart-product-catalog` (~1.2/5m). Dashboard ConfigMap `grafana-dashboard-argocd` applied to `monitoring`. **FINDING — deploy path will break:** the helm upgrade is marked `failed` (revision 3) because Helm 4 server-side-apply conflicts on `argocd-cm` (`.data.oidc.config`, `.data.url`) and `argocd-rbac-cm` (`.data.policy.csv`, `.data.scopes`) — fields owned by `kubectl-patch`/`kubectl-client-side-apply` from the ESO/LDAP post-install patches; `--take-ownership` did NOT clear it. The metrics objects still applied (upgrade is non-atomic), so live scraping works, but `_argocd_helm_deploy_release`'s `helm upgrade --install --reset-values` will hit the same field-manager conflict once 60cd500c's values land on a previously-patched release. NOT forced (would risk clobbering live ArgoCD OIDC/RBAC). Release left in `failed` state on the dev cluster — needs a rollback-or-resolve decision. **CORRECTION (investigation 2026-06-29): NOT a normal-flow bug.** `deploy_argocd` runs the helm install at `bin/cluster-up` Step 3.6 — BEFORE the out-of-band SSO config at Step 10f (`bin/cluster-up` kubectl-applies `argocd-cm.yaml`/`argocd-rbac-cm.yaml` from shopping-cart-infra → owns `policy.csv`/`scopes`, and `kubectl patch`es the Cloudflare `url`+`oidc.config` → owns those). The refresh path calls `deploy_argocd_bootstrap`, which does NOT re-run helm; and `deploy_argocd` hardcodes `release_exists=0` → plain `helm upgrade --install` (the `--reset-values` branch is dead). So metrics values ship cleanly on fresh install; the SSA field conflict only fires on a MANUAL `helm upgrade`/`deploy_argocd` against an already-SSO-patched live release (the out-of-band repro). No risky helm code fix warranted — at most a defensive error-path hint + known-issue note. See [[project_oci_argocd_platform_helm]].
- **BUGFIX COMPLETE (Codex, 2026-06-29) — surface Image Updater in `make status` + document helm SSA conflict:** `docs/bugs/v1.12.0-bugfix-make-status-image-updater.md` is implemented on `k3d-manager-v1.12.0` as commit `0ef28178` (`fix(cluster-status): surface ArgoCD Image Updater in make status + doc helm SSA conflict`). Scope held to the 3 spec files only: `bin/cluster-status` now adds a read-only **ArgoCD Image Updater** section between ApplicationSets and AWS credentials showing deployment readiness, last `Processing results:` cycle summary, flapping heuristic (`images_updated=[1-9]` in >=3 recent cycles), and watched app `argocd-image-updater.argoproj.io/image-list` annotations via plain `kubectl`; new `scripts/tests/bin/cluster_status_image_updater.bats` adds the 5 grep-on-source assertions; new `docs/issues/2026-06-29-argocd-helm-ssa-conflict-sso-cm.md` records the manual helm SSA conflict as a known issue with root cause and remediation. The helm part remained **docs-only** as required; `scripts/plugins/argocd.sh` and the deploy path were not modified. Validation passed with `shellcheck -S warning bin/cluster-status` (clean), `bash -n bin/cluster-status` (exit 0), `bats scripts/tests/bin/cluster_status_image_updater.bats` (`1..1`, pass), `bats scripts/tests/bin/cluster_refresh.bats` (`1..1`, pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **PR URL:** none; per repo rules no PR was created. **CLAUDE VERIFICATION PASS (2026-06-29):** SHA `0ef28178` confirmed on `origin/k3d-manager-v1.12.0`; scope = exactly the 3 spec files (`git show --stat`); `scripts/plugins/argocd.sh`/deploy path untouched; code matches spec verbatim (only cosmetic `->`/`-` ASCII normalization in the issue doc vs spec unicode); all 5 gates re-run by Claude — shellcheck clean, `bash -n` exit 0, new bats `1..1`, `cluster_refresh.bats` `1..1`, `_agent_audit` exit 0; memory-bank recorded in separate commit `b76bca71`. **CLAUDE LIVE `make status` PASS (2026-06-29) on hub `k3d-k3d-cluster`:** the new `=== ArgoCD Image Updater ===` section rendered `Deployment: 1/1 ready`, `Last cycle: applications=6 images_considered=1 images_skipped=5 images_updated=0 errors=0`, `WARN Flapping: 28 recent cycles wrote an update`, and `Watching: shopping-cart-product-catalog -> catalog=ghcr.io/wilddog64/shopping-cart-product-catalog:latest`. The flapping warning is corroborated three ways in the same output: product-catalog shows `OutOfSync` in the Apps list, it is the only app carrying an image-list annotation, and the heuristic fired — confirming the non-convergence bug WITHOUT grepping pod logs. **OPEN:** failed `argocd` helm release (rev 3) on dev cluster still pending rollback-or-leave decision.
- **BUGFIX COMPLETE (Codex, 2026-06-29) — stop product-catalog Image Updater flapping:** `docs/bugs/v1.12.0-bugfix-image-updater-flapping-product-catalog.md` is implemented on `k3d-manager-v1.12.0` as commit `6f3d0440` (`fix(argocd): stop product-catalog image-updater flapping via jqPathExpressions + kustomize image anchor`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the 3 spec files only: `scripts/etc/argocd/applicationsets/services-git.yaml` now switches top-level `ignoreApplicationDifferences` from `jsonPointers: [/spec/source/kustomize/images]` to `jqPathExpressions: [.spec.source.kustomize.images]`; `services/shopping-cart-product-catalog/kustomization.yaml` now adds the local `images:` anchor (`ghcr.io/wilddog64/shopping-cart-product-catalog`, `newTag: latest`); and new `scripts/tests/plugins/argocd_image_updater_flap_fix.bats` adds the 2 grep-on-source tests. `scripts/plugins/argocd.sh`, the helm deploy path, the per-app `ignoreDifferences:` block, and the Image Updater annotations were intentionally untouched. Validation passed with YAML parsing via `/private/tmp/k3d-manager-pyyaml/bin/python3` over both edited files, `bats scripts/tests/plugins/argocd_image_updater_flap_fix.bats` (`1..2`, all pass), `bats scripts/tests/plugins/argocd_image_updater_annotations.bats` (`1..6`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **PR URL:** none; per repo rules no PR was created. **Live re-validation remains Claude's post-land job** because the live cluster still runs a stale `services-git` ApplicationSet and needs redeploy against a branch carrying this fix. See [[project_oci_argocd_platform_helm]].
- **GITGUARDIAN FALSE POSITIVE (Claude, 2026-06-28) — seed BATS test fixtures:** GitGuardian flagged a Generic Password in commit `b03cf289` (`scripts/tests/plugins/shopping_cart_seed_idempotent.bats`); confirmed via `ggshield secret scan` the matches are synthetic test fixtures (`kc-db`/`kc-admin`, `src-minio-pass`/`src-minio-user`, `root-token`) — no real secret (runtime secrets use `openssl rand`). Repo guard already in place: `.gitguardian.yaml` ignores `scripts/tests/` (committed since v1.11.0 `75ebc934`) — governs ggshield CLI/CI/pre-commit. ggshield 1.52 has NO incident-resolve subcommand (only `scan` + local `ignore`); the dashboard incident is server-side and needs UI "Resolve → False positive" or REST `POST /v1/incidents/secrets/{id}/resolve` (keyring key has `incidents:write`). Issue doc `docs/issues/2026-06-28-gitguardian-false-positive-seed-test-fixtures.md`. Durable prevention = dashboard path exclusion for `scripts/tests/`. See [[reference-ggshield-resolve]].
- **BUGFIX COMPLETE (Codex, 2026-06-28) — minio/ldap/keycloak source parity:** `docs/bugs/v1.12.0-bugfix-seed-source-parity-minio-ldap-keycloak.md` is implemented on `k3d-manager-v1.12.0` as commit `b03cf289` (`fix(shopping-cart): seed minio/ldap/keycloak from source Vault for parity`), pushed to `origin/k3d-manager-v1.12.0`. Scope held to the two spec files only: `scripts/plugins/shopping_cart.sh` now makes the 4 multi-field else-branches (`minio/credentials`, `ldap/admin`, `keycloak/admin` with the missing-field guard preserved, `keycloak/clients`) try `_seed_source_data` first, `_vault_kv_put` the full source object, and extract fields from `${_src_json}` before falling back to generated values; `scripts/tests/plugins/shopping_cart_seed_idempotent.bats` now adds the source-host minio mock, `TEST_MINIO_EXISTS` gate, `jq -c`/`jq -r` handlers, and the 5th test covering multi-field source copy. Validation passed with `shellcheck -S warning scripts/plugins/shopping_cart.sh` (clean), `bash -n scripts/plugins/shopping_cart.sh`, `bats scripts/tests/plugins/shopping_cart_seed_idempotent.bats` (`1..5`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). See [[project_app_cluster_vault_auth_portability]].
- **BUGFIX COMPLETE + VERIFIED (Codex impl, Claude verify, 2026-06-28) — seed-source wiring + token hardening:** `docs/bugs/v1.11.0-bugfix-seed-source-data-unused.md` is implemented on `k3d-manager-v1.12.0` as commit `176ec5a6` (`fix(shopping-cart): seed canonical secrets from source Vault + curl --config token hardening`), pushed to `origin/k3d-manager-v1.12.0`; memory-bank recorded in separate commit `dd83c4cd`. **Claude verification PASS:** SHA confirmed on origin; scope = exactly the 2 spec files; all 8 diff blocks match the spec; the one inline comment is from the spec (line 131), not scope creep; all four gates re-run by Claude — `shellcheck -S warning` (clean), `bash -n` (clean), `bats` (`1..4`, all pass), `_agent_audit` (exit 0). Scope: `scripts/plugins/shopping_cart.sh:shopping_cart_seed_sandbox_vault_kv` (4 KV helpers → `curl --config` mode-600 header files; `_seed_source_data` wired into the 6 single-password generate-if-absent branches) + `shopping_cart_seed_idempotent.bats` (source-host mock + copy-from-source test). minio/ldap/keycloak left out → now covered by the parity follow-up spec above. See [[project_app_cluster_vault_auth_portability]].

- **v1.11.0 RELEASED + POST-MERGE COMPLETE (Claude, 2026-06-28):** PR #101 MERGED to `main` (merge SHA `75ebc934`); tagged **`v1.11.0`** + GitHub release created (`--latest`); `enforce_admins` **restored** on `main` (re-enabled via empty POST — `-f enabled=true` is rejected 422); `main` synced locally; **next branch `k3d-manager-v1.12.0`** cut from the merge SHA; retro `docs/retro/2026-06-28-v1.11.0-retrospective.md` committed on the next branch. Copilot 7 findings: 4 fixed (token-in-argv ×2 via `curl --config` mode-600 header files `b2ed5b80`, unused `_vault_ns`, API index `5c9ace1c`), 3 deferred to `docs/bugs/v1.11.0-bugfix-seed-source-data-unused.md` (latent `_seed_source_data` wiring gap, not a regression). **OPEN FOLLOW-UP:** implement the deferred seed-source bug spec in a future cycle; scratchpad failover test harness cleanup. See [[project_app_cluster_vault_auth_portability]].
- **LIVE CHECKS PASS (Claude, 2026-06-28) — failover watchdog hardening `3b32375a`:** Both deferred live checks run and PASS. **Problem-1 (launchd PATH):** the watchdog is non-functional no more — `env -i HOME=$HOME PATH=/usr/bin:/bin:/usr/sbin:/sbin CLUSTER_PROVIDER=k3s-hostinger ./bin/k3dm-vault-failover ubuntu-hostinger` now logs `hub Vault healthy (profile=hostinger); no failover needed` (was `kube-context 'ubuntu-hostinger' not found`); reinstalled the LaunchAgent (`vault_install_failover_watchdog ubuntu-hostinger`) and `launchctl kickstart gui/<uid>/com.k3d-manager.vault-failover` under real launchd logged the same healthy line to `~/Library/Logs/k3dm-vault-failover.log` — kubectl found, no error. **Problem-2 (subshell re-seed):** re-ran the temp-kubeconfig flip harness (`kubeconfig-no-laptop.yaml` lacking `k3d-k3d-cluster`, temp state=laptop, real state file NOT used). The flip now degrades instead of aborting: `probe 1/2`+`2/2 failed` → `failing over laptop -> hostinger` → re-seed `ERROR: could not read source vault-root token` → **`WARN: re-seed reported issues … continuing`** (the formerly-dead `|| _warn` guard now fires) → reconcile step reached (`_hostinger_reconcile_vault_cluster_store not loaded` — harness artifact: provider lib not sourced in isolated dispatcher run; handled gracefully) → **`INFO: failover complete: HUB_VAULT_PROFILE=hostinger`**. Real persisted profile (`~/.local/share/k3d-manager/hub-vault-profile`) untouched = `hostinger`; both Vaults healthy (`secrets/vault-0` on both contexts). LaunchAgent left **loaded** (state=active, idle between 300s intervals) — now functional. **DECISION PENDING: leave the watchdog loaded permanently? + v1.11.0 release/PR prep (CHANGELOG + retro) + scratchpad harness cleanup.** See [[project_app_cluster_vault_auth_portability]].
- **CLAUDE VERIFICATION PASS (2026-06-28) — failover watchdog hardening `3b32375a`:** Independently verified Codex's work. SHA confirmed on `origin/k3d-manager-v1.11.0` (`git branch -r --contains`); commit message exact; only `bin/k3dm-vault-failover` + `scripts/plugins/vault.sh` touched; both diff blocks match the spec byte-for-byte (PATH export + 3-line comment after `set -euo pipefail`; sole `( … )` subshell wrap). Static gates re-run by Claude all green: `shellcheck -S warning` (clean), `bash -n` (both), `bats scripts/tests/plugins/vault_failover.bats` (6/6), `bats scripts/tests/etc/hub_vault_profile.bats` (7/7), `_agent_audit` exit 0. Memory-bank commit `6a141295` is separate (memory-bank-only) and accurate. No scope creep, no fabricated SHA. **NEXT = the deferred live checks (mine): reinstall watchdog (`vault_install_failover_watchdog ubuntu-hostinger`) + `launchctl kickstart` re-verify (must log `profile=hostinger`, no `kube-context not found`); then re-run the temp-kubeconfig flip harness (must reach reconcile + "failover complete: HUB_VAULT_PROFILE=hostinger"). Harness in scratchpad.** See [[project_app_cluster_vault_auth_portability]].
- **BUGFIX COMPLETE (Codex, 2026-06-28):** `docs/plans/v1.11.0-bugfix-failover-watchdog-hardening.md` is implemented on `k3d-manager-v1.11.0` as commit `3b32375a` (`fix(vault): harden failover watchdog — launchd PATH + subshell-isolated re-seed`), pushed to `origin/k3d-manager-v1.11.0`. Scope held to the two spec files only: `bin/k3dm-vault-failover` now prepends `/opt/homebrew/bin:/usr/local/bin` to PATH with the spec’s 3-line launchd comment so Homebrew `kubectl`/`helm`/`jq` are discoverable under launchd’s minimal PATH, and `scripts/plugins/vault.sh` now runs `vault_seed_hub_into_context` in a subshell so a `laptop -> hostinger` failover degrades to warn-and-continue instead of aborting before the CSS reconcile when the laptop source is unreachable. Validation was limited to the exact requested commands and passed: `shellcheck -S warning bin/k3dm-vault-failover scripts/plugins/vault.sh` (clean), `bash -n bin/k3dm-vault-failover && bash -n scripts/plugins/vault.sh`, `bats scripts/tests/plugins/vault_failover.bats` (`1..6`, all pass), `bats scripts/tests/etc/hub_vault_profile.bats` (`1..7`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **Live-only follow-up remains deferred to Claude per user instruction:** do not run the Problem-1 `env -i` probe or the temp-kubeconfig flip here; next live verify should reinstall watchdog + kickstart re-verify (`profile=hostinger`, no error) and re-run the temp-kubeconfig flip (must reach reconcile + "failover complete"). PR URL: none (per repo rules). See [[project_app_cluster_vault_auth_portability]].
- **P4 LIVE-VERIFY ROUND 3 — 3rd BUG FOUND (failover aborts at re-seed) → FIX SPEC WRITTEN (Claude, 2026-06-28):** Item 2 (flip path) exercised SAFELY via a temp kubeconfig with the `k3d-k3d-cluster` context removed (laptop probe fails at the context-existence check; no Vault pod touched — laptop vault uses **shamir** seal so scaling it would leave it sealed) + a temp state file = `laptop`. The flip fired correctly (`probe 1/2`, `2/2` failed → `failing over laptop -> hostinger` → temp state file persisted `hostinger` → re-seed invoked), but then **aborted**: `ERROR: [vault] could not read source vault-root token from k3d-k3d-cluster/secrets` and the process EXITED — no reconcile, no "failover complete". **Root cause:** `_err` (`scripts/lib/system.sh:1717`) does `exit 1`; during a real `laptop→hostinger` failover the laptop source is unreachable (that's WHY we fail over), so `vault_seed_hub_into_context` calls `_err` → process dies. The `|| _warn "...continuing"` guard at vault.sh:1211-1212 is **dead code** (exit kills the shell before `||`). So the new profile persists to the state file (written before re-seed) but the **CSS reconcile never runs** — the ClusterSecretStore is left pointing at the dead Vault. Fix = wrap the re-seed call in a subshell `( … )` so `_err`'s exit is contained and the `|| _warn` degrades to best-effort + continues to reconcile; `vault_seed_hub_into_context` itself unchanged (P3 path relies on its exit). Spec: `docs/plans/v1.11.0-bugfix-failover-reseed-abort.md` (commit msg `fix(vault): isolate failover re-seed in subshell so it degrades instead of aborting`). **No live damage** (CSS was already in-cluster; 17/17 ES `SecretSynced`; real state file still `hostinger`). Test harness kept in scratchpad for re-verify after the fix lands. **NEXT = hand BOTH pending specs to Codex (launchd-PATH + reseed-abort); after each lands, verify + re-run the temp-kubeconfig flip (must reach reconcile + "failover complete") and the watchdog kickstart (must report profile=hostinger).** See [[project_app_cluster_vault_auth_portability]].
- **P4 LIVE-VERIFY ROUND 2 — 2nd BUG FOUND (watchdog non-functional under launchd) → FIX SPEC WRITTEN (Claude, 2026-06-28):** With the active-profile fix `57a5104f` verified PASS (live `--probe-only` now reports `profile=hostinger`), proceeded to item 1 (LaunchAgent) + item 2 (flip path). Installed the watchdog (`vault_install_failover_watchdog ubuntu-hostinger`) — render/bootstrap/load all fine — then `launchctl kickstart` forced a run that logged `ERROR: [vault] kube-context 'ubuntu-hostinger' not found in kubeconfig`. **Root cause:** launchd's minimal PATH (`/usr/bin:/bin:/usr/sbin:/sbin`) does not include `/opt/homebrew/bin`, so `_kubectl` finds no `kubectl` and the context check fails — **the LaunchAgent is non-functional as installed**. Proved it: same minimal-PATH env via `env -i` reproduces the error; adding `/opt/homebrew/bin:/usr/local/bin` to PATH yields `hub Vault healthy (profile=hostinger)`. Fix = prepend Homebrew prefixes to PATH in the wrapper `bin/k3dm-vault-failover` (single source of truth; helps manual runs too). Spec written: `docs/plans/v1.11.0-bugfix-failover-watchdog-launchd-path.md`. Booted out the broken agent (will reinstall after the fix lands). **Item 2 (flip path) BLOCKED on a safe trigger:** both Vaults are currently healthy (laptop `k3d-k3d-cluster` vault-0 up 34d; in-cluster `ubuntu-hostinger` vault-0 up 4h36m), so a real `laptop→hostinger` flip cannot fire naturally; needs a user decision on how/whether to induce it. **NEXT = hand the launchd-PATH spec to Codex; after fix: reinstall watchdog + kickstart re-verify (`profile=hostinger`, no error); then decide item 2 trigger + whether to leave the agent loaded.** See [[project_app_cluster_vault_auth_portability]].
- **BUGFIX COMPLETE (Codex, 2026-06-28):** `docs/plans/v1.11.0-bugfix-failover-active-profile-resolution.md` is implemented on `k3d-manager-v1.11.0` as commit `57a5104f` (`fix(vault): source vars.sh before reading active profile in failover watchdog`), pushed to `origin/k3d-manager-v1.11.0`. Scope held to `scripts/plugins/vault.sh` only: `vault_failover_hub_into_context` now sources `scripts/etc/vault/vars.sh` immediately after kube-context validation and before reading `HUB_VAULT_PROFILE`, so the persisted active profile from `HUB_VAULT_PROFILE_STATE_FILE` is honored in the watchdog dispatch path while explicit env still wins and the later `vars.sh` source remains unchanged. Validation passed with `shellcheck -S warning scripts/plugins/vault.sh` (clean), `bash -n scripts/plugins/vault.sh`, `bats scripts/tests/plugins/vault_failover.bats` (`1..6`, all pass), `bats scripts/tests/etc/hub_vault_profile.bats` (`1..7`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **NEXT = re-run the safe live probe-only check on `ubuntu-hostinger` and confirm it now reports `profile=hostinger`, then decide on installing the LaunchAgent.** PR URL: none (per repo rules). See [[project_app_cluster_vault_auth_portability]].
- **BUGFIX COMPLETE (Codex, 2026-06-28):** `docs/plans/v1.11.0-bugfix-vault-app-auth-role-audience.md` is implemented on `k3d-manager-v1.11.0` as commit `21ef3b8b` (`fix(vault): bind audience on app-cluster eso-app-cluster k8s-auth role`), pushed to `origin/k3d-manager-v1.11.0`. Scope held to `scripts/plugins/vault.sh` only: `configure_vault_app_auth` now defaults `audience` to `${APP_K8S_TOKEN_AUDIENCE:-https://kubernetes.default.svc.cluster.local}`, `%q`-quotes it, and writes it onto the `auth/kubernetes-app/role/eso-app-cluster` role so the app-cluster CSS remains compatible with Vault v1.21+ while preserving the existing role write behavior. Validation passed with `shellcheck -S warning scripts/plugins/vault.sh` (clean), `bash -n scripts/plugins/vault.sh`, and `./scripts/k3d-manager _agent_audit` (exit 0). **Live validation already matched this code path:** the `external-secrets` SA token on `ubuntu-hostinger` carries `aud: ["https://kubernetes.default.svc.cluster.local","k3s"]`, the running role accepted `audience=https://kubernetes.default.svc.cluster.local` with no warning, CSS re-auth stayed `Ready=True/Valid`, and all 17 ES re-synced via k8s-auth. **NEXT = install/exercise the P4 assisted-failover watchdog, then v1.11.0 release prep.** PR URL: none (per repo rules). See [[project_app_cluster_vault_auth_portability]].
- **LIVE CUTOVER ATTEMPT #3 — SUCCEEDED, IN-CLUSTER HOSTINGER HUB-VAULT NOW LIVE (Claude, 2026-06-28):** Verified the context-targeting fix `8ae113c4` (PASS: on origin, only `vault.sh` +15, diff byte-for-byte to spec, shellcheck/`bash -n`/`_agent_audit` all clean; memory-bank SHA in separate commit `53f9c05e`). **Pre-flip baseline was NOT healthy** — the laptop bridge was down again (no k3dm LaunchAgents loaded; `com.k3d-manager.ssh-tunnel` `disabled`; `vault-port-forward.plist` MISSING; :18200 & host:8200 both 000; CSS `InvalidProviderConfig`, 16/17 `SecretSyncedError`) = the recurring tunnel-flap fragility (cf. `856940f3`). **Per user decision (AskUserQuestion), restored the bridge FIRST to re-establish a clean rollback reference:** bottom-up manual runbook — (1) rendered `vault-port-forward.plist` from template + `bootstrap gui/$(id -u)` → :18200=200; (2) `launchctl enable gui/$(id -u)/com.k3d-manager.ssh-tunnel` FIRST then bootstrap → autossh `-R 8200:127.0.0.1:18200` up; (3) annotate-nudge CSS → `Ready=True/Valid`; (4) force-sync all 17 ES → **17/17 SecretSynced**. **Then THE FLIP (Step 3):** wrote profile state file `hostinger` + `CLUSTER_PROVIDER=k3s-hostinger ./scripts/k3d-manager refresh_cluster`. Log confirmed the fix firing: `[vault] in-cluster profile: configuring app-cluster auth on 'ubuntu-hostinger' Vault` → `Enabled kubernetes auth at kubernetes-app/` + `Data written to auth/kubernetes-app/config` + `Uploaded policy app-cluster-reader` (one non-fatal WARNING: `Role eso-app-cluster does not have an audience` — Vault v1.21+ will require an audience; 1.20.1 accepts it). **POST-FLIP VERIFY = FULL PASS:** in-cluster vault-0 now has `kubernetes-app/` auth (accessor `auth_kubernetes_0c8117bc`) + `app-cluster-reader` policy + role `eso-app-cluster` (bound SA `external-secrets`/ns `secrets`, policy `app-cluster-reader`, ttl 1h) — all on the IN-CLUSTER Vault, not the laptop; CSS `server=http://vault.secrets.svc:8200`, k8s auth `kubernetes-app`/`eso-app-cluster`, `Ready=True/Valid`; **17/17 ExternalSecrets `SecretSynced` via kubernetes auth**; all 12 shopping-cart pods Running. **SPEC WATCH-ITEM RESOLVED:** ESO k8s-auth IS load-bearing and works against the in-cluster CSS — NO token-auth fallback needed. The fragile laptop reverse-tunnel+socat bridge is now retired for the `ubuntu-hostinger` app cluster (reconcile logged `HUB_VAULT_PROFILE=hostinger: using in-cluster Vault, skipping reverse tunnel + socat bridge`). **OPEN FOLLOW-UPS:** (a) add `audience` to the `eso-app-cluster` role before any Vault v1.21 upgrade; (b) install/exercise the P4 assisted-failover watchdog now that profile=hostinger; (c) v1.11.0 release/PR prep (CHANGELOG + retro). See [[project_app_cluster_vault_auth_portability]].
- **MEMORY-BANK COMPACTED (Claude, 2026-06-27):** `activeContext.md` 630→14 lines, `progress.md` 593→135 lines; prior-milestone detail moved to `memory-bank/archive/activeContext-v1.6.x-v1.7.1.md` + `progress-v1.6.x-v1.6.4.md` (earlier window already in the `*-v1.4.2-v1.4.8.md` archives).
- **v1.11.0 SCOPE = Tier 3 P3 (seeding) + P4 (failover)** (deferred from v1.10.0 per max-5-plan). Branch `k3d-manager-v1.11.0` (renamed from the drifted `feat/…`; milestone branches are ALWAYS `k3d-manager-vX.Y.Z` — see [[feedback_k3d_manager_branch_naming]]).
- **P3 COMPLETE (Codex, 2026-06-27):** `docs/plans/v1.11.0-hub-vault-canonical-seeding.md` is implemented on `k3d-manager-v1.11.0` as commit `1722e07a` (`feat(vault): idempotent canonical-source seeding + vault_seed_hub_into_context (Tier 3 P3)`), pushed to `origin/k3d-manager-v1.11.0`. `scripts/plugins/shopping_cart.sh` now adds the `SEED_VAULT_*`/backup defaults, retargets the inner seed helpers, adds `_seed_source_data`, and makes `redis/cart`, `redis/orders-cache`, and `rabbitmq/default` reuse-if-exists; `scripts/plugins/vault.sh` adds public operator `vault_seed_hub_into_context <app-ctx>` to mirror the 13 canonical keys laptop→in-cluster Vault + keyring + native k8s backup Secret; new BATS cover idempotent seed behavior and the operator guards/key list. Validation passed with `shellcheck -S warning scripts/plugins/shopping_cart.sh scripts/plugins/vault.sh`, `bash -n scripts/plugins/shopping_cart.sh scripts/plugins/vault.sh`, `bats scripts/tests/plugins/vault_app_auth.bats scripts/tests/plugins/shopping_cart_seed_idempotent.bats scripts/tests/plugins/vault_seed_hub.bats` (`1..17`, all pass), `./scripts/k3d-manager test all` (`1..404`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). Live verification gap: reachable `ubuntu-hostinger` did **not** have P2b-provisioned in-cluster Vault objects (`svc/vault` + `secret/vault-root` were `NotFound`), so Change 4 keeps the spec’s assumed names; recorded in `docs/issues/2026-06-27-v1-11-0-p3-live-verification-gap.md`. Additional follow-ups recorded: stale `vault_app_auth.bats` policy assertion fixed in `docs/issues/2026-06-27-vault-app-auth-test-stale-policy-assertion.md`; exact spec blocks exceeded the if-count budget so `shopping_cart_seed_sandbox_vault_kv` + `vault_seed_hub_into_context` were temporarily allowlisted with follow-up issue `docs/issues/2026-06-27-v1-11-0-p3-if-count-allowlist.md`. **NEXT HANDOFF = P4** on `k3d-manager-v1.11.0`. See [[project_app_cluster_vault_auth_portability]].
- **P4 COMPLETE (Codex, 2026-06-27):** `docs/plans/v1.11.0-hub-vault-assisted-failover.md` is implemented on `k3d-manager-v1.11.0` as commit `34651206` (`feat(vault): assisted-failover watchdog for hub-vault profile flip + reconcile (Tier 3 P4)`), pushed to `origin/k3d-manager-v1.11.0`. `scripts/etc/vault/vars.sh` now reads a persisted `HUB_VAULT_PROFILE` state file while preserving the `laptop` default when unset/invalid; `scripts/plugins/vault.sh` adds `_vault_hub_fallback_profile`, `_vault_health_status_ok`, `_vault_probe_hub_endpoint`, `vault_failover_hub_into_context`, and `vault_install_failover_watchdog`; new `scripts/etc/launchd/com.k3d-manager.vault-failover.plist.tmpl` and executable `bin/k3dm-vault-failover` install the periodic laptop-side watchdog; `scripts/tests/etc/hub_vault_profile.bats` is now hermetic against the persisted state file and covers state-file precedence; new `scripts/tests/plugins/vault_failover.bats` covers the pure logic and arg guards; `scripts/etc/agent/if-count-allowlist` now includes `vault_failover_hub_into_context`. Validation passed with `shellcheck -S warning scripts/etc/vault/vars.sh scripts/plugins/vault.sh bin/k3dm-vault-failover`, `bash -n scripts/etc/vault/vars.sh scripts/plugins/vault.sh bin/k3dm-vault-failover`, `bats scripts/tests/etc/hub_vault_profile.bats scripts/tests/plugins/vault_failover.bats` (`1..13`, all pass), `./scripts/k3d-manager test all` (`1..410`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). Live verification gap remains recorded in `docs/issues/2026-06-27-v1-11-0-p4-live-verification-gap.md`: the probe→flip→re-seed→reconcile path and LaunchAgent install were not exercised live, and `ubuntu-hostinger` still lacks the P2b-provisioned in-cluster Vault objects. **Tier 3 is now complete; NEXT HANDOFF = v1.11.0 follow-ups / next milestone planning.** See [[project_app_cluster_vault_auth_portability]].
- **BUGFIX COMPLETE (Codex, 2026-06-28):** `docs/plans/v1.11.0-bugfix-launchd-enable-before-bootstrap.md` is implemented on `k3d-manager-v1.11.0` as commit `47ce2caa` (`fix(k3s-hostinger): launchctl enable before bootstrap so refresh heals disabled agents`), pushed to `origin/k3d-manager-v1.11.0`. Scope held to `scripts/lib/providers/k3s-hostinger.sh` only: `_hostinger_restart_launchd` now runs `launchctl enable "system/${label}"` before the system `bootstrap` path and `launchctl enable "gui/$(id -u)/${label}"` before the user `bootstrap` path, both as best-effort no-op guards so `make refresh` can recover previously disabled launchd services instead of failing with bootstrap error 5. Validation passed with `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh` (clean), `bash -n scripts/lib/providers/k3s-hostinger.sh`, and `./scripts/k3d-manager _agent_audit` (exit 0). **NEXT = live Hostinger hub-Vault cutover/runbook execution (existing P2b/P3 code, user-go required), then install/exercise the P4 watchdog after the profile flips to `hostinger`.** See [[project_app_cluster_vault_auth_portability]].
- **BUGFIX COMPLETE (Codex, 2026-06-28):** `docs/plans/v1.11.0-bugfix-deploy-eso-skip-externally-managed.md` is implemented on `k3d-manager-v1.11.0` as commit `7c6fde3e` (`fix(eso): skip Helm install when ESO is externally managed (ArgoCD-owned)`), pushed to `origin/k3d-manager-v1.11.0`. Landed scope = `scripts/plugins/eso.sh` plus `scripts/tests/plugins/eso.bats`: `deploy_eso` now treats `deploy/external-secrets` as an installed-and-ready signal when the Helm release is absent but the ESO Deployment exists and all required CRDs are already present, so ArgoCD-managed ESO skips the bundled Helm install instead of colliding on object ownership; the BATS fixture now models deployment absence by default and adds a focused externally-managed coverage case so the new branch is exercised without breaking fresh-install expectations. Validation passed with `shellcheck -S warning scripts/plugins/eso.sh` (clean), `bash -n scripts/plugins/eso.sh`, `bats scripts/tests/plugins/eso.bats` (`1..5`, all pass), `./scripts/k3d-manager test all` (`1..411`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **NEXT = retry live Hostinger hub-Vault cutover from Step 1 (`vault_deploy_hub_into_context ubuntu-hostinger`).** See [[project_app_cluster_vault_auth_portability]].
- **BUGFIX COMPLETE (Codex, 2026-06-28):** `docs/plans/v1.11.0-bugfix-vault-app-auth-incluster-context.md` is implemented on `k3d-manager-v1.11.0` as commit `8ae113c4` (`fix(vault): target in-cluster Vault context for app-cluster auth in hostinger profile`), pushed to `origin/k3d-manager-v1.11.0`. Scope held to `scripts/plugins/vault.sh` only: `configure_vault_app_auth_for_context` now save/restores kube-context and switches to `app_context` for the inner `configure_vault_app_auth` call when `HUB_VAULT_USE_BRIDGE=0`, so the `hostinger`/in-cluster profile writes `kubernetes-app` auth objects to the in-cluster Vault instead of the laptop hub Vault while preserving byte-for-byte behavior in the default `laptop` profile. Validation passed with `shellcheck -S warning scripts/plugins/vault.sh` (clean), `bash -n scripts/plugins/vault.sh`, and `./scripts/k3d-manager _agent_audit` (exit 0). **NEXT = retry live Hostinger hub-Vault cutover from Step 3 (profile flip + reconcile) and verify ESO k8s-auth actually succeeds.** See [[project_app_cluster_vault_auth_portability]].
- **LIVE CUTOVER ATTEMPT #1 BLOCKED + BUGFIX SPEC (Claude, 2026-06-28):** Started the live Hostinger hub-Vault cutover (user go given). Baseline captured: profile state file ABSENT (=`laptop`); laptop `vault-0` Running 1/1 (34d); Hostinger had NO in-cluster Vault (`svc/vault`,`secret/vault-root` NotFound); ESO CSS `vault-backend` Valid; **17/17 ExternalSecrets `SecretSynced` (rollback reference).** **Step 1 `vault_deploy_hub_into_context ubuntu-hostinger` FAILED** — `deploy_vault`→`deploy_eso` ran `helm upgrade --install external-secrets` which collided with Hostinger's **ArgoCD-managed** ESO (`argocd.argoproj.io/tracking-id: ubuntu-hostinger-eso:apps/Deployment:secrets/external-secrets`): `ServiceAccount external-secrets-cert-controller ... cannot be imported ... missing key meta.helm.sh/release-name`. **Failure was clean** — NO partial Vault state (no sts/pods/helm release; only the 3d `vault-bridge` socat svc), ESO still 17/17, laptop hub untouched, profile still `laptop`. **Root cause:** `deploy_eso` (eso.sh:139-147) gates "already installed" only on `helm status`; an externally-managed (ArgoCD) ESO has no Helm release so it wrongly attempts a fresh install. **BUGFIX SPEC WRITTEN:** `docs/plans/v1.11.0-bugfix-deploy-eso-skip-externally-managed.md` — add a Deployment-present branch (no helm release + deploy exists + CRDs present → skip install); commit msg `fix(eso): skip Helm install when ESO is externally managed (ArgoCD-owned)`. **NEXT = hand this bugfix to Codex; after it lands, RETRY the live cutover from Step 1.** See [[project_app_cluster_vault_auth_portability]].
- **LIVE CUTOVER ATTEMPT #2 — Steps 1+2 OK, FLIP BROKE ESO, ROLLED BACK CLEAN (Claude, 2026-06-28):** After the `deploy_eso` externally-managed fix (`7c6fde3e`), retried the live cutover. **Step 1 `vault_deploy_hub_into_context ubuntu-hostinger` SUCCEEDED** — in-cluster Vault HA live on Hostinger (helm release `vault` chart 0.30.1 / Vault 1.20.1, `vault-0` 1/1, `svc/vault` 8200/8201, `secret/vault-root`, Initialized+Unsealed, raft). **Step 2 `vault_seed_hub_into_context ubuntu-hostinger` SUCCEEDED** — all 13 canonical keys present (`secret/{postgres/{orders,payment,products},redis/{cart,orders-cache},minio/credentials,payment/{encryption,paypal,stripe},rabbitmq/default,ldap/*,keycloak/*}`); verified coverage maps to all 17 ES consumers. **Steps 3+4 FLIP (`HUB_VAULT_PROFILE=hostinger` state file + `CLUSTER_PROVIDER=k3s-hostinger make refresh`) BROKE ESO** — CSS correctly repointed to `http://vault.secrets.svc:8200` + kubernetes auth (mount `kubernetes-app`, role `eso-app-cluster`, SA `external-secrets/secrets`) but went `Ready=False InvalidProviderConfig: unable to create client`; all 17 ES → `SecretSyncedError`. **ROOT CAUSE:** `configure_vault_app_auth` (vault.sh:1622) issues its Vault writes via bare `_vault_exec`/`_kubectl` = the CURRENT kube-context, which during refresh was `k3d-k3d-cluster` (the laptop) since kubeconfig-merge preserves current-context. So `kubernetes-app`+`eso-app-cluster`+`app-cluster-reader` got (re)written on the LAPTOP hub vault (which already had `kubernetes-app` → "path already in use", then false "configured successfully"), while the Hostinger in-cluster vault was left with only `kubernetes/`+`token/` auth and NO `app-cluster-reader` policy (verified live: `vault auth list` + `vault policy list` on hostinger vault-0). Laptop-profile behavior is CORRECT (hub vault is on laptop); only the in-cluster/hostinger profile mis-targets. **ROLLBACK (profile→`laptop` + reconcile) SUCCEEDED** — CSS back to `vault-bridge.secrets.svc:8201` + token auth, Ready=True; ESO recovered to **17/17 SecretSynced+Ready** (3 lagging `*-admin` ES needed a force-sync annotation). In-cluster Hostinger Vault remains provisioned+seeded but idle for a fixed retry. profile state file = `laptop`; laptop hub untouched throughout. **BUGFIX SPEC WRITTEN:** `docs/plans/v1.11.0-bugfix-vault-app-auth-incluster-context.md` — `configure_vault_app_auth_for_context` switches to the app-cluster context (save/restore, like `vault_deploy_hub_into_context`) before `configure_vault_app_auth` when `HUB_VAULT_USE_BRIDGE=0`; commit msg `fix(vault): target in-cluster Vault context for app-cluster auth in hostinger profile`. Spec flags a watch-item: k8s-auth JWT path for an in-cluster CSS has never been load-bearing (laptop CSS uses token auth), so the live retry must confirm ESO can actually authenticate; token-auth fallback is a user decision if it can't. **NEXT = hand to Codex → verify → retry cutover from Step 3.** See [[project_app_cluster_vault_auth_portability]].
- **v1.11.0 carried-over follow-ups:** v1.7.1 CHANGELOG section never written; `_sts_valid` undefined at foundation `acg-credential-test:277` + dead `scripts/lib/acg/*` glob in top-level `agent_rigor.sh` (both upstream-first via lib-foundation); lib-acg absorption Phase 3 = archive repo; 1 moderate Dependabot advisory on default branch.

- **PR #100 MERGED 2026-06-27 (91578a0b) (Claude, 2026-06-27):** https://github.com/wilddog64/k3d-manager/pull/100 (`feat/v1.10.0-vault-auth-portable` → `main`, `feat(vault): Tier 3 hub-vault relocation + provider-agnostic auth`). Covers v1.10.0: provider-agnostic vault auth (`179251a3`), Tier 3 P1 seam (`22323b18`), P2a watchdog (`584202b9`), P2b provision/CSS cutover (`3323aa39`), data-layer drift fix (`e5813dbb`), hostinger LaunchAgent fix (`a8595439`); CHANGELOG `[Unreleased]` entry added (`82953b35`). **CI green; Copilot reviewed = 3 findings, ALL fixed in `1fd01e24`** (vault.sh: watchdog `kubectl apply` rc now `_err`-checked; `configure_vault_app_auth_for_context` resolves cluster name via `.contexts[]` before `.clusters[]` lookup; CA decode falls back to `base64 -D` for macOS/BSD). Findings doc `docs/issues/2026-06-27-copilot-pr100-review-findings.md` + README issue-logs row (`b985cc9c`). Re-validated: shellcheck clean, `_agent_audit` rc=0, `vault_app_auth`/`hub_vault_profile`/`vault_unseal_watchdog` BATS all pass. All 3 Copilot threads replied + resolved (verified via GraphQL); latest CI run success; **`enforce_admins restored on main post-merge). Default `HUB_VAULT_PROFILE=laptop` preserved (cutover P3-gated, v1.11.0). 1 pre-existing moderate Dependabot advisory on default branch (unrelated).
- **CLAUDE VERIFY PASS + P2b COMPLETE (Claude/Codex, 2026-06-27):** Independently verified BOTH landed commits on `origin/feat/v1.10.0-vault-auth-portable`. **data-layer fix `e5813dbb` = PASS** — SHA on origin, exact commit msg, scope = only the 2 spec files (`--stat`: data-git.yaml +3, the bats +9), diff matches the 3 `jqPathExpressions` byte-for-byte, `bats argocd_app_cluster_generator.bats` `1..4` all pass, `_agent_audit` exit 0, YAML valid (PyYAML IS installed here — Codex's `ModuleNotFoundError` was his env only). **P2a watchdog `584202b9` = PASS** — SHA on origin, exact commit msg, scope = only the 4 spec files, vars.sh + vault.sh diffs verbatim to spec, envsubst whitelist correct (inline `$rc`/`$SHARD_FILE` survived un-expanded), `bats vault_unseal_watchdog.bats` `1..5` all pass, `shellcheck -S warning` clean, `_agent_audit` exit 0, both memory-bank files carry the SHA. **P2b `3323aa39` = CLAUDE VERIFY PASS (2026-06-27):** independently confirmed — SHA on origin, exact commit msg, scope = ONLY the 5 spec files (`--stat`: vars.sh +2, vault.sh +55, shopping_cart.sh +53/-16, hub_vault_profile.bats +15, new shopping_cart_css_auth_block.bats +39), all 6 changes match the spec byte-for-byte (vars.sh both `HUB_VAULT_CSS_AUTH` defaults, vault.sh `HUB_VAULT_INCLUSTER` gate + `vault_deploy_hub_into_context` SC2064-clean trap + `app-cluster-reader` policy with exactly the 7 read-data/read,list-metadata prefixes + role flipped `eso-reader`→`app-cluster-reader`, shopping_cart.sh `_shopping_cart_css_auth_block` + token-gated root-token read + `${_css_auth_block}` injected at correct 8-space indent), `bats` `1..5`+`1..4` all pass, `shellcheck -S warning` clean, `_agent_audit` exit 0. Least-privilege held (NO `secret/data/*` wildcard); `eso-reader` untouched; default `laptop` stays byte-for-byte token-auth (behavior-preserving). Memory-bank carries the SHA in a clean separate commit `a2c8df6f` (no docs bundled into the code commit). `docs/plans/v1.10.0-hub-vault-incluster-provision.md` is implemented on `feat/v1.10.0-vault-auth-portable` as commit `3323aa39` (`feat(vault): hostinger-profile in-cluster Vault provisioning + k8s-auth CSS cutover (Tier 3 P2b)`), pushed to `origin/feat/v1.10.0-vault-auth-portable`. Scope held to the 5 spec files only: `scripts/etc/vault/vars.sh` now derives `HUB_VAULT_CSS_AUTH`, `scripts/plugins/vault.sh` adds the `HUB_VAULT_INCLUSTER=1` bypass, the operator-only `vault_deploy_hub_into_context` wrapper, and the least-privilege `app-cluster-reader` policy bound to `eso-app-cluster`, `scripts/plugins/shopping_cart.sh` adds `_shopping_cart_css_auth_block` and branches CSS auth on `HUB_VAULT_CSS_AUTH`, `scripts/tests/etc/hub_vault_profile.bats` adds 2 auth-derivation tests, and new `scripts/tests/plugins/shopping_cart_css_auth_block.bats` adds 4 helper tests. Validation passed with `shellcheck -S warning scripts/plugins/vault.sh scripts/plugins/shopping_cart.sh scripts/etc/vault/vars.sh` (clean), `bats scripts/tests/etc/hub_vault_profile.bats scripts/tests/plugins/shopping_cart_css_auth_block.bats` (`1..9`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). The default `laptop` profile remains token-auth behavior; `eso-reader` is unchanged. **TIER 1 LIVE DRY-RUN PASS (Claude, 2026-06-27):** against `ubuntu-hostinger` (reachable; ESO CRD serves `v1` matching the CSS `apiVersion`) — `kubectl apply --dry-run=server` accepted BOTH rendered CSS variants: hostinger/kubernetes-auth → `configured` (rc=0, the new k8s-auth shape validates server-side, not just YAML), laptop/token-auth → `unchanged` (rc=0, byte-identical to the live token-mode CSS = fallback render confirmed). `vault_deploy_hub_into_context` guard rails: no-arg → rc=1 + `requires an app cluster kube-context`; bad-context → rc=1 + `not found in kubeconfig`; current-context untouched (`k3d-k3d-cluster`). This is the full pre-PR bar; Tier 2 (real in-cluster provisioning via `vault_deploy_hub_into_context ubuntu-hostinger` + policy bind) is the P3-gated operational cutover, deferred.
- **HOUSEKEEPING + DIAGNOSIS (Claude/Codex, 2026-06-27):** (1) **Stale `feat/v1.11.0-hub-vault-relocation` branch DELETED** (local + remote, was `f05e5948`) — confirmed zero unique commits vs `feat/v1.10.0-vault-auth-portable` before deletion (no phantom v1.11.0). (2) **`data-layer` ArgoCD OutOfSync root-caused (benign):** app on hub `k3d-k3d-cluster`/`cicd` → deploys to `ubuntu-hostinger`/`shopping-cart-data` (generated by `scripts/etc/argocd/applicationsets/data-git.yaml`). All 7 **StatefulSets** OutOfSync, everything else Synced; all pods 1/1 Ready/Healthy/15h. Cause = ArgoCD diffs `.spec.volumeClaimTemplates[].status` (`{phase: Pending}`) + `apiVersion`/`kind` — controller-injected, absent from git, and the AppSet `ignoreDifferences` only covers `volumeClaimTemplates[].spec.storageClassName`+`.spec.volumeMode` (NOT `.status`). `volumeClaimTemplates` is immutable → permanent OutOfSync, selfHeal powerless. **Fix (k3d-manager, not subtree):** add `.spec.volumeClaimTemplates[].status` (+`.apiVersion`,`.kind`) to `data-git.yaml` ignore list. **BUGFIX COMPLETE (Codex, 2026-06-27):** `docs/bugs/v1.10.0-bugfix-data-layer-volumeclaimtemplates-status-drift.md` is implemented on `feat/v1.10.0-vault-auth-portable` as commit `e5813dbb` (`fix(argocd): ignore controller-injected volumeClaimTemplates fields in data-layer ApplicationSet`), pushed to `origin/feat/v1.10.0-vault-auth-portable`. Scope held to the two spec files only: `scripts/etc/argocd/applicationsets/data-git.yaml` gained the 3 new `jqPathExpressions` for `.status`, `.apiVersion`, and `.kind` under `volumeClaimTemplates[]`, and `scripts/tests/plugins/argocd_app_cluster_generator.bats` gained the 1 new assertion test. Validation passed with `bats scripts/tests/plugins/argocd_app_cluster_generator.bats` (`1..4`, all pass) and `./scripts/k3d-manager _agent_audit` (exit 0). The spec's extra YAML parse command `python3 -c "import yaml,sys; yaml.safe_load(...)"` failed in this environment with `ModuleNotFoundError: No module named 'yaml'`; recorded in `docs/issues/2026-06-27-data-git-yaml-validation-pyyaml-missing.md`. Distinct from `2026-06-01-argocd-respectignoredifferences-statefulset.md` (legacy per-app file in shopping-cart-infra, `spec.*` fields). Cosmetic but masks future drift. (3) **Codex P2a plan REVIEWED + APPROVED** — 11-step plan matches `docs/plans/v1.10.0-hub-vault-incluster-unseal.md` exactly (4 files, exact commit msg, push-before-done, memory-bank update); reinforced scope (only 4 files, no subtree/SA/auto-wire, envsubst whitelist exact, pinned image). Codex cleared to implement P2a on `feat/v1.10.0-vault-auth-portable`.
- **VAULT-BRIDGE STABILITY PLAN (2026-06-26, user-chosen Tier 1 + Tier 3 w/ laptop fallback):** Root-caused the recurring Hostinger secret outages = hub Vault on the Mac reached via a fragile reverse-SSH/socat bridge + ESO ExternalSecrets pinned at `refreshInterval: 24h` (→ a transient tunnel flap = 24h outage until manual force-sync). **TIER 1 COMPLETE (Codex, 2026-06-26):** `docs/bugs/2026-06-26-eso-refresh-interval-self-heal.md` is implemented in `shopping-cart-infra` on branch `fix/eso-refresh-interval-self-heal` as commit `670caa3` (`fix(eso): lower ExternalSecret refreshInterval 24h to 15m for self-heal after vault bridge flap`), pushed to origin. Changed the listed 19 YAML files from `refreshInterval: 24h` to `refreshInterval: 15m`; note the listed file `data-layer/secrets/postgres-products-externalsecret.yaml` contains two `ExternalSecret` documents on `origin/main`, so that single listed file required two line edits and the commit diff is `19 files changed, 20 insertions(+), 20 deletions(-)`. Verification in `shopping-cart-infra`: `grep -rln 'refreshInterval: 15m' . | grep -vE '\.(md)$|/archive/|\.clinerules' | wc -l` → `19`; the spec’s exact `grep -rn 'refreshInterval: 24h' . | grep -vE '\.(md)$|/archive/|\.clinerules'` still reports one docs-only hit in `CLAUDE.md` because `grep -rn` outputs `path:line:text`, so the `\.md$` exclusion does not match the full line, but no real manifests remain at `24h`. **TIER 3 DECISIONS LOCKED 2026-06-27 + P1 COMPLETE (Codex):** `docs/plans/hub-vault-relocation-fallback.md` (design, updated) — primary = **Hostinger VPS in-cluster Vault** (`http://vault.secrets.svc:8200`, OCI dropped — cannot provision), fallback = **laptop** (today's reverse-tunnel+socat bridge, **load-bearing → seed continuity mandatory**), **assisted failover up front**, **re-seed-on-failover**. Selectable via `HUB_VAULT_PROFILE=hostinger|laptop`; closes the v1.10.0 "Vault endpoint open seam". Target **v1.10.0**. **P1 shipped on branch `feat/v1.10.0-vault-auth-portable` as commit `22323b18`** (`feat(vault): HUB_VAULT_PROFILE endpoint seam for hub-vault relocation (laptop default, behavior-preserving)`): `scripts/etc/vault/vars.sh` now derives `HUB_VAULT_CSS_SERVER` + `HUB_VAULT_USE_BRIDGE` from `HUB_VAULT_PROFILE` (`laptop` default), `scripts/plugins/shopping_cart.sh` now renders the app-cluster CSS `server:` from the derived value with an inline laptop fallback, `scripts/lib/providers/k3s-hostinger.sh` now gates reverse tunnel + socat + vault-bridge setup on `HUB_VAULT_USE_BRIDGE`, and new `scripts/tests/etc/hub_vault_profile.bats` covers both profiles plus explicit overrides. Validation passed with `shellcheck -S warning scripts/etc/vault/vars.sh scripts/plugins/shopping_cart.sh scripts/lib/providers/k3s-hostinger.sh`, `bats scripts/tests/etc/hub_vault_profile.bats` (`1..3`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **P2 DECISIONS LOCKED + SPLIT (2026-06-27):** auto-unseal = Shamir `shard-1` (init `-key-shares=1 -key-threshold=1`) in the in-cluster `vault-unseal` Secret (already created by `_vault_process_init_artifacts`) replayed by an in-cluster CronJob (`vault status` exit-code driven, pinned `hashicorp/vault:1.18.3`, namespace-scoped, NO RBAC — optional secret volume); flip default = NO in P2 (cutover gated on P3 seeding — design doc corrected); CSS auth = k8s auth (no static token). Split after grounding: `deploy_vault` is ambient-context (Hostinger provider never calls it) + `eso-reader` policy grants only `secret/data/eso/*` vs CSS reading `secret` root. **P2a COMPLETE (Codex, 2026-06-27):** `docs/plans/v1.10.0-hub-vault-incluster-unseal.md` shipped on branch `feat/v1.10.0-vault-auth-portable` as commit `584202b9` (`feat(vault): in-cluster auto-unseal watchdog CronJob (Tier 3 P2a)`), pushed to `origin/feat/v1.10.0-vault-auth-portable`. Scope held exactly to the 4 spec files: `scripts/etc/vault/vars.sh` now exports pinned `VAULT_UNSEAL_IMAGE`, new `scripts/etc/vault/unseal-watchdog.yaml.tmpl` renders the namespace-scoped CronJob via the exact `envsubst` whitelist `$VAULT_NS $VAULT_UNSEAL_IMAGE $VAULT_ENDPOINT`, `scripts/plugins/vault.sh` adds public operator command `vault_install_unseal_watchdog [ns] [context]` immediately before `deploy_vault` with no auto-caller, and new `scripts/tests/etc/vault_unseal_watchdog.bats` asserts the rendered manifest. Validation passed with `bats scripts/tests/etc/vault_unseal_watchdog.bats` (`1..5`, all pass), `shellcheck -S warning scripts/etc/vault/vars.sh scripts/plugins/vault.sh` (clean), and `./scripts/k3d-manager _agent_audit` (exit 0). No PR created per repo rules. **P2b SPEC WRITTEN (Codex-ready; live grounding pass done 2026-06-27):** `docs/plans/v1.10.0-hub-vault-incluster-provision.md` = provision orchestration + CSS k8s-auth cutover (5 files: `vars.sh` +`HUB_VAULT_CSS_AUTH` derivation [laptop→token, hostinger→kubernetes], `vault.sh` +`HUB_VAULT_INCLUSTER=1` bypass of the `CLUSTER_ROLE=app` early-return + new `vault_deploy_hub_into_context` wrapper [save/restore current-context, operator-dispatchable, no auto-caller] + new least-privilege `app-cluster-reader` policy bound to the `eso-app-cluster` role, `shopping_cart.sh` +`_shopping_cart_css_auth_block` helper and CSS branch on `HUB_VAULT_CSS_AUTH` [token mode unchanged; kubernetes mode skips the laptop root-token read], +2 `hub_vault_profile.bats` tests, new `scripts/tests/plugins/shopping_cart_css_auth_block.bats`). Two forks locked via AskUserQuestion: provision = context-switch wrapper + opt-in flag (NOT thread `--context` — `_kubectl`/`_helm` are lib-foundation subtree); reader policy = new `app-cluster-reader` with the 7 real prefixes (`secret/data/{postgres,payment,redis,rabbitmq,keycloak,ldap,minio}/*` — grounded from shopping-cart-infra `remoteRef` keys; none under `eso/*`). Behavior-preserving at default `laptop`; default flip is the P3-gated cutover (runbook in the P2b spec). **NEXT HANDOFF = P2b** on branch `feat/v1.10.0-vault-auth-portable`. Release-scope: v1.10.0 = P1+P2a+P2b; P3 (seeding) + P4 (failover) → v1.11.0 (max-5-plan-docs). Builds on v1.10.0 auth helper + `vault-resilience.md` auto-unseal. **Correction recorded:** v1.10.0 alone does NOT fix the transport fragility (only swaps token→k8s-auth); Tier 3 is the transport fix. See [[project_app_cluster_vault_auth_portability]].
- **TIER 1 PR MERGED (Claude/post-merge, 2026-06-27):** shopping-cart-infra **PR #86** (`fix/eso-refresh-interval-self-heal` → `main`, merge `4afa9dce5d7912bb436ef9af939636aa2e2cbbb5`) — https://github.com/wilddog64/shopping-cart-infra/pull/86. Initial commit `670caa3` lowered `refreshInterval` to `15m`; follow-up commit `ace53cf` (`docs(eso): align refreshInterval comments + CHANGELOG with 15m self-heal interval`) landed on the SAME branch/PR and resolved the 7 real Copilot comment findings. Merged 2026-06-27; `enforce_admins` restored (re-enabled) after merge; local main synced and merge commit `4afa9dc` confirmed.
- **LIVE INCIDENT RESOLVED (2026-06-28, Claude) — ALL k3dm LaunchAgents unloaded + ssh-tunnel left `disabled` (NEW 3rd variant):** Symptom (user smoke test): CSS `vault-backend` `Ready=False` ("unable to validate store"), 17/17 ExternalSecrets not synced; collateral Keycloak 502 + ArgoCD connection-refused. **Root cause = the entire bridge chain was down at the FIRST hop:** `launchctl list | grep k3dm` showed NOTHING loaded — Mac-side Vault port-forward `:18200`=000 (the `com.k3d-manager.vault-port-forward.plist` was MISSING from `~/Library/LaunchAgents` entirely), and `com.k3d-manager.ssh-tunnel` was not just unloaded but **`disabled`** in launchd (`launchctl print-disabled gui/$(id -u)` → `"com.k3d-manager.ssh-tunnel" => disabled`), so plain `bootstrap` failed with `Input/output error` (5). Hub Vault `vault-0` on `k3d-k3d-cluster`/`secrets` was healthy + **unsealed** the whole time (not the cause). **FIX (laptop-side, bottom-up, no code change):** (1) render `vault-port-forward.plist` from `scripts/etc/launchd/com.k3d-manager.vault-port-forward.plist.tmpl` (sed `{{KUBECTL_PATH}}`=`$(command -v kubectl)`, `{{HOME}}`) → `bootout` stale → `bootstrap gui/$(id -u)` → `:18200`=200; (2) **`launchctl enable gui/$(id -u)/com.k3d-manager.ssh-tunnel` FIRST** (clears the disabled flag — bootstrap alone errors 5), then `bootstrap` → autossh `-R 8200:127.0.0.1:18200` runs, host `localhost:8200`=200, host socat already up; (3) then the standard runbook — nudge CSS (`Ready=True` in 5s) → `force-sync` all 17 ES → **17/17 SecretSynced, 0 errors**, all shopping-cart pods Running/Ready. Keycloak verified healthy via direct hub probe (`/health/ready`=200; lives on `k3d-k3d-cluster`/`identity`, NOT hostinger — the 502 was public-ingress collateral during the bridge-down window). **Key new lessons:** (a) a `disabled` LaunchAgent needs `launchctl enable` before `bootstrap` — bootstrap-alone errors 5; (b) when the whole k3dm agent set is unloaded (laptop reboot), the bridge needs restoring **bottom-up: port-forward `:18200` BEFORE the reverse tunnel** (tunnel landing is useless if `:18200` is dead); (c) force-sync is futile until the bridge is healthy end-to-end. The provider refresh (`_hostinger_refresh_access_layer`) renders+restarts these, but does NOT run on reboot — this is the laptop-side fragility the in-cluster `HUB_VAULT_PROFILE=hostinger` cutover (Tier 3 P2b/P3) ultimately retires. See [[project_app_cluster_vault_auth_portability]].
- **LIVE INCIDENT RESOLVED (2026-06-26 23:43Z) — stale ESO store after tunnel flap (recurring):** Symptom: CSS `vault-backend` `Ready=False` (`InvalidProviderConfig`/"unable to validate store"), 14/17 ExternalSecrets `SecretSyncedError` "could not get secret data from provider". **NOT the missing-LaunchAgent outage** — this time the WHOLE bridge was healthy at check time: token valid (root, reads `secret/`=200), Mac pf `18200`=200, autossh `-R 8200→18200` up, host socat `:8201`=200, host reverse-tunnel `:8200`=200. Timeline: reverse tunnel **dropped 23:28Z** (autossh `ExitOnForwardFailure=yes`+`ServerAliveInterval=30/Count=3` → a ~90s blip kills it) → CSS validation failed → autossh **self-reconnected 23:37Z** → chain healthy BUT ESO store status + the 14 ES stayed frozen at the 23:28–23:37Z error (refresh-interval backoff; controller quiet, no auto-retry). **FIX (no code change, no restart):** (1) `kubectl annotate clustersecretstore vault-backend force-sync=$(date +%s) --overwrite` → `Ready=True` in 5s; (2) the 14 ES did NOT auto-recover from store-ready alone → `force-sync` annotation on each erroring ExternalSecret → `errors=0/17` in 10s. **END STATE: all 17 ES SecretSynced, 12 shopping-cart pods Running/Ready, frontend.3ai-talk.org=200, /api/products=20.** **Recurring root fragility = the reverse-tunnel Vault bridge** (any network blip flaps it, ESO doesn't auto-clear) — this is exactly what **v1.10.0 vault-auth portability retires** (direct k8s-auth, no bridge). Recovery runbook for next time: nudge the CSS first, then force-sync stuck ES (store-ready alone won't unstick them).

- **v1.8.0 RELEASED + NEXT BRANCH CUT (2026-06-26):** PR #99 merged to `main` (`dd54b653`); tagged `v1.8.0` + GitHub release (latest); `enforce_admins` restored on `main`; `main` synced; **next branch `feat/v1.10.0-vault-auth-portable`** created from the merge SHA (tracks itself; originally cut as v1.9.0, renumbered to v1.10.0 so blue-green keeps v1.9.0). Post-merge housekeeping landed on the next branch: CHANGELOG `[Unreleased]`→`[1.8.0]`, README + `docs/releases.md` releases tables back-filled (v1.7.0/v1.7.1/v1.8.0), retro doc, standing-docs de-stale (lib-acg→foundation, gemini-cli→agy), memory-bank. **NEXT WORK = app-cluster Vault-auth portability Phase 3** (user-chosen): factor into ONE kubecontext-keyed helper callable from every provider. **SPEC WRITTEN + DECISIONS RESOLVED (2026-06-26):** `docs/plans/v1.10.0-vault-auth-portable.md` — new public `configure_vault_app_auth_for_context <ctx> [kubeconfig]` in vault.sh (derives `.cluster.server` + CA→temp PEM, subshell-guards `configure_vault_app_auth`, best-effort unless `APP_VAULT_AUTH_REQUIRED=true`), called from Hostinger/OCI/AWS-Azure(`bin/cluster-up`) register paths + BATS. **Key finding:** `configure_vault_app_auth` has NO production caller today (the "wired inline" memory was the plan, not shipped). Model = single shared `kubernetes-app` hub-Vault mount reconfigured per active cluster (per-cluster mount + per-env Vault host = documented out-of-scope seams). **Decisions:** (1) renumbered v1.9.0→**v1.10.0** (blue-green keeps v1.9.0); (2) `bin/cluster-up` Change 4 stays a **guarded no-op** until AWS/Azure exercised live (do NOT source vault.sh now); (3) best-effort default confirmed. **COMPLETE (Codex, 2026-06-26):** Task 1 bugfix shipped as `a8595439` (`fix(hostinger): ensure vault-port-forward LaunchAgent on refresh`) on `feat/v1.10.0-vault-auth-portable`; `scripts/lib/providers/k3s-hostinger.sh` refresh now declares the vault-port-forward LaunchAgent vars, renders `scripts/etc/launchd/com.k3d-manager.vault-port-forward.plist.tmpl` when missing, and restarts `com.k3d-manager.vault-port-forward` before the Grafana restart; `scripts/tests/lib/provider_contract.bats` now proves the refresh path regenerates/restarts that LaunchAgent. Validation passed with `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh`, `bash -n scripts/lib/providers/k3s-hostinger.sh`, `bats scripts/tests/lib/provider_contract.bats` (`1..41`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **COMPLETE (Codex, 2026-06-26):** Task 2 feature shipped as `179251a3` (`feat(vault): provider-agnostic app-cluster Vault auth keyed on kube-context`) on `feat/v1.10.0-vault-auth-portable`; added `configure_vault_app_auth_for_context` in `scripts/plugins/vault.sh`, wired guarded callers into Hostinger, OCI, and the AWS/Azure `bin/cluster-up` registration path (guarded no-op retained there), and added focused BATS coverage in `scripts/tests/plugins/vault_app_auth.bats`. Validation passed with `shellcheck -S warning scripts/plugins/vault.sh scripts/lib/providers/k3s-hostinger.sh scripts/lib/providers/k3s-oci.sh bin/cluster-up`, `bash -n` on the same files, `bats scripts/tests/plugins/vault_app_auth.bats` (`1..11`, all pass), `./scripts/k3d-manager test all` (`1..393`, all pass), and `./scripts/k3d-manager _agent_audit` (exit 0). **PR URL:** none; per repo rules no PR was created. See [[project_app_cluster_vault_auth_portability]]. **Open follow-ups:** v1.7.1 CHANGELOG section never written; `_sts_valid` undefined at `acg-credential-test:277`; dead `scripts/lib/acg/*` glob in `agent_rigor.sh` (both upstream-first); lib-acg absorption Phase 3 = archive repo (deferred).

> **Older entries (v1.7.1 and earlier) archived 2026-06-27** → `memory-bank/archive/activeContext-v1.6.x-v1.7.1.md` (and `activeContext-v1.4.2-v1.4.8.md` for the earlier window).
