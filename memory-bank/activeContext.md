# Active Context — k3d-manager

## Current Status (v1.14.0 RELEASED 2026-07-12; v1.15.0 RELEASED 2026-07-14; v1.16.0 active branch — Istio ambient mesh)

**WEBHOOK PYTHON-DRIFT INCIDENT + UTCNOW BUGFIX — resolved (Claude/Codex, 2026-07-18):** Recurring `make status CLUSTER_PROVIDER=k3s-hostinger` failures (❌ Pushgateway `Connection refused`, ❌ ESO CSS `Expecting value line 1 col 1`, ✅ 0/0, ❌ Data layer 4-not-ready) were **stale scrollback**, NOT live — the ❌ Pushgateway line is impossible on current code (`_provider_supports_pushgateway` in `bin/k3dm-webhook` returns False for `k3s-hostinger`), proving the paste predated that guard. Live verify (kubectl + `make status` + direct `/api/v1/health` ×5, no flap): hostinger fully healthy — postgres-orders/payment 1/1, ESO 18/18 SecretSynced, CSS Ready=True, Data layer 4/4. **Root cause of the restart failure it exposed:** the running webhook (PID 7929, up since Jul 5) was executing an in-memory 3.13 binary from a Cellar path Homebrew had since **deleted** — Homebrew moved `python@3.13`→`python@3.14` (3.13 Cellar gone; `/opt/homebrew/bin/python3`→3.14.6). The **installed** plist `~/Library/LaunchAgents/com.k3d-manager.webhook.plist` had a stale version-pinned `/opt/homebrew/bin/python3.13` (missing) → `launchctl bootstrap` failed `EX_CONFIG (78)` + penalty box. **Operational fix (no repo code change):** the repo template `scripts/etc/launchd/com.k3d-manager.webhook.plist.tmpl:10` already uses the agnostic `/opt/homebrew/bin/python3`; aligned the installed plist to match (sed `python3.13`→`python3`) + bootout/bootstrap. Webhook now fresh **PID 38697 on Python 3.14.6**, `/api/v1/health` all ✅, `make status` all ✅. Verified NO other generator/template/installed-agent pins a versioned python. **Repo code fix now pushed on `origin/k3d-manager-v1.16.0` at `a549a37a`:** `fix(webhook): use timezone-aware UTC timestamps (drop deprecated datetime.utcnow)` implemented `docs/bugs/2026-07-17-webhook-utcnow-deprecation.md` in `bin/k3dm-webhook` by replacing the 10 deprecated `datetime.datetime.utcnow()` call sites with `datetime.datetime.now(datetime.timezone.utc)`, which also fixes the latent naive-epoch offset bug on the absolute Pushgateway timestamp path. Local gates passed: `grep -c 'utcnow' bin/k3dm-webhook` → `0`, `grep -c 'datetime.datetime.now(datetime.timezone.utc)' bin/k3dm-webhook` → `10`, `python3 -c "import ast; ast.parse(open('bin/k3dm-webhook').read())"` exit 0. **Status: implemented + pushed, pending Claude live restart/re-verify only.**

**v1.16.0 PHASE 1 IMPLEMENTED + PUSHED (Codex, 2026-07-17) — Istio ambient mesh, ACG-sandbox→Hostinger via ArgoCD:** `docs/plans/v1.16.0-hostinger-istio-ambient-mesh.md` (first v1.16.0 plan doc). **Phase 1 only** is now on `origin/k3d-manager-v1.16.0` at **`125b797e`** with commit `feat(mesh): Istio ambient via ArgoCD — ACG-sandbox + Hostinger CNI groundwork (K3S_AMBIENT_MESH opt-in)`. Implemented exactly in `scripts/plugins/shopping_cart.sh`: opt-in `_AMBIENT_CILIUM_VERSION` / `_AMBIENT_ISTIO_VERSION`, guarded `k3sup` flannel-off + `--disable-network-policy` only when `K3S_AMBIENT_MESH=true`, new `_ambient_install_cilium` (ported from OCI flow, includes mandatory `cni.exclusive=false`), and Cilium install wired **before** the node-Ready wait. **SSM decision = option (b), unsupported this release:** `_ssm_bootstrap_k3s` now fails fast when `K3S_AMBIENT_MESH=true` with `K3S_AWS_SSM_ENABLED=true`, because Phase 1 must not disable flannel on SSM without a verified pre-wait Cilium install path. `scripts/lib/providers/k3s-aws.sh` was read verify-only; no `K3S_AMBIENT_MESH` surfacing change was needed there in this pass. Validation run locally: `shellcheck -S warning scripts/plugins/shopping_cart.sh` clean, `./scripts/k3d-manager _agent_audit` clean, `bats scripts/tests/plugins/shopping_cart.bats` `1..11` pass, `bats scripts/tests/lib/k3s_aws_provider.bats` `1..5` pass. **Status: implemented + pushed, pending Claude sandbox verify / CI / PR.** **Phase 2–3 remain pending and must NOT start until Claude completes the disposable ACG sandbox ambient dataplane verify.**

**v1.16.0 PHASE 1 CLAUDE-VERIFIED (Claude, 2026-07-17):** independently confirmed `125b797e` on `origin/k3d-manager-v1.16.0` — exact commit message, **only** `scripts/plugins/shopping_cart.sh` touched (`k3s-aws.sh` correctly untouched/verify-only), all 5 spec changes present + guard-wrapped, Cilium wired before node-Ready wait, SSM option (b) unsupported-error documented in commit body. Re-ran `shellcheck -S warning` (exit 0, clean) and `bats scripts/tests/plugins/shopping_cart.bats` (`1..11` pass). **One flag:** two unsolicited behavior-preserving refactors (the `external_ip` awk lookup + the `ssh_key` existence check rewritten to one-liners) — scope creep vs "minimal patches", accepted as-is, note for Copilot/retro. `_ambient_install_cilium` confirmed a faithful port of `_oci_install_cilium` (same raw-ssh convention → no `_run_command` deviation). **LIVE ACG SANDBOX SMOKE BLOCKED — no sandbox provisioned:** `aws sts get-caller-identity` → `InvalidClientTokenId`, no `ubuntu-k3s` kube context, SSH to `ubuntu` (52.37.60.119:22) times out (EC2 gone), `acg_status` reports creds expired. Gate is blocked on sandbox availability only, not on code. **To unblock:** start a new sandbox at the Pluralsight ACG console → `acg_get_credentials` → provision host → `K3S_AMBIENT_MESH=true ./scripts/k3d-manager deploy_app_cluster --confirm` on the disposable box → verify node goes Ready on Cilium + ambient capture. Phase 2 stays blocked until this smoke passes.

**v1.16.0 PHASE 1 LIVE SMOKE BUGFIX IMPLEMENTED + PUSHED (Codex, 2026-07-17):** spec `docs/bugs/2026-07-16-ambient-cilium-kubeconfig-perms.md` is now implemented on `origin/k3d-manager-v1.16.0` at **`c5a730c1`** with commit `fix(mesh): _ambient_install_cilium reads user-readable kubeconfig (k3sup non-root SSH)`. Scope held exactly to `scripts/plugins/shopping_cart.sh`: `_ambient_install_cilium` now defines `local remote_kubeconfig="/home/${ssh_user}/.kube/k3s.yaml"` and uses that user-readable copy for all 5 remote helm/kubectl `KUBECONFIG=` calls (idempotent check, repo add, repo update, helm install, rollout wait). `k8sServiceHost='${external_ip}'` was correctly left unchanged. Local gate: `shellcheck -S warning scripts/plugins/shopping_cart.sh` clean. **Status: implemented + pushed + CLAUDE E2E-VERIFIED — DONE.**

**v1.16.0 PHASE 1 AMBIENT E2E PASSED (Claude, 2026-07-17):** ran the full clean-slate fresh-sandbox e2e with the fix (`git pull` to `268d6a82`, verified `remote_kubeconfig` present in `_ambient_install_cilium`): `K3S_AMBIENT_MESH=true K3S_AWS_SSM_ENABLED=false CLUSTER_PROVIDER=k3s-aws ./scripts/k3d-manager deploy_cluster --confirm` → **exit 0**. Milestones observed live: CFN 3-node stack up → k3s installed with flannel disabled → Cilium 1.16.5 helm install **succeeded with ZERO permission-denied** (the entire bug) → `Cilium ready` on first rollout-status attempt (no 3-min timeout) → server `Node Ready` → all 3 nodes Ready. Independent kubectl verify (`--context ubuntu-k3s`): 3 nodes `Ready` (1 control-plane `ip-10-0-1-87` + 2 agents `-66`/`-93`, k3s v1.36.2), Cilium DaemonSet 3/3, all 3 `cilium-*` pods `1/1 Running` (v1.16.5 pinned digest), **no flannel pods**, and `grep -i "permission denied"` over the full deploy log = NONE. **Both agent nodes joined + reached Ready — this settles the spec's open question: `k8sServiceHost='${external_ip}'` is correct (agents can reach the API at the public IP), so leaving it untouched was right.** Fix `c5a730c1` conclusively validated from a clean slate, not a manual patch. **Phase 1 ambient dataplane verify gate = CLEARED; Phase 2 unblocked.**

**v1.16.0 PHASE 2 IMPLEMENTED + PUSHED (Codex, 2026-07-17):** implementation spec `docs/plans/v1.16.0-phase2-istio-ambient-appset.md` is now on `origin/k3d-manager-v1.16.0` at **`7e88a9d5`** with commit `feat(mesh): Istio ambient via ArgoCD ApplicationSet + deploy_istio_ambient (Phase 2)`. Scope held exactly to the 4 plan targets: `scripts/etc/argocd/projects/platform.yaml.tmpl` now permits `istio-system` on the 4 named app clusters; NEW `scripts/etc/argocd/applicationsets/istio-ambient.yaml` adds the base/istiod/cni/ztunnel ApplicationSet using per-element `helm.values` block-scalars (not `helm.parameters`); NEW `scripts/plugins/istio_ambient.sh` adds `deploy_istio_ambient`; and `scripts/k3d-manager` now exposes the dispatcher wrapper. Static gates passed locally: YAML parse via local `python` + `yaml.safe_load_all` on both YAML files, `shellcheck -S warning` clean on `scripts/plugins/istio_ambient.sh` and `scripts/k3d-manager`, `bash -n` clean on both shell files, `./scripts/k3d-manager deploy_istio_ambient --help` prints usage, and `./scripts/k3d-manager _agent_audit` exits 0. **Status: implemented + pushed, pending Claude live sandbox smoke only.** **No live sandbox smoke was run in this pass.** Phase 3 (Hostinger) remains blocked until Claude verifies ambient convergence and dataplane capture on the sandbox.

**v1.16.0 PHASE 2 CLAUDE-VERIFIED (Claude, 2026-07-17):** independently confirmed `7e88a9d5` on `origin/k3d-manager-v1.16.0` (`git branch -r --contains`) — commit message verbatim; `git show --stat` = **exactly the 4 targets** (platform.yaml.tmpl +8, istio-ambient.yaml NEW 68, istio_ambient.sh NEW 39, k3d-manager +4), memory-bank recorded separately in `47ee7bff` (2 files only, no scope leak); all 4 change bodies **byte-identical to the spec**. Re-ran gates myself: `shellcheck -S warning` clean on both shell files, `bash -n` clean on both, YAML valid on both (`ruby -ryaml YAML.load_stream` — host `python3` lacks pyyaml), `deploy_istio_ambient --help` prints usage, `./scripts/k3d-manager _agent_audit` exit 0. No scope creep, no Phase 1/Phase 3 touch, `helm.values` block-scalars used (not `helm.parameters`). **Verdict: PASS — code gate cleared.** **Next = Claude's live sandbox smoke** (register_app_cluster ubuntu-k3s → re-apply platform AppProject to hub → `APP_CLUSTER_NAME=ubuntu-k3s deploy_istio_ambient` → watch ArgoCD converge → ambient dataplane capture via ztunnel logs) — requires a live ACG sandbox (none provisioned as of Phase 1 e2e teardown). Phase 3 stays blocked until this smoke passes.

**v1.16.0 PHASE 2 LIVE SANDBOX SMOKE — Cilium pod-CIDR/VPC-overlap BUGFIX IMPLEMENTED + PUSHED (Codex, 2026-07-17):** bug spec `docs/bugs/2026-07-17-ambient-cilium-podcidr-vpc-overlap.md` is now on `origin/k3d-manager-v1.16.0` at **`bcc87f1c`** with commit `fix(mesh): pin ambient Cilium pod CIDR to avoid AWS VPC 10.0.0.0/8 overlap`. Scope held exactly to `scripts/plugins/shopping_cart.sh`: added `_AMBIENT_POD_CIDR="${AMBIENT_POD_CIDR:-10.42.0.0/16}"` beside the ambient vars and inserted `--set ipam.operator.clusterPoolIPv4PodCIDRList='{${_AMBIENT_POD_CIDR}}'` in the Cilium helm install **between** `kubeProxyReplacement` and `k8sServiceHost`, with `k8sServiceHost` / `k8sServicePort` unchanged and no switch to `ipam.mode=kubernetes`. Local gates passed: `shellcheck -S warning scripts/plugins/shopping_cart.sh` clean, `bash -n scripts/plugins/shopping_cart.sh` clean, `./scripts/k3d-manager _agent_audit` exit 0. **Status: implemented + pushed, pending Claude live sandbox re-verify only.** Phase 3 remains blocked until Claude confirms the ambient dataplane converges on the still-running sandbox and separately closes the k3s-aws `argocd-manager` SA gap.

**v1.16.0 CILIUM POD-CIDR FIX — CLAUDE LIVE RE-VERIFIED = PASS (Claude, 2026-07-17):** provisioned a FRESH ACG sandbox (acct `975201825692`; the prior `992382716987` sandbox had expired — rebuilt after a manual Pluralsight login in the CDP Chrome, see [[reference_acg_stale_tab_invalid_creds]] and the login-mechanism note below) with the fix on the working tree (`shopping_cart.sh:1057` var + `:1086` `--set`). `K3S_AMBIENT_MESH=true K3S_AWS_SSM_ENABLED=false CLUSTER_PROVIDER=k3s-aws deploy_cluster --confirm` → exit 0, 3 nodes Ready (`10.0.1.16/223/29` — **same VPC `10.0.1.0/24` overlap scenario as the bug**). Fix proven end-to-end: (1) `cilium-config cluster-pool-ipv4-cidr` = **`10.42.0.0/16`** (was `10.0.0.0/8`), ipam `cluster-pool`, Cilium DS 3/3 Running; (2) `ip route get` to peer nodes AND to API ClusterIP `10.43.0.1` now goes `dev ens5` — **NOT `dev cilium_host`** (the pre-fix black-hole route is gone); (3) `cilium-health status` = all 3 nodes **Host + Endpoint connectivity OK** (ICMP + HTTP, sub-ms RTT; pre-fix agent↔server was 100% loss); pod endpoints on `10.42.x.x` (one /24 per node, disjoint from node subnet); (4) throwaway pod → `curl -k https://10.43.0.1:443/healthz` = **HTTP 401** (API answered — this is the EXACT path that returned `i/o timeout` pre-fix). **Verdict: Cilium pod-CIDR/VPC-overlap bug (`bcc87f1c`) CONCLUSIVELY FIXED.** Remaining before Phase 3: (a) run the Phase 2 ambient deploy on this sandbox (register_app_cluster → platform AppProject → deploy_istio_ambient) to observe istio-cni 1/1 + ztunnel 3/3 + ambient capture — now unblocked since their root-cause dep (API ClusterIP reachability) is proven; (b) close the k3s-aws `argocd-manager` SA gap. Sandbox left UP for the Phase 2 ambient deploy.

**v1.16.0 PHASE 2 AMBIENT DEPLOY — CLAUDE LIVE VERIFIED = PASS + NEW istio-cni CNI-DIR BUG FOUND (Claude, 2026-07-17):** Ran the full Phase 2 ambient deploy on the still-up sandbox (acct `975201825692`, nodes `10.0.1.16/223/29`). Steps: (1) **closed the k3s-aws `argocd-manager` SA gap manually** — `kubectl --context ubuntu-k3s apply -f scripts/etc/argocd-manager.yaml` (the k3s-aws path has NO `_hostinger_ensure_argocd_manager_sa` equivalent — this is the real code gap, still needs a fix); minted an 8760h token. (2) **Re-registered** the app cluster on the hub — the stale `cluster-ubuntu-k3s` secret pointed at `https://host.k3d.internal:6443` (Hostinger convention); the AWS sandbox API is the public IP, so `ARGOCD_APP_CLUSTER_SERVER=https://44.251.39.123:6443 ARGOCD_APP_CLUSTER_TOKEN=<tok> register_app_cluster` (current context = hub `k3d-k3d-cluster`). (3) `deploy_istio_ambient --confirm` (there IS a `--confirm` safety gate). Apps were stuck "will not retry failed sync to [1.24.2]" from an 8h-old dead registration → cleared each with `annotate refresh=hard` + `patch operation`. **istio-base synced Succeeded = definitive hub→AWS-API reachability + RBAC proof.** istiod 1/1 (pod IP `10.42.0.16` = fixed CIDR), ztunnel **3/3** (was 1/3 pre-Cilium-fix). **NEW BUG — istio-cni 0/1 forever** (readiness `/readyz` 503, log: `Istio CNI is configured as chained plugin, but cannot find existing CNI network config: no networks found in /host/etc/cni/net.d`). Root cause: the istio-ambient ApplicationSet hardcodes istio-cni `cni.cniConfDir: /var/lib/rancher/k3s/agent/etc/cni/net.d` + `cni.cniBinDir: /var/lib/rancher/k3s/data/current/bin` (**standard k3s** paths), but `_ambient_install_cilium` installs Cilium with its **default** conf/bin paths (no `cni.confPath`/`cni.binPath` override) → Cilium writes `/etc/cni/net.d/05-cilium.conflist` and its binary to `/opt/cni/bin`, and k3s reads `/etc/cni/net.d` (verified: rancher net.d EMPTY, only `05-cilium.conflist` in `/etc/cni/net.d`; pod-sandbox error `failed to find plugin "istio-cni" in path [/opt/cni/bin]`). **FIX (applied LIVE as a hub-only override, repo file NOT yet changed):** set istio-cni `cniConfDir: /etc/cni/net.d` + `cniBinDir: /opt/cni/bin`. After the override + re-sync: **istio-cni 1/1 on all 3 nodes** (log: `CNI config file "" preempted by "/host/etc/cni/net.d/05-cilium.conflist" … initial installation complete`). **AMBIENT DATAPLANE CAPTURE = PASS:** labeled ns `ambient-test` `istio.io/dataplane-mode=ambient`, curl client→`server.ambient-test.svc` = HTTP 200 ×3; ztunnel access log shows HBONE capture + mTLS: `src.workload="client-…" dst.hbone_addr=10.42.1.233:80 dst.addr=…:15008` with **mutual SPIFFE identities** `spiffe://cluster.local/ns/ambient-test/sa/default`, both `direction=inbound/outbound`. **Phase 2 ambient mesh works end-to-end.** **TWO follow-ups needed before Phase 3 (both need a `docs/bugs/` spec → Codex):** (a) istio-ambient ApplicationSet cniConfDir/cniBinDir fix (`scripts/etc/argocd/applicationsets/istio-ambient.yaml` — the live hub override will be overwritten when ArgoCD re-syncs the repo file, so the repo MUST be corrected); (b) k3s-aws `argocd-manager` SA auto-creation gap (mirror `_hostinger_ensure_argocd_manager_sa`). NOTE: these are Cilium-default-path assumptions — if a future Cilium install pins `cni.confPath`/`cni.binPath` to the rancher paths instead, the ApplicationSet's original values would be correct; the durable fix should make BOTH sides agree (recommend the Cilium-default `/etc/cni/net.d` + `/opt/cni/bin`).

**HOSTINGER PLATFORM RECOVERY — 10/10 health checks GREEN (Claude, 2026-07-17):** User surfaced a `make status CLUSTER_PROVIDER=k3s-hostinger` health check with many failures (Alertmanager 502, Frontend/Pushgateway/Product-images connection-refused, ESO ClusterSecretStore parse-error, Data layer postgres-orders/payment not ready). **Root cause chain:** the ESO **webhook** on hostinger (`external-secrets-webhook.secrets.svc`) had **no endpoints** (pods down) → ArgoCD `data-layer` app SyncError `failed calling webhook "validate.externalsecret.external-secrets.io" … no endpoints available` → postgres/redis/rabbitmq/minio StatefulSets never created (only their Services existed, 9d) → every app service (basket/order/payment/product-catalog) crash-looped on DB DNS lookups (`could not translate host name postgresql-products.shopping-cart-data.svc`). **Fix (3 steps):** (1) `make refresh CLUSTER_PROVIDER=k3s-hostinger` → restarted ESO pods (all 3 ESO deploys 1/1 with live webhook endpoint `10.42.0.166:10250`) + all launchd tunnels/port-forwards + re-applied `ghcr-pull-secret` + bounced frontend; (2) re-synced `data-layer` app on the laptop hub (`annotate refresh=hard` + `patch operation`) → **Succeeded**, postgres-orders/payment/products + redis×2 + rabbitmq + minio all 1/1; (3) `rollout restart` basket-service/product-catalog (shopping-cart-apps) + payment-service (shopping-cart-payment) to clear CrashLoop backoff now that DBs+DNS resolve. **Result:** `make status` Service Health = **all ✅** (Alertmanager/ArgoCD/Frontend/Keycloak/Prometheus/Grafana 200, Product images 20/20, ESO ClusterSecretStore Ready=True, ESO ExternalSecrets 18/18 synced, Data layer 4/4). The ESO ClusterSecretStore "parse error" was a health-check false alarm — the `vault-backend` store was Valid/Ready=True throughout.

**ACG LOGIN MECHANISM — chronic "login never works" root cause (Claude, 2026-07-17):** `acg_get_credentials` reuses the :9222 CDP Chrome cookies; it does NOT log in. `loginWithPage` dead-ends on MFA (`mfa_required`). Working path = sign into Pluralsight ONCE by hand in that Chrome profile; every extraction reuses the cookie. Full detail + unblock procedure in memory file `reference_acg_login_reuses_cdp_session.md`.

**v1.15.0 RELEASED (Claude, 2026-07-14):** [PR #105](https://github.com/wilddog64/k3d-manager/pull/105) merged to `main` at `14cdaea6`. 4 code tasks: WS3 Phase 2 per-context hub-Vault CSS (`46f400b9`), WS3 Phase 3 provider demote + reachability preflight (`2f74acf3`), Trivy Standalone→ClientServer (`855a06e4`, live-verified), js-yaml DoS bump via lib-foundation subtree (`999edb81`). Copilot coverage gaps fixed `a41f8987`. `enforce_admins` restored. Full post-merge (tag/release/retro) deferred — session was scoped to the 2 carry-forwards only. Detail archived below.

> **Older entries (v1.8.0 → v1.15.0) archived 2026-07-17** → `memory-bank/archive/activeContext-v1.8.0-v1.15.0.md`. Earlier windows: `activeContext-v1.6.x-v1.7.1.md`, `activeContext-v1.4.2-v1.4.8.md`.
