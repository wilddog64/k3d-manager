# Active Context — k3d-manager

## Current Status (v1.7.1 — Active; v1.7.0 RELEASED 2026-06-13)

- **COMPLETE (Codex, 2026-06-24):** `fix(hostinger)` follow-up on commit `0e832b5e` resolved the live `make refresh CLUSTER_PROVIDER=k3s-hostinger` crash caused by `_ACG_STATE_DIR` being unset in `scripts/lib/providers/k3s-hostinger.sh`; the provider script now defines a local default state directory before using it, and the refresh command completes successfully through the ArgoCD port-forward, browser listeners, grafana, and pushgateway restarts; issue note: `docs/issues/2026-06-24-hostinger-refresh-acg-state-dir-unbound.md`; validated by re-running `make refresh CLUSTER_PROVIDER=k3s-hostinger` to completion after the fix.
- **COMPLETE (Codex, 2026-06-24):** `fix(hostinger)` commit `0e832b5e` on `feat/v1.8.0-acg-absorb-phase2-agy` restarts/self-heals the `com.k3d-manager.argocd-port-forward` LaunchAgent during `make refresh CLUSTER_PROVIDER=k3s-hostinger` so Cloudflare can still reach `http://localhost:8080` behind `argocd.3ai-talk.org`; wrote `docs/bugs/2026-06-24-hostinger-argocd-public-tunnel-refresh-gap.md`; validated with `bash -n scripts/lib/providers/k3s-hostinger.sh scripts/lib/provider.sh`, `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh scripts/lib/provider.sh`, `bats scripts/tests/lib/provider_contract.bats` (`1..37`, all pass), and `git diff --check`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (Codex, 2026-06-24):** `fix(provider)` commit `1cb723f8` on `feat/v1.8.0-acg-absorb-phase2-agy` makes live cluster state win over stale `active-provider` state so `make refresh`/`make status` switch correctly between Hostinger and ACG even when the old provider file is still present; `_acg_resolve_provider()` now probes live contexts first and only falls back to the saved file when no provider is reachable, and `scripts/tests/lib/provider_contract.bats` adds coverage for the stale-file/Hostinger switching cases; validated with `bash -n scripts/lib/provider.sh scripts/lib/providers/k3s-hostinger.sh`, `shellcheck -S warning scripts/lib/provider.sh scripts/lib/providers/k3s-hostinger.sh`, `bats scripts/tests/lib/provider_contract.bats` (`1..36`, all pass), and `git diff --check`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (Codex, 2026-06-24):** `fix(provider)` commit `23f915d3` on `feat/v1.8.0-acg-absorb-phase2-agy` keeps `make status` on the active cluster when switching between ACG and Hostinger by validating the shared `active-provider` file before trusting it, preferring live Hostinger when both providers are reachable, and recording/clearing Hostinger as the active provider during deploy/refresh/destroy; wrote `docs/bugs/2026-06-24-hostinger-provider-switch-stale-active-provider.md`; validated with `bash -n scripts/lib/provider.sh scripts/lib/providers/k3s-hostinger.sh`, `shellcheck -S warning scripts/lib/provider.sh scripts/lib/providers/k3s-hostinger.sh`, `bats scripts/tests/lib/provider_contract.bats` (`1..35`, all pass), `git diff --check`, and `./scripts/k3d-manager _agent_audit`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (Codex, 2026-06-24):** `fix(status)` commit `339152c1` on `feat/v1.8.0-acg-absorb-phase2-agy` separates preflight ArgoCD apps from the core Hostinger app table in `bin/cluster-status` so `green1-preflight-*` rows no longer read like Hostinger failure, and `scripts/lib/providers/k3s-hostinger.sh` now refreshes the local access layer on `make refresh CLUSTER_PROVIDER=k3s-hostinger` by restarting the cloudflared, browser, and pushgateway launchd services; documented the investigation in `docs/issues/2026-06-24-hostinger-status-access-layer-and-preflight-separation.md`; validated with `bash -n bin/cluster-status scripts/lib/providers/k3s-hostinger.sh`, `shellcheck -S warning bin/cluster-status scripts/lib/providers/k3s-hostinger.sh`, `git diff --check`, `./scripts/k3d-manager _agent_audit`, and `bats scripts/tests/lib/webhook.bats` (`1..29`, all pass); pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **INVESTIGATION (Codex, 2026-06-24):** The Prometheus `namespaces "monitoring" not found` failure was reproduced from a stale local LaunchAgent plist, not from the repo code. The live plist at `~/Library/LaunchAgents/com.k3d-manager.prometheus-port-forward.plist` still hardcoded `--context ubuntu-k3s`, while the repo template already targets `k3d-k3d-cluster`. Filed `docs/issues/2026-06-24-prometheus-port-forward-stale-launchagent.md` and updated `docs/howto/launchd-daemons.md` to match the current template. Manual follow-up remains `make install-prometheus-port-forward`. Commit `e45a9afe` captures the doc + investigation update.
- **COMPLETE (Codex, 2026-06-24):** `fix(webhook,status)` commit `e956c25c` on `feat/v1.8.0-acg-absorb-phase2-agy` makes `make status` provider-aware for Hostinger: `bin/cluster-status` now forwards `provider=` to `/api/v1/health`, `bin/k3dm-webhook` switches frontend/keycloak smoke probes to `frontend.3ai-talk.org` / `keycloak.3ai-talk.org` for `k3s-hostinger`, data-layer readiness uses the active app-cluster context, and `scripts/lib/provider.sh` plus `bin/k3dm-webhook` normalize the `k3s-hostiger` typo alias to `k3s-hostinger`; validated with `python3 -m py_compile bin/k3dm-webhook`, `bash -n bin/cluster-status`, `bash -n scripts/lib/provider.sh`, `shellcheck -S warning scripts/lib/provider.sh bin/cluster-status`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; bug doc: `docs/bugs/2026-06-24-hostinger-status-provider-aware-smoke.md`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (Codex, 2026-06-24):** `fix(observability)` commit `5afdebb1` on `feat/v1.8.0-acg-absorb-phase2-agy` resolves the active app-cluster context in `deploy_observability_acg()`, bootstraps `monitoring` before Alertmanager/Prometheus secrets are written, and uses the same context for the ACG Prometheus/pushgateway paths so Hostinger remains supported; validated with `shellcheck scripts/plugins/observability.sh`, `bats scripts/tests/lib/observability.bats`, `bash -n bin/cluster-up`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; bug doc: `docs/bugs/2026-06-24-observability-acg-monitoring-namespace-race.md`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **ACG ABSORPTION PHASE 2 SPEC WRITTEN (Claude, 2026-06-22) — ready for Codex:** lib-foundation v0.4.0 absorbed lib-acg as `scripts/lib/acg/` (merged to lib-foundation main `aed8c56`; post-merge done, retro written). k3d-manager Phase 2 = collapse the **two** subtrees (`scripts/lib/foundation/` + legacy `scripts/lib/acg/`) into **one**: re-pull lib-foundation (brings the acg module at `scripts/lib/foundation/scripts/lib/acg/`), `git rm -r scripts/lib/acg`, repoint the `scripts/plugins/acg.sh` stub + `scripts/plugins/gemini.sh` cdp source to the foundation acg module, fix template/test paths, remove the `lib-acg` remote. **Combined with** the gemini→agy retarget (user chose one spec): the CDP/session-check half is already migrated upstream (`acg_session_check.js` + `_antigravity_browser_ready`, no prompt wrapper), so only `scripts/plugins/gemini.sh` (Copilot-review automation) still calls `gemini` → retarget to `agy --dangerously-skip-permissions` (`_ensure_agy_cli`/`_antigravity_browser_ready` already in the foundation subtree; **agy prompt/model flags UNKNOWN — Codex must verify via `agy --help`**). Spec: `docs/plans/v1.8.0-acg-absorb-phase2-agy-rewire.md` on branch **`feat/v1.8.0-acg-absorb-phase2-agy`** (off `origin/main`). **Dependencies/notes:** (1) the `_sts_valid` one-line fix is a lib-foundation change (upstream-first, do on lib-foundation `feat/v0.4.1` before the subtree pull — do NOT edit the k3d-manager subtree copy); (2) the stale `v1.8.0-lib-acg-antigravity-migration-pointer.md` lives on `feat/eso-phase2-clustersecretstore` (commit `e9da2e4d`), NOT main — Part C (supersede it) applies only after branch convergence. **AWAITING USER HANDOFF to Codex.**
- **VERIFIED + PACKAGED + PHASE 2 SPEC'D (Claude, 2026-06-20):** Verified Codex's Fault A (`71a93e8b`) and Fault B (`7f3c5efd`) per the Codex Verification Protocol — both SHAs on origin, diffs match specs exactly (Fault A `eso.yaml` **byte-identical** to spec block; Fault B label added to both files), scope clean (no rabbitmq doc, no `-A` collateral), commit msgs verbatim, gates green (shellcheck 0, YAML parse + **live `kubectl apply --dry-run=client` accepted** `applicationset.argoproj.io/eso created (dry run)`, `_agent_audit` exit 0), memory-bank updated by Codex. **Branch-upstream footgun fixed:** all three branches (`feat/eso-app-cluster-generator`, `fix/hostinger-app-cluster-role-label`, `feat/eso-phase2-clustersecretstore`) were created via the GH API ref flow tracking `origin/main` — repointed each to its own remote branch (a bare push/pull would have hit main). **Packaging (user: one PR):** Fault A + B were on two separate branches; merged Fault B into `feat/eso-app-cluster-generator` (`--no-ff` `6c1eb550`, conflict-free, disjoint files) and pushed → **one combined PR** will cover both faults. Diff vs main: `eso.yaml` + cluster-secret label (2 files) + 2 specs + memory-bank. **Phase 2 spec WRITTEN:** `docs/plans/v1.8.0-eso-phase2-clustersecretstore-vault.md` on branch `feat/eso-phase2-clustersecretstore` (`cf37a41e`, off main). User decisions baked in: **dedicated Vault ingress host** `vault.3ai-talk.org` (Cloudflare TLS) + **k8s-auth per cluster**. Reuses existing `configure_vault_app_auth` (vault.sh:1314) + `_eso_configure_remote_vault` (eso.sh:199). **NOT Codex-ready yet** — 4 "Confirm Before Handoff" items pending user: (1) Vault local hop port (18200 LaunchAgent?), (2) public-Vault guardrail (Cloudflare Access?), (3) exact KV paths app ExternalSecrets read (least-privilege policy), (4) KV mount/version. **PR #97 OPEN + READY TO MERGE (2026-06-20):** https://github.com/wilddog64/k3d-manager/pull/97 (`feat(eso): app-cluster ESO operator generator + data-layer routing label`, head `de5496c0`). All gates pass: **CI green** (3/3 runs success), **Copilot reviewed** (1 finding — `cluster-secret.yaml.tmpl` is dead config; verified the fix correctly targets the Hostinger heredoc + `_argocd_set_active_app_cluster` runtime path; no code change; replied + thread resolved; documented `docs/issues/2026-06-20-copilot-pr97-review-findings.md`), **scope clean**, **live dry-run** accepted. **`enforce_admins` DISABLED on main** — awaiting USER merge (do NOT auto-merge). **AFTER MERGE:** run `/post-merge` (re-enable enforce_admins, sync main, next branch), apply the one-time operator steps (label live `cluster-ubuntu-hostinger`, delete orphan `data-layer` app), then resolve the 4 Phase 2 Confirm-Before-Handoff items and hand `feat/eso-phase2-clustersecretstore` to Codex.
- **COMPLETE (Codex, 2026-06-20):** Fault A on `feat/eso-app-cluster-generator` is complete and pushed. Commit `71a93e8b` adds `scripts/etc/argocd/applicationsets/eso.yaml` exactly per spec: cluster generator on `k3d-manager/role: app-cluster`, `external-secrets` chart `1.0.0`, namespace `secrets`, `installCRDs=true`, and `ServerSideApply=true`. Validation passed with `ARGOCD_NAMESPACE=cicd envsubst < scripts/etc/argocd/applicationsets/eso.yaml | KUBECONFIG=/dev/null kubectl create --dry-run=client --validate=false -f -`, `bats scripts/tests`, and `./scripts/k3d-manager _agent_audit`. Pushed to `origin/feat/eso-app-cluster-generator`.
- **COMPLETE (Codex, 2026-06-20):** Fault B on `fix/hostinger-app-cluster-role-label` is complete and pushed. Commit `7f3c5efd` adds `k3d-manager/role: app-cluster` to the Hostinger cluster-secret render in `scripts/lib/providers/k3s-hostinger.sh` and the generic template `scripts/etc/argocd/cluster-secret.yaml.tmpl`. Validation passed with `shellcheck scripts/lib/providers/k3s-hostinger.sh`, `ARGOCD_NAMESPACE=cicd envsubst < scripts/etc/argocd/applicationsets/eso.yaml | KUBECONFIG=/dev/null kubectl create --dry-run=client --validate=false -f -`, `bats scripts/tests/plugins/argocd_app_cluster_generator.bats`, `bats scripts/tests`, and `./scripts/k3d-manager _agent_audit`. Pushed to `origin/fix/hostinger-app-cluster-role-label`. Fault A was completed in the next block on `feat/eso-app-cluster-generator`.

- **COMPLETE (Codex, 2026-06-19):** Copilot PR #96 fixes landed on `k3d-manager-v1.7.1` commit `578a761c` (`fix(slack,vcluster): cluster-resume uses resolveProvider, drop rg test dep, justify preflight RBAC (Copilot PR#96)`). Applied the three exact spec blocks only: `/cluster-resume` now uses `resolveProvider(text, '')` and rejects invalid providers with a usage reply, the generator test now uses `grep -rF -- "ubuntu-k3s"` instead of `rg`, and the preflight RBAC annotation now carries the CLAUDE.md justification text while leaving the `rules` unchanged. Validation: `node --check workers/slack-relay/index.js` passed, `bats scripts/tests/plugins/argocd_app_cluster_generator.bats` passed, `./scripts/k3d-manager _agent_audit` passed; `bash -n scripts/tests/plugins/argocd_app_cluster_generator.bats` fails on BATS syntax and was documented in `docs/issues/2026-06-19-argocd-generator-bats-bash-n-failure.md`. Pushed to `origin/k3d-manager-v1.7.1`.
- **COMPLETE (Claude, 2026-06-19) — branch-protection consistency:** User flagged inconsistent "admin override". Audited `enforce_admins` across all 14 repos: 9 protected with `enforce_admins=true` (k3d-manager, rabbitmq-client-java, shopping-cart-basket/e2e-tests/frontend/infra/order/payment/product-catalog); **2 had NO branch protection at all** (observability-stack, rabbitmq-client-go); 3 have no `main` yet (rabbitmq-client-python/-dotnet/-library). Per user decision ("Just fix the 2 unprotected now"), applied protection to **observability-stack** and **rabbitmq-client-go** matching infra exactly (`enforce_admins=true`, required PR review count 1, dismiss_stale, no required status checks — neither has CI). Verified both now `enforce_admins=true reviews=1`. Note: this is the GitHub-side "admin override" (`enforce_admins`), separate from the hook's `ALLOW_MAIN_PUSH`. Left the 9 locked repos as-is per user's steady-state workflow (override OFF normally, toggled off→merge→on per PR).
- **COMPLETE + VERIFIED (Codex landed; Claude verified, 2026-06-19):** Tightened the pre-push guard in the four open repos on `chore/add-pre-push-hook`: added the `core.hooksPath` activation note + removed the `local_ref != refs/heads/main` exemption in `.githooks/pre-push`. Pushed: rabbitmq-client-python `e764fbd`, rabbitmq-client-java `47416ae`, rabbitmq-client-go `ce82ba7`, observability-stack `89ac986`. **Claude independent verify:** each SHA = head of `origin/chore/add-pre-push-hook`; scope = `.githooks/pre-push` only (+5/−2); all four hooks byte-identical (md5 `192e06bb…`, 797 B) and match the spec's expected result (both edits); LF-only, `bash -n` OK, `shellcheck` clean; ran the four guard checks myself → feature→main=1, **main→main=1 (the fix)**, override=0, non-main=0. Codex's `docs/issues/2026-06-19-pre-push-hook-verification-harness-hiccup.md` is a harmless self-note (his local harness used `status`, a zsh readonly var) — did NOT affect the hook. Copilot threads (2 per PR × 4) replied + resolved with the fix SHAs.
- **COMPLETE (Codex, 2026-06-19):** `bin/k3dm-webhook` commit `1c46232f` on `k3d-manager-v1.7.1` now passes codex answers through `--output-last-message` and reads the per-job `codex_last_message.txt` file on the codex path, so `/codex` no longer echoes the banner/prompt blob; validated with `python3 -m py_compile bin/k3dm-webhook` (clean) and `./scripts/k3d-manager _agent_audit` (clean). Push to `origin/k3d-manager-v1.7.1` succeeded. `make restart-webhook` was not run per spec.
- **HOOK ROLLOUT VERIFIED + CODEX-SLACK BUG SPEC (Claude, 2026-06-19):** Independently verified Codex's pre-push rollout: all 7 commits on origin (`observability-stack 7a532da`, `rabbitmq-client-go c3872bd`, `-java f38bea9`, `-python 0444b74`, `-dotnet 2f2953c`, `-library 0d447e3`, k3d-manager `e4acd5fa`); pre-push blob byte-identical everywhere (`410298b3`, mode 0755), feature→main exit 1 / main→main exit 0; k3d-manager pre-commit relocation byte-identical to original except `../../scripts`→`../scripts`, staged foundation edit rejected (exit 1), `_agent_audit` clean. Copilot reviewed (COMMENTED) on the 4 open PRs (observability #1, go #1, java #7, python #1) — 2 recurring valid notes: (a) local-main→remote-main still allowed (intended; branch protection backstops), (b) committed hooks need `core.hooksPath` (known; Claude-only activation). `-dotnet`/`-library` correctly have NO PR (empty GH repos, no `main` base ref — `docs/issues/2026-06-19-empty-rabbitmq-client-pr-base-ref.md`). **Diagnosed Slack `/codex` "same answer every time":** NOT the v1.6.5 thread-contamination bug (that fix is in code). Root cause = `codex exec` echoes banner+full prompt to stderr, `_posix_spawn_capture` merges stdout+stderr, parser doesn't strip it → message dominated by constant echoed system prompt. Verified codex CLI answers vary; `--output-last-message <FILE>` yields clean answer only. Spec `docs/bugs/2026-06-19-codex-slack-banner-echoes-prompt.md` (`bin/k3dm-webhook` only, branch `k3d-manager-v1.7.1`). **AWAITING USER HANDOFF to Codex;** `make restart-webhook` is Claude's post-verify step. Also flagged: `.claude/settings.local.json:565` has live `SLACK_BOT_TOKEN`/`SLACK_SIGNING_SECRET`/webhook hardcoded — rotate.
- **PARTIAL HOOK ROLLOUT (Codex, 2026-06-19):** Rolled out the committed pre-push hook to `observability-stack` PR #1 (`7a532da`), `rabbitmq-client-go` PR #1 (`c3872bd`), `rabbitmq-client-java` PR #7 (`f38bea9`), and `rabbitmq-client-python` PR #1 (`0444b74`); Copilot was tagged on each PR. `k3d-manager` commit `e4acd5fa` on `k3d-manager-v1.7.1` adds `.githooks/pre-push` and relocates pre-commit to `.githooks/pre-commit` with the path fixed to `../scripts`; direct invocation verification passed (`bash .githooks/pre-push origin` exited 1; staged `scripts/lib/foundation/CLAUDE.md` edit was rejected by `bash .githooks/pre-commit`). `./scripts/k3d-manager _agent_audit` passed. `rabbitmq-client-dotnet` and `rabbitmq-client-library` are blocked by empty GitHub repos with no `main` base ref, so `gh pr create --base main` fails (`Base ref must be a branch`); issue doc: `docs/issues/2026-06-19-empty-rabbitmq-client-pr-base-ref.md`. Awaiting user decision on whether to bootstrap those repos with a base branch or handle them specially.
- **PR OPEN + HOOK HARDENING (Claude, 2026-06-19):** Reviewed Codex's data-layer app-cluster work (`2d3af51c`/`df80415`/`d2c1ea53`) — correct & faithful to spec; both SHAs on origin; shellcheck/bats independently re-verified green. Created **shopping-cart-infra PR #84** (`fix/data-layer-cluster-generator` → main, Copilot tagged, NOT merged — must merge before k3d-manager cutover per finalizer hazard). **Branch-hygiene incident found:** infra + frontend local branches were tracking `origin/main` (bare `git push` would hit main); root cause that the committed pre-push guard never fired = `core.hooksPath=/dev/null` set on infra/frontend/product-catalog (agent hook-bypass). Fixed locally: both upstreams repointed, activated `.githooks` on infra/payment (order already active), installed local stopgap pre-push on frontend/product-catalog; all guards verified (feature→main exit 1). Wrote Codex spec `docs/bugs/2026-06-19-pre-push-hook-block-main-rollout.md` to commit `.githooks/pre-push` durably across ALL shopping-cart repos + k3d-manager (k3d-manager also relocates pre-commit into `.githooks`). **AWAITING USER HANDOFF to Codex.** Activation (`core.hooksPath`) is Claude-only post-merge.
- **COMPLETE:** data-layer app-cluster parameterization via ArgoCD cluster generator — k3d-manager commit `2d3af51c` on `k3d-manager-v1.7.1` and shopping-cart-infra commit `df80415` on `fix/data-layer-cluster-generator`; spec: `docs/bugs/v1.7.1-bugfix-data-layer-app-cluster-generator.md`; `scripts/etc/argocd/applicationsets/services-git.yaml` now uses a `matrix` generator (git × `clusters` selector `k3d-manager/role: app-cluster`) and template destination `name: '{{.name}}'`, `scripts/etc/argocd/applicationsets/data-git.yaml` was added for the shopping-cart-infra `data-layer` app with the same cluster selector, `scripts/plugins/argocd.sh` now adds `argocd.argoproj.io/cluster-name` to registered cluster secrets, defines `_argocd_set_active_app_cluster`, and auto-asserts the active app-cluster label from `_acg_provider_context "$(_acg_resolve_provider)"` with fallback to `APP_CLUSTER_NAME`, `scripts/tests/plugins/argocd_app_cluster_generator.bats` guards the static manifests, and `shopping-cart-infra/argocd/applications/data-layer.yaml` was removed; validation used `shellcheck -S warning scripts/plugins/argocd.sh` (clean), `bats scripts/tests/` (clean), `bats scripts/tests/plugins/argocd.bats scripts/tests/plugins/argocd_app_cluster_generator.bats` (18/18 green), `./scripts/k3d-manager _agent_audit` (clean), and confirmed both SHAs exist on origin; the initial broad manifest-guard attempt matched `scripts/etc/argocd/projects/platform.yaml.tmpl`, so the guard was narrowed to `applicationsets/` and documented in `docs/issues/2026-06-19-data-layer-app-cluster-generator-test-scope-drift.md`; live cutover remains Claude-only and was not run.
- **MERGED BOTH GO-REWRITE PR1s (2026-06-19, post-merge automated):** shopping-cart-payment PR #23 merged to main (`ff817b8`, squash commit `08f48f1`); shopping-cart-order PR #33 merged to main (`81401ba`, squash commit `55d7b57`). Both enforce_admins restored to `enabled=true`. feat/go-rewrite-pr2 branches created on both repos with retrospectives committed (payment `43b289f`, order `ca59861`). Next: PR2 specs (Keycloak JWT/JWKS + role-based access control).
- **COMPLETE:** payment round-5 PCI hardening — shopping-cart-payment branch `feat/go-rewrite` commit `08f48f1` (`fix(payment): zero raw card fields on request param after gateway call (PCI)`) adds four `req.*` zeroing lines immediately after the gateway call in `go/internal/payment/service.go` so the by-value inbound request no longer retains PAN/CVV/expiry in locals through the `persist`/`RunInTx` path; validation used Dockerized Go toolchains for `gofmt -l go/` (clean), `go build ./...` (green), `go vet ./...` (green), `go test ./... -race -cover` (green), `golangci-lint run` (green), and `./scripts/k3d-manager _agent_audit` (clean); branch pushed successfully to `origin/feat/go-rewrite`.
- **PAYMENT #23 ROUND-5 SPEC WRITTEN (Claude, 2026-06-19), awaiting handoff to Codex:** User flagged payment #23 still has an unaddressed comment. Found **1 unresolved Copilot thread** (`service.go:101`, `PRRT_kwDORUaaec6K2FGG`, comment `3442769663`, posted 13:28 — a NEW round-5 finding after round-4): R4 zeroed the gateway copy `request.Card*` but the inbound **`req` (`ProcessPaymentRequest`, by-value)** still holds raw PAN/CVV/expiry in `req.CardNumber`/`req.CardCvc`/`req.CardExpMonth`/`req.CardExpYear` through the `persist`/`RunInTx` DB-write path → violates PCI no-retain rule. Verified against `307ddf3` code (service.go:97-104 zeroes only `request`; `req` is by-value param, nothing after the gateway call reads `req`). Wrote spec `docs/bugs/2026-06-19-bugfix-copilot-pr23-round5.md` (work repo `shopping-cart-payment` branch `feat/go-rewrite`, **`service.go` ONLY**): add 4 `req.*` zeroing lines after the gateway call (note field is `CardCvc` not `CardCVC`). Commit msg `fix(payment): zero raw card fields on request param after gateway call (PCI)`. **AWAITING USER HANDOFF to Codex.** Thread NOT resolved yet (resolve after fix lands + CI green). enforce_admins on payment #23 already disabled (was ready-to-merge pre-round5; now blocked again on this thread).
- **VERIFIED + THREAD RESOLVED (Claude, 2026-06-19):** Independently verified order vhost fix `55d7b57` on `origin/feat/go-rewrite` (Codex Verification Protocol): SHA is HEAD of the branch (extends `1b84696`), diff matches the spec's exact new block verbatim, only `go/internal/config/config.go` (+8/−6) + NEW `config_test.go` (+36) touched (no scope creep), commit msg exact; **CI green on `55d7b57`** (Go CI run `27829140644` push + Go CI + Java CI on PR-trigger all `success`) — authoritative gate since local Go toolchain is a broken homebrew symlink. memory-bank `194a1e53` clean (only the 2 mb files). Then replied to Copilot thread `PRRT_kwDORUakXc6Kz8rH` (comment `3442010114`, reply `3442889787`) with the fix SHA and **resolved it via GraphQL → 0 unresolved threads remain on order #33**. Re-requested copilot reviewer not needed (single thread closed). enforce_admins on order #33 still untouched (`behind` main + GitGuardian-pin/admin-override task pending — see below).
- **COMPLETE:** order RabbitMQURI vhost double-escape fix — shopping-cart-order branch `feat/go-rewrite` commit `55d7b57` (`fix(order): RabbitMQURI vhost double-escape — set URL Path + RawPath`) updates `go/internal/config/config.go` so `RabbitMQURI()` uses decoded `Path` plus encoded `RawPath` instead of pre-escaping into `Path`, preserving the default `/` vhost as `/%2F` and keeping named vhosts as `/<name>`; `go/internal/config/config_test.go` was added with default/empty/named vhost cases to guard against `%252F`; validation used Dockerized Go toolchains for `gofmt -l .` (clean), `go build ./...` (green), `go vet ./...` (green), `go test ./... -race -cover` (green), `golangci-lint run` (green), and `./scripts/k3d-manager _agent_audit` (clean); branch pushed successfully to `origin/feat/go-rewrite`.
- **PAYMENT #23 ADMIN OVERRIDE ENABLED + ORDER #33 BUG SPEC WRITTEN (Claude, 2026-06-19):** Per explicit user go, disabled `enforce_admins` on `shopping-cart-payment` main (`gh api .../enforce_admins -X DELETE` → confirmed `enabled:false`) — **payment #23 (`307ddf3`) is ready for the user to merge** (CI green, 0 unresolved, round-4 verified). NOT merged by Claude (never-auto-merge). **Order #33** had 1 unresolved Copilot thread (`config.go:92`, `PRRT_kwDORUakXc6Kz8rH`): `RabbitMQURI()` **double-escapes the vhost** — `url.PathEscape("/")`→`%2F` assigned to `url.URL.Path` (decoded field) → `String()` re-escapes `%`→ `amqp://.../%252F`; default `RABBITMQ_VHOST="/"` so it breaks the **default** deployment (RabbitMQ sees vhost `%2F`, not found). Wrote spec `docs/bugs/2026-06-19-bugfix-order-rabbitmq-vhost-double-escape.md` (work repo `shopping-cart-order` branch `feat/go-rewrite`, `config.go` + NEW `config_test.go`): fix sets `u.Path` (decoded) + `u.RawPath` (encoded) so `String()` emits `/%2F` for default, `/<name>` for named; commit msg `fix(order): RabbitMQURI vhost double-escape — set URL Path + RawPath`. **DONE — fix landed (`55d7b57`), verified, thread resolved (see top entry).** enforce_admins on order #33 left as-is (still `behind` main + carried GitGuardian-pin/admin-override).

- **COMPLETE:** payment Copilot round-4 hardening — shopping-cart-payment branch `feat/go-rewrite` commit `307ddf3` (`fix(payment): Copilot round-4 hardening — currency trim, tx error cause, locked-row gateway IDs, card zeroing, DB_SSLMODE wiring`) applied the spec’s R1/R2/R3/R4/R5/R6 edits: `service.go` now normalizes currency once via `strings.ToUpper(strings.TrimSpace(req.Currency))` and zeros raw card fields immediately after gateway processing, `refund.go` now sources gateway identifiers from the locked `FOR UPDATE` row, `store.go` now preserves the original error when rollback also fails in `RunInTx`, `service_test.go` now asserts the trimmed currency result, and the base ConfigMap/Deployment now wire `db.sslmode` → `DB_SSLMODE` with the Istio-mTLS rationale comment; validation used Dockerized Go toolchains for `gofmt -l` (clean), `go build ./...` (green), `go vet ./...` (green), `go test ./... -race -cover` (green), `golangci-lint run` (green), `kubectl apply --dry-run=client -k k8s/base` (green), and `./scripts/k3d-manager _agent_audit` (clean); branch pushed successfully to `origin/feat/go-rewrite`.
- **ROUND-4 SPEC WRITTEN + 2 threads resolved (Claude, 2026-06-19):** Copilot posted a **round-4** review on payment #23 HEAD `0fda755` (2026-06-19T11:33) with **7 inline findings** — materially different from round-3 (real code bugs, not design rationale). Triaged + read the code at `0fda755`: (1) `service.go:79` currency stored/forwarded `ToUpper` without `TrimSpace` (`" usd"`→`" USD"`); (2) `store.go:73` `RunInTx` drops the original failure cause when rollback also fails; (3) `refund.go:114` `gatewayRequest` built from the non-locked read, not the `FOR UPDATE` row; (4) `service.go:99` raw card fields (esp. `CardCVC`) held in `request` past the gateway call (PCI); (5) `deployment.yaml:97` DB_SSLMODE not wired (outdated anchor); (6) `configmap.yaml:20` add `db.sslmode` key; (7) `mock.go:185` split Mock/Stripe/PayPal per-file. Per **user decision ("Spec to Codex: #1-4,#6")**: wrote `docs/bugs/2026-06-19-bugfix-copilot-pr23-round4.md` (exact old/new blocks, work repo `shopping-cart-payment` branch `feat/go-rewrite`, target `service.go`/`refund.go`/`store.go`/`configmap.yaml`/`deployment.yaml`, commit msg `fix(payment): Copilot round-4 hardening — currency trim, tx error cause, locked-row gateway IDs, card zeroing, DB_SSLMODE wiring`) fixing #1-4 + R5/R6 (DB_SSLMODE wiring). **#5 + #7 handled by Claude now (no code):** replied + resolved both via GraphQL — #7 deferred to PR2 (Stripe/PayPal are PR1 stubs; split when real logic lands), #5 outdated (description fixed round-3; wiring lands via R5). **VERIFIED — PASS (Claude, 2026-06-19):** Codex landed `307ddf3` (branch HEAD, exact commit msg); independently confirmed all 5 diffs match the spec byte-for-byte (R1 single `currency` var both call sites, R2 `fmt.Errorf("%w (additionally, rollback failed: %v)", …)`, R3 `gatewayRequest` rebuilt inside `persist` from `locked`, R4 4 card fields zeroed after `ProcessPayment`, R6 `db.sslmode` key + Istio-mTLS comment, R5 `DB_SSLMODE` from `configMapKeyRef`); scope clean (5 spec files + a +3/-0 currency assertion in `service_test.go` sending `"usd"`→asserting `"USD"`, authorized by spec step 13; k3dm `4395b655` touched only memory-bank, no code bundled); **CI all green** on `307ddf3` (go×2 / integration×2 / Build and Test / Integration Tests / Checkstyle / Code Quality / GitGuardian / Security Scan / Validate PR). Replied-with-SHA + resolved the 4 still-open threads (R1-currency was already resolved) → **0 unresolved on #23**. Re-requested `copilot-pull-request-reviewer[bot]`; `mergeable_state=blocked` (awaiting Copilot re-clear). enforce_admins UNTOUCHED — never-auto-merge prepare-and-stop; awaiting user go after Copilot re-clears.
- **COMPLETE — Copilot PR #23 round-3 threads addressed + resolved (Claude, 2026-06-19):** Copilot posted a **round-3** review against PR #23 HEAD `0fda755` with **4 unresolved findings** (2026-06-19T11:02): (1) `k8s/base/configmap.yaml:20` `mock.gateway.enabled:"true"` in base, (2) `refund.go:114` gateway call inside the `FOR UPDATE` tx, (3) `refund.go:33` correlation-id refund idempotency race, (4) `README.md:115` PR-scope description mismatch. Per user decision (**"Resolve all 3 w/ rationale"**): fixed #4 directly by rewriting the **PR #23 description Scope section** to list the real k8s/README/docs changes (mock-only PR1 gateway; `DB_SSLMODE=disable` = Istio mTLS); replied to #1/#2/#3 with rationale — #1 mock-in-base is the only implemented gateway (Go default `false`, base is the explicit dev opt-in; prod overlay-gating → PR2), #2 in-tx gateway call deliberately preserves the A6 over-refund race-closure (saga/two-phase → PR2), #3 over-refund already prevented by `FOR UPDATE`+`remaining`; residual is a bounded duplicate-record idempotency leak under concurrent same-correlation-id (unique partial index + in-tx recheck → PR2). Resolved all 4 via GraphQL — **0 unresolved on #23**. PR2 follow-ups (mock overlay-gating + refund saga + correlation-id idempotency) added to PR description Notes. CI green (go/go-ci + integration + Security Scan). Re-requested `copilot-pull-request-reviewer[bot]`; `mergeable_state=blocked` (awaiting re-clear). **enforce_admins UNTOUCHED — never-auto-merge prepare-and-stop.**
- **COMPLETE — Codex round-2 work VERIFIED + Copilot threads resolved (Claude, 2026-06-19):** Independently verified payment `0fda755` and order `1b84696` on `origin/feat/go-rewrite` (SHAs exist, exact commit msg, file scope clean — payment 11 files, order 7 incl migration `git mv`, no creep; order `config.go:47` sslmode default UNTOUCHED per spec prohibition; all A1–A7/B1–B6 diffs match spec line-for-line incl A6 `FOR UPDATE`+`RunInTx` refund rewrite, B6 `ANY($1::uuid[])` batch, B2 `pubMu`). Go gates not runnable locally (no Go on host) but **PR CI green on both** (go/go-ci + integration + GitGuardian), `kubectl kustomize k8s/base` renders both, `_agent_audit` clean. memory-bank `477af5e7` records both SHAs (stale OPEN row replaced, no dup). Then replied to all **17 round-2 inline threads** per-finding (payment 11→`0fda755`, order 6→`1b84696`; B5 sslmode got Istio-mTLS rationale) and resolved the 12 still-unresolved via GraphQL — **0 unresolved remain both PRs**. Re-requested `copilot-pull-request-reviewer[bot]` on #23 + #33. **enforce_admins UNTOUCHED on both — never-auto-merge prepare-and-stop:** payment #23 `mergeable_state=blocked` (awaiting Copilot re-clear), order #33 `mergeable_state=behind` (branch behind main; also carried GitGuardian-pin + admin-override task pending). Awaiting user go after Copilot re-clears.
- **COMPLETE:** Copilot round-2 Go-rewrite hardening finished — payment repo `shopping-cart-payment` branch `feat/go-rewrite` commit `0fda755` and order repo `shopping-cart-order` branch `feat/go-rewrite` commit `1b84696` landed the 17 spec fixes from `docs/bugs/2026-06-18-bugfix-copilot-pr-round2-hardening.md`: payment A1–A7 (mock PAN sentinels, idempotency/order short-circuit before gateway selection, sensitive `req` removed from persist path, 4xx-mapped router errors + generic 500 with cause logging, removed `RateLimitPerMinute`, transactional refund re-check with `GetPaymentForUpdate`, and mock gateway defaulted off with explicit configmap opt-in) plus order B1–B6 (RabbitMQ creds default empty, serialized publishes, merged CHANGELOG `### Added`, moved the Go-only migration fixture to `go/internal/order/testdata/`, explicit DB_SSLMODE configmap documentation, and batched `ListByCustomer` grouping); validation used Dockerized Go toolchains for `gofmt -l` (clean), `go build ./...` (green), `go vet ./...` (green), `go test ./...` (green), `kubectl kustomize k8s/base` renders, and `./scripts/k3d-manager _agent_audit` (clean); both branches were pushed to `origin/feat/go-rewrite`.
- **COMPLETE:** Copilot PR review findings docs backfill — payment repo `shopping-cart-payment` branch `feat/go-rewrite` commit `e2adc72` and order repo `shopping-cart-order` branch `feat/go-rewrite` commit `b45ed2b` both added the missing per-PR findings docs plus the README Issue Logs updates required by `docs/bugs/2026-06-18-bugfix-pr-findings-docs-backfill.md`; payment created `docs/issues/2026-06-18-copilot-pr23-review-findings.md` and updated the README Issue Logs top 5 to include PR #23 and PR #19, while order created `docs/issues/2026-06-16-copilot-pr33-review-findings.md` and updated the README Issue Logs top 5 to include PR #33 and keep the five most recent entries by date; validation used `./scripts/k3d-manager _agent_audit` clean plus commit/file checks confirming payment changed only `README.md` and the new PR #23 issue doc, and order changed only `README.md` and the new PR #33 issue doc; both branches were pushed to `origin/feat/go-rewrite`.
- **COMPLETE — Copilot PR #23 threads addressed + resolved (Claude, 2026-06-18):** Copilot's 28-finding "⚠️ Not ready to approve" review (id `4524340362`) was against commit `736028dd` (pre-hardening); PR #23 HEAD is now `f39fba1` (the verified-PASS hardening commit), which already fixes every actionable finding. Replied to all **23 unresolved** inline threads (3 were already resolved) referencing `f39fba1` per-finding, then resolved all 23 via GraphQL — **0 unresolved remain**. Two findings replied as **deliberately DECLINED w/ rationale** (not fixed): mandatory-idempotency-key (`handler.go:49`) and 201-for-all-states (`handler.go:59`) — both break the Java/e2e contract; double-charge risk is instead closed by the unique-violation race fix. Re-requested `copilot-pull-request-reviewer[bot]` so it re-evaluates `f39fba1`. PR still `mergeable_state: blocked`. `enforce_admins` still **true — UNTOUCHED** (never-auto-merge; will prepare-and-stop only after Copilot re-clears). DoD-gap doc now landed (backfill `e2adc72`); Copilot has since posted a **round-2** review (`4529362992`, 11 new findings) — see round-2 hardening spec row at top.

- **COMPLETE:** payment Go PR1 Copilot hardening — shopping-cart-payment branch `feat/go-rewrite` commit `f39fba1` (`fix(payment): harden Go PR1 per Copilot review (gateways, crypto, config, tx)`) landed the 14 spec changes from `docs/bugs/2026-06-18-bugfix-payment-go-pr1-copilot-hardening.md`, including hardcoded-secret cleanup, fail-fast encryption, fail-fast gateway selection, mock fail-fast stubs, request-context readiness, bounded rate limiting, transaction wrapping, and stripped PAN/CVV literals in the touched tests; validated with the repo Go test sweep plus a green integration run against a real Postgres seeded from the Java Flyway migrations; PR #23 GitHub Actions run `https://github.com/wilddog64/shopping-cart-payment/actions/runs/27796636227` finished green with both the `go` job (`.../job/82257747892`) and the `integration` job (`.../job/82257747894`) successful; branch pushed successfully to `origin/feat/go-rewrite`.
- **PR OPEN — Copilot NOT ready to approve (Claude, 2026-06-18):** opened **shopping-cart-payment PR #23** (`feat/go-rewrite`→`main`, mirrors order #33) for the 4-commit Go-rewrite PR1 (`ddc6c82`→`f2e47ff`→`6ee63e3`→`736028d`). Scope clean (only `go/` + `go-ci.yml` + `CHANGELOG.md`, 0 behind main). **All CI green** (Go `go`+`integration`, Java Build/Checkstyle/Integration/Validate PR, GitGuardian, Security Scan). Copilot tagged → returned **"⚠️ Not ready to approve" with 28 inline findings**. `enforce_admins` still **true** (untouched — NOT merge-ready). Triage groups: (A) hardcoded cred defaults in `config.go` (DB `changeme123`, RabbitMQ guest/guest, Stripe/PayPal placeholder keys + enabled-by-default, `sslmode=disable`); (B) **CRITICAL — gateway stubs return fake success while enabled** (`mock.go` Stripe :136 / PayPal :175 mark paid without charging; `gateway.go:37` silent mock fallback; `main.go:49` mock always enabled) — echoes my `ddc6c82` verification flag #6; (C) encryption (`encryption.go` :37 random key when ENCRYPTION_KEY empty → fail-fast; :63/:86 gate on `IsEnabled()` not `enabled`) — echoes flag; (D) correctness (idempotency optional `handler.go:49` + race `service.go:38`; no DB txn around multi-write `service.go:77`; 202-vs-201 `handler.go:59`; readiness `context.Background()` `health.go:40`; scan ignores parse err `store.go:458`; unbounded rate-limiter map `middleware.go:45`; NUMERIC scale unvalidated `handler.go:203/213`); (E) PCI — full PAN/CVV literals in 5 test files. Next: triage into a Codex bugfix spec (fix PR1-blockers, defer real Stripe/PayPal to PR2 but make stubs fail-fast/disabled). NOT merged.
- **VERIFIED — PASS (Claude, 2026-06-18):** independently confirmed BOTH CI-gate SHAs on `origin/feat/go-rewrite` (not the reported values). **payment `736028d`** — only `.github/workflows/go-ci.yml` touched (29 ins / 0 del; existing `go` job byte-for-byte unchanged), diff identical to spec block, workflow-level `permissions: contents: read` intact + no per-job permissions block, YAML parses, exact commit msg; Actions run `27753447269` `integration` job green incl. the `go test -tags integration ./internal/payment/...` step. **order `ac85ad5`** — exactly the 3 spec files (101 ins / 0 del), self-seed + Postgres/RabbitMQ(`app/app`) job + both DSN/URI envs match spec; `V1__init_schema.sql` column types differ from the spec's *draft* (status VARCHAR(255), total_amount/unit_price NUMERIC(10,2), updated_at nullable) = the **required DDL↔JPA reconciliation**, proven correct by **Java CI green** (`27753450427`, Hibernate `ddl-auto: validate`); Go CI run `27753449878` `integration` job green. **PR #33 state:** all required checks (`Build & Test`, `Checkstyle`) + go-ci + integration + **GitGuardian green** ("no secrets present anymore"); `enforce_admins` already **disabled** (admin override on); blocker is `mergeStateStatus: BEHIND` (strict mode) — admin can bypass or update-branch. GitGuardian "won't resolve" = lingering *dashboard incident* on `66f7e0e`'s throwaway `"postgres"` default (PR check is green, not blocking) → clear via squash-merge (drops `66f7e0e` from main) or dashboard-resolve. NOT merged (never-auto-merge — awaiting user go).
- **COMPLETE:** shopping-cart-payment + shopping-cart-order Go CI integration gates — payment repo `shopping-cart-payment` branch `feat/go-rewrite` commit `736028d` added the new `.github/workflows/go-ci.yml` `integration` job (Postgres 16 service + `go test -tags integration ./internal/payment/...`) while leaving the existing `go` job unchanged; the branch push produced GitHub Actions run `https://github.com/wilddog64/shopping-cart-payment/actions/runs/27753447269` and its `integration` job finished green (`.../job/82109342359`, with the existing `go` job green at `.../job/82109342363`). order repo `shopping-cart-order` branch `feat/go-rewrite` commit `ac85ad5` added `src/main/resources/db/migration/V1__init_schema.sql`, self-seeded `go/internal/order/store_integration_test.go`, and added the `.github/workflows/go-ci.yml` `integration` job (Postgres + RabbitMQ); the branch push produced `Go CI` run `https://github.com/wilddog64/shopping-cart-order/actions/runs/27753449878` and `Java CI` run `https://github.com/wilddog64/shopping-cart-order/actions/runs/27753450427`, both green (order push-run `Go CI` `https://github.com/wilddog64/shopping-cart-order/actions/runs/27753447696` also green). Validation used Dockerized Go integration tests for payment and order, `gofmt -l internal/order/store_integration_test.go`, `go vet -tags integration ./...`, `python3` YAML parsing via a disposable `/tmp/k3dm-pyyaml` venv, and `./scripts/k3d-manager _agent_audit`; local Maven `verify` for order was blocked by GitHub Packages auth (`com.shoppingcart:rabbitmq-client:1.0.1` 401), recorded in `docs/issues/2026-06-17-order-java-maven-verify-github-packages-401.md`; both branches were pushed to `origin/feat/go-rewrite`.
- **SPEC WRITTEN (Claude, 2026-06-17), awaiting handoff to Codex:** order Go CI integration gate + missing DB schema — spec `docs/bugs/2026-06-17-bugfix-order-go-ci-integration-gate.md`; work repo `shopping-cart-order` branch `feat/go-rewrite` (PR #33). Runtime-verified the order rewrite has NO DB schema at all (no migrations, `ddl-auto: validate`, store doesn't create tables): `TestPostgresStoreRoundTrip` FAILS on clean Postgres with `relation "orders" does not exist`; `TestRabbitPublisherPublishesOrderCreated` PASSES against vanilla rabbitmq:3.13. Fix (3 files): NEW `src/main/resources/db/migration/V1__init_schema.sql` (orders+order_items DDL derived from store.go SQL constants), self-seed it in `store_integration_test.go` (mirror payment), and add a go-ci `integration` job with Postgres+RabbitMQ services running `-tags integration ./internal/order/... ./internal/events/...`. Codex must reconcile DDL types with the Java JPA entities so Hibernate validate passes. DoD blocks on the new job green + Java CI green. User chose Full fix over RabbitMQ-only/defer.
- **SPEC WRITTEN (Claude, 2026-06-17), awaiting handoff to Codex:** payment Go CI integration-test gate — spec `docs/bugs/2026-06-17-bugfix-payment-go-ci-integration-gate.md`; work repo `shopping-cart-payment` branch `feat/go-rewrite`, file `.github/workflows/go-ci.yml` ONLY. Root cause: `go-ci` ran only `go test ./... -race -cover`, which compiles out the `//go:build integration` test, so the double-advance regression in `f2e47ff` stayed invisible to CI (that test only runs by hand). Fix adds an `integration` job: `postgres:16-alpine` service container + `go test -tags integration ./internal/payment/...` (test self-seeds the real Flyway migrations). DoD blocks on the new job showing green in the `go-ci` run, not just the push. Verified during this session: `go-ci.yml` has no `-tags integration`; no vCluster preflight CI exists yet (roadmap v1.1.1) and no `repository_dispatch` listener — opening the payment PR triggers only in-repo Java + Go CI, not any vCluster test.
- **COMPLETE:** payment list-scanner double-advance fix + order rewrite config hardening already present — payment repo `shopping-cart-payment` branch `feat/go-rewrite` commit `6ee63e3` now points both list loops at singular `scanPaymentRow`/`scanRefundRow` and deletes the wrappers in `go/internal/payment/store.go`; `go test -tags integration ./internal/payment/...` passed against a throwaway Postgres seeded with the real Java Flyway migrations; order repo `shopping-cart-order` branch `feat/go-rewrite` was already at matching commit `c5a5d95` for the requested `DB_PASSWORD` default removal, `DB_SSLMODE`/`DBSSLMode`, canonical AMQP dialer, and `ErrOrderNotFound` row-count guard, so no additional order diff was needed in this run; `./scripts/k3d-manager _agent_audit` passed; payment branch pushed to `origin/feat/go-rewrite`.
- **COMPLETE:** argocd-port-forward plist regeneration — k3d-manager commit `3e149449` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.7.1-bugfix-refresh-argocd-pf-plist-regen.md`; `bin/cluster-refresh` now regenerates `~/Library/LaunchAgents/com.k3d-manager.argocd-port-forward.plist` from `${_ACG_STATE_DIR}/bin/argocd-port-forward.sh` when the plist is missing, mirroring the keycloak/grafana blocks, then continues into `_launchd_ensure`; validation used `shellcheck -S warning bin/cluster-refresh`, `bash -n bin/cluster-refresh`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(refresh): regenerate missing argocd-port-forward plist from wrapper`
- **VERIFIED + E2E PASS (Claude, 2026-06-17):** `3e149449` (memory-bank `5f87175c`) VERIFIED on origin — diff = exactly the keycloak-mirror regen block + `_launchd_ensure` retained after the guard (scope clean, `bin/cluster-refresh` only, +33/-2), commit body + trailer intact; `shellcheck -S warning` rc=0, `bash -n` OK. **Live e2e (Claude):** killed the manual stopgap wrapper, removed agent + plist (`:8080` dead, proper path `000`) → `make refresh` → log printed `ArgoCD port-forward plist missing — regenerating from wrapper...` then `argocd-port-forward: loaded`; NO `Bootstrap failed: 5` for any daemon. Surface confirmed: plist regenerated (711 B), agent in `launchctl list` (pid 84840), `:8080` served by launchd-managed kubectl (pid 83436), `https://argocd.shopping-cart.local/healthz`=**200** + root=**200**, socat log clean (`healthz reachable — monitoring backend availability`, no `Connection refused`). **The third bug is fully resolved — proper ArgoCD path is now launchd-managed (no manual wrapper needed). The `:8081` fallback can be torn down.** All three v1.7.1 refresh self-heal fixes (`ee9d7a7e` cert/wrapper, `424e46e1` bootout, `3e149449` pf-plist regen) now PROVEN end-to-end together.
- **SPEC WRITTEN (Claude, 2026-06-17): payment Go rewrite PR1** — spec `docs/plans/shopping-cart-payment-go-rewrite-pr1.md` (work repo `shopping-cart-payment`, branch `feat/go-rewrite`). Mirrors order PR1 (`shopping-cart-order-go-rewrite-pr1.md`). Payment-specific contract captured from live Java source: HTTP `/api/v1/payments` (process 201/202 + idempotency keys, get variants, refunds), Flyway-owned 4-table schema (payments/refunds/payment_methods/transactions, `NUMERIC(19,4)` scale **4**), DTO JSON shapes, PaymentStatus/RefundStatus/TransactionType enums, PaymentGateway interface + Router (default mock), AES-256-GCM EncryptionService/PciDataMasker, port 8084. PR1 = functional core + MOCK gateway + idempotency + refunds + audit + encryption + actuator + rate-limit, `OAUTH2_ENABLED=false` (open); PR2 (NOT this) = Keycloak JWT + `@PreAuthorize` role matrix + Stripe/PayPal + Vault creds. Java+Go side-by-side under `go/`, zero deploy risk. READY FOR CODEX (user hands off). NOTE: order PR2 (JWT) spec still pending too.
- **COMPLETE:** shopping-cart-payment Go rewrite PR1 — work repo `shopping-cart-payment` branch `feat/go-rewrite` from `origin/main`; commit `ddc6c82` (`feat(payment): Go rewrite PR1 — functional core (HTTP + Postgres + mock gateway + refunds)`) added the side-by-side `go/` module, `go/Dockerfile`, `go/Dockerfile.local`, additive `.github/workflows/go-ci.yml`, and a contract-preserving HTTP + Postgres + mock-gateway + refunds implementation; the Go module root is `go/` (`module github.com/wilddog64/shopping-cart-payment/go`), `go.sum` was generated, and validation used Dockerized Go toolchains for `gofmt -l .` (clean), `go vet ./...` (clean), `go test ./... -race -cover` (all pass; no tests), and `docker build -f go/Dockerfile go` (success); branch pushed successfully to `origin/feat/go-rewrite`.
- **COMPLETE:** shopping-cart-payment Go rewrite PR1 follow-up — work repo `shopping-cart-payment` branch `feat/go-rewrite`; commit `f2e47ff` (`test(payment): PR1 follow-ups — tests, CHANGELOG, CI gates, amount JSON number shape`) added the response-only fixed-scale amount marshaler (`PaymentResponse.amount` / `RefundResponse.amount` now emit unquoted JSON numbers at scale 4), the PR1 unit test suite (status machines, idempotent replay, refund guards, mock-gateway paths, encryption/masking, handler error mapping, amount-shape assertions), a build-tagged Postgres integration test against the Java Flyway schema, `CHANGELOG.md` `[Unreleased]`, `.github/workflows/go-ci.yml` `permissions: contents: read` + pinned `golangci-lint` gate, and `go/.golangci.yml`; confirmed against Java before changing that gateway defaults already match (`STRIPE_ENABLED`/`PAYPAL_ENABLED` stay true) and DB startup stays fail-fast like the Java service, so no code change was needed there; validation used Dockerized Go toolchains for `gofmt -l go/` (clean), `go vet ./...` (clean), `golangci-lint run` (clean), `go test ./... -race -cover` (pass), and `docker build -f go/Dockerfile go` (success); branch pushed successfully to `origin/feat/go-rewrite`.
- **VERIFIED — RUNTIME PASS / DoD INCOMPLETE (Claude, 2026-06-17):** payment Go rewrite `ddc6c82` VERIFIED on `origin/feat/go-rewrite` (real SHA, is branch HEAD, correct commit message + trailer). **Scope clean** — all changes confined to `go/`, `.github/workflows/go-ci.yml` (Java tree `src/**`/`pom.xml`/root Dockerfile/`db/migration/**` untouched), +2583/-0, 19 files. **Contract faithful:** decimal scale 4, port 8084, mock env vars, AES-256-GCM (12B nonce/16B tag), full health endpoint set, rate limiter + complete security-header set, idempotency returns existing payment, refund over-refund guard, 201/202 split; card exp fields are `string` — **matches Java DTO exactly** (my first runtime test wrongly sent numbers; Go correctly rejected). **Runtime PROVEN (Docker build + run vs throwaway Postgres w/ real Flyway V1+V2 schema):** image builds, `/actuator/health|liveness|readiness` all 200, `POST /api/v1/payments` mock → **201 COMPLETED**, card masked to `4242`/`visa` (no PAN), idempotency repeat → same id, over-refund 999 → 400, refund 10.00 → 200, DB persisted 3 payments / 4 transactions (audit) / 1 refund, amount stored `42.5000` (NUMERIC 19,4). **NOT mergeable as-is — DoD gaps + 1 contract risk to kick back to Codex:** (1) ❌ NO `_test.go` files — DoD required unit + integration tests; `go test ./...` passes VACUOUSLY. (2) ❌ NO CHANGELOG `[Unreleased]` entry (DoD). (3) ⚠️ `go-ci.yml` missing `golangci-lint run` (spec) AND `permissions: contents: read` (CLAUDE.md security rule). (4) ⚠️ **amount serializes as JSON STRING** `"42.5"` (quotes; `99.0000`→`"99"`, trailing zeros + point trimmed) — spec said JSON NUMBER scale 4; Jackson likely emits a number → **confirm against Java output or the Playwright e2e will see a different shape.** (5) ⚠️ Dockerfile deviates: `golang:1.21`→distroless (not spec's alpine:3.19) + HEALTHCHECK dropped (distroless has no shell) — reasonable but a deviation. (6) ⚠️ Stripe/PayPal default `ENABLED=true` + stub fakes success (no external call); Java encryption-key interop unverified (no test). (7) note: DB-down is FATAL at startup (main.go:43), slightly at odds w/ spec's "readiness 503 while running". **Verdict: send back to Codex for tests + CHANGELOG + CI gates (golangci-lint, permissions) + amount-shape confirmation before PR.**
- **FIXES SPEC WRITTEN (Claude, 2026-06-17): payment Go rewrite PR1 follow-ups** — spec `docs/plans/shopping-cart-payment-go-rewrite-pr1-fixes.md`, work repo `shopping-cart-payment`, **same branch `feat/go-rewrite`** (additional commit on top of `ddc6c82`, NOT a new branch/PR). Closes the DoD gaps from the `ddc6c82` verification: (1) amount serializes as JSON STRING `"42.5"` → fix to UNQUOTED JSON NUMBER scale 4 matching Java/Jackson (recommended `type Amount decimal.Decimal` w/ `StringFixed(4)` MarshalJSON; confirm scale vs Java first); (2) add unit + integration test suite (status machines, refund guards, idempotent replay, scale-4 math, mock success/failure, encryption round-trip+masking, handler 404/400 bodies, amount-shape assert; integration = testcontainers+Flyway, gated); (3) `go-ci.yml` add `permissions: contents: read` + pinned `golangci-lint` step; (4) CHANGELOG `[Unreleased]`. Plus confirmations C1 gateway `enabled` defaults vs Java (currently default true), C2 DB-down should be non-fatal startup (readiness 503, not crash-loop — matters for preflight/blue-green), C3 distroless Dockerfile deviation = keep+note. Commit msg: `test(payment): PR1 follow-ups — tests, CHANGELOG, CI gates, amount JSON number shape`. READY FOR CODEX (user hands off).
- **VERIFIED — FAIL / KICK BACK TO CODEX (Claude, 2026-06-17):** payment PR1 follow-ups `f2e47ff` on `origin/feat/go-rewrite` (real SHA, branch HEAD, exact commit msg, parent `ddc6c82`). **Scope clean** — 15 files all under `go/` + `go-ci.yml` + `CHANGELOG.md`, Java tree untouched. **Fixes 1/3/4 GOOD:** amount marshaler (`money.go` `Amount.MarshalJSON` → unquoted `StringFixed(4)`, wired only at DTO boundary; bidirectional unit test asserts `"amount":42.5000` present AND quoted form absent); CI gates (`permissions: contents: read` + `golangci-lint-action@v6` pinned `v1.61.0` after vet/before test); CHANGELOG `[Unreleased]→Added`. `paymentStore` interface added to `store.go` = scope-justified (enables mock-store unit tests). **Ran in Docker `golang:1.21` vs throwaway `postgres:16-alpine` w/ real Flyway V1/V2:** `go test ./... -race -cover` → **unit ALL GREEN**. BUT `go test -tags integration` → **FAIL** `integration_test.go:80 list payments: no rows in result set`. **Root-caused live = DOUBLE-ADVANCE bug:** `GetPaymentsByCustomer` loop `for rows.Next()` calls `scanPaymentRows(rows)` which calls `rows.Next()` AGAIN internally → 1 row yields `pgx.ErrNoRows`, N rows drops every other row. **SAME bug in `GetRefundsByPayment`/`scanRefundRows`.** **Pre-existing from `ddc6c82`** (f2e47ff `store.go` diff only added the interface, never touched these) — PR1's list endpoints were always broken; new integration test correctly catches it but was COMMITTED RED → Codex did NOT run `-tags integration` (his COMPLETE entry says only unit `go test` passed). **Live-facing:** breaks `GET /api/v1/payments/customer/:customerId`, `GET /api/v1/payments?customerId=`, `GET /api/v1/payments/:paymentId/refunds`. **Fix = drop the inner `rows.Next()` from `scanPaymentRows`/`scanRefundRows` (delegate to singular `scanPaymentRow`/`scanRefundRow`; loop already advances) + require `go test -tags integration` green before done.** Verdict: kick back to Codex via a short bugfix spec on `feat/go-rewrite`. Fixes 1/3/4 + unit suite stay.
- **SPEC WRITTEN (Claude, 2026-06-17):** `docs/bugs/2026-06-17-bugfix-payment-go-rewrite-list-double-advance.md` — work repo `shopping-cart-payment`, branch `feat/go-rewrite`, target `go/internal/payment/store.go` ONLY. Exact old/new blocks: both list loops switch `scanPaymentRows`/`scanRefundRows` → singular `scanPaymentRow`/`scanRefundRow`; delete both wrapper funcs (each called once, now dead → `unused` lint). DoD blocks completion on `go test -tags integration ./internal/payment/...` GREEN against real Flyway schema (NOT unit-only) + `gofmt`/`go vet`/`golangci-lint`/`_agent_audit` clean; `integration_test.go` must NOT be weakened. **Awaiting user handoff to Codex** — Claude wrote spec, does not run the handoff.
- **BUG SPEC WRITTEN (Claude, 2026-06-17): preflight vCluster has no ESO → data-layer cascade** — spec `docs/bugs/v1.1.1-bugfix-preflight-vcluster-eso-missing.md` on `k3d-manager-v1.7.1`. Confirmed live: green1 vcluster has NO external-secrets pods / no green1 ESO app → `green1-preflight-data-layer` SyncError `one or more synchronization tasks are not valid`, per-resource `ExternalSecret ... CRD is installed on the destination cluster` → no redis-cart/postgresql → basket/frontend CrashLoop (`redis-cart...no such host`). = preflight platform bug #3 (ESO on app cluster), k3d-manager NOT shopping-cart-infra (data-layer manifests are correct). Fix direction: trace where ESO installs for a normal cluster (platform-helm member vs discrete step) and mirror into the vCluster gated BEFORE data-layer (sync-wave / readiness on `externalsecrets.external-secrets.io` CRD), target vcluster via kubeconfig like the ghcr-pull-secret step; `bin/cluster-preflight` (+ maybe a preflight ESO manifest). THE convergence blocker for delete-on-success. NOT yet handed to Codex (needs platform-helm trace first). Persistent-cluster `make status` "Data layer 4 not ready" is a SEPARATE symptom (regular data-layer → ubuntu-k3s, currently unconfigured/Unknown).
- **SPEC WRITTEN (Claude, 2026-06-17): hub Grafana federation for k3s-hostinger** — spec `docs/plans/v1.7.2-hostinger-grafana-federation.md` on `k3d-manager-v1.7.1`. GAP root-caused live: two app clusters registered on the hub — `ubuntu-k3s` (ACG sandbox, tunnel) IS federated (`federate-acg` job `host.internal:19090` + `acg-prometheus` datasource + `kubectl pf 19090:9090 --context ubuntu-k3s` in cluster-up/refresh + `deploy_observability_acg` stands up its Prometheus); `ubuntu-hostinger` (persistent VPS) has NONE of the three → hub Grafana shows no k3s-hostinger metrics. Fix mirrors the ACG pattern for Hostinger: NEW `observability-hostinger` ApplicationSet + Prometheus-only `kube-prometheus-stack-hostinger-values.yaml` (NodePort 30090, `externalLabels.cluster: ubuntu-hostinger`, no Grafana/AM) deployed by NEW public `deploy_observability_hostinger`; `_pf_ensure` `host.internal:29090 → ubuntu-hostinger svc/prometheus-operated:9090` in `bin/cluster-refresh`; `federate-hostinger` job + `hostinger-prometheus` datasource in hub values. 5 files (2 new, 3 edit). No hardcoded IPs. READY FOR CODEX (user hands off). After it lands: Claude runs `./scripts/k3d-manager deploy_observability_hostinger` + `make refresh` + verifies `up{cluster="ubuntu-hostinger"}` in hub Grafana.
- **APP-LAYER INVESTIGATION (Claude, 2026-06-17):** broken/out-of-sync services on both the hub `green1-preflight-*` apps AND the regular hub apps are all APP-LAYER, head = **`data-layer` SyncError "one or more synchronization tasks are not valid"** → Redis (`redis-cart.shopping-cart-data.svc`) never deploys → green1 vcluster `basket`/`frontend` CrashLoopBackOff (`failed to connect to Redis: no such host`, 110+ restarts); `shopping-cart-product-catalog` OutOfSync/Missing; `order`/`payment` Degraded (config/ESO). Regular hub `shopping-cart-apps`/`shopping-cart-data` namespaces are EMPTY (apps Missing/Degraded). NOT k3d-manager infra — = pre-existing migration platform bugs #2-#5, track in shopping-cart repos as GitHub Issues. green1 vcluster still UP on `ubuntu-hostinger` (9h).
- **COMPLETE:** launchd bootout-before-bootstrap fix — k3d-manager commit `424e46e1` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.7.1-bugfix-launchd-ensure-bootout-before-bootstrap.md`; `bin/cluster-refresh` system-domain `_launchd_ensure` now best-effort bootouts the stale launchd record before bootstrapping so a previously crash-looped daemon can re-bootstrap cleanly; validation used `shellcheck -S warning bin/cluster-refresh`, `bash -n bin/cluster-refresh`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(refresh): bootout stale launchd record before bootstrap in _launchd_ensure`
- **VERIFIED + THIRD BUG (Claude, 2026-06-17):** bootout fix `424e46e1` (memory-bank `051b0614`) VERIFIED on origin — diff = exactly the one `launchctl bootout system "${plist}" || true` line before bootstrap in `_launchd_ensure`'s system branch (scope clean, `bin/cluster-refresh` only, +1), commit body + trailer intact; gates `shellcheck -S warning` / `bash -n` / `_agent_audit` all rc=0. **Live e2e (Claude):** removed TLS material → `make refresh` → all 3 browser daemons (argocd/keycloak/frontend) **bootstrapped cleanly — NO `Bootstrap failed: 5`** (bootout fix PROVEN). **But ArgoCD still 000 on the proper path** — THIRD distinct bug root-caused live: the socat daemon now runs but crash-loops on its upstream `socat E TCP:127.0.0.1:8080: Connection refused`. Nothing serves `:8080` — the `com.k3d-manager.argocd-port-forward` launchd agent's **plist is missing** from `~/Library/LaunchAgents/` (every other agent's plist present; the wrapper `~/.local/share/k3d-manager/bin/argocd-port-forward.sh` IS present, Jun 12). `bin/cluster-refresh:331-334` calls `_launchd_ensure` on that missing plist with **no regenerate-from-wrapper logic** — unlike the sibling keycloak (`336-375`) + grafana (`377-423`) blocks which rebuild the plist if missing. So refresh bootstraps nothing → `:8080` dead → socat → refused → ArgoCD unreachable. **PROVEN:** with the socat daemon already up, a manual `kubectl port-forward svc/argocd-server 8080:80 -n cicd --context k3d-k3d-cluster` made `curl -sk https://argocd.shopping-cart.local/healthz` return **200** instantly. **Spec: `docs/bugs/v1.7.1-bugfix-refresh-argocd-pf-plist-regen.md`** (1-block, `bin/cluster-refresh` only — mirror keycloak: regen plist from wrapper if missing, `ProgramArguments`=`/bin/bash <wrapper>`, before `_launchd_ensure`). READY FOR CODEX. **`http://localhost:8081/` fallback still 200** until the proper path is restored.
- **COMPLETE:** argocd-browser-https refresh self-heal — k3d-manager commit `ee9d7a7e` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.7.1-bugfix-argocd-browser-refresh-self-heal.md`; `bin/cluster-refresh` now sources `scripts/plugins/argocd.sh` after `tunnel.sh`, defines `PLUGINS_DIR` before sourcing it so `set -u` does not abort during plugin load, and regenerates the Vault-issued TLS material plus the canonical `argocd-browser-https` wrapper before `_system_daemon_install_if_missing`; the `_argocd_write_browser_https_wrapper` call keeps the same argument order as `bin/cluster-up`; validation used `shellcheck -S warning bin/cluster-refresh`, `bash -n bin/cluster-refresh`, `./scripts/k3d-manager _agent_audit`, and a `set -u` source-order sanity check confirming `PLUGINS_DIR` is not unbound when `argocd.sh` loads; commit message: `fix(refresh): self-heal argocd-browser-https cert + wrapper before bootstrap`
- **VERIFIED + NEW BUG (Claude, 2026-06-17):** self-heal fix `ee9d7a7e` (memory-bank `f28c9285`) VERIFIED on origin — diff = exactly the `PLUGINS_DIR` line + the cert/wrapper self-heal guard, scope clean (`bin/cluster-refresh` only, +21), commit body intact. **Live self-heal test (Claude):** removed TLS material → `make refresh` → the self-heal FIRED correctly: TLS material re-issued from Vault PKI (`ca.crt`/`fullchain.crt`/`tls.crt`/`tls.key` fresh, cert `CN=argocd.shopping-cart.local` valid Jun17→Jul17), wrapper regenerated at canonical `~/.local/share/k3d-manager/bin/argocd-browser-https.sh`. **The artifact self-heal works — but the daemon still won't load.** NEW bug root-caused live: `_launchd_ensure` system-domain branch (`bin/cluster-refresh:81-84`) runs `launchctl bootstrap system` WITHOUT a preceding `bootout` → a previously-crash-looped daemon leaves a stale service record in the system domain → bootstrap returns `Bootstrap failed: 5: Input/output error` for ALL 3 browser daemons (argocd/keycloak/frontend, all route through `_launchd_ensure`). Sibling fns already do it right: `_launchd_restart:100-101` + `cluster-up:557→574` bootout-then-bootstrap; `_launchd_ensure` just missed the bootout. **Spec: `docs/bugs/v1.7.1-bugfix-launchd-ensure-bootout-before-bootstrap.md`** (1-hunk, `bin/cluster-refresh` only — add `launchctl bootout system "${plist}" || true` before the bootstrap; best-effort no-op when no stale record). One fix recovers all 3 daemons. READY FOR CODEX. **ArgoCD still reachable via `http://localhost:8081/` fallback** (port-forward up) until the proper path is restored; sudo-interactive `launchctl bootout`/`bootstrap` would also recover it now that cert+wrapper exist.
- **COMPLETE:** preflight vcluster distro schema fix — k3d-manager commit `41829082` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.1.1-bugfix-preflight-vcluster-distro-schema.md`; `scripts/etc/vcluster/values-preflight.yaml` now removes the invalid `controlPlane.distro.k3s` block while keeping the NodePort service and the raised control-plane requests/limits unchanged; validation used the spec-required PyYAML parse check via a disposable `/tmp/k3dm-pyyaml` venv because the host Python lacks `yaml`; commit message: `fix(preflight): drop invalid k3s distro from vcluster values (CLI 0.35.0 schema)`
- **VERIFIED + NEW BUG (Claude, 2026-06-17):** Codex distro fix `41829082` (+ memory-bank `288fd1cd`) VERIFIED on origin — diff = exactly the 3-line `controlPlane.distro` removal, limits unchanged (1500m/1536Mi), valid YAML, scope clean (commit body empty — cosmetic). **Live e2e `make preflight NAME=green1 MODE=--auto` (PREFLIGHT_WAIT_TIMEOUT=900):** distro fix WORKS — `vcluster create green1` succeeded in 6s, `green1-0` syncer **0 restarts** (sizing fix holds); ghcr-pull-secret + registration + cluster-name label all ran. **But the run died in ~37s, not 900s.** NEW k3d-manager TOOLING BUG, root-caused live: `_cluster_preflight_wait_for_apps` (`bin/cluster-preflight` line 152) has an **unguarded `read`** — `kubectl -o jsonpath` emits no trailing newline and is empty for freshly-created apps; bash `read` returns exit 1 on EOF-without-delimiter → `set -euo pipefail` aborts on the first app of the first poll (no `Timed out` line → EXIT trap → `run FAILED rc=1`). Proven in bash (earlier zsh disproof was a shell artifact). Fix = append `\|\| true` to the read. **Spec: `docs/bugs/v1.1.1-bugfix-preflight-wait-read-set-e-abort.md`** (1-line, bin/cluster-preflight only). READY FOR CODEX. **CONVERGENCE FINDING (green1 left UP ~8min):** the wait bug MASKED real progress — `green1-preflight-platform` (nested ArgoCD app-of-apps) + observability (prometheus/grafana/trivy) reached **Synced/Healthy**. Remaining failures are all APP-LAYER, root = **`green1-preflight-data-layer` SyncError** (`shopping-cart-infra` path data-layer, "synchronization tasks are not valid") → no `redis-cart.shopping-cart-data.svc` → basket/frontend CrashLoop (`failed to connect to Redis: no such host`); ESO secrets `order-service-secrets-x-…-x-green1` / `payment-db-credentials-x-…-x-green1` never materialize → order/payment CreateContainerConfigError. = pre-existing migration platform bugs #2-#5 — APP-LAYER, track in shopping-cart repos as GitHub Issues, NOT k3d-manager. True delete-on-success needs them fixed. **green1 KEPT live for ArgoCD-UI demo** (hub ArgoCD = `cicd` ns on `k3d-k3d-cluster`; `kubectl -n cicd port-forward svc/argocd-server 8080:443`; filter `green1-preflight-`). Stale `pf1-preflight-*` orphans pruned.
- **HANDED TO CODEX (2026-06-17):** preflight wait-loop `read` trips `set -e` — spec `docs/bugs/v1.1.1-bugfix-preflight-wait-read-set-e-abort.md` on `k3d-manager-v1.7.1` (committed `89a536ed`, on origin blob `39414ea`). 1-line fix: append `) || true` to the per-app status `read` in `_cluster_preflight_wait_for_apps` (`bin/cluster-preflight:152`). Handoff block printed for Codex; awaiting his SHA. After it lands, Claude re-runs `make preflight NAME=<fresh> MODE=--auto` (wait should now poll full budget; run still ends FAILED/keep due to app-layer data-layer SyncError — correct behavior).
- **SPEC WRITTEN (Claude, 2026-06-17): argocd frontend unreachable — `make refresh` cannot self-heal the browser-https listener** — spec `docs/bugs/v1.7.1-bugfix-argocd-browser-refresh-self-heal.md` on `k3d-manager-v1.7.1`. INVESTIGATION (triggered by "argocd frontend become inaccessible"): hub `argocd-server` pod healthy (2/2, 4d8h), Vault issuer healthy (`secrets/vault-0`, NOT `vault` ns), Istio VS `argocd.shopping-cart.local`→`default-gateway` intact — purely a HOST-side access collapse. `argocd.shopping-cart.local`→`127.0.0.1` (hosts) needs the launchd daemon `com.k3d-manager.argocd-browser-https` (socat: TLS-terminate `:443` w/ Vault cert → plaintext `:8080`; argocd-server runs `--insecure`). Daemon DEAD (not in `launchctl list`): cert dir `~/.local/share/k3d-manager/argocd-browser-https-tls/` EMPTY + wrapper missing at canonical `bin/argocd-browser-https.sh` (only stale copy at share root) → socat crash-loops `SSL_CTX_use_certificate_file: No such file or directory` → launchd drops it. **`make refresh` CANNOT fix it** (confirmed): `_system_daemon_install_if_missing` returns early when plist exists; `_launchd_ensure` just re-bootstraps the dead plist; cluster-refresh doesn't even `source scripts/plugins/argocd.sh`, so it can't call `_argocd_issue_browser_tls_material`/`_argocd_write_browser_https_wrapper` (those run only in `bin/cluster-up`). Fix = source argocd plugin + re-issue cert + regenerate wrapper before bootstrap (mirrors cluster-up:510/523), `bin/cluster-refresh` only. **User unblocked NOW** via `kubectl -n cicd port-forward svc/argocd-server 8081:443` → `http://localhost:8081/` (HTTP, not HTTPS — argocd-server is `--insecure`). Proper-path repair needs `make up` (heavy, sudo-interactive) until this spec lands. **SPEC CORRECTED (2026-06-17) after spec-review-loop caught a defect:** `scripts/plugins/argocd.sh` references `PLUGINS_DIR` at LOAD time (sources vault.sh+eso.sh); only the dispatcher sets `PLUGINS_DIR`, and `cluster-refresh` is a standalone bin/ script → sourcing argocd.sh would abort with `PLUGINS_DIR: unbound variable` under `set -u`, crashing EVERY refresh (worse than the bug being fixed; shellcheck/`bash -n`/`_agent_audit` would NOT catch it — runtime unbound-var). Fix: Change 1 now also adds `PLUGINS_DIR="${PLUGINS_DIR:-${REPO_ROOT}/scripts/plugins}"` before the source; verified full chain sources clean + shellcheck zero warnings. Spec READY FOR CODEX (corrected version; not yet handed off).
- **VERIFIED (Claude, 2026-06-16):** preflight four-spec batch — k3d-manager commits `b01398ea` (cluster-name-label), `4e8a77ee` (make-help), `2b46f0ce` (vcluster sizing), `a6b053fc` (ghcr-pull-secret) all on `origin/k3d-manager-v1.7.1`; each diff matches its spec exactly, scope clean, all gates pass (shellcheck/bash -n/yaml/make -n help/_agent_audit). **Codex left memory-bank uncommitted** (origin had 0/4 SHAs) — Claude committed it as `4feda2dd`. Commit bodies have literal `\n` (Codex artifact, cosmetic).
- **DONE (Claude direct, 2026-06-16):** `Makefile` preflight help line now lists `MODE=--auto|--keep|--reuse` (was missing `--reuse`, which `bin/cluster-preflight` accepts) — commit `e2c53156` on `k3d-manager-v1.7.1`, pushed; `make -n help` OK. One-line doc fix, user-authorized direct edit.
- **BLOCKER — sizing fix `2b46f0ce` is BROKEN; corrected spec written (Claude, 2026-06-16):** the end-to-end re-run failed at the FIRST step — `vcluster create` aborts with `invalid config: ... unknown field "k3s"`. Root cause: **CLI/chart version split** — `vcluster` CLI is **0.35.0** which REMOVED the k3s distro and validates `-f` against its own schema before deploying chart 0.32.1 (whose `values.yaml` still lists a `k3s:` block — the trap). So `controlPlane.distro.k3s.enabled: true` (from `2b46f0ce`) is rejected. **The k3s change was also unnecessary:** live-proved on the host (`ubuntu-hostinger`) that the **k8s default distro + raised limits (1500m/1536Mi) alone** gives a stable control plane — `gtest-0` ran **0 restarts at 3 min** (old 500m-capped pf1 had 8 syncer restarts by the same age). The 500m CPU cap was the real throttle behind the lease timeout. **Corrected spec: `docs/bugs/v1.1.1-bugfix-preflight-vcluster-distro-schema.md`** — drop the `distro:` block entirely (keep raised limits). Commit msg: `fix(preflight): drop invalid k3s distro from vcluster values (CLI 0.35.0 schema)`. Ready for Codex. After it lands, Claude re-runs `make preflight NAME=<fresh> MODE=--auto` for the green delete-on-success proof. **pf1 + all orphan vclusters/PVCs (pf1/pr33/spike/vok) torn down; `vclusters` ns clean; host at 2% CPU / 11% mem.**

### Go-rewrite decision (2026-06-14, confirmed) — order + payment → Go
- Rewrite ONLY the two JVM backends `shopping-cart-order` + `shopping-cart-payment` in Go (memory footprint on 7.8 Gi Hostinger VPS + 24 GB Mac). basket already Go; product-catalog (Python) + frontend (Node) stay. Memory: `project_shopping_cart_go_rewrite.md`.
- **CORRECTION to earlier framing:** migration platform bugs #2-#5 below are NOT throwaway — data-layer/ESO/context-resolution/smoke-probes are needed by ANY shopping-cart (Java or Go). They proceed independently. Throwaway risk is only app-level order/payment JVM tuning (Spring env, actuator probes, heap-sized requests) in those repos — defer that, not the platform plumbing.
- vCluster preflight CI planned as the rewrite's validation gate: `project_vcluster_preflight_ci.md` (warm-host vcluster per PR → full stack + PR image → e2e gate → merge syncs to Hostinger; matches roadmap v1.1.1).
- **PLAN WRITTEN (2026-06-14):** `docs/plans/v1.1.1-vcluster-preflight-webhook.md` — webhook-as-runner variant (NO self-hosted GH runner; the k3dm-webhook server on the warm host is the execution engine, modeled on `_run_cluster`). One `bin/cluster-preflight` script, 3 entry points (make/Slack/GitHub stub), one log link. Phased: P1 `bin/cluster-preflight` + `make preflight` (locally provable) → P2 webhook `/api/v1/preflight` + Slack `/cluster-preflight` + R2/OCI log link → P3 `preflight/vcluster` commit-status merge gate + GH-hosted PR stub (HTTP-only, no runner). NOT yet handed to Codex; P1 deploy-fn name must be pinned from `shopping_cart.sh` before implementing. AWAITING USER GO.
- **PHASE 1 ATTEMPT FAILED + DIAGNOSED (2026-06-15, Claude live debug):** TWO root causes. (1) **Spec defect (Claude's):** Phase 1 told Codex to run `deploy_argocd_bootstrap APP_CLUSTER_NAME=<probe>`, which re-applies the SHARED live `services-git`/`observability-acg` ApplicationSets in place → **retargeted live ubuntu-hostinger shopping-cart apps onto the throwaway vcluster** (`docs/issues/2026-06-14-vcluster-preflight-phase1-live-retarget-failure.md`). Codex caught it, restored, and rewrote `bin/cluster-preflight` to render PREFIXED clones (`<name>-preflight-*` appsets + `<name>-preflight-platform` AppProject) — CORRECT pattern, SALVAGEABLE base. (2) **Environmental:** vcluster embedded control plane crash-loops on k3d-on-Docker-on-Mac (kube-controller-manager loses leader election; lease renewal to 127.0.0.1:6443 exceeds 5s; `phase1new-0` restarted 65×) → flaky API → the ESO/OpenAPI/TLS-timeout symptoms. **DECISION (user 2026-06-15): run the preflight vcluster INSIDE the Hostinger k3s host (`ubuntu-hostinger`), NOT local Mac k3d. OCI explicitly rejected.** Consequence: vcluster API must be EXPOSED EXTERNALLY from Hostinger + registered with Mac hub ArgoCD by external URL (`.vclusters.svc:443` only worked when ArgoCD+vcluster shared a cluster). **Live cleanup DONE (Claude):** deleted 17 orphan `phase1*` apps + 7 `cluster-phase1*` secrets + crash-looping `phase1new` vcluster; verified live shopping-cart apps back on `ubuntu-hostinger` (Degraded/Missing = pre-existing BUG #2-#5). Spec updated (Hostinger host + external-URL registration + keep-prefixed-clone). **Codex uncommitted WIP on disk (NOT reverted/committed):** `bin/cluster-preflight` (salvageable), `scripts/etc/vcluster/argocd-manager-rbac.yaml`, `Makefile`; + 2 scope-creep plugin edits — `scripts/plugins/vcluster.sh` (BAD: `_err`→`_warn` host-unreachable, hardcodes `k3d-k3d-cluster` — revert + redo clean host-context param) and `scripts/plugins/eso.sh` (timeout band-aids — drop on stable Hostinger host). NEXT: re-handoff as a REVISION task (adapt good script → Hostinger host + external-URL registration), not from scratch.
- **COMPLETE:** Phase 1a Hostinger spike — k3d-manager commit `fe566b2a` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.1.1-vcluster-preflight-webhook.md` § Phase 1a; `bin/cluster-preflight` now defaults `HOST_CONTEXT` to `ubuntu-hostinger`, creates the vcluster with `scripts/etc/vcluster/values-preflight.yaml` (`controlPlane.service.spec.type: NodePort`), derives `APP_CLUSTER_SERVER` from `HOSTINGER_HOST` + the live vcluster NodePort, enforces the `curl -ksS --max-time 10 .../healthz` firewall guardrail, applies `argocd-manager` RBAC, mints the token, switches back to the hub `k3d-k3d-cluster` for `register_app_cluster`, and STOPs right after registration; `scripts/plugins/vcluster.sh` now supports `VCLUSTER_VALUES_FILE` and fails host-unreachable as `_err` with a clean `VCLUSTER_HOST_CONTEXT` parameter, `scripts/plugins/eso.sh` timeout band-aids were reverted, and the spike live-proofed `spike-0` at restart count 0 with `curl .../healthz` returning `ok` plus `secret/cluster-spike` present on the hub; validation used `shellcheck -S warning bin/cluster-preflight scripts/plugins/vcluster.sh scripts/plugins/eso.sh`, `bash -n bin/cluster-preflight scripts/plugins/vcluster.sh scripts/plugins/eso.sh`, `bats scripts/tests/plugins/vcluster.bats`, `./scripts/k3d-manager _agent_audit`, `make -n preflight NAME=spike MODE=--destroy`, and the live `make preflight NAME=spike` spike; transient failures and cleanup notes were recorded in `docs/issues/2026-06-14-vcluster-preflight-phase1-live-retarget-failure.md` and `docs/issues/2026-06-15-vcluster-preflight-phase1a-registration-context.md`; commit message: `feat(preflight): hostinger vcluster NodePort spike (Phase 1a)`
- **COMPLETE:** vCluster preflight Phase 1b deploy path wired — k3d-manager commit `644d3d6d` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.1.1-vcluster-preflight-phase1b-deploy.md`; `bin/cluster-preflight` now accepts `--reuse`, updates usage text, skips create/register when reusing an already-registered vCluster, renders and applies the prefixed-clone ArgoCD resources, waits for the preflight apps to become Synced/Healthy, and exits `0` on success; validation used `shellcheck -S warning bin/cluster-preflight`, `bash -n bin/cluster-preflight`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(preflight): wire deploy path + --reuse mode + fix success exit code`
- **COMPLETE:** preflight `--destroy` keep-on-failure gate — k3d-manager commit `75c8103c` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.1.1-bugfix-preflight-destroy-on-success-keep-on-failure.md`; `bin/cluster-preflight` now keeps the vCluster and prints a manual-cleanup hint when `--destroy` exits non-zero, but still prunes the vCluster and ArgoCD resources on success; the `--destroy` usage line now documents delete-on-success / keep-on-failure semantics; validation used `shellcheck -S warning bin/cluster-preflight`, `bash -n bin/cluster-preflight`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(preflight): --destroy keeps vCluster on failure, deletes only on success`. Verified on `origin` by Claude (static PASS); live test pending.
- **COMPLETE:** preflight `--auto` default mode — k3d-manager commit `97178adc` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.1.1-bugfix-preflight-auto-default-mode.md`; `bin/cluster-preflight` now defaults to `--auto`, accepts `--auto|--keep|--reuse`, renames the smart teardown guard from `--destroy` to `--auto`, and keeps the teardown body itself unchanged; `Makefile` now documents `MODE=--auto|--keep, default --auto`; validation used `shellcheck -S warning bin/cluster-preflight`, `bash -n bin/cluster-preflight`, `./scripts/k3d-manager _agent_audit`, and `grep -n -- '--destroy' bin/cluster-preflight Makefile` (no output); commit message: `feat(preflight): --auto smart teardown is the default; --keep is the keep-always override`. **VERIFIED (Claude, 2026-06-16):** static PASS + live `bin/cluster-preflight vauto` proved default=`--auto` and keep-on-failure (log `[preflight] --auto: run FAILED (rc=1) — keeping vauto ...`); no debris, pr33 untouched. **delete-on-success still pending live** — blocked by pr33 holding the `vclusters` ns (vcluster won't create a 2nd in same ns) + 3 convergence bugs; needs pr33 teardown + a converging deploy.
- **OPEN (Codex, handed off 2026-06-16):** preflight appset-environment isolation — spec `docs/bugs/v1.1.1-bugfix-preflight-appset-environment-isolation.md` on `k3d-manager-v1.7.1`. ROOT CAUSE confirmed live on the `vok` repro: preflight registers `cluster-vok` with `environment: dev`; the LIVE production `platform-helm` ApplicationSet selects `environment In [dev,infra,prod]` only → production deploys `vok-platform` (proj `platform`, owned by LIVE `platform-helm` per ownerReferences) onto the throwaway → `InvalidSpecError` → blocks the clone and cascades (data-layer SyncError, `shopping-cart-data` ns missing). **Selector check complete (2026-06-16):** `platform-helm` is the ONLY leaking appset (cluster generator templating `destination.server: {{.server}}`); `services-git` (git generator) + `observability-acg` (list generator) both hardcode `destination.name: ubuntu-hostinger` → neither leaks. Fix = register vcluster with `environment: preflight` (override already supported via `register_app_cluster` env `ARGOCD_APP_CLUSTER_ENVIRONMENT`, no subtree edit) + rewrite clone platform-helm selector to `environment: preflight`. 3-hunk patch to `bin/cluster-preflight` only. Diagnosis: `docs/issues/2026-06-16-vcluster-preflight-production-appset-leak.md`. **VERIFIED COMPLETE (Claude, 2026-06-16):** Codex commit `ceffbdb5` (memory-bank `368c9687`) on origin — diff = exactly the 3 hunks (`bin/cluster-preflight` only, +3), commit msg exact, shellcheck + `bash -n` + `_agent_audit` all rc=0. **Live end-to-end re-run done (`pf1`, 2026-06-16):** the isolation fix WORKS — `cluster-pf1` registered `environment: preflight`, NO leaked `pf1-platform` (production no longer grabs the throwaway). BUT a SECOND bug surfaced (see next item): the platform clone still generated zero apps → run still rc=1/kept. `vok`+`pr33` torn down; `pf1` kept as the active repro for the second fix.
- **OPEN (Codex, spec ready 2026-06-16):** preflight platform clone never generates — secret missing `argocd.argoproj.io/cluster-name` label — spec `docs/bugs/v1.1.1-bugfix-preflight-cluster-name-label.md` on `k3d-manager-v1.7.1`. The clone appset's generator ANDs `argocd.argoproj.io/cluster-name In [<name>]` (added by the render `awk`) with `environment In [preflight]`; `register_app_cluster` never sets the cluster-name label and ArgoCD does NOT auto-inject it → empty selection → "generated successfully" but zero apps → platform app-of-apps (namespaces/ESO/CRDs/nested ArgoCD) never installs → data-layer + shopping-cart clones stay Missing. **PROVEN live (pf1):** manually `kubectl label secret cluster-pf1 argocd.argoproj.io/cluster-name=pf1 --overwrite` → `pf1-preflight-platform` generated in ~5s (owned by the clone appset, proj `pf1-preflight-platform`) → platform app-of-apps began installing (argocd ConfigMaps/Secrets Synced, Progressing). Fix = label the secret in `bin/cluster-preflight` right after `register_app_cluster` (context still hub). 1-hunk patch to `bin/cluster-preflight` only; do NOT touch `register_app_cluster` (shared) or the render awk (it's correct). After it lands (on top of `ceffbdb5`), Claude re-runs `make preflight NAME=<fresh>` for the delete-on-success proof; if convergence is healthy but >300s on the warm host, tuning `PREFLIGHT_WAIT_TIMEOUT` is a separate follow-up.
- **NEW BLOCKERS surfaced by the `pf1` end-to-end run (2026-06-16) — beyond the 2 isolation bugs, NOT yet spec'd; need user scope decision:** with both isolation fixes applied (label added manually), the platform clone installs but the stack still does not go fully green: (1) **vcluster control-plane CrashLoop → cascade — ROOT CAUSE PINNED (2026-06-16):** `scripts/etc/vcluster/values-preflight.yaml` caps `controlPlane.statefulSet.resources.limits` at `cpu: 500m / memory: 512Mi`, AND neither vcluster values file sets `distro:` so vcluster 0.32.1 runs the heavy default **k8s distro** (embedded etcd + standalone kube-controller-manager; images `loft-sh/vcluster-pro:0.32.1` + `loft-sh/kubernetes:v1.35.0`). The control plane is CPU-throttled by the 500m limit → lease renewals exceed the 5s deadline (`context deadline exceeded` on `127.0.0.1:6443/.../leases/kube-controller-manager`) → `kube-controller-manager` exits 1 → syncer `/healthz` times out → kubelet kills syncer (liveness) → `pf1-0` syncer CrashLoop (8 restarts) → vcluster API (`kubernetes.default.svc` 10.43.120.152:443) flaps → `argocd-pf1-server` + everything talking to the API crashloops. **NOT host capacity** (node at 21% CPU / 34% mem of 2vCPU/7.7Gi) — it's the POD limit + heavy distro. **Fix (values-preflight.yaml, not subtree):** set `distro: k3s` (lightweight sqlite, single binary) and raise controlPlane limits to ~1500m CPU / ~1.5-2Gi (node has headroom). (2) **shopping-cart `basket/frontend/order` ImagePullBackOff** — confirmed `NO ghcr-pull-secret in shopping-cart-apps` ns inside the vcluster; the preflight does not propagate the GHCR pull secret into the throwaway cluster. Both are concrete/fixable (no host dead-end). **SPECS WRITTEN + pushed (2026-06-16):** blocker 3 → `docs/bugs/v1.1.1-bugfix-preflight-vcluster-controlplane-sizing.md` (values-preflight.yaml: `distro.k3s.enabled: true` + limits 1500m/1536Mi); blocker 4 → `docs/bugs/v1.1.1-bugfix-preflight-ghcr-pull-secret.md` (new `shopping_cart_provision_ghcr_pull_secret` wrapper in `scripts/plugins/shopping_cart.sh` + a provisioning step in `bin/cluster-preflight` after token mint, targeting the vcluster via `KUBECONFIG=VC_KCFG`+`APP_CONTEXT=current-context`; also creates the missing `shopping-cart-data` ns). User chose "spec both, hand off all to Codex" — handed off with the label + make-help specs. `pf1` left up as the repro; tear down after Codex lands the fixes (then Claude re-runs `make preflight NAME=<fresh>` for the green delete-on-success proof).
- **OPEN (Codex, spec ready 2026-06-16):** `make help` omits the `preflight` target — spec `docs/bugs/v1.1.1-bugfix-make-help-missing-preflight.md` on `k3d-manager-v1.7.1`. The `help` recipe is a hand-maintained `@echo` block (`Makefile:429-470`) that doesn't parse `##` comments, so `preflight` (target at `Makefile:53`, doc comment at `:52`) was never listed. Fix = add one `@echo "    make preflight ..."` line after the `make test` line. 1-hunk patch to `Makefile` only.
- **COMPLETE:** preflight appset-environment isolation — k3d-manager commit `ceffbdb5` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.1.1-bugfix-preflight-appset-environment-isolation.md`; `bin/cluster-preflight` now defaults `APP_CLUSTER_ENVIRONMENT=preflight`, passes `ARGOCD_APP_CLUSTER_ENVIRONMENT` through `register_app_cluster`, and rewrites the cloned platform-helm selector values from `[dev, infra, prod]` to `[${APP_CLUSTER_ENVIRONMENT}]` so the live `platform-helm` appset stops selecting the throwaway vCluster while the preflight clone still converges; validation used `shellcheck -S warning bin/cluster-preflight`, `bash -n bin/cluster-preflight`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(preflight): isolate vcluster from production appsets via environment label`
- **SPEC WRITTEN (2026-06-15): Go rewrite PR1 — order** — `docs/plans/shopping-cart-order-go-rewrite-pr1.md`. Work repo `shopping-cart-order`, branch `feat/go-rewrite` from `origin/main` (multi-repo: spec in k3d-manager, work in order repo). **Phased (user decided "Functional core first"):** PR1 = HTTP + Postgres (pgx) + RabbitMQ events (amqp091-go) + status machine + security headers + `/actuator/health*`+`/prometheus` + per-IP rate limiter, running `OAUTH2_ENABLED=false` (API open, matches Java `SecurityConfig` fallback chain); **PR2 (separate spec, NOT yet written)** = Keycloak JWT/JWKS + role checks on `/api/orders/**` toggled by `OAUTH2_ENABLED=true` (matches Java `OAuth2SecurityConfig` `@Order(1)` chain — the configmap sets it true in-cluster). Stack mirrors basket: gin + golang-jwt + uuid + prometheus + zap; money = shopspring/decimal (never float). Contract reproduced exactly from Java source: 5 endpoints, camelCase JSON, ISO-8601 timestamps, decimal numbers; DB schema OWNED EXTERNALLY (init-SQL per `docs/plans/v1.2.0-fix-orders-init-sql-and-security-config.md` — map to existing `orders`/`order_items` cols, do NOT manage DDL, live schema wins); 5 events to `events` topic exchange (routing key=type, EventEnvelope, copy `@JsonProperty` field names verbatim from `event/*.java`); status state machine PENDING→PAID→PROCESSING→SHIPPED→COMPLETED (+CANCELLED) with per-target side effects. Keep port 8080 + actuator paths + image name so `k8s/base/*` needs no change. **Java and Go ship SIDE BY SIDE (user decided 2026-06-15):** Java tree (`src/**`, `pom.xml`, root `Dockerfile`, existing `ci.yml`) stays intact; Go module goes under a `go/` subdir with its own additive `.github/workflows/go-ci.yml`; deployed artifact stays Java (Go inert until a later cutover PR repoints image/ArgoCD after preflight proves e2e — PR1 = zero deployment risk). **Acceptance contract = `shopping-cart-e2e-tests` (Playwright) unchanged**, but PR1 final acceptance GATES ON vcluster preflight Phase 1b (deploys stack + runs e2e) — do NOT cut the deployed image to Go until e2e green through the preflight; PR1 lands impl+CI+tests only. order first (lower blast radius than payment money path). **AWAITING USER GO to hand off to Codex.**
- **COMPLETE:** shopping-cart-order Go rewrite PR1 — work repo `shopping-cart-order` branch `feat/go-rewrite` from `origin/main`; commit `66f7e0e` (`feat(order): Go rewrite PR1 — functional core (HTTP + Postgres + RabbitMQ events)`) added the side-by-side `go/` module, `go/Dockerfile`, `go/Dockerfile.local`, additive `.github/workflows/go-ci.yml`, `CHANGELOG.md` Unreleased note, contract-preserving HTTP + Postgres + RabbitMQ implementation, and unit/integration tests; validation used Dockerized Go toolchains for `gofmt -l .`, `go vet ./...`, `golangci-lint run`, `go test ./... -race -cover`, and `docker build -f go/Dockerfile go`; branch pushed successfully to `origin/feat/go-rewrite`. PR2 JWT/JWKS remains deferred.
- **COMPLETE:** shopping-cart-order Go rewrite PR1 follow-up — work repo `shopping-cart-order` branch `feat/go-rewrite`; commit `c5a5d95` (`fix(order): harden Go rewrite config, publisher, and not-found handling`) changed only `go/internal/config/config.go`, `go/internal/events/publisher.go`, and `go/internal/order/store.go` to clear the GitGuardian password default, add `DB_SSLMODE` / `DBSSLMode`, switch RabbitMQ dialing to `amqp.DefaultDial(5 * time.Second)`, and return `ErrOrderNotFound` when `Update` affects 0 rows; validation used Dockerized Go toolchains for `gofmt -l .`, `go vet ./...`, `go test ./... -race -cover`, and `docker build -f go/Dockerfile go`; branch pushed successfully to `origin/feat/go-rewrite`.
- **HANDED TO CODEX (2026-06-15): Phase 1a DE-RISK SPIKE** — `docs/plans/v1.1.1-vcluster-preflight-webhook.md` § "Phase 1a". Live Hostinger findings (Claude, `--context ubuntu-hostinger`): box has headroom NOW (7.75Gi/2vCPU, only 9% mem / 2% cpu used — cart apps mostly Degraded so not consuming); **no ingress/LB/Istio gateway** (k3s `--disable traefik servicelb`), only a NodePort service exists → vcluster API exposure = **NodePort on the Hostinger public IP** (`2.25.146.252`, the same IP the hub reaches the host API on; derive from `HOSTINGER_HOST`, do NOT hardcode). vcluster CLI 0.34.3 → NodePort via `controlPlane.service.spec.type: NodePort`. Spike scope: vcluster on `ubuntu-hostinger` + NodePort expose + **firewall-reachability guardrail (STOP if blocked)** + mint token + `register_app_cluster`, then STOP (no ESO/no stack). Proves (a) vcluster stable on Hostinger (vs `phase1new-0` 65 restarts on Mac k3d) and (b) NodePort punches the VPS firewall. Also reverts the bad `vcluster.sh`/`eso.sh` edits. Phase 1b (full stack into the vcluster) only after spike proves out — that's where in-vcluster Istio/Vault/ESO weight on 2 cores gets validated. AWAITING CODEX.

### ACG→Hostinger migration (in progress, 2026-06-14) — bug queue, NOT a feature release (docs/bugs/, exempt from 5-doc limit)
- **MIGRATION STARTED:** ran `./scripts/k3d-manager deploy_argocd_bootstrap --skip-appproject` (user-authorized) — re-applied 5 ApplicationSets so `services-git`/`observability` retarget `destination.name` to `ubuntu-hostinger`. shopping-cart-* apps now Synced and deployed to `ubuntu-hostinger` (was bare kube-system). The hub→Hostinger cluster secret `cluster-ubuntu-hostinger` resolves to `https://2.25.146.252:6443`.
- **BUG #1 COMPLETE+VERIFIED (Claude):** ghcr-pull-secret was provisioned on hardcoded `--context ubuntu-k3s` → Hostinger pods `ImagePullBackOff`. Fixed `shopping_cart_create_ghcr_pull_secret` to resolve `${APP_CONTEXT:-$(_acg_provider_context "$(_acg_resolve_provider)")}`. Commit `56f559c4`; bug `docs/bugs/2026-06-14-bugfix-ghcr-pull-secret-hardcoded-context.md`; gates shellcheck/bash -n/_agent_audit clean. Provisioned live on `ubuntu-hostinger` via `gh auth token` (has read:packages) — secret created in shopping-cart-apps/payment/data + SAs patched. **Verified:** pods left ImagePullBackOff (images pull); now fail on downstream deps (next bugs).
- **OPEN BUG #2:** `data-layer` Application still targets `ubuntu-k3s` — it's a STANDALONE app (no AppSet owner) sourced from `shopping-cart-infra.git` path `data-layer`, destination pinned in that repo. apps on Hostinger have no postgres/minio.
- **OPEN BUG #3:** ESO NOT installed on Hostinger app cluster (`server doesn't have resource type externalsecrets`) → app secrets/config never sync → `CreateContainerConfigError`/CrashLoop on order/basket/frontend. (Hub ESO is healthy + correct for the hub; the APP cluster needs its own ESO + ClusterSecretStore.)
- **OPEN BUG #4:** broad `ubuntu-k3s` hardcoding — 57 in `scripts/plugins/shopping_cart.sh`, 13 in `bin/k3dm-webhook`, plus `argocd.sh`, `observability.sh`, `bin/cluster-up`, `bin/cluster-sync-apps`, `bin/smoke-test-cluster-health`, `bin/rotate-ghcr-pat`. The provisioning flow (vault seed, ESO install, cluster secrets) all target ubuntu-k3s.
- **OPEN BUG #5:** `_SMOKE_ENDPOINTS` + data-layer probe in `bin/k3dm-webhook` target the ACG surface (localhost port-forwards, `*.shopping-cart.local`, `--context ubuntu-k3s`) — re-point to Hostinger (`*.3ai-talk.org` / `ubuntu-hostinger`) so `make status CLUSTER_PROVIDER=k3s-hostinger` Service Health reflects Hostinger. (Step 2 — do after the apps actually run.)
- **OPEN:** `acg-kube-prometheus-stack` / `acg-trivy-operator` retargeted to `ubuntu-hostinger` but `SYNC=Unknown` (observability-acg AppSet) — investigate once cart apps are up.

- **COMPLETE:** consolidate hostinger-status into provider-aware cluster-status — k3d-manager commit `19e2ea71` on `k3d-manager-v1.7.1`; spec: `docs/bugs/2026-06-14-bugfix-consolidate-status-into-cluster-status.md`; `bin/cluster-status` now carries the Hostinger-only API Health and Hub ArgoCD Registration checks, gates the SSH Tunnel section to tunneled providers only, and keeps Service Health unchanged; `Makefile` status now falls through to `bin/cluster-status` for `k3s-hostinger`; `bin/k3dm-webhook` `_run_hostinger_status` now runs `bin/cluster-status` with `CLUSTER_PROVIDER=k3s-hostinger` and the timeout hint points to `make status CLUSTER_PROVIDER=k3s-hostinger`; `bin/hostinger-status` was removed; validation used `shellcheck -S warning bin/cluster-status`, `bash -n bin/cluster-status`, `python3 -m py_compile bin/k3dm-webhook`, `./scripts/k3d-manager _agent_audit`, `git grep -n 'bin/hostinger-status' -- bin/ scripts/ workers/ Makefile` (no output), and `make -n status CLUSTER_PROVIDER=k3s-hostinger` resolving to `bin/cluster-status`; commit message: `refactor(status): consolidate hostinger-status into provider-aware cluster-status`
- **COMPLETE (Claude-corrected):** hostinger-status posts the report body inside a code fence — `_run_hostinger_status` now posts `report[-3500:]` wrapped in a ```` ``` ```` block under `🖥️ *Hostinger app cluster status*`, with the full report + `⚠️ exited non-zero` on a non-success exit; `timeout=180` + timeout-hint line intact. **Two commits:** `5f764399` (Codex) fixed the primary symptom (body instead of "report gathered") but DEVIATED from spec — hand-edited a narrow `if/else` slice: no code fence, header `✅` not `🖥️`, failure path left on tail-8-lines; **`3557c1f3` (Claude)** applied the spec's exact new block. Bug spec for the deviation: `docs/bugs/2026-06-14-bugfix-hostinger-status-report-fence.md`. Gates `python3 -m py_compile bin/k3dm-webhook` + `./scripts/k3d-manager _agent_audit` clean. Operator follow-on: `make restart-webhook` then Slack smoke `/hostinger-status`. spec: `docs/bugs/2026-06-14-bugfix-hostinger-status-report-body.md`; commit messages: `fix(slack): hostinger-status posts the report body, not just "complete"` + `fix(slack): hostinger-status report posts inside a code fence`
- **OPEN (live state, diagnosed 2026-06-14):** 9 ArgoCD apps show `SYNC=Unknown` because they still target the decommissioned `ubuntu-k3s` ACG cluster via dead tunnel `host.k3d.internal:6443` (ComparisonError). Root cause: ApplicationSet retarget `4e603170` (parameterize `destination.name`→`${APP_CLUSTER_NAME}` default `ubuntu-hostinger`) is in git but NOT applied to the live hub — live ApplicationSets `services-git`+`observability-acg` still template `destination.name: ubuntu-k3s`. Both cluster secrets exist (`cluster-ubuntu-hostinger` + stale `cluster-ubuntu-k3s`). Fix = re-apply ApplicationSets: `./scripts/k3d-manager deploy_argocd_bootstrap --skip-appproject` (code already defaults `APP_CLUSTER_NAME=ubuntu-hostinger`, `argocd.sh:1079`) — but this LIVE-migrates 9 shopping-cart+observability workloads onto Hostinger → needs explicit user go. AWAITING USER DECISION (run now / spec it / user runs).
- **VERIFIED (Claude, 2026-06-14):** Default `/cluster-up|status|refresh` to Hostinger + add Hostinger refresh — k3d-manager commit `001ae8e6` on `k3d-manager-v1.7.1` — PASS (5 spec files only, no subtree; both `_provider_map` sites patched, both thread branches split+provider-aware, gates clean: node/py_compile/shellcheck/bash -n/_agent_audit all green, `make refresh` case resolves hostinger→`refresh_cluster` and default/aws→`bin/cluster-refresh`). Operator follow-on (not in commit): `make deploy-worker` + `make restart-webhook` + Slack smoke test. spec: `docs/plans/v1.7.1-cluster-default-hostinger.md`; `workers/slack-relay/index.js` now defaults `/cluster-up`, `/cluster-status`, and `/cluster-refresh` to `hostinger` while keeping `/cluster-down` sandbox-default unless explicitly set to hostinger, relays provider on up/status/refresh/down, and leaves `/cluster-resume` unchanged; `bin/k3dm-webhook` now maps `hostinger→k3s-hostinger` in both `_provider_map` sites, routes hostinger down through `make down CLUSTER_PROVIDER=k3s-hostinger`, accepts hostinger on `/api/v1/cluster`, adds hostinger-aware thread dispatch for `cluster-status` and `cluster-refresh`, and introduces `_run_hostinger_refresh`; `Makefile` `refresh` is provider-cased so `CLUSTER_PROVIDER=k3s-hostinger` runs `./scripts/k3d-manager refresh_cluster`; `scripts/lib/core.sh` now exposes public `refresh_cluster()` dispatch; `scripts/lib/providers/k3s-hostinger.sh` now implements `_provider_k3s_hostinger_refresh_cluster()` by reusing `_hostinger_merge_kubeconfig` + `_hostinger_register_cluster` and verifying `/healthz`; validation used `shellcheck -S warning scripts/lib/core.sh scripts/lib/providers/k3s-hostinger.sh`, `bash -n scripts/lib/core.sh scripts/lib/providers/k3s-hostinger.sh`, `node --check workers/slack-relay/index.js`, `python3 -m py_compile bin/k3dm-webhook`, `./scripts/k3d-manager _agent_audit`, `bats scripts/tests/lib/provider_contract.bats`, `git grep -n '_provider_map = ' bin/k3dm-webhook` (showing both updated occurrences), and `make -n refresh` / `make -n refresh CLUSTER_PROVIDER=k3s-hostinger`; commit message: `feat(slack): default /cluster-up|status|refresh to Hostinger; add hostinger refresh`
- **SPEC WRITTEN (2026-06-15) — AWAITING USER GO to hand to Codex:** webhook `cluster-up/down/resume` dispatch fixes — consolidates two filed issues (`docs/issues/2026-06-15-cluster-up-not-in-standalone-slack-commands.md` + `docs/issues/2026-06-15-cluster-up-hostinger-routes-to-k3s-aws.md`) into one bugfix spec `docs/bugs/v1.7.1-bugfix-webhook-cluster-dispatch.md`. Two defects, same dispatcher, post-rename drift: (1) `cluster-up/down/resume` missing from `_STANDALONE_CMDS` (553–556) → top-level Slack posts dropped; (2) provider allowlist on up/down/**resume** (679/701/723) is `("aws","gcp","az")` else `"aws"` → `hostinger`/typo silently falls back to ACG aws (job `4570a5e0` ~12m ACG timeout). Fix: add the 3 cluster cmds to standalone (NOT bare up/down/resume aliases); set all 3 allowlists to `("aws","gcp","az","hostinger")` else **`"hostinger"`** (user decision 2026-06-15 — matches status/refresh, ACG stays explicit via `cluster-up aws`). ACG logic untouched (aws provider). Only `bin/k3dm-webhook` + CHANGELOG + memory-bank. Gates: `_agent_audit` + `py_compile`. Operator follow-on (Claude, not Codex): `make restart-webhook`. Commit msg: `fix(webhook): route cluster-up/down/resume standalone + hostinger provider (default hostinger, not aws)`. NOTE: issue docs #1 mislabeled `Status: Fixed` (no code change) — both are Filed; spec supersedes.
- **VERIFIED (Claude, 2026-06-14):** Slack `/acg-*` → `/cluster-*` hard cutover `2472b525` on `k3d-manager-v1.7.1` — PASS. Both grep gates empty, scope = only `workers/slack-relay/index.js` + `bin/k3dm-webhook` (48/48), worker `ALLOWED_COMMANDS` + 5 handlers renamed, keep-list intact (`acg-state`, `acg-*` ArgoCD prefix, `🔄 *ACG Refresh*`, `/hostinger-status`, `/api/v1/cluster*`, `_run_cluster_*`, argocd `acg` stage), `node --check`/`py_compile`/`_agent_audit` clean. One cosmetic deviation: Codex left a duplicate `"cluster-status"` in `_THREAD_COMMANDS` line 548 (spec B1 had removed it) — harmless (Python set dedupes) but **fixed directly by Claude** in commit `0d34995d` per spec B1, then `make restart-webhook`. **Operator follow-on still REQUIRED for the cutover to take effect:** `make deploy-worker` + re-register the 5 slash commands at api.slack.com (`/acg-*` → `/cluster-*`) + smoke test `/cluster-status` then `/cluster-refresh`. Spec: `docs/plans/v1.7.1-rename-slack-commands-cluster.md`.
- **VERIFIED (Claude, 2026-06-14):** `bin/acg-*` → `bin/cluster-*` rename `bd1f7754` — PASS. Renames detected via `git mv` (5 bin + 3 BATS, history preserved), grep gate empty, `make -n up/down/refresh/status/sync-apps` → `bin/cluster-*`, webhook real invocation (line 1870) → `bin/cluster-refresh`, Slack `/acg-*` tokens + `🔄 *ACG Refresh*` kept, 11/11 BATS pass, shellcheck/py_compile/_agent_audit clean. One unspec'd-but-justified deviation: Codex added a `docker` stub to `cluster_down.bats` setup (disclosed in `docs/issues/2026-06-14-cluster-down-bats-docker-prune-stub.md`) because `bin/cluster-down:241` `docker system prune` under `set -e` fails when docker CLI is present but daemon is down — makes the suite hermetic, accepted. Cosmetic nit: `@test` titles still read "acg-down …" (descriptions only; invocations use `bin/cluster-down`). Follow-up commits: `b85e0ec2` (memory-bank), `f90d1954` (issue doc).
- **COMPLETE:** `bin/acg-*` → `bin/cluster-*` rename — k3d-manager commit `bd1f7754` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.7.1-rename-acg-to-cluster-binaries.md`; 5 binaries (`bin/acg-up|down|refresh|status|sync-apps`) and 3 BATS suites were renamed with `git mv`; `Makefile` now points the provider-aware targets at `bin/cluster-up|down|refresh|status|sync-apps`; `bin/k3dm-webhook` `_run_cluster_refresh` now references `bin/cluster-refresh`; renamed scripts and suites only changed self-name/path refs while preserving ACG-concept labels and internal constants; validation used `shellcheck -S warning bin/cluster-up bin/cluster-down bin/cluster-refresh bin/cluster-status bin/cluster-sync-apps`, `bash -n` on each renamed script, `python3 -m py_compile bin/k3dm-webhook`, `./scripts/k3d-manager _agent_audit`, `bats scripts/tests/bin/cluster_up.bats scripts/tests/bin/cluster_down.bats scripts/tests/bin/cluster_sync_apps.bats`, `make -n up`, `make -n down`, `make -n refresh`, `make -n status`, `make -n sync-apps`, and `grep -rn "bin/acg-" bin/ scripts/tests/bin/ workers/ Makefile` (no output); commit message: `refactor(bin): rename acg-* cluster scripts to cluster-* (make/slack interface unchanged)`
- **COMPLETE:** Slack `/hostinger-status` + ACG-lab-only relabel — k3d-manager commit `227de0f3` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.7.1-slack-hostinger-status.md`; `workers/slack-relay/index.js` now allowlists `/hostinger-status`, relabels `/acg-up` and `/acg-down` as ACG lab sandbox actions in the acks, and forwards `/hostinger-status` to the webhook; `bin/k3dm-webhook` now registers `hostinger-status` in `_THREAD_COMMANDS`/`_STANDALONE_CMDS`, adds a `/hostinger-status` thread-dispatch branch, introduces `_run_hostinger_status` to run `bin/hostinger-status`, exposes `/api/v1/hostinger-status`, and relabels the queued `/acg-up`/`/acg-down` notifications; validation used `node --check workers/slack-relay/index.js`, `python3 -m py_compile bin/k3dm-webhook`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(slack): add /hostinger-status; relabel acg-up/acg-down as ACG-lab-only`
- **PR #95 MERGED (e6156a2ab49fda1652cd1208107646fdd30be459) 2026-06-13:** `feat: k3s-hostinger cluster provider (v1.7.0)` — https://github.com/wilddog64/k3d-manager/pull/95. Post-merge complete: enforce_admins RESTORED ✓; v1.7.0 tag + release created ✓; k3d-manager-v1.7.1 branch created ✓; retrospective committed ✓; memory-bank updated ✓
- **COMPLETE:** ApplicationSet retarget `ubuntu-k3s`→`ubuntu-hostinger` — k3d-manager commit `4e603170` on `k3d-manager-v1.7.0`; spec: `docs/plans/v1.7.0-applicationset-retarget-hostinger.md`; `scripts/etc/argocd/applicationsets/services-git.yaml` and `scripts/etc/argocd/applicationsets/observability-acg.yaml` now parameterize `destination.name` with `${APP_CLUSTER_NAME}`, and `scripts/plugins/argocd.sh` defaults/exports `APP_CLUSTER_NAME=ubuntu-hostinger` while including it in the `envsubst` allowlist so the Hostinger app cluster is the default target but `APP_CLUSTER_NAME=ubuntu-k3s` still overrides for ACG; validation used `shellcheck -S warning scripts/plugins/argocd.sh`, `git grep -n 'name: ubuntu-k3s' scripts/etc/argocd/applicationsets/` (no output), and envsubst render checks for both default and override values; commit message: `feat(argocd): parameterize ApplicationSet destination via APP_CLUSTER_NAME (default ubuntu-hostinger)`
- **COMPLETE:** `make status CLUSTER_PROVIDER=k3s-hostinger` full report — k3d-manager commit `a5b9073a` on `k3d-manager-v1.7.0`; spec: `docs/plans/v1.7.0-k3s-hostinger-status-report.md`; added new read-only `bin/hostinger-status` (sourced via `scripts/lib/system.sh`) that reports App Nodes, App Pods, API `/healthz`, hub ArgoCD registration secret `cluster-ubuntu-hostinger` in `cicd`, and ArgoCD Apps/ApplicationSets, and repointed the Makefile `k3s-hostinger` status arm to it; validation used `shellcheck -S warning bin/hostinger-status`, `bash -n bin/hostinger-status`, `./scripts/k3d-manager _agent_audit`, and `make -n status CLUSTER_PROVIDER=k3s-hostinger`; commit message: `feat(k3s-hostinger): full make status report via bin/hostinger-status`
- **COMPLETE:** default `HOSTINGER_HOST` via config vars — k3d-manager commit `f13529f7` on `k3d-manager-v1.7.0`; spec: `docs/bugs/2026-06-13-bugfix-hostinger-host-default.md`; new `scripts/etc/hostinger/vars.sh` now provides env-overridable defaults for `HOSTINGER_HOST` (`srv1754834.hstgr.cloud`), `HOSTINGER_SSH_USER`, and `HOSTINGER_SSH_KEY`, and `scripts/lib/providers/k3s-hostinger.sh` sources that vars file and updates the deploy help text so `HOSTINGER_HOST` is documented as defaulted rather than REQUIRED; validation used `shellcheck -S warning scripts/etc/hostinger/vars.sh scripts/lib/providers/k3s-hostinger.sh`, `bash -n scripts/lib/providers/k3s-hostinger.sh`, `./scripts/k3d-manager _agent_audit`, and `make -n up CLUSTER_PROVIDER=k3s-hostinger` (still expands to `deploy_cluster --confirm`); commit message: `feat(k3s-hostinger): default HOSTINGER_HOST via scripts/etc/hostinger/vars.sh`
- **COMPLETE + VERIFIED BY CLAUDE (2026-06-13):** register `ubuntu-hostinger` with hub ArgoCD — k3d-manager commit `2fd9e179` (memory `c0be23ef`) on `k3d-manager-v1.7.0`; spec: `docs/plans/v1.7.0-k3s-hostinger-argocd-register.md`; `scripts/lib/providers/k3s-hostinger.sh` now preserves the operator current-context while merging kubeconfig, adds `_hostinger_register_cluster` to render the `cluster-ubuntu-hostinger` mTLS secret via `mktemp` + heredoc + `set +x` suppression (no `--from-literal`), adds `_hostinger_deregister_cluster`, and wires registration into `deploy_cluster` plus best-effort deregistration into `destroy_cluster`; commit message: `feat(k3s-hostinger): register app cluster with hub ArgoCD via mTLS cluster secret`. **Claude verification:** diff only `k3s-hostinger.sh` (69+/2−, exact spec match incl. verbatim jsonpath + unquoted `<<EOF`); shellcheck/`bash -n`/`_agent_audit` clean; `git grep from-literal` empty; **LIVE** re-deploy with hostname → merge preserved hub `current-context: k3d-k3d-cluster` (hijack fixed), `cluster-ubuntu-hostinger` secret created in `cicd` with labels `secret-type=cluster`+`cluster-name=ubuntu-hostinger`, config = mTLS (caData+certData+keyData, insecure unset), server `https://2.25.146.252:6443`; **gold-standard:** rebuilt a kubeconfig from the SECRET's own creds → `kubectl get nodes` returned node Ready (the exact creds ArgoCD will use authenticate). NOTE: ArgoCD connection-state annotation empty until an app targets the cluster (expected for registration-only). **NEXT (separate spec):** ApplicationSet retarget `ubuntu-k3s`→`ubuntu-hostinger` to move shopping-cart workloads.
- **SMOKE TEST PASS (2026-06-13):** k3s-hostinger provider PROVEN live — `HOSTINGER_HOST=2.25.146.252 CLUSTER_PROVIDER=k3s-hostinger ./scripts/k3d-manager deploy_cluster --confirm` → k3s `v1.35.5+k3s1` installed, node `srv1754834` Ready (control-plane, Ubuntu 24.04.4, IP 2.25.146.252), kubeconfig merged as `ubuntu-hostinger`, label `k3d-manager/node-type=server` applied, system pods (coredns/local-path/metrics-server) Running, traefik+servicelb correctly disabled. **Cluster is LIVE NOW** (context `ubuntu-hostinger`). Follow-on: ArgoCD hub registration of `ubuntu-hostinger` + shopping-cart app deploy.
- **COMPLETE + VERIFIED BY CLAUDE (2026-06-13):** k3s-hostinger k3sup bootstrap + host-to-IP resolution — k3d-manager commit `e5fb99ce` on `k3d-manager-v1.7.0` (memory commit `41b407ae`); spec: `docs/bugs/2026-06-13-bugfix-hostinger-k3sup-load-and-ip-resolve.md`; `scripts/lib/providers/k3s-hostinger.sh` now sources `scripts/plugins/shopping_cart.sh` so `_ensure_k3sup` is defined and adds `_hostinger_resolve_ip` to pass a literal IPv4 through unchanged or resolve `HOSTINGER_HOST` via `dig`/`getent`/`python3` before feeding k3sup `--ip`; commit message: `fix(k3s-hostinger): source shopping_cart for _ensure_k3sup; resolve host to IP for k3sup`. **Claude verification:** diff only `k3s-hostinger.sh` (29+/3−, exact spec match incl. `# shellcheck source=/dev/null` directive); shellcheck/`bash -n`/`_agent_audit` all clean; resolver unit-tested (IP→passthrough, `srv1754834.hstgr.cloud`→`2.25.146.252`, garbage→error+rc1); **LIVE re-test with the HOSTNAME** (`HOSTINGER_HOST=srv1754834.hstgr.cloud … deploy_cluster --confirm`) exit 0 — zero `_ensure_k3sup`/`parse IP` errors, node Ready. Both bugs dead end-to-end.
- **COMPLETE:** `make up CLUSTER_PROVIDER=k3s-hostinger` deploy-gate fix — k3d-manager commit `72d1d2e3` on `k3d-manager-v1.7.0`; spec: `docs/bugs/2026-06-13-bugfix-hostinger-make-up-confirm.md`; `Makefile` now passes `--confirm` in the `k3s-hostinger` `up` arm so `deploy_cluster` clears `__k3dm_deploy_guard_args` and the provider actually runs; validation used `make -n up CLUSTER_PROVIDER=k3s-hostinger` (showed `deploy_cluster --confirm`) and `git diff --check`; commit message: `fix(make): pass --confirm to k3s-hostinger up arm so deploy gate accepts it`
- **COMPLETE:** k3s-hostinger sudo de-obfuscation + spurious issue doc removed — k3d-manager commit `6c592550` on `k3d-manager-v1.7.0`; spec: `docs/bugs/2026-06-13-bugfix-hostinger-sudo-deobfuscate.md`; `scripts/lib/providers/k3s-hostinger.sh` now writes the remote uninstall command as a single `_run_command -- ssh … 'sudo /usr/local/bin/k3s-uninstall.sh'` line so `_agent_audit`'s sanctioned wrapper exemption applies, and the out-of-spec `docs/issues/2026-06-13-webhook-live-tests-require-running-service.md` was deleted; validation used `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(k3s-hostinger): write remote sudo via _run_command line (de-obfuscate); drop spurious issue doc`
- **HOSTINGER VPS PROVISIONED + HARDENED (2026-06-13):** permanent single-node **app cluster** host replacing the ACG EC2 leg (infra/hub stays on the M4 Air orbstack). Box: `srv1754834` @ **2.25.146.252**, Ubuntu 24.04.4 LTS, x86_64, **2 vCPU (AMD EPYC 9354P) / 7.8 Gi RAM / 96 G disk**, no swap (k3s-correct). KVM 2 plan, $215/2yr (invoice H_45050610), usable until ~2028. Access: `ssh -i ~/.ssh/hostinger ubuntu@2.25.146.252` — **ops user `ubuntu` + passwordless sudo** (matches `add_ubuntu_k3s_cluster` default). Hardened: **root SSH login DISABLED** (`PermitRootLogin no`), **key-only auth** (`PasswordAuthentication no`) via `/etc/ssh/sshd_config.d/00-hardening.conf` (sorts before cloud-init drop-ins), **Docker + system containerd DISABLED** (k3s uses bundled containerd; 7.3 Gi free). k3s/kubectl/helm/k3sup not yet installed. **SPEC WRITTEN (awaiting handoff to Codex):** `docs/plans/v1.7.0-k3s-hostinger-provider.md` — thin `k3s-hostinger` provider, SSH-target only, NO Hostinger API (just `HOSTINGER_HOST` env + `~/.ssh/hostinger`); 4 targets: NEW `scripts/lib/providers/k3s-hostinger.sh` (mirror `k3s-az.sh` MINUS all VM lifecycle — deploy=k3sup install, destroy=k3s-uninstall, VM never deleted) + register in `provider.sh` (normalize/context/resolve: `k3s-hostinger`↔`ubuntu-hostinger`) + `core.sh` (both validation case stmts) + `Makefile` (up/down/status route like k3s-oci→deploy_cluster, NOT bin/acg-up). NO hardcoded IP. Commit msg: `feat(provider): add k3s-hostinger single-node app-cluster provider (SSH/k3sup, no VM lifecycle)`. Follow-on (not in spec): ArgoCD hub registration of ubuntu-hostinger + live deploy/smoke test (`HOSTINGER_HOST=2.25.146.252 make up CLUSTER_PROVIDER=k3s-hostinger`). Provisioning is NOT scripted (Hostinger purchase + panel OS/SSH were one-time manual; everything above the box is scripted). [[project_makefile_multi_cloud]]
- **COMPLETE:** k3s-hostinger provider added — k3d-manager commit `f528203b` on `k3d-manager-v1.7.0`; spec: `docs/plans/v1.7.0-k3s-hostinger-provider.md`; added new `scripts/lib/providers/k3s-hostinger.sh` SSH/k3sup provider for the permanent Hostinger VPS (no VM lifecycle), registered `k3s-hostinger` in `scripts/lib/provider.sh` normalize/context/resolve maps, allowed `k3s-hostinger` in both `scripts/lib/core.sh` provider-validation case statements, and routed `Makefile` `up`/`down`/`status` to `deploy_cluster`, `destroy_cluster --confirm`, and `kubectl --context ubuntu-hostinger` respectively; validation used `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh scripts/lib/provider.sh scripts/lib/core.sh`, `bats scripts/tests/lib/provider_contract.bats`, `./scripts/k3d-manager _agent_audit`, `git diff --check --cached`, `make -n up CLUSTER_PROVIDER=k3s-hostinger`, `make -n down CLUSTER_PROVIDER=k3s-hostinger`, `make -n status CLUSTER_PROVIDER=k3s-hostinger`, and `CLUSTER_PROVIDER=k3s-hostinger ./scripts/k3d-manager deploy_cluster --help`; the broader `bats scripts/tests/lib` bundle hit unrelated live-webhook failures, documented in `docs/issues/2026-06-13-webhook-live-tests-require-running-service.md`; commit message: `feat(provider): add k3s-hostinger single-node app-cluster provider (SSH/k3sup, no VM lifecycle)`
- **PR #94 MERGED (56eeb58c7303ec28e864ab6cf93c9063a24b5c27) 2026-06-13:** `feat: v1.6.5 — k3s-az Azure provider + provisioning hardening`. Post-merge complete: enforce_admins RESTORED ✓; v1.6.5 tag + release created ✓; k3d-manager-v1.7.0 branch created ✓; retrospective committed ✓; memory-bank updated ✓. **Live smoke test was DEFERRED** (ACG sandbox block + Azure sandbox expired — no cluster until ~2026-06-16); v1.6.5 shipped with static validation (shellcheck/bats/node --check) only. **ACG demoted to learning sandbox** — abuse-limit incident from Azure SP credential recreate storm crystallized decision to move permanent app cluster to Hostinger (KVM 2, 2vCPU/8GB, $215/2yr) and keep ACG for occasional labs. Next work: **Hostinger single-node app-cluster provider** + **lib-acg re-extract spec** (`feat/v0.1.8` — bounded device-code wait, re-extract-before-recreate for Azure SP panel).
- **Branch:** `k3d-manager-v1.7.0` created from v1.6.5 merge SHA (2026-06-13)
- **Post-merge (v1.6.5):** enforce_admins RESTORED ✓; v1.6.5 tag + release created (`https://github.com/wilddog64/k3d-manager/releases/tag/v1.6.5`) ✓; retrospective committed (`bbf79dbd`) ✓; memory-bank updated ✓
- **lib-acg v0.1.5 post-merge COMPLETE (2026-06-11):** PR #42 merged to lib-acg main (`180beb8`); enforce_admins restored ✓; v0.1.5 tag created + pushed ✓; GitHub release created ✓; feat/v0.1.6 branch created ✓; retrospective committed (`e1d662c`) ✓; k3d-manager subtree synced (`0008c065`) ✓; memory-bank updated ✓
- **BUG DOC (lib-acg, handed to Codex):** playwright preflight `node -e "require('playwright')"` resolves from cwd not `_LIB_ACG_ROOT` → false "playwright npm module not found" when run from consumer repo root (`make refresh`). playwright v1.60.0 IS installed; the real extraction (`node "$playwright_script"`) resolves from the script dir and works — only the preflight guard false-fails. Spec (lib-acg `feat/v0.1.8`): `docs/bugs/2026-06-12-playwright-preflight-cwd-resolution.md` (committed `694d59e`). Fix: prefix all 3 occurrences (`acg.sh:292`, `gcp.sh:64`, `gcp.sh:154`) with `NODE_PATH="${_LIB_ACG_ROOT}/node_modules"`. Workaround in use today: `NODE_PATH=.../scripts/lib/acg/node_modules make refresh` (verified — got past the gate). Operator follow-up after merge: subtree-pull into k3d-manager. Commit: `fix(acg,gcp): resolve playwright preflight from _LIB_ACG_ROOT (cwd-independent)`. See [[feedback_lib_acg_subtree_discipline]].
- **BUG DOC (spec written — awaiting user OK to hand to Codex):** `make refresh`/`make status` not provider-aware — hardcoded AWS/`ubuntu-k3s`. `acg-refresh` never reads `CLUSTER_PROVIDER` (all AWS + `ubuntu-k3s` literals); `make refresh` doesn't forward the var; `make status` `case` has no `k3s-az` arm → Azure falls through to AWS. Root cause: active provider never recorded at provision. Design (user 2026-06-12): **state file primary, kube-context probe fallback, explicit env overrides**. Centralized resolver in `scripts/lib/provider.sh` (`_acg_resolve_provider`/`_acg_provider_context`/`_acg_normalize_provider`/`_acg_record_provider`); `acg-up` writes `${_ACG_STATE_DIR}/active-provider` on success, `acg-down` clears it; `acg-refresh`+`acg-status` self-resolve; Makefile forwards `CLUSTER_PROVIDER` only when explicit (`$(origin …)`). Spec: `docs/bugs/2026-06-12-acg-refresh-status-provider-aware.md`. 6 files. Commit: `fix(acg): provider-aware refresh/status via recorded active-provider state`.
- **COMPLETE:** provider-aware refresh/status via recorded active-provider state — k3d-manager commit `d0c4959b` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-acg-refresh-status-provider-aware.md`; `scripts/lib/provider.sh` now has `_ACG_ACTIVE_PROVIDER_FILE` plus `_acg_normalize_provider`, `_acg_provider_context`, `_acg_record_provider`, and `_acg_resolve_provider` (explicit env > active-provider file > reachable-context probe > default `k3s-aws`); `bin/acg-up` records the normalized provider on success, `bin/acg-down` clears the active-provider file after teardown, `bin/acg-refresh` resolves provider/context once, sources Azure/GCP plugins only when needed, uses provider-specific credentials, threads the active provider through the tunnel host, and replaces the remaining `ubuntu-k3s` refresh literals with `${_app_context}`; `bin/acg-status` now self-resolves the provider/context and only shows the AWS credentials block for AWS; `Makefile` forwards `CLUSTER_PROVIDER` only when explicitly set and lets `bin/acg-status` resolve itself for non-OCI providers; validation used `shellcheck -S warning scripts/lib/provider.sh bin/acg-up bin/acg-down bin/acg-refresh bin/acg-status`, `bats scripts/tests/lib/provider_contract.bats`, `./scripts/k3d-manager _agent_audit`, `git grep -n 'ubuntu-k3s' -- bin/acg-refresh` (no output), `make -n refresh`, `make -n refresh CLUSTER_PROVIDER=k3s-az`, `make -n status`, `make -n status CLUSTER_PROVIDER=k3s-az`, and `make -n status APP_CONTEXT=ubuntu-azure`; commit message: `fix(acg): provider-aware refresh/status via recorded active-provider state`
- **CONTEXT (Azure not AWS):** the active cluster the user is working is **Azure** (`ubuntu-azure` @ `168.62.53.150`), not AWS. `ubuntu-azure` also unreachable (`context deadline exceeded`). The earlier "CLUSTER UNREACHABLE" AWS-host diagnosis was against the wrong provider because refresh/status default to AWS — see the provider-aware bug above. Re-diagnose against `ubuntu-azure` once refresh/status are provider-aware (or pass `CLUSTER_PROVIDER=k3s-az` explicitly).
- **ACG SANDBOX BLOCK — temporary 72h (2026-06-12→13):** A Cloud Guru blocked sandbox creation ("repeated detected misuse" / "Ten or more instances created at a time") from the Azure SP-credentials **recreate storm** (`_do_restart` + repeated `make up CLUSTER_PROVIDER=k3s-az` re-runs after the device-code hang). **Resolution: block is temporary — sandbox access RESUMES after 72 hours (~2026-06-16).** Account was **cancelled** by user but remains **usable until 2027-03-07 (Mar 7 2027)**. **DO NOT attempt provisioning during the 72h window** — re-tries can escalate. Use the window to **land the lib-acg re-extract spec** so resumed access doesn't re-trip the limiter. Strategic: ACG playground is an *educational* surface with hard abuse limits, NOT an automation backend; auto-recreate to chase SP creds was removed once (`2026-06-11-azure-portal-only-restart-loop-removed.md`) as too fragile AND is a TOS tripwire.
- **FREE-TIER PROVISIONING SQUEEZE (2026-06-13):** with ACG blocked, the roadmap's cloud fallbacks are both unavailable: **OCI Always Free A1 (Ampere ARM) is capacity-blocked** ("Out of host capacity") in the user's region — only **one AD in the west**, so no alt-AD retry works; A1 is effectively unobtainable there. OCI x86 micro (E2.1.Micro, 1GB) is too small for the k3s+Istio+Vault stack. Home lab (Mac Mini M5, `k3s-local-arm64`) is the durable answer but not until **Oct 2026**. ⇒ In the gap, the only reliable backend is **local on the M4 Air** (orbstack/k3d). Reconsider whether the remote app cluster can run locally (2nd k3d / vCluster) until the home lab lands.
- **BUG DOC (lib-acg, spec written — awaiting user OK to hand to Codex):** Azure SP panel renders intermittently; current flow either **hangs forever** on device-code (`wait "$az_pid"` has no timeout in `_az_portal_valid`) or chases SP via sandbox **recreate** (abuse-flagged). Root cause: SP is a *render* flake, not a provisioning flake — remedy is **page-reload re-extract** (no instance), not recreate. Spec (lib-acg `feat/v0.1.8`): `docs/bugs/2026-06-12-az-sp-reextract-before-recreate.md`. `bin/acg-credential-test`: (1) re-extract-first Step 3 dispatch — up to `AZURE_SP_EXTRACT_ATTEMPTS`(5) page reloads, then **cap 1** `_do_restart` (`AZURE_SP_RECREATE_MAX`) with `AZURE_SP_RECREATE_COOLDOWN`(30s), then device-code last; (2) bound device-code `wait` with `AZURE_DEVICE_CODE_TIMEOUT`(120s). User decisions (2026-06-12): re-extract = primary retry (confirmed `acg_credentials.js` reloads page); recreate cap = **1** with cooldown (kept over recommended 0 despite history). **No live test until ACG block clears** — fix is static/shellcheck-verifiable. Gate: `shellcheck -S warning bin/acg-credential-test`. Commit: `fix(credential-test): re-extract Azure SP panel before recreate; bound device-code wait`. See [[feedback_lib_acg_subtree_discipline]].
- **Open items:** lib-acg Azure auth follow-ups (portal login preference, screenshot archival) — specs in `docs/bugs/`, `docs/issues/`
- **COMPLETE:** lib-acg playwright preflight cwd-independent fix — lib-acg commit `9bc4390` on `feat/v0.1.8`; spec: `docs/bugs/2026-06-12-playwright-preflight-cwd-resolution.md`; `scripts/plugins/acg.sh` and `scripts/plugins/gcp.sh` now prefix all three `node -e "require('playwright')"` preflight checks with `NODE_PATH="${_LIB_ACG_ROOT}/node_modules"` so consumer-repo-root invocations no longer false-fail; validation used `shellcheck -S warning scripts/plugins/acg.sh scripts/plugins/gcp.sh`, `git grep -n 'node -e "require('\''playwright'\'')"' scripts/plugins/` (all 3 NODE_PATH-prefixed), and a `/tmp` functional check proving the bare `node -e "require('playwright')"` fails while `NODE_PATH=/Users/cliang/src/gitrepo/personal/lib-acg/node_modules node -e "require('playwright')"` passes; operator follow-up: subtree-pull into k3d-manager after merge; commit message: `fix(acg,gcp): resolve playwright preflight from _LIB_ACG_ROOT (cwd-independent)`
- **COMPLETE:** Slack relay az provider alias fix — k3d-manager commit `fc00daf3` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-slack-relay-az-provider.md`; `workers/slack-relay/index.js` now accepts `az` as the canonical provider token, maps `azure` to `az`, and normalizes both `/acg-up` and `/acg-resume` before falling back to `aws`; validation used `node --check workers/slack-relay/index.js` and a normalization probe confirming `az -> az`, `azure -> az`, `aws -> aws`, `gcp -> gcp`, and garbage -> `aws`; operator follow-up: run `cd workers/slack-relay && npx --yes wrangler deploy` so Slack picks up the alias fix; commit message: `fix(slack-relay): accept az provider (+ azure alias) for /acg-up and /acg-resume`
- **BUG DOC:** `_deleteConflictingSandbox` misses conflict when Azure panel closed + panelStartBtn clicks disabled button forever — spec: `docs/bugs/2026-06-12-sandbox-conflict-detection-auto-shutdown-only.md`; root cause: `Auto Shutdown` detection only works when provider panel is open; conflict banner text always present but not checked; also `_waitForCredentials` panelStartBtn has no `isDisabled` check; fix: 3 changes to `sandbox.js` on lib-acg `feat/v0.1.7`
- **COMPLETE:** SSH host-key hygiene — non-interactive provisioning + automated known_hosts prune — k3d-manager commit `2cfeec24` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-acg-up-prune-known-hosts.md`; `scripts/plugins/shopping_cart.sh` now seeds raw EC2 host keys before both `k3sup` calls and adds `StrictHostKeyChecking=no`/`UserKnownHostsFile=/dev/null` to the two bare `ssh` heredocs, while `bin/acg-up` now prunes stale globally-routable IPv4 entries from `~/.ssh/known_hosts` at Step 0/12 and keeps hostname/private/loopback/CGNAT/hashed lines; validation used `shellcheck -S warning scripts/plugins/shopping_cart.sh bin/acg-up`, a dry-run of the prune filter over hostname + private + loopback + public-IP samples, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(ssh): non-interactive host keys in shopping_cart + prune known_hosts on acg-up preflight`
- **COMPLETE:** Azure SP validation restart-on-failure fix — lib-acg commit `3b72a4a` on `feat/v0.1.7`; spec: `docs/bugs/2026-06-12-acg-credential-test-azure-sp-invalid-no-restart.md`; `bin/acg-credential-test` now mirrors the AWS restart pattern for Azure SP validation failures by calling `_do_restart`, waiting for CDP readiness, re-extracting credentials, and re-validating before falling back to the existing Azure auth failure message; validation used `shellcheck -S warning bin/acg-credential-test`; commit message: `fix(credential-test): restart sandbox on Azure SP validation failure — mirrors AWS restart pattern`
- **COMPLETE:** Sandbox delete conflict blocked by open panel — lib-acg commit `cdd9ba4` on `feat/v0.1.7`; spec: `docs/bugs/2026-06-12-sandbox-delete-conflict-blocked-by-open-panel.md`; `playwright/lib/sandbox.js` now closes the target panel before conflict deletion and stops `_waitForCredentials` from falling through to reopen logic when partial credentials are visible but no panel-scoped Start Sandbox was found; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): close target panel before conflict deletion; prevent reopen fall-through in credential wait`
- **ARCHITECTURE CORRECTION (2026-06-12):** `scripts/lib/core.sh` is **NOT** the lib-foundation subtree. The subtree is at prefix `scripts/lib/foundation/` (lib-foundation's `scripts/lib/core.sh` → `scripts/lib/foundation/scripts/lib/core.sh`). `scripts/k3d-manager:81-86` sources the foundation copy first, then sources the **local** `scripts/lib/core.sh` last — so the local `_cluster_provider` (with the cloud-provider allowlist at lines 19, 782) is what runs; the foundation one is shadowed. ⇒ commit `976a9617` editing `scripts/lib/core.sh` was **NOT** a subtree violation; the backout was on a false premise. User decision: handle rename as a **local edit** (simplest path).
- **MERGED — lib-foundation extensibility hook:** [PR #30](https://github.com/wilddog64/lib-foundation/pull/30) merged `9c0f5ba` (hook `f7a9178` + Copilot stdout-leak fix `8de372f` + issue doc `5509c1b`). Subtree pulled into k3d-manager `61273526` (`scripts/lib/foundation/` now current; foundation core.sh has hook + `>/dev/null` fix). NOTE: hook is a **library improvement only** — shadowed by local `core.sh`, NOT wired into k3d-manager runtime. lib-foundation `main`@`9c0f5ba`, unprotected (no tag — CHANGELOG `[Unreleased]`).
- **COMPLETE:** Rename CLUSTER_PROVIDER `k3s-azure` → `k3s-az` (hard) + slack arg `azure` → `az` — k3d-manager commit `46d9b360` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-rename-k3s-azure-to-k3s-az.md`; local-edit approach confirmed: `scripts/lib/core.sh` (lines 19, 782), `bin/acg-up` (6×), `bin/acg-down` (3×), provider file `scripts/lib/providers/k3s-azure.sh` → `scripts/lib/providers/k3s-az.sh` via `git mv`, and `bin/k3dm-webhook` 7 lines (map 284/398 + tuples 643/665/687/2473/2526) now use `k3s-az` / `az` only; validation used `shellcheck -S warning scripts/lib/core.sh scripts/lib/providers/k3s-az.sh bin/acg-up bin/acg-down`, `python3 -m py_compile bin/k3dm-webhook`, `git grep -n 'k3s-azure' -- scripts/lib/core.sh scripts/lib/providers bin/` (no output), `git grep -n '\"azure\"' -- bin/k3dm-webhook` (no output), and `make status CLUSTER_PROVIDER=k3s-az` (exited 0); operator follow-up: run `make restart-webhook` so Slack picks up the renamed provider arg; commit message: `refactor(provider): rename k3s-azure → k3s-az and slack arg azure → az`
- **BUG DOC (regression from `46d9b360`):** `make up CLUSTER_PROVIDER=k3s-az` fails with `Cluster provider 'k3s-az' does not implement action 'deploy_cluster'`. Root cause: `_cluster_provider_call` (`scripts/lib/provider.sh:67`) derives the function name as `_provider_${provider//-/_}_${action}` → `_provider_k3s_az_deploy_cluster`, but the rename only replaced the hyphen form `k3s-azure`; the **underscore** function identifiers are still `_provider_k3s_azure_deploy_cluster` / `_destroy_cluster`. Spec: `docs/bugs/2026-06-12-k3s-az-function-name-dispatch.md` — rename 3 occurrences (2 defs in `scripts/lib/providers/k3s-az.sh:155,240` + 1 caller `bin/acg-down:81`) `k3s_azure`→`k3s_az`. Gate: `git grep -n 'k3s_azure' -- scripts/ bin/` empty. Handed to Codex.
- **COMPLETE:** k3s-az dispatch function-name fix — k3d-manager commit `90a842af` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-k3s-az-function-name-dispatch.md`; `scripts/lib/providers/k3s-az.sh` now exports `_provider_k3s_az_deploy_cluster` and `_provider_k3s_az_destroy_cluster`, and `bin/acg-down` now calls `_provider_k3s_az_destroy_cluster --confirm`; validation used `shellcheck -S warning scripts/lib/providers/k3s-az.sh bin/acg-down`, `git grep -n 'k3s_azure' -- scripts/ bin/` (no output), and `make up CLUSTER_PROVIDER=k3s-az` reached Step 2/12 and resolved the provider dispatch before the smoke run was stopped; commit message: `fix(provider): rename k3s-az dispatch functions k3s_azure → k3s_az`
- **COMPLETE:** data-layer StatefulSet presence poll — k3d-manager commit `5864483c` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-data-layer-statefulset-presence-poll.md`; `scripts/plugins/shopping_cart.sh` now replaces the one-shot `kubectl get statefulset` presence check with a bounded 300s `until` poll for `postgresql-orders`, `postgresql-payment`, and `postgresql-products` before the unchanged rollout-status loop runs; validation used `shellcheck -S warning scripts/plugins/shopping_cart.sh`; commit message: `fix(shopping_cart): poll for data-layer StatefulSet presence instead of one-shot check`
- **lib-acg v0.1.7 MERGED + SYNCED (2026-06-12):** PR #44 squash-merged to lib-acg main (`f5e1f6a`) — Azure SP validation restart (3b72a4a), conflict detection via banner (b57131c), close-panel+fall-through (cdd9ba4), Copilot isDisabled timeout fix (87a7ff3); CI green, Copilot thread resolved, enforce_admins restored ✓. k3d-manager subtree synced via `git subtree pull --squash` → merge `1b80e3a9` + squash artifact `a2f2234f` (`696ca83f..f5e1f6ad`); blobs match lib-acg main. NOT pushed yet; v0.1.6 + v0.1.7 untagged (settle at post-merge). NEXT: test real provision `make up CLUSTER_PROVIDER=k3s-azure`
- **BUG DOC:** Azure SP OAuth reprovision budget too small — Pluralsight SP sometimes provisions with no secret; recovery needs 5–6 delete+reprovision cycles, but `_waitForCredentials` deadline (420s) + cap (`deleteCycleCount < 3`) only allow ~2–3 (one cycle ≈ 240s, so 420s deadline fits ~1.5; deadline is the real limiter, not the cap) — guaranteed failure on the bad case. Spec: `docs/bugs/2026-06-12-azure-sp-oauth-population-timing.md`; Part 1a = read-only CDP probe `playwright/diag/azure-field-timing.js` (counts reprovision cycles); Part 1b = `AZURE-TIMING` per-cycle logs in `sandbox.js`; data gate (≥5 runs, reproduce 5–6-cycle case); Part 2 = raise cap + deadline to span worst case. lib-acg `feat/v0.1.7`. NOTE: `cdd9ba4` verified working — AWS extracts cleanly
- **COMPLETE:** Sandbox conflict detection via banner text — lib-acg commit `b57131c` on `feat/v0.1.7`; spec: `docs/bugs/2026-06-12-sandbox-conflict-detection-auto-shutdown-only.md`; `playwright/lib/sandbox.js` now detects conflicts from the always-visible "shut down your current X sandbox" banner, skips disabled Start Sandbox buttons during credential wait, and re-checks the conflict banner before treating a disabled Start Sandbox as "already running"; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): detect conflict via banner text; skip disabled Start Sandbox in credential wait`
- **COMPLETE:** Azure sandbox SP-empty delete+restart recovery — lib-acg commit `a8d5789` on `feat/v0.1.6`; spec: `docs/bugs/v1.6.5-bugfix-sandbox-azure-sp-empty-delete-restart.md`; `playwright/lib/sandbox.js` now tracks partial credential visibility with `partialCredsFirstSeen` and `deleteCycleCount`, and for Azure only it deletes and restarts the sandbox after 60s when Application Client ID / Secret stay empty, up to 3 cycles, while leaving the existing reopen/start logic unchanged; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): delete+restart Azure sandbox when SP fields empty after 60s — up to 3 cycles`
- **COMPLETE:** lib-acg Copilot PR #43 findings addressed — fix commit `f0bc7e8` on `feat/v0.1.6`; (1) `partialCredsFirstSeen` timer now starts only when `vals.some()` is true — delete cannot fire during initial field loading when 0 fields are populated; `partialCredsFirstSeen > 0` guard added to delete condition; (2) `deleteCycleCount++` moved inside `if (deleteBtn)` — transient button-not-found no longer consumes a delete cycle; (3) CHANGELOG updated to accurately describe the trigger as partially populated state; both threads resolved; CI green; issue doc: `docs/issues/2026-06-12-copilot-pr43-lib-acg-sandbox-delete-restart.md`
- **COMPLETE:** lib-acg v0.1.6 post-merge — PR #43 merged to lib-acg main (`696ca83f`); enforce_admins restored ✓; no version tag (CHANGELOG has [Unreleased] only) ✓; feat/v0.1.7 branch created ✓; retro committed (`ebab772`) ✓; k3d-manager subtree pulled (`13b9a43c`) — `sandbox.js` now has all v0.1.6 fixes in `scripts/lib/acg/`
- **COMPLETE:** acg-up Azure fast-path guard fix — k3d-manager commit `96eb6b48` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-azure-group-list-before-az-ok.md`; `bin/acg-up` now initializes `_az_existing_rg` to empty, only runs `az group list` after `_az_ok` has already succeeded, and keeps the existing Playwright extraction slow-path unchanged; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): gate az group list behind _az_ok — unconditional call triggered device code auth`
- **COMPLETE:** _waitForCredentials single reopen attempt timing fix — lib-acg commit `209d77d` on `feat/v0.1.6`; spec: `docs/bugs/2026-06-11-waitforcredentials-single-reopen-too-aggressive.md`; `playwright/lib/sandbox.js` now uses a `reopenCount` counter with up to 3 reopen attempts and an 8s post-reopen wait, so Azure panels that stay closed briefly during render no longer abort after a single 3s retry; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): allow 3 reopen attempts with 8s wait — single 3s attempt too tight for Azure panel render`
- **COMPLETE:** acg-up Vault port-forward stale process + orbctl GUI dialog hint — k3d-manager commit `a5b87701` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-vault-pf-stale-port.md`; `bin/acg-up` now kills any stale `kubectl port-forward` holders on port `18200` via `lsof -ti` before re-binding the Vault port-forward, and logs an OrbStack GUI password dialog warning before `orbctl start` so the pause is explained; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): kill stale vault port-forward by lsof before rebind — warn on orbctl GUI dialog`
- **COMPLETE:** acg-up Docker preflight OrbStack VM recovery — k3d-manager commit `a1f5d478` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-docker-preflight-short-wait.md`; `bin/acg-up` now uses `orbctl start` when `orbctl status` reports `Stopped`, removes the `osascript` update-dialog dismiss block, only keeps `open -a` as a fallback when `orbctl` is not in PATH, and waits for both `docker info` and `~/.orbstack/run/docker.sock` to recover before continuing; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): use orbctl start to recover stopped OrbStack VM — remove osascript, open-a is a no-op`
- **COMPLETE:** acg-up Docker daemon preflight — k3d-manager commit `4adf5a1a` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-docker-daemon-precheck.md`; `bin/acg-up` now runs a Docker preflight immediately before Step 3.5, dismisses OrbStack update dialogs with `osascript`, waits up to 60s for `docker info` to recover, falls back to `open -a OrbStack` or `open -a Docker` if needed, and fails fast with a clear `_err` if the daemon still is unavailable; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): add Docker daemon preflight — dismiss OrbStack update dialogs and recover before Hub cluster step`
- **COMPLETE:** ACG restart Start Sandbox detached-overlay fallback — lib-acg commit `1c81cb4` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-acg-restart-start-sandbox-unscoped-fallback.md`; `playwright/acg_restart.js` now falls back to an unscoped `Start Sandbox` check when the panel renders as a detached overlay and the `Close` button shows the panel is open, so `_sandboxNotYetStarted` can still trigger; validation used `node --check playwright/acg_restart.js`; commit message: `fix(restart): add unscoped Start Sandbox fallback for detached panel overlay`
- **COMPLETE:** Azure portal-only restart loop removed — lib-acg commit `d503653` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-azure-portal-only-restart-loop-removed.md`; `bin/acg-credential-test` now removes the portal-only sandbox restart loop and fails fast with a clear delete-and-recreate message when portal-only Azure creds hit MFA, while leaving SP and identity validation unchanged; validation used `shellcheck -S warning bin/acg-credential-test` and `git diff --check -- bin/acg-credential-test`; commit message: `fix(credential-test): remove portal-only restart loop — replace with fail-fast error`
- **COMPLETE:** ACG session-extended toast Escape fallback — lib-acg commit `515f583` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-addlocatorhandler-close-button-closes-credential-panel.md`; `playwright/lib/sandbox.js` now presses `Escape` inside the `addLocatorHandler` for the "session extended" toast instead of clicking an unscoped close button, so the credential panel is no longer closed by the toast-dismiss handler; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): use Escape in addLocatorHandler — unscoped close button was closing credential panel`
- **COMPLETE:** ACG conflict warning provider parse fix — lib-acg commit `5f704d1` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-conflict-warning-parse-delete-provider.md`; `playwright/lib/sandbox.js` now parses the conflicting provider name from the conflict warning text, closes the open target panel, deletes the named provider sandbox directly, and retries Open Sandbox; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): parse conflicting provider from warning text — broken Auto Shutdown detection never found AWS`
- **COMPLETE:** Azure portal device-code fallback — lib-acg commit `24a89f1` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-az-portal-mfa-device-code-fallback.md`; `bin/acg-credential-test` now falls back from `az login --username --password` to `az login --use-device-code` inside `_az_portal_valid`, then validates the session with `az account get-access-token` so MFA-enforced portal-only sandboxes can still authenticate without SP credentials; validation used `shellcheck -S warning bin/acg-credential-test` and `git diff --check -- bin/acg-credential-test`; commit message: `fix(credential-test): add device code fallback in _az_portal_valid — MFA blocks username/password login`
- **COMPLETE:** Azure portal device-code browser automation — lib-acg commit `e9cc187` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-az-portal-device-code-browser-automation.md`; `bin/acg-credential-test` now runs `az login --use-device-code` in the background, captures the device URL and code, and uses new `playwright/acg_azure_device_login.js` via existing Chrome CDP to open the Microsoft device login page and confirm sign-in automatically; validation used `node --check playwright/acg_azure_device_login.js`, `shellcheck -S warning bin/acg-credential-test`, and `git diff --check`; commit message: `fix(credential-test): automate device code sign-in via CDP — replace manual browser prompt`
- **COMPLETE:** Azure provisioning banner wait fix — lib-acg commit `0514cea` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-waitforcredentials-provisioning-panel-closes.md`; `playwright/lib/sandbox.js` now checks the page text for the Azure provisioning banner ("Hang tight" / "Finalizing your playground") before reopening a closed panel in `_waitForCredentials`, waiting 5s instead of throwing while Azure is still starting; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): wait for provisioning banner in _waitForCredentials — panel auto-closes during Azure startup`
- **COMPLETE:** acg-up Step 1 Azure fast-path skips extraction on stale sandbox creds — k3d-manager commit `1f7e3ef3` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-azure-stale-credentials-skip.md`; `bin/acg-up` now probes `az group list` before taking the Azure fast-path, only skips credential extraction when `_az_ok` and a non-empty resource group are both present, and aborts with `_err` if `AZ_RESOURCE_GROUP` is still empty after extraction; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): require non-empty resource group in Azure fast-path — stale sandbox creds no longer skip extraction`
- **COMPLETE:** azure.sh top-level ESO vars source removal — k3d-manager commit `5b3c1c6b` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-azure-sh-toplevel-source-eso-vars.md`; `scripts/plugins/azure.sh` no longer sources `scripts/etc/azure/azure-vars.sh` at plugin load time, so `az ad app create` only runs inside `_create_azure_eso_store` when ESO setup actually needs it; validation used `shellcheck -S warning scripts/plugins/azure.sh` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(azure): remove top-level azure-vars.sh source from azure.sh — az ad app create ran on every plugin load`
- **COMPLETE:** Azure Step 1 credentials + azure-vars KV_NAME guard — k3d-manager commit `b3badc83` on `k3d-manager-v1.6.5`; spec: `docs/plans/v1.6.5-bugfix-acg-up-azure-step1-credentials.md`; `bin/acg-up` now performs Azure credential extraction in the `k3s-azure` Step 1 case via `scripts/lib/acg/bin/acg-credential-test`, skips Playwright extraction when Azure CLI credentials are already valid, and populates `AZ_RESOURCE_GROUP` from `az group list` when missing; `scripts/etc/azure/azure-vars.sh` now guards `KV_ID` with `${KV_NAME:+...}` so sourcing the plugin no longer trips `set -u` before Step 1; validation used `shellcheck -S warning bin/acg-up`, `shellcheck -S warning scripts/etc/azure/azure-vars.sh`, `./scripts/k3d-manager _agent_audit`, and `grep -n "_write_azure_credentials" scripts/lib/acg/bin/acg-credential-test`; commit message: `fix(acg-up,azure-vars): guard KV_NAME in azure-vars.sh; extract Azure creds in Step 1 via acg-credential-test`
- **COMPLETE:** Azure SP absent retry fix landed in lib-acg — lib-acg commit `b61e22e` on `feat/v0.1.5`; spec: `lib-acg/docs/plans/v0.1.5-bugfix-azure-sp-absent-retry.md`; `bin/acg-credential-test` now retries sandbox restart and re-extraction up to 3 times when Azure portal-only sandboxes are missing SP credentials, while leaving SP and identity failures permanent; k3d-manager subtree pull still pending; validation used `shellcheck -S warning bin/acg-credential-test` and `git diff --check -- bin/acg-credential-test`; commit message: `fix(credential-test): retry sandbox restart when Azure SP credentials absent`
- **COMPLETE:** ACG conflict-regression cleanup — lib-acg commit `7547130` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-delete-conflicting-sandbox-panelInStartState-global-check-regression.md`; `playwright/lib/sandbox.js` now removes the global `panelInStartState` heuristic entirely, leaving the scoped delete flow to decide whether the conflicting sandbox is running; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): remove panelInStartState heuristic — global DOM check causes bidirectional regression`
- **COMPLETE:** ACG conflict-warning false positive guard — lib-acg commit `488b558` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-delete-conflicting-sandbox-false-positive-start-state.md`; `playwright/lib/sandbox.js` now skips the `panelInStartState` heuristic whenever the ACG conflict warning is active, so the conflicting sandbox is no longer misclassified as a safe Start Sandbox state; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): skip panelInStartState heuristic when ACG conflict warning is active`
- **COMPLETE:** codex repeating same answer — k3d-manager commit `1e7e9292` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-codex-repeat-answer.md`; `bin/k3dm-webhook` now writes `action=ask` for ask sub-jobs and `_find_job_by_thread_ts` skips any job whose `action` file is `ask`, so thread follow-up `/codex` replies no longer re-consume their own prior answers; validation used `python3 -m py_compile bin/k3dm-webhook`, `git diff --check -- bin/k3dm-webhook`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(webhook): skip ask sub-jobs in _find_job_by_thread_ts to stop codex repeating prior answers`
- **COMPLETE:** lib-acg v0.4.0 post-merge — lib-acg PR #41 (`feat/v0.1.4` → `main`) merged to `adafde3c`; Azure sandbox provider + 25+ Playwright fix sweep; v0.4.0 tag created + pushed ✓; GitHub release created ✓; enforce_admins restored ✓; feat/v0.1.5 branch created ✓; retro committed (`docs/retro/2026-06-10-v0.4.0-retrospective.md`) ✓; subtree pulled into k3d-manager as `57e33ed6` on `k3d-manager-v1.6.5` (delta: CHANGELOG/README/package.json version bumps only — Playwright fixes already in subtree via prior pull)
- **COMPLETE:** lib-acg sandbox.js addLocatorHandler strict-mode fix — lib-acg commit `2dec28c1` (PR #40 merged 2026-06-10); `scripts/lib/acg/playwright/lib/sandbox.js` now uses `.first()` on the `addLocatorHandler` trigger locator; subtree pulled into k3d-manager as `959ea763` on `k3d-manager-v1.6.5`; Gemini's direct subtree edit reverted; issue doc: `docs/issues/2026-06-10-sandbox-js-addlocatorhandler-strict-mode-violation.md`; commit message: `chore(subtree): pull lib-acg main (2dec28c1) — fix addLocatorHandler strict-mode violation`
- **COMPLETE:** webhook provider arg — k3d-manager commit `3b91b257` on `k3d-manager-v1.6.5`; spec: `docs/plans/v1.6.5-acg-up-slack-provider-arg.md`; `bin/k3dm-webhook` now parses an optional `aws|gcp|azure` provider token for `/acg-up`, `/acg-down`, and `/acg-resume`, defaults to `aws`, and includes the selected provider in the queued Slack notification text; validation used `python3 -m py_compile bin/k3dm-webhook` and `./scripts/k3d-manager _agent_audit`; commit message: `feat(webhook): add optional provider arg to /acg-up, /acg-down, /acg-resume Slack commands`
- **COMPLETE:** acg-up azure wiring — k3d-manager commit `6dda3d20` on `k3d-manager-v1.6.5`; spec: `docs/plans/v1.6.5-acg-up-azure-wiring.md`; `bin/acg-up` now sources the Azure provider, leaves Step 1 as a no-op passthrough for `k3s-azure`, excludes it from the SSH tunnel step, and exits early after the cluster is up; `bin/acg-down` now handles `k3s-azure` teardown and updates its provider usage comment; validation used `shellcheck -S warning bin/acg-up`, `shellcheck -S warning bin/acg-down`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(acg-up): wire k3s-azure provider into bin/acg-up and bin/acg-down`
- **COMPLETE:** k3s-azure provider — k3d-manager commit `d37a011a` on `k3d-manager-v1.6.5`; spec: `docs/plans/v1.6.5-k3s-azure-provider.md`; added new `scripts/lib/providers/k3s-azure.sh` and updated `scripts/lib/core.sh` allowlists so `CLUSTER_PROVIDER=k3s-azure` now provisions an Azure VM, installs k3s with k3sup, merges the kubeconfig as `ubuntu-azure`, and tears the cluster down cleanly; validation used `shellcheck -S warning scripts/lib/providers/k3s-azure.sh`, `shellcheck -S warning scripts/lib/core.sh`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(provider): add k3s-azure provider — VM provision, k3sup install, shopping-cart deploy`
- **COMPLETE:** ACG restart failure screenshots archived to shared screenshots dir — k3d-manager commit `acb8a16d` on `k3d-manager-v1.6.4`; `scripts/lib/acg/playwright/acg_restart.js` still writes `k3dm-acg-screenshot-<ts>.png` on failure, and `bin/k3dm-webhook` now copies that PNG into `~/.local/share/k3d-manager/screenshots/` and recognizes the archived path in failure analysis; validation used `node --check scripts/lib/acg/playwright/acg_restart.js`, `python3 -m py_compile bin/k3dm-webhook`, `bats scripts/tests/lib/acg.bats`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(webhook): archive ACG restart screenshots to shared dir`
- **REVERTED:** lib-acg subtree sync for Azure panel reopen fix — k3d-manager commit `df7bf539` reverted merge commit `0cf7c02a` / squashed subtree import `b510f592`, restoring the pre-sync state because the upstream lib-acg fix has not been tested or merged into the tracked subtree yet; the Azure panel reopen bug doc remains open for the next round.
- **BUG DOC:** Azure provider panel closed reopen loop — `docs/bugs/2026-06-09-azure-provider-panel-closed-reopen-loop.md`; the Azure credential wait loop repeatedly logs `Azure panel closed — re-opening to retrieve credentials...` even when the sandbox is valid, so the panel reopen heuristic needs a follow-up.
- **COMPLETE:** Changelog note for prompt hardening — k3d-manager commit `c248f220` on `k3d-manager-v1.6.4`; `CHANGELOG.md` now records that the `/claude`, `/gemini`, and `/codex` prompts keep raw probe commands and verbose kubectl output out of the Slack `ANSWER:` while preserving concise diagnostic conclusions; no code or doc behavior changed beyond the changelog note; commit message: `docs: note prompt hardening in changelog`
- **COMPLETE:** Remaining fork-based webhook subprocess calls removed — k3d-manager commit `7f1253f9` on `k3d-manager-v1.6.4`; bug doc: `docs/bugs/2026-06-09-webhook-macos-nef-remaining-fork-paths.md`; `bin/k3dm-webhook` now uses `os.posix_spawn` for the last fork-based helper paths (`SLACK_SIGNING_SECRET` / webhook token keychain lookups, ArgoCD label lookups and patching, cleanup commands, and the `make up` launcher in `_run_upgrade`), so the webhook no longer relies on `subprocess.run`/`Popen` in the macOS NEF hot path; validation used `python3 -m py_compile bin/k3dm-webhook`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(webhook): remove remaining fork-based subprocess calls`
- **COMPLETE:** Slack thread replies use bare command names — k3d-manager commit `2fcbb73c` on `k3d-manager-v1.6.4`; `docs/howto/slack-slash-commands.md` now says thread replies should use `claude`, `gemini`, or `codex` without a leading `/`, and the thread context section still explains that the last 20 thread messages are fetched and prepended to the agent prompt; validation used `git diff --check -- docs/howto/slack-slash-commands.md`; commit message: `docs: clarify thread replies use bare command names`
- **COMPLETE:** Slack command docs refined for Codex diagnostics — k3d-manager commit `7e7db5ef` on `k3d-manager-v1.6.4`; `docs/howto/slack-slash-commands.md` now says `codex` is for code questions plus read-only kubectl diagnostics for pod investigation, with `kubectl describe` only when a pod is not Running/Completed; this reflects the actual prompt behavior more accurately than the prior blanket "No" entry; validation used `git diff --check -- docs/howto/slack-slash-commands.md`; commit message: `docs: clarify codex read-only kubectl diagnostics in slack commands`
- **COMPLETE:** ACG credentials flow rendered as Mermaid sequence diagram — k3d-manager commit `96c5b8c9` on `k3d-manager-v1.6.4`; `docs/howto/acg-credentials-flow.md` now replaces the ASCII decision tree with a Mermaid `sequenceDiagram` covering the `acg_get_credentials` launch, CDP attach, sign-in, sandbox open/start/resume choices, AWS credential extraction, and manual paste fallback; validation used `git diff --check -- docs/howto/acg-credentials-flow.md`; commit message: `docs: convert acg credentials flow to sequence diagram`
- **COMPLETE:** Slack command docs aligned with current alias surface — k3d-manager commits `972b82bd` + `e492ac90` on `k3d-manager-v1.6.4`; `docs/howto/slack-slash-commands.md` now leads with `/claude`, `/gemini`, and `/codex` as the public agent commands, keeps only a legacy `/ask` compatibility note for thread replies, and documents `/argocd-upgrade <chart_version> [acg|infra]` with the current stage default + behavior; `docs/architecture/cloudflare-slack-relay.md` now describes `/api/v1/argocd-upgrade` as the label-patch workflow and updates the thread-reply example to `/claude`; `README.md` now links the Slack slash-commands guide from the main How-To index; validation used `git diff --check -- docs/howto/slack-slash-commands.md docs/architecture/cloudflare-slack-relay.md README.md`; commit message: `docs: link Slack commands guide from README`
- **COMPLETE:** Slack `logs` command fallback — k3d-manager commit `86f2ae89` on `k3d-manager-v1.6.4`; bug doc: `docs/bugs/2026-06-09-logs-command-falls-back-to-output.md`; `bin/k3dm-webhook` now has the `logs` thread command fall back to `JOB_DIR/<job_id>/output` when `log` is absent, so `acg-up` jobs that only write `output` no longer reply `No log found for job <job_id>`; validation used `python3 -m py_compile bin/k3dm-webhook`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(webhook): logs command falls back to output for acg-up jobs`
- **COMPLETE:** Pushgateway deployment metrics retry gap — k3d-manager commit `5d4273cd` on `k3d-manager-v1.6.4`; bug doc: `docs/bugs/2026-06-09-pushgateway-deployment-metrics-gap.md`; `bin/k3dm-webhook` now probes `/-/healthy` and retries the Pushgateway metric POST up to 6 times with 5s sleeps before giving up, so transient Pushgateway readiness gaps no longer drop deployment samples from Grafana; validation used `python3 -m py_compile bin/k3dm-webhook`, `./scripts/k3d-manager _agent_audit`, and `git diff --check`; commit message: `fix(webhook): retry Pushgateway metric pushes on transient readiness gaps`
- **BUG DOC:** Prometheus basic auth should be Vault-managed — `docs/bugs/2026-06-09-prometheus-basic-auth-vault-managed.md`; follow-up notes that the current `admin:password` bootstrap and `show-service-passwords` output should be replaced with Vault-backed Prometheus credentials in the next round.
- **COMPLETE:** Prometheus login info in `show-service-passwords` — k3d-manager commit `911f2d84` on `k3d-manager-v1.6.4`; `Makefile` `show-service-passwords` now includes a Prometheus block that reads the Vault root token, fetches `secret/k3d-manager/prometheus-basic-auth`, and prints `https://prometheus.3ai-talk.org` with `user: admin` and `password: password` so the new basic-auth bootstrap is reflected in the service credential report; validation used `make -n show-service-passwords`, `git diff --check -- Makefile`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(makefile): show Prometheus login info in service credentials`
- **COMPLETE:** Prometheus auth bootstrap non-interactive — k3d-manager commit `9f06bd6d` on `k3d-manager-v1.6.4`; bug doc: `docs/bugs/2026-06-08-prometheus-auth-missing.md`; `scripts/plugins/observability.sh` now creates `secret/k3d-manager/prometheus-basic-auth` through the Vault HTTP API instead of the interactive `kubectl run --rm -it vault ... vault kv put ...` path, retries the read after creation, and hard-fails if the secret still cannot be read so `prometheus-web-config` is never rendered with an empty unauthenticated config; validation used `shellcheck -S warning scripts/plugins/observability.sh` and `git diff --check -- scripts/plugins/observability.sh docs/bugs/2026-06-08-prometheus-auth-missing.md`; commit message: `fix(observability): make prometheus auth bootstrap non-interactive`
- **COMPLETE:** docs bug filings + package-lock ignore — k3d-manager commit `e7990da7` on `k3d-manager-v1.6.4`; committed the new 2026-06-08 bug docs under `docs/bugs/` and `docs/issues/`, and added `package-lock.json` to `.gitignore` so the incidental local lockfile stays untracked; commit message: `docs: capture bug docs and ignore package-lock.json`
- **COMPLETE:** Prometheus Pushgateway + Grafana deployment metrics — k3d-manager commit `f7a28f85` on `k3d-manager-v1.6.4`; `bin/k3dm-webhook` now calls `_push_metrics(action, provider, status, duration_secs, job_id)` from `_finish()` in both `_run_cluster` and `_run_cluster_resume`, pushing `k3dm_deployment_duration_seconds`, `k3dm_deployment_last_timestamp_seconds`, and `k3dm_deployment_success` gauges to Prometheus Pushgateway at `localhost:9091` (env: `K3DM_PUSHGATEWAY_URL`); `bin/acg-up` Step 14c installs Pushgateway port-forward LaunchAgent; `bin/acg-down` unloads it; `scripts/plugins/observability.sh` `_deploy_pushgateway_acg()` Helm-installs `prometheus-community/prometheus-pushgateway` v2.14.0 and applies `scripts/etc/grafana/dashboards/k3dm-deployments-configmap.yaml`; Prometheus scrape job added to `kube-prometheus-stack-acg-values.yaml`; Grafana dashboard "k3dm Deployment Metrics" auto-loaded via sidecar ConfigMap; `docs/architecture/cloudflare-slack-relay.md` Section 3 added with metrics flow diagram; commit message: `feat(observability): Prometheus Pushgateway + Grafana deployment metrics`; **webhook restart pending** (wait for acg-up job to complete)
- **COMPLETE:** thread reply unknown-command noise — k3d-manager commit `46da77cb` on `k3d-manager-v1.6.4`; added `_THREAD_COMMANDS` set + silent-return guard at top of `_handle_thread_command`; conversational replies ("thanks", "ok") now produce no response; all known commands still dispatch correctly; bug doc: `docs/bugs/2026-06-08-thread-reply-triggers-unknown-command.md`; commit message: `fix(webhook): ignore non-command thread replies — silent return for conversational text`
- **COMPLETE:** OBSERVATIONS pod-state diagnosis rule — k3d-manager commit `89d5dee5` on `k3d-manager-v1.6.4`; all three agent system prompts (Claude, Gemini, Codex) now include a pod-state rule requiring `kubectl describe pod` before filing any observation about a pod not in Running/Completed state; bug doc: `docs/bugs/2026-06-08-observations-pod-state-no-describe.md`; commit message: `fix(webhook): require kubectl describe before filing pod-state OBSERVATIONS`
- **COMPLETE:** prometheus-operator ContainerCreating fix — k3d-manager commit `7cf0b92e` on `k3d-manager-v1.6.4`; `scripts/etc/helm/observability/kube-prometheus-stack-acg-values.yaml` now sets `prometheusOperator.tls.enabled: false` alongside existing `admissionWebhooks.enabled: false`; `admissionWebhooks.enabled: false` alone was insufficient — operator still mounted `acg-kube-prometheus-stack-admission` TLS secret for its own HTTPS endpoint; live fix: deleted both webhook configurations and stuck pod; operator came up 1/1 Running; bug doc `docs/bugs/2026-06-07-prometheus-stack-operator-admission-secret-missing.md` updated to document both keys are required; commit message: `fix(observability): disable prometheusOperator TLS to fix operator ContainerCreating`
- **COMPLETE:** webhook slash command aliases + diagnostic tool access + Gemini prompt fix — k3d-manager commit `c29f2662` on `k3d-manager-v1.6.4`; spec: `docs/bugs/v1.6.4-bugfix-combined.md`; `bin/k3dm-webhook` now normalizes `/gemini`, `/claude`, and `/codex` into `ask` dispatch, shows the alias list in the unknown-command help text, spells out allowed diagnostic tools for Claude, and replaces Gemini's "cannot run kubectl" wording with analysis-oriented guidance; `bin/k3dm-ask-bash` now allows read-only diagnostic paths under `/etc`, `/Library/LaunchDaemons`, `/Library/LaunchAgents`, `${HOME}/.kube`, `${HOME}/.local/share/k3d-manager`, `${HOME}/.cloudflared`, `/var/log`, `/usr/local/bin`, `/usr/bin`, `/bin`, and `/opt/homebrew` while still blocking out-of-scope paths; validation used `shellcheck -S warning bin/k3dm-ask-bash` and `python3 -m py_compile bin/k3dm-webhook`; commit message: `feat(webhook): slash command aliases, expanded agent diagnostic tools, Gemini prompt fix`

## Current Status (Post-Merge 2026-06-07 — k3d-manager PR #92 v1.6.3 merged)
- **k3d-manager PR #92 MERGED** — `fix(webhook,acg-up): webhook NEF atfork SIGSEGV fixes + /ask multi-agent + /acg-resume checkpoint-based re-entry`; merge SHA `0d8d24024eeeb6cf2a0701d78d7dbc9acbb719d4` (merged 2026-06-07); enforce_admins restored ✓; v1.6.3 tag created ✓; GitHub release created ✓; v1.6.4 branch created ✓; retrospective committed (`3b0eb93c`) ✓; memory-bank updating (in progress)
- **COMPLETE:** acg-refresh ArgoCD labels + Cloudflare daemon type + /etc/hosts cleanup — k3d-manager commit `15b3d713` on `k3d-manager-v1.6.4`; (1) ArgoCD labels added to cluster-ubuntu-k3s patch; (2) Cloudflare tunnel fixed LaunchDaemons/system → LaunchAgents/user; (3) grafana/prometheus removed from acg-up _HOSTS_LIST; issue docs: `docs/issues/2026-06-07-acg-refresh-drops-argocd-cluster-labels.md` + `docs/issues/2026-06-08-grafana-routing-loop-etc-hosts-conflict.md`
- **Cluster recovery COMPLETE** (2026-06-08) — Resolved cascading failure from Step 10b; manually synced `data-layer` ArgoCD app; restarted crashing app pods; restored port-forwards via `acg-refresh`; seeded product catalog DB. All services healthy.
- Current branch: `k3d-manager-v1.6.4`
- Active cluster state: `ubuntu-k3s` fully bootstrapped and healthy; `k3d-k3d-cluster` healthy.
- **COMPLETE:** all post-NEF subprocess.run calls migrated — k3d-manager commit `da0a2693` on `k3d-manager-v1.6.3`; `bin/k3dm-webhook` now uses `_posix_spawn_capture` (no fork) for ALL subprocess calls that run after any HTTPS request has loaded macOS NEF: `_run_cluster_status` (hub nodes, ACG nodes, acg_state, pods, ArgoCD apps, AWS), `_run_post_provision_check` (ArgoCD apps + triage kubectl/logs), `_run_cluster_refresh` (acg-refresh with __WEBHOOK_SUCCESS__ sentinel), `_run_analyze` (kubectl describe+pods), `_collect_cluster_state` (nodes+pods+apps), and `_record_acg_state` (configmap via bash pipe); SSH tunnel check now uses Python `socket.create_connection` (no subprocess at all); `/acg-status` infrastructure checks now return real data instead of all ❌; webhook restarted
- **COMPLETE:** smoke test data-layer readiness — k3d-manager commit `ae1da7eb` on `k3d-manager-v1.6.3`; `_smoke_test_services` now checks all 4 data-layer StatefulSets (`postgresql-orders`, `postgresql-payment`, `postgresql-products`, `minio`) on `ubuntu-k3s` via `_posix_spawn_capture` (safe after NEF load); result appended as `Data layer: N/4 ready`; covers all 3 call sites: post-provision, /acg-status, /acg-refresh; webhook restarted
- **COMPLETE:** post-provision smoke always runs + /acg-status reconcile — k3d-manager commits `619d47cf` + `0bdf74e9` on `k3d-manager-v1.6.3`; `619d47cf` removed the tunnel early-return; `0bdf74e9` removed the second ArgoCD early-return (moved ArgoCD lines into else branch so `_smoke_test_services` always runs regardless of tunnel or hub-cluster availability); `_run_cluster_status` reconciliation note added; webhook restarted
- **COMPLETE:** /ask claude+codex NEF atfork SIGSEGV — k3d-manager commit `638f9064` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-webhook-ask-subprocess-sigsegv.md`; `bin/k3dm-webhook` now uses `_posix_spawn_capture(cmd, timeout, cwd=None, env=None)` after `_call_gemini` and before `_analyze_stall`, and `_run_cluster_ask` now captures `claude`/`codex` output via `os.posix_spawn` instead of `subprocess.run` so macOS NEF atfork SIGSEGV no longer crashes the webhook; validation used `python3 -m py_compile bin/k3dm-webhook` and `make restart-webhook`; commit message: `fix(webhook): replace subprocess.run with posix_spawn for /ask claude and /ask codex to fix NEF atfork SIGSEGV`
- **COMPLETE:** data-layer sync timeout false failure — k3d-manager commit `e712ef44` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-up-data-layer-sync-timeout-false-failure.md`; `bin/acg-up` now checks the four data-layer StatefulSets (`postgresql-orders`, `postgresql-payment`, `postgresql-products`, `minio`) at the 180s retry timeout, and if they are all Ready it `_warn`s and continues instead of `_err`/`exit 1`; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): demote data-layer sync timeout to warning when StatefulSets are Ready`
- **COMPLETE:** ask-agent sanitize order — k3d-manager commit `4ebe23c6` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-webhook-ask-sanitize-order.md`; `bin/k3dm-webhook` now sanitizes the raw user question immediately in the ask command handler before prepending job context, rejects empty sanitized questions there, and changes `_run_cluster_ask` to perform injection-only validation on the assembled prompt; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): sanitize user question before job context prepend to fix ask-agent rejection`
- **COMPLETE:** /tmp file leaks cleanup — k3d-manager commit `cb17dd72` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-tmp-file-leaks.md`; `bin/install-sudoers.sh` now installs an `EXIT` trap to remove `_tmpfile`, `scripts/lib/providers/k3s-oci-storage.sh` now removes the downloaded etcd snapshot on upload failure, `bin/acg-down` now removes tunnel/watch logs and `/tmp/k3dm-acg-screenshot-*.png`, `bin/k3dm-cleanup` now prunes `/tmp/k3dm-acg-screenshot-*.png` (keeping 5) and stale `playwright-artifacts-*` dirs, and `Makefile` adds `clean-tmp` plus the `.PHONY` entry and places the target immediately after `restart-webhook`; validation used `shellcheck -S warning bin/install-sudoers.sh`, `shellcheck -S warning scripts/lib/providers/k3s-oci-storage.sh`, `shellcheck -S warning bin/acg-down`, `shellcheck -S warning bin/k3dm-cleanup`, `make -n clean-tmp`, and `git diff --check`; commit message: `fix(tmp): add EXIT traps and cleanup for /tmp file leaks from install-sudoers, k3s-oci-storage, and session teardown`
- **COMPLETE:** data-layer sync skip-if-ready — k3d-manager commit `63ef22b7` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-data-layer-skip-if-ready.md`; `bin/acg-up` now checks the four data-layer StatefulSets (`postgresql-orders`, `postgresql-payment`, `postgresql-products`, `minio`) on `ubuntu-k3s` before step 10b and, when all are ready, writes the checkpoint and skips the ArgoCD sync wait plus PostSync force-sync path; validation used `shellcheck -S warning bin/acg-up`, `bats scripts/tests/bin/acg_up.bats`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): skip data-layer sync wait when StatefulSets already ready on ubuntu-k3s`
- **COMPLETE:** data-layer ArgoCD controller reconnect wait — k3d-manager commit `0c759324` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-data-layer-argocd-controller-reconnect-race.md`; `bin/acg-up` now waits up to 120s after the ArgoCD application controller rollout for `argocd cluster list -o json` to report `ubuntu-k3s` as `Successful`, warning and continuing if the controller never reconnects before step 10b starts; validation used `shellcheck -S warning bin/acg-up`, `bats scripts/tests/bin/acg_up.bats`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): wait for ArgoCD controller to reconnect to ubuntu-k3s before data-layer sync`
- **COMPLETE:** ArgoCD cluster secret stale refresh — k3d-manager commit `b46391f1` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-argocd-cluster-secret-stale.md`; `bin/acg-up` no longer computes `_cp_server` and now registers the ArgoCD app cluster with the default `https://host.k3d.internal:6443` server, while `bin/acg-refresh` adds a new step 2b to refresh the ArgoCD cluster secret with the current `argocd-manager-token` after sandbox rotation; validation used `shellcheck -S warning bin/acg-refresh` and `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up,acg-refresh): refresh ArgoCD cluster secret with host.k3d.internal on each sandbox rotation`
- **COMPLETE:** acg-refresh section 2b `register_app_cluster` unbound — k3d-manager commit `8ebf6d6b` on `k3d-manager-v1.6.3`; bug doc: `docs/bugs/v1.6.3-bugfix-acg-refresh-register-app-cluster-unbound.md`; Codex used `register_app_cluster` (not in scope in `acg-refresh` — defined in `scripts/plugins/argocd.sh` which is never sourced) instead of spec's direct heredoc+`kubectl apply`; replaced with `mktemp` + xtrace-save/restore + heredoc + `kubectl apply -f` + `rm`; shellcheck clean; commit message: `fix(acg-refresh): replace register_app_cluster with direct kubectl apply in section 2b`
- **COMPLETE:** data-layer ArgoCD sync timeout auto-retry — k3d-manager commit `d42377b5` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-data-layer-sync-timeout-retry.md`; `bin/acg-up` step 10b timeout handler now warns, runs `argocd app sync data-layer --force --timeout 180`, and waits another 180s before `_err` so transient ArgoCD slowness can recover without human intervention; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): auto force-sync data-layer on ArgoCD sync timeout before failing`
- **COMPLETE:** /ask Slack command — k3d-manager commit `12b5874c` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-acg-ask-claude-troubleshoot.md`; `bin/k3dm-webhook` now adds `_run_cluster_ask(job_id, agent, question, response_url, thread_ts=None)` with claude/gemini/codex subprocess dispatch plus 120s timeout/ANSI stripping, exposes `/api/v1/ask`, and `workers/slack-relay/index.js` now allows `/ask` and forwards the agent/question/thread context; validation used `python3 -m py_compile bin/k3dm-webhook` and `node --check workers/slack-relay/index.js`; commit message: `feat(webhook,worker): add /ask Slack command — claude/gemini/codex multi-agent troubleshooting`
- **COMPLETE:** host.k3d.internal DNS fast-path — `bin/k3dm-webhook` `_analyze_failure` now detects `host.k3d.internal` + `no such host` and returns CLUSTER_FAILURE/TRANSIENT with `/acg-refresh` then `/acg-resume` advice; py_compile clean; webhook restarted PID 54211
- **COMPLETE:** acg-up EXIT trap kills ArgoCD port-forward — k3d-manager commit `31cfc6d3` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-up-cleanup-kills-argocd-pf.md`; `_acg_up_cleanup` no longer unloads the persistent ArgoCD LaunchAgent on EXIT, so step 4b survives successful exits and failures alike; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): remove ArgoCD port-forward unload from EXIT trap`
- **COMPLETE:** Gemini CLI Warning: banners in Slack — k3d-manager commit `6a5514b8` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-gemini-cli-warnings-in-slack.md`; `bin/k3dm-webhook` now strips `Warning:` banner lines from Gemini CLI output immediately after reading the temp file and before the existing cleanup regexes, so Slack failure analysis no longer includes startup banners; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): strip Gemini CLI Warning: banners from failure analysis output`
- **COMPLETE:** Gemini NEF fork bug + Keycloak port 18080 conflict — k3d-manager commit `14552407` on `k3d-manager-v1.6.3`; `bin/k3dm-webhook` `_call_gemini` replaced `subprocess.run(capture_output=True)` with `os.posix_spawn` + temp file output (same pattern as `_posix_spawn_job`) to avoid macOS NEF atfork SIGSEGV that killed the Gemini child silently; `bin/acg-up` step 10d changed `lsof -ti | head -1` to `lsof -ti` (all PIDs) + `xargs kill` + sleep 2 to clear all holders of port 18080 before the Keycloak port-forward; commit message: `fix(webhook,acg-up): fix Gemini NEF fork bug and Keycloak port 18080 conflict`
- **COMPLETE:** NEF atfork SIGSEGV documented — issue doc: `docs/issues/2026-06-07-webhook-macos-nef-atfork-sigsegv.md`; README Issue Logs table updated
- **COMPLETE:** auto-install acg npm deps — k3d-manager commit `8e884706` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-bugfix-acg-npm-deps.md`; `bin/acg-refresh` and `bin/acg-up` now guard `[[ ! -d "${REPO_ROOT}/scripts/lib/acg/node_modules/playwright" ]]` and run `npm --prefix ... install --prefer-offline` when node_modules missing
- **COMPLETE:** `/acg-resume` Slack command — k3d-manager commit `bcc3a56e` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-slack-resume-command.md`; 13 changes across `bin/acg-up`, `Makefile`, `bin/k3dm-webhook`, `workers/slack-relay/index.js`
- **COMPLETE:** /ask agent hardening (2026-06-06) — commits `2f19e4e5`→`c1a02efc` on `k3d-manager-v1.6.3`; gemini text-only via `_call_gemini` (no tool-approval hang), codex `--skip-git-repo-check` after `exec`, semaphore(2)+max-turns+timeout, read-only bash sandbox (`bin/k3dm-ask-bash`), prompt injection guard+structural separation, repo scope enforcement `--add-dir`+path guard, thread context via `conversations.replies` API, filing mode (write `docs/issues/`+git commit/push), fix mode (`K3DM_FIX_MODE=1`: rollout restart, delete pod, argocd sync); docs updated
- **COMPLETE:** argocd-redis cicd namespace missing on ubuntu-k3s — k3d-manager commit `e11408cb` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-up-argocd-redis-namespace-missing.md`; `bin/acg-up` now explicitly creates `cicd` namespace on `ubuntu-k3s` via idempotent apply before argocd-redis secret step, removing fragile 120s wait loop
- **COMPLETE:** prometheus port-forward wrong kubectl context — k3d-manager commit `51939697` on `k3d-manager-v1.6.3`; spec: `docs/bugs/2026-06-06-prometheus-port-forward-wrong-kubectl-context.md`; `scripts/etc/launchd/com.k3d-manager.prometheus-port-forward.plist.tmpl` now targets `k3d-k3d-cluster` instead of `ubuntu-k3s` so the `monitoring` namespace is port-forwarded from the hub cluster; validation used `git diff --check` on the plist change; commit message: `fix(launchd): prometheus port-forward plist targets k3d-k3d-cluster not ubuntu-k3s`; user still runs `make install-prometheus-port-forward` manually to reinstall the live plist
- **COMPLETE:** acg-refresh CoreDNS restart-only (no ConfigMap patch) — k3d-manager commit `182c2f7f` on `k3d-manager-v1.6.3`; spec: `docs/bugs/2026-06-06-acg-refresh-coredns-restarts-instead-of-patching-configmap.md`; `bin/acg-refresh` now detects the host IP via `docker exec k3d-k3d-cluster-server-0 getent hosts host.docker.internal`, patches the CoreDNS `NodeHosts` ConfigMap with `host.k3d.internal` prepended ahead of existing entries, then restarts CoreDNS and waits for rollout; validation used `shellcheck -S warning bin/acg-refresh`; commit message: `fix(acg-refresh): patch CoreDNS NodeHosts ConfigMap with host.k3d.internal before restart`
- **COMPLETE (manual live fix 2026-06-06):** CoreDNS NodeHosts ConfigMap patched `0.250.250.254 host.k3d.internal`; shopping-cart apps syncing
- **COMPLETE:** .ask-sandbox/bash removed from git — commit `4f8e52b8`; absolute-path symlink was blocking ArgoCD from comparing any app on this branch; webhook now creates symlink at startup
- **COMPLETE:** DNS short-circuit advice incomplete — k3d-manager commit `45bc9afd` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-bugfix-dns-short-circuit-advice.md`; `bin/acg-refresh` now restarts CoreDNS on the hub cluster before step 2b and confirms the rollout/status to restore `host.k3d.internal` resolution, while `bin/k3dm-webhook` now explains the CoreDNS NodeHosts cache issue and says `/acg-refresh` both restarts CoreDNS and re-registers the ArgoCD cluster secret before `/acg-resume`; validation used `shellcheck -S warning bin/acg-refresh` and `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(acg-refresh,webhook): restart CoreDNS on hub cluster in acg-refresh to restore host.k3d.internal resolution`
- **COMPLETE:** Gemini side-observation bug filing — k3d-manager commit `f52363cc` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-gemini-bug-filing.md`; `Makefile` now adds `file-bug` after `fix-status`, `bin/k3dm-ask-bash` allows `make file-bug` in fix mode, and `bin/k3dm-webhook` now requires Gemini `ANSWER:` + optional `OBSERVATIONS:` blocks, parses observations into `docs/bugs/YYYY-MM-DD-<slug>.md`, and appends `Also filed: ...` to Slack replies; validation used `shellcheck -S warning bin/k3dm-ask-bash`, `python3 -m py_compile bin/k3dm-webhook`, and `make -n file-bug FILE_TITLE=\"test\" FILE_BODY=\"test\"`; commit message: `feat(webhook,ask-sandbox): gemini side-observation bug filing — structured OBSERVATIONS block + make file-bug target`
- **COMPLETE:** /ask codex+claude OBSERVATIONS filing — k3d-manager commit `628ca8a6` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-bugfix-ask-observations-all-agents.md`; `bin/k3dm-webhook` now adds the mandatory `ANSWER:` / optional `OBSERVATIONS:` response format to both `claude_system` and `codex_system`, sets `_observe` for the non-filing claude path and codex path, and routes their shared subprocess finish block through `_parse_gemini_observations` so codex and claude observations file bug docs like gemini; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): extend OBSERVATIONS bug-filing to /ask codex and /ask claude paths`
- **COMPLETE:** dedup bug/issue filing — k3d-manager commit `ce561b0e` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-bugfix-dedup-bug-filing.md`; `bin/k3dm-webhook` now glob-checks `docs/bugs/*-<slug>.md` before writing Gemini-filed bug docs and reuses the existing path when found; `Makefile` `file-bug` now exits 0 with `already filed: <path>` if a slug already exists; `CLAUDE.md` now includes a `Docs Conventions` section between Code Style and Security Rules telling agents to dedup before filing into `docs/bugs/` or `docs/issues/`; validation used `python3 -m py_compile bin/k3dm-webhook` and `make -n file-bug FILE_TITLE=\"test\" FILE_BODY=\"test\"`; commit message: `fix(webhook,makefile): guard against duplicate bug filing — skip write if slug already exists in docs/bugs/`
- **COMPLETE:** `make fix-*` agent fix targets — k3d-manager commits `ccbe1396` + `0ec95290` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-fix-make-targets.md`; `Makefile` now adds `fix-list`, `fix-restart`, `fix-delete-pod`, `fix-sync`, `fix-force-sync`, `fix-eso-refresh`, and `fix-status`; `bin/k3dm-ask-bash` now allows `make fix-*` in fix mode; `bin/k3dm-webhook` fix-mode prompt tells Claude to run `make fix-list` first; `0ec95290` adds inline `## description` to `fix-list:` so it self-documents; `make fix-list` shows all 7 targets
- **COMPLETE:** acg-resume kubeconfig fetch retry + Gemini fix advice to /acg-resume — k3d-manager commit `a8c5d6b0` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-resume-kubeconfig-retry-slack-fix.md`; `bin/acg-up` step-3c guard now retries `add_ubuntu_k3s_cluster` up to 3 times with 5s sleeps after checkpoint-skipped step 2, and `_err`s with Slack advice to retry via `/acg-resume` after all attempts fail; `bin/k3dm-webhook` now short-circuits `_analyze_failure` for `context "ubuntu-k3s" does not exist` and updates Gemini's fix hint to `/acg-resume` in Slack; validation used `shellcheck -S warning bin/acg-up` and `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(acg-up,webhook): retry kubeconfig fetch on SSH delay + fix Gemini fix-hint to /acg-resume`
- **COMPLETE:** acg-resume ubuntu-k3s context missing — k3d-manager commit `a4c5bc95` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-resume-ubuntu-k3s-context-missing.md`; `bin/acg-up` now adds a step 3c guard that re-fetches the `ubuntu-k3s` kubeconfig via `add_ubuntu_k3s_cluster` when the context is missing after checkpoint-skipped step 2, and `bin/k3dm-webhook` now short-circuits `_analyze_failure` with targeted advice for `context "ubuntu-k3s" does not exist` telling the user to run `/acg-refresh` and retry `/acg-resume`; validation used `shellcheck -S warning bin/acg-up` and `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(acg-up,webhook): restore ubuntu-k3s kubeconfig on resume + targeted Slack advice`
- **COMPLETE:** /acg-refresh Slack slash command — k3d-manager commit `ba66f049` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-bugfix-acg-refresh-slack-command.md`; `bin/k3dm-webhook` now accepts `/acg-refresh` and routes it to `/api/v1/cluster-refresh`, while `workers/slack-relay/index.js` relays the slash command to the webhook and preserves thread context; validation used `python3 -m py_compile bin/k3dm-webhook`, `node --check workers/slack-relay/index.js`, `make restart-webhook`, and `cd workers/slack-relay && npx wrangler deploy`; commit message: `feat(webhook,worker): add /acg-refresh Slack slash command to restore tunnel + credentials`
- **COMPLETE:** PostSync hooks force-sync — k3d-manager commit `7811e450` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-bugfix-data-layer-postsync-hooks.md`; `bin/acg-up` now force-syncs `data-layer` immediately after `deploy_shopping_cart_data` to trigger PostSync hooks (MinIO bucket init + image upload); validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): force-sync data-layer after deploy to trigger PostSync hooks`
- **COMPLETE:** ESO smoke checks — k3d-manager commit `954576fb` on `k3d-manager-v1.6.2`; spec: `docs/plans/v1.6.2-eso-smoke-check.md`; `_smoke_test_services(retries=None)` now supports the on-demand fast path and appends ESO `ClusterSecretStore/vault-backend` plus `ExternalSecrets` sync status to the post-provision smoke report, with ESO pod-state hints added to the smoke triage `ns_map`; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `feat(webhook): add ESO ClusterSecretStore + ExternalSecrets checks to smoke test`
- **COMPLETE:** Keycloak admin token retry on slow start — k3d-manager commit `b603547b` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-bugfix-keycloak-token-retry.md`; `bin/acg-up` now uses a 5-attempt retry loop with 15s sleeps between attempts before fetching the Keycloak admin token, and only kills the port-forward on final failure; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): retry Keycloak admin token fetch up to 5 times on slow start`
- **COMPLETE:** acg-status on-demand health check — k3d-manager commit `b3bacf13` on `k3d-manager-v1.6.2`; spec: `docs/plans/v1.6.2-acg-status-health-check.md`; `_run_cluster_status()` now runs `_smoke_test_services(retries=1)` and Gemini triage on demand, `bin/k3dm-webhook` exposes `GET /api/v1/health` for the JSON smoke report, and `bin/acg-status` appends a Service Health section that curls the webhook endpoint with the bearer token from Keychain; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck -S warning bin/acg-status`, and `make restart-webhook`; commit message: `feat(webhook,acg-status): on-demand smoke test + Gemini triage in acg-status and /acg-status slash command`
- **COMPLETE:** smoke endpoint fixes — k3d-manager commit `6ad543d0` on `k3d-manager-v1.6.2`; added Keycloak (`http://keycloak.shopping-cart.local/health/live`) to `_SMOKE_ENDPOINTS`, changed Grafana URL from `/login` to `/` (Cloudflare tunnel only exposes root path), updated Gemini triage `ns_map` to include Keycloak in `identity` namespace; webhook restarted
- **COMPLETE:** Prometheus port-forward LaunchAgent — k3d-manager commit `498dde9e` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-prometheus-port-forward-launchagent.md`; added `scripts/etc/launchd/com.k3d-manager.prometheus-port-forward.plist.tmpl` plus `Makefile` targets `install-prometheus-port-forward` and `uninstall-prometheus-port-forward` so `kubectl port-forward svc/prometheus-operated 19090:9090 -n monitoring --context ubuntu-k3s` stays alive across SSH reconnects; validation used `make -n install-prometheus-port-forward`; commit message: `feat(launchd): add Prometheus port-forward LaunchAgent (ubuntu-k3s:19090)`
- **COMPLETE:** launchd daemon reference doc — `docs/howto/launchd-daemons.md` committed `93c4a94c`; documents all 11 `com.k3d-manager.*` LaunchAgents with ports, clusters, install methods, and log paths
- **COMPLETE:** post-provision service smoke test — k3d-manager commit `8f64cccf` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-smoke-test-post-provision.md`; `bin/k3dm-webhook` now adds `_smoke_test_services()` with retrying HTTP checks for ArgoCD, Frontend, Prometheus, Grafana, and product images, appends the smoke results to the post-provision Slack message, and keeps the existing degraded-app Gemini triage loop unchanged while adding a follow-up Gemini triage pass for failed smoke checks; validation used `python3 -m py_compile bin/k3dm-webhook` and `./scripts/k3d-manager _agent_audit`; commit message: `feat(webhook): post-provision service smoke test — HTTP checks for ArgoCD, Frontend, Prometheus, Grafana, product images`
- **COMPLETE:** lib-acg v0.1.2 — Start Sandbox viewport fix — lib-acg PR #37 merged (`4d5aa477`); subtree pulled into k3d-manager; lib-acg next branch: `feat/v0.1.3`
- **COMPLETE:** lib-acg v0.1.3 — restart-flow Start Sandbox viewport fix — lib-acg commit `d04e70d` on `feat/v0.1.3`; spec: `docs/bugs/2026-06-05-acg-restart-start-sandbox-outside-viewport.md`; `playwright/acg_restart.js` now scrolls `_startBtnEarly` and `_startBtnPanel` into view before clicking in the restart flow, and `CHANGELOG.md` adds an `[Unreleased]` fix entry; validation used `node --check playwright/acg_restart.js`; commit message: `fix(acg_restart): scrollIntoViewIfNeeded before Start Sandbox clicks in restart flow`
- **COMPLETE:** lib-acg v0.1.1 — sandbox navLink.click() regression fix — lib-acg PR #36 merged (`f63b7ca3`); subtree pulled into k3d-manager
- **COMPLETE:** Gemini failure analysis duplicates in Slack — k3d-manager commits `cb7cc9bb`, `35c66881`, `b48cc75a` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-bugfix-gemini-duplicate-analysis-output.md`; real fix: screenshot file path dropped from `_analyze_failure` prompt (`cb7cc9bb`) — without a path Gemini has no trigger for file-reading tool calls; `GEMINI_CLI_TRUST_WORKSPACE=true` removal in `cb7cc9bb` was a regression (broke headless operation) and restored in `b48cc75a`, while `bin/k3dm-cleanup` prunes old failure PNGs and Chromium cache dirs, `scripts/etc/launchd/com.k3d-manager.cleanup.plist.tmpl` schedules the daily cleanup LaunchAgent, `Makefile` adds `install-cleanup`, and `bin/acg-down` now prunes Docker build cache/unused images after k3d teardown; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck -S warning bin/k3dm-cleanup`, and `shellcheck -S warning bin/acg-down`; commit message: `feat(cleanup): add state dir cleanup script + LaunchAgent + docker prune on acg-down`
- **COMPLETE:** lib-acg provider pattern refactor — lib-acg PR #35 merged (SHA `2f0a39c0`); spec: `docs/plans/v1.6.2-lib-acg-provider-refactor.md`; split `playwright/acg_credentials.js` into `playwright/lib/{browser,sandbox,output}.js` plus `playwright/providers/{aws,gcp,azure}.js`, added Jest unit tests under `tests/providers/`; 8 Copilot findings fixed in `145bee4`; subtree pulled into k3d-manager (`8742438a` on `k3d-manager-v1.6.2`)
- **COMPLETE:** Keycloak port-forward LaunchAgent not reinstalled by acg-refresh — k3d-manager commit `67614ab9` on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-keycloak-launchagent-not-reinstalled-by-acg-refresh.md`; `bin/acg-refresh` now regenerates `com.k3d-manager.keycloak-port-forward` from the wrapper script when the plist is missing before `_launchd_ensure`, using either `${_ACG_STATE_DIR}/bin/keycloak-port-forward.sh` or `${_ACG_STATE_DIR}/keycloak-port-forward.sh`; validation used `shellcheck -S warning bin/acg-refresh`; commit message: `fix(acg-refresh): regenerate Keycloak port-forward plist when missing on refresh`
- **COMPLETE:** Vault port-forward LaunchAgent not installed by acg-up — k3d-manager commit `8769dfde` on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-vault-port-forward-launchagent-not-installed-in-acg-up.md`; `bin/acg-up` now installs `com.k3d-manager.vault-port-forward` immediately after Vault is confirmed reachable via `sed` substitution into `~/Library/LaunchAgents/`, and `bin/acg-down` unloads and removes the plist after the PID-based stop so the port-forward is managed as a persistent LaunchAgent during macOS provisioning; validation used `shellcheck -S warning bin/acg-up` and `shellcheck -S warning bin/acg-down`; commit message: `fix(acg-up): install Vault port-forward LaunchAgent during provisioning`
- **COMPLETE:** Prometheus ACG web.config.file conflict bugfix — commit `c21b7cc9` on `k3d-manager-v1.6.1`; removed `additionalArgs` + `secrets` from `kube-prometheus-stack-acg-values.yaml`; prometheus-operator v0.79.2 blocks `web.config.file` in `additionalArgs` (managed internally); bug doc: `docs/bugs/v1.6.1-bugfix-prometheus-acg-web-config-arg.md`
- **COMPLETE:** /acg-status stale ArgoCD display — k3d-manager commit `6651a192` on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-acg-status-stale-argocd-display.md`; `bin/k3dm-webhook` now softens the unreachable message to say the cluster may be rebuilding or the sandbox may have expired, and adds `_(cached — ACG cluster was unreachable)_` to the ArgoCD apps header when `acg_reachable` is false so stale hub data is clearly labeled; validation used `python3 -m py_compile bin/k3dm-webhook` and the running webhook was restarted with `make restart-webhook`; commit message: `fix(webhook): add stale caveat to ArgoCD apps when ACG cluster unreachable`
- **COMPLETE:** bin/acg-up LaunchAgent RunAtLoad fix — k3d-manager commit `3678b8ef` on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-launchagent-missing-runatload.md`; added `RunAtLoad` to the ArgoCD and Keycloak LaunchAgent heredocs and added a new frontend-port-forward LaunchAgent generator with `RunAtLoad`, `KeepAlive`, and `EnvironmentVariables` `KUBECONFIG`; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): add RunAtLoad and frontend-port-forward generator to LaunchAgent plists`
- **COMPLETE:** ArgoCD port-forward LaunchAgent missing RunAtLoad — added `RunAtLoad: true` to `~/Library/LaunchAgents/com.k3d-manager.argocd-port-forward.plist` and re-bootstrapped; suspected trigger: macOS upgrade unloaded agent from gui domain without restart; bug doc: `docs/bugs/v1.6.1-bugfix-argocd-launchagent-missing-runatload.md`; 502 resolved, port 8080 healthy
- **COMPLETE:** Vault port-forward LaunchAgent — k3d-manager commit `8046537f` on `k3d-manager-v1.6.1`; spec: `docs/plans/v1.6.1-vault-port-forward-launchagent.md`; added `scripts/etc/launchd/com.k3d-manager.vault-port-forward.plist.tmpl` plus `Makefile` targets `install-vault-port-forward` and `uninstall-vault-port-forward` so `kubectl port-forward vault-0 18200:8200 -n secrets --context k3d-k3d-cluster` stays alive on mac; validation used `make -n install-vault-port-forward`, `./scripts/k3d-manager _agent_checkpoint`, `./scripts/k3d-manager _agent_lint`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(launchd): add Vault port-forward LaunchAgent to keep port 18200 alive`
- **COMPLETE:** Slack slash commands + Worker secrets fixed — slash command Request URLs updated to `https://k3dm-slack-relay.k3dm.workers.dev`; `SLACK_SIGNING_SECRET` + `WEBHOOK_TOKEN` re-synced; `/acg-status` confirmed working in #acg-automation; issue doc: `docs/issues/2026-06-04-slack-slash-commands-wrong-url.md`
- **COMPLETE:** webhook plist Python 3.13 fix — live LaunchAgent plist updated to `/opt/homebrew/bin/python3` (Python 3.13); plist template `scripts/etc/launchd/com.k3d-manager.webhook.plist.tmpl` updated to match; Python 3.9 SIGSEGV on macOS 26.5.1 Beta resolved; bug doc: `docs/bugs/v1.6.0-bugfix-webhook-python39-sigsegv.md`
- **COMPLETE:** webhook restart-orphan Slack notify — k3d-manager commit `1549f4c4` on `k3d-manager-v1.6.0`; `_clear_stale_jobs()` now posts Slack notification when orphaning a running job; spec: `docs/plans/v1.6.0-bugfix-webhook-restart-orphan.md`
- **COMPLETE:** Slack thread commands — k3d-manager commit `1726f1a8` on `k3d-manager-v1.6.0`; kill/status/logs/diagnosis from job thread replies via Events API; spec: `docs/plans/v1.6.0-slack-thread-commands.md`
- **COMPLETE:** Slack threading for webhook job notifications — k3d-manager commit `ba2e7974` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-slack-threading.md`; `bin/k3dm-webhook` now adds `SLACK_BOT_TOKEN`/`SLACK_CHANNEL_ID`, posts thread-root notifications through `chat.postMessage`, saves `JOB_DIR/<job_id>/thread_ts`, and threads all subsequent job updates, stall alerts, failure analyses, cleanup notices, and post-provision health checks through `_notify_job()` while keeping the incoming-webhook fallback; `Makefile` now exposes `update-webhook-slack` to inject the Slack env vars into the LaunchAgent plist and restart the webhook; validation used `python3 -m py_compile bin/k3dm-webhook`, `bats scripts/tests/lib/webhook.bats`, and `make -n update-webhook-slack SLACK_BOT_TOKEN=x SLACK_CHANNEL_ID=y`; commit message: `feat(webhook): Slack threading — group all job notifications into a single thread`
- **COMPLETE:** k3s-aws idempotent re-run guard — k3d-manager commit `d86b9ca6` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-acg-up-idempotency.md`; `scripts/lib/providers/k3s-aws.sh` now checks `kubectl get nodes --context ubuntu-k3s --no-headers` before `deploy_app_cluster --confirm` and skips the k3s deploy when all three nodes are already Ready, preserving idempotent re-runs; validation used `shellcheck -S warning scripts/lib/providers/k3s-aws.sh` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(k3s-aws): skip deploy_app_cluster when nodes already Ready — idempotent re-run`
- **COMPLETE:** k3dm-webhook server landed on `k3d-manager-v1.6.0`; commit `5084ef9b` (`feat(webhook): k3dm-webhook server for remote ACG/infra upgrade orchestration`) adds `bin/k3dm-webhook`, `bin/k3dm-webhook-setup`, `scripts/etc/launchd/com.k3d-manager.webhook.plist.tmpl`, and the `webhook.3ai-talk.org` ingress entry in `scripts/etc/cloudflared-config.yml.tmpl`; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck bin/k3dm-webhook-setup`, and the localhost auth probe returned HTTP 401 for a wrong Bearer token.
- **COMPLETE:** k3dm-webhook BATS suite landed on `k3d-manager-v1.6.0`; commit `a51ff8ff` (`test(webhook): BATS suite — unit, Level 1/2/3 e2e guards`) adds `scripts/tests/lib/webhook.bats` and fixes `bin/k3dm-webhook` to emit compact JSON and treat malformed `/api/*` paths as 400 so the spec checks pass; validation used `shellcheck scripts/tests/lib/webhook.bats` and `bats scripts/tests/lib/webhook.bats` with 11 unit tests passing and Level 1/2/3 skipped, plus the mismatch was documented in `docs/issues/2026-06-01-webhook-bats-mismatch-path-normalization.md`.
- **COMPLETE:** k3dm-webhook cluster command endpoint landed on `k3d-manager-v1.6.0`; commit `bd02d2d3` (`feat(webhook): add /api/v1/cluster endpoint for make up/down`) adds `_run_cluster`, restructures `do_POST` to share auth + size-cap guards, and keeps `KEEP_LOCAL=1` hardcoded for `make down`; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck scripts/tests/lib/webhook.bats`, and `bats scripts/tests/lib/webhook.bats` with 16 unit tests passing and Level 1/2/3 skipped.
- **COMPLETE:** webhook stall detection — `bin/k3dm-webhook` commit `892cc66e` (`feat(webhook): stall detection — AI analysis + kill action for hung acg-up jobs`) adds `_STALL_TICKS_THRESHOLD`, `_running_procs`, `_analyze_stall`, the stall Slack alert, and `/api/v1/cluster` kill handling on `k3d-manager-v1.6.0`; validation used `python3 -m py_compile bin/k3dm-webhook`.
- **COMPLETE:** post-provision health check — k3d-manager commit `864130895cb4f3a69314f418ac87e05a25983ea6` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-post-provision-health-check.md`; `bin/k3dm-webhook` now starts `_run_post_provision_check(job_id, provider)` as a daemon thread after successful `make up`, waits 90s, checks the SSH tunnel, triages degraded ArgoCD apps with Claude Haiku, and posts the consolidated report to Slack; validation used `python3 -m py_compile bin/k3dm-webhook`, `bats scripts/tests/lib/webhook.bats`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(webhook): post-provision health check — AI triage of degraded apps after acg-up`
- **BUG DOC:** stale sudoers install path — `docs/bugs/v1.6.0-bugfix-stale-sudoers-install-path.md`; deployed `/etc/sudoers.d/k3d-manager` uses old Homebrew `install` path; fix is `sudo bin/install-sudoers.sh`; no code change required.
- **COMPLETE:** webhook failure analysis — Claude fixes `b6cacaa5` `d51f6228` `4712215d` `85b5337e` on `k3d-manager-v1.6.0`; `_analyze_failure` guarded with inner try/except so `_finish("failed")` always runs; stdout drained in background thread + `os.killpg` kills `sleep 12600` orphan immediately when make exits; AI switched from Claude API (paid) to `gemini` CLI free tier (`_call_gemini` shells out to `gemini --model gemini-2.5-flash`); pending live Slack validation of Gemini triage output.
- **COMPLETE:** webhook failure analysis node state — k3d-manager commit `54ba9c18` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-webhook-failure-analysis-node-state.md`; `bin/k3dm-webhook` now prepends remote cluster node reachability from `kubectl get nodes --context ubuntu-k3s --no-headers` to `_collect_cluster_state()`, keeps pod/app state collection, and updates the Gemini prompt to return `CLUSTER_FAILURE|APP_FAILURE` before `TRANSIENT|PERMANENT` so node-unreachable failures are distinguished from app-only failures; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): add node state to failure analysis; distinguish cluster vs app failure`
- **COMPLETE:** webhook token auto-rotation — k3d-manager commit `91193d5f` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-webhook-token-autorotate.md`; added `bin/rotate-webhook-token` to generate a new 32-byte token, update the `k3dm-webhook-token` Keychain item, push `WEBHOOK_TOKEN` to `workers/slack-relay` via `wrangler secret put`, and restart the LaunchAgent; added `scripts/etc/com.k3d-manager.webhook-token-rotate.plist` with `StartInterval=21600` and `RunAtLoad=false`, plus `Makefile` targets for `rotate-webhook-token` and `install-token-rotator`; validation used `shellcheck -S warning bin/rotate-webhook-token` and `make -n rotate-webhook-token install-token-rotator`; commit message: `feat(webhook): auto-rotate bearer token every 6 hours via LaunchAgent`
- **COMPLETE:** webhook startup cleanup — k3d-manager commit `c7c06273bba11ff556f75acf5f1941d38b3f8905` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-webhook-startup-cleanup.md`; `bin/k3dm-webhook` now adds `_clear_stale_jobs()` before `_MAX_JOB_AGE_SECS` and calls it in `__main__` so orphaned `running` jobs are marked `failed` on restart and the concurrent job guard no longer blocks a fresh provision; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): clear stale running jobs on startup`
- **COMPLETE:** ArgoCD upgrade notification scaffolding landed on `k3d-manager-v1.6.0`; commit `3b290b45` (`feat(argocd): upgrade pipeline notification via SendGrid + PagerDuty`) adds `scripts/etc/argocd/platform-ops/notify.sh`, `scripts/etc/argocd/platform-ops/notification-secret.yaml.tmpl`, and `deploy_argocd_platform_ops` in `scripts/plugins/argocd.sh`; validation used `shellcheck scripts/etc/argocd/platform-ops/notify.sh` and `shellcheck scripts/plugins/argocd.sh` with no warnings.
- **COMPLETE:** ArgoCD CVE scan promotion landed on `k3d-manager-v1.6.0`; commit `775ecaf1` (`feat(argocd): bi-weekly CVE scan CronJob — idempotent ACG→infra→OCI pipeline`) adds `scripts/etc/argocd/platform-ops/rbac.yaml`, `scripts/etc/argocd/platform-ops/cve-scan-cronjob.yaml`, and `scripts/etc/argocd/platform-ops/cve-scan.sh`, and replaces `deploy_argocd_platform_ops` in `scripts/plugins/argocd.sh` to deploy the platform-ops namespace, RBAC, CronJob, scan-script ConfigMap, and notification Secret; validation used `shellcheck --shell=sh scripts/etc/argocd/platform-ops/cve-scan.sh` and `shellcheck scripts/plugins/argocd.sh` with no warnings.
- **COMPLETE:** ArgoCD CVE scan promotion moved to Hub cluster on `k3d-manager-v1.6.0`; commit `89201efb` (`feat(argocd): run CVE scan CronJob on Hub cluster — direct kubectl, no webhook`) updates `scripts/etc/argocd/platform-ops/cve-scan-cronjob.yaml` to mount optional `oci-kubeconfig`, rewrites `scripts/etc/argocd/platform-ops/cve-scan.sh` for direct kubectl stage patching, and updates `deploy_argocd_platform_ops` help text to the Hub/OCI topology while keeping the existing RBAC content idempotent; validation used `shellcheck --shell=sh scripts/etc/argocd/platform-ops/cve-scan.sh` and `shellcheck scripts/plugins/argocd.sh` with no warnings.
- **COMPLETE:** v1.6.0 Worker deploy workflow landed on `k3d-manager-v1.6.0`; commit `d7ebb11f` (`ci: add Cloudflare Worker deploy workflow and secrets setup script`) adds `.github/workflows/deploy-worker.yml` and `bin/k3dm-worker-setup` for deploying the Slack relay Worker and interactively setting the three required GitHub secrets; validation used `yamllint .github/workflows/deploy-worker.yml`, `shellcheck bin/k3dm-worker-setup`, and `bash -n bin/k3dm-worker-setup` with no errors.
- **COMPLETE:** Playwright screenshot diagnosis Phase A — lib-acg PR #32 merged (SHA `9a7c008`); Copilot finding fixed (`ae94605` — mode 0o600 for screenshot file); subtree pulled into k3d-manager (`698d351e`); enforce_admins restored; next branch `fix/next-improvements` created in lib-acg. Phase B (webhook vision + `update-webhook-env`) ready to implement.
- **COMPLETE:** acg-up idempotency bugfix — k3d-manager commit `d86b9ca6` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-acg-up-idempotency.md`; `_provider_k3s_aws_deploy_cluster` now skips `deploy_app_cluster` when ≥ 3 nodes already Ready in `ubuntu-k3s` context; shellcheck clean; agent_audit clean.
- **COMPLETE:** Gemini timeout prompt-leak bugfix — commit `bd253426` (`fix(webhook): catch TimeoutExpired separately to prevent prompt leak in Slack`) on `k3d-manager-v1.6.0`; `_call_gemini` now catches `subprocess.TimeoutExpired` before `Exception` to prevent the full prompt from leaking into Slack notifications; timeout raised from 60s to 120s; spec: `docs/plans/v1.6.0-bugfix-gemini-timeout-leak.md`; webhook restarted.
- **COMPLETE:** v1.6.0 acg-down sudoers cleanup landed on `k3d-manager-v1.6.0`; commit `ab4f6e39` (`fix(acg-down): remove redundant pre-auth block; add deploy-worker make target`) removes the stale macOS pre-auth block from `bin/acg-down` and adds the `deploy-worker` target to `Makefile`; validation confirmed `grep -n "Pre-authoriz\|sudo -n true" bin/acg-down` returns nothing.
- **COMPLETE:** acg-down orphaned daemons bugfix landed on `k3d-manager-v1.6.0`; commit `81122796` (`fix(acg-down): unload cloudflare-tunnel, frontend, and keycloak-pf daemons on teardown`) adds teardown for the Cloudflare named tunnel, frontend browser HTTP listener, and Keycloak port-forward daemons inside the macOS `bin/acg-down` path; validation used `shellcheck -S warning bin/acg-down`, `./scripts/k3d-manager _agent_audit`, and `grep -n "Pre-authoriz\|sudo -n true" bin/acg-down` returned no output.
- **COMPLETE:** Hub cluster health check bugfix landed on `k3d-manager-v1.6.0`; commit `4bc87455` (`fix(acg-up): three-tier Hub cluster health check with auto-start on sleep/wake`) adds a binary-presence guard before `k3d cluster list`, auto-starts a registered-but-stopped Hub cluster, and retries API reachability up to 12×5s after `k3d cluster start`; validation used `grep -n "k3d binary not found\|cluster start\|Waiting for Hub cluster" bin/acg-up`, `shellcheck -S warning bin/acg-up`, and `./scripts/k3d-manager _agent_audit` all passed.
- **COMPLETE:** headless LaunchDaemon / cloudflared privilege escalation bugfix landed on `k3d-manager-v1.6.0`; commit `fa6011d5` (`fix(acg-up,acg-down): skip system LaunchDaemon ops in headless context; cloudflared → LaunchAgent`) updates `bin/acg-up` to skip the three browser LaunchDaemon installs when running headless, moves the Cloudflare named tunnel from `/Library/LaunchDaemons` to `~/Library/LaunchAgents`, and updates `bin/acg-down` so the Cloudflare tunnel is handled as a user LaunchAgent while the three browser listener teardowns skip in headless context; validation used `shellcheck -S warning bin/acg-up bin/acg-down`, `./scripts/k3d-manager _agent_audit`, and the required grep checks on both files passed.
- **COMPLETE:** v1.5.3 observability ACG Alertmanager + Grafana landed on `k3d-manager-v1.5.3`; commit `5cd6ea53` (`feat(observability): prune noisy default rules; enable ACG Alertmanager + Grafana`) adds defaultRules pruning to both observability values files, enables ACG Alertmanager on NodePort `30093` with Vault-backed `alertmanager-smtp-secret`, enables ACG Grafana on NodePort `30030`, and extends `deploy_observability_acg()` to read Vault credentials and create the secret on `ubuntu-k3s`; validation used `shellcheck scripts/plugins/observability.sh`, Ruby YAML parsing for both values files, and the spec-required PyYAML validation failed because `yaml` is not installed in this environment (issue note: `docs/issues/2026-06-01-observability-values-pyyaml-missing.md`).
- **COMPLETE:** Vault CA trust idempotency fix landed on `k3d-manager-v1.5.3`; commit `e99ffcc9` (`fix(acg-up): verify-cert idempotency check + interactive-sudo soft for Vault CA trust`) now skips `security add-trusted-cert` when `security verify-cert` says the cert is already trusted and uses `--interactive-sudo --soft` so the warning fallback can fire; validation used `shellcheck -S warning bin/acg-up`, `bats scripts/tests/bin/acg_up.bats`, and `./scripts/k3d-manager _agent_audit` all passed.
- **COMPLETE:** ClusterSecretStore timeout retry fix landed on `k3d-manager-v1.5.3`; commit `ec29ef3b` (`fix(shopping_cart): retry ClusterSecretStore wait with force-reconcile on timeout`) now retries `kubectl wait` for `clustersecretstore/vault-backend` in three 90s windows and annotates `k3d-manager/reconcile-at` after each timeout so ESO re-validates immediately; validation used `shellcheck -S warning scripts/plugins/shopping_cart.sh` and `./scripts/k3d-manager _agent_audit` both passed.
- **COMPLETE:** ArgoCD app registration + acg-down sudo routing fix landed on `k3d-manager-v1.5.3`; commit `410ec1e1` (`fix(acg-up/down): register ArgoCD apps in acg-up + route sudo through _run_command in acg-down`) now calls `register_shopping_cart_apps` at Step 11b in `bin/acg-up` and routes macOS sudo pre-authorization through `_run_command --interactive-sudo --soft -- true` in `bin/acg-down`; validation used `shellcheck -S warning bin/acg-up`, `shellcheck -S warning bin/acg-down`, and `./scripts/k3d-manager _agent_audit` all passed.
- **COMPLETE:** OCI object storage backup/restore landed on `k3d-manager-v1.5.1`; commits `9b8d6a78` (`feat: OCI object storage backup/restore for k3s-oci provider`) and `f5d2ce0e` (`test(provider): align k3s-oci deploy sequence BATS with auto-backup`) add `scripts/lib/providers/k3s-oci-storage.sh`, wire `scripts/lib/providers/k3s-oci.sh` to source it and auto-backup after deploy, and expose `oci_backup` / `oci_restore` through `scripts/k3d-manager` + Makefile backup/restore targets; validation used `shellcheck scripts/lib/providers/k3s-oci-storage.sh scripts/lib/providers/k3s-oci.sh scripts/k3d-manager scripts/tests/lib/k3s_oci_provider.bats`, `bash -n` on the touched shell files, `bats scripts/tests/lib/k3s_oci_provider.bats`, and `./scripts/k3d-manager oci_backup --help` / `oci_restore --help`; live OCI backup/restore was not run in this workspace (see `docs/issues/2026-05-31-oci-object-storage-live-backup-restore-not-run.md`).
- **lib-foundation interactive sudo no-TTY fix committed and pushed** — lib-foundation branch `feat/v0.3.20`, commit `00309de`; k3d-manager subtree merge `2501ba97` on `k3d-manager-v1.5.0`.
- **Vault CA PTY fix committed and pushed** — commit `7b7a8ea2` on `k3d-manager-v1.5.0` (`fix(acg-up): skip add-trusted-cert when Vault CA already trusted — no PTY needed`); `bin/acg-up` now skips `security add-trusted-cert` when `security verify-cert` says the Vault CA is already trusted.
- **redis-cart-secret race fix committed and pushed** — commit `780930ac` on `k3d-manager-v1.5.0` (`fix(shopping-cart): wait for redis-cart-secret before copy — prevents race on fresh sandbox`); `scripts/plugins/shopping_cart.sh` now waits for `redis-cart-secret` before copying it into `shopping-cart-apps`.
- **COMPLETE:** v1.5.4 observability + vuln scan landed on `k3d-manager-v1.5.0`; commit `44f4c92b` (`feat(observability): Prometheus+Grafana+Trivy on Hub+ACG, federated via port-forward`) adds the Hub/ACG ApplicationSets, Prometheus/Grafana/Trivy Helm values, `scripts/plugins/observability.sh`, `scripts/tests/lib/observability.bats`, `Makefile` observability targets, and ACG port-forward management in `bin/acg-up`/`bin/acg-down`; validation used `shellcheck -S warning bin/acg-up bin/acg-down scripts/k3d-manager scripts/plugins/observability.sh`, `shellcheck -S warning scripts/tests/lib/observability.bats`, `bats scripts/tests/lib/observability.bats`, and `./scripts/k3d-manager _agent_audit`.
- **COMPLETE:** v1.5.5 observability rules + alertmanager landed on `k3d-manager-v1.5.0` and `docs/next-improvements`; k3d-manager commit `5fd1f4fd` (`feat(observability): enable Alertmanager with Vault-backed email-to-SMS config`) enables Alertmanager with Vault-backed `alertmanager-smtp-secret` creation in `scripts/plugins/observability.sh`, updates `scripts/etc/helm/observability/kube-prometheus-stack-values.yaml`, and adds `Makefile` `alertmanager-secret`; shopping-cart-infra commit `41f00da` (`feat(monitoring): add shopping-cart PrometheusRules + ArgoCD Application`) adds `monitoring/rules/shopping-cart-apps.yaml` and `monitoring/rules/argocd-application.yaml`; the Application was applied to the Hub cluster with `kubectl apply --validate=false -f monitoring/rules/argocd-application.yaml --context k3d-k3d-cluster -n cicd`; validation used `shellcheck scripts/plugins/observability.sh`, Ruby YAML parsing for both YAML files, and the spec-required PyYAML command failed because `yaml` is not installed in this environment (issue note: `docs/issues/2026-05-31-shopping-cart-prometheusrules-pyyaml-missing.md`).
- **COMPLETE:** v1.5.6 observability template refactor landed on `k3d-manager-v1.5.0`; commit `001f37ba` (`refactor(observability): move PrometheusRules + Alertmanager config to template files`) adds `scripts/etc/prometheus/rules/shopping-cart-apps.yaml` and `scripts/etc/prometheus/alertmanager.yaml.tmpl`, rewrites `scripts/plugins/observability.sh` to load Alertmanager config from the template via `envsubst`, applies `scripts/etc/prometheus/rules/` directly, and deletes the stale `shopping-cart-rules` ArgoCD Application from the Hub cluster; validation used `shellcheck scripts/plugins/observability.sh`, Ruby YAML parsing for both new files, and the live ArgoCD cleanup `kubectl delete application shopping-cart-rules -n cicd --context k3d-k3d-cluster --ignore-not-found` succeeded.
- **COMPLETE:** ArgoCD persistent OutOfSync fix — shopping-cart-infra PR #79 merged to main (SHA `d5be9712cfb0dbe7d6a9667ba6ec8289dd16e5a9`); enforce_admins restored ✓; docs/next-improvements branch updated with retrospective (SHA `1f63eec`) ✓; memory-bank updating (in progress)
- **DEFERRED to v1.6.3:** `/resume` Slack thread command — checkpoint-based pipeline re-entry for `bin/acg-up`; use case: tunnel DOWN mid-provision → `/acg-refresh` → `/resume` continues from failed step; requires checkpoint file per step + `RESUME_FROM` env var support in `make up`; design (step granularity) TBD
- **OPEN:** 3 bugs found during `make up` smoke test; specs in `docs/bugs/`; Codex task assigned — see progress.md
- **COMPLETE:** data-layer ArgoCD ordering bug — k3d-manager commit `deb4c3b24a88cc950d5458d1f0b0a02e5a75ba42` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-data-layer-argocd-ordering.md`; `bin/acg-up` now applies `shopping-cart-infra/argocd/applications/data-layer.yaml` immediately before `deploy_shopping_cart_data` so the ArgoCD sync starts before the wait loop; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): apply data-layer ArgoCD Application before deploy_shopping_cart_data wait loop`
- **COMPLETE:** webhook auto-cleanup after acg-up failure — k3d-manager commit `1eee5c95ae5a2ea73f4ffd347603104ffddc13f8` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-webhook-acg-up-cleanup.md`; `bin/k3dm-webhook` now starts `_run_cleanup(provider)` in a daemon thread after `_finish("failed")` for `action == "up"` failures so AWS teardown runs in the background and posts start/completion Slack notifications; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): auto-cleanup AWS resources after acg-up failure`
- **COMPLETE:** data-layer ArgoCD sync-wait — k3d-manager commit `5a59a8b13834db7cdad0e008ad3cf75843822e49` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-data-layer-argocd-sync-wait.md`; `bin/acg-up` now waits for `shopping-cart-infra/argocd/applications/data-layer.yaml` to report `Synced` before entering `deploy_shopping_cart_data`, and `scripts/plugins/shopping_cart.sh` now fails fast if the `shopping-cart-data` StatefulSet is still missing after ArgoCD sync; validation used `shellcheck -S warning bin/acg-up` and `shellcheck -S warning scripts/plugins/shopping_cart.sh`; commit message: `fix(acg-up): wait for data-layer ArgoCD sync before StatefulSet readiness check`
- **COMPLETE:** Prometheus basic auth landed on `k3d-manager-v1.6.0`; commit `3b3ef58a` (`fix(observability): add Prometheus basic auth via Vault-backed web config secret`) adds `_prometheus_acg_web_config_secret` to `scripts/plugins/observability.sh`, appends `prometheus-web-config` and `web.config.file` to `scripts/etc/helm/observability/kube-prometheus-stack-acg-values.yaml`, and applies the web config secret from Vault into `monitoring`; validation used `shellcheck -S warning scripts/plugins/observability.sh` and Ruby YAML parsing for the values file, both passing with no output.
- **COMPLETE:** shopping-cart-infra PR #83 merged to main (`28a58358347be5a229fdaa1b579f6696830ef4b8`); enforce_admins restored ✓; `docs/next-improvements` branch created ✓; retrospective committed (`54b26a1`) ✓; memory-bank updated ✓
- **COMPLETE:** acg_watch disown bugfix landed on `k3d-manager-v1.6.0`; commit `d0191b07` (`fix(k3s-aws): disown acg_watch background process to release webhook stdout pipe`) adds `disown "${_watcher_pid}"` immediately after `acg_watch &` in `scripts/lib/providers/k3s-aws.sh` so the webhook stdout pipe is released; validation used `shellcheck -S warning scripts/lib/providers/k3s-aws.sh` and `./scripts/k3d-manager _agent_audit` both passed.
- **COMPLETE:** webhook concurrent job TOCTOU race bugfix landed on `k3d-manager-v1.6.0`; commit `c5cb0cbc` (`fix(webhook): close TOCTOU race in concurrent cluster job guard`) writes `running` in the `/api/v1/cluster` HTTP handler before starting the worker thread and removes the duplicate `running` write from `_run_cluster`; validation used `python3 -m py_compile bin/k3dm-webhook` and `bats scripts/tests/lib/webhook.bats` with 29 tests passing.
- **COMPLETE:** platform-helm multi-env ArgoCD landed in `scripts/etc/argocd/applicationsets/platform-helm.yaml` and `scripts/plugins/argocd.sh`; commit `0e4268a5` on `k3d-manager-v1.5.0`. Rendered YAML validated with Ruby because the spec-required PyYAML module was not installed; issue note recorded in `docs/issues/2026-05-30-platform-helm-services-git-pyyaml-missing.md`.
- **COMPLETE:** services-git ExternalSecret refreshInterval drift ignore landed in `scripts/etc/argocd/applicationsets/services-git.yaml`; commit `fd947a59` on `k3d-manager-v1.5.0`. YAML validated with Ruby because the spec-required PyYAML module was not installed; issue note recorded in `docs/issues/2026-05-30-platform-helm-services-git-pyyaml-missing.md`.
- **COMPLETE:** shopping-cart ALTER USER cleanup, platform project destination fix, and ESO force-refresh landed on `k3d-manager-v1.5.0`; commits `3ad44199`, `3fee4d61`, and `bbb4d008` pushed to origin. The spec-required PyYAML validation for `platform.yaml.tmpl` was unavailable here; issue note recorded in `docs/issues/2026-05-30-shopping-cart-platform-project-pyyaml-missing.md`.
- **COMPLETE:** OCI provider landed on `k3d-manager-v1.5.0`; commit `72a5d38e` (`fix(provider): move OCI vars to scripts/etc/oci/vars.sh — follow k3s/azure provider convention`) moves OCI env defaults from `scripts/etc/vars.sh` to `scripts/etc/oci/vars.sh` and removes the unconditional global source; `shellcheck scripts/lib/providers/k3s-oci.sh` and `shellcheck scripts/k3d-manager` both passed.
- **COMPLETE:** k3s-oci provider BATS coverage landed on `k3d-manager-v1.5.0`; commit `30cd7adb` (`test(provider): BATS tests for k3s-oci deploy/destroy/validate/reconfigure`) adds `scripts/tests/lib/k3s_oci_provider.bats`; `bats scripts/tests/lib/k3s_oci_provider.bats`, `shellcheck scripts/tests/lib/k3s_oci_provider.bats`, and `./scripts/k3d-manager _agent_audit` all passed.
- **COMPLETE:** k3s-oci two-node BATS coverage + agent wait loop bug fix landed on `k3d-manager-v1.5.0`; commit `823f0349` (`test(k3s-oci): BATS coverage for two-node provider + fix agent wait loop`) — replaces `grep -v master` wait loop (k3s has no master label) with a 2-node Ready count; 25 tests covering step sequencing, idempotency, Cilium Helm flags, private-IP join, smoke-test thresholds, destroy; 25/25 green; shellcheck clean.
- **COMPLETE:** k3s-oci two-node cluster landed on `k3d-manager-v1.5.0`; commit `2ccbff18` (`feat(k3s-oci): two-node cluster — server + agent, Cilium CNI (2×2OCPU/12GB)`) updates `scripts/lib/providers/k3s-oci.sh` to provision server + agent instances, install Cilium, and smoke-test two Ready nodes; `shellcheck scripts/lib/providers/k3s-oci.sh`, `bash -n scripts/lib/providers/k3s-oci.sh`, and `./scripts/k3d-manager _agent_audit` all passed.
- **COMPLETE:** state-dir layout reorganization landed on `k3d-manager-v1.5.0`; commit `af16d821` (`refactor(state-dir): reorganize runtime files into bin/ run/ logs/ subdirectories`) updates `bin/acg-up`, `bin/acg-down`, `bin/acg-sync-apps`, `scripts/plugins/argocd.sh`, `scripts/plugins/ssm.sh`, `scripts/lib/acg/scripts/plugins/acg.sh`, and `scripts/lib/acg/scripts/lib/cdp.sh` to place runtime wrappers in `bin/`, PID/plist files in `run/`, and logs in `logs/`.
- **v1.4.12 SHIPPED** — PR #83 merged (`c5949cf47097d3a89ff485e01f68c2725644d327`); tag v1.4.12 created ✓; GitHub release created ✓; enforce_admins restoration pending (API call); retrospective committed (`3c99fc85`) ✓; next branch k3d-manager-v1.5.0 created ✓
- **COMPLETE:** shopping-cart-e2e-tests PR #2 post-merge — enforce_admins restored ✓, `docs/next-improvements` branch synced ✓, retrospective committed (`7b7e9c7`) ✓, pushed to origin ✓; PR fixed CI trigger config by removing `push`/`pull_request` blocks that caused ECONNREFUSED on live services — now `workflow_dispatch` and `schedule` only
- **COMPLETE:** OIDC issuer URLs now use the external Keycloak domain in `shopping-cart-infra` (`1fff014`) and `shopping-cart-payment` (`24606e6`), and the Node.js 20 → 22 upgrade landed in `shopping-cart-frontend` (`908ce07`) plus `shopping-cart-e2e-tests` (`0be83fb`); all commits were pushed to their requested branches on origin.
- **COMPLETE:** OIDC issuer URLs now use the external Keycloak domain in `shopping-cart-infra` (`identity/keycloak/kustomization.yaml`, `argocd/config/argocd-cm.yaml`) and `shopping-cart-payment` (`k8s/base/configmap.yaml`); committed as `1fff014` (`fix(keycloak): update OIDC issuer URLs to external domain for ArgoCD and Keycloak config`) and `24606e6` (`fix(auth): update OIDC issuer and JWK URIs to external Keycloak domain`) and pushed to `origin/shopping-cart-infra-v0.5.0` and `origin/docs/next-improvements`.
- **COMPLETE:** `/etc/hosts` updates now go through the scoped helper `bin/update-hosts-entry.sh`; `bin/install-sudoers.sh` installs/removes `/usr/local/bin/k3d-manager-update-hosts` with a matching NOPASSWD rule, and `bin/acg-up` no longer prewarms sudo with `sudo -v`; committed as `3c290cb6` (`fix(sudo): extract /etc/hosts update to scoped helper for pipeline compatibility`) and pushed to `origin/k3d-manager-v1.5.0`.
- **COMPLETE:** Passwordless sudo rules + macOS host-operation prewarm landed for `bin/acg-up`, `bin/acg-down`, `bin/acg-refresh`, `Makefile`, `docs/howto/makefile.md`, and `bin/install-sudoers.sh`; committed as `26a75bcb` (`feat(sudo): passwordless sudo rules + prewarm for macOS host operations`) and pushed to `origin/k3d-manager-v1.5.0`.
- **COMPLETE:** `/etc/hosts` pipeline-compatible helper extraction landed for `bin/acg-up`, `bin/install-sudoers.sh`, `bin/update-hosts-entry.sh`, and `scripts/tests/test_install_sudoers.bats`; committed as `8c9095c0` (`fix(sudo): extract /etc/hosts update to scoped helper for pipeline compatibility`) and pushed to `origin/k3d-manager-v1.5.0`.
- **COMPLETE:** `acg-down` cached-credentials guard landed in `bin/acg-down`; commit `b8cdaad7` (`fix(acg-down): skip sudo -v pre-auth when credentials are already cached`) now skips `sudo -v` only when the TTY guard passes and credentials are stale. `shellcheck -S warning bin/acg-down` and the staged repo audit passed.
- **COMPLETE:** Vault CA PTY fix — `bin/acg-up` now skips `security add-trusted-cert` when `security verify-cert` reports the Vault CA is already trusted; commit `7b7a8ea2` pushed to `origin/k3d-manager-v1.5.0`. `shellcheck -S warning bin/acg-up scripts/plugins/shopping_cart.sh` and `_agent_audit` passed.
- **COMPLETE:** redis-cart-secret race fix — `scripts/plugins/shopping_cart.sh` now waits for `redis-cart-secret` before copying it into `shopping-cart-apps`; commit `780930ac` pushed to `origin/k3d-manager-v1.5.0`. `shellcheck -S warning bin/acg-up scripts/plugins/shopping_cart.sh` and `_agent_audit` passed.
- **NOTE:** `scripts/tests/bin/acg_up.bats` still has one stale assertion (`acg-up sources the Argo CD plugin before readiness checks`) that looks for `ArgoCD browser login URL: https://argocd.shopping-cart.local`; recorded in `docs/issues/2026-05-30-acg-up-bats-argocd-browser-login-url-test-drift.md`.
- **NOTE:** `scripts/tests/test_install_sudoers.bats` needed `BATS_TEST_DIRNAME/../..` to resolve the repo root in this checkout; the spec text used `../../..`, which pointed one directory above the repo and broke the first BATS run. Recorded in `docs/issues/2026-05-30-sudo-passwordless-bats-repo-root-mismatch.md`.
- **POST-MERGE v1.4.9 COMPLETE:** v1.4.9 tag created + pushed ✓, next branch `k3d-manager-v1.5.0` created ✓, retrospective committed (`e1bf78fb`) ✓, README releases table + `docs/releases.md` added (`ed343d17`) ✓, enforce_admins restoration deferred (API endpoint issue) ⏸, memory-bank updating (in progress)
- **POST-MERGE shopping-cart-frontend PR #17 COMPLETE:** enforce_admins restored ✓, `docs/next-improvements` branch synced ✓, retrospective committed (`31f8c4e`) ✓, README releases table + `docs/releases.md` added (`2e11026`) ✓, memory-bank updated ✓
- **POST-MERGE shopping-cart-frontend PR #18 COMPLETE:** enforce_admins restored ✓, `docs/next-improvements` branch checked out ✓, retrospective committed (`2828c18`) ✓, memory-bank updated ✓
- **COMPLETE:** sandbox TTL check before provision now runs in the AWS skip-extraction path and extends the sandbox when remaining time is below threshold; committed as `a89cc2e5` (`fix(acg-up): check sandbox TTL before provisioning; extend if below threshold`) and pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** CoreDNS duplicate `hosts` plugin crash fixed by patching `data.NodeHosts` instead of injecting a second `hosts {}` block into Corefile; committed as `5efe45e8` (`fix(acg-up): patch NodeHosts instead of injecting duplicate hosts block into CoreDNS Corefile`) and pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** Cloudflare tunnel missing Keycloak fixed by generating `~/.cloudflared/config.yml` from `scripts/etc/cloudflared-config.yml.tmpl` and advertising `keycloak=` in tunnel URLs; committed as `d8f2ca47` (`fix(acg-up): generate cloudflared config from template; add keycloak to Cloudflare tunnel`) and pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** Keycloak public URL follow-up landed across all four repos: `k3d-manager` commit `ea9977b4` sets Keycloak `frontendUrl` to `https://keycloak.3ai-talk.org` after realm import; `shopping-cart-basket` commit `cb5a294` sets `OAUTH2_ISSUER_URI` to the Cloudflare public issuer; `shopping-cart-order` commit `fb578ca` sets both `OAUTH2_ISSUER_URI` and `OAUTH2_JWK_SET_URI` to the Cloudflare public issuer; `shopping-cart-frontend` commit `cb348c4` sets both `VITE_KEYCLOAK_URL` occurrences to `https://keycloak.3ai-talk.org`; explicit feature refs were pushed to `origin/fix/keycloak-public-url` in each shopping-cart repo, and the upstream-push quirk was recorded in `docs/issues/2026-05-19-git-push-upstream-default-targeted-main.md`.
- **COMPLETE:** Ghost State Delete Sandbox button not clicking — root cause: Ghost State recovery never triggered when sandbox had positive TTL but invalid credentials (`InvalidClientTokenId`). Fix: added `--force-restart` flag to `acg_extend.js` (bypasses TTL guard, goes straight to delete+restart); pass-through in `acg.sh` `_acg_extend_playwright` (`"${@:2}"`); `bin/acg-up` `elif` branch detects invalid creds on running sandbox and calls `acg_extend_playwright "${_acg_sandbox_ref}" --force-restart`. lib-acg commit `f5bc18d` on `fix/ghost-state-waitForSelector` pushed to origin; k3d-manager commit `92c438ca` on `k3d-manager-v1.4.9` pushed to origin.
- **COMPLETE:** Ghost State Delete Sandbox selector fix landed in `lib-acg` commit `0248736` (`fix(acg-extend): wait for sandbox card buttons after Ghost State re-navigation`) on `fix/ghost-state-waitForSelector`; `playwright/acg_extend.js` now waits for sandbox card buttons after Ghost State re-navigation and shortens `deleteBtn.isVisible` to 10s; branch pushed to `origin/fix/ghost-state-waitForSelector`; live `make credential-test` still failed with `InvalidClientTokenId` and is documented in `docs/issues/2026-05-20-lib-acg-credential-test-invalid-client-token.md`.
- **COMPLETE:** e2e-tests CI push/PR triggers removed in `shopping-cart-e2e-tests/.github/workflows/e2e-tests.yml`; `push` and `pull_request` blocks were replaced with the spec comment because `api-tests` and `flow-tests` require live services on `localhost:8083`, `localhost:8000`, and `localhost:8080`; committed as `99499d3` (`fix(ci): disable push/PR triggers — e2e tests require live services`) and pushed to `origin/fix/e2e-workflow-push-trigger`.
- **COMPLETE:** `acg-up` now patches ArgoCD to use `${ARGOCD_PUBLIC_URL:-https://argocd.3ai-talk.org}`, adds the Cloudflare public URL to the Keycloak `argocd` client redirect URIs, and replaces the two `trycloudflare.com` quick tunnels with a single named tunnel driven by `~/.cloudflared/config.yml`; committed as `00d3a65f` (`fix(acg-up): replace trycloudflare quick tunnels with named cloudflare tunnel; wire ArgoCD URL + Keycloak redirect`) and pushed to `origin/k3d-manager-v1.4.8`; validated with `shellcheck -S warning bin/acg-up`.
- **k3d-manager v1.4.7 released** — CHANGELOG versioned, tag `v1.4.7` pushed to origin, GitHub release created, enforce_admins restored on main, next branch `k3d-manager-v1.4.8` created
- **enforce_admins restored on k3d-manager main** — `true` ✓ + `required_approving_review_count=1` ✓
- **v1.4.7 retrospective committed** — `docs/retro/2026-05-18-v1.4.7-retrospective.md` on k3d-manager-v1.4.8, commit `f6632b4f`, pushed to origin
- **lib-foundation PR #28 post-merge completed** — fix: Copilot CLI auth + rigor scanner improvements; merge SHA `fee313ed` synced locally; next branch `feat/v0.3.20` checked out; retrospective `2026-05-18-pr28-retrospective.md` created + committed `a64e2ad` + pushed to origin; enforce_admins restoration pending (API call)
- **lib-acg PR #13 post-merge completed** — docs: PR #12 retrospective and Phase 3 completion; merge SHA `1bdc663` synced locally; next branch `docs/next-improvements` already existed + checked out; retrospective `2026-05-18-pr13-retrospective.md` created + committed `4d93147` + pushed to origin; enforce_admins restoration pending (API call)
- **COMPLETE:** `acg-up` now seeds Keycloak LDAP user passwords for `admin`, `developer`, and `operator` into Vault at `keycloak/users/<user>` and updates the live LDAP passwords on every `make up`; committed as `34c6f8a9` (`fix(acg-up): seed Keycloak LDAP user passwords in Vault on every make up`) and pushed to `origin/k3d-manager-v1.4.6`; validated with `shellcheck -S warning bin/acg-up` and `bats scripts/tests/bin/acg_up.bats`.
- **COMPLETE:** lib-foundation no-TTY sudo fallback — `scripts/lib/system.sh` now adds `sudo -n` when `interactive_sudo` is requested without a TTY; lib-foundation commit `00309de`, k3d-manager subtree merge `2501ba97`.
- **shopping-cart-product-catalog PR #20 merged** — `fix/argocd-shared-namespace` → `main`, merge SHA `12dfe79`. Service port fix (8082). enforce_admins restored. `docs/next-improvements` branch created.
- **shopping-cart-order PR #29 merged** — `fix/argocd-shared-namespace` → `main`, merge SHA `8a2739d`. Service port + SecurityConfig.java health fixes (8081). enforce_admins restored. `docs/next-improvements` branch created.
- **lib-acg PR #12 merged** — feat/acg-multi-provider → main, SHA `8fe35fa1`. Exit 0 on stale "Session extended" toast at startup (process.exit bypass finally block fix by Copilot). enforce_admins + required_approving_review_count=1 restored on lib-acg. feat/acg-gcp-credentials branch created from merge SHA; retrospective added + committed + pushed (commit `0b9c224`).
- **lib-acg PR #11 merged** — fix/acg-credentials-extend-dialog → main, SHA `feeb8e80`. AWS credential extraction + dialog handling. enforce_admins restored on lib-acg (required_approving_review_count=1). feat/acg-multi-provider branch created from merge SHA; retrospective added + committed + pushed (commit `d2e3e48`).
- **COMPLETE:** Validation follow-up notes for the ACG extend-dialog work were committed as `65582d0c` (`docs: record validation follow-up notes`) and include `docs/issues/2026-05-16-git-commit-index-lock-permission-error.md` plus `docs/issues/2026-05-17-repo-test-all-existing-failures.md`; PR URL: not created (per repository instruction).
- **k3d-manager v1.4.6 acg "Session extended" toast fix — completed** — replaced `element.click()` toast dismissals with `page.mouse.click()` at all 7 sites and added a proactive `addLocatorHandler` in `acg_extend.js`; committed as `718562b9` (`fix(acg): click toast via mouse events`); validated with `node --check scripts/lib/acg/playwright/acg_credentials.js`, `node --check scripts/lib/acg/playwright/acg_extend.js`, `bats scripts/tests/lib/acg.bats`, and `bats scripts/tests/plugins/acg.bats`; PR URL: not created (per repository instruction).
- **k3d-manager PR #74 merged** — `k3d-manager-v1.4.5` → `main`, merge SHA `8f93df25`. ACG extend navigation fix + frontend SSO wiring. enforce_admins restored. v1.4.6 branch created with retrospective.
- **shopping-cart-infra PR #58 merged** — `shopping-cart-infra-v0.5.3` → `main`, merge SHA `67f8d228`. ArgoCD SSO OIDC callback fix (`url: http://` → `url: https://`). enforce_admins restored. v0.5.4 branch created with retrospective.
- **shopping-cart-infra PR #57 merged** — `shopping-cart-infra-v0.5.2` → `main`, merge SHA `adbaec8`. Networking + mTLS fixes for SSO. enforce_admins restored. v0.5.3 branch created with retrospective and spec commits deleted.
- **shopping-cart-infra PR #56 merged** — `shopping-cart-infra-v0.5.1` → `main`, merge SHA `79c42b71`. Keycloak reconcile hook python3 → kcadm.sh fix. enforce_admins restored. v0.5.2 branch created with retrospective.
- **COMPLETE:** products ConfigMap schema mismatch fix — `shopping-cart-infra/data-layer/postgresql/products/configmap.yaml` now matches `init-db.sql` by keeping only the `categories` schema + seed data; committed as `eb05a3f` (`fix(products-db): sync configmap init SQL with init-db.sql — remove products DDL, let SQLAlchemy own schema`) on `fix/products-db-schema` and pushed to `origin/fix/products-db-schema`.
- `k3d-manager` main branch protection is restored (`enforce_admins=true`).
- `shopping-cart-infra` main branch protection is restored (`enforce_admins=true`).
- **shopping-cart-frontend PR #15 merged** — `shopping-cart-frontend-v0.5.1` → `main`, merge SHA `19e47118`. Keycloak SSO wiring (CI build-args + nginx CSP). enforce_admins restored. v0.5.1 branch created with retrospective.
- `v1.4.4`, `v1.4.3`, and `v1.4.2` remain shipped; branch protection was restored after each merge.
- The `shopping-cart-infra` release tag `v0.4.0` was corrected to point at `674b7b1` (`docs: consolidate v0.4.0 release notes`), which includes the Keycloak realm-import fix.
- The live Keycloak realm JSON reconciliation spec now lives in `shopping-cart-infra` instead of `k3d-manager`.

## Implementation Order (2026-05-29) — CRITICAL
Deploy OCI with stable main BEFORE any v1.6.0+ work. Sequence:
1. `v1.5.0-k3s-oci-provider.md` → OCI cluster + k3s + ArgoCD + full stack → known-good baseline
2. `v1.5.0-platform-helm-argocd-multi-env.md` → multi-env ArgoCD versioning
3. v1.6.0 webhook + notifications + CVE scan (on proven OCI)
4. v1.7.0+ monitoring / self-healing / blue-green

## Architectural Decision (2026-05-29)
- **Blue/green + vCluster stress testing on OCI** — two layers: vCluster (ephemeral full-stack isolated test env, `trap EXIT` cleanup) + Argo Rollouts (production selector swap); stress-runner custom Docker image (ARM64: vcluster CLI + kubectl + k6 + kustomize) in AnalysisTemplate Job; k6 hits vCluster endpoint (not preview-service); `autoPromotionEnabled: false`; Istio traffic mirroring deferred to v1.10.0; spec: `docs/plans/v1.9.0-blue-green-argo-rollouts.md`
- **Self-healing pipeline** — Alertmanager fires PrometheusRule → POST to `k3dm-webhook /api/v1/remediate` → handler dispatch (Vault unseal, ArgoCD sync, ESO force-sync, pod restart, ACG make up); per-alert cooldown prevents hammer; spec: `docs/plans/v1.8.0-self-healing-alertmanager-webhook.md`; gates on v1.6.0 + v1.7.0
- **Observability → OCI, not laptop** — Prometheus + Grafana run on OCI so monitoring survives laptop reboots. Laptop/ACG use Prometheus agent mode with remote_write to OCI. Grafana exposed via `grafana.3ai-talk.org` Cloudflare tunnel. Spec: `docs/plans/v1.7.0-observability-oci.md`. Implementation gates on OCI cluster live (v1.5.0).

## In Progress (v1.5.0)
- **ASSIGNED TO CODEX:** State-dir layout reorganization — spec: `docs/plans/v1.5.2-state-dir-layout.md` (commit `e66b3659` on `k3d-manager-v1.5.0`); 7 files: `bin/acg-up`, `bin/acg-down`, `bin/acg-sync-apps`, `scripts/plugins/argocd.sh`, `scripts/plugins/ssm.sh`, `scripts/lib/acg/scripts/plugins/acg.sh`, `scripts/lib/acg/scripts/lib/cdp.sh`; moves *.sh to `bin/`, *.pid/*.plist to `run/`, *.log to `logs/`; commit message: `refactor(state-dir): reorganize runtime files into bin/ run/ logs/ subdirectories`
- **COMPLETE:** ArgoCD sync before redis deploy — k3d-manager commit `2a37829d` on `k3d-manager-v1.5.0`; spec: `docs/bugs/v1.5.0-bugfix-deploy-data-layer-argocd-sync-before-redis.md`; `deploy_shopping_cart_data()` now runs `argocd app sync data-layer --server localhost:8080 --insecure --wait --timeout 120 2>/dev/null || true` before `kubectl apply -f redis/cart/` to prevent the ESO race on fresh sandbox.
- **ASSIGNED TO CODEX:** Passwordless sudo for macOS host operations — spec: `docs/plans/v1.5.0-sudo-passwordless.md` (commit `01d27232` on `k3d-manager-v1.5.0`); deliverables: `bin/install-sudoers.sh` (new), `sudo -v` prewarm in `bin/acg-up/down/refresh`, `Makefile` `sudoers` target, `docs/howto/makefile.md` Host Setup section, `scripts/tests/test_install_sudoers.bats`; commit message: `feat(sudo): passwordless sudo rules + prewarm for macOS host operations`

## Pending — Next Release Cycle
- **Node.js 20 → 22:** All shopping-cart CI workflows use `node-version: '20'`; GitHub Actions has deprecated Node.js 20 runner. Bump to `'22'` in all 5 repos. Issue doc: `shopping-cart-frontend/docs/issues/2026-05-22-nodejs20-deprecation.md`. Write one Codex spec covering all repos, single-pass.

## In Progress (v1.4.9)
- **IN PROGRESS:** acg-cluster.yaml subtree path restoration — lib-acg v0.2.0 removed `scripts/etc/acg-cluster.yaml` (CloudFormation template) without updating the reference in `acg.sh` (`_LIB_ACG_ROOT/scripts/etc/acg-cluster.yaml`), breaking `make up` with `Invalid template path`. Restored at subtree path `scripts/lib/acg/scripts/etc/acg-cluster.yaml` in k3d-manager; added to hardcoded-ip-allowlist for pre-commit IP literal check. CHANGELOG updated; PR to be created.
- **COMPLETE:** lib-acg PR #19 merged (`7a31eaf`) — docs/copilot-pr18-findings → main; 6 Copilot doc accuracy findings addressed (spec doc files header, Rules section, DoD memory-bank checkboxes, What NOT to Do); Apache 2.0 LICENSE added to lib-acg. enforce_admins restored on lib-acg main. lib-acg next branch: `docs/next-improvements` (commit `9262547`).
- **IN PROGRESS:** shopping-cart-e2e-tests PR #1 open (`chore/add-apache-license`); adds Apache 2.0 LICENSE + copilot-instructions.md + README/package.json license field; 8 Copilot findings fixed in `ca25f79` (PaymentClient, generateTestId, 60s timeout, localhost scope, MIT→Apache 2.0 in README/package.json); all 8 threads resolved; waiting for 3rd Copilot review. No branch protection on e2e-tests main yet.
- **IN PROGRESS:** shopping-cart-infra PR #60 open (`shopping-cart-infra-v0.5.5`); fixes Keycloak ERR_NAME_NOT_RESOLVED from office laptop — KC_HOSTNAME_URL→https://keycloak.3ai-talk.org, argocd-cm issuer+url→3ai-talk.org, realm argocd redirectUri→/auth/callback; CI green; 2 Copilot findings fixed in `6a9d128`; waiting for 2nd Copilot review. ArgoCD-cm and argocd-server already patched live.
- **COMPLETE:** Ghost State SPA render timing fix — lib-acg PR #18 merged (`0b40f42`); skeleton-loader wait + 30s deleteBtn timeout in acg_extend.js; subtree pull into k3d-manager pushed to origin/k3d-manager-v1.4.9.
- **COMPLETE:** lib-acg Ghost State nav fix + acg_check_ttl regression — lib-acg PR #17 merged (`6a69232`): skip Open Sandbox when expired + re-navigate to listing before Ghost State delete; subtree pull from lib-acg main (`936fea39`) restored `acg_check_ttl` in `scripts/lib/acg/scripts/plugins/acg.sh` (had been regressed by prior pull from `docs/next-improvements`). enforce_admins restored on lib-acg.
- **COMPLETE:** expired sandbox auto-restart — spec: `docs/bugs/2026-05-20-acg-up-expired-sandbox-auto-restart.md`; `bin/acg-up` now calls `acg_extend_playwright` (Ghost State Recovery: Delete→Start) + re-extracts credentials instead of `_err`; commit `0395cf8e` pushed to `origin/k3d-manager-v1.4.9`. Supersedes `5962c514` (abort-only approach was wrong).
- **COMPLETE:** sandbox TTL check before provision — spec: `docs/bugs/2026-05-19-acg-up-sandbox-ttl-check-before-provision.md`; `bin/acg-up` now checks sandbox TTL in the AWS skip-extraction path, extending when below threshold; commit `a89cc2e5` pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** Cloudflare tunnel missing Keycloak — spec: `docs/bugs/2026-05-19-acg-up-cloudflare-tunnel-missing-keycloak.md`; `scripts/etc/cloudflared-config.yml.tmpl` added and Step 10h now generates `~/.cloudflared/config.yml` when missing or missing the keycloak ingress; commit `d8f2ca47` pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** CoreDNS duplicate `hosts` plugin crash — spec: `docs/bugs/2026-05-19-acg-up-coredns-duplicate-hosts-plugin.md`; `bin/acg-up` now patches `data.NodeHosts` instead of injecting a second `hosts {}` block into Corefile; commit `5efe45e8` pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** CloudFormation IAM race condition — spec: `docs/bugs/2026-05-19-acg-up-cloudformation-race-after-credential-extraction.md`; `bin/acg-up` polls `aws cloudformation list-stacks` after fresh credential extraction (up to 12×15s); commit `2e46a4a2` pushed to `origin/k3d-manager-v1.4.9`.

## In Progress (v1.4.7)
- **NO-OP:** ArgoCD SSO `invalid_scope: groups` spec was already satisfied on `origin/main` in `shopping-cart-infra`; `requestedScopes` already contains only `openid`, `profile`, and `email`, so there was nothing to commit. Issue doc: `docs/issues/2026-05-18-argocd-groups-scope-already-fixed.md`.

## Completed (v1.4.6)
- **SPEC WRITTEN:** Frontend API contract mismatch — spec: `docs/bugs/2026-05-17-frontend-api-contract-mismatch.md`; products blank (backend `items/total/page_size/pages` → frontend needs `data/totalItems/pageSize/totalPages`; `quantity` → `stock`, `image_url` → `imageUrl`); orders 400 (missing `customerId`; backend returns plain array, frontend expects paginated shape); all fixes in `shopping-cart-frontend` on branch `fix/frontend-api-contract`.
- **SPEC WRITTEN:** Keycloak JWT issuer mismatch (app cluster) — spec: `docs/bugs/2026-05-17-keycloak-jwt-issuer-mismatch-app-cluster.md`; OAUTH2_ISSUER_URI must change from `keycloak.identity.svc.cluster.local` to `keycloak.shopping-cart.local` (matches actual JWT iss); `bin/acg-up` Step 10g.5 adds SSH tunnel + iptables DNAT + CoreDNS patch on ubuntu-k3s; changes also in `shopping-cart-order/k8s/base/configmap.yaml` and `shopping-cart-basket/k8s/base/configmap.yaml`.
- **SPEC WRITTEN:** product-catalog init SQL UUID/SERIAL mismatch — spec: `docs/bugs/2026-05-17-product-catalog-init-sql-serial-vs-uuid.md`; `shopping-cart-infra/data-layer/postgresql/products/init-db.sql` must remove the SERIAL products table DDL so SQLAlchemy owns the schema; workaround (table drop + restart) applied 2026-05-17 on live cluster.
- **LIVE STATE:** order-service and product-catalog both returning 200 on live ubuntu-k3s cluster (2026-05-17); ArgoCD self-heal is ON for all shopping-cart apps; next pod restart will pick up OAUTH2_ENABLED=true from configmap → JWT auth will fail again until Keycloak JWT spec is implemented.
- **COMPLETE:** `acg-up` now skips AWS credential extraction when `_acg_check_credentials` says the current AWS credentials are valid, otherwise it falls back to Playwright extraction; committed as `7d2be2e6` (`fix(acg-up): skip credential extraction when existing AWS credentials are valid`) and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** frontend 504 fix for shared namespace services — `shopping-cart-product-catalog/k8s/base/service.yaml` now exposes the ClusterIP on `8082` and `shopping-cart-order/k8s/base/service.yaml` now exposes the ClusterIP on `8081`, matching the frontend nginx upstream config; committed as `345d89a` in product-catalog and `0edba5b` in order, both on `fix/argocd-shared-namespace` and both pushed to origin.
- **COMPLETE:** k3s-aws CloudFormation stack status-aware provisioning — `scripts/lib/providers/k3s-aws.sh` now checks the existing CloudFormation stack state before provisioning, reuses healthy stacks without `--recreate`, and only recreates broken states; committed as `25f867f0` (`fix(k3s-aws): only recreate CloudFormation stack when broken, reuse when healthy`) and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** 127.0.0.2 loopback alias launchd agent for frontend port-forward — added a best-effort `ifconfig lo0 alias 127.0.0.2` check plus a persistent `/Library/LaunchDaemons/com.k3d-manager.loopback-alias.plist` bootstrap in Step 10g of `bin/acg-up`, before the frontend-browser-http wrapper is written; committed as `8066f35e` (`fix(acg-up): add 127.0.0.2 loopback alias launchd agent for frontend port-forward`) and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** frontend-browser-http wrapper kubectl path fix — `bin/acg-up` now bakes the absolute `kubectl` path into the Step 10g launchd wrapper before writing `~/.local/share/k3d-manager/frontend-browser-http.sh`; committed as `4b0509ff` (`fix(acg-up): bake absolute kubectl path into frontend-browser-http wrapper`) and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Cloudflare Tunnel launchd agents for public ArgoCD + frontend access — added Step 10h in `bin/acg-up` to install macOS launchd agents for `cloudflared tunnel --url http://localhost:8080` and `cloudflared tunnel --url http://127.0.0.2:80`, writing random `trycloudflare.com` URLs to `~/.local/share/k3d-manager/tunnel-urls.txt`; committed as `e45a6b22` (`feat(acg-up): add Cloudflare Tunnel launchd agents for public ArgoCD + frontend access`) and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** frontend.shopping-cart.local direct port-forward fix — replaced the broken ServiceEntry/VirtualService path with a launchd-managed `kubectl port-forward --address=127.0.0.2 svc/frontend 80:80` flow in `bin/acg-up`, changed the frontend hosts entry to `127.0.0.2`, and updated the Step 13 text; committed as `84cb5a21` (`fix(acg-up): replace broken ServiceEntry with direct launchd port-forward on 127.0.0.2 for frontend`) and pushed to `origin/k3d-manager-v1.4.6`.
- **NEXT:** Cloudflare Tunnel for ArgoCD + frontend access — spec: `docs/bugs/2026-05-17-cloudflare-tunnel-argocd-frontend.md`; start only after the frontend port-forward fix is merged and verified.
- **COMPLETE:** frontend.shopping-cart.local NodePort/ServiceEntry/VirtualService wiring — added Step 10g in `bin/acg-up` to create the ubuntu-k3s `frontend-nodeport` NodePort 30080 service, derive the ubuntu-k3s IP from kubeconfig, add the k3d `frontend-ubuntu-k3s` ServiceEntry, and add the `frontend` VirtualService for `frontend.shopping-cart.local`; committed as `14e7b8b0` (`fix(acg-up): wire frontend.shopping-cart.local via NodePort + ServiceEntry + VirtualService`) and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Keycloak empty-admin-secret hard-fail fix — `scripts/plugins/keycloak.sh` now hard-fails when the admin ExternalSecret is not Ready or the admin secret decodes to an empty password, and `bin/acg-up` now fails fast if `keycloak/admin` is missing either Vault field; committed as `1ab96ed1` (`fix(keycloak): hard-fail on empty admin secret in deploy_keycloak and acg-up Vault read`) and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** lib-acg Extend Your Session dialog AppleScript click fix — replaced `bringToFront()` + `keyboard.press('Enter')` with `page.evaluate()`-computed screen coordinates and `child_process.execSync(\`osascript -e 'tell application "System Events" to click at {x, y}'\`)`; committed as `7874e9d` (`fix(acg): click extend dialog close button via AppleScript screen-coordinate click`) and pushed to `origin/fix/acg-credentials-extend-dialog`.
- **COMPLETE (STILL FAILING — superseded):** lib-acg Extend Your Session dialog bringToFront/Enter attempt — `1e7f2ff` (`bringToFront()` + `keyboard.press('Enter')`) dismissed iTerm2 hotkey window and reset page focus before Enter arrived; superseded by the AppleScript screen-coordinate click fix.
- **COMPLETE:** lib-acg credential-test write fix — `bin/acg-credential-test` now writes AWS credentials to `~/.aws/credentials` under `[default]` and validates with `AWS_CONFIG_FILE=/dev/null aws sts get-caller-identity`; committed as `217609a` (`fix(bin): write AWS credentials to ~/.aws/credentials; validate via default profile`) and pushed to `origin/fix/acg-credentials-extend-dialog`.
- **COMPLETE:** Codex — addLocatorHandler post-handler wait loop in BOTH `acg_extend.js` + `acg_credentials.js` — spec: `docs/bugs/2026-05-16-acg-extend-locator-handler-no-wait-after.md` (updated to cover both files); implemented `{ noWaitAfter: true }` in `acg_extend.js` + `{ times: 1, noWaitAfter: true }` in `acg_credentials.js`; committed as `5a638912` (`fix(acg): add noWaitAfter: true to both addLocatorHandler calls`) and pushed to `origin/k3d-manager-v1.4.6`; `node --check scripts/lib/acg/playwright/acg_extend.js`, `node --check scripts/lib/acg/playwright/acg_credentials.js`, `_agent_audit`, and `_agent_lint` passed; `./scripts/k3d-manager test all` failed in unrelated existing suites (`scripts/tests/plugins/eso.bats`, `scripts/tests/plugins/gcp.bats`, `scripts/tests/plugins/keycloak.bats`), recorded in `docs/issues/2026-05-17-repo-test-all-existing-failures.md`
- **COMPLETE (STILL FAILING — new issue):** Codex — acg "Session extended" toast × — page.mouse.click bounding-rect + addLocatorHandler in acg_extend.js — spec: `docs/bugs/2026-05-16-session-extended-mouse-click-and-locator-handler.md`; committed as `718562b9`; × click now works but addLocatorHandler loops infinitely — superseded by noWaitAfter spec.
- **COMPLETE (STILL FAILING):** Codex — acg "Session extended" toast × — page.evaluate DOM traversal — spec: `docs/bugs/2026-05-16-session-extended-evaluate-close-button.md`; replaces 2-line `_hasCIClose` click block with page.evaluate TreeWalker DOM traversal; committed as `ebd3a985` and pushed to `origin/k3d-manager-v1.4.6`; still failing — element.click() does not trigger React synthetic event handler
- **COMPLETE (STILL FAILING):** Codex — acg "Session extended" toast never dismissed — text-locator + page-level CI close — spec: `docs/bugs/2026-05-16-session-extended-text-locator-page-level-close.md`; changed all 7 dismiss sites from `:has-text(…):has(button)` to `text="…"` and replaced the 3-line count+fallback with page-level `button[aria-label="Close" i]` + `[role="alert"] button` / `[role="status"] button` fallback; committed as `70ce5ca9`; still failing — toast has no aria-label, no role=alert, fallback hangs 30s
- **COMPLETE:** Codex — acg-credentials Start Sandbox outside viewport — spec: `docs/bugs/2026-05-16-acg-credentials-start-sandbox-outside-viewport.md`; added 3× retry loop (800ms) to final scroll+click in `_clickStartSandbox`; committed as `7b828419` and pushed to `origin/k3d-manager-v1.4.6`
- **COMPLETE:** Codex — acg "Session extended" × close fallback — spec: `docs/bugs/2026-05-16-session-extended-close-fallback-button-last.md`; added count-check + `button.last()` fallback at all 7 dismiss sites in `acg_credentials.js` + `acg_extend.js`; committed as `e464bafa` and pushed to `origin/k3d-manager-v1.4.6`
- **COMPLETE (STILL FAILING):** Codex — acg "Session extended" × button not clicked — spec: `docs/bugs/2026-05-16-session-extended-button-aria-label.md`; changed all 7 `.locator('button').first()` dismiss calls to `.locator('button[aria-label="Close"]').first()` in `acg_credentials.js` (4 sites) and `acg_extend.js` (3 sites); committed as `fd9b2fe7`; still failing — button[aria-label="Close"] not found in container, .catch swallows silently
- **COMPLETE:** Codex — acg-up return outside function — spec: `docs/bugs/2026-05-16-acg-up-return-outside-function.md`; changed `bin/acg-up` `|| return 1` to `|| exit 1`; committed as `fd9b2fe7`
- **COMPLETE:** Codex — acg-credentials _clickStartSandbox blocked by Session extended modal — spec: `docs/bugs/2026-05-16-acg-credentials-click-start-sandbox-blocked-by-session-extended.md`; `_clickStartSandbox` now dismisses the "Your sandbox has been extended." card before scrolling/clicking Start Sandbox; committed as `644ec725` and pushed to `origin/k3d-manager-v1.4.6`
- **COMPLETE:** Codex — acg-credentials isEnabled() gate skips Start Sandbox click — spec: `docs/bugs/2026-05-16-acg-credentials-start-sandbox-isenabled-gate.md`; `scripts/lib/acg/playwright/acg_credentials.js` Pattern 1 now always clicks Start Sandbox when visible; committed as `ee8be3ca` and pushed to `origin/k3d-manager-v1.4.6`
- **COMPLETE:** Codex — acg-up aws sts shortcut skips credential extraction — spec: `docs/bugs/2026-05-16-acg-up-credential-shortcut-skips-extraction.md`; `bin/acg-up` now always calls `acg_get_credentials ${sandbox_url:+"$sandbox_url"} || return 1` for `k3s-aws`; committed as `b1da7ef4` and pushed to `origin/k3d-manager-v1.4.6`
- **COMPLETE:** Codex — acg-credentials Escape does not dismiss "Extend Your Session" dialog — spec: `docs/bugs/2026-05-16-acg-credentials-escape-does-not-dismiss-extend-session-dialog.md`; `scripts/lib/acg/playwright/acg_credentials.js` now clicks `_extendSessionPrompt.locator('button[aria-label="Close"]').first().click({ force: true })` instead of pressing Escape; committed as `c66e8bc5` and pushed to `origin/k3d-manager-v1.4.6`
- **COMPLETE:** Codex — acg Session extended modal × button not clicked — wrong locator scope — spec: `docs/bugs/2026-05-16-acg-session-extended-modal-wrong-button.md`; changed all 6 locator declarations from `[role="dialog"]:has-text("Session extended")`.first() to `:has-text("Your sandbox has been extended."):has(button)`.last() in both files; committed as `8eb6fd02` and pushed to `origin/k3d-manager-v1.4.6`
- **COMPLETE:** Codex — k3s-aws CloudFormation deploy fails when stack is in terminal failure state — spec: `docs/bugs/2026-05-16-k3s-aws-cloudformation-rollback-state.md`; `scripts/lib/providers/k3s-aws.sh` now calls `acg_provision --confirm --recreate`; committed as `bc4485d8` and pushed to `origin/k3d-manager-v1.4.6`
- **COMPLETE:** Codex — acg Session extended modal not closed (Escape unreliable) — spec: `docs/bugs/2026-05-16-acg-session-extended-modal-escape-unreliable.md`; `scripts/lib/acg/playwright/acg_extend.js` and `scripts/lib/acg/playwright/acg_credentials.js` now click the dialog's scoped button instead of pressing Escape; implementation committed as `f39adc25`; validation note committed as `43dc422a`; both pushed to `origin/k3d-manager-v1.4.6`
- **NOTE:** Direct ad hoc `_agent_checkpoint` / `_agent_lint` / `_agent_audit` invocation in this shell emitted `.git/index.lock` permission errors and missing `_err` helper output; recorded in `docs/issues/2026-05-16-git-commit-index-lock-permission-error.md`
- **COMPLETE:** `bats scripts/tests/bin/acg_up.bats` step-number grep drift resolved — updated the expectation to `Step 10e/14 — Installing Istio ingress HTTP listener` so it matches current `bin/acg-up`
- **COMPLETE:** Codex — acg-credentials handler loops 3× then times out; Start Sandbox button outside viewport — spec: `docs/bugs/2026-05-16-acg-credentials-handler-times-1-click-helper.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now adds `{ times: 1 }` to the "Extend Your Session" handler, waits 1000ms to settle after dismiss, adds `_clickStartSandbox(page, buttonLocator)` with hidden-prompt wait + scroll + force-click, and routes both Start Sandbox call sites through the helper; committed as `982d551e` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-credentials fallback Escape closes credentials panel — spec: `docs/bugs/2026-05-16-acg-credentials-credential-wait-escape-closes-panel.md`; `scripts/lib/acg/playwright/acg_credentials.js` now removes blind fallback Escape from the handler and `_waitForCredentials` inline dismiss, only presses Escape when "Session extended" is confirmed visible, and waits for each dialog to hide; committed as `b7f0d0a5` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE (STILL FAILING):** Codex — acg-credentials Start Sandbox detaches after modal dismiss — spec: `docs/bugs/2026-05-16-acg-credentials-start-sandbox-detached-after-modal-dismiss.md`; committed as `7d2faaba`; still failing — `_clickStartSandbox` with `force:true` doesn't help when button is truly hidden behind re-appearing modal (Cancel in handler doesn't extend session → dialog loops); superseded by extend-session-via-filled-button spec.
- **COMPLETE (STILL FAILING):** Codex — acg-credentials handler Cancel/filled-button causes dialog loop — spec: `docs/bugs/2026-05-16-acg-credentials-extend-session-via-filled-button.md`; committed as `452fee93`; still failing — `pando-button--usage_filled` also doesn't extend the session; dialog loops 3× and 30s budget exhausted. Extend button selector is unknown after 5+ attempts.
- **COMPLETE (SUPERSEDED):** Codex — acg-credentials remove addLocatorHandler — spec: `docs/bugs/2026-05-16-acg-credentials-remove-addlocatorhandler.md`; committed as `02d792a4`; superseded — visual inspection of live dialog confirmed button text is "Extend Session" (not "Extend"); re-adding handler with exact label.
- **COMPLETE (STILL FAILING):** Codex — acg-credentials re-add addLocatorHandler with exact "Extend Session" button label and fix inline dismiss in `_waitForCredentials` — spec: `docs/bugs/2026-05-16-acg-credentials-addlocatorhandler-extend-session-button.md`; committed as `6154b0df`; still failing — handler fires 3× due to SPA re-render flicker on `data-open`, Start Sandbox button outside viewport after dialog dismiss; superseded by handler-times-1-click-helper spec.
- **COMPLETE:** Codex — acg-credentials handler non-blocking 250ms — spec: `docs/bugs/2026-05-16-acg-credentials-handler-nonblocking-250ms.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now removes the redundant hidden `waitFor` and settles with `waitForTimeout(250)` only; committed as `e2d4bb1b` and pushed to `origin/k3d-manager-v1.4.6`. Still failing: post-dismiss React re-render detaches Start Sandbox button — separate bug.
- **COMPLETE (SUPERSEDED):** Codex — acg-credentials handler dismiss-only with waitFor + 500ms settle — spec: `docs/bugs/2026-05-16-acg-credentials-handler-dismiss-waitfor.md`; branch: `k3d-manager-v1.4.6`; committed as `73660e6a`; still failing — waitFor is redundant with Playwright built-in, consumes entire 30s in one cycle; superseded by nonblocking-250ms spec.
- **COMPLETE:** Codex — acg-credentials handler clicks Cancel instead of Extend, dialog reappears 5× and times out — spec: `docs/bugs/2026-05-16-acg-credentials-extend-session-not-cancel.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now clicks `Extend` first in the `addLocatorHandler` callback, falls back to `Cancel`/Escape, and keeps the 500ms Escape plus 1s settle wait; committed as `a07c3c10` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-credentials startButton detaches after React re-render following modal close — spec: `docs/bugs/2026-05-16-acg-credentials-extend-modal-handler-animation-settle.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now waits 1s at the end of the `addLocatorHandler` callback so the modal animation and React re-render settle before Playwright resumes the intercepted click; committed as `0fbdba3079b8c1e183e8cb1b72fdbf1429a3f3ff` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-credentials addLocatorHandler post-handler wait loops on CSS slide-out animation — spec: `docs/bugs/2026-05-16-acg-credentials-extend-modal-handler-wait-timeout.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now scopes `page.addLocatorHandler()` to `[data-open="true"]` so the post-handler wait exits immediately when Cancel sets `data-open="false"`; committed as `1026389cdbb4353ec4a4dba6b402f0ec5e9bcb88` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-credentials "Extend Your Session" modal still blocks during sandbox wait and button-click phase — spec: `docs/bugs/2026-05-16-acg-credentials-extend-modal-addlocatorhandler.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now registers `page.addLocatorHandler()` immediately after page establishment to auto-dismiss the modal at any point; committed as `4cbe3b7e26e41eee1a2e05897a75c1d030557ef8` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — add frontend.shopping-cart.local via Istio ingress gateway — spec: `docs/plans/v1.4.6-frontend-browser-access-via-istio-ingress.md`; branch: `k3d-manager-v1.4.6`; `bin/acg-up` now adds `frontend.shopping-cart.local` to `_HOSTS_LIST`, points Step 10e at `svc/istio-ingressgateway` in `istio-system`, removes the localhost:3000 frontend port-forward, and prints the frontend URL in the final summary; committed as `e701139a17eb4acb19bb84976db5e4a14e795a8f` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-credentials "Extend Your Session" modal not dismissed — spec: `docs/plans/v1.4.6-bugfix-acg-credentials-extend-session-modal.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now dismisses the lingering "Extend Your Session" prompt before the Start/Open flow; committed as `69204c91e5e060df6e2ca230c1180d6055f1d91d` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-credentials GCP reads AWS inputs (wrong positional extraction + early `_waitForCredentials` return) — spec: `docs/plans/v1.4.6-bugfix-acg-credentials-gcp-reads-aws-inputs.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now waits for GCP JSON content and extracts by content instead of position; committed as `1ba5c5ac66d5493cdfa8ca661c0372ca518c6882` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-credentials "Session extended" modal not dismissed — spec: `docs/plans/v1.4.6-bugfix-acg-credentials-session-extended-modal.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now dismisses the lingering "Session extended" modal before the Start/Open flow; committed as `3444c171d4797e4cc305b4fc9c1bc707b6a9fb5c` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-credentials wrong sandbox provider — spec: `docs/plans/v1.4.6-bugfix-acg-credentials-wrong-sandbox-provider.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_credentials.js` now selects the provider-specific "Open Sandbox" card by walking ancestors before clicking; committed as `5dfbdf44c2f3dce49e53f1e3f245b5c8e3a470fd` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — acg-extend "Start Sandbox" race — spec: `docs/plans/v1.4.6-bugfix-acg-extend-start-sandbox-race.md`; branch: `k3d-manager-v1.4.6`; `scripts/lib/acg/playwright/acg_extend.js` now exits 0 when the fallback button text is "Start Sandbox" instead of waiting for an extend button that never appears; committed as `bae89aafae42aeea7b7d71cd65eed4c24ca84962` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — auto-trust Vault CA cert in macOS System Keychain — spec: `docs/plans/v1.4.6-auto-trust-vault-ca.md`; branch: `k3d-manager-v1.4.6`; `bin/acg-up` now calls `security add-trusted-cert` via `_run_command --interactive-sudo --soft` immediately after `_argocd_issue_browser_tls_material` on macOS and `scripts/tests/bin/acg_up.bats` asserts the new call; committed as `42d9941306658c259f367916bc68ea69573a3085` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — /etc/hosts sudo + HTTPS listener timeout — spec: `docs/bugs/2026-05-15-acg-up-etc-hosts-sudo-and-listener-timeout.md`; branch: `k3d-manager-v1.4.6`; `bin/acg-up` now uses `--interactive-sudo` for the `/etc/hosts` update and `ARGOCD_BROWSER_LISTENER_WAIT_TIMEOUT:-120` for the HTTPS listener; committed as `0ea4cc9f89649a3a5ca7d8a2da4df4f6b39fb4b9` and pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** Codex — argocd-cm HTTP URL breaks OIDC SSO callback — spec: `shopping-cart-infra/docs/bugs/2026-05-15-argocd-cm-http-url-breaks-sso-callback.md`; branch: `shopping-cart-infra-v0.5.3`; changed `url: http://` → `url: https://` in `argocd/config/argocd-cm.yaml` line 13; committed as `411ebb2e3b551cccbe44260c30bb561940357eb8` and pushed to `origin/shopping-cart-infra-v0.5.3`.
  - Root cause: `url: http://argocd.shopping-cart.local` causes OIDC callback redirect to port 80 (Keycloak port-forward) instead of port 443 (ArgoCD socat HTTPS wrapper).
  - Commit message: `fix(argocd): use https url so OIDC callback resolves to ArgoCD not Keycloak`

## Post-Merge Housekeeping — 2026-05-15 (shopping-cart-frontend #15)
- **shopping-cart-frontend PR #15 merged** — shopping-cart-frontend-v0.5.1 → main, SHA `19e47118a2156589fadcfc8998b37f29bda18f75`
- **Keycloak SSO wiring shipped** — `.github/workflows/ci.yml` now passes `VITE_KEYCLOAK_URL` build arg; `nginx.conf` updated with canonical `keycloak.shopping-cart.local` CSP allowlist; deployment stabilized with `nginx-cache` emptyDir
- **enforce_admins restored** — `true` ✓ (shopping-cart-frontend)
- **v0.5.1 branch created** — `shopping-cart-frontend-v0.5.1` from merge SHA; retrospective added + committed + pushed
- **Retrospective doc** — `docs/retro/2026-05-15-v0.5.1-retrospective.md` (on shopping-cart-frontend-v0.5.1, commit `60e660f`)
- **Summary** — v0.5.1 completed the multi-repo SSO wiring after a revert from an accidental main push. Copilot caught a merge-base misalignment that hid the functional commits from the PR diff; the fix was re-applied via the feature branch merge.


## Post-Merge Housekeeping — 2026-05-15 (shopping-cart-infra #57)
- **shopping-cart-infra PR #57 merged** — shopping-cart-infra-v0.5.2 → main, SHA `adbaec8de6725817ba55b8a36c1653a5fa1bb3ae`
- **Networking + mTLS fixes shipped** — `project: shopping-cart` AppProject now applied, Keycloak DestinationRule with `tls.mode: DISABLE` for ArgoCD OIDC token exchange
- **enforce_admins restored** — `true` ✓ (shopping-cart-infra)
- **v0.5.3 branch created** — `shopping-cart-infra-v0.5.3` from merge SHA; retrospective added + committed + pushed
- **Retrospective doc** — `docs/retro/2026-05-15-v0.5.2-retrospective.md` (on shopping-cart-infra-v0.5.3, commit `907bf06`)
- **Branch cleanup** — deleted v0.4.0, v0.5.0, v0.5.2; retained v0.5.3 as current feature branch
- **Summary** — v0.5.2 fixed the remaining SSO blockers: the `shopping-cart` AppProject was never applied to the cluster (blocking the networking ArgoCD app), and Istio mTLS was blocking ArgoCD's in-cluster OIDC token exchange to Keycloak. Copilot caught a backwards fix in the initial commit.

## Post-Merge Housekeeping — 2026-05-15 (shopping-cart-infra #56)
- **shopping-cart-infra PR #56 merged** — shopping-cart-infra-v0.5.1 → main, SHA `79c42b71db07f0889f90e744d571d2a9998a4934`
- **Keycloak reconcile hook fix shipped** — replaced python3 JSON parsing with kcadm.sh -q server-side filters; hook now compatible with ubi9-minimal
- **enforce_admins restored** — `true` ✓ (shopping-cart-infra)
- **v0.5.2 branch created** — `shopping-cart-infra-v0.5.2` from merge SHA; retrospective added + committed + pushed
- **Retrospective doc** — `docs/retro/2026-05-15-v0.5.1-retrospective.md` (on shopping-cart-infra-v0.5.2)
- **Summary** — v0.5.1 fixed SSO login failures caused by the reconcile hook's python3 dependency, which is absent in the ubi9-minimal container. Follow-up commit corrected a `|| echo 0` pattern that would have produced invalid integer output for the mapper-exists check.

## Post-Merge Housekeeping — 2026-05-15 (shopping-cart-infra #55 + rigor-cli #10)
- **shopping-cart-infra PR #55 merged** — bug/keycloak-ldap-mappers-missing → main, SHA `ff9e4d5a`
- **rigor-cli PR #10 merged** — rigor-cli-v0.1.6 → main, SHA `2d6d4b68`, tagged `v0.1.6`
- **enforce_admins restored on both repos** — `true` ✓ (shopping-cart-infra + rigor-cli)
- **rigor-cli v0.1.6 tag + GitHub release created** — merge SHA `2d6d4b68` tagged, released 2026-05-15
- **Retrospectives created** — shopping-cart-infra `docs/retro/2026-05-15-v0.5.0-retrospective.md`, rigor-cli `docs/retro/2026-05-15-v0.1.6-retrospective.md` (on next branches)
- **Next branches created** — shopping-cart-infra-v0.5.1, rigor-cli-v0.1.7; retrospectives committed and pushed
- **bug/keycloak-ldap-mappers-missing deleted** — merged branch cleaned up from shopping-cart-infra

## Post-Merge Housekeeping — 2026-05-08 (v1.4.4 + PR #37)
- **k3d-manager PR #73 + shopping-cart-infra PR #37 merged** — both enforced to main
- **k3d-manager enforce_admins restored** — `true` ✓
- **shopping-cart-infra enforce_admins restored** — `true` ✓
- **k3d-manager v1.4.4 tag + release created** — merge SHA `92ccaec1` tagged, released 2026-05-08
- **Retrospectives created** — k3d-manager `docs/retro/2026-05-08-v1.4.4-retrospective.md`, shopping-cart-infra `docs/retro/2026-05-08-pr37-keycloak-externalsecret-retrospective.md`
- **Next branches created/synced** — k3d-manager-v1.4.5, shopping-cart-infra docs/next-improvements

## Shipped in v1.4.2
- `_ai_agent_review` dispatch wrapper added to lib-foundation; copilot plugin functions route through it
- `K3DM_ENABLE_AI` gate removed from lib-foundation backend (`_copilot_review`) — gate stays in callers
- ArgoCD bootstrap: `ARGOCD_SERVER_WAIT_TIMEOUT` configurable (default 600s)
- `bin/acg-up`: bootstrap refresh on existing Hub when AppProject/ApplicationSets absent
- `bin/acg-up`: `--confirm` flag added to `deploy_argocd_bootstrap` call
- lib-acg PR #10 subtree pull: `launchctl bootout`, dead Linux else-block removal

## Deferred to v1.4.3
- **`deploy_argocd_bootstrap "$@"` passthrough** — removed in v1.4.2 (Copilot finding); correct fix is lib-foundation change to filter flags explicitly; callers (especially `provision-tomcat`) depend on this behavior
- **lib-foundation upstream doc fix** — `scripts/lib/foundation/docs/api/functions.md` usage snippet still has k3d-manager-specific `K3DM_ENABLE_AI=1` context; needs upstream lib-foundation PR

## Current Focus (v1.4.5)
- **COMPLETE:** v1.4.6 follow-up — `scripts/lib/acg/playwright/acg_extend.js` updated for Auto Shutdown click recovery, debug screenshots, and broader extend selectors as commit `34c41ced492f37525f170feed6664156b5647634`; `bin/acg-up` moved `/etc/hosts` updates before step 4c and then corrected the ArgoCD browser listener healthz URL to `127.0.0.1` as commit `9c0c6e63f128be4db3d166007ff4909c623e9faa`, both pushed to `origin/k3d-manager-v1.4.6`.
- **COMPLETE:** multi-repo SSO wiring follow-up — `k3d-manager` ACG sandbox navigation fix committed as `3bb7aa817983948cd426bc6a96525a811b2bd7ff` on `k3d-manager-v1.4.5`; `shopping-cart-infra` SSO ingress/workflow update committed as `8c581840b904f21459fa225c80ddfe54f93ed9aa` on `shopping-cart-infra-v0.5.3`; `shopping-cart-frontend` CI/CSP update committed as `6120783b8af124abebca7f242f5b905a29734836` on `shopping-cart-frontend-v0.5.1` plus issue-doc commit `64c943e5b39ec1468c1de8f0a7e6768a1d4cb6b1` on the same branch, with `main` later reverted to `2872a4cd9b120b55673b0c2633ab8e6270cb6b8c` after an accidental remote update. Manual cluster/bootstrap follow-up step 9 was intentionally skipped per instruction.
- **COMPLETE:** `fix(networking)` — shopping-cart-infra branch `shopping-cart-infra-v0.5.2` updated per spec `docs/bugs/2026-05-15-networking-app-project-and-mtls.md`; changed `argocd/applications/networking.yaml` to `project: platform`, added `networking/istio/keycloak-destinationrule.yaml`, committed as `6c70fee99baa2c2f29330113e4f1c96b2b94cf75`, and pushed to `origin/shopping-cart-infra-v0.5.2`.
- **COMPLETE:** `fix(acg-up)` — spec `docs/bugs/v1.4.5-bugfix-etc-hosts-soft.md`. Added `--soft` to line 639 (SHA: `3c096f6`).
- **COMPLETE:** `fix(acg-up-bugs)` — spec `docs/plans/v1.4.5-bugfix-gemini.md`. Fixed CoreDNS NodeHosts patch and /etc/hosts sudo fallback (SHA: `3c096f6`).
- **COMPLETE:** `fix(argocd-sso)` — spec at `docs/plans/v1.4.5-argocd-sso-keycloak.md`; implemented 5-change ArgoCD SSO wiring.
- **COMPLETE:** pyjenkinsapi rigor-cli v0.1.6 subtree pull.
- **COMPLETE:** `fix(ldap)` — shopping-cart-infra PR #39 `fix/ldap-duplicate-externalsecret` (SHA: `afaf109`) + PR #38 SSO wiring (SHA: `d314c34`). Removed duplicate `ldap-secrets-externalsecret.yaml` entry; ArgoCD SSO Keycloak wired end-to-end. enforce_admins restored on shopping-cart-infra.
- **COMPLETE:** `fix(argocd)` — spec `docs/bugs/v1.4.5-bugfix-services-git-identity-exclude.md` (SHA: `fa7bb830`). Excluded `shopping-cart-identity` from `services-git` ApplicationSet generator; re-applied identity Application with correct local-cluster destination.
- **COMPLETE:** `fix(vault)` — spec `docs/bugs/v1.4.5-bugfix-eso-ldap-policy-missing-keycloak.md` (SHA: `48938dea`). Extended `LDAP_VAULT_POLICY_PREFIX` default to `ldap,keycloak` so `eso-ldap-directory` policy covers `keycloak/*` paths.
- **COMPLETE:** `fix(acg-up)` — spec `docs/bugs/v1.4.5-bugfix-identity-appproject-missing.md` (SHA: `771ba5cf`). Changed `project: shopping-cart` → `project: platform` in step 10c inline Application; removed dead `services/shopping-cart-identity/kustomization.yaml`.
- **COMPLETE:** `fix(identity-eso-bootstrap)` — spec `docs/bugs/v1.4.5-bugfix-identity-externalsecret-bootstrap.md`. Bug 1 (`f969b299`). Bugs 2+3: shopping-cart-infra PR #40 merged to main (`dfe00df1`). Bugs 4+5: shopping-cart-infra PR #41 merged to main (`180f5f89`) — Keycloak PostgreSQL driver + LDAP LDIF chown fixes. enforce_admins restored.
- **MERGED Bug 6 — LDAP CrashLoopBackOff fix** — shopping-cart-infra PR #42 merged (`11aa8d7d`) 2026-05-09. enforce_admins restored. Retro: `docs/retro/2026-05-09-pr42-ldap-ldif-staging-retrospective.md` (on `docs/next-improvements`).
- **MERGED Bug 7 — LDAP CrashLoopBackOff (LDAP_PORT service link conflict)** — shopping-cart-infra PR #43 merged (`dcd18af7`) 2026-05-09. enforce_admins restored. Retro: `docs/retro/2026-05-09-pr43-ldap-service-links-retrospective.md` (on `docs/next-improvements`).
- **COMPLETE:** `fix(eso)` — spec `docs/bugs/v1.4.5-bugfix-deploy-eso-no-version-pin.md` (SHA: `50851f0e`). Pinned ESO Helm chart version to `1.0.0` (overridable via `ESO_HELM_CHART_VERSION`); matches remote cluster install in `acg-up`.
- **COMPLETE Bug 8 — CoreDNS Corefile patched with Keycloak ClusterIP** — `34e0101f`. Removed NodeHosts/IngressGateway approach; added stable `hosts` block in Corefile pointing to Keycloak ClusterIP. Spec: `docs/bugs/2026-05-09-argocd-sso-coredns-keycloak-clusterip.md`.
- **COMPLETE Bug 9 — frontend port-forward launchd agent** — `fb56a443`. Step 13 added to acg-up; plist targets `ubuntu-k3s/shopping-cart-apps/frontend:80 → localhost:3000`. Spec: `docs/bugs/2026-05-09-acg-up-frontend-port-forward.md`.
- **COMPLETE Bug 10 — ArgoCD localhost:8080 readiness gate** — `bin/acg-up` now waits for `localhost:8080/healthz` after loading the Argo CD launchd agent and fails fast with the log tail if the listener never becomes reachable. Spec: `docs/bugs/2026-05-11-acg-up-argocd-port-forward-readiness.md`.
- **COMPLETE Bug 11 — acg-down keep-hub preservation trust gap** — `bin/acg-down --confirm --keep-hub` now logs the resolved Hub cluster and the `Makefile` wrapper exposes `KEEP_LOCAL` directly, so the preserved-Hub path is unambiguous. Spec: `docs/bugs/2026-05-11-acg-down-keep-hub-preservation.md`.
- **COMPLETE Bug 12 — acg-up missing Argo CD plugin source** — `bin/acg-up` now sources `scripts/plugins/argocd.sh` before Step 4b calls `_argocd_wait_for_local_port_forward` (SHA: `6052786a`). Spec: `docs/bugs/2026-05-11-acg-up-missing-argocd-source.md`.
- **COMPLETE Bug 13 — acg-up PLUGINS_DIR unbound before plugin sourcing** — `bin/acg-up` now initializes `PLUGINS_DIR="${SCRIPT_DIR}/plugins"` before sourcing `scripts/plugins/argocd.sh` (SHA: `5d4b9b1e`). Spec: `docs/issues/2026-05-11-acg-up-unbound-plugins-dir.md`.
- **COMPLETE Bug 14 — Keycloak `argocd` client redirect reconciliation** — `bin/acg-up` now reconciles the existing `argocd` client redirect URIs even when the realm already exists, so rebuilds do not preserve stale OIDC callbacks. Spec: `docs/bugs/2026-05-11-keycloak-argocd-client-redirect-reconciliation.md`.
- **COMPLETE Bug 15 — Argo CD port-forward flaps after sandbox rebuild** — `bin/acg-up` now renders a self-healing launchd wrapper that restarts the Argo CD port-forward whenever `localhost:8080/healthz` stops responding. Spec: `docs/issues/2026-05-11-acg-up-argocd-port-forward-flapping.md`. Commit: `18f8d884`.
- **FOLLOW-UP:** `scripts/plugins/argocd.sh:_argocd_write_port_forward_wrapper` is temporarily allowlisted because the Agent Audit counter over-reports the helper as if it contained 11 `if` blocks. Spec: `docs/issues/2026-05-11-argocd-port-forward-wrapper-if-count-mismatch.md`.
- **FOLLOW-UP:** `scripts/plugins/keycloak.sh:deploy_keycloak` remains over the Agent Audit if-count threshold and is temporarily allowlisted pending a separate refactor. Spec: `docs/issues/2026-05-11-keycloak-deploy_keycloak-if-count.md`.
- **COMPLETE:** Keycloak admin token mismatch after rebuild. `bin/acg-up` now preserves existing Vault identity secrets on rebuild, and the live Vault `secret/keycloak/admin` secret was restored to the historical working password version. Specs: `docs/bugs/2026-05-11-keycloak-admin-password-reseed-on-rebuild.md` and `docs/issues/2026-05-11-keycloak-admin-token-reconciliation-failure.md`.
- **OPEN:** Argo CD rejects `localhost:8080` return URLs during SSO login. Live `/auth/login` rejects `return_url=http://localhost:8080/...` while `https://argocd.shopping-cart.local/...` succeeds. Spec: `docs/issues/2026-05-11-argocd-localhost-return-url-rejected.md`.
- **COMPLETE:** Argo CD canonical HTTPS hostname is now backed by a Vault PKI-issued local TLS listener. `bin/acg-up` installs a root-owned `launchd` daemon that terminates TLS for `argocd.shopping-cart.local:443` and proxies to the existing `localhost:8080` Argo CD port-forward, and `bin/acg-down` removes it. Spec: `docs/issues/2026-05-11-argocd-canonical-https-listener-missing.md` (fixed).
- **COMPLETE:** Argo CD browser TLS role creation now derives `allowed_domains=shopping-cart.local` for `argocd.shopping-cart.local` so Vault accepts the PKI role write on rebuild. Spec: `docs/issues/2026-05-11-argocd-browser-tls-role-allowed-domains.md` (fixed).
- **COMPLETE:** Argo CD browser TLS role write now authenticates to Vault before upserting the PKI role so the browser TLS helper does not hit `403 permission denied` on rebuild. Spec: `docs/issues/2026-05-11-argocd-browser-tls-vault-login-permission-denied.md`.
- **COMPLETE:** Argo CD browser TLS cleanup now skips revoke when the prior cert is already gone, so stale serials no longer abort `make up`. Fixed in `f2942bf9`. Spec: `docs/issues/2026-05-11-argocd-browser-tls-revoke-missing-cert.md`.
- **PLANNED:** `make up TRUST_CA=1` host CA trust bootstrap — spec `docs/plans/v1.4.5-trust-ca-auto-import.md`. Goal is to keep trust installation opt-in while letting the helper auto-detect macOS vs Linux.
- **COMPLETE:** `fix(acg-up)` now preserves stderr from the macOS browser `launchctl` install/bootstrap path and reports `argocd-browser-https-launchctl.log` on failure. Commit `8d18e18c`.
- **COMPLETE:** `fix(acg-up)` now treats the macOS browser `launchctl bootout` step as best-effort, so a missing existing listener no longer aborts bootstrap. Commit `8d18e18c`.
- **NOTE:** Added `docs/issues/2026-05-12-acg-up-argocd-browser-bootout-fails-when-listener-missing.md` to record `launchctl bootout system` exit code `5` during rebuilds.
- **COMPLETE:** `docs: stop advertising localhost as browser entrypoint` — `bin/acg-up` now labels localhost as terminal-only and `docs/howto/argocd.md` points browser SSO users at `https://argocd.shopping-cart.local`. Commit `b086aef2`. Added `docs/issues/2026-05-12-argocd-localhost-browser-entrypoint-misleading.md`.
- **COMPLETE:** `fix(acg-up)` — browser HTTPS preflight now skips the privileged launchd reinstall when `https://argocd.shopping-cart.local:443/healthz` is already healthy, so repeat `make up` runs no longer prompt for sudo unnecessarily. Commit `d5be3d0e`.
- **COMPLETE:** `fix(acg-up)` — browser-side Keycloak now runs through a local launchd-managed HTTP listener on `keycloak.shopping-cart.local:80` while `/etc/hosts` keeps the hostname on `127.0.0.1`, fixing the Safari “can’t connect to server” redirect path. Commit `bca8a30c`. Issue doc: `docs/issues/2026-05-12-keycloak-browser-http-listener-missing.md`. Supersedes the earlier ingress-gateway browser mapping (`99581cc7`).
- **COMPLETE:** `fix(acg-up)` — the Argo CD port-forward wrapper now falls back from the requested `k3d-k3d-cluster` context to `kubectl config current-context` and then to the kubeconfig default, preventing `make up` from looping on a missing-context launchd environment. Commit `3f8d5150`. Issue doc: `docs/issues/2026-05-12-argocd-port-forward-context-fallback.md`.
- **COMPLETE:** `fix(acg-up)` — removed the macOS Safari auto-open entirely so `make up` finishes bootstrap without launching a browser, then prints the canonical Argo CD login URL for manual navigation. Issue doc: `docs/issues/2026-05-12-acg-up-browser-auto-open-removed-for-noninteractive-flows.md`.
- **IN PROGRESS:** `fix(acg-up)` follow-up — generic `open` can still leave Safari on an old localhost session in some cases, so the browser handoff may still need a more reliable Safari-specific path. Issue doc: `docs/issues/2026-05-12-argocd-safari-stale-localhost-tab-keeps-reopening.md`.
- **COMPLETE:** `fix(argocd-sso)` follow-up — live Keycloak `client_attributes` preserved the stale `pkce.code.challenge.method` row even after the realm JSON changed. `bin/acg-up` now deletes that row during client reconciliation. Issue doc: `docs/issues/2026-05-12-keycloak-client-attributes-merge-preserves-pkce.md`.
- **COMPLETE:** `refactor(bin)` — renamed the `bin/*.sh` helpers to extensionless `bin/*` scripts and updated the live caller paths plus active docs to match. Commit `d498dcf9`.
- **COMPLETE:** `fix(vault-exec)` — the new `bin/vault-exec` helper now auto-authenticates `vault ...` troubleshooting commands with the live `vault-root` token. Issue doc: `docs/issues/2026-05-12-vault-exec-403-unauthenticated-root-token.md`.
- **COMPLETE:** `fix(acg-up)` follow-up — `make up` now fails if the Keycloak realm import does not actually happen, so the bootstrap cannot silently report success with stale SSO state. Issue doc: `docs/issues/2026-05-12-acg-up-silent-keycloak-realm-import-skip.md`.
- **COMPLETE:** `fix(acg-up)` follow-up — realm import status capture no longer concatenates a fallback `000` onto a real Keycloak `409` response. Issue doc: `docs/issues/2026-05-12-acg-up-keycloak-realm-import-status-concatenated-fallback.md`.
- **COMPLETE:** `fix(acg-up)` follow-up — Keycloak readiness gate now uses a configurable, longer default wait window so cold cluster rebuilds do not fail at the 5 minute mark. Issue doc: `docs/issues/2026-05-12-keycloak-api-readiness-timeout-too-short.md`.
- **COMPLETE:** `fix(makefile)` — `make down` no longer preserves the local Hub by default; set `KEEP_LOCAL=1` to keep it. Issue doc: `docs/issues/2026-05-12-make-down-default-preserved-local-hub-was-counterintuitive.md`.
- **COMPLETE:** `fix(acg-down)` — browser launchd bootout is best-effort for both Keycloak and ArgoCD listeners, so already-removed daemons only warn instead of failing `make down`. Issue doc: `docs/issues/2026-05-12-acg-down-browser-launchd-bootout-missing-listener-aborted.md`.
- **IN PROGRESS:** `fix(acg-up)` follow-up — the first fallback patch still rendered an optional `_kubectl_context_args[@]` array under `set -u` and quoted an empty kubeconfig value. New patch switches the wrapper to a scalar context argument and renders absent kubeconfig as truly empty. Issue doc: `docs/issues/2026-05-12-argocd-port-forward-wrapper-empty-array-unbound.md`.
- **COMPLETE:** `fix(acg-up)` follow-up — Keycloak readiness no longer polls a dead `kubectl port-forward` tunnel; `bin/acg-up` now waits on `deployment/keycloak` becoming `Available` and dumps deploy/pod status on timeout. Issue doc: `docs/issues/2026-05-12-keycloak-readiness-loop-watched-dead-port-forward.md`.
- **NOTE:** Live SSO login still fails at the LDAP bind layer even though Keycloak and Argo CD are up. `bin/ldap-search` against the live `openldap-admin` secret returns `ldap_bind: Invalid credentials (49)`. Issue doc: `docs/issues/2026-05-12-live-ldap-bind-secret-invalid-credentials.md`.
- **COMPLETE:** `fix(argocd-sso)` follow-up — Argo CD OIDC config requested the unsupported `groups` scope, causing `invalid_scope: Invalid scopes: openid profile email groups`. Patched `shopping-cart-infra/argocd/config/argocd-cm.yaml` to remove `groups` from `requestedScopes` while keeping the groups claim mapper. Shopping-cart-infra commit: `09fbb7da14b447ccdbed17bd4a422eebeaa2811d`. Issue doc: `docs/issues/2026-05-12-argocd-invalid-scope-groups-requested.md`.
- **COMPLETE:** `fix(argocd-sso)` follow-up — shopping-cart-infra Keycloak LDAP federation was still keyed on `uid`, which kept Argo CD SSO logins on the wrong identifier. Patched `identity/keycloak/realm-shopping-cart.json` to use `mail` as the LDAP username attribute, documented the failure in `docs/issues/2026-05-12-keycloak-ldap-login-username-mail.md`, and opened shopping-cart-infra PR #47 (`https://github.com/wilddog64/shopping-cart-infra/pull/47`). Commit: `eed0b01`. Copilot review was requested with a PR comment.
- **NEW FINDING:** Live Keycloak login still fails with `User returned from LDAP has null username!` when `usernameLDAPAttribute=mail`. The live LDAP entry does contain `mail: admin@shopping-cart.local`, but Keycloak’s import path returns `attributes from LDAP: {}` and aborts with `invalid_user_credentials`. Issue doc: `docs/issues/2026-05-12-keycloak-ldap-null-username-mail-mapping.md`.
- **NEW FINDING:** Live LDAP bind still failed because Keycloak was configured with `LDAP_BIND_DN: cn=admin,dc=shopping-cart,dc=local` while the live LDAP admin secret exposes `ldap-admin`. I patched the shopping-cart-infra source of truth to use `cn=ldap-admin,dc=shopping-cart,dc=local`, opened PR #48 (`https://github.com/wilddog64/shopping-cart-infra/pull/48`), and resolved the Copilot review thread after adding `LDAP_ADMIN_USERNAME: "ldap-admin"` to `identity/ldap/deployment.yaml`. Issue doc: `docs/issues/2026-05-12-shopping-cart-infra-keycloak-ldap-bind-dn-mismatch.md`.
- **NEW FINDING:** The shopping-cart-infra Keycloak startup import path is still feeding `identity/keycloak/realm-shopping-cart.json` to `kc.sh import` without rendering the bind credential first, so the live LDAP provider can import stale secret values unless the initContainer render step substitutes them. I created bugfix branch `bug/keycloak-realm-import-render-placeholders` and documented it in `docs/issues/2026-05-13-keycloak-realm-import-placeholders-not-rendered.md`.
- **COMPLETE:** shopping-cart-infra PR #51 addressed Copilot review feedback by wiring `keycloak-client-secrets` into the Keycloak initContainer env and aligning the bug doc to the actual mounted `identity/keycloak/realm-shopping-cart.json` import source. Review threads were resolved with `gh api graphql`. Commit: `1ffaf46`.
- **COMPLETE:** shopping-cart-infra PR #52 Copilot review feedback was addressed by keeping `identity/keycloak/realm-shopping-cart.json` as the single Keycloak import source, adding `KC_DB_USERNAME` to the generated `keycloak-config`, removing the stale `identity/keycloak/configmap.yaml`, and importing the realm with `--db=postgres --override=true`. Review threads were resolved with `gh api graphql`. Commit: `17e965c`.
- **COMPLETE:** shopping-cart-infra `main` was merged into `shopping-cart-infra-v0.4.0` without rewriting history; the remote branch now points at merge commit `9d1778c`, and `enforce_admins` was restored on `main` (`true`).
- **NEW FINDING:** Live Keycloak still re-ran the `shopping-cart` realm import on startup and hit `Key (name)=(shopping-cart) already exists`. The updated fix removes the boot-time realm import from `shopping-cart-infra` entirely and lets the live `acg-up` reconciliation step own realm sync after Keycloak becomes healthy. Issue docs: `docs/issues/2026-05-13-keycloak-realm-import-should-skip-existing-shopping-cart-realm.md` and `docs/issues/2026-05-14-keycloak-realm-sync-moved-out-of-startup.md`.
- **COMPLETE:** shopping-cart-infra v0.4.0 release note / PR follow-up — added a short README release-notes bullet linking the realm-import idempotency bug doc, tagged the branch tip as `v0.4.0`, opened PR #53 (`https://github.com/wilddog64/shopping-cart-infra/pull/53`), and requested Copilot review via `gh api`.
- **COMPLETE:** shopping-cart-infra v0.4.0 release material refresh — consolidated the README release-note entry into the existing `### Release Notes` section in CI/CD Integration and removed the duplicate top-level section; PR #54 (`https://github.com/wilddog64/shopping-cart-infra/pull/54`) still carries the release-note material. Release note commit: `2e97ec6` (`docs: add v0.4.0 release note`); release tag `v0.4.0` now points at `674b7b1`.
- **COMPLETE:** shopping-cart-infra `v0.4.0` tag correction — the tag had pointed at `128ab3f` before the Keycloak fix; it now points at `674b7b1` (`docs: consolidate v0.4.0 release notes`), which keeps the fix in the release tag.
- **FOLLOW-UP:** PR #53 YAML lint initially failed because `identity/keycloak/deployment.yaml` had one overlong `sed` line in the realm-import render block. I wrapped the command, pushed commit `272f5d2`, and left the PR to rerun CI.
- **FOLLOW-UP:** Copilot then flagged the fresh-DB precheck path. The fix now guards the `realm` lookup with `to_regclass('public.realm')` before querying for `shopping-cart`, so a brand-new Keycloak database falls through to the import without a misleading `relation "realm" does not exist` error.
- **COMPLETE:** shopping-cart-infra main branch protection admin enforcement restored on request; verified `enforce_admins=true`.
- **NEW FINDING:** Copilot then flagged the LDAP username mapping itself. I created shopping-cart-infra bugfix branch `fix/keycloak-ldap-username-attribute-uid`, changed `LDAP_USERNAME_ATTRIBUTE` / `usernameLDAPAttribute` from `mail` to `uid`, and documented it in `docs/issues/2026-05-13-keycloak-ldap-username-attribute-uid.md`.
- **COMPLETE:** shopping-cart-infra PR #49 merged and both `main` + `shopping-cart-infra-v0.4.0` were synced to merge commit `1fb43a4` (`Merge remote-tracking branch 'origin/main'`). `main` branch protection admin enforcement was restored (`enforce_admins=true`).
- **NOTE:** shopping-cart-infra `main` and `shopping-cart-infra-v0.4.0` are already aligned at `1fb43a4` (`rev-list --left-right --count origin/shopping-cart-infra-v0.4.0...origin/main` returned `0 0`), so the requested pull-down/merge was a no-op. Branch protection on `main` is already enabled (`enforce_admins=true`).
- **NOTE:** shopping-cart-infra `shopping-cart-v0.3.0` already contains the merged Keycloak realm startup import fix (`0dd3b55`) and its prerequisite LDAP root-DN revert (`5da2560`). `main` has no additional commits to merge back into that branch, so the requested main→v0.3.0 sync is a no-op.
- **COMPLETE:** Added identity troubleshooting helpers for live pod inspection: `bin/vault-exec`, `bin/ldap-search`, `bin/keycloak-logs`, and `bin/ldap-logs`. Shared wrapper logic lives in `scripts/lib/identity_tools.sh`, and the new how-to doc is `docs/howto/identity-troubleshooting.md`.
- **IN PROGRESS:** `acg-up` now syncs `known_hosts` for the managed AWS hosts (`ubuntu`, `ubuntu-tunnel`, `ubuntu-1`, `ubuntu-2`) so stale public IP entries are pruned and current ones are retained. Issue doc: `docs/issues/2026-05-13-acg-up-maintain-known-hosts-for-managed-aws-ips.md`.
- **COMPLETE:** shopping-cart-infra Copilot review follow-up — clarified the issue-doc heading for PR #47, committed as `ea491dd`, pushed to `shopping-cart-infra-v0.4.0`, and resolved the Copilot review thread via `gh api graphql`.
- **COMPLETE:** shopping-cart-infra branch protection admin override — disabled `enforce_admins` on `main` so repo admins can bypass branch protection when needed. Also confirmed the `identity/config/realm-shopping-cart.json` change is realm-wide shopping-cart identity config, not Argo CD-only, even though the `argocd` client was the visible login path.
- **Pending:** `refactor(plugins)` — spec at `docs/plans/v1.4.3-refactor-k3s-remote-plugin.md`; assign to Codex after SSO wiring is verified. Updated to include `k3s-aws.sh` and `k3s-gcp.sh` source-line changes (both source `shopping_cart.sh` directly — must rename to `k3s_remote.sh`).
- **NEW FINDING:** `make up` still occasionally times out waiting for Keycloak `deployment/keycloak` to become `Available`. Live inspection showed the pod stuck in `ContainerCreating` at 87s with `0/1` readiness, while the Keycloak manifest uses a 30s startup delay and `failureThreshold: 30` (300s). Issue doc: `docs/issues/2026-05-13-keycloak-intermittent-startup-never-reaches-available.md`.
- **FIX IN PROGRESS:** The Keycloak readiness gate now polls for `deployment/keycloak` to exist before waiting for `Available`, so an ArgoCD sync delay no longer fails `make up` immediately with `NotFound`. New issue doc: `docs/issues/2026-05-13-keycloak-deployment-notfound-should-poll-for-creation.md`.
- **NEW FINDING:** The live Argo CD ConfigMap is still being rendered with `url: https://argocd.dev.local.me`, which causes `/auth/login` to reject the canonical browser flow even after cluster rebuilds. `scripts/etc/argocd/vars.sh` now defaults `ARGOCD_VIRTUALSERVICE_HOST` to `argocd.shopping-cart.local`. Issue doc: `docs/issues/2026-05-13-argocd-virtualservice-host-default-still-dev-local-me.md`.
- **COMPLETE:** `fix(acg-up)` — `bin/acg-up` now reads the current shopping-cart-infra realm import artifact at `identity/keycloak/realm-shopping-cart.json` instead of the deleted `identity/config/realm-shopping-cart.json`, and the Keycloak fixture test was updated to match. Issue doc: `docs/issues/2026-05-13-acg-up-uses-stale-realm-json-path-after-shopping-cart-infra-layout-change.md`. Commit: `c0566cb4`.
- **COMPLETE:** shopping-cart-infra `shopping-cart-infra-v0.5.0` branch cut from the merged `main` tip (`4a6c14f`), and `main` branch protection was restored (`enforce_admins=true`).
- **COMPLETE:** shopping-cart-infra `shopping-cart-infra-v0.5.0` was cleaned up and re-created from the current `main` tip (`4a6c14f`) after the release sync, keeping the release line aligned with the merged `main` commit.
- **COMPLETE:** shopping-cart-infra `v0.4.0` release object was published on the verified tag `v0.4.0` with notes describing the Argo CD PostSync live-reconcile fix. Release URL: `https://github.com/wilddog64/shopping-cart-infra/releases/tag/v0.4.0`.
- **Next:** `feat(providers)` — spec at `docs/plans/v1.4.3-service-mesh-lb-k3s-remote.md`; depends on refactor spec.
- **Next:** `feat(tunnel)` — spec at `docs/plans/v1.4.3-chisel-tunnel.md`; depends on refactor spec.
- Preserve subtree discipline: `scripts/lib/foundation/` and `scripts/lib/acg/` edits upstream first.

## Notes
- The two baseline failures in `scripts/tests/plugins/argocd.bats` remain unresolved (pre-existing, unrelated to v1.4.2 changes).
- Historical branch pruning recommendation recorded in `docs/issues/2026-05-13-k3d-manager-historical-branches-prune-recommendation.md`: `docs/next-improvements` and `k3d-manager-v1.1.0` through `k3d-manager-v1.4.4` are not ancestors of `main` or `k3d-manager-v1.4.5`; keep them for auditability unless the team explicitly decides to prune refs.
- Retro (v1.4.4): `docs/retro/2026-05-08-v1.4.4-retrospective.md`
- Retro (v1.4.3): `docs/retro/2026-05-08-v1.4.3-retrospective.md`
- Retro (v1.4.2): `docs/retro/2026-05-07-v1.4.2-retrospective.md`
