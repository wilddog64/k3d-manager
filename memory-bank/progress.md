# Progress — k3d-manager

## Status
v1.14.0 RELEASED 2026-07-12 · v1.15.0 RELEASED 2026-07-14 · **v1.16.0 active — Istio ambient mesh**.

> Full per-item detail (gate dumps, live-verify logs) archived 2026-07-19 → `memory-bank/archive/progress-v1.16.0-detail-thru-2026-07-19.md`. Older: `progress-v1.6.5-v1.15.0.md`, `-v1.6.x-v1.6.4.md`, `-v1.4.2-v1.4.8.md`.

## v1.16.0 — DONE + CLAUDE-VERIFIED on `origin/k3d-manager-v1.16.0` (all one file unless noted)
- [x] `1cc55252` — pin hub k3d image `v1.32.0-k3s1` (istioctl precheck floor). Live-verified: fresh hub reports v1.32.0+k3s1, 0×IST0142.
- [x] `e118f664` — add `shopping-cart` AppProject + `_argocd_deploy_appproject` loop (2 files). Live e2e: appset ErrorOccurred cleared, `ubuntu-k3s-data-layer` generated.
- [x] `64168cc7` — permit `secrets` namespace in the `shopping-cart` AppProject for `vault-bridge`. One file; +8 lines. **CLAUDE-VERIFIED = PASS**: SHA on origin, exact message, diff byte-identical to spec, yq clean, `secrets`→4 / `shopping-cart-`→12, bats 15/15, `_agent_audit` 0; memory-bank separate commit `17ca8380`. **FRESH-REBUILD e2e PASS (2026-07-19):** `make down` (DOWN_EXIT=0) → `make up` (UP_EXIT=0) on new sandbox `739527292320`; committed template (no manual patch) → `secrets/Service/vault-bridge` Synced, `ubuntu-k3s-data-layer` Synced/Healthy, 7 data-layer + full app tier pods 1/1.
- [x] `5be42ae4` — pin `k3sup install` and `k3sup join` to `--k3s-version "${K3S_VERSION:-v1.32.0+k3s1}"` so k3s-aws provisioning survives the `update.k3s.io` channel-resolver outage. One file `scripts/plugins/shopping_cart.sh`; +3/−1. Gates recorded on this machine: `grep -c 'k3s-version "${K3S_VERSION:-v1.32.0+k3s1}"' scripts/plugins/shopping_cart.sh` → `2`; `shellcheck -S warning scripts/plugins/shopping_cart.sh` → 0 warnings; `bats scripts/tests/plugins/shopping_cart.bats` → 11/11 passing; `./scripts/k3d-manager _agent_audit` exit 0; `git diff --stat` before commit → exactly 1 file changed. Pushed to `origin/k3d-manager-v1.16.0`; PR URL not created per repo rule.
- [x] `bca7d59a` — default `K3S_AMBIENT_MESH=true` in the k3s-aws up flow so ambient installs Cilium, while forcing `false` under SSM. One file `scripts/lib/providers/k3s-aws.sh`; +6/−1. Gates recorded on this machine: `grep -c 'K3S_AMBIENT_MESH:-true' scripts/lib/providers/k3s-aws.sh` → `1`; `grep -c 'K3S_AMBIENT_MESH="${_ambient_mesh}"' scripts/lib/providers/k3s-aws.sh` → `1`; `shellcheck -S warning scripts/lib/providers/k3s-aws.sh` → 0 warnings; `bats scripts/tests/lib/k3s_aws_provider.bats` → `5 tests, 0 failures`; `./scripts/k3d-manager _agent_audit` exit 0; `git show --stat bca7d59a` → exactly 1 file changed. Pushed to `origin/k3d-manager-v1.16.0`; Claude live rebuild/re-verify still pending.
- [x] `ce4d83f0` — point ambient istio-cni at Cilium default CNI conf/bin dirs (`cniConfDir: /etc/cni/net.d`, `cniBinDir: /opt/cni/bin`). One file `scripts/etc/argocd/applicationsets/istio-ambient.yaml`; +2/−2. **CLAUDE-VERIFIED = PASS (2026-07-19):** SHA on origin, exact message, diff byte-identical to spec, YAML parses (ruby), `rancher/k3s`→0, `/etc/cni/net.d`+`/opt/cni/bin`→1 each, `values: |` block-scalar preserved (4), memory-bank separate commit `914fece6`. **Live re-verify (2026-07-19) = PARTIAL:** fix rendered from git (DaemonSet mounts `/etc/cni/net.d` + `/opt/cni/bin`), ztunnel now 1/1 ×3, istiod 1/1 — but **istio-cni still 0/1** because the `make up` cluster runs FLANNEL, not Cilium (no cilium pods; base conflist `10-flannel.conflist` at the rancher path; `/etc/cni/net.d` empty). NEW ROOT CAUSE: `make up CLUSTER_PROVIDER=k3s-aws` never sets `K3S_AMBIENT_MESH=true`, so `_ambient_install_cilium` is skipped. See OPEN blocker 1 in activeContext — owner decision on direction (A/B/C) required before a Codex spec.
- [x] `896d08ad` — derive appset-loop `APP_CLUSTER_NAME` from the resolved active cluster, not hardcoded hostinger. One file `scripts/plugins/argocd.sh`; moved `_active_app_cluster` resolution ahead of the default and changed the fallback to `ubuntu-k3s` via `APP_CLUSTER_NAME="${APP_CLUSTER_NAME:-${_active_app_cluster:-ubuntu-k3s}}"`. Gates: `grep -c 'APP_CLUSTER_NAME:-ubuntu-hostinger'` → 0; `grep -c 'APP_CLUSTER_NAME:-${_active_app_cluster:-ubuntu-k3s}'` → 1; `shellcheck -S warning scripts/plugins/argocd.sh` baseline 0 and after 0; `bats scripts/tests/plugins/argocd.bats` 15/15; `./scripts/k3d-manager _agent_audit` exit 0. Pushed to `origin/k3d-manager-v1.16.0`.
- [x] `ce4d83f0` — point ambient istio-cni at Cilium default CNI conf/bin dirs. One file `scripts/etc/argocd/applicationsets/istio-ambient.yaml`; changed `cniConfDir` `/var/lib/rancher/k3s/agent/etc/cni/net.d` → `/etc/cni/net.d` and `cniBinDir` `/var/lib/rancher/k3s/data/current/bin` → `/opt/cni/bin`, exactly per `docs/bugs/2026-07-17-ambient-istio-cni-conf-bin-dir-mismatch.md`. Validation on this machine: YAML parse OK via `ruby -ryaml`, `bats scripts/tests` 6/6, `./scripts/k3d-manager _agent_audit` exit 0. Pushed to `origin/k3d-manager-v1.16.0`; Claude live re-sync/dataplane verification still pending.
- [x] `1af15217` — right-size istiod (100m/512Mi) + ztunnel (100m/256Mi) for 2-CPU hostinger node. Code PASS; **istiod live-PROVEN 2026-07-19** (istiod 1/1 Running on ubuntu-k3s spoke after dest-validation cleared). ztunnel sizing untested (scheduled fine, but blocked on istio-cni conf/bin dir mismatch — separate spec `2026-07-17-...`).
- [x] `39788cb1` — cluster-up Step 10b: stop applying deleted data-layer manifest; key wait/sync to `${APP_CLUSTER_NAME}-data-layer`. Live: make up exit 0, abort gone.
- [x] `18b92cd2` — key `services-git` Application names by cluster (`{{.name}}-{{.path.basename}}`) + `preserveResourcesOnDeletion`. Live: 12 apps 6-per-cluster, ubuntu-k3s app tier up.
- [x] `2966a3b9` — fail loudly on unset appset envsubst vars + default `AMBIENT_ISTIO_VERSION` in bootstrap scope (3 files). Verified: render yields `targetRevision: 1.24.2`.
- [x] `89c2efd6` — app-cve-scan dispatch via wget + wget bats coverage (2 files). Verified incl. mutation A/B; network-isolation gate 0.
- [x] `babb3c80` — app-cve-scan curl→BusyBox wget. `5fcc3f89` — argocd-cve-scan fail loudly on missing infra secret (Phase 1).
- [x] Test-integrity batch `3b281a70`→`db1ed1ce`→`6f6212dd` (5 files); trivy dashboard split `4c89dabb`; argocd BATS regression fix `e3a75f1f`; ArgoCD-managed Grafana dashboards `0e8b49c3`; status not-deployed-vs-fail `3938626e`; webhook utcnow `a549a37a`; multi-app-cluster Phase 1 `2fa78e35` + Phase 3 `f03df202`; ambient Phases 1–2 (`125b797e`, `c5a730c1`, `7e88a9d5`, `bcc87f1c`) — all verified, ambient dataplane capture PASS on sandbox.

## PENDING
- [ ] **Claude live re-verify for ambient k3s-aws rebuild — cluster+Cilium UP; NEW bug found + specced (2026-07-19).** `bca7d59a` + `5be42ae4` both VERIFIED PASS; fresh `make down`/`make up CLUSTER_PROVIDER=k3s-aws` **proved both fixes at runtime**: k3sup logged `Using v1.32.0+k3s1 as release` (pin works, no channel call) and `K3S_AMBIENT_MESH=true — flannel disabled; Cilium will be installed`; **Cilium 1.16.5 installed successfully** — live cluster now has 3 nodes Ready (all `v1.32.0+k3s1`), cilium agent 1/1 ×3, cilium-envoy 1/1 ×3, cilium-operator 1/1. **BUT `make up` exited 1 at Step 2** "Waiting for all 3 nodes to be Ready" — ROOT CAUSE (confirmed): `k3s-aws.sh:198` `(( node_attempts++ ))` post-increment from 0 returns exit 1 under `bin/cluster-up`'s `set -euo pipefail`, killing the script on the FIRST loop iteration; the Cilium path exposes it because freshly-joined agents are slow to become Ready so the loop body IS entered (flannel agents were Ready on first check → bug masked). Same latent twin at `k3s-aws.sh:86` (SSM path). **Spec `docs/bugs/2026-07-19-k3s-aws-node-wait-set-e-post-increment.md` (commit `8a97d77e`) assigned to Codex** — replace both `(( var++ ))` with `var=$(( var + 1 ))`. **Unblock now:** re-running `make up` against the already-Ready cluster skips the loop body (until-cond true on entry → bug dormant) → flows past Step 2 to hub/ArgoCD/istio; running that now. **After up:** node `/etc/cni/net.d` has a Cilium conflist → `istio-cni-node` `1/1` ×3 + ztunnel `1/1` → ambient dataplane capture, then re-check the two broken app services (Frontend + Product images). **AMBIENT STACK VERIFIED GREEN (2026-07-19) before sandbox expiry:** resume `make up` completed all 14 steps (wrapper-rc=0, no failure trap); `istio-cni-node` **1/1 ×3** (blocker cleared), `istiod` 1/1, `ztunnel` 1/1 ×3, zero `no networks found` in istio-cni logs, Cilium 1.16.5 healthy, app tier (frontend, product-catalog, basket, order, payment) all 1/1. **ONLY the ambient dataplane capture remains** — task `be3dh3gsc` produced NO results (`ambient-capture.log` shows only `dial tcp 54.218.126.61:6443: i/o timeout` — the ACG sandbox TTL expired mid-capture, killing the cluster). **2026-07-20 state:** sandbox dead (`aws sts` → `InvalidClientTokenId`); `acg_get_credentials` re-run exits 0 but re-scrapes STALE creds from the expired page (known trap, `reference_acg_stale_tab_invalid_creds`) → **needs a MANUAL Start-Sandbox click in the :9222 CDP Chrome window** before any rebuild. Spec `8a97d77e` still UNIMPLEMENTED on origin (`grep -cE '\(\( (node_)?attempts\+\+ \)\)'` → `2`), so the next cold rebuild WILL abort at "Waiting for all 3 nodes to be Ready" — either land the fix via Codex first, or re-run `make up` once 3 nodes are Ready (proven workaround).
- [ ] **HANDED OFF TO CODEX 2026-07-20 — k3s-aws wait-loop `set -e` fix** — spec `docs/bugs/2026-07-19-k3s-aws-node-wait-set-e-post-increment.md` (spec commit `8a97d77e`), branch `k3d-manager-v1.16.0`. Replace both `(( attempts++ ))` (~line 86) and `(( node_attempts++ ))` (~line 198) in `scripts/lib/providers/k3s-aws.sh` with `var=$(( var + 1 ))`. Baselines recorded on this machine at handoff time: disappearance gate `grep -cE '\(\( (node_)?attempts\+\+ \)\)' scripts/lib/providers/k3s-aws.sh` → **`2`** (must become `0`); `shellcheck -S warning scripts/lib/providers/k3s-aws.sh` → **0 warnings** (must stay 0). Exact commit message: `fix(k3s-aws): make node-ready + SSM-register wait loops set -e safe (assignment not (( var++ )))`. **Claude verifies after push** (SHA on `origin/k3d-manager-v1.16.0`, diff byte-identical to spec, `git show --stat` = exactly 1 file, bats, `_agent_audit`), then runs the cold-rebuild live proof once the ACG sandbox is manually restarted.
- [ ] **app-cve-scan report-repository registry-prefix mismatch** — `docs/bugs/2026-07-18-app-cve-scan-report-repository-registry-prefix-mismatch.md`. Unassigned.
- [ ] **`app_cve_scan.bats` stubs curl not wget** (post-`89c2efd6` this is covered by the wget harness — but the standalone `docs/bugs/2026-07-18-app-cve-scan-bats-stubs-curl-not-wget.md` is SUPERSEDED; do not implement separately).
- [ ] **BLOCKED — Hub `environment=infra` registration** — `docs/bugs/2026-07-18-hub-infra-registration-blocked-platform-helm-selfheal.md`. DO NOT EXECUTE (platform-helm selfHeal → 2nd argo-cd release + downgrade). Owner decision (options A–D).
- [ ] **Triage — hub `argocd` Helm release `failed`** (rev 3, 2026-06-29). Blocks infra-registration options B/C.
- [ ] **Makefile stale ACG sandbox URL default** — `docs/bugs/2026-07-19-makefile-stale-acg-sandbox-url-default.md`. Unassigned.
- [ ] **app-cluster Vault portability** — `docs/bugs/2026-07-07-app-cluster-vault-portability.md` (hub-vault-profile global-file leak; `_hub_vault_profile_context()` returns empty on k3s-aws path). Unassigned.

## Releases
- [x] v1.15.0 (2026-07-14): PR #105 → `main` `14cdaea6`. Post-merge tag/release/retro deferred.
- [x] v1.14.0 (2026-07-12): PR #104 → `main` `5c412e15`. Tag + release + retro done.
