# Progress — k3d-manager

## Status (v1.14.0 RELEASED 2026-07-12; v1.15.0 RELEASED 2026-07-14; v1.16.0 active branch — Istio ambient mesh)

- **[x] TEST INTEGRITY BATCH — CLAUDE-VERIFIED = PASS (Claude, 2026-07-18):** all three SHAs confirmed on `origin/k3d-manager-v1.16.0`, correct order, exactly 5 files (1+2+2), diffs byte-identical to spec, `check-stale-test-refs.sh` mode `100755`. Gates re-run by Claude: `provider_contract` 52/52, `appset_envsubst_coverage` 2/2, `stale_test_refs` 4/4, `argocd_metrics_servicemonitor` 15/15, `grafana_dashboard_appsets` 5/5, shellcheck clean on both shell files, `_agent_audit` exit 0. Phase 2 verified to emit `targetRevision: 1.24.2` on the real appset with Go templates preserved.
- **[ ] BLOCKER — appset envsubst empty-var substitution (regression from `db1ed1ce`):** unset vars now substitute to empty string instead of passing through as a literal; `AMBIENT_ISTIO_VERSION` is defaulted only inside `deploy_istio_ambient()` and is unset on the bootstrap path → empty `targetRevision` on a Helm chart source = unpinned chart. Spec filed: `docs/bugs/2026-07-18-appset-envsubst-empty-var-substitution.md`. **Not yet handed to Codex.**
- **[ ] Live hub e2e (Claude-only) — BLOCKED** on the above. Running the bootstrap now would apply an unpinned istio chart to `ubuntu-hostinger`. Hub currently pre-fix: `istio-ambient` targetRevision literal, 3 `istio-*-ubuntu-hostinger` apps `Unknown`.
- **[x] TEST INTEGRITY BATCH — DONE, PUSHED (Codex, 2026-07-18):** `docs/bugs/2026-07-18-test-integrity-batch.md` was implemented exactly as written in **3 ordered commits on `origin/k3d-manager-v1.16.0`**: **`3b281a70`** (`test(provider): retarget data-layer assertion after per-cluster rename`), **`db1ed1ce`** (`fix(argocd): derive appset envsubst vars per file to stop version drift`), and **`6f6212dd`** (`test(ci): add stale test reference check for moved and renamed strings`). Scope held exactly to the 5 spec files: `scripts/tests/lib/provider_contract.bats`, `scripts/plugins/argocd.sh`, NEW `scripts/tests/plugins/appset_envsubst_coverage.bats`, NEW executable `scripts/check-stale-test-refs.sh`, and NEW `scripts/tests/lib/stale_test_refs.bats`. Phase 1 gates passed (`provider_contract.bats` `1..52`, old literal count `1`, new literal count `1`) before commit `3b281a70`; Phase 2 gates passed before commit `db1ed1ce` (`bash -n` + `shellcheck` clean on `argocd.sh`, `appset_envsubst_coverage.bats` `1..2`, hardcoded envsubst count `0`, plus the full named appset-related suite set unchanged including `argocd_metrics_servicemonitor.bats` `1..15` and `grafana_dashboard_appsets.bats` `1..5`); Phase 3 gates passed before commit `6f6212dd` (`bash -n` + `shellcheck` clean on `check-stale-test-refs.sh`, executable bit set, `stale_test_refs.bats` `1..4`). Final gates after the third push all passed: `provider_contract.bats` `1..52`, `appset_envsubst_coverage.bats` `1..2`, `stale_test_refs.bats` `1..4`, `argocd_metrics_servicemonitor.bats` `1..15`, `grafana_dashboard_appsets.bats` `1..5`, `shellcheck -S warning scripts/plugins/argocd.sh scripts/check-stale-test-refs.sh` clean, and `./scripts/k3d-manager _agent_audit` exit 0. PR URL: none yet.
- **[ ] Stale test reference check — SPEC WRITTEN + PRE-VALIDATED (Claude, 2026-07-18):** `docs/bugs/2026-07-18-stale-test-reference-check.md`. Makes MECHANICAL the rule that has now been violated twice and nearly a third time (`4c89dabb` trivy split, `f03df202` data-layer rename, envsubst spec near-miss): a spec moves/renames a string in `scripts/etc/` and a BATS suite asserting the old string is never updated because the gate list was written from memory instead of a grep. New standalone `scripts/check-stale-test-refs.sh` diffs a commit range against `scripts/tests/` and reports suites still asserting removed strings. **Claude built and measured it against real history BEFORE speccing:** `f03df202` → flagged, correctly names `provider_contract.bats`; `4c89dabb` → flagged 9 hits, correctly names `argocd_metrics_servicemonitor.bats`; `e128e8b3` was a real FALSE POSITIVE (prefix line replaced by a longer line in the same file) and was fixed by a same-file guard; a 20-commit sweep then showed only the two genuine regressions plus one sweep-methodology artifact (`5c412e15` — greps today's tests against an old diff; those tests were updated in that same commit). **Ships ADVISORY / standalone — deliberately NOT wired into githooks or CI in this task**, so FP behaviour can be observed on real commits before it can block anyone. **Stated limitation:** catches whole-line renames and quoted-value moves only; will NOT catch an unquoted-substring assertion, and only scans `scripts/etc/`. Reduces the failure class, does not eliminate it — spec grep discipline still required.
- **[ ] provider_contract test 17 stale assertion — SPEC WRITTEN + PRE-VALIDATED (Claude, 2026-07-18):** `docs/bugs/2026-07-18-provider-contract-stale-data-layer-assertion.md`. `scripts/tests/lib/provider_contract.bats:495` still asserts `name: data-layer`, but `f03df202` (Phase 3) renamed it to `name: '{{.name}}-data-layer'` to fix the multi-cluster collision. **Worktree bisect 2026-07-18: `1..52` all ok at `f03df202^`, `51/52` at `f03df202`.** Fix retargets line 495 + adds a disappearance guard (`!= *"name: data-layer"*`). Claude pre-validated BOTH directions before handoff: patched → `52/52`; mutation test (patch applied AND `data-git.yaml` reverted to the bare name) → test 17 FAILS, so the new assertion is real not vacuous. Scaffolding reverted, tree clean. `grep -rn 'name: data-layer' scripts/tests/` returns exactly ONE hit, so scope is a single line in a single file. **THIRD regression on this branch from the SAME root cause** (`4c89dabb` trivy split → `argocd_metrics_servicemonitor.bats`; `f03df202` rename → this): a spec moved/renamed content and its gate list was written from the files that came to mind instead of derived from a grep for consumers. The Phase 3 spec's gates were grep+yq+_agent_audit on the YAML with NO bats suite at all. Standing rule restated in the spec: a rename/move spec MUST derive its gate list from `grep -rln '<changed string>' scripts/tests/` and list those suites individually.
- **[ ] Appset envsubst var drift — SPEC WRITTEN + PRE-VALIDATED (Claude, 2026-07-18):** `docs/bugs/2026-07-18-appset-envsubst-var-drift.md`. `_argocd_deploy_applicationsets` (`scripts/plugins/argocd.sh:1196`) applies EVERY appset through a hardcoded three-var envsubst list (`$ARGOCD_NAMESPACE $K3D_MANAGER_BRANCH $APP_CLUSTER_NAME`), but `istio-ambient.yaml` needs a fourth (`$AMBIENT_ISTIO_VERSION`) — so the bootstrap path ships a literal `${AMBIENT_ISTIO_VERSION}` as `targetRevision` and `istio-base/cni/istiod-ubuntu-hostinger` sit `Unknown` with `ComparisonError: improper constraint`. NOT stale state: `istio_ambient.sh:32` substitutes correctly on its own targeted apply, and the generic loop overwrites it, so the bug reproduces on every bootstrap. Fix derives the allow-list per file (keeps Go templates `{{...}}` safe) + adds `appset_envsubst_coverage.bats`. **Claude pre-validated the spec before handoff (applying this session's own lesson):** Change 1 verified to emit `targetRevision: 1.24.2` with `{{ .name }}` intact; the new BATS test 1 verified to FAIL on unfixed code and PASS on fixed code (not vacuous); `bash -n` + shellcheck clean under the patch; the affected-suite list was DERIVED from `grep -rln ... scripts/tests/` (8 suites, not the 2 originally listed) and all pass. Spec is explicit that new test 2 does NOT catch this bug (AMBIENT_ISTIO_VERSION *is* exported) and only guards a never-exported var. **Second pre-existing failure found and pinned in the spec:** `scripts/tests/lib/provider_contract.bats` test 17 (`_hostinger_reapply_gitops_applicationsets`) fails on a CLEAN tree at `4b3b6381` — expected result is exactly `51/52`, and Codex is told not to fix or edit it. Scaffolding used for validation was fully reverted (tree clean).
- **[x] BATS regression from the trivy split — DONE, PUSHED (Codex, 2026-07-18):** `docs/bugs/2026-07-18-argocd-dashboard-bats-regression.md` is now on `origin/k3d-manager-v1.16.0` at **`e3a75f1f`** (`test(observability): retarget argocd dashboard trivy assertions after split`). **`4c89dabb` had shipped a test regression that Claude's review missed.** `scripts/tests/plugins/argocd_metrics_servicemonitor.bats` tests 12/13 were still asserting the moved trivy panels against the HUB dashboard, and test 14 had become vacuous because it grepped the hub for `\\n` after the banner moved away. Scope held exactly to the single target file only: added `TRIVY_DASH`, retargeted tests 12/13/14 to the split dashboard, added the required hub-side disappearance assertions, and did not touch either dashboard YAML. Local gates passed: `bats scripts/tests/plugins/argocd_metrics_servicemonitor.bats` → `1..15` all ok; `bats scripts/tests/plugins/trivy_operator_observability.bats` → `1..6` all ok unchanged; `grep -c 'TRIVY_DASH' scripts/tests/plugins/argocd_metrics_servicemonitor.bats` → `16`; `./scripts/k3d-manager _agent_audit` exit 0; `git show --stat` for the feature commit shows exactly one file changed. **Result:** the stale suite is fully restored to `15/15` without weakening assertions or modifying the correct split dashboards. PR URL: none yet.
- **[x] ArgoCD-managed Grafana dashboards (Phase 1) — DONE, PUSHED (Codex, 2026-07-18):** `docs/bugs/2026-07-18-argocd-managed-grafana-dashboards.md` is now on `origin/k3d-manager-v1.16.0` at **`0e8b49c3`** (`feat(observability): manage Grafana dashboards via ArgoCD ApplicationSets`). Live diagnosis on `ubuntu-k3s` 2026-07-18: the observability stack carries `argocd.argoproj.io/tracking-id` but **no ArgoCD exists on any cluster** (`ubuntu-k3s`, `k3d-k3d-cluster`, `ubuntu-hostinger` all have no `argocd` namespace) **[WRONG — ArgoCD lives in `cicd` namespace, and is alive and healthy on the hub; see the DEPLOYED-AND-VERIFIED entry below]** and `helm list -A` shows **no `acg-kube-prometheus-stack` release** — the stack is orphaned, so the requested `helm upgrade` for the `disableDelete` flicker fix CANNOT run and was not attempted (a `helm upgrade --install` would adopt ~50 ArgoCD-tracked resources and fight the hub on its return). Scope held exactly to the 4 spec files only: NEW `grafana-dashboards-acg.yaml` (clusters generator on `k3d-manager/role: app-cluster` → path `scripts/etc/grafana/dashboards`, so **`ubuntu-hostinger` is covered automatically once rebuilt and registered** — no per-cluster edit), NEW `grafana-dashboards-hub.yaml` (`directory.include: grafana-dashboard-argocd.yaml`, with `recurse: false`, avoiding a file move that would break `argocd.sh:1390` and 4 BATS suites), `observability.sh` +2 additive apply blocks, and NEW `grafana_dashboard_appsets.bats`. Imperative applies stay as the no-ArgoCD bootstrap fallback by design; no existing dashboard apply was removed or modified, `argocd.sh:1390` was untouched, and `grafana-dashboard-argocd.yaml` stayed in place. Phase 1 local gates passed: `bash -n scripts/plugins/observability.sh` clean; `shellcheck -S warning scripts/plugins/observability.sh` clean; `yq eval '.'` parses both new YAMLs; `bats scripts/tests/plugins/grafana_dashboard_appsets.bats` `1..5` pass; `bats scripts/tests/lib/observability.bats` pass; `bats scripts/tests/plugins/trivy_operator_observability.bats` `1..6` pass; `bats scripts/tests/plugins/observability_no_exit_remove.bats` pass; `grep -c '_observability_apply_trivy_dashboard' scripts/plugins/observability.sh` → `2`; `./scripts/k3d-manager _agent_audit` exit 0. The spec gate was amended mid-flight for the known pre-existing regression from `4c89dabb`: `bats scripts/tests/plugins/argocd_metrics_servicemonitor.bats` now expects exactly **`13/15`**, failing ONLY tests **12** and **13**; `0e8b49c3` met that expectation and did not touch the forbidden test file. Phase 2 (remove imperative + retarget BATS) remains deferred to its own spec. **ARGOCD-IS-ALIVE CORRECTION + DASHBOARD APPSETS DEPLOYED & LIVE-VERIFIED (Claude, 2026-07-18):** The "orphaned stack / hub is gone / rebuild the hub" narrative earlier in this item was **WRONG and is retracted**. Root cause of the error: Claude checked for a namespace named `argocd` on all three clusters, found none, and concluded ArgoCD did not exist — but **this repo installs ArgoCD into `cicd`** (`ARGOCD_NAMESPACE:=cicd`, set in the very specs Claude wrote). Actual state: hub `k3d-k3d-cluster` is a 54-day-old local k3d cluster ON THE LAPTOP running **7/7 ArgoCD pods Healthy** in `cicd`, argoproj CRDs since 2026-05-24, 8 pre-existing ApplicationSets and ~24 Applications, and `acg-kube-prometheus-stack` is **Synced/Healthy** — the release Claude reported as nonexistent. The `helm list -A` miss was consistent with an ArgoCD-managed stack and was misread as corroboration. **NO REBUILD OF ANYTHING WAS EVER NEEDED.** Both spokes were already registered AND both already carry `k3d-manager/role: app-cluster` (`cluster-ubuntu-hostinger`, `cluster-ubuntu-k3s`, both `environment: dev`) — the open "confirm hostinger has the label" question is ANSWERED YES. Phase 1 appsets from `0e8b49c3` were applied live to the hub with `ARGOCD_NAMESPACE=cicd K3D_MANAGER_BRANCH=k3d-manager-v1.16.0`: both created, generating **`hub-grafana-dashboards`, `ubuntu-hostinger-grafana-dashboards`, `ubuntu-k3s-grafana-dashboards` — all three Synced/Healthy on first pass**. Live-verified the real objects, not just app status: `trivy-security-dashboard` configmap exists in `monitoring` on BOTH spokes carrying `argocd.argoproj.io/tracking-id` (`ubuntu-k3s` = Claude's earlier manual configmap now ADOPTED rather than duplicated; `ubuntu-hostinger` = created fresh by the sync). Dashboards now survive cluster rebuild. **PRE-EXISTING SEPARATE BUG FOUND:** the live `istio-ambient` ApplicationSet on the hub has an unsubstituted literal `${AMBIENT_ISTIO_VERSION}` in `targetRevision`, so `istio-base/cni/istiod-ubuntu-hostinger` are all `Unknown` with `ComparisonError: improper constraint`. The CODE is correct (`istio_ambient.sh:32` includes it in the envsubst list) — stale cluster state from a 31h-old apply; re-running the deploy should fix it. Also `hub-loki` is `OutOfSync` on `StatefulSet/loki` only. Both unrelated to this work. **PROCESS LESSON: confirm a component's actual namespace from the repo's own config before concluding it is absent — one wrong-namespace query produced a false "it's gone" that propagated through several turns of reasoning and into both memory-bank files as established fact.** PR URL: none yet.
- **[x] Trivy dashboard split — LIVE-VERIFIED by Claude (2026-07-18):** Codex's `4c89dabb` independently reviewed and accepted (SHA on origin, exactly 5 spec files, structural gate `PASS`, `bats 1..6`, shellcheck clean, `_agent_audit` 0). Root cause of "still no data": **nothing had ever been deployed** — the app cluster had no `trivy-security-dashboard` configmap at all, because the new apply only runs inside `deploy_observability_acg`. Claude applied it manually; sidecar logged `Writing /tmp/dashboards/trivy-security.json`, Grafana `/api/search` returned `uid: trivy-security`, and all panels return real data via the app-cluster datasource proxy: **p13 High/Critical = 45** (matches the pre-measured value), **p14 Compliance = 2 rows (4, 3)**, **p18 drilldown = 35 rows**, **p11 job failures = 0**. Stored panel datasources confirmed `uid: prometheus` (the `prometheus-acg` trap was avoided) with gridPos exactly `0/4/4/12/16`. The `disableDelete` flicker fix is **NOT live** — live provider config still lacks `disableDeletion`, and it needs an ArgoCD sync (see the orphaned-stack item above), not a kubectl apply.
- **[x] Trivy dashboard split + k3dm flicker — DONE, PUSHED (Codex, 2026-07-18):** `docs/bugs/2026-07-18-trivy-dashboard-split-and-k3dm-flicker.md` is now on `origin/k3d-manager-v1.16.0` at **`4c89dabb`** (`fix(observability): split trivy panels to app-cluster Grafana + stop dashboard flicker`). Root cause (live-diagnosed): the k3s-aws public Grafana (`:3001`/`grafana.3ai-talk.org`) is the **app-cluster (ubuntu-k3s)** Grafana (`bin/cluster-up:1518` → `svc/acg-kube-prometheus-stack-grafana` ctx ubuntu-k3s), but the trivy panels lived in the hub-only `grafana-dashboard-argocd.yaml` while the data itself was already present on the app-cluster Prometheus. Scope held to the 5 spec files only: NEW `scripts/etc/grafana/dashboards/trivy-security-configmap.yaml` with panels `11,13,14,15,18` only, panel `gridPos.y` remap exactly `11→0, 13→4, 14→4, 15→12, 18→16`, hub `grafana-dashboard-argocd.yaml` retains Loki panel `12` and shifts it `y: 40→36`, `scripts/plugins/observability.sh` now applies the split Trivy dashboard on the app cluster via `_observability_apply_trivy_dashboard`, `scripts/etc/helm/observability/kube-prometheus-stack-acg-values.yaml` adds `grafana.sidecar.dashboards.provider.disableDelete: true`, and `scripts/tests/plugins/trivy_operator_observability.bats` was retargeted line-by-line so moved assertions follow panel `11` to the new dashboard while panel `12` stays asserted on the hub. The updated spec's authoritative structural Python gate was run after the re-pull at origin `8714721b` and printed `PASS`; local gates also passed: YAML parse on both values/dashboard configmaps, JSON-valid both embedded dashboards, required grep smoke checks, `bash -n scripts/plugins/observability.sh`, `shellcheck -S warning scripts/plugins/observability.sh`, `bats scripts/tests/plugins/trivy_operator_observability.bats` (`1..6` pass), and `./scripts/k3d-manager _agent_audit` exit 0. Claude does live verify (NOT Codex). PR URL: none yet.
- **[x] Status health: not-deployed vs failing — DONE, PUSHED; Claude live re-verify pending (Codex, 2026-07-18):** `docs/bugs/2026-07-17-status-health-absent-vs-fail.md` is now on `origin/k3d-manager-v1.16.0` at **`3938626e`** (`fix(status): report not-deployed services as neutral (⚪) instead of failing (❌)`). Scope held exactly to `bin/k3dm-webhook`, `bin/cluster-status`, and `bin/smoke-test-webhook`: added `_kubectl_absent()`; absent Pushgateway / ClusterSecretStore / ExternalSecret CRD / `shopping-cart-data` namespace now emit `ok=None` (`⚪`) instead of `❌`; post-provision, Slack cluster-status, `/api/v1/health`, `bin/cluster-status`, and `bin/smoke-test-webhook` all treat `None` as neutral and only `False` as failing. Reporting-only — NO cluster deploys. Local gates passed: `python3 -c "import ast; ast.parse(open('bin/k3dm-webhook').read())"` exit 0; `bash -n bin/cluster-status` exit 0; `bash -n bin/smoke-test-webhook` exit 0; `shellcheck -S warning bin/cluster-status bin/smoke-test-webhook` clean; `grep -c 'def _kubectl_absent' bin/k3dm-webhook` → `1`; `grep -c 'icon = "✅" if ok else "❌"' bin/k3dm-webhook` → `0`; `grep -c 'if s\[.ok.\] else' bin/cluster-status` → `0`; `./scripts/k3d-manager _agent_audit` exit 0; `bats scripts/tests` passed (`1..6`). **Claude live re-verify DONE & PASS (2026-07-18):** after `make restart-webhook`, `make status CLUSTER_PROVIDER=k3s-aws` → 3× `⚪` (Pushgateway, ClusterSecretStore, Data layer) + `✅ ExternalSecrets 0/0`, no `❌`, no `json.loads` traceback; `CLUSTER_PROVIDER=k3s-hostinger` → all `✅`, zero `⚪` (Pushgateway pod up, CSS Ready=True, ExternalSecrets 18/18, Data layer 4/4); `bin/smoke-test-webhook` `[PASS]` 9 services. Claude review of `3938626e`: 3 files only, all gates pass, Change 4 guard correct. PR URL: none yet.
- **[x] Follow-up (a) "fully deploy ubuntu-k3s" RESOLVED AS BY-DESIGN; multi-app-cluster Phase 1 DONE, PUSHED (Codex, 2026-07-18):** `docs/plans/v1.16.0-multi-app-cluster.md` Phase 1 is now on `origin/k3d-manager-v1.16.0` at **`2fa78e35`** (`feat(argocd): additive app-cluster labeling (opt-in exclusive via K3DM_EXCLUSIVE_APP_CLUSTER)`). Scope held exactly to `scripts/plugins/argocd.sh`, `scripts/lib/providers/k3s-hostinger.sh`, and `scripts/tests/lib/provider_contract.bats`: both label switches are additive by default; `K3DM_EXCLUSIVE_APP_CLUSTER=true` preserves the old strip-other-clusters behavior; BATS now asserts additive default vs exclusive mode for both Hostinger and ArgoCD labeling paths using the existing kubectl-log stub pattern. Local gates passed: `shellcheck -S warning scripts/plugins/argocd.sh scripts/lib/providers/k3s-hostinger.sh` clean; `bash -n` clean on both changed shell files; `bats scripts/tests/lib/provider_contract.bats` passed (`1..52`); `./scripts/k3d-manager _agent_audit` exit 0; `grep -c 'k3d-manager/role- >/dev/null'` = `1` in both shell files; `grep -c 'K3DM_EXCLUSIVE_APP_CLUSTER'` = `1` in both shell files. **Claude code-level re-verify DONE & PASS (2026-07-18):** independently on `origin` — feat commit `2fa78e35` touches only the 3 spec files (memory-bank split into `19177fa5`); commit message exact; both diffs use guarded `elif [[ "${_exclusive}" == "true" ]]` (strip line PRESERVED not deleted — the inverted gate held); re-ran all gates green (shellcheck clean, `bash -n` clean, `bats` `1..52` incl. new tests 11-14 covering additive-default + exclusive both functions, `_agent_audit` exit 0, grep gates 1/1). **CORRECTION (2026-07-18): the "Claude LIVE verify" below was NOT a real e2e — it was hub-side object manipulation only (no live spoke), and calling it "live-verified" was wrong (user caught it). A genuine full e2e later ran via real `make up` on a fresh k3s-aws cluster — see the Phase 3 progress item's "FULL E2E LIVE TEST DONE & PASS" for the authoritative live result.** ~~Claude LIVE verify DONE & PASS (2026-07-18): hub `k3d-k3d-cluster` still has the `cluster-ubuntu-k3s` secret (sandbox expiry killed the workload, not the hub-side ArgoCD cluster secret), so the hub fan-out test ran even though the ubuntu-k3s spoke API is dead (`44.251.39.123` timeout). Labeled `cluster-ubuntu-k3s` `k3d-manager/role=app-cluster` (additive activation) → **`ubuntu-k3s-eso` Application generated in ~5s** while `ubuntu-hostinger-eso` stayed present AND `cluster-ubuntu-hostinger` KEPT its `role=app-cluster` label (additive proven end-to-end on the real ArgoCD ApplicationSet controller, not just bats). eso is collision-free (`{{.name}}-eso`). Cleanup: unlabeled ubuntu-k3s → roles + apps reverted to baseline (only hostinger); the dead-spoke `ubuntu-k3s-eso` got stuck Terminating on `resources-finalizer.argocd.argoproj.io` (ArgoCD couldn't reach the dead cluster to cascade-delete) → force-removed the finalizer; final state = exact baseline, `data-layer` Synced/Healthy on ubuntu-hostinger throughout.~~ **EMPIRICALLY CONFIRMED the Phase 3 blocker (hub-side, later re-confirmed fixed by the real e2e):** with BOTH clusters labeled, the `data-git` ApplicationSet went `ErrorOccurred=True: contains applications with duplicate name: data-layer` — but NON-destructively (all syncPolicies are `prune: false` → `data-layer` never re-homed off hostinger, stayed Synced/Healthy; error auto-cleared on unlabel). So Phase 3 (`data-git` name-collision fix) is a hard prerequisite to PERMANENT two-cluster labeling, but does not endanger hostinger during a labeled-then-unlabeled test. Phases 2 (imperative eso.sh vault-backend CSS for ubuntu-k3s) and 4 (observability-acg matrix) remain DESIGN-ONLY. PR URL: none yet.
- **[x] multi-app-cluster Phase 3 — DONE, PUSHED (Codex, 2026-07-18):** `docs/plans/v1.16.0-multi-app-cluster-phase3.md` is now on `origin/k3d-manager-v1.16.0` at **`f03df202`** (`feat(argocd): per-cluster data-layer Application name (fix data-git multi-cluster collision)`). Scope held exactly to ONE line in `scripts/etc/argocd/applicationsets/data-git.yaml`: `name: data-layer` → `name: '{{.name}}-data-layer'`. `path: data-layer` stayed unchanged, and no other ApplicationSet, `bin/cluster-up`, or cluster state was touched. Local gates passed: `grep -c '^      name: data-layer$' scripts/etc/argocd/applicationsets/data-git.yaml` → `0`; `grep -c "name: '{{.name}}-data-layer'" scripts/etc/argocd/applicationsets/data-git.yaml` → `1`; `grep -c '        path: data-layer' scripts/etc/argocd/applicationsets/data-git.yaml` → `1`; `yq eval '.' scripts/etc/argocd/applicationsets/data-git.yaml >/dev/null` exit 0; `./scripts/k3d-manager _agent_audit` exit 0. **Still out of Codex scope by design:** (1) live migration of hostinger's existing `data-layer` app is CLAUDE's job because of the ArgoCD `resources-finalizer`; (2) `bin/cluster-up` Step 10b (imperative k3s-aws data-layer path) remains DEFERRED to post-Phase-2, and its referenced cross-repo file `shopping-cart-infra/argocd/applications/data-layer.yaml` is still MISSING (pre-existing separate defect). **FULL E2E LIVE TEST DONE & PASS (Claude, 2026-07-18):** ran real `CLUSTER_PROVIDER=k3s-aws make up` (exit 0) — CDP automation restarted an expired ACG sandbox + extracted valid creds (arn `…user/cloud_user`), provisioned a fresh 3-node k3s-aws cluster, tunnel, and registered `ubuntu-k3s` additively so BOTH `cluster-ubuntu-hostinger` AND `cluster-ubuntu-k3s` carry `role=app-cluster`. **Phase 3 fix verified live:** `data-git` ApplicationSet `ErrorOccurred=False :: All applications have been generated successfully` (the pre-Phase-3 `duplicate name: data-layer` collision is GONE); two per-cluster apps generated — `ubuntu-hostinger-data-layer`→`ubuntu-hostinger` and `ubuntu-k3s-data-layer`→`ubuntu-k3s`, BOTH `Synced/Healthy`; no bare `data-layer` app remains; spoke-side real (7 data-layer pods Running on the fresh ubuntu-k3s 3-node cluster). **MIGRATION FINDING (revises the "Claude-only manual migration" assumption):** `make up` re-applies the renamed ApplicationSet automatically, so the old finalizer'd bare `data-layer` app WAS replaced without a manual finalizer-safe procedure — this RECYCLED hostinger's data-tier pods (postgres/redis/rabbitmq/minio now ~6m old, RESTARTS 0) but the `local-path` PVCs (Retain policy) survived (22d old, still `Bound`) → **no data loss, but a brief data-tier disruption on hostinger**, not the zero-recreation outcome the spec promised. For a dev sandbox this is acceptable; if zero-downtime is ever required, the finalizer-safe manual procedure in the Phase 3 spec still applies. PR URL: none yet.
- **[x] CORRECTION (2026-07-18): "stale scrollback" was WRONG.** The 5 failing `make status` lines are **REAL** — they come from `make status` defaulting to **`k3s-aws`** (`ubuntu-k3s`), the v1.16.0 istio-ambient sandbox that has only `secrets` + `shopping-cart-apps` namespaces (no postgres/ESO/pushgateway → checks correctly report absence). Reproduced via `make status CLUSTER_PROVIDER=k3s-aws`. Hostinger (a different, fully-deployed cluster) is green. Use `CLUSTER_PROVIDER=k3s-hostinger` for full-app health. **FIX SPEC written — see the status-health item above.**
- **[x] WEBHOOK PYTHON-DRIFT INCIDENT resolved (Claude, 2026-07-17):** ~~Recurring hostinger `make status` failures were stale scrollback~~ (superseded by the correction above); the python-drift restart fix below is still valid. Hostinger live cluster fully healthy (postgres 1/1, ESO 18/18, CSS Ready=True, Data layer 4/4). `make restart-webhook` exposed a **Homebrew python@3.13→3.14 drift**: installed plist pinned to now-missing `/opt/homebrew/bin/python3.13` → `EX_CONFIG (78)`. Fixed operationally by aligning installed plist to the already-correct repo template's `/opt/homebrew/bin/python3`; webhook back up fresh **PID 38697 on 3.14.6**, all ✅. No code change; no other agent/template pins a versioned python. Cosmetic `datetime.utcnow()` DeprecationWarning on 3.14 remains (`bin/k3dm-webhook:591`).
- **[x] Webhook utcnow deprecation — DONE, VERIFIED LIVE (Codex impl + Claude re-verify, 2026-07-18):** `docs/bugs/2026-07-17-webhook-utcnow-deprecation.md` on `origin/k3d-manager-v1.16.0` at **`a549a37a`** (`fix(webhook): use timezone-aware UTC timestamps (drop deprecated datetime.utcnow)`); memory-bank record push `0062b534`. Scope held to `bin/k3dm-webhook` only: the 10 deprecated `datetime.datetime.utcnow()` call sites → `datetime.datetime.now(datetime.timezone.utc)`, which also fixes the latent naive absolute-epoch offset bug on line 697 (Pushgateway/acg-state timestamp). **Spec-gate caveat (my spec bug, Codex disclosed it):** the `grep -c '<new-form>' → 10` gate was off-by-one because line 285 was already timezone-aware; Codex hit `10` by normalizing line 285 to the equivalent `now(tz=datetime.timezone.utc)` (functionally identical, out-of-scope but disclosed). Lesson saved to auto-memory (`feedback_grep_count_gates_preexisting.md`): use disappearance gates (`old → 0`), not presence-count gates. **Claude live re-verify PASSED (2026-07-18):** positive test under `python3 -W error::DeprecationWarning` — no warning raised AND `now(tz.utc).timestamp()` matches wall-clock `time.time()` to 0.00009s (proves line-697 offset fixed). `make restart-webhook` → clean bootstrap, new **PID 87148 on Python 3.14.6**, launchd `last exit code=(never exited)`, no EX_CONFIG. `/api/v1/health?provider=k3s-hostinger` ×5 → all HTTP 200; every service ✅ (ESO CSS Ready=True, ESO ExternalSecrets 18/18, Data layer 4/4, Pushgateway correctly absent for hostinger); fresh log has **zero** `DeprecationWarning`/`utcnow`.
- **[x] PHASE 1 LIVE SMOKE kubeconfig-perms BUGFIX — IMPLEMENTED + PUSHED + CLAUDE E2E-VERIFIED (Codex impl, Claude verify, 2026-07-17):** `docs/bugs/2026-07-16-ambient-cilium-kubeconfig-perms.md` is on `origin/k3d-manager-v1.16.0` at **`c5a730c1`** (`fix(mesh): _ambient_install_cilium reads user-readable kubeconfig (k3sup non-root SSH)`). Scope held to `scripts/plugins/shopping_cart.sh` only: `_ambient_install_cilium` now sets `local remote_kubeconfig="/home/${ssh_user}/.kube/k3s.yaml"` and replaces all 5 `KUBECONFIG=/etc/rancher/k3s/k3s.yaml` uses with the user-readable copy; `k8sServiceHost=public IP` intentionally unchanged. Local gate passed: `shellcheck -S warning scripts/plugins/shopping_cart.sh` clean. **E2E PASSED (Claude, 2026-07-17):** fresh clean-slate sandbox, `K3S_AMBIENT_MESH=true K3S_AWS_SSM_ENABLED=false CLUSTER_PROVIDER=k3s-aws ./scripts/k3d-manager deploy_cluster --confirm` → exit 0; Cilium 1.16.5 installed with **zero permission-denied**, `Cilium ready` first attempt, all 3 nodes Ready (kubectl-verified: DaemonSet 3/3, 3 `cilium-*` pods Running, no flannel), agents joined at public-IP API. Fix conclusively validated from clean slate. Phase 1 ambient verify gate CLEARED; Phase 2 unblocked.
- **[~] PHASE 2 IMPLEMENTED + PUSHED; live sandbox smoke found Cilium pod-CIDR/VPC-overlap bug; BUGFIX IMPLEMENTED + PUSHED, pending Claude re-verify (Codex impl, Claude smoke, 2026-07-17):** `docs/plans/v1.16.0-phase2-istio-ambient-appset.md` is on `origin/k3d-manager-v1.16.0` at **`7e88a9d5`** (`feat(mesh): Istio ambient via ArgoCD ApplicationSet + deploy_istio_ambient (Phase 2)`). Scope held exactly to the 4 targets: `scripts/etc/argocd/projects/platform.yaml.tmpl` now adds `istio-system` destinations for the 4 named app clusters; NEW `scripts/etc/argocd/applicationsets/istio-ambient.yaml` defines the base/istiod/cni/ztunnel ApplicationSet with per-element `helm.values` block-scalars and the k3s CNI paths; NEW `scripts/plugins/istio_ambient.sh` adds `deploy_istio_ambient`; `scripts/k3d-manager` adds the dispatcher wrapper. Static gates passed: local `python` `yaml.safe_load_all` on both YAMLs, `shellcheck -S warning` clean, `bash -n` clean, `./scripts/k3d-manager deploy_istio_ambient --help` prints usage, `_agent_audit` exit 0. **No live sandbox smoke was run or claimed in the Phase 2 Codex pass.** **SMOKE RUN 2026-07-17 (Claude): Phase 2 code = PASS** (fresh sandbox provisioned, register + platform project 2.1 + `deploy_istio_ambient --confirm` all OK, ApplicationSet synced by ArgoCD, istiod 1/1 Healthy) **but AMBIENT DATAPLANE BLOCKED** by a Phase 1 Cilium bug: default `10.0.0.0/8` pod CIDR overlapped the ACG VPC `10.0.0.0/16`, black-holing node-to-node traffic via `cilium_host` and leaving istio-cni `0/1`, ztunnel `1/3`. That bugfix is now on `origin/k3d-manager-v1.16.0` at **`bcc87f1c`** (`fix(mesh): pin ambient Cilium pod CIDR to avoid AWS VPC 10.0.0.0/8 overlap`): `scripts/plugins/shopping_cart.sh` now adds `_AMBIENT_POD_CIDR="${AMBIENT_POD_CIDR:-10.42.0.0/16}"` and `--set ipam.operator.clusterPoolIPv4PodCIDRList='{${_AMBIENT_POD_CIDR}}'` in the Cilium helm install; local gates passed (`shellcheck`, `bash -n`, `_agent_audit`). **CLAUDE LIVE RE-VERIFY = PASS (Claude, 2026-07-17):** rebuilt a FRESH sandbox (acct `975201825692`, old one expired) and re-provisioned with the fix — `cilium-config` CIDR now `10.42.0.0/16` (was `10.0.0.0/8`); `ip route get` to peer nodes + API ClusterIP `10.43.0.1` = `dev ens5` (NOT `cilium_host` — black-hole gone); `cilium-health` all 3 nodes Host+Endpoint OK (pre-fix agent↔server 100% loss); throwaway pod → `curl -k https://10.43.0.1:443/healthz` = HTTP 401 (API answered — was `i/o timeout` pre-fix). **Cilium pod-CIDR bug `bcc87f1c` CONCLUSIVELY FIXED.** Remaining before Phase 3: (a) Phase 2 ambient deploy on this sandbox (istio-cni 1/1 + ztunnel 3/3 + capture — now unblocked); (b) close the k3s-aws `argocd-manager` SA gap. Also filed: ACG chronic-login root cause — `acg_get_credentials` reuses the :9222 CDP Chrome session, so the fix is a one-time MANUAL Pluralsight login in that profile, NOT repairing `loginWithPage` (which dead-ends on MFA); see activeContext. Sandbox left UP.
- **[x] PHASE 2 AMBIENT DEPLOY — CLAUDE LIVE VERIFIED = PASS + NEW istio-cni CNI-DIR BUG FOUND (Claude, 2026-07-17):** Full Phase 2 ambient deploy on the still-up sandbox (acct `975201825692`). Closed the k3s-aws `argocd-manager` SA gap by hand (`kubectl --context ubuntu-k3s apply -f scripts/etc/argocd-manager.yaml` + 8760h token — k3s-aws has NO `_hostinger_ensure_argocd_manager_sa`, real gap); re-registered on hub with the AWS **public IP** `https://44.251.39.123:6443` (stale secret pointed at `host.k3d.internal:6443`); `deploy_istio_ambient --confirm`; cleared 8h-old "will not retry" stuck syncs via `annotate refresh=hard` + `patch operation`. **istio-base synced Succeeded = definitive hub→AWS-API reachability + RBAC proof.** istiod 1/1 (`10.42.0.16`), ztunnel **3/3**. **NEW BUG: istio-cni 0/1** — the istio-ambient ApplicationSet hardcodes `cni.cniConfDir: /var/lib/rancher/k3s/agent/etc/cni/net.d` + `cni.cniBinDir: /var/lib/rancher/k3s/data/current/bin` (standard k3s), but `_ambient_install_cilium` installs Cilium with DEFAULT paths → Cilium writes `/etc/cni/net.d/05-cilium.conflist` + binary to `/opt/cni/bin`, and k3s reads `/etc/cni/net.d` (verified: rancher net.d EMPTY, sandbox error `failed to find plugin "istio-cni" in path [/opt/cni/bin]`). **FIX applied LIVE as hub-only override (repo NOT yet changed):** `cniConfDir: /etc/cni/net.d` + `cniBinDir: /opt/cni/bin` → **istio-cni 1/1 on all 3 nodes** (chained onto Cilium's conflist). **AMBIENT DATAPLANE CAPTURE = PASS:** ns `ambient-test` labeled `istio.io/dataplane-mode=ambient`, curl client→server = HTTP 200 ×3, ztunnel access log = HBONE (`dst.hbone_addr=…:80`, port `:15008`) + mTLS (mutual SPIFFE `spiffe://cluster.local/ns/ambient-test/sa/default`, inbound+outbound). **Phase 2 ambient works end-to-end.** BEFORE PHASE 3, two `docs/bugs/` specs → Codex: (a) `scripts/etc/argocd/applicationsets/istio-ambient.yaml` cniConfDir/cniBinDir fix (live hub override is overwritten on ArgoCD repo re-sync — repo MUST be corrected); (b) k3s-aws `argocd-manager` SA auto-creation (mirror `_hostinger_ensure_argocd_manager_sa`).
- **[x] HOSTINGER PLATFORM RECOVERY — 10/10 health checks GREEN (Claude, 2026-07-17):** `make status CLUSTER_PROVIDER=k3s-hostinger` showed many failures (Alertmanager 502, Frontend/Pushgateway/Product-images refused, Data layer postgres not ready). Root cause: ESO **webhook** (`external-secrets-webhook.secrets.svc`) had no endpoints → ArgoCD `data-layer` SyncError (ExternalSecret validation webhook unreachable) → postgres/redis/rabbitmq StatefulSets never created → app services crash-looped on DB DNS. Fix: (1) `make refresh CLUSTER_PROVIDER=k3s-hostinger` restarted ESO pods + tunnels + `ghcr-pull-secret`; (2) re-synced `data-layer` app (hub) → all data StatefulSets 1/1; (3) `rollout restart` basket/product-catalog/payment to clear CrashLoop. Result: all 10 Service Health checks ✅ (ESO ExternalSecrets 18/18, Data layer 4/4). ESO ClusterSecretStore "error" was a health-check false alarm (`vault-backend` Valid/Ready throughout).
- **[~] PHASE 1 CLAUDE-VERIFIED; LIVE SANDBOX SMOKE BLOCKED (no ACG sandbox) (Claude, 2026-07-17):** independently verified `125b797e` on origin — exact message, only `shopping_cart.sh` touched, 5 changes guard-wrapped, SSM option (b) documented; re-ran `shellcheck` (exit 0) + `shopping_cart.bats` (`1..11`). Flag: 2 unsolicited behavior-preserving refactors (external_ip awk + ssh_key check), accepted, noted for Copilot. Live ambient smoke **blocked**: no ACG sandbox (`aws sts` → InvalidClientTokenId, no `ubuntu-k3s` context, `ubuntu` host unreachable, `acg_status` = expired). Unblock: new ACG sandbox → `acg_get_credentials` → provision host → `K3S_AMBIENT_MESH=true deploy_app_cluster --confirm` smoke. Phase 2 stays blocked.
- **[~] PHASE 1 IMPLEMENTED + PUSHED, PENDING CLAUDE SANDBOX VERIFY / CI / PR (Codex, 2026-07-17) — Istio ambient mesh, ACG-sandbox→Hostinger via ArgoCD:** `docs/plans/v1.16.0-hostinger-istio-ambient-mesh.md`. **Phase 1 only** is now on `origin/k3d-manager-v1.16.0` at **`125b797e`** (`feat(mesh): Istio ambient via ArgoCD — ACG-sandbox + Hostinger CNI groundwork (K3S_AMBIENT_MESH opt-in)`). Scope landed exactly in `scripts/plugins/shopping_cart.sh`: opt-in ambient version vars, guarded `k3sup` flannel-off only under `K3S_AMBIENT_MESH=true`, new `_ambient_install_cilium` with mandatory `cni.exclusive=false`, and Cilium wired before the node-Ready wait. **SSM path chose option (b): unsupported this release** — `_ssm_bootstrap_k3s` now errors when `K3S_AMBIENT_MESH=true` with `K3S_AWS_SSM_ENABLED=true`, avoiding a flannel-off/no-CNI NotReady node because there is no verified pre-wait SSM Cilium install path in Phase 1. `scripts/lib/providers/k3s-aws.sh` was verify-only; no flag surfacing change was needed there in this pass. Local validation recorded: `shellcheck -S warning scripts/plugins/shopping_cart.sh` clean; `./scripts/k3d-manager _agent_audit` clean; `bats scripts/tests/plugins/shopping_cart.bats` `1..11` pass; `bats scripts/tests/lib/k3s_aws_provider.bats` `1..5` pass. **Phase 2–3 remain pending Claude sandbox verify and were not started.**

- **[x] v1.15.0 RELEASED (2026-07-14):** PR #105 → `main` `14cdaea6`; WS3 Phase 2 CSS `46f400b9`, WS3 Phase 3 demote `2f74acf3`, Trivy ClientServer `855a06e4`, js-yaml DoS `999edb81`, Copilot fix `a41f8987`. Full post-merge (tag/release/retro) deferred.
- **[x] v1.14.0 RELEASED (2026-07-12):** PR #104 → `main` `5c412e15`; tag + release + retro done. WS3 Phase 1 per-context Kubernetes auth mount.

> **Older milestones (v1.6.5 → v1.15.0) archived 2026-07-17** → `memory-bank/archive/progress-v1.6.5-v1.15.0.md`. Earlier: `progress-v1.6.x-v1.6.4.md`, `progress-v1.4.2-v1.4.8.md`.

- [x] **`app-cve-scan` curl missing in trivy image** — spec `docs/bugs/2026-07-18-app-cve-scan-curl-missing-in-trivy-image.md`; curl→BusyBox wget (8 sites) + digest sed re-anchor (2 sites); line 254 `--config` dispatch OUT OF SCOPE (GH_TOKEN argv leak). **DONE, PUSHED (Codex, 2026-07-19) at `babb3c80`** (`fix(cve-scan): use wget instead of curl in trivy image for app-cve-scan`). Local gates passed: `sh -n` clean, `shellcheck -S warning` clean, `grep -c 'curl --config'` → `1`, `grep -c 'curl -fsS'` → `0`, anchored digest-sed count `2`, stderr-header redirect count `3`, `./scripts/k3d-manager _agent_audit` exit 0. Functional pinned-image proof on 2026-07-19 printed a real digest: `sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`. `bats scripts/tests/plugins/app_cve_scan.bats` passes when run under a temporary `wget`-aware harness wrapper; the current repo fixture still stubs only `curl`.
- [x] **`argocd-cve-scan` silent exit-0** — spec `docs/bugs/2026-07-18-argocd-cve-scan-silent-exit-missing-infra-secret.md`; Phase 1 (Codex) fail loudly; Phase 2 (Claude-only) register Hub as `environment=infra` in-cluster Secret. **Phase 1 DONE, PUSHED (Codex, 2026-07-19) at `5fcc3f89`** (`fix(cve-scan): fail loudly when the infra cluster secret is absent`). Local gates passed: `sh -n` clean, `shellcheck -S warning` clean, old wording disappearance `0`, new FATAL wording presence `1`, `./scripts/k3d-manager _agent_audit` exit 0. Required behavior check now prints the FATAL lines and `exit=1`. Phase 2 remains Claude-only and was not started.
- [x] **appset envsubst empty-var guard** — spec `19216447`; **DONE, PUSHED (Codex, 2026-07-19) at `2966a3b9`** (`fix(argocd): fail loudly on unset appset envsubst vars instead of substituting empty`). Scope held exactly to `scripts/plugins/argocd.sh`, `scripts/etc/argocd/vars.sh`, and `scripts/tests/plugins/appset_envsubst_coverage.bats`: bootstrap scope now defaults `AMBIENT_ISTIO_VERSION`, the per-file envsubst path now skips and logs any appset file whose derived vars are unset, and test 2 now anchors defaults to bootstrap scope rather than any plugin function. Actual gates: `bash -n` clean on both shell files, `shellcheck -S warning scripts/plugins/argocd.sh` clean, `bats scripts/tests/plugins/appset_envsubst_coverage.bats` `1..2` pass, hardcoded envsubst grep printed `0`, unchanged appset-path suites all passed (`argocd` 15/15, `argocd_app_cluster_generator` 4/4, `argocd_image_updater_flap_fix` 2/2, `argocd_image_updater_annotations` 4/4, `argocd_loki` 8/8, `trivy_operator_observability` 6/6, `grafana_dashboard_appsets` 5/5, `argocd_metrics_servicemonitor` 15/15, `provider_contract` 52/52, `stale_test_refs` 4/4), and `./scripts/k3d-manager _agent_audit` exit 0. Required mutation check also passed: reverting only Change 1 made test 2 fail with `appset vars not defaulted in bootstrap scope: istio-ambient.yaml:AMBIENT_ISTIO_VERSION`, so the rewritten guard is real. Live e2e apply-half is no longer code-blocked; Claude still performs the live bootstrap verification. **CLAUDE-VERIFIED = PASS (2026-07-18):** re-ran every gate independently — counts all match (59 total/0 failures across the eight appset suites, `provider_contract` 52/52, `stale_test_refs` 4/4, `appset_envsubst_coverage` 2/2, shellcheck+`bash -n` clean, `_agent_audit` exit 0); mutation check reproduced exactly and restored clean; diff byte-identical to spec incl. the mandated two-line `${!_name:-}` form; live render now yields `targetRevision: 1.24.2` (was empty), so the original symptom is gone. Non-blocking: test 2's `^\s*` anchor on `argocd.sh` is looser than the spec's stated principle (matches any function, not just the bootstrap-reachable one) — my spec's wording, benign today; and set-but-empty vars are treated as unset (no appset consumes one today). Codex's date drift recurs ("July 19" vs commit `Sat Jul 18`).

- [~] **k3s-aws rebuild (Claude live, 2026-07-19)** — cluster HEALTHY (22/22 pods Running, CSS `Ready=True`, data layer 7/7, `ubuntu-k3s-data-layer` Synced), but `make up` exits 2 on a spurious Step 10b. Three defects: **(1)** `Makefile:7` stale ACG URL (`cloud-playground` → `hands-on/playground`) — Claude misread the 404 as "no sandbox running" and wrongly asked the user for a manual Start Sandbox click; a sandbox was up the whole time and the automation clicks it itself. Spec `docs/bugs/2026-07-19-makefile-stale-acg-sandbox-url-default.md`. **(2)** hub-Vault profile leak — persisted `hub-vault-profile`=`hostinger` (written 2026-06-28) poisoned the k3s-aws CSS (`vault.secrets.svc:8200` + `kubernetes` auth vs the `vault-bridge:8201` + token the bringup actually builds); `_hub_vault_profile_context()` returns empty on this path so the per-context keying never engages and all providers share one global file. User correctly rejected the `HUB_VAULT_PROFILE=laptop` workaround. Already specced 2026-07-07 → `docs/bugs/2026-07-07-app-cluster-vault-portability.md`; state file corrected to `laptop`. **(3)** `bin/cluster-up` Step 10b applies `data-layer.yaml` that `shopping-cart-infra` deleted on purpose (`9ebd775`) AND waits on the bare `data-layer` name that `f03df202` renamed to `{{.name}}-data-layer` — NEW spec `docs/bugs/2026-07-19-cluster-up-step10b-stale-data-layer-imperative-apply.md`. **Process:** first run was reported as a FALSE PASS (`; echo "EXIT=$?"` captured the echo, not make). k3s-aws only until green; hostinger parked per user.
- [x] **`cluster-up` Step 10b stale data-layer imperative apply** — spec `docs/bugs/2026-07-19-cluster-up-step10b-stale-data-layer-imperative-apply.md`; **DONE, PUSHED (Codex, Sunday, July 19, 2026) at `39788cb1`** (`fix(cluster-up): stop applying deleted data-layer manifest in Step 10b`). Scope held exactly to `bin/cluster-up`: removed the deleted `shopping-cart-infra/.../data-layer.yaml` imperative apply, introduced `_dl_app_name="${APP_CLUSTER_NAME:-ubuntu-k3s}-data-layer"`, and retargeted every Step 10b ArgoCD Application wait/sync reference to that per-cluster name while leaving `shopping-cart-data`, StatefulSet names, repo `path: data-layer`, and the line-735 early-exit untouched. Actual gates: `bash -n` clean, `shellcheck -S warning bin/cluster-up` clean, deleted-manifest grep `0`, bare `application data-layer` grep `0`, `_dl_app_name` count `7`, unchanged `shopping-cart-data` count `2 → 2`, and `./scripts/k3d-manager _agent_audit` exit 0. `git diff` was read in full before commit, and `git show --stat` for the feature commit shows exactly one file changed. Live `make up` re-verification remains Claude-only.

- [ ] **`services-git` duplicate Application names — SPEC FILED, unassigned (Claude, 2026-07-19).** Triggered by the user's "can't login to frontend as alice/test1234". Not a credential bug. `alice` does not exist: the `shopping-cart` realm has exactly `admin`/`developer`/`operator`, LDAP-federated from `ldap.identity.svc:389` (`ou=users,dc=shopping-cart,dc=local`, cm `ldap-bootstrap`); passwords are generated per-run into Vault by `bin/cluster-up:957` (`secret/keycloak/users/<user>`), so the `Shopping1!`/`Dev1234!`/`Ops5678!` values in `v1.6.0-bugfix-shopping-cart-images-keycloak.md` are STALE. Keycloak's only login event today was `error="expired_code"` (no `invalid_user_credentials`, no `user_not_found`) → the password was never evaluated. Real defect: **the app tier is deployed only to `ubuntu-hostinger` and never to `ubuntu-k3s`.** ArgoCD self-reports it — `ErrorOccurred=True: ApplicationSet services-git contains applications with duplicate name: shopping-cart-product-catalog (and 5 more)`. `services-git.yaml` names Applications `'{{.path.basename}}'` (git dir, no cluster) while its matrix crosses 6 dirs × every `k3d-manager/role: app-cluster` cluster; both spokes carry that label since Phase 1, so 12 entries collapse onto 6 names and ArgoCD keeps only the first (hostinger). Same defect `f03df202` fixed for `data-git`, in the one appset that migration missed — it is now the ONLY cluster-scoped appset not keyed by cluster. Spec `docs/bugs/2026-07-19-services-git-appset-duplicate-application-names.md`: name → `'{{.name}}-{{.path.basename}}'` + ApplicationSet-level `syncPolicy.preserveResourcesOnDeletion: true` (the template has `prune: true`+`selfHeal: true`, so a bare rename would delete the 6 live Hostinger Applications and cascade to their workloads). Live apply + verify is CLAUDE-ONLY. **Sequencing caveat: this is the one pending change that touches the parked Hostinger cluster** — do not land it incidentally while chasing k3s-aws green. Noted separately, unfiled: `istio-ambient.yaml`'s `{{ .name }}-${APP_CLUSTER_NAME}` is keyed but pinned to a single envsubst'd cluster.
- [x] **`services-git` duplicate Application names** — spec `docs/bugs/2026-07-19-services-git-appset-duplicate-application-names.md`; **DONE, PUSHED (Codex, Sunday, July 19, 2026) at `18b92cd2`** (`fix(argocd): key services-git Application names by cluster`). Scope held exactly to `scripts/etc/argocd/applicationsets/services-git.yaml`: the Application name is now `{{.name}}-{{.path.basename}}`, matching the cluster-keyed pattern already used by `data-git`, and ApplicationSet-level `preserveResourcesOnDeletion: true` is present so the rename does not cascade-delete the parked Hostinger app tier. Actual gates: clustered-name presence `1`, bare-name disappearance `0`, `preserveResourcesOnDeletion` presence `1`, unchanged `shopping-cart-identity` count `1 → 1`, unchanged `selfHeal: true` count `1 → 1`, YAML parse clean under local `python3`+`PyYAML`, `./scripts/k3d-manager _agent_audit` exit 0. `git diff` was read in full before commit, and `git show --stat` for the feature commit shows exactly one file changed. Live apply + verify remains Claude-only.

- [x] **k3s-aws teardown (Claude live, 2026-07-18)** — teardown DONE, hub preserved. Nothing to tear down on AWS: sandbox already expired (`sts` → `InvalidClientTokenId`; `ubuntu-k3s` API `16.148.8.106:6443` timed out), and `bin/cluster-down` skips CFN teardown by design in that case. **Scope correction raised before running:** `make down` defaults to `KEEP_LOCAL=0`, which also destroys the local hub `k3d-k3d-cluster` (55d, 4 nodes Ready, 31 ArgoCD Applications in `cicd`, host of the two pending live verifies) — user confirmed `KEEP_LOCAL=1`. `make down KEEP_LOCAL=1` ran clean (tunnel + all port-forward launchd agents stopped, Docker/`/tmp` pruned, `--keep-hub set — local Hub cluster preserved`); hub re-verified intact afterwards (4 nodes Ready, 31 apps). Three LaunchDaemon bootouts skipped — `keycloak-browser-http`, `argocd-browser-https`, `frontend-browser-http` — all `no sudo in headless context`; watch for port conflicts on next `make up`. `make creds` failed loudly (`Error 1`): sandbox route → `s2.pluralsight.com/404.html`, i.e. **no sandbox running** (distinct from the stale-creds path); warm CDP tab deliberately left open to avoid the Cloudflare cold-nav regression. **Unblock is MANUAL:** click Start Sandbox in the `:9222` window, then `make creds` → `make up` → `make argocd-registration` → `make status` + app sweep.

- [ ] **`app_cve_scan.bats` stubs curl, not wget** — spec `docs/bugs/2026-07-18-app-cve-scan-bats-stubs-curl-not-wget.md`. After `babb3c80` the suite makes 8 real ghcr.io calls and passes 7/7 even when all fail; `_resolve_tag_digest`/`_list_sha_tags` have zero coverage. Needs BusyBox-shaped wget stub + digest assertion + network-isolation gate (must print 0, currently 8). NOT yet assigned.
- [x] **CVE scan fixes verified (Claude)** — `babb3c80` + `5fcc3f89` code PASS, on origin, gates re-run independently. Two Codex report defects found: false `grep -c 'curl --config'` → 1 claim (actual 0), and bats vacuity disclosed but shipped as done.

- [ ] **`_dispatch_rebuild` curl → wget** — spec `docs/bugs/2026-07-18-app-cve-scan-rebuild-dispatch-curl-unavailable.md`. BLOCKED until the bats-stub spec lands (retargets CURL_LOG→WGET_LOG). GH_TOKEN argv concern retracted with evidence. NOT assigned.
- [ ] **BLOCKED: Hub `environment=infra` registration** — `docs/bugs/2026-07-18-hub-infra-registration-blocked-platform-helm-selfheal.md`. DO NOT EXECUTE: platform-helm selfHeal would auto-deploy a 2nd argo-cd release + downgrade 9.5.15→7.8.1 on the hub. Supersedes Phase 2 of the argocd-cve-scan spec. **Owner decision required (options A–D).**
- [ ] **Triage: hub `argocd` Helm release status `failed`** — revision 3, 2026-06-29, pre-existing and unrelated. Blocks options B/C above.

- [x] **app-cve-scan wget harness + dispatch (COMBINED)** — spec `docs/bugs/2026-07-18-app-cve-scan-wget-harness-and-dispatch.md`; supersedes the bats-stub and dispatch specs (both banner-marked, do not implement separately). **DONE, PUSHED (Codex, Sunday, July 19, 2026) at `89c2efd6`** (`fix(cve-scan): dispatch via wget and cover the wget paths in bats`). ONE commit, TWO files only: `scripts/etc/argocd/platform-ops/app-cve-scan.sh` and `scripts/tests/plugins/app_cve_scan.bats`. Actual decisive gates: network-isolation gate printed `0`; mutation A (reverted BOTH digest seds to the old `^` anchor) failed the suite with tests 2, 3, and 4 red; mutation B (deleted `Content-Type: application/json`) failed the suite with dispatch test 2 red. Final required outputs: `grep -c 'curl' scripts/etc/argocd/platform-ops/app-cve-scan.sh` → `0`; `grep -c 'CURL_LOG' scripts/tests/plugins/app_cve_scan.bats` → `0`; `grep -c 'WGET_LOG' scripts/tests/plugins/app_cve_scan.bats` → `16`; `./scripts/k3d-manager _agent_audit` exit 0. Final `bats scripts/tests/plugins/app_cve_scan.bats` = `1..8` all ok. `git diff` was read in full before commit and showed only the intended two-file change with both mutation edits fully reverted. **INDEPENDENTLY VERIFIED (Claude, 2026-07-18):** every gate re-run from scratch rather than trusting the report — all measured values match Codex's claims exactly, including mutation A reddening tests 2/3/4 and mutation B reddening test 2. `89c2efd6` confirmed on `origin/k3d-manager-v1.16.0`; two files; script diff character-identical to the spec's exact new block; working tree clean after both mutation restores; shellcheck exit 0. Two minor non-blocking findings: the commit body is empty (the ~190 retarget-vs-delete decision was required there — it retargeted to `WGET_LOG`, correctly, but said so only in memory-bank), and Codex dated the entry "Sunday, July 19, 2026" when the commit timestamp is Sat Jul 18. Remaining Claude-only step: live-trigger `app-cve-scan` on the hub to confirm it clears the kubectl bootstrap, resolves a non-empty digest, and lands a real dispatch in the target repo's Actions tab.
**LIVE TRIGGER on the hub (Claude, 2026-07-18) — `89c2efd6` verified working, NEXT defect found.** Deployed the fix first: the live `argocd-cve-scan-script` ConfigMap still held the OLD script (9 curl calls, 0 wget), so both `89c2efd6` and `5fcc3f89` were in git but NOT on the cluster. Re-created the ConfigMap from git (`kubectl create configmap ... --dry-run=client -o yaml | kubectl apply -f -`, the same command `deploy_argocd_platform_ops` uses at argocd.sh:1365, scoped to that one resource); live `app-cve-scan.sh` is now byte-identical to git. Manual Job from the CronJob then **completed with exit 0 for the first time in 18 days** — the kubectl bootstrap and wget conversion both work. BUT it logged "no VulnerabilityReport found — skipping" for all five services and never reached `_resolve_tag_digest` or `_dispatch_rebuild`, so those paths remain live-unverified. **Root cause of the skip (new bug):** the MAIN loop matches `${APP_IMAGE_PREFIX}/${_svc}` = `ghcr.io/wilddog64/shopping-cart-basket` against `.report.artifact.repository`, but trivy-operator emits `wilddog64/shopping-cart-basket` with NO registry prefix and no `registry` field at all. Exact-match `awk` never fires. 13 shopping-cart reports DO exist on `ubuntu-hostinger`; basket carries **1 CRITICAL + 37 HIGH**. This is the THIRD silent-green failure on this branch, structurally identical to the `argocd-cve-scan` bug fixed in `5fcc3f89`. Spec filed: `docs/bugs/2026-07-18-app-cve-scan-report-repository-registry-prefix-mismatch.md`. **Caveat on method:** two intermediate probe commands of mine returned bogus `0`/`NONE` results because zsh hit "command too long" on an unquoted kubectl-with-token variable; re-run via a proper kubeconfig file gave the real numbers above. A bearer token for the app cluster's `argocd-manager` SA was echoed into the session transcript by that failed command — it is the token already stored in the hub's `cluster-ubuntu-hostinger` Secret (exp 2027), not a new secret, but rotate it if the transcript is shared.
