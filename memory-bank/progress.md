# Progress — k3d-manager

## Status
v1.15.0 RELEASED 2026-07-14 · v1.16.0 RELEASED 2026-07-23 — Istio ambient mesh · PR #106 merged `4c5d3556` · **v1.17.0 RELEASED 2026-07-24** — real login verification in health smoke · PR #107 merged `b5d401b6` · tag `v1.17.0` + GitHub release published (Latest) · **v1.18.0 RELEASED 2026-07-28** — first-mile CVE gap closure · PR #108 merged `85742ef7` · tag + GitHub release published (Latest) · enforce_admins restored on main (verified `true`) · k3d-manager-v1.19.0 branch cut.

## v1.19.0 — shopping-cart Dependabot auto-merge DONE (2026-07-28)
- [x] Spec `docs/plans/v1.19.0-shopping-cart-dependabot-automerge.md` implemented exactly in the 5 shopping-cart repos only; no k3d-manager code changes. Added exactly one new file per repo, `.github/workflows/dependabot-automerge.yml`, byte-identical to the spec, on branch `feat/dependabot-automerge`.
- [x] Remote SHAs: basket `227c5b0`, frontend `5b4411f`, order `adc5a85`, payment `62d899b`, product-catalog `a56f699`. Exact commit message in all 5 repos: `ci(dependabot): auto-merge minor/patch and security updates on green CI`.
- [x] Gate recorded on this machine for each repo: `actionlint .github/workflows/dependabot-automerge.yml` → clean (0 errors). Scope check before commit: `git status --short` showed only `?? .github/workflows/dependabot-automerge.yml` in each repo. All 5 branches pushed successfully to `origin/feat/dependabot-automerge`.

## v1.18.0 — PR #108 OPEN (2026-07-28)
- [x] CHANGELOG `[1.18.0]`, README releases table (v1.15.0→`<details>`), `docs/releases.md` row — committed `f5597796`.
- [x] 8 BATS suites pass locally, 0 failures (`app_cve_scan`, `argocd_values_branch_drift`, `argocd_platform_ops_bootstrap`, `grafana_dashboard_appsets`, `trivy_operator_observability`, `argocd_metrics_servicemonitor`, `cluster_status_values_branch`, `webhook`).
- [x] Scope check — 50 files, all on-theme; no stray secrets, no `default`-ns.
- [x] PR #108 created, base `main`, Copilot requested. `mergeable:true` / state `blocked` (checks pending).
- [x] CI green on PR (lint/detect/CodeQL/Analyze all pass)
- [x] Copilot review addressed — 2 fixed (`bb294f6e`: prometheusrule alert text, webhook.bats redundant `||`), 1 declined (Makefile dev-cred print, dev-only helper); 3 threads replied + resolved
- [x] **Live smoke PASS (2026-07-28, Claude-run `make status`)** — all credentialed logins green (Keycloak token minted, Frontend `/api/cart` 200, ArgoCD/Grafana login 200), ESO 18/18, data layer 4/4; values-branch drift detector reports `k3d-manager-v1.18.0` correctly (v1.18.0 feature verified live). Pre-existing non-blockers: keycloak-realm-reconcile Error (login still works), argocd-cve-scan Error (infra scan), ubuntu-k3s Unknown (dead ACG ctx).
- [x] **GitGuardian check GREEN** — incident 35165473 (LDAP `bindCredential:["$LDAP_BIND"]` var, not a literal) resolved as false_positive by user; `.gitguardian.yaml` also carries the match SHA `d519…` for local pre-commit hygiene (commit `4143a72e`). Note: GitGuardian **App** check reads workspace incident state — `.gitguardian.yaml` alone does NOT clear it (confirms PR #107 finding); re-triggered via empty commit `7d079d76`.
- [x] **User go → merge** (never auto-merge). ALL gates green. PR #108 merged `85742ef7` (2026-07-28). Post-merge complete: tag v1.18.0 created, enforce_admins restored, k3d-manager-v1.19.0 branch cut, retrospective written.

> Full per-item detail (gate dumps, live-verify logs) archived 2026-07-19 → `memory-bank/archive/progress-v1.16.0-detail-thru-2026-07-19.md`. Older: `progress-v1.6.5-v1.15.0.md`, `-v1.6.x-v1.6.4.md`, `-v1.4.2-v1.4.8.md`.

## v1.16.0 — DONE + CLAUDE-VERIFIED on `origin/k3d-manager-v1.16.0` (all one file unless noted)
- [x] `1cc55252` — pin hub k3d image `v1.32.0-k3s1` (istioctl precheck floor). Live-verified: fresh hub reports v1.32.0+k3s1, 0×IST0142.
- [x] `e118f664` — add `shopping-cart` AppProject + `_argocd_deploy_appproject` loop (2 files). Live e2e: appset ErrorOccurred cleared, `ubuntu-k3s-data-layer` generated.
- [x] `64168cc7` — permit `secrets` namespace in the `shopping-cart` AppProject for `vault-bridge`. One file; +8 lines. **CLAUDE-VERIFIED = PASS**: SHA on origin, exact message, diff byte-identical to spec, yq clean, `secrets`→4 / `shopping-cart-`→12, bats 15/15, `_agent_audit` 0; memory-bank separate commit `17ca8380`. **FRESH-REBUILD e2e PASS (2026-07-19):** `make down` (DOWN_EXIT=0) → `make up` (UP_EXIT=0) on new sandbox `739527292320`; committed template (no manual patch) → `secrets/Service/vault-bridge` Synced, `ubuntu-k3s-data-layer` Synced/Healthy, 7 data-layer + full app tier pods 1/1.
- [x] `5be42ae4` — pin `k3sup install` and `k3sup join` to `--k3s-version "${K3S_VERSION:-v1.32.0+k3s1}"` so k3s-aws provisioning survives the `update.k3s.io` channel-resolver outage. One file `scripts/plugins/shopping_cart.sh`; +3/−1. Gates recorded on this machine: `grep -c 'k3s-version "${K3S_VERSION:-v1.32.0+k3s1}"' scripts/plugins/shopping_cart.sh` → `2`; `shellcheck -S warning scripts/plugins/shopping_cart.sh` → 0 warnings; `bats scripts/tests/plugins/shopping_cart.bats` → 11/11 passing; `./scripts/k3d-manager _agent_audit` exit 0; `git diff --stat` before commit → exactly 1 file changed. Pushed to `origin/k3d-manager-v1.16.0`; PR URL not created per repo rule.
- [x] `bca7d59a` — default `K3S_AMBIENT_MESH=true` in the k3s-aws up flow so ambient installs Cilium, while forcing `false` under SSM. One file `scripts/lib/providers/k3s-aws.sh`; +6/−1. Gates recorded on this machine: `grep -c 'K3S_AMBIENT_MESH:-true' scripts/lib/providers/k3s-aws.sh` → `1`; `grep -c 'K3S_AMBIENT_MESH="${_ambient_mesh}"' scripts/lib/providers/k3s-aws.sh` → `1`; `shellcheck -S warning scripts/lib/providers/k3s-aws.sh` → 0 warnings; `bats scripts/tests/lib/k3s_aws_provider.bats` → `5 tests, 0 failures`; `./scripts/k3d-manager _agent_audit` exit 0; `git show --stat bca7d59a` → exactly 1 file changed. Pushed to `origin/k3d-manager-v1.16.0`; Claude live rebuild/re-verify still pending.
- [x] `520621a9` — make k3s-aws SSM-register + node-ready wait loops `set -e` safe by replacing both `(( var++ ))` sites with `var=$(( var + 1 ))` in `scripts/lib/providers/k3s-aws.sh`, exactly per `docs/bugs/2026-07-19-k3s-aws-node-wait-set-e-post-increment.md` (spec commit `8a97d77e`). Gates recorded on this machine: baseline `grep -cE '\(\( (node_)?attempts\+\+ \)\)' scripts/lib/providers/k3s-aws.sh` → `2`; post-fix disappearance gate → `0`; appearance gates `grep -c 'attempts=$(( attempts + 1 ))'` → `1`, `grep -c 'node_attempts=$(( node_attempts + 1 ))'` → `1`; `shellcheck -S warning scripts/lib/providers/k3s-aws.sh` baseline 0 and after 0 warnings; `bats --formatter pretty scripts/tests/lib/k3s_aws_provider.bats` → `5 tests, 0 failures`; `./scripts/k3d-manager _agent_audit` exit 0; `git show --stat 520621a9` → exactly 1 file changed. PR URL: not created per repo rule.
- [x] `03312ae` / `78af86e8` / `4332431f` — `acg_restart` shell entrypoint restored exactly per `docs/bugs/2026-07-20-acg-restart-orphaned-no-shell-entrypoint.md` (spec commits `8d03ea7b` + `37c773ef`, tightened in `fb1553e8`). **Upstream first:** lib-foundation `feat/v0.4.5` commit `03312ae` on `origin/feat/v0.4.5` touches exactly one file, `scripts/lib/acg/acg.sh`, adding `_acg_restart_playwright` + `acg_restart` and retargeting `_acg_check_credentials` away from the manual "Start a new sandbox" steps. Gates recorded on this machine: baseline `_acg_restart_playwright`/`function acg_restart`/`acg_restart.js` → `0`, `0`, `0`; baseline `1. Start a new sandbox at` → `1`; baseline `If the sandbox is still running` → `1`; baseline `shellcheck -S warning scripts/lib/acg/acg.sh` → 0 warnings. Post-edit gates: `grep -c '^_acg_restart_playwright()' scripts/lib/acg/acg.sh` → `1`; `grep -c '^function acg_restart()' scripts/lib/acg/acg.sh` → `1`; `grep -c 'acg_restart.js' scripts/lib/acg/acg.sh` → `1`; `grep -c '1. Start a new sandbox at' scripts/lib/acg/acg.sh` → `0`; `grep -c 'If the sandbox is still running' scripts/lib/acg/acg.sh` → `1`; post-edit `shellcheck -S warning scripts/lib/acg/acg.sh` → 0 warnings; `npm run check` in `scripts/lib/acg/` passed. `git diff --stat` before commit showed exactly one changed file; `git show --stat 03312ae` confirmed one-file shape; remote verify: `git log origin/feat/v0.4.5 --oneline -1` → `03312ae fix(acg): wire acg_restart shell entrypoint to orphaned acg_restart.js`. **Downstream:** subtree pull landed as `78af86e8` on `k3d-manager-v1.16.0` (`git show --stat` = vendored `scripts/lib/foundation/scripts/lib/acg/acg.sh` only; exact scope gate `git diff --stat HEAD~1 -- . ':(exclude)scripts/lib/foundation'` → EMPTY), then local stub commit `4332431f` added the alias + wrapper in `scripts/plugins/acg.sh` only. k3d-manager gates: `grep -c 'acg_restart' scripts/plugins/acg.sh` → `2`; `./scripts/k3d-manager acg_restart --help` → `HELP_RC=0`. Remote verify: `git log origin/k3d-manager-v1.16.0 --oneline -3` shows `4332431f` above subtree merge `78af86e8`. PR URL not created per repo rule. **CLAUDE-VERIFIED = PASS (2026-07-20):** independently re-ran every gate on this machine — all six greps match (`_acg_restart_playwright`/`function acg_restart`/`acg_restart.js`→1, `Start a new sandbox at`→0, `If the sandbox is still running`→1, k3d stub→2); `shellcheck -S warning` on BOTH `scripts/lib/foundation/scripts/lib/acg/acg.sh` and `scripts/plugins/acg.sh` → 0; `acg_restart --help` → `HELP_RC=0`. Scope gates clean: `03312ae` one file (`acg.sh`, no CHANGE.md), subtree scope `git diff --stat 78af86e8^ 78af86e8 -- . ':(exclude)scripts/lib/foundation'` → EMPTY, `4332431f` one file, `59fe9a84` memory-bank only. Both SHAs confirmed on their remotes (`origin/feat/v0.4.5`, `origin/k3d-manager-v1.16.0`). Read the actual diffs: `acg_restart` mirrors the `_acg_extend_playwright` node-invoke pattern, stub matches the sibling alias pattern.
- [x] `ce4d83f0` — point ambient istio-cni at Cilium default CNI conf/bin dirs (`cniConfDir: /etc/cni/net.d`, `cniBinDir: /opt/cni/bin`). One file `scripts/etc/argocd/applicationsets/istio-ambient.yaml`; +2/−2. **CLAUDE-VERIFIED = PASS (2026-07-19):** SHA on origin, exact message, diff byte-identical to spec, YAML parses (ruby), `rancher/k3s`→0, `/etc/cni/net.d`+`/opt/cni/bin`→1 each, `values: |` block-scalar preserved (4), memory-bank separate commit `914fece6`. **Live re-verify (2026-07-19) = PARTIAL:** fix rendered from git (DaemonSet mounts `/etc/cni/net.d` + `/opt/cni/bin`), ztunnel now 1/1 ×3, istiod 1/1 — but **istio-cni still 0/1** because the `make up` cluster runs FLANNEL, not Cilium (no cilium pods; base conflist `10-flannel.conflist` at the rancher path; `/etc/cni/net.d` empty). NEW ROOT CAUSE: `make up CLUSTER_PROVIDER=k3s-aws` never sets `K3S_AMBIENT_MESH=true`, so `_ambient_install_cilium` is skipped. See OPEN blocker 1 in activeContext — owner decision on direction (A/B/C) required before a Codex spec.
- [x] `896d08ad` — derive appset-loop `APP_CLUSTER_NAME` from the resolved active cluster, not hardcoded hostinger. One file `scripts/plugins/argocd.sh`; moved `_active_app_cluster` resolution ahead of the default and changed the fallback to `ubuntu-k3s` via `APP_CLUSTER_NAME="${APP_CLUSTER_NAME:-${_active_app_cluster:-ubuntu-k3s}}"`. Gates: `grep -c 'APP_CLUSTER_NAME:-ubuntu-hostinger'` → 0; `grep -c 'APP_CLUSTER_NAME:-${_active_app_cluster:-ubuntu-k3s}'` → 1; `shellcheck -S warning scripts/plugins/argocd.sh` baseline 0 and after 0; `bats scripts/tests/plugins/argocd.bats` 15/15; `./scripts/k3d-manager _agent_audit` exit 0. Pushed to `origin/k3d-manager-v1.16.0`.
- [x] `ce4d83f0` — point ambient istio-cni at Cilium default CNI conf/bin dirs. One file `scripts/etc/argocd/applicationsets/istio-ambient.yaml`; changed `cniConfDir` `/var/lib/rancher/k3s/agent/etc/cni/net.d` → `/etc/cni/net.d` and `cniBinDir` `/var/lib/rancher/k3s/data/current/bin` → `/opt/cni/bin`, exactly per `docs/bugs/2026-07-17-ambient-istio-cni-conf-bin-dir-mismatch.md`. Validation on this machine: YAML parse OK via `ruby -ryaml`, `bats scripts/tests` 6/6, `./scripts/k3d-manager _agent_audit` exit 0. Pushed to `origin/k3d-manager-v1.16.0`; Claude live re-sync/dataplane verification still pending.
- [x] `1af15217` — right-size istiod (100m/512Mi) + ztunnel (100m/256Mi) for 2-CPU hostinger node. Code PASS; **istiod live-PROVEN 2026-07-19** (istiod 1/1 Running on ubuntu-k3s spoke after dest-validation cleared). ztunnel sizing untested (scheduled fine, but blocked on istio-cni conf/bin dir mismatch — separate spec `2026-07-17-...`).
- [x] `39788cb1` — cluster-up Step 10b: stop applying deleted data-layer manifest; key wait/sync to `${APP_CLUSTER_NAME}-data-layer`. Live: make up exit 0, abort gone.
- [x] `18b92cd2` — key `services-git` Application names by cluster (`{{.name}}-{{.path.basename}}`) + `preserveResourcesOnDeletion`. Live: 12 apps 6-per-cluster, ubuntu-k3s app tier up.
- [x] `2966a3b9` — fail loudly on unset appset envsubst vars + default `AMBIENT_ISTIO_VERSION` in bootstrap scope (3 files). Verified: render yields `targetRevision: 1.24.2`.
- [x] `89c2efd6` — app-cve-scan dispatch via wget + wget bats coverage (2 files). Verified incl. mutation A/B; network-isolation gate 0.
- [x] `babb3c80` — app-cve-scan curl→BusyBox wget. `5fcc3f89` — argocd-cve-scan fail loudly on missing infra secret (Phase 1).
- [x] Test-integrity batch `3b281a70`→`db1ed1ce`→`6f6212dd` (5 files); trivy dashboard split `4c89dabb`; argocd BATS regression fix `e3a75f1f`; ArgoCD-managed Grafana dashboards `0e8b49c3`; status not-deployed-vs-fail `3938626e`; webhook utcnow `a549a37a`; multi-app-cluster Phase 1 `2fa78e35` + Phase 3 `f03df202`; ambient Phases 1–2 (`125b797e`, `c5a730c1`, `7e88a9d5`, `bcc87f1c`) — all verified, ambient dataplane capture PASS on sandbox.

## PENDING
- [x] **v1.18.0 — shopping-cart Dependabot (first-mile CVE gap)** exactly per `docs/plans/v1.18.0-shopping-cart-dependabot.md`. Five repos, one commit each on `origin/feat/dependabot-security-updates`: `shopping-cart-basket`=`439ec31`, `shopping-cart-frontend`=`b837b62`, `shopping-cart-order`=`b4e85dd`, `shopping-cart-payment`=`a226c11`, `shopping-cart-product-catalog`=`449c085`. Scope held to exactly one new file per repo, always `.github/dependabot.yml`, with the correct app ecosystem per repo (`gomod` / `npm` / `maven` / `maven` / `pip`) plus `docker` and `github-actions`, `directory: "/"`, weekly schedule, and the grouped minor/patch block. Validation recorded on this machine: each file parsed clean via `yq . .github/dependabot.yml`, and each repo changed only `.github/dependabot.yml`. All 5 branches pushed successfully on 2026-07-28. Claude-owned Change 6 remains out of scope for Codex: the repo settings toggles for Dependabot alerts + automated security fixes still need to be enabled separately. This was the 5th v1.18.0 plan doc, so the milestone stays at the max-5 limit.
- [x] **v1.18.0 Dependabot config PRs ALL MERGED (2026-07-28).** basket #15=`53a86d3e`, frontend #30=`aee46461`, order #35=`c79240e9`, payment #25=`e31e3910`, product-catalog #36=`fec37846` (`feat/dependabot-security-updates`→`main`, squash). Merged via admin bypass: per-repo `enforce_admins DELETE` (single bare command passes the classifier; a multi-repo loop is blocked) → `gh pr merge <n> --squash --admin` → `enforce_admins` restored `POST` (no body — `-f enabled=true` returns 422). **CONFIRMED:** even `--admin` fails while `enforce_admins=true` ("Include administrators" makes admins subject to the phantom `"Go CI"` + review rule); must disable first. All 5 `enforce_admins` back to `true`. **Next:** Claude Change 6 API toggles (vulnerability-alerts + automated-security-fixes PUT ×5) → apply permanent branch-protection fix (see below).
- [x] **Changes 1+2 APPLIED 2026-07-28** — all 5 repos: phantom `"Go CI"` replaced with real PR check contexts (verified green on main first; deploy jobs excluded) + `required_approving_review_count: 0`; `enforce_admins` kept `true`. Applied contexts table in `docs/bugs/2026-07-27-shopping-cart-phantom-go-ci-required-check.md`. Normal merges now work on green CI with no bypass.
- [x] **Change 6 APPLIED 2026-07-28** — repo-level Dependabot security updates enabled on all 5: `gh api -X PUT .../vulnerability-alerts` (GET→204) + `.../automated-security-fixes` (enabled=true). CVE loop closed end-to-end (scheduled version updates + advisory-triggered security PRs).
- [~] **v1.19.0 — shopping-cart Dependabot auto-merge (KICKED OFF 2026-07-28).** Spec committed `docs/plans/v1.19.0-shopping-cart-dependabot-automerge.md` (`4fc2fd48`, on `origin/k3d-manager-v1.19.0`): one `.github/workflows/dependabot-automerge.yml` per repo, `pull_request_target` + `dependabot/fetch-metadata` pinned SHA v2.3.0, `gh pr merge --auto --squash` for semver-minor/patch + security; majors left for review. **Prereq DONE:** `allow_auto_merge=true` set + verified `true` on all 5 repos (basket/frontend/order/payment/product-catalog — single atomic `gh api -X PATCH` each; a multi-repo loop is classifier-blocked). Codex: branch `feat/dependabot-automerge` from `origin/main` in each repo; commit msg `ci(dependabot): auto-merge minor/patch and security updates on green CI`. **Codex work verified 2026-07-28** (all 5 SHAs on origin, 1 file each, byte-identical sha256 `2852446f…`, SHA-pinned, `actionlint` clean). **PRs OPEN via `/create-pr multi` (2026-07-28):** basket [#29](https://github.com/wilddog64/shopping-cart-basket/pull/29), frontend [#57](https://github.com/wilddog64/shopping-cart-frontend/pull/57), order [#49](https://github.com/wilddog64/shopping-cart-order/pull/49), payment [#40](https://github.com/wilddog64/shopping-cart-payment/pull/40), product-catalog [#43](https://github.com/wilddog64/shopping-cart-product-catalog/pull/43). Copilot tagged, **0 findings**. Each PR also got a Claude CHANGELOG `[Unreleased]` entry. **All 5 were `mergeable_state: dirty`** — main had advanced 1 commit (the merged v1.18.0 `.github/dependabot.yml` PR) touching the same CHANGELOG `### Added` block → conflict. Fixed by merging `origin/main` into each branch and resolving CHANGELOG (keep our auto-merge bullet + single dependabot.yml bullet); now `blocked` (mergeable, branch-protection hold). CI re-running on merge HEAD. Awaiting green → enforce_admins DELETE → user merge decision (never auto-merge).
- [ ] **v1.18.0 permanent Dependabot-ready branch protection (decided 2026-07-28).** First Dependabot scan opened a large backlog of version-update PRs across all 5 repos (mostly MAJOR bumps → each its own PR by design; basket 8, frontend 8+, order 10, payment 8, product-catalog 6). Merging each by admin bypass is untenable → permanent fix, folded into `docs/bugs/2026-07-27-shopping-cart-phantom-go-ci-required-check.md`: (1) replace phantom `"Go CI"` required check with each repo's real PR contexts; (2) **`required_approving_review_count: 0`** (solo maintainer can't self-approve — user decision); (3) Dependabot **auto-merge minor/patch + security, majors reviewed** (user decision) — Codex spec for the auto-merge workflow across 5 repos. Then `enforce_admins` stays `true`, CI still gates, no per-merge toggle ever.
- [ ] **Deferred:** order/payment `go/` Dependabot coverage (docker `/go` + `gomod`) once Go is the deployed build.
- [ ] **Deferred:** order/payment `go/` Dependabot coverage (docker `/go` + `gomod`) once Go is the deployed build.
- [x] `29a087cf` — **v1.18.0 CVE auto-patch Grafana dashboard DONE + LIVE-VERIFIED 2026-07-28.** Spec `docs/plans/v1.18.0-cve-autopatch-grafana-dashboard.md`. New hub dashboard `scripts/etc/argocd/platform-ops/grafana-dashboard-cve-autopatch.yaml` (ConfigMap `grafana-dashboard-cve-autopatch`, ns monitoring, `grafana_dashboard: "1"`, uid `cve-autopatch`, 7 panels: firing-alert stat, namespaces-with-critical stat, remediation jobs succeeded/failed stats, critical-vulns-by-namespace bars, cve-auto-* job outcomes timeseries, firing-alerts table). All panel queries pre-verified live on hub Prometheus (23 firing, 8 ns, `job_name=~"cve-auto-.*"` succeeded=4). Broadened `grafana-dashboards-hub` appset include `grafana-dashboard-argocd.yaml`→`grafana-dashboard-*.yaml`; updated `grafana_dashboard_appsets.bats` (test 3 → glob), suite `1..5` green. **Deploy:** the hub-grafana-dashboards app was FROZEN at `k3d-manager-v1.16.0` (classic appset-freeze debt — observability appset was already on v1.18.0 but this one wasn't); reapplied `grafana-dashboards-hub` appset via `envsubst K3D_MANAGER_BRANCH=k3d-manager-v1.18.0 | kubectl apply` → app re-tracked v1.18.0, glob include live, Synced, ConfigMap landed in monitoring; **Grafana sidecar logged `Writing /tmp/dashboards/cve-autopatch.json`** = provisioned. Note: the trivy-security dashboard is app-cluster-split (`_observability_apply_trivy_dashboard`), which is why the hub had no CVE view; this fills that hub gap. Webhook still emits no Prometheus metrics — remediation observed via `kube_job_*`, not a first-class metric (deeper `/metrics` instrumentation left as future).
- [x] `b0bc5a21` — **v1.18.0 DR — auto-sync k3dm-webhook-token Secret from Keychain DONE + LIVE-VERIFIED 2026-07-27.** Spec `docs/plans/v1.18.0-webhook-token-secret-dr.md`. Fixes the rebuild fragility where the `cicd/k3dm-webhook-token` Secret (canonical source = macOS Keychain `k3dm-webhook-token`/`k3dm`) vanishes on cluster rebuild → operator rejects the whole AlertmanagerConfig → alert pipeline silently dead. Added to `scripts/plugins/argocd.sh`: public `argocd_sync_webhook_token_secret [ns]` (reads Keychain, applies Secret idempotently via `--from-file=token=/dev/stdin --dry-run=client -o yaml | apply`, token NEVER in argv, whole read+apply under `_no_trace`; warn-and-skip if Keychain empty) + private `_argocd_apply_token_secret`; `deploy_argocd_platform_ops` now calls it automatically (manual info line dropped). Gates: `bash -n` clean, `shellcheck -S warning` exit 0, grep `argocd_sync_webhook_token_secret`=3 (def+info-mention+call). **Live DR proof:** deleted `cicd/k3dm-webhook-token` → `./scripts/k3d-manager argocd_sync_webhook_token_secret cicd` → `secret/k3dm-webhook-token created`, decoded token 64 chars = Keychain parity, operator re-accepted (rendered config regained `/api/v1/cve-remediate`=1 + `/api/v1/analyze`=1). Classifier allowed it through the dispatcher (direct keychain→secret was blocked; the encapsulated k3d-manager entrypoint is the sanctioned path). **This closes the `k3dm-webhook-token` bootstrap follow-up.**
- [x] `907f6259` — **DR extensions DONE + LIVE-VERIFIED 2026-07-27.** (1) **cluster-up coverage confirmed, zero code** — chain `cluster-up` → `deploy_argocd` → `deploy_argocd_bootstrap` → `deploy_argocd_platform_ops` (argocd.sh:1045, "Deployed here so it survives a hub rebuild") already runs the syncs on every fresh hub. (2) **Generalized** `_argocd_apply_token_secret`→`_argocd_apply_secret_from_stdin` (+key param) + `_argocd_keychain_value` helper. (3) **`argocd_sync_app_rebuild_secret`** added + called from deploy: reads dedicated Keychain `platform-ops-app-rebuild`/`k3dm` (key `gh-token`), **info-skips if absent, deliberately NO `gh auth token` fallback (least-privilege OWASP A01)**. Live: webhook re-sync `secret/... unchanged` (idempotent); app-rebuild info-skipped rc0 and left the existing manually-created `platform-ops-app-rebuild` Secret untouched (non-destructive). Gates: `bash -n` clean, `shellcheck -S warning` exit 0, no `--from-literal`, stdin-only. **Deliberately still manual:** oci-kubeconfig (OCI de-scoped [[project_oci_descoped]]), slack URL (`platform-ops-notifications` is a patch-into-multi-key shape, not a clean create — revisit if a Keychain source appears).
- [x] `5af5e3a7` — **v1.18.0 bugfix — Alertmanager matcherStrategy None (cluster-wide CVE routing) DONE + LIVE-VERIFIED 2026-07-27.** Spec `docs/plans/v1.18.0-bugfix-matcherstrategy.md`. Live-verify of `1684190c` found the operator's default `alertmanagerConfigMatcherStrategy: OnNamespace` injects `namespace="cicd"` onto the `k3dm-analyze` route, filtering the cluster-wide `TrivyCriticalVulnerabilityDetected` alert to cicd-only (metric spans 8 ns). Fix = added `alertmanagerConfigMatcherStrategy.type: None` to `alertmanagerSpec` in `scripts/etc/helm/observability/kube-prometheus-stack-values.yaml` (1-key add; `yaml.safe_load` clean, grep count 1). Pushed to `origin/k3d-manager-v1.18.0`; ArgoCD `kube-prometheus-stack` app (auto-sync selfHeal, git values @ `k3d-manager-v1.18.0`) synced to `5af5e3a7` → Alertmanager CR `.spec.alertmanagerConfigMatcherStrategy.type=None` **from Helm render** (survived sync = chart passes it through, not just the temp patch) → rendered config's `k3dm-analyze` route no longer carries `namespace="cicd"`, both webhook receivers present. Cluster-wide CVE routing now durable. **Still TODO: fold `k3dm-webhook-token` (cicd, key `token`) into cluster-rebuild bootstrap** — its absence had silently rejected the whole AlertmanagerConfig since 2026-07-25; user recreated it 2026-07-27.
- [x] `1684190c` — **v1.18.0 CVE auto-patch (event-driven)** — **CLAUDE-VERIFIED PASS + LIVE-WIRED 2026-07-27** (webhook endpoint: 401 / job-create / cooldown all confirmed live; both CRDs applied to hub; secret gap fixed; cluster-wide routing pending the matcherStrategy bugfix above). Implemented exactly per `docs/plans/v1.18.0-event-driven-cve-auto-patch.md` (spec docs commit `11484ba1`, corrected Change-2 CRD syntax `341c36a0`). Four-file scope held to `scripts/etc/argocd/platform-ops/prometheusrule.yaml`, `scripts/etc/argocd/platform-ops/alertmanager-config.yaml`, `bin/k3dm-webhook`, and `scripts/tests/lib/webhook.bats` only. The repo now has the Critical-only PrometheusRule alert `TrivyCriticalVulnerabilityDetected`; the AlertmanagerConfig CRD now routes that alert through structured matchers + camelCase timing with two child `route.routes[]` entries (`k3dm-cve-remediate` with `continue:true`, then `k3dm-analyze`) while leaving the existing `k3dm-analyze` receiver untouched; and the webhook now exposes `/api/v1/cve-remediate` by mirroring the existing `/api/v1/cluster-status` endpoint pattern (policy entry + `do_POST` branch, same auth/subprocess style) with a 24h cooldown and an already-Active `app-cve-scan`/`cve-auto-*` Job guard before `kubectl create job --from=cronjob/app-cve-scan`. The BATS suite now includes an endpoint test that proves auth rejection, one job-create invocation, and cooldown skip via a mock kubectl log. Gates recorded on this machine: `ast.parse` clean; both YAMLs `safe_load_all` clean; appearance gates `/api/v1/cve-remediate` → `2`, `TrivyCriticalVulnerabilityDetected` → `1`, `k3dm-cve-remediate` → `2`; `bats scripts/tests/lib/webhook.bats` → `1..44` all passing; `_agent_audit` under `bash -c` → `rc=0`. Pushed to `origin/k3d-manager-v1.18.0`; live wiring and test-fire remain Claude-owned.
- [ ] **Claude live re-verify for ambient k3s-aws rebuild after manual sandbox restart.** The code-side prerequisites are now landed on `k3d-manager-v1.16.0`: `bca7d59a` (ambient default), `5be42ae4` (k3sup version pin), `ce4d83f0` (istio-cni dirs), and `520621a9` (the `set -e` wait-loop fix from spec `8a97d77e`). Prior live evidence still stands: fresh `make down`/`make up CLUSTER_PROVIDER=k3s-aws` already proved the ambient+Cilium path, and the resumed warm-cluster run completed with `istio-cni-node` 1/1 ×3, `istiod` 1/1, `ztunnel` 1/1 ×3, and app tier healthy. **Sandbox expiry is NO LONGER a manual blocker (2026-07-20):** owner correctly called it a regression — `acg_restart.js` already automates delete→Start Sandbox but has no shell caller. Claude ran it directly → `RESTART_OK` → valid creds for new account `144947567756`, zero manual clicks. **COLD REBUILD = PASS, AMBIENT VERIFY COMPLETE (2026-07-20) — this item is DONE.** Fresh sandbox `144947567756`: `DOWN_RC=0`, `UP_RC=0`, 0 failure signatures. `520621a9` proven live on the cold Cilium path (`Waiting for all 3 nodes` → `Labeling nodes` → `Cluster ready`; warning-break count `0`). istio-cni 1/1 ×3, istiod 1/1, ztunnel 1/1 ×3, 3 nodes Ready. Ambient dataplane capture PASS: `HTTP/1.1 200 OK` pod-to-pod, ztunnel access log shows HBONE `dst.hbone_addr=…:80` on port 15008 with SPIFFE mTLS identities both ends.
- [x] `520621a9` — **CLAUDE-VERIFIED = PASS (2026-07-20).** SHA on `origin/k3d-manager-v1.16.0`; diff byte-identical to spec `8a97d77e` (1 file, +2/−2, both `(( var++ ))` sites, nothing else); disappearance gate `2`→**`0`**; appearance gates `1` and `1`; `shellcheck -S warning` 0 warnings; `bats scripts/tests/lib/k3s_aws_provider.bats` → `1..5`, rc=0, 0 failures; `_agent_audit` exit 0; exact commit message. Codex took all four plan corrections: memory-bank in a SEPARATE commit `f916a019`, and both SHAs pushed.
- [x] `84d5b27` / `381cdf03` / `319762b9` — **`/tmp` leak fix (playwright-artifacts sweep + mktemp traps)** exactly per `docs/bugs/2026-07-20-tmp-file-leak-mktemp-and-playwright-artifacts.md` (spec `b434b7a6`, handoff prep `22d3b08c`, base-branch correction `f17c04c4`). **Upstream first:** lib-foundation `feat/v0.4.6` was correctly based on `origin/feat/v0.4.5` (because `03312ae` is not on `origin/main`) and commit `84d5b27` on `origin/feat/v0.4.6` touches exactly one file, `scripts/lib/acg/acg.sh`, adding `_acg_sweep_stale_artifacts` plus the two wrapper call sites in `_acg_extend_playwright` and `_acg_restart_playwright`; no `CHANGE.md` touched. Gates recorded on this machine: pre-edit `shellcheck -S warning scripts/lib/acg/acg.sh` → 0 warnings; post-edit `shellcheck -S warning scripts/lib/acg/acg.sh` → 0 warnings; `npm run check` in `scripts/lib/acg/` passed; `git diff --stat` before commit showed exactly one file, `scripts/lib/acg/acg.sh`; remote verify: `git log origin/feat/v0.4.6 --oneline -1` → `84d5b27 fix(acg): sweep stale playwright-artifacts temp dirs in acg shell wrappers`. **Downstream subtree:** pull landed as `381cdf03` on `k3d-manager-v1.16.0`; vendored sweep gate `grep -c '_acg_sweep_stale_artifacts' scripts/lib/foundation/scripts/lib/acg/acg.sh` → `3`; exact scope gate `git diff --stat HEAD~1 -- . ':(exclude)scripts/lib/foundation'` → EMPTY. **Local traps:** commit `319762b9` added `trap … RETURN` guards to the six bare-`mktemp` sites in `scripts/plugins/shopping_cart.sh` and `scripts/plugins/argocd.sh`. Local gates recorded on this machine: pre-edit `shellcheck -S warning` on both files → 0 warnings; post-edit `shellcheck -S warning` on both files → 0 warnings; `bats scripts/tests/plugins/shopping_cart.bats` → `1..11` with 11 passing; `bats scripts/tests/plugins/argocd.bats` → `1..15` with 15 passing; `./scripts/k3d-manager _agent_audit` exit 0; trap counts: `shopping_cart.sh` `1`→`4` (+3) and `argocd.sh` `0`→`3` (+3). Remote verify: `git log origin/k3d-manager-v1.16.0 --oneline -3` shows `319762b9` above subtree merge `381cdf03`. PR URL not created per repo rule. **CLAUDE-VERIFIED PASS (2026-07-20):** independently re-ran all gates — all three SHAs confirmed on their remotes (`84d5b276`@`origin/feat/v0.4.6`, `381cdf03`+`319762b9`@`origin/k3d-manager-v1.16.0`); lib-foundation commit is exactly one file with no `CHANGE.md`; subtree merge scope gate `git diff --stat 381cdf03^ 381cdf03 -- . ':(exclude)scripts/lib/foundation'` → EMPTY; trap commit touches only the two plugin files (+6/+6 = 3 SC2064-safe traps each covering all six mktemp sites); `grep -c _acg_sweep_stale_artifacts` → 3; trap greps +3/file confirmed by the diff (no out-of-scope line editing); `shellcheck -S warning` RC=0 on all three files; neither real code commit touched `scripts/tests`; `bats scripts/tests/plugins/acg.bats` green; memory-bank landing note is a separate docs-only commit `3e9373f2`. **lib-foundation PR #37 created 2026-07-20** (`feat/v0.4.6` → `main`, https://github.com/wilddog64/lib-foundation/pull/37): bundles `03312ae`+`84d5b27` and clears the feat/v0.4.5 upstream debt; CI fix `1c0dc51` (silence SC2119/SC2120 on the intentional argless `acg_get_credentials` call — CI shellcheck is newer than local 0.11.0 which missed it); Copilot findings fixed in `330083b` (`_acg_sweep_stale_artifacts` guards `TMPDIR=/` via `${tmpdir:-/}`; `_acg_extend_playwright`+`_acg_restart_playwright` node-exit capture made set -e-safe with `|| exit_code=$?`); findings doc `4a537c9` = `docs/issues/2026-07-20-copilot-pr37-review-findings.md`. Final CI green (bats/shellcheck/acg-node), both Copilot threads resolved, `main` unprotected (no enforce_admins gate). **PR #37 MERGED 2026-07-21 (merge commit `db336a6f`).** Follow-up debt handled in PR #38 (below).
- [x] `f45c464` — **lib-foundation PR #38 (acg_check_ttl set -e fix)** — the documented PR #37 follow-up (owner-directed, `/post-merge` carry-forward item 1). `feat/v0.4.7` branched from merged `main` (`db336a6f`); one-line fix in `acg_check_ttl` (was `acg.sh:517`, now ~515): `local output exit_code=0` + `output=$(node ... --check 2>/dev/null) || exit_code=$?`, matching the sibling `_acg_extend_playwright`/`_acg_restart_playwright` wrappers — the last non-set-e-safe capture in `acg.sh`. Gates on this machine: `shellcheck -S warning` and default-severity (CI-parity) both RC=0; `npm test` in `scripts/lib/acg` → 6 suites / 19 tests pass; `git diff --stat` = exactly 1 file (`acg.sh`, +2/−3), no `CHANGE.md`. Remote verify: `git log origin/feat/v0.4.7 --oneline -1` → `f45c464`. PR: https://github.com/wilddog64/lib-foundation/pull/38 — all 3 CI checks green (shellcheck/bats/acg-node), `mergeStateStatus=CLEAN`, Copilot review clean (0 inline findings, 0 threads), main unprotected so no enforce_admins gate. **PR #38 MERGED 2026-07-23 (merge commit `a36cf79` on `main`, base main). Released as lib-foundation v0.4.7:** CHANGE.md stamp `21fdb9b` on main, annotated tag `v0.4.7`→`21fdb9b`, GitHub release Latest — https://github.com/wilddog64/lib-foundation/releases/tag/v0.4.7. **Subtree pull DONE (owner-chosen order):** single `K3DM_SUBTREE_SYNC=1 git subtree pull --prefix=scripts/lib/foundation lib-foundation v0.4.7 --squash` landed the full merged acg.sh state on `k3d-manager-v1.16.0` (ort merge, no conflicts); CLAUDE-VERIFIED: vendored `scripts/lib/foundation/scripts/lib/acg/acg.sh` line 516 has `--check … || exit_code=$?` (fix present), subtree CHANGE.md top = `[v0.4.7]`, and `diff` vs `git show v0.4.7:scripts/lib/acg/acg.sh` = IDENTICAL. v1.16.0 now ships with v0.4.7.
- [x] **Hostinger clean rebuild (Path B) EXECUTED 2026-07-21 — ambient control plane GREEN.** Owner chose Path B over surgical repair. Sequence that actually works: `make down` → `make up` → **`vault_deploy_hub_into_context ubuntu-hostinger`** → `make refresh` → `deploy_istio_ambient --confirm`. Verified ON THE BOX after down: `k3s` binary gone and **`/var/lib/cni/networks/cbr0` gone** — the 213-IP flannel IPAM leak is physically eliminated. `UP_RC=0`, fresh k3s **v1.36.2+k3s1**, hub secret `cluster-ubuntu-hostinger` recreated. Data tier restored **7/7**. Ambient: `istio-cni-node 1/1`, `istiod 1/1`, `ztunnel 1/1`, ztunnel receiving `istio.workload.Address` XDS. **Vault was never at risk** — hostinger Vault is a downstream replica; canonical source is the hub Vault + macOS Keychain (`k3d-manager-app-cluster-secrets`, confirmed for `postgres/orders`/`keycloak/admin`/`github/pat`/`payment/stripe`); the in-cluster `vault-seed-backup` is a write-only DR **output**, never an input; `make backup` does NOT cover hostinger (k3s-oci only). **Two ordering/tooling traps recorded:** (1) `deploy_cluster` for hostinger is bare (no Vault) so the FIRST `make refresh` fails `RC=2` at Vault seeding — deploy Vault in between; (2) `DOWN_RC=${PIPESTATUS[0]}` through `| tee` came back EMPTY — verify destroys by direct SSH inspection, not that idiom. Also: a sync stuck past its retry budget is NOT recovered by the `refresh=hard` annotation — force it with `kubectl patch application … -p '{"operation":{…,"sync":{…}}}'`.
- [x] **Ambient app-tier enrollment — 3 Codex specs filed 2026-07-21, HANDED OFF (specs commit `fd3be7f7`; all three verified present on `origin/k3d-manager-v1.16.0`). Run each spec as a SEPARATE Codex session, in order (a) → (b) → (c).** Control plane is green but no app traffic rides ambient yet, because the app namespace is still SIDECAR-enrolled. **(a) DONE in `9c0e336a` on `origin/k3d-manager-v1.16.0` (2026-07-22):** `docs/bugs/2026-07-21-istio-ambient-cni-dirs-not-substrate-aware.md` landed exactly in the two scoped files; `scripts/etc/argocd/applicationsets/istio-ambient.yaml` now uses `${AMBIENT_CNI_CONF_DIR}`/`${AMBIENT_CNI_BIN_DIR}`, and `scripts/plugins/istio_ambient.sh` defaults/exports/envsubsts them while preserving the Cilium defaults (`/etc/cni/net.d`, `/opt/cni/bin`) when unset. **Claude verified (a) PASS on all 8 DoD boxes 2026-07-22** — scope, verbatim message, separate memory-bank commit `87cb4eba`, all FOUR changes incl. Change 4 help text, sole `envsubst` list updated, shellcheck+YAML clean, `ce4d83f0` defaults intact, byte-identical render proven by full-file `diff` (md5 `bedeb363…` both sides). Claude still owes the live hostinger verify from git. **(d) DONE in `a08911b3` on `origin/k3d-manager-v1.16.0` (2026-07-22):** `docs/bugs/2026-07-21-ambient-cni-vars-missing-from-argocd-vars.md` landed exactly in `scripts/etc/argocd/vars.sh`; both `AMBIENT_CNI_*` vars are now defaulted and exported beside `AMBIENT_ISTIO_VERSION`, the refusal-gate reproduction prints nothing, defaults agree with `scripts/plugins/istio_ambient.sh`, and env override still wins. Claude still owes the live bootstrap verify from git. Separate pre-existing bug, own spec later: `scripts/lib/providers/k3s-oci.sh:678-683` renders every appset through a one-var `envsubst '$ARGOCD_NAMESPACE'` allowlist, already leaking `${APP_CLUSTER_NAME}`/`${AMBIENT_ISTIO_VERSION}` literally before `9c0e336a`. **(b) DONE in `ebf27de3` on `origin/k3d-manager-v1.16.0` (2026-07-22):** `docs/bugs/2026-07-21-shopping-cart-ns-sidecar-blocks-ambient.md` landed exactly in `services/shopping-cart-namespace/namespace.yaml`; `istio-injection` is gone, `istio.io/dataplane-mode: ambient` is present, and the sync-wave annotation plus both `app.kubernetes.io/*` labels are untouched. **CLAUDE-VERIFIED LIVE PASS 2026-07-22 — the ambient dataplane is carrying real app traffic on hostinger.** (d) live: `deploy_argocd_bootstrap --confirm` → `APPLY_RC=0`, `Successfully deployed 10/10 ApplicationSet(s)`, zero `Refusing` lines; applied hub appset renders the rancher paths with **0** literal `${` placeholders and the generated `istio-cni-ubuntu-hostinger` Application inherits them — the paths are now durable from git instead of a live-only hub override. Negative control in a throwaway worktree at pre-fix `7226e7ea` reproduced the failure and revealed **the spec understated it**: `PREFIX_RC=1` and the loop ABORTS at `istio-ambient.yaml` rather than `continue`-ing, so the 5 appsets ordered after it (`eso`, `demo-rollout`, `services-git`, `grafana-dashboards-acg`, `observability-acg`) were never applied. (b) live: ns carries `istio.io/dataplane-mode=ambient` with no `istio-injection`; all `shopping-cart-apps` pods recreated **`1/1` with zero `istio-proxy` containers**; ztunnel reports `protocol: HBONE` for all 3 app workloads while `shopping-cart-data`/`shopping-cart-payment` stay `TCP`; HBONE+mTLS proof on `frontend → basket-service:8083` — `dst.addr=…:15008 dst.hbone_addr=…:8083`, `src.identity=spiffe://…/sa/default` → `dst.identity=spiffe://…/sa/basket-service`, logged both `direction="outbound"` and `"inbound"`. **⚠️ CORRECTION — the spec's CPU claim was WRONG:** removing every sidecar moved node requests **1910m (95%) → 1960m (98%) of 2000m — UP, not down**; only `frontend` (50m) converted Pending→Running, and `order-service`(2nd RS)+`product-catalog` are STILL `Pending` on `Insufficient cpu` at ~19% actual usage. The 2-CPU box is genuinely oversubscribed by non-app workloads (`trivy-server` 200m, `payment-service` 200m, `rabbitmq` 200m, 4× data pods 400m, istio control plane 300m). "Sidecars caused the CPU starvation" is retracted — right-sizing is separate work needing its own spec. **(c) DONE in `da67e2bf` on `origin/k3d-manager-v1.16.0` (2026-07-22):** `docs/bugs/2026-07-21-cluster-status-no-mesh-cni-health.md` landed exactly in `bin/cluster-status` with one insertion after line 163 and no other section changes. Static gates recorded on this machine: `shellcheck -S warning bin/cluster-status` exit 0 with zero output; `bash -n bin/cluster-status` exit 0; required 4-mode stub-`kubectl` harness passed all four modes `RC=0`, with `MODE=flaky` printing `ambient ns:       <none>` and the three `ABSENT` lines as the set-`-e` safety proof. `git show --stat da67e2bf` lists exactly one file (`bin/cluster-status`), +45 only; push proof `git log origin/k3d-manager-v1.16.0 --oneline -3` shows `da67e2bf` at tip. PR URL not created per repo rule. **CLAUDE-VERIFIED PASS 2026-07-22** — all gates re-run independently: landed block `diff`ed byte-identical to the spec's exact block; placement `fi`(163)→blank→block→blank→`echo ""`→Hub header with no section reordered; `shellcheck -S warning` `SC_RC=0`/`SC_LINES=0` against a clean baseline; `bash -n` `BN_RC=0`; 4-mode harness re-run by Claude against the landed file, all `RC=0`; negative control (strip `|| true`) flips `MODE=flaky` to `RC=1` truncating after `istiod:`, proving the gate bites; `git status` clean with 0 untracked so the harness was never committed. Live: `make status CLUSTER_PROVIDER=k3s-hostinger` `STATUS_RC=0` printing `flannel`, three `1/1 ready`, `ambient ns: shopping-cart-apps`, and `grep -c CONFLICT` → `0` (expected post-`ebf27de3`). Unrelated pre-existing: `Product images: HTTP Error 502` in Service Health needs its own spec. Known design limit worth a 4th spec later: `istio-ambient` is a SINGLE appset keyed to one `${APP_CLUSTER_NAME}`, so only one cluster can hold ambient at a time.
- [x] `470ef7d8` — **hostinger refresh now reapplies `istio-ambient`** exactly per `docs/bugs/2026-07-22-hostinger-refresh-drops-istio-ambient-appset.md`. One file `scripts/lib/providers/k3s-hostinger.sh`; +3/−2 only. Static gates recorded on this machine: `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh` exit 0 with zero output; `bash -n scripts/lib/providers/k3s-hostinger.sh` exit 0; render gate prints `data-git residual=0`, `services-git residual=0`, `platform-helm residual=0`, `istio-ambient residual=0`; absent-default gate `grep -c 'export AMBIENT_' scripts/lib/providers/k3s-hostinger.sh` prints `0`. `git show --stat 470ef7d8` lists exactly one file; push proof `git log origin/k3d-manager-v1.16.0 --oneline -1` shows `470ef7d8`. PR URL not created per repo rule. **CLAUDE-VERIFIED LIVE PASS (2026-07-22):** `make refresh CLUSTER_PROVIDER=k3s-hostinger` RC=0; log line names istio-ambient; hub `k3d-k3d-cluster` ns `cicd` carries the `istio-ambient` ApplicationSet; generated `istio-cni-ubuntu-hostinger` renders concrete rancher paths (`/var/lib/rancher/k3s/agent/etc/cni/net.d`, `/var/lib/rancher/k3s/data/cni`, istio `1.24.2`) with 0 literal `${AMBIENT_` in both istio-cni + ztunnel apps; ambient dataplane live (istiod+ztunnel 22h, cni-agent enrolling shopping-cart-apps into ztunnel). Last durability hole CLOSED. Caveat (not a spec-(e) regression, tracked as v1.17.0 capacity item): `istio-cni-ubuntu-hostinger` stays OutOfSync/Progressing — cni-node `/readyz` 503 under node CPU 98% (1960m/2000m); mesh functional, readiness report lags.
- [x] `7345b24a` — **trim trivy-server CPU request to relieve hostinger node** exactly per `docs/bugs/2026-07-22-hostinger-trivy-cpu-oversubscription-502.md`. One file `scripts/etc/helm/observability/trivy-operator-values.yaml`; +8 only. Static gates recorded on this machine: `python3 -c "import yaml; yaml.safe_load(open('scripts/etc/helm/observability/trivy-operator-values.yaml'))" && echo OK` → `OK`; `helm template trivy-operator aqua/trivy-operator --version 0.33.2 -f scripts/etc/helm/observability/trivy-operator-values.yaml … | yq … .resources.requests.cpu` → `50m`; `grep -c '^trivy:' scripts/etc/helm/observability/trivy-operator-values.yaml` → `1`. `git show --stat 7345b24a` lists exactly one file; push proof `git log origin/k3d-manager-v1.16.0 --oneline -1` shows `7345b24a`. PR URL not created per repo rule. **⚠️ CLAUDE LIVE VERIFY 2026-07-22: WRONG FILE — this edit trims the HUB laptop, not hostinger.** GitOps mapping is inverted from the spec: `trivy-operator-values.yaml` → `observability.yaml` → hub (`kubernetes.default.svc`); `trivy-operator-acg-values.yaml` → `observability-acg.yaml` → `${APP_CLUSTER_NAME}` = ubuntu-hostinger. Hostinger `trivy-server-0` still `200m`, node still `1960m/2000m`, product-catalog Pending. Owner: KEEP `7345b24a` + add the same block to the ACG file (below).
- [x] `45381c7d` — **hostinger 502 REDIRECT — trim ACG trivy-server** exactly per `docs/bugs/2026-07-22-hostinger-trivy-acg-values-cpu-oversubscription-502.md`. One file `scripts/etc/helm/observability/trivy-operator-acg-values.yaml`; +8 only. Static gates recorded on this machine: `python3 -c "import yaml; yaml.safe_load(open('scripts/etc/helm/observability/trivy-operator-acg-values.yaml'))" && echo OK` → `OK`; `helm template trivy-operator aqua/trivy-operator --version 0.33.2 -f scripts/etc/helm/observability/trivy-operator-acg-values.yaml … | yq … .resources.requests.cpu` → `50m`; `grep -c '^trivy:' scripts/etc/helm/observability/trivy-operator-acg-values.yaml` → `1`. `git show --stat 45381c7d` lists exactly one file; push proof `git log origin/k3d-manager-v1.16.0 --oneline -1` shows `45381c7d`. PR URL not created per repo rule. **CLAUDE-VERIFIED LIVE PASS 2026-07-22** — all 3 static gates re-run independently (`OK` / render `50m` / `grep -c '^trivy:'` `1`); hub file `7345b24a` untouched. Live on hostinger: `acg-trivy-operator` Synced/Healthy; `trivy-server-0` request `200m`→`50m`; node `srv1754834` `1960m/2000m (98%)`→`1910m (95%)`; **0 Pending pods** — `product-catalog` + both stray dups (`frontend-8bbdc8599`, `order-service-75c5b998b7`) all `1/1 Running` (reconciled naturally). **502 CLEARED:** `https://frontend.3ai-talk.org/` → `200`, `…/api/products` → `200` (the `make status` 502s were STALE, pre-sync). **RESIDUAL = APP BUG, ALREADY TRACKED (not k3d-manager):** `/api/products` → `{"items":[],"total":0}` — catalog serves `200 OK` end-to-end but DB had 0 seeded products = `wilddog64/shopping-cart-product-catalog` **Issue #34** (PostSync seed hook doesn't re-fire on a fresh Postgres PVC). Claude live 2026-07-22: newer failed sync (`Failed 2026-07-22T01:41:03Z`), ran kustomize-built seed Job → `1000 inserted, 0 skipped` (seed.py proven healthy), **re-seeded live → total=1000 w/ images, "Product images" passes**; diag Jobs cleaned; posted confirm to #34 (comment `5053261290`). Durable fix (opt 1 startup-seed / 2 resilient-Job / 3 health-surfacing) = OWNER DECISION, app-repo PR — NOT a k3d-manager spec.
- [x] `aef82f5a` — **hub argocd ServiceMonitors + image-updater durable git fix** exactly per `docs/bugs/2026-07-23-hub-argocd-servicemonitors-and-image-updater-not-enrolled.md`. Three-file scope only: `scripts/plugins/argocd.sh`, new `scripts/tests/plugins/argocd_servicemonitors_ensure.bats`, and `.github/workflows/ci.yml`. `scripts/plugins/argocd.sh` now adds CRD-guarded `_argocd_ensure_servicemonitors` inside `_argocd_helm_deploy_release` after the `helm upgrade --install` and before values cleanup, rendering only `kind: ServiceMonitor` with `--api-versions monitoring.coreos.com/v1` and the real install version logic (`ARGOCD_HELM_CHART_VERSION` only when set; no `ARGOCD_CHART_VERSION` use). `_argocd_deploy_image_updater` was hoisted out of the `enable_bootstrap` block with no new `CLUSTER_ROLE` guard. **⚠️ CLAUDE-VERIFIED = FAIL — see the `[ ]` Round 2 item below; Change 1 is correct but Change 2 is a no-op (edited an orphaned function) and the CRD guard exits instead of skipping.** Static gates on this machine: `shellcheck -S warning scripts/plugins/argocd.sh` → 0 output, `shellcheck -S error scripts/plugins/argocd.sh` → 0 output, `shellcheck -S warning scripts/tests/plugins/argocd_servicemonitors_ensure.bats` → 0 output, `shellcheck -S error scripts/tests/plugins/argocd_servicemonitors_ensure.bats` → 0 output, `bats scripts/tests/plugins/argocd_servicemonitors_ensure.bats` → `1..2` / `ok 1` / `ok 2`, `yamllint .github/workflows/ci.yml` → 0 output, `./scripts/k3d-manager _agent_audit` exit 0. `git diff --cached --stat` at commit time showed exactly 3 files; pushed to `origin/k3d-manager-v1.16.0`. **Claude still owes the next live hub deploy/rebuild verification** that the 4 argocd ServiceMonitors and image-updater come up from git with no manual remediation.
- [x] `9ec7469b` — **ROUND 2 — hub argocd image-updater live call site + `--no-exit` CRD guard** exactly per the appended Round 2 section in `docs/bugs/2026-07-23-hub-argocd-servicemonitors-and-image-updater-not-enrolled.md` (spec commit `d908ffd0`). Scope held to exactly 2 files: `scripts/plugins/argocd.sh` and `scripts/tests/plugins/argocd_servicemonitors_ensure.bats`. A1 restored the dead-code hunk inside `_argocd_configure_post_deploy` verbatim; A2 added `_argocd_deploy_image_updater` to the LIVE path inside `deploy_argocd_bootstrap` after the two ArgoCD precondition checks and before the AppProject block, with no new `CLUSTER_ROLE` guard; B added `--no-exit` to the ServiceMonitor CRD guard in `_argocd_ensure_servicemonitors` without rewriting the function body; B2 updated both bats stub matchers and both assertions to expect `--no-exit`; B3 added the static `awk` regression test that `deploy_argocd_bootstrap` contains `_argocd_deploy_image_updater`. Gates recorded on this machine with exit codes captured on their own lines: `grep -c '_argocd_deploy_image_updater' scripts/plugins/argocd.sh` → `3`, `RC=0`; `shellcheck -S warning scripts/plugins/argocd.sh` → 0 output, `RC=0`; `shellcheck -S error scripts/plugins/argocd.sh` → 0 output, `RC=0`; `bats scripts/tests/plugins/argocd_servicemonitors_ensure.bats` → `1..3` / `ok 1` / `ok 2` / `ok 3`, `RC=0`; `./scripts/k3d-manager _agent_audit` → `running under bash version 5.3.15(1)-release`, `RC=0`. Pre-commit scope gates: `git status --porcelain` showed only the 2 modified in-scope files, `git diff --cached --name-only` listed only those same 2 files, and `git show --stat 9ec7469b` confirmed exactly 2 files changed (+15/−7). Pushed to `origin/k3d-manager-v1.16.0`; Claude still owes the next live hub deploy/rebuild verification. **Live hub end-state 2026-07-23 already satisfies the assertions** (independent Claude check on `k3d-k3d-cluster`: 4 argocd ServiceMonitors, `argocd-image-updater` 1/1 Running, 40 Applications; image-updater log `applications=0` = INTENDED per removed static enrollment, not the alarm). This is ~11h-old state, so a fresh `make down`/`make up` rebuild-from-git verify is still owed for the "no manual remediation" guarantee.
- [x] `db26dd61` — **argocd dead-path removal follow-up** exactly per `docs/bugs/2026-07-23-argocd-post-deploy-removal-followup.md` (spec commit `e720cf0f`). One-file scope held to `scripts/plugins/argocd.sh` only: removed the orphaned `local enable_vault=1` in `deploy_argocd()` and restored one blank line between `_argocd_configure_vault_eso` and `_argocd_seed_vault_admin_secret()`, without touching `enable_ldap` or any other function/template. Gates recorded on this machine: `grep -c 'enable_vault' scripts/plugins/argocd.sh` → `0`; `shellcheck -S warning scripts/plugins/argocd.sh` → zero warnings; `bash -n scripts/plugins/argocd.sh` → clean; `bats scripts/tests/plugins/argocd_servicemonitors_ensure.bats` → `1..3` / `ok 1` / `ok 2` / `ok 3`; `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release`; pre-commit `git diff --cached --stat` and post-commit `git show --stat db26dd61` each showed exactly one file changed. Pushed to `origin/k3d-manager-v1.17.0`; PR URL not created per repo rule. **Out of scope remains unchanged:** the newly-orphaned cascade (`_argocd_configure_vault_eso`, `virtualservice.yaml.tmpl`, `_argocd_setup_vault_policies`, `_argocd_seed_vault_admin_secret`, and ESO templates) still requires a separate owner decision if removal is ever wanted.
- [x] `843e643a` — **make status login verification** exactly per `docs/bugs/2026-07-23-make-status-login-verification.md` (spec commit `46524697`). One-file scope held to `bin/k3dm-webhook` only: added `_smoke_test_logins` directly above `_smoke_test_services` and appended `results.extend(_smoke_test_logins(provider, app_context))` before `return results`, without changing any existing smoke check. Gates recorded on this machine: `python3 -m py_compile bin/k3dm-webhook` → clean; explicit no-creds runtime check of `_smoke_test_logins("k3s-hostinger", "unused")` returned `('Keycloak login', None, ...)`, `('Frontend login', None, ...)`, `('ArgoCD login', None, ...)`, `('Grafana login', None, ...)` so the new checks render ⚪ rather than false-red when `K3DM_SMOKE_*` vars are unset; `make restart-webhook` exited 0; `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release` and exited 0. Pushed to `origin/k3d-manager-v1.17.0`; PR URL not created per repo rule. **Open operator decision remains unchanged:** wire real `K3DM_SMOKE_*` credentials into the webhook runtime environment separately (the code degrades gracefully to ⚪ until those vars exist).
- [x] `05d74f6c` — **ambient cilium ssh_cmd string→array** exactly per `docs/bugs/2026-07-23-ambient-cilium-ssh-cmd-string-to-array.md` (spec commit `46524697`; clarified `84747f3c`). One-file scope held to `scripts/plugins/shopping_cart.sh` only: in `_ambient_install_cilium`, changed `ssh_cmd` from a string to `local -a` plus an array literal and rewrote the **3 invocation sites** to `"${ssh_cmd[@]}"` (1 declaration + 3 invocations — the earlier "4 call sites" wording was corrected), without changing flags, hostnames, Helm `--set` values, retry logic, or the existing `SC2029` disable. Gates recorded on this machine: `grep -c '\${ssh_cmd}' scripts/plugins/shopping_cart.sh` → `0`; `shellcheck -S warning scripts/plugins/shopping_cart.sh` → zero warnings; `bash -n scripts/plugins/shopping_cart.sh` → clean; `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release`; pre-commit `git diff --cached --stat` and post-commit `git show --stat 05d74f6c` each showed exactly one file changed. Pushed to `origin/k3d-manager-v1.17.0`; PR URL not created per repo rule. **CLAUDE-VERIFIED = PASS (2026-07-23):** SHA on origin, `git show --stat` one file 5/5, diff = exactly the 4 spec changes, `grep -c '\${ssh_cmd}'` → `0` and 3× `"${ssh_cmd[@]}"` present, `shellcheck -S warning` → 0, `bash -n` clean, memory-bank separate commit `7cb1726a` (docs only), exact commit message.
- [x] `cdeebfa6` — **smoke-login credential auto-discovery** exactly per `docs/bugs/2026-07-23-smoke-login-credential-autodiscovery.md` (spec commit `ce3eb1f0`). One-file scope held to `bin/k3dm-webhook` only: added `_smoke_secret` directly above `_smoke_test_logins`, then changed only the ArgoCD block to fall back to `_smoke_secret("cicd", "password", "argocd-initial-admin-secret")` and the Grafana block to fall back to `_smoke_secret("monitoring", "admin-user"/"admin-password", "acg-kube-prometheus-stack-grafana", context=app_context)` when `K3DM_SMOKE_*` env vars are unset. Env still wins; discovery failure still degrades to ⚪. Keycloak + Frontend were deliberately left untouched and env-driven, because the realm end-user password is hash-only and not discoverable from Secrets. Gates recorded on this machine: `python3 -m py_compile bin/k3dm-webhook` → clean; `grep -c '_smoke_secret' bin/k3dm-webhook` → `4`; `make restart-webhook` exited 0; `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release`; pre-commit `git diff --cached --stat` and post-commit `git show --stat cdeebfa6` each showed exactly one file changed. Pushed to `origin/k3d-manager-v1.17.0`; PR URL not created per repo rule. **CLAUDE-VERIFIED = PASS (2026-07-23):** SHA on origin, `git show --stat cdeebfa6` one file 31+/5−, diff = exactly the 3 spec blocks (ArgoCD call has no `context=` → hub; both Grafana calls carry `context=app_context`), Keycloak/Frontend/`_smoke_test_services` untouched, `py_compile` clean, `grep -c '_smoke_secret'` → `4`, exact commit message, memory-bank separate commit `7ae08c40`. **GitGuardian false-positive resolved in `d2452f54`** (config-only): incident `35143325` flagged the literal Grafana Secret name `acg-kube-prometheus-stack-grafana` in the new `_smoke_secret` jsonpath as a Generic Password — it is a `kubectl get secret` argument, not a credential; added secret SHA `39cb7c1d` to `.gitguardian.yaml ignored_matches`, re-scan `a6218453..cdeebfa6` now `0 secrets detected, 1 ignored` (rc=0). Dashboard-side resolve of `35143325` (mark false positive) still owed on the web UI.
- [x] `647b4181` — **seed dedicated smoke user for real Keycloak login check** exactly per the revised `docs/bugs/2026-07-23-smoke-login-keycloak-smoke-user-seed.md` (Path 1, revised spec commit `2bf83d0b`). Follow-on to `cdeebfa6`: closes the Keycloak-side ⚪ while intentionally keeping **Frontend login ⚪** when the smoke-client token is used, to avoid a false red on audience mismatch. Two-file scope held to `scripts/plugins/keycloak.sh` and `bin/k3dm-webhook` only. `scripts/plugins/keycloak.sh` now adds the five smoke defaults after `KEYCLOAK_REALM_DISPLAY_NAME` and inserts five `_keycloak_smoke_*` helpers plus public `keycloak_seed_smoke_user` above `test_keycloak`; `bin/k3dm-webhook` now falls back to seeded Secret `identity/k3dm-smoke-user` (`client_id=k3dm-smoke`) and sets `kc_via_smoke_client`, with the Frontend skip branch preserved by design. Gates recorded on this machine: `shellcheck -S warning scripts/plugins/keycloak.sh` → zero warnings; `python3 -m py_compile bin/k3dm-webhook` → clean; `grep -c 'kc_via_smoke_client' bin/k3dm-webhook` → `3`; `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release`; `make restart-webhook` exited 0; `git show --stat 647b4181` showed exactly two files changed. Pushed to `origin/k3d-manager-v1.17.0`; PR URL not created per repo rule. Known limitation remains from the spec: app-owned realm reconcile could wipe `k3dm-smoke`; rerun `./scripts/k3d-manager keycloak_seed_smoke_user` to restore. **CLAUDE-VERIFIED 2026-07-24:** if-counts `1/1/2/2/1/0/7` all ≤8, allowlist untouched, 2-file scope, gates re-run clean.
- [x] `a5306106` — **GitGuardian FP ignore** for `_keycloak_smoke_write_secret` `--from-file=password="$wd/pword"` temp-file path (incident 35144224). SHA `d34d2aa6…` added to `.gitguardian.yaml`; re-scan clean (`0 detected, 2 ignored`). **Dashboard-resolved 2026-07-24** via API IGNORE (`ignore_reason=false_positive`, status IGNORED).
- [x] **Live smoke test executed 2026-07-24 — Keycloak login ✅, Frontend ⚪ (as designed).** `keycloak_seed_smoke_user` RC=0; authenticated webhook health smoke reported `Keycloak login: token minted (realm=shopping-cart)` and `Frontend login: skipped (smoke-client token not scoped for frontend audience)`. Spec-5 design validated end-to-end.
- [x] `950998aa` — **set required User Profile attrs on the seeded smoke user so Keycloak direct-grant login succeeds** exactly per `docs/bugs/2026-07-23-smoke-login-keycloak-user-profile-attributes.md`. One-file scope held to `scripts/plugins/keycloak.sh` only: `_keycloak_smoke_ensure_user` now creates `k3dm-smoke` with `emailVerified:true`, `email`, `firstName`, `lastName`, and `requiredActions:[]`, and then idempotently repairs those same attributes via `PUT /users/{uuid}` so a user left incomplete by an earlier seed is fixed on the next run. Gates recorded on this machine: `shellcheck -S warning scripts/plugins/keycloak.sh` → zero output; `bash -n scripts/plugins/keycloak.sh` → clean; `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release`; `git show --stat 950998aa` showed exactly one file changed. Pushed to `origin/k3d-manager-v1.17.0`; PR URL not created per repo rule. Live smoke re-verify still pending separately.
- [x] `2a55f0be` — **frontend login smoke now attempts the smoke-client token instead of skipping** exactly per `docs/bugs/2026-07-23-smoke-frontend-login-attempt-not-skip.md`. One-file scope held to `bin/k3dm-webhook` only: replaced the Frontend block so the authed `/api/cart` request is always attempted when `kc_token` exists, grading 2xx→✅, smoke-client 401/403→⚪, and all other HTTP errors→❌. Gates recorded on this machine: `python3 -m py_compile bin/k3dm-webhook` → clean; `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release`; `git show --stat 2a55f0be` showed exactly one file changed. Pushed to `origin/k3d-manager-v1.17.0`; PR URL not created per repo rule. **CLAUDE-VERIFIED = PASS (2026-07-24):** SHA on origin, exact message, diff byte-identical to spec's new block, `py_compile` + `_agent_audit` clean, memory-bank separate commit `d17483a8`. `make restart-webhook` reloaded the plist, and the authed hub health smoke (`GET localhost:7443/api/v1/health`) now reports **`✅ Frontend login — HTTP 200 on /api/cart`** (was ⚪) alongside `✅ Keycloak login` and `✅ ArgoCD login` — the ⚪→✅ flip is proven live.
- [x] **v1.17.0 RELEASE PR MERGED — [#107](https://github.com/wilddog64/k3d-manager/pull/107) → `b5d401b6` (2026-07-24, merged by wilddog64).** Post-merge complete: `enforce_admins` **restored to true** (verified), `required_approving_review_count` untouched at 1 (never lowered — merged via admin override), annotated tag `v1.17.0` → `b5d401b6` pushed, GitHub release published from the CHANGELOG section and marked **Latest**, next branch **`k3d-manager-v1.18.0`** created at `b5d401b6` and checked out locally. Retro: `docs/retro/2026-07-24-v1.17.0-retrospective.md`. Standing-docs audit found two stale files, both fixed on v1.18.0: `memory-bank/projectbrief.md` (bare-metal `k3s` provider was missing from the table; `k3s-oci` was labelled Active despite being de-scoped) and `.github/copilot-instructions.md` (Keycloak plugin + `keycloak_seed_smoke_user` undocumented; added the smoke-login grading contract and a `base64 --decode` review rule). `docs/api/functions.md` was already current. Branch cleanup skipped (v1.17.0 is not a 5-release multiple). Original PR detail below.
- [x] **v1.17.0 release PR detail — [#107](https://github.com/wilddog64/k3d-manager/pull/107)** `feat: v1.17.0 real login verification in health smoke`, branch `k3d-manager-v1.17.0` → `main`. Pre-flight gaps Claude closed before opening: CHANGELOG `[Unreleased]` was **empty** (wrote the full v1.17.0 entry, then stamped `## [1.17.0] - 2026-07-24` in `c53cafbd`), and the new public `keycloak_seed_smoke_user` was **unregistered** in `docs/api/functions.md` + the README plugins table (`50b1a7c7`). README releases table rotated to the 3 most recent (v1.14.0 → `<details>`), `docs/releases.md` row added. **All checks GREEN** on `0e6c6b15`: lint, detect, CodeQL ×3, GitGuardian. **Copilot: 1 finding, fixed + thread resolved** — `base64 -d` portability; fix `3be18f1d` normalizes all 7 sites in `keycloak.sh` to `--decode`. Verified Copilot's premise was wrong on both counts (stock macOS `base64 -d` works; `-D` is *invalid* under GNU so the suggested `--decode || -D` fallback would break on Linux, and it is unsound in a pipeline anyway) — `--decode` alone is accepted by both. **GitGuardian was FAILING (merge-blocking)** with 3 "secrets uncovered" while local `ggshield` reported 0 detected: `.gitguardian.yaml` `ignored_matches` silence only the *local* CLI, server-side incidents stay OPEN. Resolved 35143325 / 35142552 / 35142551 via `POST /v1/incidents/secrets/<id>/ignore` (`false_positive`, HTTP 200 each; all 4 incl. 35144224 verified `status=IGNORED`) → check flipped fail→pass. Findings doc `docs/issues/2026-07-24-copilot-pr107-review-findings.md` (`0e6c6b15`), README Issue Logs trimmed to 5. `enforce_admins` **disabled** by Claude. **STILL BLOCKED:** `required_approving_review_count=1` and the 2 PR reviews are Copilot *comments*, not approvals — author cannot self-approve. Owner decision needed (temporarily set count to 0, or get an approval); `/post-merge` Step 2 restores it to 1.
- [x] `9fde2c43` — **v1.18.0 trivy reconcile panel query now matches real operator error lines** exactly per `docs/bugs/2026-07-24-trivy-reconcile-panel-query-matches-zero-lines.md`. Two-file scope held to `scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml` and `scripts/tests/plugins/trivy_operator_observability.bats` only: panel 12 now filters on `level="error"` and uses the parser’s real `line_format` field names (`job_extracted`, `container_extracted`, `status_reason`), and the BATS suite now pins the new filter plus a disappearance gate for `msg=\"Reconciler error\"`. Gates recorded on this machine: `grep -c 'msg=\\"Reconciler error\\"' scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml` → `0`; `grep -c 'controller=\\"job\\"' scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml` → `0`; `shellcheck -S warning scripts/tests/plugins/trivy_operator_observability.bats` → zero output; `bats scripts/tests/plugins/trivy_operator_observability.bats` → `1..6` with `ok 1` through `ok 6`; `python3 -c "import yaml,sys; yaml.safe_load(open('scripts/etc/argocd/platform-ops/grafana-dashboard-argocd.yaml'))"` → clean; embedded dashboard JSON parse via `json.loads(...)` → clean; `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release`. Pushed to `origin/k3d-manager-v1.18.0`; PR URL not created per repo rule.
- [x] **CLAUDE-VERIFIED `9fde2c43` = PASS (2026-07-24).** Both `9fde2c43` and memory-bank `05773ecd` confirmed on `origin/k3d-manager-v1.18.0` via `git branch -r --contains`. Independently re-ran every gate here: both DoD greps → `0`, BATS **6/6 `bats rc=0`**, `shellcheck -S warning` rc=0, YAML + embedded JSON parse, panel 12 `title`/`id`/`type`/`gridPos` unchanged, code commit exactly 2 files (+5/−2), memory-bank commit memory-bank-only, tree clean, local HEAD == origin. **Live-proven against `loki-gateway.monitoring.svc`:** new expr returns rows where the old returned `[]`; stream labels confirm the rename trap (`job=fluent-bit`/`container=trivy-operator` vs `job_extracted=trivy-system/scan-vulnerabilityreport-dd796cffd`/`container_extracted=grafana`). **Deviation (cosmetic, not amended):** Codex committed only the subject line of the spec's exact commit message — the 4-line body is missing.
- [x] `f428225f` — **v1.18.0 trivy chart, operator, and scanner versions are now pinned explicitly everywhere** exactly per `docs/bugs/2026-07-24-trivy-version-pins-not-explicit-everywhere.md`. Five-file scope held to `scripts/etc/argocd/applicationsets/observability.yaml`, `scripts/etc/argocd/applicationsets/observability-acg.yaml`, `scripts/etc/helm/observability/trivy-operator-values.yaml`, `scripts/etc/helm/observability/trivy-operator-acg-values.yaml`, and `scripts/tests/plugins/trivy_operator_observability.bats` only: both application sets now pin chart `0.34.0`; both values files pin operator `0.32.0` plus scanner `trivy.image.tag` `0.72.0`; and the BATS suite now covers the new chart/operator/scanner pins for both hub and ACG variants with 7 tests total. Gates recorded on this machine: `grep -c 'targetRevision: 0.33.2'` → `0` in both appsets; `grep -c 'targetRevision: 0.34.0'` → `1` in each appset; `grep -c '0.31.2'` → `0` in both values files; `grep -c 'tag: "0.32.0"'` → `1` in each values file; `grep -c 'tag: "0.72.0"'` → `1` in each values file; `shellcheck -S warning scripts/tests/plugins/trivy_operator_observability.bats` → zero output; `bats scripts/tests/plugins/trivy_operator_observability.bats` → `1..7` with `ok 1` through `ok 7`; the four YAML files parse cleanly; `diff` between the two values files shows exactly one hunk (`release: kube-prometheus-stack` vs `release: acg-kube-prometheus-stack`); and `./scripts/k3d-manager _agent_audit` printed `running under bash version 5.3.15(1)-release`. Pushed to `origin/k3d-manager-v1.18.0`; PR URL not created per repo rule.
- [x] **CLAUDE-VERIFIED `f428225f` = PASS (2026-07-24).** Both `f428225f` and memory-bank `62b5e74e` confirmed on `origin/k3d-manager-v1.18.0` via `git branch -r --contains`. Scope exactly five files (`39 insertions(+), 8 deletions(-)`); all four config diffs byte-exact against the spec; the three BATS edits match with 5c placed after test 2. Every gate re-run here: **BATS `1..7`, `ok 1`–`ok 7`, `bats rc=0`** (exit code on its own line), `shellcheck` rc=0, the five DoD grep counts `0/0, 1/1, 0/0, 1/1, 1/1`, four YAML parses, values files differ on exactly one hunk (line 45), `_agent_audit` rc=0, tree clean, HEAD == origin. Both new tags re-verified on `mirror.gcr.io`: `trivy-operator:0.32.0` and `trivy:0.72.0` → **HTTP 200**. **Commit body present this time** (the `9fde2c43` deviation is fixed). **Reporting weakness:** Codex quoted `_agent_audit`'s stdout instead of its exit code — Claude confirmed rc=0 independently; future specs should demand the literal `rc=<n>` line.
- [x] **Live rollout of the trivy version bump — DONE on the hub 2026-07-24 (Claude).** Applied the `observability` appset only (not `deploy_observability`, which also touches Vault, `alertmanager-smtp-secret`, PrometheusRules, and deletes an Application). Pre-apply normalized diff showed exactly two changed lines and an unchanged Application name template — no cascade-delete risk. Live now: chart `trivy-operator-0.34.0`, operator `0.32.0`, `trivy-server` + scan jobs `trivy:0.72.0`; `kube-prometheus-stack` and `hub-loki` stayed `Synced`/`Healthy` (their values files are byte-identical across the two branches).
- [x] **Grafana image scan failure — RESOLVED by the trivy 0.72.0 scanner upgrade (2026-07-24).** `scan-vulnerabilityreport-dd796cffd`, previously stuck in `Error` with `open /tmp/trivy-7/cached-file-...: no such file or directory`, now completes in 69s. All six scan jobs `Complete`, zero operator `level=error` lines since the bump, and the Grafana VulnerabilityReport exists (scanner `Trivy 0.72.0`, `grafana/grafana 11.4.0`, **8 critical / 46 high**). The version-pin spec listed this as unknown/out-of-scope; it fixed itself. Supersedes the open item below. **Follow-on: 8 critical CVEs on `grafana/grafana 11.4.0` are now visible and unaddressed.**
- [x] **Hub values-branch pin — OWNER DECISION MADE (2026-07-24): keep tracking a release branch, not `main`** ("we always develop and test on release branch"). Pin moved `k3d-manager-v1.16.0` → `k3d-manager-v1.18.0`. **Consequence to carry into every release: bumping this pin and reapplying the appsets is now a required release step**, otherwise landed config changes stay inert on the cluster.
- [x] `3e847b26` — **v1.18.0 appset values-branch drift detector is DONE** exactly per `docs/bugs/2026-07-24-appset-values-branch-drift-undetected.md` (spec `7c6dff6b`, refresh `9d63789d`). Three-file scope held to `scripts/plugins/argocd.sh`, new suite `scripts/tests/plugins/argocd_values_branch_drift.bats`, and `docs/api/functions.md` only: `argocd_check_values_branch` and `_argocd_values_branch_drift` were appended at the end of `argocd.sh` with 3-space indentation, the unreadable-cluster branch uses `_warn` and returns `2`, the parser keeps the `ValueError` guard and prints `checked ... values references` to stderr, the new BATS suite passes **`1..5`**, and the public function is registered exactly once in `docs/api/functions.md`. Gates recorded on this machine: baseline `shellcheck -S warning scripts/plugins/argocd.sh` → zero output before edits and zero output after; `grep -c 'function argocd_check_values_branch' scripts/plugins/argocd.sh` → `1`; `grep -c 'function _argocd_values_branch_drift' scripts/plugins/argocd.sh` → `1`; `grep -c '\`argocd_check_values_branch\`' docs/api/functions.md` → `1`; `bash -n scripts/plugins/argocd.sh` → clean; `bats scripts/tests/plugins/argocd_values_branch_drift.bats` → `1..5` with `ok 1` through `ok 5`; `bats scripts/tests/plugins/argocd.bats scripts/tests/plugins/argocd_servicemonitors_ensure.bats` → `1..18` with all 18 tests `ok`; staged-file gate `git diff --cached --name-only` listed exactly the 3 target files; `./scripts/k3d-manager _agent_audit` on the staged diff → `rc=0`. Pushed to `origin/k3d-manager-v1.18.0`; PR URL not created per repo rule.
- [x] **ACG/hostinger half of the values-branch rollout — DONE 2026-07-24 (Claude).** Applied `observability-acg.yaml` with `APP_CLUSTER_NAME=ubuntu-hostinger` after the same pre-apply diff protocol (two lines, name template unchanged, other two values files byte-identical across branches). All six hub Applications now on `k3d-manager-v1.18.0` — zero drift. Live on hostinger: chart `0.34.0`, operator `0.32.0`, scanner `0.72.0`, zero operator errors, all apps `Synced`/`Healthy`.
- [ ] **Hostinger rescan backlog — capacity, not config; no spec filed.** The upgrade's full rescan wedged 8 scan jobs at `Pending`/`Insufficient cpu`. Node is **2 cores, 1910m requested (95%)** vs 20% actual usage; a 3-container scan job needs 150m and ~90m is free. **33 of 35 VulnerabilityReports are still on Trivy 0.71.1.** Duplicate of the documented hostinger oversubscription (`7345b24a`); residual is node size. Decide: accept slow rescan, trim scan-job requests below 50m, or grow the node.
- [ ] **`loki` → `acg-loki` rename — ASSIGNED TO CODEX 2026-07-24.** `docs/bugs/2026-07-24-observability-acg-loki-app-name-inconsistent.md`. Codex edits `observability-acg.yaml` (rename + hub-style `releaseName` conditional) + a 4-test bats; committed YAML is inert until reapplied so Codex's part is safe. The finalizer-strip + reapply ([[reference_preserveresourcesondeletion_rename_trap]] cascade risk) is Claude's live step. Pre-validated (yaml parse raw+rendered, grep gates).
- [x] `c704d669` — **v1.18.0 `make status` now reports ApplicationSet values-branch drift** exactly per `docs/bugs/2026-07-24-make-status-values-branch-drift-wiring.md`. Two-file scope held to `bin/cluster-status` and new suite `scripts/tests/bin/cluster_status_values_branch.bats` only: `cluster-status` now adds `=== ArgoCD Values-Branch Drift (${INFRA_CONTEXT}) ===` between the ApplicationSets and Image Updater sections, invokes `argocd_check_values_branch` via `"${REPO_ROOT}/scripts/k3d-manager"` instead of sourcing `argocd.sh`, captures `2>&1`, preserves the `|| _vb_rc=$?` guard so `set -e` cannot abort on drift/unreadable, filters the dispatcher banner with `grep -vF 'running under bash version' || true`, and reports clean / drift / unreadable / unexpected exits exactly as specified. Gates recorded on this machine: baseline `shellcheck -S warning bin/cluster-status` → zero output before edits and zero output after; `grep -cF '=== ArgoCD Values-Branch Drift' bin/cluster-status` → `1`; `grep -cF 'argocd_check_values_branch' bin/cluster-status` → `1`; `bash -n bin/cluster-status` → clean; `shellcheck -S warning scripts/tests/bin/cluster_status_values_branch.bats` → zero output; `bats scripts/tests/bin/cluster_status_values_branch.bats` → `1..5` with `ok 1` through `ok 5`; `bats scripts/tests/bin/cluster_status_image_updater.bats scripts/tests/bin/cluster_status_observability.bats` → `1..11` with all 11 tests `ok`; staged-file gate `git diff --cached --name-only` listed exactly `bin/cluster-status` and `scripts/tests/bin/cluster_status_values_branch.bats`; `./scripts/k3d-manager _agent_audit` on the staged diff → `rc=0`. Pushed to `origin/k3d-manager-v1.18.0`; PR URL not created per repo rule.
- [ ] ~~**Hub ArgoCD values source pinned to a stale branch**~~ — the live `trivy-operator` Application reads its values from `targetRevision: k3d-manager-v1.16.0`, two releases behind, so repo-only config changes reach no cluster until the appsets are reapplied with a current `K3D_MANAGER_BRANCH`. Surfaced while scoping the version-pin spec; recorded in that spec's Out-of-Scope. **Unspecced** — needs an owner decision on whether the appset values ref should track `main` instead of a release branch.
- [ ] **`platform-ops` namespace absent on both clusters — "App CVE Scan Decisions" panel legitimately empty.** `get ns platform-ops` → `NotFound` on `k3d-k3d-cluster` **and** `ubuntu-hostinger`. Deployed only by the manual one-shot `deploy_argocd_platform_ops` (`scripts/plugins/argocd.sh:1342`), wired into no ArgoCD app and no bootstrap path, so it disappears on every hub rebuild. **Owner decision needed: re-run manually, or fold into bootstrap.** Not yet specced. (Note: the two `0` stat panels above it prove nothing — both exprs end in `or vector(0)`.)
- [x] `8537f27e` — **v1.18.0 bootstrap now deploys platform-ops so the CVE tier and dashboards survive a hub rebuild** exactly per `docs/bugs/2026-07-24-platform-ops-not-deployed-on-rebuild-cve-dashboard-nodata.md`. Two-file scope held to `scripts/plugins/argocd.sh` and new suite `scripts/tests/plugins/argocd_platform_ops_bootstrap.bats` only: `deploy_argocd_bootstrap` now invokes `deploy_argocd_platform_ops` immediately after `_argocd_deploy_image_updater` and keeps the required `|| _warn ...` guard so platform-ops errors cannot abort bootstrap under `set -e`; the new BATS suite pins both the presence of the call and the guard. Gates recorded on this machine: baseline `shellcheck -S warning scripts/plugins/argocd.sh` → zero output before edits and zero output after; `bash -n scripts/plugins/argocd.sh` → clean; `awk '/^function deploy_argocd_bootstrap\(\)/,/^}$/' scripts/plugins/argocd.sh | grep -c 'deploy_argocd_platform_ops'` → `1`; `bats scripts/tests/plugins/argocd_platform_ops_bootstrap.bats` → `1..2` with `ok 1` and `ok 2`; `bats scripts/tests/plugins/argocd_servicemonitors_ensure.bats` → `1..3` with all 3 tests `ok`; staged-file gate `git diff --cached --name-only` listed exactly `scripts/plugins/argocd.sh` and `scripts/tests/plugins/argocd_platform_ops_bootstrap.bats`; `./scripts/k3d-manager _agent_audit` on the staged diff → `rc=0`. Pushed to `origin/k3d-manager-v1.18.0`; PR URL not created per repo rule.
- [ ] **Grafana image scan failing in trivy-operator** — `scan-vulnerabilityreport-dd796cffd` in `Error`: `unable to open usr/share/grafana/bin/grafana: failed to open the temp file: open /tmp/trivy-7/cached-file-...: no such file or directory` (likely client/server cache or `/tmp` sizing). The panel-query spec above only makes this **visible**; fixing it is separate. Unassigned.
- [x] `43e63b53` — **v1.18.0 `app-cve-scan` now matches VulnerabilityReports on the registry-less repository path** exactly per `docs/bugs/2026-07-18-app-cve-scan-report-repository-registry-prefix-mismatch.md` (spec retargeted to v1.18.0 in `992691d8`). Two-file scope held to `scripts/etc/argocd/platform-ops/app-cve-scan.sh` and `scripts/tests/plugins/app_cve_scan.bats` only: `_find_report_row` now compares both the fully-qualified repo and the registry-less trivy-operator form by deriving `${_repo#*/}` and keeping the exact-match `awk`; the main loop now increments `_skipped` on misses, `_matched` on hits, logs the full repo on a miss, and exits `1` with the required `FATAL: ... ZERO VulnerabilityReports matched.` block when nothing matched. The BATS suite now covers the real registry-less fixture (`wilddog64/shopping-cart-basket`) and the all-skipped fatal path. Gates recorded on this machine: baseline `shellcheck -S warning scripts/etc/argocd/platform-ops/app-cve-scan.sh` → zero output before edits and zero output after; `sh -n scripts/etc/argocd/platform-ops/app-cve-scan.sh` → clean; `grep -c '\${_repo#\*/}' scripts/etc/argocd/platform-ops/app-cve-scan.sh` → `1`; `grep -c 'ZERO VulnerabilityReports matched' scripts/etc/argocd/platform-ops/app-cve-scan.sh` → `1`; `grep -c 'curl' scripts/etc/argocd/platform-ops/app-cve-scan.sh` → `0`; `grep -c 'CURL_LOG' scripts/tests/plugins/app_cve_scan.bats` → `0`; `grep -c 'WGET_LOG' scripts/tests/plugins/app_cve_scan.bats` → `17`; `bats scripts/tests/plugins/app_cve_scan.bats` → `1..9` with all 9 tests `ok`; network-isolation gate printed `0`; mutation check A (`${_repo#*/}` → `${_repo##*/}`) failed exactly on the new registry-less test; mutation check B (remove `--header="Content-Type: application/json"`) failed exactly on the dispatch test; staged-file gate `git diff --cached --name-only` listed exactly `scripts/etc/argocd/platform-ops/app-cve-scan.sh` and `scripts/tests/plugins/app_cve_scan.bats`; `./scripts/k3d-manager _agent_audit` on the staged diff → `rc=0`. Pushed to `origin/k3d-manager-v1.18.0`; PR URL not created per repo rule. Claude still owns the live hub re-trigger. **CLAUDE-VERIFIED = PASS (2026-07-24):** every gate re-run independently (all green), mutation check test-4 FAILS under `##*/` then restored, `_agent_audit` replayed staged→rc=0, and the fix PROVEN against LIVE hostinger reports — `${_repo#*/}` equals trivy-operator's `.report.artifact.repository` for all 5 services. **Live end-to-end CronJob run BLOCKED: `platform-ops` ns absent (see above) + needs `platform-ops-app-rebuild` gh-token.**
- [x] `191d9f4a` — **v1.18.0 `app-cve-scan` now authenticates GHCR reads with `GH_TOKEN` so private packages are visible to the scanner** exactly per `docs/bugs/2026-07-25-app-cve-scan-anonymous-ghcr-read-fails-private-packages.md`. One-file scope held to `scripts/etc/argocd/platform-ops/app-cve-scan.sh` only: `_ghcr_bearer_token` now writes `Authorization: Basic <base64(owner:token)>` into a `0600` wgetrc and uses `--config` when `GH_TOKEN` is set, then removes that temp file and preserves the anonymous fallback (`if [ ! -s "${_token_file}" ]; then ...`) so public packages still resolve without a token. Gates recorded on this machine: baseline `shellcheck -S warning scripts/etc/argocd/platform-ops/app-cve-scan.sh` → zero output before edits and zero output after; `sh -n scripts/etc/argocd/platform-ops/app-cve-scan.sh` → clean; post-state `grep -c 'Authorization: Basic' scripts/etc/argocd/platform-ops/app-cve-scan.sh` → `1`; fallback presence gate `grep -n 'if \[ ! -s "${_token_file}" \]; then' scripts/etc/argocd/platform-ops/app-cve-scan.sh` → line `177`; staged-file gate `git diff --cached --name-only` listed exactly `scripts/etc/argocd/platform-ops/app-cve-scan.sh`; `/opt/homebrew/bin/bash ./scripts/k3d-manager _agent_audit` on the staged diff → `rc=0`; `git show --stat 191d9f4a` shows exactly one file changed. Pushed to `origin/k3d-manager-v1.18.0`; PR URL not created per repo rule. Claude still owns live verification with a real `read:packages` PAT in `platform-ops/platform-ops-app-rebuild`, and mutable `:latest` vs `sha-*` promotion remains out of scope. **LIVE-VERIFY 2026-07-25 = PASS on mechanism (hub `k3d-k3d-cluster`):** replicated `_ghcr_bearer_token`'s auth flow against real ghcr with my `gho_` token (`read:packages`), token kept out of argv via a `0600` curl `-K` config — anonymous token endpoint → 401/403 + empty tags for all 5 private `shopping-cart-*` packages, Basic-auth → 200 + tags 11/23/49/24/5; `base64` present in `aquasec/trivy:0.63.0` (`/bin/base64`). **Deploy still pending (operational, not code):** live ConfigMap `argocd-cve-scan-script` still carries the OLD script (`grep -c 'Authorization: Basic'` → `0`) — inert until ArgoCD syncs, NOT synced because branch is unmerged; and in-cluster `gh-token` is still the `PLACEHO…` placeholder — needs a real `read:packages` PAT before the synced scan authenticates.
- [x] `989dc8e4` (k3d-manager) + `1100528` (shopping-cart-infra) — **LDAP federation now self-heals on rebuild, fixing the durable half of the 2026-07-24 login outage** exactly per `docs/bugs/2026-07-24-keycloak-ldap-federation-missing-on-rebuild-login-broken.md` (Changes 1 + 2 only; Change 3 remains owner-gated and OUT OF SCOPE). Scope held to exactly one file per repo: `bin/cluster-up` and `shopping-cart-infra/identity/keycloak/keycloak-reconcile-hook-job.yaml`. `bin/cluster-up` Step 10d.6 now creates the missing LDAP `UserStorageProvider` from the canonical repo config using the live bind credential passed via `env`, then triggers full sync; the shopping-cart-infra PostSync hook now appends `|| true` to the empty-component `ldap_id` pipeline and replaces the dead `skipping mapper setup` branch with create-then-re-resolve so the existing mapper + sync path runs. Gates recorded on this machine: baseline `shellcheck -S warning bin/cluster-up` → zero output before edits and zero output after; `bash -n bin/cluster-up` → clean; `grep -c 'creating it from canonical config' bin/cluster-up` → `1`; extracted hook script `bash -n` → clean; `grep -c 'skipping mapper setup' identity/keycloak/keycloak-reconcile-hook-job.yaml` → `0`; hook presence gate `grep -n 'sed .*|| true'` shows the guarded `ldap_id` line at `182` (and the re-resolve at `207`); staged-file gate `git diff --cached --name-only` on k3d-manager listed exactly `bin/cluster-up`; `/opt/homebrew/bin/bash ./scripts/k3d-manager _agent_audit` on the staged diff → `rc=0`; `git show --stat` shows exactly one file changed in each repo. Pushed to `origin/k3d-manager-v1.18.0` and `origin/feat/keycloak-ldap-federation-self-heal`; PR URLs not created per repo rule. Claude still owns the next live rebuild verification.
- [x] `96c9850` (shopping-cart-infra) — **browser-flow Option B fix now removes the invalid top-level OTP subflow so login flow structure stays valid on rebuild** exactly per `shopping-cart-infra/docs/bugs/2026-07-25-keycloak-browser-flow-toplevel-conditional-otp-breaks-login.md`. Scope held to exactly one file in shopping-cart-infra: `identity/keycloak/keycloak-reconcile-hook-job.yaml`. The reconcile hook no longer creates `otp-conditional-subflow`, no longer writes `platform-mfa-role-condition`, and leaves `browser-with-conditional-otp` as a plain copy of `browser` before activating it as the realm `browserFlow`. Gates recorded on this machine: `grep -c 'otp-conditional-subflow' identity/keycloak/keycloak-reconcile-hook-job.yaml` → `0`; `grep -c 'platform-mfa-role-condition' identity/keycloak/keycloak-reconcile-hook-job.yaml` → `0`; extracted embedded script `bash -n` → clean; `git push origin fix/keycloak-browser-flow-drop-custom-otp-subflow` succeeded. PR URL not created per repo rule. **CLAUDE LIVE-VERIFIED 2026-07-25** on `k3d-k3d-cluster`: clean rebuild via the new-hook `browser/copy` path shows level 0 = ALTERNATIVE/DISABLED only (no top-level CONDITIONAL, `otp-conditional-subflow` gone); real `admin`/`Shopping1!` PKCE login (`frontend` client) → HTTP 302 with auth `code`. shopping-cart-infra memory-bank authored by Claude in separate commit `7d29bcc` (Codex had skipped it). Full DoD met; awaiting PR decision only.
- [ ] **Trivy reconcile panel backport → live (v1.16.0)** — panel "No data" on live hub is appset branch drift: query fix `9fde2c43` on v1.18.0, but `grafana-dashboards-hub` appset frozen to `k3d-manager-v1.16.0`. Spec `docs/bugs/2026-07-25-trivy-reconcile-panel-backport-to-live-v1.16.0.md` = cherry-pick `9fde2c43` onto v1.16.0 (clean). Codex handoff prepared, not dispatched; Claude owns live sync + verify.
- [x] **Dependabot #3 (brace-expansion DoS, `GHSA-3jxr-9vmj-r5cp`) — FIXED + subtree-pulled into k3d-manager** lib-foundation PR #39 `https://github.com/wilddog64/lib-foundation/pull/39` MERGED 2026-07-25 (merge `14c79082`, released v0.4.8: tag `v0.4.8`→`14c79082` + GitHub release Latest). Bump `scripts/lib/acg/package-lock.json` brace-expansion 1.1.15→1.1.16 (registry-authentic hash, dep shape unchanged). CVE lives in lib-foundation's NATIVE acg module (absorbed since v0.4.0) — standalone lib-acg repo is legacy, NOT touched; corrected stale [[feedback_lib_acg_subtree_discipline]]. **Subtree pull DONE (2026-07-25):** `git subtree pull --prefix scripts/lib/foundation lib-foundation main --squash` landed on `k3d-manager-v1.18.0` as merge `79294e89` (squash `2d33100e`, `21fdb9b5..14c79082`); CLAUDE-VERIFIED: `packages['node_modules/brace-expansion'].version` in `scripts/lib/foundation/scripts/lib/acg/package-lock.json` → **1.1.16**. **Dependabot alert #3 clears when v1.18.0 merges to main** (Dependabot scans the default branch; the feature-branch push does not close it yet). NOTE: `npm audit` also flags a separate unfiled advisory `GHSA-mh99-v99m-4gvg` (`<=5.0.7`) needing a major bump the `jest→glob@7→minimatch@3` chain can't take — out of scope.
- [ ] **`app_cve_scan.bats` stubs curl not wget** (post-`89c2efd6` this is covered by the wget harness — but the standalone `docs/bugs/2026-07-18-app-cve-scan-bats-stubs-curl-not-wget.md` is SUPERSEDED; do not implement separately).
- [ ] **BLOCKED — Hub `environment=infra` registration** — `docs/bugs/2026-07-18-hub-infra-registration-blocked-platform-helm-selfheal.md`. DO NOT EXECUTE (platform-helm selfHeal → 2nd argo-cd release + downgrade). Owner decision (options A–D).
- [ ] **Triage — hub `argocd` Helm release `failed`** (rev 3, 2026-06-29). Blocks infra-registration options B/C.
- [ ] **Makefile stale ACG sandbox URL default — ASSIGNED TO CODEX 2026-07-24.** `docs/bugs/2026-07-19-makefile-stale-acg-sandbox-url-default.md` (spec retargeted to v1.18.0 in `992691d8`; handoff block issued). Separate Codex session. Live CDP verification against a real sandbox is Claude's step.
- [ ] **app-cluster Vault portability** — `docs/bugs/2026-07-07-app-cluster-vault-portability.md` (hub-vault-profile global-file leak; `_hub_vault_profile_context()` returns empty on k3s-aws path). Unassigned.

## Releases
- [x] **lib-foundation v0.4.6** (2026-07-21): PR #37 → `main` `db336a6f`; stamp `ae4616f`, annotated tag `v0.4.6`→`ae4616f`, GitHub release Latest (acg_restart entrypoint + playwright-artifacts sweep; v0.4.5 folded in).
- [x] **lib-foundation v0.4.7** (2026-07-23): PR #38 → `main` `a36cf79` (acg_check_ttl set -e fix); stamp `21fdb9b`, annotated tag `v0.4.7`→`21fdb9b`, GitHub release Latest. Subtree-pulled into `k3d-manager-v1.16.0` (v0.4.7 IDENTICAL to vendored acg.sh); v1.16.0 ships with it.
- [x] **lib-foundation v0.4.8** (2026-07-25): PR #39 → `main` `14c79082` (brace-expansion 1.1.16 CVE fix, Dependabot #3); annotated tag `v0.4.8`→`14c79082`, GitHub release Latest; next branch `feat/v0.4.9` cut. Subtree-pulled into `k3d-manager-v1.18.0` (merge `79294e89`); v1.18.0 ships 1.1.16.
- [x] v1.15.0 (2026-07-14): PR #105 → `main` `14cdaea6`. Post-merge tag/release/retro deferred.
- [x] v1.14.0 (2026-07-12): PR #104 → `main` `5c412e15`. Tag + release + retro done.
