# Progress — k3d-manager

## Status (v1.7.1 — Active; v1.7.0 RELEASED 2026-06-13)

- **VERIFIED + FOLLOW-UP SPEC → CODEX (2026-06-25):** Verified `c9e10fb4`/`6f55195d`/`f7351e71` on origin (scope exact, shellcheck clean, bats 40/40 + 13/13, `_agent_audit` 0). Code faithful, but Change 4's routing through `register_app_cluster` dropped Hostinger TLS to `insecure:true` (live public cluster) and made the refresh depend on the `argocd-manager` SA on Hostinger. Follow-up spec `docs/bugs/2026-06-25-hostinger-argocd-registration-secure-tls-and-sa.md` (CA-verified TLS via optional `ARGOCD_APP_CLUSTER_CA_DATA`; ensure SA via `scripts/etc/argocd-manager.yaml`; insecure default → false). Commit `fix(hostinger): CA-verified TLS + ensure argocd-manager SA for app-cluster registration`. Handed to Codex. Operator-pending: green1 Part A live `helm -n vclusters uninstall green1`.
- **COMPLETE (2026-06-25):** Spec A Part B and Spec B Part B are complete on `feat/v1.8.0-acg-absorb-phase2-agy`: commit `c9e10fb4` (`fix(hostinger): relabel app-cluster role on hub + retire stale preflight stack`) added `_argocd_hub_kubectl_cmd` in `scripts/plugins/argocd.sh`, moved Hostinger app-cluster relabeling onto the hub context, and rerouted Hostinger registration through `register_app_cluster` so `cluster-ubuntu-hostinger` carries `environment`, `argocd-chart-version`, and `argocd-replicas`; commit `6f55195d` (`fix(vcluster): deregister preflight vcluster from hub ArgoCD on destroy`) added `_vcluster_deregister_from_hub` plus the `DRY_RUN` notice and calls it before host-side `vcluster delete`. Tests added in `scripts/tests/lib/provider_contract.bats` and `scripts/tests/plugins/vcluster.bats`. Validation passed with `shellcheck -S warning scripts/plugins/argocd.sh scripts/lib/providers/k3s-hostinger.sh scripts/tests/lib/provider_contract.bats`, `bats scripts/tests/lib/provider_contract.bats` (`1..40`, all pass), `shellcheck -S warning scripts/plugins/vcluster.sh scripts/tests/plugins/vcluster.bats`, `bats scripts/tests/plugins/vcluster.bats` (`1..13`, all pass), and `./scripts/k3d-manager _agent_audit`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy` (no PR created).
- **D2 SPEC WRITTEN (2026-06-25):** Remaining Hostinger red is a **node CPU ceiling**, not wiring — `ubuntu-hostinger` is 2 CPU/96% requested and the **orphaned green1 vcluster** (helm `green1`, vcluster-0.32.1, 8d, `vclusters` ns) holds ~470m. Cutover removed green1 from the hub but not the host Helm release; `vcluster_destroy` only does the host delete, never the hub deregister. Spec `docs/bugs/2026-06-25-retire-orphaned-green1-vcluster-host-cpu-ceiling.md`: Part A live `helm -n vclusters uninstall green1` (frees CPU → data layer schedules → frontend 200); Part B extends `vcluster_destroy` with `_vcluster_deregister_from_hub`, commit `fix(vcluster): deregister preflight vcluster from hub ArgoCD on destroy`. SUPERSEDES cutover Change 2; corrected cutover Change 4 (register_app_cluster already stamps the labels).
- **PART A DONE + PART B → CODEX (2026-06-25):** Executed the live hub runbook — role label moved to `cluster-ubuntu-hostinger`, cleared from `cluster-ubuntu-k3s` (ACG secret preserved); `data-git`/`services-git`/`eso`/`observability-acg` Synced on `ubuntu-hostinger`; green1 preflight retired (appsets + child apps + orphan deleted, `cluster-green1` deleted). **Grafana 502 RESOLVED** (`grafana.3ai-talk.org`=200, `localhost:3001` listening). Discovered: (D1) `platform-helm` selects `environment` label, not role → hostinger secret missing `environment`/`argocd-chart-version`/`argocd-replicas` → folded into Part B Change 4; (D2) minio/postgresql-orders/payment Pending on **node CPU ceiling** → frontend still 502, Hostinger VM capacity follow-up (not Part B). Part B (Changes 1–4) handed to Codex on `feat/v1.8.0-acg-absorb-phase2-agy`. Spec: `docs/bugs/2026-06-25-hostinger-cutover-app-cluster-role-and-green1-retire.md`.
- **SPEC WRITTEN (2026-06-25) — superseded above:** Root-caused the still-red Hostinger services (Frontend/Grafana/Product-images 502, postgresql-orders/payment not ready). Live hub (`k3d-k3d-cluster`/`cicd`) verified: (1) `k3d-manager/role: app-cluster` stuck on dead `cluster-ubuntu-k3s` not `cluster-ubuntu-hostinger`; (2) `green1-preflight-*` stack pins deleted branch `k3d-manager-v1.7.1` (`ComparisonError`); (3) CODE — Hostinger refresh relabel runs against app context not hub, `|| true` hides failure. Grafana 502 is downstream (grafana only in green1; dead `grafana-port-forward` LaunchAgent → no `localhost:3001` → cloudflared 502). User decision: cut over to `ubuntu-hostinger`, **preserve ACG `ubuntu-k3s` secret**. Spec `docs/bugs/2026-06-25-hostinger-cutover-app-cluster-role-and-green1-retire.md` (Part A live operator runbook, Part B Codex code).
- **PARTIAL (2026-06-25):** `fix(hostinger)` commit `1a7ddbc4` fixes the Hostinger refresh/status/app-cluster drift: `scripts/lib/providers/k3s-hostinger.sh` now reassigns the active ArgoCD app-cluster role to `ubuntu-hostinger`, refreshes provider-specific Grafana/Pushgateway launchd assets, and bootstraps the Hostinger `vault-backend` bridge/store path; `scripts/plugins/shopping_cart.sh` now resolves the active app-cluster context instead of hardcoding `ubuntu-k3s`; `scripts/etc/argocd/projects/platform.yaml.tmpl` now admits Hostinger/GCP/Azure destinations for platform, ESO, and observability apps; and `bin/k3dm-webhook` now reports provider-correct ESO health while skipping the unsupported Pushgateway probe on Hostinger. Bug doc: `docs/bugs/2026-06-25-hostinger-refresh-status-app-cluster-drift.md`. Runtime follow-up remains open in `docs/issues/2026-06-25-hostinger-post-refresh-runtime-followups.md` because the Hostinger refresh path is repaired but frontend/Grafana/product-images still return `502` and `postgresql-orders` / `postgresql-payment` are still not ready. Validation passed with `python3 -m py_compile bin/k3dm-webhook`, `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh scripts/plugins/shopping_cart.sh scripts/tests/lib/provider_contract.bats scripts/tests/plugins/shopping_cart.bats`, `bats scripts/tests/lib/provider_contract.bats scripts/tests/plugins/shopping_cart.bats` (`1..49`, all pass), and `./scripts/k3d-manager _agent_audit`; pushed status pending the memory-bank update commit.
- **COMPLETE (2026-06-25):** `fix(hostinger)` commit `80b79800` restores the Hostinger public Keycloak access path on refresh by regenerating/restarting `com.k3d-manager.keycloak-port-forward`, rewriting `~/.local/share/k3d-manager/bin/keycloak-port-forward.sh` from the shared wrapper template, and clearing stale Keycloak wrapper processes plus `localhost:8880` listeners before Cloudflare is restarted; bug doc: `docs/bugs/2026-06-25-hostinger-refresh-keycloak-public-sso-gap.md`; validation passed with `bash -n scripts/lib/providers/k3s-hostinger.sh scripts/tests/lib/provider_contract.bats`, `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh scripts/tests/lib/provider_contract.bats`, `bats scripts/tests/lib/provider_contract.bats` (`1..37`, all pass), `./scripts/k3d-manager _agent_audit`, live `make refresh CLUSTER_PROVIDER=k3s-hostinger`, elevated `curl -I --max-time 10 http://127.0.0.1:8880/health/live` returning `HTTP/1.1 200 OK`, elevated `curl -Ik --max-time 15 https://keycloak.3ai-talk.org/realms/shopping-cart/.well-known/openid-configuration` returning `HTTP/2 200`, and elevated `curl -skL -o /dev/null -w '%{http_code}\n' 'https://argocd.3ai-talk.org/auth/login?return_url=https%3A%2F%2Fargocd.3ai-talk.org%2Fapplications'` returning `200`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (2026-06-24):** `fix(hostinger)` commit `f9bf7e59` makes the Hostinger refresh path Bash 3.2-safe, recreates the browser wrappers at `_ACG_STATE_DIR/bin/argocd-browser-https.sh` and `_ACG_STATE_DIR/bin/frontend-browser-http.sh`, and restores the access layer on `make refresh CLUSTER_PROVIDER=k3s-hostinger`; issue note: `docs/issues/2026-06-24-hostinger-refresh-bash32-mapfile.md`; validation passed with `bash -n scripts/lib/providers/k3s-hostinger.sh scripts/tests/lib/provider_contract.bats bin/cluster-refresh`, `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh bin/cluster-refresh`, `bats scripts/tests/lib/provider_contract.bats scripts/tests/bin/cluster_refresh.bats`, `./scripts/k3d-manager _agent_audit`, live `make refresh CLUSTER_PROVIDER=k3s-hostinger`, `curl -I --max-time 3 https://argocd.3ai-talk.org`, and `curl -I --max-time 3 http://localhost:8080/healthz`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (2026-06-25):** `fix(hostinger)` commit `340301fc` kills stale `argocd-port-forward.sh` wrapper processes before the Hostinger refresh restarts `com.k3d-manager.argocd-port-forward`, and tightens the shared ArgoCD wrapper readiness gate so a new child cannot inherit readiness from an old `8080` listener; updated `docs/issues/2026-06-24-hostinger-argocd-port-forward-flapping-after-refresh.md`; validation passed with `bash -n scripts/lib/providers/k3s-hostinger.sh scripts/etc/argocd/port-forward-wrapper.sh.tmpl`, `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh scripts/etc/argocd/port-forward-wrapper.sh.tmpl scripts/tests/lib/provider_contract.bats`, `bats scripts/tests/lib/provider_contract.bats` (`1..37`, all pass), `./scripts/k3d-manager _agent_audit`, live `make refresh CLUSTER_PROVIDER=k3s-hostinger`, elevated `curl -I --max-time 5 http://127.0.0.1:8080/healthz` returning `HTTP/1.1 200 OK`, and elevated `curl -Ik --max-time 10 https://argocd.3ai-talk.org/healthz` returning `HTTP/2 200`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **PARTIAL (2026-06-24):** `fix(hostinger)` commit `e9ff532a` restores the repo-managed Cloudflare tunnel during `make refresh CLUSTER_PROVIDER=k3s-hostinger` by stopping stale Homebrew `cloudflared`, regenerating the canonical `com.k3d-manager.cloudflare-tunnel` LaunchAgent from `scripts/etc/cloudflared/config.yml`, and keeping the Hostinger refresh path on the repo-managed tunnel label; issue notes: `docs/issues/2026-06-24-hostinger-cloudflared-homebrew-service-mismatch.md` and `docs/issues/2026-06-24-hostinger-argocd-port-forward-flapping-after-refresh.md`; validation passed with `bash -n scripts/lib/providers/k3s-hostinger.sh`, `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh scripts/tests/lib/provider_contract.bats`, `bats scripts/tests/lib/provider_contract.bats` (`1..37`, all pass), `./scripts/k3d-manager _agent_audit`, live `make refresh CLUSTER_PROVIDER=k3s-hostinger`, and `launchctl print gui/$(id -u)/com.k3d-manager.cloudflare-tunnel`; live follow-up remains open because `curl -I --max-time 5 http://127.0.0.1:8080/healthz` still refused connections after a clean bootout/bootstrap of `com.k3d-manager.argocd-port-forward`.
- **COMPLETE (2026-06-24):** `fix(hostinger)` commit `261194de` clears stale `localhost:8080` listeners before restarting the generated ArgoCD port-forward during `make refresh CLUSTER_PROVIDER=k3s-hostinger`, rewrites `~/.local/share/k3d-manager/bin/argocd-port-forward.sh` from `scripts/etc/argocd/port-forward-wrapper.sh.tmpl`, and keeps the wrapper Bash 3.2-safe for the macOS LaunchAgent path; issue note: `docs/issues/2026-06-24-hostinger-argocd-port-forward-8080-bind-conflict.md`; validation passed with `bash -n scripts/lib/providers/k3s-hostinger.sh scripts/tests/lib/provider_contract.bats`, `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh`, `bats scripts/tests/lib/provider_contract.bats scripts/tests/plugins/argocd.bats`, `./scripts/k3d-manager _agent_audit`, a live `make refresh CLUSTER_PROVIDER=k3s-hostinger`, and an elevated `curl -I --max-time 3 http://localhost:8080/healthz` returning `HTTP/1.1 200 OK`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (2026-06-24):** `fix(hostinger)` follow-up on commit `0e832b5e` resolved the live `make refresh CLUSTER_PROVIDER=k3s-hostinger` crash caused by `_ACG_STATE_DIR` being unset in `scripts/lib/providers/k3s-hostinger.sh`; the provider script now defines a local default state directory before using it, and the refresh command completes successfully through the ArgoCD port-forward, browser listeners, grafana, and pushgateway restarts; issue note: `docs/issues/2026-06-24-hostinger-refresh-acg-state-dir-unbound.md`; validated by re-running `make refresh CLUSTER_PROVIDER=k3s-hostinger` to completion after the fix.
- **COMPLETE (2026-06-24):** `fix(hostinger)` commit `0e832b5e` restarts/self-heals the `com.k3d-manager.argocd-port-forward` LaunchAgent during `make refresh CLUSTER_PROVIDER=k3s-hostinger` so Cloudflare can still reach `http://localhost:8080` behind `argocd.3ai-talk.org`; bug doc: `docs/bugs/2026-06-24-hostinger-argocd-public-tunnel-refresh-gap.md`; validation passed with `bash -n scripts/lib/providers/k3s-hostinger.sh scripts/lib/provider.sh`, `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh scripts/lib/provider.sh`, `bats scripts/tests/lib/provider_contract.bats` (`1..37`, all pass), and `git diff --check`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (2026-06-24):** `fix(provider)` commit `1cb723f8` makes live cluster state win over stale `active-provider` state so `make refresh`/`make status` switch correctly between Hostinger and ACG even when the old provider file is still present; `_acg_resolve_provider()` now probes live contexts first and only falls back to the saved file when no provider is reachable, and `scripts/tests/lib/provider_contract.bats` adds coverage for the stale-file/Hostinger switching cases; validation passed with `bash -n scripts/lib/provider.sh scripts/lib/providers/k3s-hostinger.sh`, `shellcheck -S warning scripts/lib/provider.sh scripts/lib/providers/k3s-hostinger.sh`, `bats scripts/tests/lib/provider_contract.bats` (`1..36`, all pass), and `git diff --check`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (2026-06-24):** `fix(provider)` commit `23f915d3` keeps `make status` on the active cluster when switching between ACG and Hostinger by validating the shared `active-provider` file before trusting it, preferring live Hostinger when both providers are reachable, and recording/clearing Hostinger as the active provider during deploy/refresh/destroy; bug doc: `docs/bugs/2026-06-24-hostinger-provider-switch-stale-active-provider.md`; validation passed with `bash -n scripts/lib/provider.sh scripts/lib/providers/k3s-hostinger.sh`, `shellcheck -S warning scripts/lib/provider.sh scripts/lib/providers/k3s-hostinger.sh`, `bats scripts/tests/lib/provider_contract.bats` (`1..35`, all pass), `git diff --check`, and `./scripts/k3d-manager _agent_audit`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (2026-06-24):** `fix(status)` commit `339152c1` on `feat/v1.8.0-acg-absorb-phase2-agy` separates preflight ArgoCD apps from the core Hostinger app table in `bin/cluster-status` and restores the Hostinger access layer during `make refresh CLUSTER_PROVIDER=k3s-hostinger` by restarting the cloudflared, browser, and pushgateway launchd services; investigation note: `docs/issues/2026-06-24-hostinger-status-access-layer-and-preflight-separation.md`; validation passed with `bash -n bin/cluster-status scripts/lib/providers/k3s-hostinger.sh`, `shellcheck -S warning bin/cluster-status scripts/lib/providers/k3s-hostinger.sh`, `git diff --check`, `./scripts/k3d-manager _agent_audit`, and `bats scripts/tests/lib/webhook.bats` (`1..29`, all pass); pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **INVESTIGATION (2026-06-24):** Confirmed the `namespaces "monitoring" not found` failure came from a stale local Prometheus LaunchAgent plist, not the repo code. Current repo template already targets `k3d-k3d-cluster`; added `docs/issues/2026-06-24-prometheus-port-forward-stale-launchagent.md` and synced `docs/howto/launchd-daemons.md`. Manual reinstall still required: `make install-prometheus-port-forward`. Commit `e45a9afe` captures the note.
- **COMPLETE (2026-06-24):** `fix(webhook,status)` commit `e956c25c` on `feat/v1.8.0-acg-absorb-phase2-agy` makes `make status` provider-aware for Hostinger: `bin/cluster-status` forwards `provider=` to `/api/v1/health`, `bin/k3dm-webhook` uses Hostinger public frontend/keycloak URLs and the active app-cluster context for smoke/data-layer checks, and both provider helpers normalize the `k3s-hostiger` typo alias to `k3s-hostinger`; validation passed with `python3 -m py_compile bin/k3dm-webhook`, `bash -n bin/cluster-status`, `bash -n scripts/lib/provider.sh`, `shellcheck -S warning scripts/lib/provider.sh bin/cluster-status`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; bug doc: `docs/bugs/2026-06-24-hostinger-status-provider-aware-smoke.md`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **COMPLETE (2026-06-24):** `fix(observability)` commit `5afdebb1` on `feat/v1.8.0-acg-absorb-phase2-agy` resolves the active app-cluster context in `deploy_observability_acg()`, bootstraps `monitoring` before Alertmanager/Prometheus secrets are written, and uses the same context for the ACG Prometheus/pushgateway paths so Hostinger remains supported; validation passed with `shellcheck scripts/plugins/observability.sh`, `bats scripts/tests/lib/observability.bats`, `bash -n bin/cluster-up`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; bug doc: `docs/bugs/2026-06-24-observability-acg-monitoring-namespace-race.md`; pushed to `origin/feat/v1.8.0-acg-absorb-phase2-agy`.
- **SPEC WRITTEN — ACG Absorption Phase 2 + agy retarget (Claude, 2026-06-22), awaiting Codex handoff:** lib-foundation v0.4.0 merged (`aed8c56`) absorbed lib-acg → `scripts/lib/acg/` module. k3d-manager Phase 2 collapses two subtrees into one: re-pull lib-foundation, `git rm -r scripts/lib/acg`, repoint `scripts/plugins/acg.sh` stub + `scripts/plugins/gemini.sh` cdp source to `scripts/lib/foundation/scripts/lib/acg/`, fix template/test paths, drop `lib-acg` remote. Combined with gemini→agy retarget (only `gemini.sh` still calls `gemini`; CDP/session already migrated upstream). Spec `docs/plans/v1.8.0-acg-absorb-phase2-agy-rewire.md` on `feat/v1.8.0-acg-absorb-phase2-agy` (off `origin/main`). Notes: `_sts_valid` fix is upstream-first (lib-foundation `feat/v0.4.1`); agy CLI flags need `agy --help` verification; pointer-doc supersede (Part C) deferred to branch convergence (doc is on the ESO branch, not main).
- **VERIFIED + PACKAGED + PHASE 2 SPEC'D (Claude, 2026-06-20):** Fault A (`71a93e8b`) + Fault B (`7f3c5efd`) verified clean (diffs match specs, scope clean, gates green incl. live `kubectl apply --dry-run`). Fixed upstream-tracking footgun on all 3 ESO branches (were tracking `origin/main`). Per user (one PR): merged Fault B into `feat/eso-app-cluster-generator` (`6c1eb550`, conflict-free) → single combined PR pending. Phase 2 spec written: `docs/plans/v1.8.0-eso-phase2-clustersecretstore-vault.md` on `feat/eso-phase2-clustersecretstore` (`cf37a41e`); decisions = dedicated Vault ingress `vault.3ai-talk.org` + k8s-auth per cluster; **not Codex-ready** — 4 Confirm-Before-Handoff items pending user. **PR #97 OPEN + READY (head `de5496c0`):** CI green, Copilot 1 finding (dead-template, no change, resolved + documented), scope clean, `enforce_admins` off — awaiting user merge (no auto-merge). After merge: `/post-merge` + one-time operator steps + resolve 4 Phase 2 items → hand `feat/eso-phase2-clustersecretstore` to Codex.
- **COMPLETE (2026-06-20):** Fault A on `feat/eso-app-cluster-generator` is complete and pushed. Commit `71a93e8b` adds `scripts/etc/argocd/applicationsets/eso.yaml` exactly per spec: cluster generator on `k3d-manager/role: app-cluster`, `external-secrets` chart `1.0.0`, namespace `secrets`, `installCRDs=true`, and `ServerSideApply=true`. Validation passed with `ARGOCD_NAMESPACE=cicd envsubst < scripts/etc/argocd/applicationsets/eso.yaml | KUBECONFIG=/dev/null kubectl create --dry-run=client --validate=false -f -`, `bats scripts/tests`, and `./scripts/k3d-manager _agent_audit`. Pushed to `origin/feat/eso-app-cluster-generator`.
- **COMPLETE (2026-06-20):** Fault B on `fix/hostinger-app-cluster-role-label` is complete and pushed. Commit `7f3c5efd` adds `k3d-manager/role: app-cluster` to the Hostinger cluster-secret render in `scripts/lib/providers/k3s-hostinger.sh` and the generic `scripts/etc/argocd/cluster-secret.yaml.tmpl`. Validation passed with `shellcheck scripts/lib/providers/k3s-hostinger.sh`, `ARGOCD_NAMESPACE=cicd envsubst < scripts/etc/argocd/applicationsets/eso.yaml | KUBECONFIG=/dev/null kubectl create --dry-run=client --validate=false -f -`, `bats scripts/tests/plugins/argocd_app_cluster_generator.bats`, `bats scripts/tests`, and `./scripts/k3d-manager _agent_audit`. Push to `origin/fix/hostinger-app-cluster-role-label` succeeded. Fault A was completed in the next block on `feat/eso-app-cluster-generator`.
- **COMPLETE (2026-06-19):** Copilot PR #96 fixes landed on `k3d-manager-v1.7.1` commit `578a761c` (`fix(slack,vcluster): cluster-resume uses resolveProvider, drop rg test dep, justify preflight RBAC (Copilot PR#96)`). Applied the three exact spec blocks only: `/cluster-resume` now uses `resolveProvider(text, '')` and rejects invalid providers with a usage reply, the generator test now uses `grep -rF -- "ubuntu-k3s"` instead of `rg`, and the preflight RBAC annotation now carries the CLAUDE.md justification text while leaving the `rules` unchanged. Validation: `node --check workers/slack-relay/index.js` passed, `bats scripts/tests/plugins/argocd_app_cluster_generator.bats` passed, `./scripts/k3d-manager _agent_audit` passed; `bash -n scripts/tests/plugins/argocd_app_cluster_generator.bats` fails on BATS syntax and was documented in `docs/issues/2026-06-19-argocd-generator-bats-bash-n-failure.md`. Pushed to `origin/k3d-manager-v1.7.1`.
- **COMPLETE (2026-06-19):** Branch-protection consistency — added `enforce_admins=true` + required PR review (count 1, dismiss_stale, no status checks) to the 2 previously-unprotected repos (observability-stack, rabbitmq-client-go), matching the 9 already-locked repos. Verified both. The 3 no-`main` repos (python/dotnet/library) deferred.
- **COMPLETE (2026-06-19):** Tightened the pre-push guard in the four open repos on `chore/add-pre-push-hook` by adding the activation note and removing the `local_ref != refs/heads/main` exemption in `.githooks/pre-push`; validation passed with `bash -n`, `shellcheck`, and the four direct-invocation checks in each repo (`feature→main=1`, `main→main=1`, override=0, non-main=0). Pushed commits: rabbitmq-client-python `e764fbd`, rabbitmq-client-java `47416ae`, rabbitmq-client-go `ce82ba7`, observability-stack `89ac986`. Added `docs/issues/2026-06-19-pre-push-hook-verification-harness-hiccup.md` to record the transient local harness failure encountered while verifying.
- **COMPLETE (2026-06-19):** `bin/k3dm-webhook` commit `1c46232f` on `k3d-manager-v1.7.1` now uses `--output-last-message` for Codex and reads the per-job `codex_last_message.txt` file on the codex path, eliminating the echoed banner/prompt blob from `/codex` replies. Verified with `python3 -m py_compile bin/k3dm-webhook` and `./scripts/k3d-manager _agent_audit`; pushed to `origin/k3d-manager-v1.7.1`. `make restart-webhook` was intentionally not run per task instructions.
- **VERIFIED + SPEC (2026-06-19):** Verified Codex pre-push rollout (7 commits on origin, hook byte-identical/mode 0755, exit-1 behavior correct, k3d-manager pre-commit path fix + foundation-edit rejection + `_agent_audit` clean; Copilot reviewed 4 open PRs; `-dotnet`/`-library` blocked on empty-repo base ref). Diagnosed Slack `/codex` "same answer" → codex banner+echoed-prompt captured via merged stdout+stderr (NOT v1.6.5 thread bug). Spec `docs/bugs/2026-06-19-codex-slack-banner-echoes-prompt.md` (`bin/k3dm-webhook`, fix = `--output-last-message`). AWAITING HANDOFF. Flagged hardcoded Slack secrets in `.claude/settings.local.json` — rotate.
- **PARTIAL (2026-06-19):** Rolled out committed pre-push hook to `observability-stack` PR #1 (`7a532da`), `rabbitmq-client-go` PR #1 (`c3872bd`), `rabbitmq-client-java` PR #7 (`f38bea9`), and `rabbitmq-client-python` PR #1 (`0444b74`); Copilot tagged. `k3d-manager` commit `e4acd5fa` adds `.githooks/pre-push` and relocated `.githooks/pre-commit` with the path fixed to `../scripts`; direct invocation checks passed and `_agent_audit` is green. `rabbitmq-client-dotnet` and `rabbitmq-client-library` are blocked because GitHub reports empty repos and `gh pr create --base main` fails with `Base ref must be a branch`; issue doc: `docs/issues/2026-06-19-empty-rabbitmq-client-pr-base-ref.md`.
- **PR OPEN (2026-06-19):** shopping-cart-infra **PR #84** (`fix/data-layer-cluster-generator` → main, Copilot tagged, NOT merged) removes static data-layer app; merge before k3d-manager cutover (finalizer hazard). Reviewed Codex data-layer work — correct, shellcheck/bats re-verified green.
- **HOOK HARDENING (2026-06-19):** Found infra+frontend local branches tracking `origin/main` and `core.hooksPath=/dev/null` (hook bypass) on infra/frontend/product-catalog → committed pre-push guard was inert. Fixed local upstreams + activated `.githooks`/local stopgaps; guards verified. Codex spec `docs/bugs/2026-06-19-pre-push-hook-block-main-rollout.md` to commit `.githooks/pre-push` across all shopping-cart repos + k3d-manager — AWAITING HANDOFF.
- **COMPLETE:** data-layer app-cluster parameterization via ArgoCD cluster generator — k3d-manager commit `2d3af51c` on `k3d-manager-v1.7.1` and shopping-cart-infra commit `df80415` on `fix/data-layer-cluster-generator`; spec: `docs/bugs/v1.7.1-bugfix-data-layer-app-cluster-generator.md`; `scripts/etc/argocd/applicationsets/services-git.yaml` now uses a `matrix` generator (git × `clusters` selector `k3d-manager/role: app-cluster`) and template destination `name: '{{.name}}'`, `scripts/etc/argocd/applicationsets/data-git.yaml` was added for the shopping-cart-infra `data-layer` app with the same cluster selector, `scripts/plugins/argocd.sh` now adds `argocd.argoproj.io/cluster-name` to registered cluster secrets, defines `_argocd_set_active_app_cluster`, and auto-asserts the active app-cluster label from `_acg_provider_context "$(_acg_resolve_provider)"` with fallback to `APP_CLUSTER_NAME`, `scripts/tests/plugins/argocd_app_cluster_generator.bats` guards the static manifests, and `shopping-cart-infra/argocd/applications/data-layer.yaml` was removed; validation used `shellcheck -S warning scripts/plugins/argocd.sh` (clean), `bats scripts/tests/` (clean), `bats scripts/tests/plugins/argocd.bats scripts/tests/plugins/argocd_app_cluster_generator.bats` (18/18 green), `./scripts/k3d-manager _agent_audit` (clean), and confirmed both SHAs exist on origin; the initial broad manifest-guard attempt matched `scripts/etc/argocd/projects/platform.yaml.tmpl`, so the guard was narrowed to `applicationsets/` and documented in `docs/issues/2026-06-19-data-layer-app-cluster-generator-test-scope-drift.md`; live cutover remains Claude-only and was not run.
- **MERGED (2026-06-19):** shopping-cart-order Go rewrite PR1 — PR #33 merged to main (`81401ba`); enforce_admins restored; feat/go-rewrite-pr2 branch created + retrospective committed (`ca59861`).
- **MERGED (2026-06-19):** shopping-cart-payment Go rewrite PR1 — PR #23 merged to main (`ff817b8`); enforce_admins restored; feat/go-rewrite-pr2 branch created + retrospective committed (`43b289f`).
- **COMPLETE:** order RabbitMQURI vhost double-escape fix — shopping-cart-order branch `feat/go-rewrite` commit `55d7b57` (`fix(order): RabbitMQURI vhost double-escape — set URL Path + RawPath`) updates `go/internal/config/config.go` so `RabbitMQURI()` uses decoded `Path` plus encoded `RawPath` instead of pre-escaping into `Path`, preserving the default `/` vhost as `/%2F` and keeping named vhosts as `/<name>`; `go/internal/config/config_test.go` was added with default/empty/named vhost cases to guard against `%252F`; validation used Dockerized Go toolchains for `gofmt -l .` (clean), `go build ./...` (green), `go vet ./...` (green), `go test ./... -race -cover` (green), `golangci-lint run` (green), and `./scripts/k3d-manager _agent_audit` (clean); branch pushed successfully to `origin/feat/go-rewrite`.
- **COMPLETE:** payment round-5 PCI hardening — shopping-cart-payment branch `feat/go-rewrite` commit `08f48f1` (`fix(payment): zero raw card fields on request param after gateway call (PCI)`) adds four `req.*` zeroing lines immediately after the gateway call in `go/internal/payment/service.go` so the by-value inbound request no longer retains PAN/CVV/expiry in locals through the `persist`/`RunInTx` path; validation used Dockerized Go toolchains for `gofmt -l go/` (clean), `go build ./...` (green), `go vet ./...` (green), `go test ./... -race -cover` (green), `golangci-lint run` (green), and `./scripts/k3d-manager _agent_audit` (clean); branch pushed successfully to `origin/feat/go-rewrite`.
- **PAYMENT #23 OVERRIDE ENABLED + ORDER #33 BUG SPEC (Claude, 2026-06-19):** Per explicit user go, `enforce_admins` disabled on payment main (confirmed `enabled:false`) → **#23 (`307ddf3`) ready for user to merge** (CI green, 0 unresolved). NOT merged by Claude. Order #33: 1 unresolved Copilot thread `config.go:92` — `RabbitMQURI()` double-escapes vhost (`PathEscape→%2F` into `url.URL.Path` → `String()` re-escapes → `%252F`; default `RABBITMQ_VHOST="/"` breaks default deploy). Spec `docs/bugs/2026-06-19-bugfix-order-rabbitmq-vhost-double-escape.md` (order `feat/go-rewrite`, `config.go` + new `config_test.go`; fix = `u.Path` decoded + `u.RawPath` encoded). Commit msg `fix(order): RabbitMQURI vhost double-escape — set URL Path + RawPath`. AWAITING USER HANDOFF to Codex; thread resolved after fix + CI green.
- **COMPLETE:** payment Copilot round-4 hardening — shopping-cart-payment branch `feat/go-rewrite` commit `307ddf3` (`fix(payment): Copilot round-4 hardening — currency trim, tx error cause, locked-row gateway IDs, card zeroing, DB_SSLMODE wiring`) applied the spec’s R1/R2/R3/R4/R5/R6 edits: `service.go` now normalizes currency once via `strings.ToUpper(strings.TrimSpace(req.Currency))` and zeros raw card fields immediately after gateway processing, `refund.go` now sources gateway identifiers from the locked `FOR UPDATE` row, `store.go` now preserves the original error when rollback also fails in `RunInTx`, `service_test.go` now asserts the trimmed currency result, and the base ConfigMap/Deployment now wire `db.sslmode` → `DB_SSLMODE` with the Istio-mTLS rationale comment; validation used Dockerized Go toolchains for `gofmt -l` (clean), `go build ./...` (green), `go vet ./...` (green), `go test ./... -race -cover` (green), `golangci-lint run` (green), `kubectl apply --dry-run=client -k k8s/base` (green), and `./scripts/k3d-manager _agent_audit` (clean); branch pushed successfully to `origin/feat/go-rewrite`.
- **ROUND-4 SPEC WRITTEN + 2 threads resolved (Claude, 2026-06-19):** Copilot **round-4** on payment #23 HEAD `0fda755` (11:33) — 7 findings, real code bugs this time. Wrote `docs/bugs/2026-06-19-bugfix-copilot-pr23-round4.md` per user decision **"Spec to Codex: #1-4,#6"**: fixes #1 currency trim (`service.go`), #2 tx error-cause loss (`store.go`), #3 locked-row gateway IDs (`refund.go`), #4 card-field zeroing/PCI (`service.go`), #6/R5 DB_SSLMODE wiring (`configmap.yaml`+`deployment.yaml`); commit msg `fix(payment): Copilot round-4 hardening — currency trim, tx error cause, locked-row gateway IDs, card zeroing, DB_SSLMODE wiring`. Claude replied+resolved #7 (mock.go split → PR2) and #5 (outdated). **VERIFIED — PASS (Claude):** Codex `307ddf3` (branch HEAD, exact msg) — all 5 diffs match spec byte-for-byte, scope clean (5 files + a +3/-0 `service_test.go` currency assertion per step 13; k3dm `4395b655` memory-bank only), CI all green. Replied-with-SHA + resolved the remaining 4 threads (R1 already resolved) → **0 unresolved on #23**. Re-requested Copilot; `mergeable_state=blocked`. enforce_admins UNTOUCHED — prepare-and-stop, awaiting user go after Copilot re-clears.
- **COMPLETE (Claude, 2026-06-19):** Copilot PR #23 **round-3** threads addressed + resolved. 4 unresolved findings on HEAD `0fda755` (mock-in-base configmap, gateway-call-in-`FOR UPDATE`-tx, correlation-id refund race, README/PR-scope mismatch). Per user decision **"Resolve all 3 w/ rationale"**: rewrote PR #23 description Scope section (fixes #4 directly); replied #1/#2/#3 with rationale (deliberate PR1 design / bounded race already over-refund-safe; mock overlay-gating + refund saga + correlation-id idempotency tracked as PR2 follow-ups in PR Notes); resolved all 4 via GraphQL → **0 unresolved on #23**. CI green (go/go-ci/integration/Security Scan). Re-requested Copilot; `mergeable_state=blocked`. **enforce_admins UNTOUCHED — prepare-and-stop.**
- **COMPLETE (Claude, 2026-06-19):** Codex round-2 work VERIFIED + Copilot threads resolved. Verified payment `0fda755` / order `1b84696` on `origin/feat/go-rewrite` (exist, exact msg, scope clean, order `config.go:47` UNTOUCHED, all 17 diffs match spec); PR CI green both (go/go-ci + integration + GitGuardian), kustomize renders both, `_agent_audit` clean; membank `477af5e7` accurate. Replied to all 17 round-2 threads (payment 11→`0fda755`, order 6→`1b84696`, B5 Istio-mTLS rationale) + resolved 12 unresolved via GraphQL → **0 unresolved both PRs**. Re-requested Copilot on #23/#33. **enforce_admins UNTOUCHED both — prepare-and-stop:** #23 `blocked` (awaiting Copilot re-clear), #33 `behind` main (+ carried GitGuardian-pin/admin-override). Awaiting user go.
- **COMPLETE:** Copilot round-2 Go-rewrite hardening finished — payment repo `shopping-cart-payment` branch `feat/go-rewrite` commit `0fda755` and order repo `shopping-cart-order` branch `feat/go-rewrite` commit `1b84696` landed the 17 spec fixes from `docs/bugs/2026-06-18-bugfix-copilot-pr-round2-hardening.md`: payment A1–A7 (mock PAN sentinels, idempotency/order short-circuit before gateway selection, sensitive `req` removed from persist path, 4xx-mapped router errors + generic 500 with cause logging, removed `RateLimitPerMinute`, transactional refund re-check with `GetPaymentForUpdate`, and mock gateway defaulted off with explicit configmap opt-in) plus order B1–B6 (RabbitMQ creds default empty, serialized publishes, merged CHANGELOG `### Added`, moved the Go-only migration fixture to `go/internal/order/testdata/`, explicit DB_SSLMODE configmap documentation, and batched `ListByCustomer` grouping); validation used Dockerized Go toolchains for `gofmt -l` (clean), `go build ./...` (green), `go vet ./...` (green), `go test ./...` (green), `kubectl kustomize k8s/base` renders, and `./scripts/k3d-manager _agent_audit` (clean); both branches were pushed to `origin/feat/go-rewrite`.
- **COMPLETE:** Copilot PR review findings docs backfill — payment repo `shopping-cart-payment` branch `feat/go-rewrite` commit `e2adc72` and order repo `shopping-cart-order` branch `feat/go-rewrite` commit `b45ed2b` both added the missing per-PR findings docs plus the README Issue Logs updates required by `docs/bugs/2026-06-18-bugfix-pr-findings-docs-backfill.md`; payment created `docs/issues/2026-06-18-copilot-pr23-review-findings.md` and updated the README Issue Logs top 5 to include PR #23 and PR #19, while order created `docs/issues/2026-06-16-copilot-pr33-review-findings.md` and updated the README Issue Logs top 5 to include PR #33 and keep the five most recent entries by date; validation used `./scripts/k3d-manager _agent_audit` clean plus commit/file checks confirming payment changed only `README.md` and the new PR #23 issue doc, and order changed only `README.md` and the new PR #33 issue doc; both branches were pushed to `origin/feat/go-rewrite`.
- **COMPLETE:** payment Go PR1 Copilot hardening — shopping-cart-payment branch `feat/go-rewrite` commit `f39fba1` (`fix(payment): harden Go PR1 per Copilot review (gateways, crypto, config, tx)`) landed the 14 spec changes from `docs/bugs/2026-06-18-bugfix-payment-go-pr1-copilot-hardening.md`, including hardcoded-secret cleanup, fail-fast encryption, fail-fast gateway selection, mock fail-fast stubs, request-context readiness, bounded rate limiting, transaction wrapping, and stripped PAN/CVV literals in the touched tests; validated with the repo Go test sweep plus a green integration run against a real Postgres seeded from the Java Flyway migrations; PR #23 GitHub Actions run `https://github.com/wilddog64/shopping-cart-payment/actions/runs/27796636227` finished green with both the `go` job (`.../job/82257747892`) and the `integration` job (`.../job/82257747894`) successful; branch pushed successfully to `origin/feat/go-rewrite`.
- **COMPLETE:** shopping-cart-payment + shopping-cart-order Go CI integration gates — payment repo `shopping-cart-payment` branch `feat/go-rewrite` commit `736028d` added the new `.github/workflows/go-ci.yml` `integration` job (Postgres 16 service + `go test -tags integration ./internal/payment/...`) while leaving the existing `go` job unchanged; the branch push produced GitHub Actions run `https://github.com/wilddog64/shopping-cart-payment/actions/runs/27753447269` and its `integration` job finished green (`.../job/82109342359`, with the existing `go` job green at `.../job/82109342363`). order repo `shopping-cart-order` branch `feat/go-rewrite` commit `ac85ad5` added `src/main/resources/db/migration/V1__init_schema.sql`, self-seeded `go/internal/order/store_integration_test.go`, and added the `.github/workflows/go-ci.yml` `integration` job (Postgres + RabbitMQ); the branch push produced `Go CI` run `https://github.com/wilddog64/shopping-cart-order/actions/runs/27753449878` and `Java CI` run `https://github.com/wilddog64/shopping-cart-order/actions/runs/27753450427`, both green (order push-run `Go CI` `https://github.com/wilddog64/shopping-cart-order/actions/runs/27753447696` also green). Validation used Dockerized Go integration tests for payment and order, `gofmt -l internal/order/store_integration_test.go`, `go vet -tags integration ./...`, `python3` YAML parsing via a disposable `/tmp/k3dm-pyyaml` venv, and `./scripts/k3d-manager _agent_audit`; local Maven `verify` for order was blocked by GitHub Packages auth (`com.shoppingcart:rabbitmq-client:1.0.1` 401), recorded in `docs/issues/2026-06-17-order-java-maven-verify-github-packages-401.md`; both branches were pushed to `origin/feat/go-rewrite`.
- **SPEC WRITTEN (Claude, 2026-06-17), awaiting Codex:** order Go CI integration gate + missing DB schema — spec `docs/bugs/2026-06-17-bugfix-order-go-ci-integration-gate.md`; work repo `shopping-cart-order` branch `feat/go-rewrite` (PR #33). Order rewrite has no DB schema (runtime-verified: `relation "orders" does not exist` on clean Postgres; RabbitMQ publisher test passes). Fix adds `src/main/resources/db/migration/V1__init_schema.sql` (orders+order_items), self-seeds it in `store_integration_test.go`, and adds a go-ci `integration` job (Postgres + RabbitMQ) running `-tags integration`. DoD: new job green + Java CI green (migration must satisfy Hibernate `ddl-auto: validate`). User chose Full fix.
- **SPEC WRITTEN (Claude, 2026-06-17), awaiting Codex:** payment Go CI integration gate — spec `docs/bugs/2026-06-17-bugfix-payment-go-ci-integration-gate.md`; work repo `shopping-cart-payment` branch `feat/go-rewrite`, file `.github/workflows/go-ci.yml` only. `go-ci` ran only `go test ./... -race -cover` (compiles out the `//go:build integration` test), so the `f2e47ff` double-advance regression was invisible to CI. Fix adds an `integration` job (`postgres:16-alpine` service + `go test -tags integration ./internal/payment/...`). DoD: new job green in the `go-ci` run. Also confirmed this session: no vCluster preflight CI exists (roadmap v1.1.1) and no `repository_dispatch` listener, so a payment PR triggers only in-repo Java + Go CI.
- **COMPLETE:** payment list-scanner double-advance fix + order rewrite config hardening already present — payment repo `shopping-cart-payment` branch `feat/go-rewrite` commit `6ee63e3` now points both list loops at singular `scanPaymentRow`/`scanRefundRow` and deletes the wrappers in `go/internal/payment/store.go`; `go test -tags integration ./internal/payment/...` passed against a throwaway Postgres seeded with the real Java Flyway migrations; order repo `shopping-cart-order` branch `feat/go-rewrite` was already at matching commit `c5a5d95` for the requested `DB_PASSWORD` default removal, `DB_SSLMODE`/`DBSSLMode`, canonical AMQP dialer, and `ErrOrderNotFound` row-count guard, so no additional order diff was needed in this run; `./scripts/k3d-manager _agent_audit` passed; payment branch pushed to `origin/feat/go-rewrite`.
- **COMPLETE:** argocd-port-forward plist regeneration — k3d-manager commit `3e149449` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.7.1-bugfix-refresh-argocd-pf-plist-regen.md`; `bin/cluster-refresh` now regenerates `~/Library/LaunchAgents/com.k3d-manager.argocd-port-forward.plist` from `${_ACG_STATE_DIR}/bin/argocd-port-forward.sh` when the plist is missing, mirroring the keycloak/grafana blocks, then continues into `_launchd_ensure`; validation used `shellcheck -S warning bin/cluster-refresh`, `bash -n bin/cluster-refresh`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(refresh): regenerate missing argocd-port-forward plist from wrapper`
- **COMPLETE:** shopping-cart-payment Go rewrite PR1 — work repo `shopping-cart-payment` branch `feat/go-rewrite` from `origin/main`; commit `ddc6c82` (`feat(payment): Go rewrite PR1 — functional core (HTTP + Postgres + mock gateway + refunds)`) added the side-by-side `go/` module, `go/Dockerfile`, `go/Dockerfile.local`, additive `.github/workflows/go-ci.yml`, and a contract-preserving HTTP + Postgres + mock-gateway + refunds implementation; the Go module root is `go/` (`module github.com/wilddog64/shopping-cart-payment/go`), `go.sum` was generated, and validation used Dockerized Go toolchains for `gofmt -l .` (clean), `go vet ./...` (clean), `go test ./... -race -cover` (all pass; no tests), and `docker build -f go/Dockerfile go` (success); branch pushed successfully to `origin/feat/go-rewrite`.
- **COMPLETE:** shopping-cart-payment Go rewrite PR1 follow-up — work repo `shopping-cart-payment` branch `feat/go-rewrite`; commit `f2e47ff` (`test(payment): PR1 follow-ups — tests, CHANGELOG, CI gates, amount JSON number shape`) added the response-only fixed-scale amount marshaler (`PaymentResponse.amount` / `RefundResponse.amount` now emit unquoted JSON numbers at scale 4), the PR1 unit test suite (status machines, idempotent replay, refund guards, mock-gateway paths, encryption/masking, handler error mapping, amount-shape assertions), a build-tagged Postgres integration test against the Java Flyway schema, `CHANGELOG.md` `[Unreleased]`, `.github/workflows/go-ci.yml` `permissions: contents: read` + pinned `golangci-lint` gate, and `go/.golangci.yml`; confirmed against Java before changing that gateway defaults already match (`STRIPE_ENABLED`/`PAYPAL_ENABLED` stay true) and DB startup stays fail-fast like the Java service, so no code change was needed there; validation used Dockerized Go toolchains for `gofmt -l go/` (clean), `go vet ./...` (clean), `golangci-lint run` (clean), `go test ./... -race -cover` (pass), and `docker build -f go/Dockerfile go` (success); branch pushed successfully to `origin/feat/go-rewrite`.
- **VERIFIED FAIL (Claude, 2026-06-17):** `f2e47ff` Fixes 1/3/4 + unit suite GOOD (amount unquoted scale-4, CI gates, CHANGELOG, all unit tests green in Docker), BUT `go test -tags integration` FAILS — `list payments: no rows in result set`. Root cause = double-advance bug in `GetPaymentsByCustomer`/`GetRefundsByPayment` (`scanPaymentRows`/`scanRefundRows` call `rows.Next()` inside a `for rows.Next()` loop), pre-existing from `ddc6c82`, breaks 3 live list endpoints. Codex committed the integration test RED (never ran `-tags integration`). KICK BACK: drop inner `rows.Next()` from the two plural scanners + require `go test -tags integration` green. Bugfix spec on `feat/go-rewrite` pending.
- **SPEC WRITTEN (Claude, 2026-06-17):** corrective bugfix spec `docs/bugs/2026-06-17-bugfix-payment-go-rewrite-list-double-advance.md` (work repo `shopping-cart-payment`, branch `feat/go-rewrite`, file `go/internal/payment/store.go` only). Fix = point both list loops at singular `scanPaymentRow`/`scanRefundRow` and delete the two double-advancing wrappers; DoD requires `go test -tags integration` green (not unit-only) + `_agent_audit`. Awaiting handoff to Codex (Claude does not run handoffs).
- **COMPLETE:** launchd bootout-before-bootstrap fix — k3d-manager commit `424e46e1` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.7.1-bugfix-launchd-ensure-bootout-before-bootstrap.md`; `bin/cluster-refresh` system-domain `_launchd_ensure` now best-effort bootouts the stale launchd record before bootstrapping so a previously crash-looped daemon can re-bootstrap cleanly; validation used `shellcheck -S warning bin/cluster-refresh`, `bash -n bin/cluster-refresh`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(refresh): bootout stale launchd record before bootstrap in _launchd_ensure`
- **VERIFIED COMPLETE + THIRD BUG (Claude, 2026-06-17):** bootout fix `424e46e1` (memory-bank `051b0614`) verified on origin (diff = the single bootout line, scope clean, body+trailer intact; shellcheck/`bash -n`/`_agent_audit` rc=0). **Live e2e PASSED for the bootout fix** — `rm TLS material` + `make refresh` bootstrapped all 3 browser daemons with NO `Bootstrap failed: 5`. BUT ArgoCD still 000 on the proper path: socat now runs but crash-loops `connect 127.0.0.1:8080: Connection refused` — the `com.k3d-manager.argocd-port-forward` plist is missing and `bin/cluster-refresh:331-334` calls `_launchd_ensure` with no regen-from-wrapper logic (unlike keycloak/grafana). Proven: a manual `:8080` port-forward made `curl -sk https://argocd.shopping-cart.local/healthz` return 200. **OPEN (Codex):** spec `docs/bugs/v1.7.1-bugfix-refresh-argocd-pf-plist-regen.md` — regen the plist from `~/.local/share/k3d-manager/bin/argocd-port-forward.sh` if missing, mirroring the keycloak block; 1-block, `bin/cluster-refresh` only. ArgoCD reachable meanwhile via `http://localhost:8081/`.
- **COMPLETE:** argocd-browser-https refresh self-heal — k3d-manager commit `ee9d7a7e` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.7.1-bugfix-argocd-browser-refresh-self-heal.md`; `bin/cluster-refresh` now sources `scripts/plugins/argocd.sh` after `tunnel.sh`, defines `PLUGINS_DIR` before sourcing it so `set -u` does not abort during plugin load, and regenerates the Vault-issued TLS material plus the canonical `argocd-browser-https` wrapper before `_system_daemon_install_if_missing`; the `_argocd_write_browser_https_wrapper` call keeps the same argument order as `bin/cluster-up`; validation used `shellcheck -S warning bin/cluster-refresh`, `bash -n bin/cluster-refresh`, `./scripts/k3d-manager _agent_audit`, and a `set -u` source-order sanity check confirming `PLUGINS_DIR` is not unbound when `argocd.sh` loads; commit message: `fix(refresh): self-heal argocd-browser-https cert + wrapper before bootstrap`
- **VERIFIED COMPLETE + NEW BUG (Claude, 2026-06-17):** self-heal `ee9d7a7e` verified on origin (diff exact, scope clean, body intact) and **live self-heal test PASSED for artifacts** — `rm TLS material` + `make refresh` re-issued the Vault PKI cert (`CN=argocd.shopping-cart.local`, Jun17→Jul17) + regenerated the wrapper at the canonical path. BUT the daemon still won't load: `_launchd_ensure` system branch (`bin/cluster-refresh:81-84`) bootstraps WITHOUT a prior `bootout` → stale system-domain record → `Bootstrap failed: 5: Input/output error` on all 3 browser daemons. **OPEN (Codex):** spec `docs/bugs/v1.7.1-bugfix-launchd-ensure-bootout-before-bootstrap.md` — add `launchctl bootout system "${plist}" || true` before bootstrap (mirrors `_launchd_restart`/`cluster-up`); 1-hunk, `bin/cluster-refresh` only; one fix recovers all 3 daemons. ArgoCD reachable meanwhile via `http://localhost:8081/` fallback.
- **COMPLETE:** preflight vcluster distro schema fix — k3d-manager commit `41829082` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.1.1-bugfix-preflight-vcluster-distro-schema.md`; `scripts/etc/vcluster/values-preflight.yaml` now removes the invalid `controlPlane.distro.k3s` block while keeping the NodePort service and the raised control-plane requests/limits unchanged; validation used the spec-required PyYAML parse check via a disposable `/tmp/k3dm-pyyaml` venv because the host Python lacks `yaml`; commit message: `fix(preflight): drop invalid k3s distro from vcluster values (CLI 0.35.0 schema)`
- **COMPLETE:** preflight four-spec batch — k3d-manager commits `b01398ea`, `4e8a77ee`, `2b46f0ce`, and `a6b053fc` on `k3d-manager-v1.7.1`; specs: `docs/bugs/v1.1.1-bugfix-preflight-cluster-name-label.md`, `docs/bugs/v1.1.1-bugfix-make-help-missing-preflight.md`, `docs/bugs/v1.1.1-bugfix-preflight-vcluster-controlplane-sizing.md`, and `docs/bugs/v1.1.1-bugfix-preflight-ghcr-pull-secret.md`; `bin/cluster-preflight` now labels the registered cluster secret with `argocd.argoproj.io/cluster-name=<name>` and provisions `ghcr-pull-secret` in the vCluster app namespaces via `shopping_cart_provision_ghcr_pull_secret`, `Makefile` help now documents `make preflight`, `scripts/etc/vcluster/values-preflight.yaml` now enables the k3s distro and raises control-plane limits, and `scripts/plugins/shopping_cart.sh` adds the resolve+create wrapper; validation used `shellcheck -S warning bin/cluster-preflight scripts/plugins/shopping_cart.sh`, `bash -n bin/cluster-preflight scripts/plugins/shopping_cart.sh`, `make -n help`, `./scripts/k3d-manager _agent_audit`, and a disposable `/tmp/k3dm-pyyaml` venv for `python3 -c 'import yaml; yaml.safe_load(...)'`; all four commits were pushed to `origin/k3d-manager-v1.7.1`.
- **DECISION (2026-06-14):** Go rewrite scoped to `shopping-cart-order` + `shopping-cart-payment` ONLY (the two JVM backends); basket already Go; product-catalog (Python) + frontend (Node) stay. App-level only — migration platform bugs #2-#5 (data-layer/ESO/context/smoke) are NOT throwaway and proceed. Memory: `project_shopping_cart_go_rewrite.md`. vCluster preflight planned as validation gate: `project_vcluster_preflight_ci.md`.
- **PLAN WRITTEN (2026-06-14):** `docs/plans/v1.1.1-vcluster-preflight-webhook.md` — webhook-as-runner vCluster preflight (no self-hosted GH runner; k3dm-webhook on warm host is the engine). One `bin/cluster-preflight` script, 3 entry points (make/Slack/GitHub HTTP stub), one log link (R2/OCI). Phased P1 `make preflight` → P2 webhook `/api/v1/preflight` + Slack `/cluster-preflight` → P3 `preflight/vcluster` commit-status merge gate. NOT handed to Codex; awaiting user go. P1 deploy-fn name TBD from `shopping_cart.sh`.
- **SPEC WRITTEN (2026-06-15):** Go rewrite **PR1 — order** — `docs/plans/shopping-cart-order-go-rewrite-pr1.md`; work repo `shopping-cart-order` branch `feat/go-rewrite` from `origin/main`. User chose "Functional core first": PR1 = HTTP+Postgres+RabbitMQ events+status machine+security headers+actuator/health+rate limiter, `OAUTH2_ENABLED=false`; PR2 (later spec) = Keycloak JWT/JWKS+role checks. Stack = gin+pgx+amqp091-go+zap+prometheus+shopspring/decimal (mirrors basket). Reproduces Java contract exactly; DB schema owned externally (map, don't manage DDL); 5 events to `events` exchange. Keep port 8080+actuator paths+image name so k8s manifests unchanged. **Java+Go SIDE BY SIDE (user 2026-06-15):** keep Java tree intact, Go module under `go/` subdir + additive `go-ci.yml`; deployed artifact stays Java until later cutover PR (PR1 = zero deploy risk). Acceptance = e2e suite unchanged, but final acceptance gates on vcluster preflight Phase 1b (don't cut deployed image to Go until e2e green). order first (lower blast radius than payment).
- **VERIFIED — order Go rewrite PR1 (Claude, 2026-06-16):** shopping-cart-order commit `66f7e0e125f5e15d616b66c97c2c0ac23dbf1cfc` on `origin/feat/go-rewrite` — **PASS**. (1) SHA on origin, commit msg exact. (2) Scope clean: 1 commit, 22 files, all `go/**` + additive `.github/workflows/go-ci.yml` + CHANGELOG, Java tree untouched (all insertions). (3) Gates green via Docker (host has no go): gofmt clean, `go vet` OK, `go test ./... -race -cover` all pass (integration tests `//go:build integration` gated), `docker build -f go/Dockerfile go` OK; CI workflow pinned actions + `permissions: contents: read`. (4) **Contract fidelity vs Java source = faithful** (read from Java, not guessed): OrderResponse 16-field order, all 5 events (types/versions/@JsonProperty) incl. `OrderShippedEvent.estimatedDelivery` LocalDate→DateOnly, EventEnvelope, `decimal.MarshalJSONWithoutQuotes=true` in BOTH order+events pkgs (unquoted JSON numbers like Jackson), RabbitMQ `events` topic+durable / routing-key=type / Persistent / best-effort, state machine + side-effects (estimatedDelivery=now+5d, refundInitiated=true hardcode matching Java stub, cancel blocked on SHIPPED/COMPLETED), error codes NOT_FOUND/INVALID_STATE, DB cols incl. `shipping_*` (Java embeddable @Column verified) + `order_items` omits computed `subtotal`. (5) **Live runtime smoke** (Docker postgres+service, OAUTH2_ENABLED=false): all 5 endpoints + health/prometheus — 201 create (decimals as numbers, subtotal computed, address round-trips), PENDING→SHIPPED 400 INVALID_STATE w/ Java-identical message, PENDING→PAID 200 paidAt set, cancel PAID 200, unknown→404 NOT_FOUND, empty-items→400 BAD_REQUEST, 6 security headers present, X-Correlation-Id echoed. **Minor:** decimal `12.50`→`12.5` (trailing zero dropped) — numerically equal, transparent to JS/Playwright e2e. NEXT: PR2 spec (Keycloak JWT) + payment rewrite; final acceptance still gated on vcluster preflight e2e.
- **PR OPEN — order Go rewrite PR1 (2026-06-16):** [shopping-cart-order #33](https://github.com/wilddog64/shopping-cart-order/pull/33) `feat/go-rewrite`→`main`, Copilot tagged. CI: Java Build&Test/Checkstyle ✅, go-ci ✅, **GitGuardian ❌** (config.go:45 `DB_PASSWORD` default `"postgres"` — matches Java application.yml:8; required check, blocks merge). Copilot 5 inline: (a) config.go:73 `sslmode=disable` explicit (Java pgjdbc no-SSL default = same posture); (b) publisher.go:315 custom Dial bypasses amqps TLS (latent, RABBITMQ_USE_TLS=false in PR1); (c) store.go:194 Update ignores RowsAffected (no ErrOrderNotFound on missing row); (d) handler.go:98 customerId from body & (e) handler.go:193 list by query param — **both by-design PR1, auth deferred to PR2 (OAUTH2_ENABLED=false)**. Fixes = Codex bugfix spec (shopping-cart = no direct Claude edits); a+b+c+GitGuardian to fix, d+e to defer w/ Copilot reply. enforce_admins NOT touched (CI red).
- **SPEC WRITTEN (2026-06-16):** `docs/bugs/2026-06-16-bugfix-order-go-rewrite-pr33-findings.md` — Codex bugfix for `shopping-cart-order` `feat/go-rewrite` (existing PR #33 branch, no new branch). USER DECISIONS: GitGuardian → **remove** `DB_PASSWORD` default (`getEnv("DB_PASSWORD","")`, code-only green; check is GitGuardian **GitHub App** not ggshield, so `.gitguardian.yaml` would NOT clear it); fix **a+b+c** (a: `DB_SSLMODE` env, default `disable`; b: `amqp.DefaultDial(5s)` + drop unused `net` import; c: `store.go` Update `RowsAffected()==0`→`ErrOrderNotFound`), **defer d+e to PR2** (handler.go customerId from JWT — auth). 3 files, exact old/new blocks, Dockerized gates. Commit msg in spec. **HANDED TO CODEX (2026-06-15)** — same host/same branch, Codex reads the spec off disk (no git pull/push of the spec needed). After Codex pushes the order fix to `feat/go-rewrite`: Claude verifies origin SHA + diff vs spec, posts Copilot replies (a/b/c=fixed SHA, d/e=PR2 defer), resolves threads, re-checks CI (incl. GitGuardian now clearing), then disables enforce_admins + notifies user PR #33 ready to merge.
- **COMPLETE:** shopping-cart-order Go rewrite PR1 — work repo `shopping-cart-order` branch `feat/go-rewrite` from `origin/main`; commit `66f7e0e` (`feat(order): Go rewrite PR1 — functional core (HTTP + Postgres + RabbitMQ events)`) added the side-by-side `go/` module, `go/Dockerfile`, `go/Dockerfile.local`, additive `.github/workflows/go-ci.yml`, `CHANGELOG.md` Unreleased note, contract-preserving HTTP + Postgres + RabbitMQ implementation, and unit/integration tests; validation used Dockerized Go toolchains for `gofmt -l .`, `go vet ./...`, `golangci-lint run`, `go test ./... -race -cover`, and `docker build -f go/Dockerfile go`; branch pushed successfully to `origin/feat/go-rewrite`. PR2 JWT/JWKS remains deferred.
- **COMPLETE:** shopping-cart-order Go rewrite PR1 follow-up — work repo `shopping-cart-order` branch `feat/go-rewrite`; commit `c5a5d95` (`fix(order): harden Go rewrite config, publisher, and not-found handling`) changed only `go/internal/config/config.go`, `go/internal/events/publisher.go`, and `go/internal/order/store.go` to clear the GitGuardian password default, add `DB_SSLMODE` / `DBSSLMode`, switch RabbitMQ dialing to `amqp.DefaultDial(5 * time.Second)`, and return `ErrOrderNotFound` when `Update` affects 0 rows; validation used Dockerized Go toolchains for `gofmt -l .`, `go vet ./...`, `go test ./... -race -cover`, and `docker build -f go/Dockerfile go`; branch pushed successfully to `origin/feat/go-rewrite`. **VERIFIED (Claude, 2026-06-15):** SHA confirmed on origin, diff matches spec exactly, only the 3 files, handler.go untouched, required CI (Build&Test + Checkstyle + go-ci) green. Copilot threads handled — a/b/c replied "Fixed in c5a5d95" + RESOLVED; d/e replied "Deferred to PR2 (Keycloak JWT/JWKS auth)" + left OPEN intentionally. **GitGuardian still RED (option A chosen):** App scans all 2 PR commits; secret persists in original `66f7e0e` diff so a code-only HEAD fix can't clear it. GitGuardian is NOT a required check (required = Build&Test + Checkstyle only) → does not block merge; squash-merge lands clean main. User to resolve GitGuardian incident `27408404` on dashboard.gitguardian.com, then check re-runs green. **enforce_admins NOT yet disabled** — pending GitGuardian green. Commit message deviates from spec (`harden...` vs spec's exact message) — cosmetic, set correct message at squash-merge.
- **COPILOT RESOLVED — shopping-cart-order PR #31 (Claude, 2026-06-16):** [PR #31](https://github.com/wilddog64/shopping-cart-order/pull/31) `fix/order-remove-placeholder-secret`. Copilot flagged title/diff mismatch — title claimed it removes the placeholder `order-service` secret manifest + adds two issue docs, but the diff is 1 file / 97 insertions / 0 deletions (only the PostgreSQL auth-failure RCA doc). **Root cause:** the branch's secret-removal commit `c5b0f67` and the ESO-drift doc `6e98f03` already landed on `main` independently (branch is `BEHIND` main); the only remaining delta vs main is the RCA doc `d78ea21`. **Action (no code edit — PR metadata only):** retitled to `docs(issues): add order-service PostgreSQL auth-failure RCA` + rewrote body to match the actual diff; replied to Copilot comment `3306753500` + RESOLVED thread `PRRT_kwDORUakXc6E7AZH`. RCA doc is secret-clean (only field names `DB_PASSWORD`/`order-service-secrets`, no values). Checks: Build&Test + Checkstyle + **GitGuardian all PASS**; `mergeable_state: behind` (admin-merge works or update branch). **MERGED (2026-06-16):** squash merge commit `af143dd3651f7ba20645f0ec456e8f34519c0fd9` on `main`; local main synced. No tag/release (CHANGELOG `[Unreleased]` only, docs-only, not a milestone → no retro). **enforce_admins on shopping-cart-order main left DISABLED intentionally** — PR #33 merges next in the same repo; to be RE-ENABLED after #33 merges (do not forget). **Doc findings (Claude, surfaced pre-merge):** the RCA documents a STILL-OPEN incident — live ArgoCD `shopping-cart-order` = `Degraded` as of 2026-06-16 (credential mismatch `order-service-secrets.DB_PASSWORD` ↔ live `postgresql-orders` unresolved); doc references dead context `ubuntu-k3s` (migrated to `ubuntu-hostinger`); twin of already-merged `...secret-eso-ownership-drift.md` (no cross-link). **Follow-up (app-layer bug, own GH issue):** order-service still Degraded — real credential-seeding bug, independent of the merged doc.
- **PR #33 COPILOT FULLY RESOLVED + GITGUARDIAN GREEN (Claude, 2026-06-16):** Re-checked #33 on user report "copilot made comments again" — NO new Copilot review (head still `c5a5d95`, last review 02:04). The 2 still-OPEN threads were the deferred d/e `handler.go` auth findings (customerId from body / query param) that we'd replied-to but not resolved. Resolved both deferred threads via GraphQL (`PRRT_kwDORUakXc6Jwb-C`, `PRRT_kwDORUakXc6Jwb-P`) — now **5/5 threads resolved** (a/b/c fixed, d/e deferred-to-PR2 + resolved). **GitGuardian now PASSES** (`✅ no secrets present anymore` issue comment; fresh scan cleared after incident `27408404` resolved). All checks green: Build&Test ✅ Checkstyle ✅ go-ci ✅ GitGuardian ✅. `mergeable_state: behind`. enforce_admins on shopping-cart-order main already DISABLED (from #31). **#33 is ready to merge** (squash title = spec exact: `fix(order): remove hardcoded DB_PASSWORD default; configurable sslmode; canonical AMQP dial; Update row-count guard`). After merge: RE-ENABLE enforce_admins + post-merge.
- **PHASE 1 FAILED + DIAGNOSED (2026-06-15, Claude live):** 2 root causes — (1) spec defect: `deploy_argocd_bootstrap APP_CLUSTER_NAME=<probe>` retargeted LIVE ubuntu-hostinger apps onto the throwaway vcluster (Codex caught+restored, then rewrote `bin/cluster-preflight` to render PREFIXED `<name>-preflight-*` clones — correct, salvageable); (2) environmental: vcluster control plane crash-loops on k3d-on-Mac (leader-election lease timeout, `phase1new-0` 65 restarts) → flaky API/ESO/TLS failures. **DECISION (user): vcluster runs INSIDE Hostinger k3s (`ubuntu-hostinger`), NOT local Mac k3d; OCI rejected** → vcluster API exposed externally + registered with hub ArgoCD by external URL. Live debris cleaned (17 `phase1*` apps + 7 `cluster-phase1*` secrets + crash-loop vcluster); live apps verified back on ubuntu-hostinger. Spec updated. Codex WIP uncommitted on disk (`bin/cluster-preflight` salvageable + rbac yaml + Makefile; 2 bad plugin edits to revert: `vcluster.sh` guardrail-weakening, `eso.sh` timeout band-aids). NEXT = REVISION handoff to Hostinger host, awaiting user go.
- **COMPLETE:** Phase 1a Hostinger spike — k3d-manager commit `fe566b2a` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.1.1-vcluster-preflight-webhook.md` § Phase 1a; `bin/cluster-preflight` now defaults `HOST_CONTEXT` to `ubuntu-hostinger`, creates the vcluster with `scripts/etc/vcluster/values-preflight.yaml` (`controlPlane.service.spec.type: NodePort`), derives `APP_CLUSTER_SERVER` from `HOSTINGER_HOST` + the live vcluster NodePort, enforces the `curl -ksS --max-time 10 .../healthz` firewall guardrail, applies `argocd-manager` RBAC, mints the token, switches back to the hub `k3d-k3d-cluster` for `register_app_cluster`, and STOPs right after registration; `scripts/plugins/vcluster.sh` now supports `VCLUSTER_VALUES_FILE` and fails host-unreachable as `_err` with a clean `VCLUSTER_HOST_CONTEXT` parameter, `scripts/plugins/eso.sh` timeout band-aids were reverted, and the spike live-proofed `spike-0` at restart count 0 with `curl .../healthz` returning `ok` plus `secret/cluster-spike` present on the hub; validation used `shellcheck -S warning bin/cluster-preflight scripts/plugins/vcluster.sh scripts/plugins/eso.sh`, `bash -n bin/cluster-preflight scripts/plugins/vcluster.sh scripts/plugins/eso.sh`, `bats scripts/tests/plugins/vcluster.bats`, `./scripts/k3d-manager _agent_audit`, `make -n preflight NAME=spike MODE=--destroy`, and the live `make preflight NAME=spike` spike; transient failures and cleanup notes were recorded in `docs/issues/2026-06-14-vcluster-preflight-phase1-live-retarget-failure.md` and `docs/issues/2026-06-15-vcluster-preflight-phase1a-registration-context.md`; commit message: `feat(preflight): hostinger vcluster NodePort spike (Phase 1a)`
- **COMPLETE:** vCluster preflight Phase 1b deploy path wired — k3d-manager commit `644d3d6d` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.1.1-vcluster-preflight-phase1b-deploy.md`; `bin/cluster-preflight` now accepts `--reuse`, updates usage text, skips create/register when reusing an already-registered vCluster, renders and applies the prefixed-clone ArgoCD resources, waits for the preflight apps to become Synced/Healthy, and exits `0` on success; validation used `shellcheck -S warning bin/cluster-preflight`, `bash -n bin/cluster-preflight`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(preflight): wire deploy path + --reuse mode + fix success exit code`
- **COMPLETE:** preflight `--destroy` keep-on-failure gate — k3d-manager commit `75c8103c` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.1.1-bugfix-preflight-destroy-on-success-keep-on-failure.md`; `bin/cluster-preflight` now keeps the vCluster and prints a manual-cleanup hint when `--destroy` exits non-zero, but still prunes the vCluster and ArgoCD resources on success; the `--destroy` usage line now documents delete-on-success / keep-on-failure semantics; validation used `shellcheck -S warning bin/cluster-preflight`, `bash -n bin/cluster-preflight`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(preflight): --destroy keeps vCluster on failure, deletes only on success`
- **COMPLETE:** consolidate hostinger-status into provider-aware cluster-status — k3d-manager commit `19e2ea71` on `k3d-manager-v1.7.1`; spec: `docs/bugs/2026-06-14-bugfix-consolidate-status-into-cluster-status.md`; `bin/cluster-status` now carries the Hostinger-only API Health and Hub ArgoCD Registration checks, gates the SSH Tunnel section to tunneled providers only, and keeps Service Health unchanged; `Makefile` status now falls through to `bin/cluster-status` for `k3s-hostinger`; `bin/k3dm-webhook` `_run_hostinger_status` now runs `bin/cluster-status` with `CLUSTER_PROVIDER=k3s-hostinger` and the timeout hint points to `make status CLUSTER_PROVIDER=k3s-hostinger`; `bin/hostinger-status` was removed; validation used `shellcheck -S warning bin/cluster-status`, `bash -n bin/cluster-status`, `python3 -m py_compile bin/k3dm-webhook`, `./scripts/k3d-manager _agent_audit`, `git grep -n 'bin/hostinger-status' -- bin/ scripts/ workers/ Makefile` (no output), and `make -n status CLUSTER_PROVIDER=k3s-hostinger` resolving to `bin/cluster-status`; commit message: `refactor(status): consolidate hostinger-status into provider-aware cluster-status`
- **COMPLETE (Claude-corrected):** hostinger-status posts the report body inside a code fence — `5f764399` (Codex) fixed the symptom but DEVIATED from spec (no code fence, `✅` not `🖥️`, failure path on tail-8-lines); **`3557c1f3` (Claude)** applied the spec's exact new block: `report[-3500:]` in a ```` ``` ```` block under `🖥️ *Hostinger app cluster status*`, full report + `⚠️ exited non-zero` on non-success, `timeout=180` intact. Bug spec: `docs/bugs/2026-06-14-bugfix-hostinger-status-report-fence.md`; primary spec: `docs/bugs/2026-06-14-bugfix-hostinger-status-report-body.md`; gates `py_compile` + `_agent_audit` clean.
- **OPEN (diagnosed 2026-06-14):** 9 ArgoCD apps `SYNC=Unknown` — still target dead `ubuntu-k3s` (retarget `4e603170` in git not applied to hub). Fix = re-apply ApplicationSets (`deploy_argocd_bootstrap --skip-appproject`, defaults `APP_CLUSTER_NAME=ubuntu-hostinger`) — LIVE-migrates 9 workloads onto Hostinger; awaiting user decision.
- **FIXED (Claude direct, 2026-06-15):** webhook `cluster-up/down/resume` dispatch — applied directly (user-authorized; Codex busy on Go rewrite) on `k3d-manager-v1.7.1`. Spec: `docs/bugs/v1.7.1-bugfix-webhook-cluster-dispatch.md` (consolidates `docs/issues/2026-06-15-cluster-up-not-in-standalone-slack-commands.md` + `...-hostinger-routes-to-k3s-aws.md`). Added `cluster-up/down/resume` to `_STANDALONE_CMDS` (top-level posts were dropped); allowlists on lines 680/702/724 now `("aws","gcp","az","hostinger")` default `"hostinger"` (was `aws` → silent ACG sandbox run, job `4570a5e0`). ACG untouched (aws provider, explicit). Gates: `python3 -m py_compile bin/k3dm-webhook` OK, `_agent_audit` exit 0. CHANGELOG `[Unreleased] Fixed` added. Commit msg: `fix(webhook): route cluster-up/down/resume standalone + hostinger provider (default hostinger, not aws)`. Operator follow-on: `make restart-webhook`. Must land before testing Codex's Go rewrite.
- **VERIFIED (Claude, 2026-06-14):** Default `/cluster-up|status|refresh` to Hostinger + add Hostinger refresh — k3d-manager commit `001ae8e6` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.7.1-cluster-default-hostinger.md`; `workers/slack-relay/index.js` now defaults `/cluster-up`, `/cluster-status`, and `/cluster-refresh` to `hostinger` while keeping `/cluster-down` sandbox-default unless explicitly set to hostinger, relays provider on up/status/refresh/down, and leaves `/cluster-resume` unchanged; `bin/k3dm-webhook` now maps `hostinger→k3s-hostinger` in both `_provider_map` sites, routes hostinger down through `make down CLUSTER_PROVIDER=k3s-hostinger`, accepts hostinger on `/api/v1/cluster`, adds hostinger-aware thread dispatch for `cluster-status` and `cluster-refresh`, and introduces `_run_hostinger_refresh`; `Makefile` `refresh` is provider-cased so `CLUSTER_PROVIDER=k3s-hostinger` runs `./scripts/k3d-manager refresh_cluster`; `scripts/lib/core.sh` now exposes public `refresh_cluster()` dispatch; `scripts/lib/providers/k3s-hostinger.sh` now implements `_provider_k3s_hostinger_refresh_cluster()` by reusing `_hostinger_merge_kubeconfig` + `_hostinger_register_cluster` and verifying `/healthz`; validation used `shellcheck -S warning scripts/lib/core.sh scripts/lib/providers/k3s-hostinger.sh`, `bash -n scripts/lib/core.sh scripts/lib/providers/k3s-hostinger.sh`, `node --check workers/slack-relay/index.js`, `python3 -m py_compile bin/k3dm-webhook`, `./scripts/k3d-manager _agent_audit`, `bats scripts/tests/lib/provider_contract.bats`, `git grep -n '_provider_map = '` bin/k3dm-webhook` (showing both updated occurrences), and `make -n refresh` / `make -n refresh CLUSTER_PROVIDER=k3s-hostinger`; commit message: `feat(slack): default /cluster-up|status|refresh to Hostinger; add hostinger refresh`
- **VERIFIED (Claude, 2026-06-14):** Slack `/acg-*` → `/cluster-*` hard cutover `2472b525` — PASS (both gates empty, 2 files only, worker + webhook renamed, keep-list intact, node/py_compile/audit clean). Cosmetic deviation: duplicate `"cluster-status"` in `_THREAD_COMMANDS` (set dedupes, harmless) **fixed by Claude** in `0d34995d` per spec B1 + `make restart-webhook`. Cutover NOT live until operator runs `make deploy-worker` + re-registers 5 slash commands at api.slack.com. Spec: `docs/plans/v1.7.1-rename-slack-commands-cluster.md`.
- **VERIFIED (Claude, 2026-06-14):** `bin/acg-*` → `bin/cluster-*` rename `bd1f7754` — PASS (git mv renames, grep gate empty, `make -n` → `bin/cluster-*`, 11/11 BATS, shellcheck/py_compile/audit clean). Justified deviation: `docker` stub added to `cluster_down.bats` (disclosed `docs/issues/2026-06-14-cluster-down-bats-docker-prune-stub.md`; `bin/cluster-down:241` `docker system prune` under `set -e` fails without daemon) — accepted. Follow-ups `b85e0ec2`, `f90d1954`.
- **COMPLETE:** `bin/acg-*` → `bin/cluster-*` rename — k3d-manager commit `bd1f7754` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.7.1-rename-acg-to-cluster-binaries.md`; 5 binaries (`bin/acg-up|down|refresh|status|sync-apps`) and 3 BATS suites were renamed with `git mv`; `Makefile` now points the provider-aware targets at `bin/cluster-up|down|refresh|status|sync-apps`; `bin/k3dm-webhook` `_run_cluster_refresh` now references `bin/cluster-refresh`; renamed scripts and suites only changed self-name/path refs while preserving ACG-concept labels and internal constants; validation used `shellcheck -S warning bin/cluster-up bin/cluster-down bin/cluster-refresh bin/cluster-status bin/cluster-sync-apps`, `bash -n` on each renamed script, `python3 -m py_compile bin/k3dm-webhook`, `./scripts/k3d-manager _agent_audit`, `bats scripts/tests/bin/cluster_up.bats scripts/tests/bin/cluster_down.bats scripts/tests/bin/cluster_sync_apps.bats`, `make -n up`, `make -n down`, `make -n refresh`, `make -n status`, `make -n sync-apps`, and `grep -rn "bin/acg-" bin/ scripts/tests/bin/ workers/ Makefile` (no output); commit message: `refactor(bin): rename acg-* cluster scripts to cluster-* (make/slack interface unchanged)`
- **COMPLETE:** Slack `/hostinger-status` + ACG-lab-only relabel — k3d-manager commit `227de0f3` on `k3d-manager-v1.7.1`; spec: `docs/plans/v1.7.1-slack-hostinger-status.md`; `workers/slack-relay/index.js` now allowlists `/hostinger-status`, relabels `/acg-up` and `/acg-down` as ACG lab sandbox actions in the acks, and forwards `/hostinger-status` to the webhook; `bin/k3dm-webhook` now registers `hostinger-status` in `_THREAD_COMMANDS`/`_STANDALONE_CMDS`, adds a `/hostinger-status` thread-dispatch branch, introduces `_run_hostinger_status` to run `bin/hostinger-status`, exposes `/api/v1/hostinger-status`, and relabels the queued `/acg-up`/`/acg-down` notifications; validation used `node --check workers/slack-relay/index.js`, `python3 -m py_compile bin/k3dm-webhook`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(slack): add /hostinger-status; relabel acg-up/acg-down as ACG-lab-only`
- **PR #95 MERGED (e6156a2ab49fda1652cd1208107646fdd30be459) 2026-06-13:** `feat: k3s-hostinger cluster provider (v1.7.0)` — https://github.com/wilddog64/k3d-manager/pull/95. Post-merge complete: enforce_admins RESTORED ✓; v1.7.0 tag + release created ✓; k3d-manager-v1.7.1 branch created ✓; retrospective committed ✓; memory-bank updated ✓
- **COMPLETE:** ApplicationSet retarget `ubuntu-k3s`→`ubuntu-hostinger` — k3d-manager commit `4e603170` on `k3d-manager-v1.7.0`; spec: `docs/plans/v1.7.0-applicationset-retarget-hostinger.md`; `scripts/etc/argocd/applicationsets/services-git.yaml` and `scripts/etc/argocd/applicationsets/observability-acg.yaml` now parameterize `destination.name` with `${APP_CLUSTER_NAME}`, and `scripts/plugins/argocd.sh` defaults/exports `APP_CLUSTER_NAME=ubuntu-hostinger` while including it in the `envsubst` allowlist so the Hostinger app cluster is the default target but `APP_CLUSTER_NAME=ubuntu-k3s` still overrides for ACG; validation used `shellcheck -S warning scripts/plugins/argocd.sh`, `git grep -n 'name: ubuntu-k3s' scripts/etc/argocd/applicationsets/` (no output), and envsubst render checks for both default and override values; commit message: `feat(argocd): parameterize ApplicationSet destination via APP_CLUSTER_NAME (default ubuntu-hostinger)`
- **COMPLETE:** `make status CLUSTER_PROVIDER=k3s-hostinger` full report — k3d-manager commit `a5b9073a` on `k3d-manager-v1.7.0`; spec: `docs/plans/v1.7.0-k3s-hostinger-status-report.md`; added new read-only `bin/hostinger-status` (sourced via `scripts/lib/system.sh`) that reports App Nodes, App Pods, API `/healthz`, hub ArgoCD registration secret `cluster-ubuntu-hostinger` in `cicd`, and ArgoCD Apps/ApplicationSets, and repointed the Makefile `k3s-hostinger` status arm to it; validation used `shellcheck -S warning bin/hostinger-status`, `bash -n bin/hostinger-status`, `./scripts/k3d-manager _agent_audit`, and `make -n status CLUSTER_PROVIDER=k3s-hostinger`; commit message: `feat(k3s-hostinger): full make status report via bin/hostinger-status`
- **COMPLETE:** default `HOSTINGER_HOST` via config vars — k3d-manager commit `f13529f7` on `k3d-manager-v1.7.0`; spec: `docs/bugs/2026-06-13-bugfix-hostinger-host-default.md`; new `scripts/etc/hostinger/vars.sh` now provides env-overridable defaults for `HOSTINGER_HOST` (`srv1754834.hstgr.cloud`), `HOSTINGER_SSH_USER`, and `HOSTINGER_SSH_KEY`, and `scripts/lib/providers/k3s-hostinger.sh` sources that vars file and updates the deploy help text so `HOSTINGER_HOST` is documented as defaulted rather than REQUIRED; validation used `shellcheck -S warning scripts/etc/hostinger/vars.sh scripts/lib/providers/k3s-hostinger.sh`, `bash -n scripts/lib/providers/k3s-hostinger.sh`, `./scripts/k3d-manager _agent_audit`, and `make -n up CLUSTER_PROVIDER=k3s-hostinger` (still expands to `deploy_cluster --confirm`); commit message: `feat(k3s-hostinger): default HOSTINGER_HOST via scripts/etc/hostinger/vars.sh`.
- **COMPLETE + VERIFIED BY CLAUDE:** register `ubuntu-hostinger` with hub ArgoCD — k3d-manager commit `2fd9e179` (memory `c0be23ef`) on `k3d-manager-v1.7.0`; spec: `docs/plans/v1.7.0-k3s-hostinger-argocd-register.md`; preserves operator current-context in merge, adds `_hostinger_register_cluster` (mTLS `cluster-ubuntu-hostinger` secret via `mktemp`+heredoc+`set +x`, no `--from-literal`) + `_hostinger_deregister_cluster`, wires into deploy/destroy; commit `feat(k3s-hostinger): register app cluster with hub ArgoCD via mTLS cluster secret`. **Claude verify:** gates clean (shellcheck/`bash -n`/`_agent_audit`, no `--from-literal`); **live** re-deploy preserved hub `current-context: k3d-k3d-cluster`, secret created in `cicd` (mTLS caData/certData/keyData, server `https://2.25.146.252:6443`), secret-derived creds ran `kubectl get nodes` → node Ready. Next: ApplicationSet retarget `ubuntu-k3s`→`ubuntu-hostinger`.
- **SMOKE TEST PASS (2026-06-13):** k3s-hostinger provider proven live against the real VPS — k3s `v1.35.5+k3s1`, node `srv1754834` Ready, `ubuntu-hostinger` context merged, node labeled, system pods Running, traefik/servicelb disabled. Cluster LIVE (ran `deploy_cluster --confirm` with `HOSTINGER_HOST=2.25.146.252`).
- **COMPLETE + VERIFIED BY CLAUDE:** k3s-hostinger k3sup bootstrap + host-to-IP resolution — k3d-manager commit `e5fb99ce` (memory `41b407ae`) on `k3d-manager-v1.7.0`; spec: `docs/bugs/2026-06-13-bugfix-hostinger-k3sup-load-and-ip-resolve.md`; sources `shopping_cart.sh` for `_ensure_k3sup` + adds `_hostinger_resolve_ip` (IPv4 passthrough / `dig`/`getent`/`python3`) before k3sup `--ip`; shellcheck/`bash -n`/`_agent_audit` clean, resolver unit-tested, **live hostname re-test exit 0 (both bugs gone, node Ready)**; commit message: `fix(k3s-hostinger): source shopping_cart for _ensure_k3sup; resolve host to IP for k3sup`
- **COMPLETE:** `make up CLUSTER_PROVIDER=k3s-hostinger` deploy-gate fix — k3d-manager commit `72d1d2e3` on `k3d-manager-v1.7.0`; spec: `docs/bugs/2026-06-13-bugfix-hostinger-make-up-confirm.md`; `Makefile` now passes `--confirm` in the `k3s-hostinger` `up` arm so `deploy_cluster` clears `__k3dm_deploy_guard_args` and the provider actually runs; validation used `make -n up CLUSTER_PROVIDER=k3s-hostinger` (showed `deploy_cluster --confirm`) and `git diff --check`; commit message: `fix(make): pass --confirm to k3s-hostinger up arm so deploy gate accepts it`.
- **HOSTINGER VPS LIVE + SPEC WRITTEN (2026-06-13):** permanent single-node app-cluster host `2.25.146.252` (Ubuntu 24.04, 2vCPU/8GB) provisioned + hardened (ops user `ubuntu`+passwordless sudo, root SSH + password auth disabled, Docker off). **SPEC (awaiting handoff to Codex):** `docs/plans/v1.7.0-k3s-hostinger-provider.md` — thin `k3s-hostinger` provider (NEW `providers/k3s-hostinger.sh` SSH/k3sup, no VM lifecycle; register in provider.sh + core.sh + Makefile; `HOSTINGER_HOST` env, no hardcoded IP). Commit: `feat(provider): add k3s-hostinger single-node app-cluster provider (SSH/k3sup, no VM lifecycle)`. Replaces ACG EC2 app-cluster leg; infra stays on laptop hub.
- **COMPLETE:** k3s-hostinger sudo de-obfuscation + spurious issue doc removed — k3d-manager commit `6c592550` on `k3d-manager-v1.7.0`; spec: `docs/bugs/2026-06-13-bugfix-hostinger-sudo-deobfuscate.md`; `scripts/lib/providers/k3s-hostinger.sh` now writes the remote uninstall command as a single `_run_command -- ssh … 'sudo /usr/local/bin/k3s-uninstall.sh'` line so `_agent_audit`'s sanctioned wrapper exemption applies, and the out-of-spec `docs/issues/2026-06-13-webhook-live-tests-require-running-service.md` was deleted; validation used `shellcheck -S warning scripts/lib/providers/k3s-hostinger.sh` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(k3s-hostinger): write remote sudo via _run_command line (de-obfuscate); drop spurious issue doc`
- **RELEASED:** k3d-manager v1.6.5 — PR #94 merged to main (SHA `56eeb58c7303ec28e864ab6cf93c9063a24b5c27`); enforce_admins restored ✓; v1.6.5 tag created + pushed ✓; GitHub release created ✓; k3d-manager-v1.7.0 branch created ✓; retrospective committed (`bbf79dbd`) ✓; memory-bank updated ✓
- **COMPLETE:** provider-aware refresh/status via recorded active-provider state — k3d-manager commit `d0c4959b` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-acg-refresh-status-provider-aware.md`; `scripts/lib/provider.sh` now has `_ACG_ACTIVE_PROVIDER_FILE` plus `_acg_normalize_provider`, `_acg_provider_context`, `_acg_record_provider`, and `_acg_resolve_provider` (explicit env > active-provider file > reachable-context probe > default `k3s-aws`); `bin/acg-up` records the normalized provider on success, `bin/acg-down` clears the active-provider file after teardown, `bin/acg-refresh` resolves provider/context once, sources Azure/GCP plugins only when needed, uses provider-specific credentials, threads the active provider through the tunnel host, and replaces the remaining `ubuntu-k3s` refresh literals with `${_app_context}`; `bin/acg-status` now self-resolves the provider/context and only shows the AWS credentials block for AWS; `Makefile` forwards `CLUSTER_PROVIDER` only when explicitly set and lets `bin/acg-status` resolve itself for non-OCI providers; validation used `shellcheck -S warning scripts/lib/provider.sh bin/acg-up bin/acg-down bin/acg-refresh bin/acg-status`, `bats scripts/tests/lib/provider_contract.bats`, `./scripts/k3d-manager _agent_audit`, `git grep -n 'ubuntu-k3s' -- bin/acg-refresh` (no output), `make -n refresh`, `make -n refresh CLUSTER_PROVIDER=k3s-az`, `make -n status`, `make -n status CLUSTER_PROVIDER=k3s-az`, and `make -n status APP_CONTEXT=ubuntu-azure`; commit message: `fix(acg): provider-aware refresh/status via recorded active-provider state`
- **ACG SANDBOX BLOCK — temporary 72h (2026-06-12→13):** all-provider sandbox creation blocked ("repeated detected misuse" / "10+ instances") from the Azure SP recreate storm. **Resumes ~2026-06-16.** Account cancelled but usable until **2027-03-07**. Do NOT provision during the window; land the lib-acg re-extract spec first so resumed access doesn't re-trip.
- **BUG DOC (lib-acg `feat/v0.1.8`, spec written — awaiting user OK to hand to Codex):** Azure SP panel flaky-render → device-code hang (`wait "$az_pid"` no timeout) or abuse-flagged recreate. Spec: `docs/bugs/2026-06-12-az-sp-reextract-before-recreate.md`. `bin/acg-credential-test`: re-extract-first Step 3 (page reload ×`AZURE_SP_EXTRACT_ATTEMPTS`=5, then cap-1 `_do_restart` + `AZURE_SP_RECREATE_COOLDOWN`=30s, then device-code) + bounded `AZURE_DEVICE_CODE_TIMEOUT`=120s wait. Gate: `shellcheck -S warning bin/acg-credential-test`. NO live test until ACG block clears. Commit: `fix(credential-test): re-extract Azure SP panel before recreate; bound device-code wait`
- **lib-acg v0.1.5 post-merge COMPLETE:** PR #42 merged to lib-acg main (`180beb8`); enforce_admins restored ✓; v0.1.5 tag + release created ✓; feat/v0.1.6 branch created ✓; retrospective committed ✓; k3d-manager subtree sync complete (`0008c065`) ✓
- **COMPLETE:** Sandbox delete conflict blocked by open panel — lib-acg commit `cdd9ba4` on `feat/v0.1.7`; spec: `docs/bugs/2026-06-12-sandbox-delete-conflict-blocked-by-open-panel.md`; `playwright/lib/sandbox.js` now closes the target panel before conflict deletion and stops `_waitForCredentials` from falling through to reopen logic when partial credentials are visible but no panel-scoped Start Sandbox was found; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): close target panel before conflict deletion; prevent reopen fall-through in credential wait`
- **COMPLETE:** SSH host-key hygiene — non-interactive provisioning + automated known_hosts prune — k3d-manager commit `2cfeec24` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-acg-up-prune-known-hosts.md`; `scripts/plugins/shopping_cart.sh` now seeds raw EC2 host keys before both `k3sup` calls and adds `StrictHostKeyChecking=no`/`UserKnownHostsFile=/dev/null` to the two bare `ssh` heredocs, while `bin/acg-up` now prunes stale globally-routable IPv4 entries from `~/.ssh/known_hosts` at Step 0/12 and keeps hostname/private/loopback/CGNAT/hashed lines; validation used `shellcheck -S warning scripts/plugins/shopping_cart.sh bin/acg-up`, a dry-run of the prune filter over hostname + private + loopback + public-IP samples, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(ssh): non-interactive host keys in shopping_cart + prune known_hosts on acg-up preflight`
- **COMPLETE:** lib-acg playwright preflight cwd-independent fix — lib-acg commit `9bc4390` on `feat/v0.1.8`; spec: `docs/bugs/2026-06-12-playwright-preflight-cwd-resolution.md`; `scripts/plugins/acg.sh` and `scripts/plugins/gcp.sh` now prefix all three `node -e "require('playwright')"` preflight checks with `NODE_PATH="${_LIB_ACG_ROOT}/node_modules"` so consumer-repo-root invocations no longer false-fail; validation used `shellcheck -S warning scripts/plugins/acg.sh scripts/plugins/gcp.sh`, `git grep -n 'node -e "require('\''playwright'\'')"' scripts/plugins/` (all 3 NODE_PATH-prefixed), and a `/tmp` functional check proving the bare `node -e "require('playwright')"` fails while `NODE_PATH=/Users/cliang/src/gitrepo/personal/lib-acg/node_modules node -e "require('playwright')"` passes; operator follow-up: subtree-pull into k3d-manager after merge; commit message: `fix(acg,gcp): resolve playwright preflight from _LIB_ACG_ROOT (cwd-independent)`
- **COMPLETE:** Slack relay az provider alias fix — k3d-manager commit `fc00daf3` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-slack-relay-az-provider.md`; `workers/slack-relay/index.js` now accepts `az` as the canonical provider token, maps `azure` to `az`, and normalizes both `/acg-up` and `/acg-resume` before falling back to `aws`; validation used `node --check workers/slack-relay/index.js` and a normalization probe confirming `az -> az`, `azure -> az`, `aws -> aws`, `gcp -> gcp`, and garbage -> `aws`; operator follow-up: run `cd workers/slack-relay && npx --yes wrangler deploy` so Slack picks up the alias fix; commit message: `fix(slack-relay): accept az provider (+ azure alias) for /acg-up and /acg-resume`
- **COMPLETE:** Sandbox conflict detection via banner text — lib-acg commit `b57131c` on `feat/v0.1.7`; spec: `docs/bugs/2026-06-12-sandbox-conflict-detection-auto-shutdown-only.md`; `playwright/lib/sandbox.js` now detects conflicts from the always-visible "shut down your current X sandbox" banner, skips disabled Start Sandbox buttons during credential wait, and re-checks the conflict banner before treating a disabled Start Sandbox as "already running"; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): detect conflict via banner text; skip disabled Start Sandbox in credential wait`
- **COMPLETE:** Azure SP validation restart-on-failure fix — lib-acg commit `3b72a4a` on `feat/v0.1.7`; spec: `docs/bugs/2026-06-12-acg-credential-test-azure-sp-invalid-no-restart.md`; `bin/acg-credential-test` now mirrors the AWS restart pattern for Azure SP validation failures by calling `_do_restart`, waiting for CDP readiness, re-extracting credentials, and re-validating before falling back to the existing Azure auth failure message; validation used `shellcheck -S warning bin/acg-credential-test`; commit message: `fix(credential-test): restart sandbox on Azure SP validation failure — mirrors AWS restart pattern`
- **ARCH CORRECTION:** `scripts/lib/core.sh` is the **local** override (allowlist lines 19, 782), NOT the subtree — subtree is at `scripts/lib/foundation/`; dispatcher sources local core.sh last so it shadows foundation's `_cluster_provider`. ⇒ `976a9617` was NOT a subtree violation; backout was on a false premise. Rename handled as **local edit** (user choice).
- **MERGED:** lib-foundation extensibility hook — [PR #30](https://github.com/wilddog64/lib-foundation/pull/30) merged `9c0f5ba` (hook `f7a9178` + Copilot stdout-leak fix `8de372f` + issue doc `5509c1b`; 21/21 BATS). Subtree pulled into k3d-manager `61273526`. Hook is **library-only** (shadowed by local core.sh — NOT wired into k3dm runtime). lib-foundation `main` unprotected, no tag (CHANGELOG `[Unreleased]`).
- **COMPLETE:** Rename CLUSTER_PROVIDER `k3s-azure` → `k3s-az` (hard) + slack arg `azure` → `az` — k3d-manager commit `46d9b360` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-rename-k3s-azure-to-k3s-az.md`; local-edit approach confirmed: `scripts/lib/core.sh` (lines 19, 782), `bin/acg-up` (6×), `bin/acg-down` (3×), provider file `scripts/lib/providers/k3s-azure.sh` → `scripts/lib/providers/k3s-az.sh` via `git mv`, and `bin/k3dm-webhook` 7 lines (map 284/398 + tuples 643/665/687/2473/2526) now use `k3s-az` / `az` only; validation used `shellcheck -S warning scripts/lib/core.sh scripts/lib/providers/k3s-az.sh bin/acg-up bin/acg-down`, `python3 -m py_compile bin/k3dm-webhook`, `git grep -n 'k3s-azure' -- scripts/lib/core.sh scripts/lib/providers bin/` (no output), `git grep -n '\"azure\"' -- bin/k3dm-webhook` (no output), and `make status CLUSTER_PROVIDER=k3s-az` (exited 0); operator follow-up: run `make restart-webhook` so Slack picks up the renamed provider arg; commit message: `refactor(provider): rename k3s-azure → k3s-az and slack arg azure → az`
- **COMPLETE:** data-layer StatefulSet presence poll — k3d-manager commit `5864483c` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-data-layer-statefulset-presence-poll.md`; `scripts/plugins/shopping_cart.sh` now replaces the one-shot `kubectl get statefulset` presence check with a bounded 300s `until` poll for `postgresql-orders`, `postgresql-payment`, and `postgresql-products` before the unchanged rollout-status loop runs; validation used `shellcheck -S warning scripts/plugins/shopping_cart.sh`; commit message: `fix(shopping_cart): poll for data-layer StatefulSet presence instead of one-shot check`
- **lib-acg v0.1.7 MERGED + SYNCED:** PR #44 squash-merged (`f5e1f6a`); k3d-manager subtree synced via `git subtree pull --squash` → merge `1b80e3a9` + squash `a2f2234f`; blobs match lib-acg main; enforce_admins restored ✓; NOT pushed yet; v0.1.6 + v0.1.7 untagged (settle at post-merge); NEXT: test `make up CLUSTER_PROVIDER=k3s-azure`
- **BUG DOC:** Azure SP OAuth reprovision budget too small — deadline (420s) + cap (`<3`) allow only ~2–3 cycles but worst case needs 5–6; deadline is the real limiter (one cycle ≈ 240s) — spec: `docs/bugs/2026-06-12-azure-sp-oauth-population-timing.md`; Part 1a probe + Part 1b `AZURE-TIMING` logs → data gate → Part 2 raise cap + deadline; Codex to implement in lib-acg `feat/v0.1.7` (`cdd9ba4` verified working)
- **COMPLETE:** Azure sandbox SP-empty delete+restart recovery — lib-acg PR #43 merged (`696ca83f`); Copilot fixes in `f0bc7e8`; k3d-manager subtree pulled `13b9a43c` on `k3d-manager-v1.6.5`; enforce_admins restored ✓; feat/v0.1.7 created ✓; retro `ebab772` on feat/v0.1.7 ✓
- **COMPLETE:** acg-up Azure fast-path guard fix — k3d-manager commit `96eb6b48` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-azure-group-list-before-az-ok.md`; `bin/acg-up` now initializes `_az_existing_rg` to empty, only runs `az group list` after `_az_ok` has already succeeded, and keeps the existing Playwright extraction slow-path unchanged; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): gate az group list behind _az_ok — unconditional call triggered device code auth`
- **COMPLETE:** acg-up Docker preflight OrbStack VM recovery — k3d-manager commit `a1f5d478` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-docker-preflight-short-wait.md`; `bin/acg-up` now uses `orbctl start` when `orbctl status` reports `Stopped`, removes the `osascript` update-dialog dismiss block, only keeps `open -a` as a fallback when `orbctl` is not in PATH, and waits for both `docker info` and `~/.orbstack/run/docker.sock` to recover before continuing; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): use orbctl start to recover stopped OrbStack VM — remove osascript, open-a is a no-op`
- **COMPLETE:** acg-up Docker daemon preflight — k3d-manager commit `4adf5a1a` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-docker-daemon-precheck.md`; `bin/acg-up` now runs a Docker preflight immediately before Step 3.5, dismisses OrbStack update dialogs with `osascript`, waits up to 60s for `docker info` to recover, falls back to `open -a OrbStack` or `open -a Docker` if needed, and fails fast with a clear `_err` if the daemon still is unavailable; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): add Docker daemon preflight — dismiss OrbStack update dialogs and recover before Hub cluster step`

## Status (v1.6.4 — RELEASED 2026-06-10)
- **RELEASED:** k3d-manager v1.6.4 — PR #93 merged to main (SHA `e2820483a40ca49c17c3be64d37988bfd96fcdd0`); enforce_admins restored ✓; v1.6.4 tag created + pushed ✓; GitHub release created ✓; v1.6.5 branch created ✓; retrospective committed (`0ddd2006`) ✓; memory-bank updated ✓
- **COMPLETE:** ACG restart Start Sandbox detached-overlay fallback — lib-acg commit `1c81cb4` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-acg-restart-start-sandbox-unscoped-fallback.md`; `playwright/acg_restart.js` now falls back to an unscoped `Start Sandbox` check when the panel renders as a detached overlay and the `Close` button shows the panel is open, so `_sandboxNotYetStarted` can still trigger; validation used `node --check playwright/acg_restart.js`; commit message: `fix(restart): add unscoped Start Sandbox fallback for detached panel overlay`
- **COMPLETE:** Azure portal-only restart loop removed — lib-acg commit `d503653` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-azure-portal-only-restart-loop-removed.md`; `bin/acg-credential-test` now removes the portal-only sandbox restart loop and fails fast with a clear delete-and-recreate message when portal-only Azure creds hit MFA, while leaving SP and identity validation unchanged; validation used `shellcheck -S warning bin/acg-credential-test` and `git diff --check -- bin/acg-credential-test`; commit message: `fix(credential-test): remove portal-only restart loop — replace with fail-fast error`
- **COMPLETE:** ACG session-extended toast Escape fallback — lib-acg commit `515f583` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-addlocatorhandler-close-button-closes-credential-panel.md`; `playwright/lib/sandbox.js` now presses `Escape` inside the `addLocatorHandler` for the "session extended" toast instead of clicking an unscoped close button, so the credential panel is no longer closed by the toast-dismiss handler; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): use Escape in addLocatorHandler — unscoped close button was closing credential panel`
- **COMPLETE:** Azure portal device-code fallback — lib-acg commit `24a89f1` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-az-portal-mfa-device-code-fallback.md`; `bin/acg-credential-test` now falls back from `az login --username --password` to `az login --use-device-code` inside `_az_portal_valid`, then validates the session with `az account get-access-token` so MFA-enforced portal-only sandboxes can still authenticate without SP credentials; validation used `shellcheck -S warning bin/acg-credential-test` and `git diff --check -- bin/acg-credential-test`; commit message: `fix(credential-test): add device code fallback in _az_portal_valid — MFA blocks username/password login`
- **COMPLETE:** Azure portal device-code browser automation — lib-acg commit `e9cc187` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-az-portal-device-code-browser-automation.md`; `bin/acg-credential-test` now runs `az login --use-device-code` in the background, captures the device URL and code, and uses new `playwright/acg_azure_device_login.js` via existing Chrome CDP to open the Microsoft device login page and confirm sign-in automatically; validation used `node --check playwright/acg_azure_device_login.js`, `shellcheck -S warning bin/acg-credential-test`, and `git diff --check`; commit message: `fix(credential-test): automate device code sign-in via CDP — replace manual browser prompt`
- **COMPLETE:** Azure provisioning banner wait fix — lib-acg commit `0514cea` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-waitforcredentials-provisioning-panel-closes.md`; `playwright/lib/sandbox.js` now checks the page text for the Azure provisioning banner ("Hang tight" / "Finalizing your playground") before reopening a closed panel in `_waitForCredentials`, waiting 5s instead of throwing while Azure is still starting; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): wait for provisioning banner in _waitForCredentials — panel auto-closes during Azure startup`
- **COMPLETE:** ACG conflict warning provider parse fix — lib-acg commit `5f704d1` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-conflict-warning-parse-delete-provider.md`; `playwright/lib/sandbox.js` now parses the conflicting provider name from the conflict warning text, closes the open target panel, deletes the named provider sandbox directly, and retries Open Sandbox; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): parse conflicting provider from warning text — broken Auto Shutdown detection never found AWS`
- **BUG DOC:** lib-acg Azure credential-test prefers portal login over SP validation — `docs/issues/2026-06-10-lib-acg-azure-credential-test-prefers-portal-over-sp.md`; `make credential-test PROVIDER=azure` extracts `AZURE_CLIENT_ID` / `AZURE_CLIENT_SECRET`, but `bin/acg-credential-test` still fails on the portal `az login --username/--password` branch first (`AADSTS50126`) instead of using the SP credentials.
- **COMPLETE:** Azure Step 1 credentials + azure-vars KV_NAME guard — k3d-manager commit `b3badc83` on `k3d-manager-v1.6.5`; spec: `docs/plans/v1.6.5-bugfix-acg-up-azure-step1-credentials.md`; `bin/acg-up` now performs Azure credential extraction in the `k3s-azure` Step 1 case via `scripts/lib/acg/bin/acg-credential-test`, skips Playwright extraction when Azure CLI credentials are already valid, and populates `AZ_RESOURCE_GROUP` from `az group list` when missing; `scripts/etc/azure/azure-vars.sh` now guards `KV_ID` with `${KV_NAME:+...}` so sourcing the plugin no longer trips `set -u` before Step 1; validation used `shellcheck -S warning bin/acg-up`, `shellcheck -S warning scripts/etc/azure/azure-vars.sh`, `./scripts/k3d-manager _agent_audit`, and `grep -n "_write_azure_credentials" scripts/lib/acg/bin/acg-credential-test`; commit message: `fix(acg-up,azure-vars): guard KV_NAME in azure-vars.sh; extract Azure creds in Step 1 via acg-credential-test`
- **COMPLETE:** Azure SP absent retry fix landed in lib-acg — lib-acg commit `b61e22e` on `feat/v0.1.5`; spec: `lib-acg/docs/plans/v0.1.5-bugfix-azure-sp-absent-retry.md`; `bin/acg-credential-test` now retries sandbox restart and re-extraction up to 3 times when Azure portal-only sandboxes are missing SP credentials, while leaving SP and identity failures permanent; k3d-manager subtree pull still pending; validation used `shellcheck -S warning bin/acg-credential-test` and `git diff --check -- bin/acg-credential-test`; commit message: `fix(credential-test): retry sandbox restart when Azure SP credentials absent`
- **COMPLETE:** ACG conflict-regression cleanup — lib-acg commit `7547130` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-delete-conflicting-sandbox-panelInStartState-global-check-regression.md`; `playwright/lib/sandbox.js` now removes the global `panelInStartState` heuristic entirely, leaving the scoped delete flow to decide whether the conflicting sandbox is running; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): remove panelInStartState heuristic — global DOM check causes bidirectional regression`
- **COMPLETE:** ACG conflict-warning false positive guard — lib-acg commit `488b558` on `feat/v0.1.5`; spec: `docs/bugs/2026-06-11-delete-conflicting-sandbox-false-positive-start-state.md`; `playwright/lib/sandbox.js` now skips the `panelInStartState` heuristic whenever the ACG conflict warning is active, so the conflicting sandbox is no longer misclassified as a safe Start Sandbox state; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): skip panelInStartState heuristic when ACG conflict warning is active`
- **COMPLETE:** k3s-az dispatch function-name fix — k3d-manager commit `90a842af` on `k3d-manager-v1.6.5`; spec: `docs/bugs/2026-06-12-k3s-az-function-name-dispatch.md`; `scripts/lib/providers/k3s-az.sh` now exports `_provider_k3s_az_deploy_cluster` and `_provider_k3s_az_destroy_cluster`, and `bin/acg-down` now calls `_provider_k3s_az_destroy_cluster --confirm`; validation used `shellcheck -S warning scripts/lib/providers/k3s-az.sh bin/acg-down`, `git grep -n 'k3s_azure' -- scripts/ bin/` (no output), and `make up CLUSTER_PROVIDER=k3s-az` reached Step 2/12 and resolved the provider dispatch before the smoke run was stopped; commit message: `fix(provider): rename k3s-az dispatch functions k3s_azure → k3s_az`
- **COMPLETE:** webhook provider arg — k3d-manager commit `3b91b257` on `k3d-manager-v1.6.5`; spec: `docs/plans/v1.6.5-acg-up-slack-provider-arg.md`; `bin/k3dm-webhook` now parses an optional `aws|gcp|azure` provider token for `/acg-up`, `/acg-down`, and `/acg-resume`, defaults to `aws`, and includes the selected provider in the queued Slack notification text; validation used `python3 -m py_compile bin/k3dm-webhook` and `./scripts/k3d-manager _agent_audit`; commit message: `feat(webhook): add optional provider arg to /acg-up, /acg-down, /acg-resume Slack commands`
- **COMPLETE:** acg-up azure wiring — k3d-manager commit `6dda3d20` on `k3d-manager-v1.6.5`; spec: `docs/plans/v1.6.5-acg-up-azure-wiring.md`; `bin/acg-up` now sources the Azure provider, leaves Step 1 as a no-op passthrough for `k3s-azure`, excludes it from the SSH tunnel step, and exits early after the cluster is up; `bin/acg-down` now handles `k3s-azure` teardown and updates its provider usage comment; validation used `shellcheck -S warning bin/acg-up`, `shellcheck -S warning bin/acg-down`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(acg-up): wire k3s-azure provider into bin/acg-up and bin/acg-down`
- **COMPLETE:** k3s-azure provider — k3d-manager commit `d37a011a` on `k3d-manager-v1.6.5`; spec: `docs/plans/v1.6.5-k3s-azure-provider.md`; added new `scripts/lib/providers/k3s-azure.sh` and updated `scripts/lib/core.sh` allowlists so `CLUSTER_PROVIDER=k3s-azure` now provisions an Azure VM, installs k3s with k3sup, merges the kubeconfig as `ubuntu-azure`, and tears the cluster down cleanly; validation used `shellcheck -S warning scripts/lib/providers/k3s-azure.sh`, `shellcheck -S warning scripts/lib/core.sh`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(provider): add k3s-azure provider — VM provision, k3sup install, shopping-cart deploy`
- **COMPLETE:** lib-acg v0.4.0 post-merge + k3d-manager subtree pull — lib-acg PR #41 merged (`adafde3c`); v0.4.0 tag + release created; enforce_admins restored; feat/v0.1.5 branch created; retro committed; k3d-manager subtree pull `57e33ed6` pushed to `k3d-manager-v1.6.5`
- **COMPLETE:** codex repeating same answer — k3d-manager commit `1e7e9292` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-codex-repeat-answer.md`; `bin/k3dm-webhook` now writes `action=ask` for ask sub-jobs and `_find_job_by_thread_ts` skips any job whose `action` file is `ask`, so thread follow-up `/codex` replies no longer re-consume their own prior answers; validation used `python3 -m py_compile bin/k3dm-webhook`, `git diff --check -- bin/k3dm-webhook`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(webhook): skip ask sub-jobs in _find_job_by_thread_ts to stop codex repeating prior answers`
- **BUG DOC:** Azure credential-test screenshots stay in `/tmp` — `docs/bugs/2026-06-09-azure-credential-test-screenshot-archival.md`; `make credential-test PROVIDER=azure` currently reports an Azure screenshot path under `/tmp`, should be archived under `~/.local/share/k3d-manager/screenshots/`.
- **COMPLETE:** ACG restart failure screenshots archived to shared screenshots dir — k3d-manager commit `acb8a16d` on `k3d-manager-v1.6.4`; `scripts/lib/acg/playwright/acg_restart.js` still writes `k3dm-acg-screenshot-<ts>.png` on failure, and `bin/k3dm-webhook` now copies that PNG into `~/.local/share/k3d-manager/screenshots/` and recognizes the archived path in failure analysis; validation used `node --check scripts/lib/acg/playwright/acg_restart.js`, `python3 -m py_compile bin/k3dm-webhook`, `bats scripts/tests/lib/acg.bats`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(webhook): archive ACG restart screenshots to shared dir`
- **REVERTED:** lib-acg subtree sync for Azure panel reopen fix — k3d-manager commit `df7bf539` reverted merge commit `0cf7c02a` / squashed subtree import `b510f592`, restoring the pre-sync state because the upstream lib-acg fix has not been tested or merged into the tracked subtree yet; the Azure panel reopen bug doc remains open for the next round.
- **COMPLETE:** Changelog note for prompt hardening — k3d-manager commit `c248f220` on `k3d-manager-v1.6.4`; `CHANGELOG.md` now records that the `/claude`, `/gemini`, and `/codex` prompts keep raw probe commands and verbose kubectl output out of the Slack `ANSWER:` while preserving concise diagnostic conclusions; no code or doc behavior changed beyond the changelog note; commit message: `docs: note prompt hardening in changelog`
- **COMPLETE:** Remaining fork-based webhook subprocess calls removed — k3d-manager commit `7f1253f9` on `k3d-manager-v1.6.4`; bug doc: `docs/bugs/2026-06-09-webhook-macos-nef-remaining-fork-paths.md`; `bin/k3dm-webhook` now uses `os.posix_spawn` for the last fork-based helper paths (`SLACK_SIGNING_SECRET` / webhook token keychain lookups, ArgoCD label lookups and patching, cleanup commands, and the `make up` launcher in `_run_upgrade`), so the webhook no longer relies on `subprocess.run`/`Popen` in the macOS NEF hot path; validation used `python3 -m py_compile bin/k3dm-webhook`, `git diff --check`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(webhook): remove remaining fork-based subprocess calls`
- **COMPLETE:** Slack thread replies use bare command names — k3d-manager commit `2fcbb73c` on `k3d-manager-v1.6.4`; `docs/howto/slack-slash-commands.md` now says thread replies should use `claude`, `gemini`, or `codex` without a leading `/`, and the thread context section still explains that the last 20 thread messages are fetched and prepended to the agent prompt; validation used `git diff --check -- docs/howto/slack-slash-commands.md`; commit message: `docs: clarify thread replies use bare command names`
- **COMPLETE:** Slack command docs refined for Codex diagnostics — k3d-manager commit `7e7db5ef` on `k3d-manager-v1.6.4`; `docs/howto/slack-slash-commands.md` now says `codex` is for code questions plus read-only kubectl diagnostics for pod investigation, with `kubectl describe` only when a pod is not Running/Completed; this reflects the actual prompt behavior more accurately than the prior blanket "No" entry; validation used `git diff --check -- docs/howto/slack-slash-commands.md`; commit message: `docs: clarify codex read-only kubectl diagnostics in slack commands`
- **COMPLETE:** ACG credentials flow rendered as Mermaid sequence diagram — k3d-manager commit `96c5b8c9` on `k3d-manager-v1.6.4`; `docs/howto/acg-credentials-flow.md` now replaces the ASCII decision tree with a Mermaid `sequenceDiagram` covering the `acg_get_credentials` launch, CDP attach, sign-in, sandbox open/start/resume choices, AWS credential extraction, and manual paste fallback; validation used `git diff --check -- docs/howto/acg-credentials-flow.md`; commit message: `docs: convert acg credentials flow to sequence diagram`
- **COMPLETE:** Slack command docs aligned with current alias surface — k3d-manager commits `972b82bd` + `e492ac90` on `k3d-manager-v1.6.4`; `docs/howto/slack-slash-commands.md` now leads with `/claude`, `/gemini`, and `/codex` as the public agent commands, keeps only a legacy `/ask` compatibility note for thread replies, and documents `/argocd-upgrade <chart_version> [acg|infra]` with the current stage default + behavior; `docs/architecture/cloudflare-slack-relay.md` now describes `/api/v1/argocd-upgrade` as the label-patch workflow and updates the thread-reply example to `/claude`; `README.md` now links the Slack slash-commands guide from the main How-To index; validation used `git diff --check -- docs/howto/slack-slash-commands.md docs/architecture/cloudflare-slack-relay.md README.md`; commit message: `docs: link Slack commands guide from README`
- **COMPLETE:** Pushgateway deployment metrics retry gap — k3d-manager commit `5d4273cd` on `k3d-manager-v1.6.4`; bug doc: `docs/bugs/2026-06-09-pushgateway-deployment-metrics-gap.md`; `bin/k3dm-webhook` now probes `/-/healthy` and retries the Pushgateway metric POST up to 6 times with 5s sleeps before giving up, so transient Pushgateway readiness gaps no longer drop deployment samples from Grafana; validation used `python3 -m py_compile bin/k3dm-webhook`, `./scripts/k3d-manager _agent_audit`, and `git diff --check`; commit message: `fix(webhook): retry Pushgateway metric pushes on transient readiness gaps`
- **BUG DOC:** Prometheus basic auth should be Vault-managed — `docs/bugs/2026-06-09-prometheus-basic-auth-vault-managed.md`; follow-up notes that the current `admin:password` bootstrap and `show-service-passwords` output should be replaced with Vault-backed Prometheus credentials in the next round.
- **COMPLETE:** Prometheus login info in `show-service-passwords` — k3d-manager commit `911f2d84` on `k3d-manager-v1.6.4`; `Makefile` `show-service-passwords` now includes a Prometheus block that reads the Vault root token, fetches `secret/k3d-manager/prometheus-basic-auth`, and prints `https://prometheus.3ai-talk.org` with `user: admin` and `password: password` so the new basic-auth bootstrap is reflected in the service credential report; validation used `make -n show-service-passwords`, `git diff --check -- Makefile`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(makefile): show Prometheus login info in service credentials`
- **COMPLETE:** Prometheus auth bootstrap non-interactive — k3d-manager commit `9f06bd6d` on `k3d-manager-v1.6.4`; bug doc: `docs/bugs/2026-06-08-prometheus-auth-missing.md`; `scripts/plugins/observability.sh` now creates `secret/k3d-manager/prometheus-basic-auth` through the Vault HTTP API instead of the interactive `kubectl run --rm -it vault ... vault kv put ...` path, retries the read after creation, and hard-fails if the secret still cannot be read so `prometheus-web-config` is never rendered with an empty unauthenticated config; validation used `shellcheck -S warning scripts/plugins/observability.sh` and `git diff --check -- scripts/plugins/observability.sh docs/bugs/2026-06-08-prometheus-auth-missing.md`; commit message: `fix(observability): make prometheus auth bootstrap non-interactive`
- **COMPLETE:** docs bug filings + package-lock ignore — k3d-manager commit `e7990da7` on `k3d-manager-v1.6.4`; committed the new 2026-06-08 bug docs under `docs/bugs/` and `docs/issues/`, and added `package-lock.json` to `.gitignore` so the incidental local lockfile stays untracked; commit message: `docs: capture bug docs and ignore package-lock.json`
- **COMPLETE:** webhook slash command aliases + diagnostic tool access + Gemini prompt fix — k3d-manager commit `c29f2662` on `k3d-manager-v1.6.4`; spec: `docs/bugs/v1.6.4-bugfix-combined.md`; `bin/k3dm-webhook` now normalizes `/gemini`, `/claude`, and `/codex` into `ask` dispatch, shows the alias list in the unknown-command help text, spells out allowed diagnostic tools for Claude, and replaces Gemini's "cannot run kubectl" wording with analysis-oriented guidance; `bin/k3dm-ask-bash` now allows read-only diagnostic paths under `/etc`, `/Library/LaunchDaemons`, `/Library/LaunchAgents`, `${HOME}/.kube`, `${HOME}/.local/share/k3d-manager`, `${HOME}/.cloudflared`, `/var/log`, `/usr/local/bin`, `/usr/bin`, `/bin`, and `/opt/homebrew` while still blocking out-of-scope paths; validation used `shellcheck -S warning bin/k3dm-ask-bash` and `python3 -m py_compile bin/k3dm-webhook`; commit message: `feat(webhook): slash command aliases, expanded agent diagnostic tools, Gemini prompt fix`

## Status (v1.6.3 — RELEASED 2026-06-07)
- **RELEASED:** k3d-manager v1.6.3 — PR #92 merged to main (SHA `0d8d24024eeeb6cf2a0701d78d7dbc9acbb719d4`); enforce_admins restored ✓; v1.6.3 tag created + pushed ✓; GitHub release created ✓; v1.6.4 branch created ✓; retrospective committed (`3b0eb93c`) ✓; memory-bank updated ✓
- **COMPLETE:** data-layer sync timeout false failure — k3d-manager commit `e712ef44` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-up-data-layer-sync-timeout-false-failure.md`; `bin/acg-up` now checks the four data-layer StatefulSets (`postgresql-orders`, `postgresql-payment`, `postgresql-products`, `minio`) at the 180s retry timeout, and if they are all Ready it `_warn`s and continues instead of `_err`/`exit 1`; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): demote data-layer sync timeout to warning when StatefulSets are Ready`
- **COMPLETE:** data-layer sync skip-if-ready — k3d-manager commit `63ef22b7` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-data-layer-skip-if-ready.md`; `bin/acg-up` now checks the four data-layer StatefulSets (`postgresql-orders`, `postgresql-payment`, `postgresql-products`, `minio`) on `ubuntu-k3s` before step 10b and, when all are ready, writes the checkpoint and skips the ArgoCD sync wait plus PostSync force-sync path; validation used `shellcheck -S warning bin/acg-up`, `bats scripts/tests/bin/acg_up.bats`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): skip data-layer sync wait when StatefulSets already ready on ubuntu-k3s`
- **COMPLETE:** data-layer ArgoCD controller reconnect wait — k3d-manager commit `0c759324` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-data-layer-argocd-controller-reconnect-race.md`; `bin/acg-up` now waits up to 120s after the ArgoCD application controller rollout for `argocd cluster list -o json` to report `ubuntu-k3s` as `Successful`, warning and continuing if the controller never reconnects before step 10b starts; validation used `shellcheck -S warning bin/acg-up`, `bats scripts/tests/bin/acg_up.bats`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): wait for ArgoCD controller to reconnect to ubuntu-k3s before data-layer sync`
- **COMPLETE:** acg-up EXIT trap kills ArgoCD port-forward — k3d-manager commit `31cfc6d3` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-up-cleanup-kills-argocd-pf.md`; `_acg_up_cleanup` no longer unloads the persistent ArgoCD LaunchAgent on EXIT, so step 4b survives successful exits and failures alike; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): remove ArgoCD port-forward unload from EXIT trap`
- **COMPLETE:** Gemini CLI Warning: banners in Slack — k3d-manager commit `6a5514b8` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-gemini-cli-warnings-in-slack.md`; `bin/k3dm-webhook` now strips `Warning:` banner lines from Gemini CLI output immediately after reading the temp file and before the existing cleanup regexes, so Slack failure analysis no longer includes startup banners; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): strip Gemini CLI Warning: banners from failure analysis output`
- **COMPLETE:** ArgoCD cluster secret stale refresh — k3d-manager commit `b46391f1` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-argocd-cluster-secret-stale.md`; `bin/acg-up` no longer computes `_cp_server` and now registers the ArgoCD app cluster with the default `https://host.k3d.internal:6443` server, while `bin/acg-refresh` adds a new step 2b to refresh the ArgoCD cluster secret with the current `argocd-manager-token` after sandbox rotation; validation used `shellcheck -S warning bin/acg-refresh` and `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up,acg-refresh): refresh ArgoCD cluster secret with host.k3d.internal on each sandbox rotation`
- **COMPLETE:** acg-refresh section 2b `register_app_cluster` unbound — k3d-manager commit `8ebf6d6b` on `k3d-manager-v1.6.3`; bug doc: `docs/bugs/v1.6.3-bugfix-acg-refresh-register-app-cluster-unbound.md`; Codex used `register_app_cluster` (not in scope in `acg-refresh`) instead of spec's direct heredoc+`kubectl apply`; replaced with `mktemp` + xtrace-save/restore + heredoc + `kubectl apply -f` + `rm`; shellcheck clean; commit message: `fix(acg-refresh): replace register_app_cluster with direct kubectl apply in section 2b`
- **COMPLETE:** data-layer ArgoCD sync timeout auto-retry — k3d-manager commit `d42377b5` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-data-layer-sync-timeout-retry.md`; `bin/acg-up` step 10b timeout handler now warns, runs `argocd app sync data-layer --force --timeout 180`, and waits another 180s before `_err` so transient ArgoCD slowness can recover without human intervention; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): auto force-sync data-layer on ArgoCD sync timeout before failing`
- **COMPLETE:** host.k3d.internal DNS fast-path — `bin/k3dm-webhook` `_analyze_failure` now short-circuits for `host.k3d.internal` + `no such host`; CLUSTER_FAILURE/TRANSIENT + `/acg-refresh` advice; webhook restarted PID 54211
- **COMPLETE:** /ask Slack command — k3d-manager commit `12b5874c` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-acg-ask-claude-troubleshoot.md`; `bin/k3dm-webhook` now adds `_run_cluster_ask(job_id, agent, question, response_url, thread_ts=None)` with claude/gemini/codex subprocess dispatch plus 120s timeout/ANSI stripping, exposes `/api/v1/ask`, and `workers/slack-relay/index.js` now allows `/ask` and forwards the agent/question/thread context; validation used `python3 -m py_compile bin/k3dm-webhook` and `node --check workers/slack-relay/index.js`; commit message: `feat(webhook,worker): add /ask Slack command — claude/gemini/codex multi-agent troubleshooting`
- **COMPLETE:** auto-install acg npm deps — k3d-manager commit `8e884706` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-bugfix-acg-npm-deps.md`; `bin/acg-refresh` and `bin/acg-up` now guard `[[ ! -d "${REPO_ROOT}/scripts/lib/acg/node_modules/playwright" ]]` and run `npm --prefix ... install --prefer-offline` when node_modules is missing; commit message: `fix(acg-refresh,acg-up): auto-install acg npm deps when node_modules missing`
- **COMPLETE:** `/acg-resume` Slack command — k3d-manager commit `bcc3a56e` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-slack-resume-command.md`; 13 changes across `bin/acg-up`, `Makefile`, `bin/k3dm-webhook`, `workers/slack-relay/index.js`; commit message: `feat(acg-up,webhook,worker): add /acg-resume Slack command — checkpoint-based pipeline re-entry`
- **COMPLETE:** /ask agent hardening (2026-06-06) — multiple commits on `k3d-manager-v1.6.3`; fixes: gemini tool-approval hang (`5c479f44`), codex skip-git-repo-check (`1df6b73f`), semaphore(2)+max-turns+timeout (`f6096044`), read-only bash sandbox (`d28f15ce`), prompt injection guard+structural separation (`d6588d52`), repo scope enforcement --add-dir+path guard (`01ed9fc5`), thread context injection via conversations.replies (`1c876a49`), filing mode write docs/issues/+git commit/push (`64580490`), fix mode argocd-sync+rollout-restart+delete-pod (`c1a02efc`); docs: guardrails howto updated (`040e7c3b`, `112f2a88`)
- **COMPLETE:** argocd-redis cicd namespace missing on ubuntu-k3s — k3d-manager commit `e11408cb` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-up-argocd-redis-namespace-missing.md`; `bin/acg-up` now explicitly creates `cicd` namespace on `ubuntu-k3s` via idempotent apply before creating argocd-redis secret, removing fragile 120s wait loop; commit message: `fix(acg-up): explicitly create cicd namespace on ubuntu-k3s before argocd-redis secret step`
- **COMPLETE:** `make fix-*` agent fix targets — k3d-manager commits `ccbe1396` + `0ec95290` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-fix-make-targets.md`; `Makefile` now adds `fix-list`, `fix-restart`, `fix-delete-pod`, `fix-sync`, `fix-force-sync`, `fix-eso-refresh`, and `fix-status`; `bin/k3dm-ask-bash` now allows `make fix-*` in fix mode; `bin/k3dm-webhook` fix-mode prompt tells Claude to run `make fix-list` first; `0ec95290` adds inline `## description` to `fix-list:` so it self-documents; `make fix-list` shows all 7 targets
- **COMPLETE:** prometheus port-forward wrong kubectl context — k3d-manager commit `51939697` on `k3d-manager-v1.6.3`; spec: `docs/bugs/2026-06-06-prometheus-port-forward-wrong-kubectl-context.md`; `scripts/etc/launchd/com.k3d-manager.prometheus-port-forward.plist.tmpl` now targets `k3d-k3d-cluster` instead of `ubuntu-k3s` so the `monitoring` namespace is port-forwarded from the hub cluster; validation used `git diff --check` on the plist change; commit message: `fix(launchd): prometheus port-forward plist targets k3d-k3d-cluster not ubuntu-k3s`; user still runs `make install-prometheus-port-forward` manually to reinstall the live plist
- **COMPLETE:** acg-refresh CoreDNS restart-only (no ConfigMap patch) — k3d-manager commit `182c2f7f` on `k3d-manager-v1.6.3`; spec: `docs/bugs/2026-06-06-acg-refresh-coredns-restarts-instead-of-patching-configmap.md`; `bin/acg-refresh` now detects the host IP via `docker exec k3d-k3d-cluster-server-0 getent hosts host.docker.internal`, patches the CoreDNS `NodeHosts` ConfigMap with `host.k3d.internal` prepended ahead of existing entries, then restarts CoreDNS and waits for rollout; validation used `shellcheck -S warning bin/acg-refresh`; commit message: `fix(acg-refresh): patch CoreDNS NodeHosts ConfigMap with host.k3d.internal before restart`
- **COMPLETE:** .ask-sandbox/bash removed from git — commit `4f8e52b8`
- **COMPLETE:** DNS short-circuit advice incomplete — k3d-manager commit `45bc9afd` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-bugfix-dns-short-circuit-advice.md`; `bin/acg-refresh` now restarts CoreDNS on the hub cluster before step 2b and confirms the rollout/status to restore `host.k3d.internal` resolution, while `bin/k3dm-webhook` now explains the CoreDNS NodeHosts cache issue and says `/acg-refresh` both restarts CoreDNS and re-registers the ArgoCD cluster secret before `/acg-resume`; validation used `shellcheck -S warning bin/acg-refresh` and `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(acg-refresh,webhook): restart CoreDNS on hub cluster in acg-refresh to restore host.k3d.internal resolution`
- **COMPLETE:** Gemini side-observation bug filing — k3d-manager commit `f52363cc` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-gemini-bug-filing.md`; `Makefile` now adds `file-bug` after `fix-status`, `bin/k3dm-ask-bash` allows `make file-bug` in fix mode, and `bin/k3dm-webhook` now requires Gemini `ANSWER:` + optional `OBSERVATIONS:` blocks, parses observations into `docs/bugs/YYYY-MM-DD-<slug>.md`, and appends `Also filed: ...` to Slack replies; validation used `shellcheck -S warning bin/k3dm-ask-bash`, `python3 -m py_compile bin/k3dm-webhook`, and `make -n file-bug FILE_TITLE=\"test\" FILE_BODY=\"test\"`; commit message: `feat(webhook,ask-sandbox): gemini side-observation bug filing — structured OBSERVATIONS block + make file-bug target`
- **COMPLETE:** acg-resume kubeconfig fetch retry + Gemini fix advice to /acg-resume — k3d-manager commit `a8c5d6b0` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-resume-kubeconfig-retry-slack-fix.md`; `bin/acg-up` step-3c guard now retries `add_ubuntu_k3s_cluster` up to 3 times with 5s sleeps after checkpoint-skipped step 2, and `_err`s with Slack advice to retry via `/acg-resume` after all attempts fail; `bin/k3dm-webhook` now short-circuits `_analyze_failure` for `context "ubuntu-k3s" does not exist` and updates Gemini's fix hint to `/acg-resume` in Slack; validation used `shellcheck -S warning bin/acg-up` and `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(acg-up,webhook): retry kubeconfig fetch on SSH delay + fix Gemini fix-hint to /acg-resume`
- **COMPLETE:** acg-resume ubuntu-k3s context missing — k3d-manager commit `a4c5bc95` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-acg-resume-ubuntu-k3s-context-missing.md`; `bin/acg-up` now adds a step 3c guard that re-fetches the `ubuntu-k3s` kubeconfig via `add_ubuntu_k3s_cluster` when the context is missing after checkpoint-skipped step 2, and `bin/k3dm-webhook` now short-circuits `_analyze_failure` with targeted advice for `context "ubuntu-k3s" does not exist` telling the user to run `/acg-refresh` and retry `/acg-resume`; validation used `shellcheck -S warning bin/acg-up` and `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(acg-up,webhook): restore ubuntu-k3s kubeconfig on resume + targeted Slack advice`
- **COMPLETE:** /ask codex+claude OBSERVATIONS filing — k3d-manager commit `628ca8a6` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-bugfix-ask-observations-all-agents.md`; `bin/k3dm-webhook` now adds the mandatory `ANSWER:` / optional `OBSERVATIONS:` response format to both `claude_system` and `codex_system`, sets `_observe` for the non-filing claude path and codex path, and routes their shared subprocess finish block through `_parse_gemini_observations` so codex and claude observations file bug docs like gemini; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): extend OBSERVATIONS bug-filing to /ask codex and /ask claude paths`
- **COMPLETE:** /tmp file leak cleanup — k3d-manager commit `cb17dd72` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-tmp-file-leaks.md`; 5 files: `bin/install-sudoers.sh`, `scripts/lib/providers/k3s-oci-storage.sh`, `bin/acg-down`, `bin/k3dm-cleanup`, `Makefile`; commit message: `fix(tmp): add EXIT traps and cleanup for /tmp file leaks from install-sudoers, k3s-oci-storage, and session teardown`
- **COMPLETE:** webhook ask-agent sanitize order — k3d-manager commit `4ebe23c6` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-webhook-ask-sanitize-order.md`; `bin/k3dm-webhook` now sanitizes the raw user question before job context prepend and performs injection-only check in `_run_cluster_ask`; commit message: `fix(webhook): sanitize user question before job context prepend to fix ask-agent rejection`
- **COMPLETE:** dedup bug/issue filing — k3d-manager commit `ce561b0e` on `k3d-manager-v1.6.3`; spec: `docs/plans/v1.6.3-bugfix-dedup-bug-filing.md`; `bin/k3dm-webhook` now glob-checks `docs/bugs/*-<slug>.md` before writing Gemini-filed bug docs and reuses the existing path when found; `Makefile` `file-bug` now exits 0 with `already filed: <path>` if a slug already exists; `CLAUDE.md` now includes a `Docs Conventions` section between Code Style and Security Rules telling agents to dedup before filing into `docs/bugs/` or `docs/issues/`; validation used `python3 -m py_compile bin/k3dm-webhook` and `make -n file-bug FILE_TITLE=\"test\" FILE_BODY=\"test\"`; commit message: `fix(webhook,makefile): guard against duplicate bug filing — skip write if slug already exists in docs/bugs/`

## Status (v1.6.2 — RELEASED 2026-06-05)
- **RELEASED:** k3d-manager v1.6.2 — PR #91 merged to main (SHA `74b320c37cddfaff561d74dd96972713edb559b0`); enforce_admins restored ✓; v1.6.2 tag created + pushed ✓; GitHub release created ✓; v1.6.3 branch created ✓; retrospective committed (`40609b28`) ✓; memory-bank updated ✓

## Status (v1.6.1 — RELEASED 2026-06-05)
- **COMPLETE:** /acg-refresh Slack slash command — k3d-manager commit `ba66f049` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-bugfix-acg-refresh-slack-command.md`; `bin/k3dm-webhook` now accepts `/acg-refresh` and routes it to `/api/v1/cluster-refresh`, while `workers/slack-relay/index.js` relays the slash command to the webhook and preserves thread context; validation used `python3 -m py_compile bin/k3dm-webhook`, `node --check workers/slack-relay/index.js`, `make restart-webhook`, and `cd workers/slack-relay && npx wrangler deploy`; commit message: `feat(webhook,worker): add /acg-refresh Slack slash command to restore tunnel + credentials`
- **RELEASED:** k3d-manager v1.6.1 — PR #90 merged to main (SHA `d2a8615e282b9103a20649dac9a49c64886299fe`); enforce_admins restored ✓; v1.6.1 tag created + pushed ✓; GitHub release created ✓; v1.6.2 branch created ✓; retrospective committed (`f5f1d6bc`) ✓; memory-bank updated ✓
- **COMPLETE:** Keycloak admin token retry on slow start — k3d-manager commit `b603547b` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-bugfix-keycloak-token-retry.md`; `bin/acg-up` now uses a 5-attempt retry loop with 15s sleeps between attempts before fetching the Keycloak admin token, and only kills the port-forward on final failure; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): retry Keycloak admin token fetch up to 5 times on slow start`
- **COMPLETE:** PostSync hooks force-sync — k3d-manager commit `7811e450` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-bugfix-data-layer-postsync-hooks.md`; `bin/acg-up` now force-syncs `data-layer` immediately after `deploy_shopping_cart_data` to trigger PostSync hooks (MinIO bucket init + image upload); validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): force-sync data-layer after deploy to trigger PostSync hooks`
- **COMPLETE:** ESO smoke checks — k3d-manager commit `954576fb` on `k3d-manager-v1.6.2`; spec: `docs/plans/v1.6.2-eso-smoke-check.md`; `_smoke_test_services(retries=None)` now supports the on-demand fast path and appends ESO `ClusterSecretStore/vault-backend` plus `ExternalSecrets` sync status to the post-provision smoke report, with ESO pod-state hints added to the smoke triage `ns_map`; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `feat(webhook): add ESO ClusterSecretStore + ExternalSecrets checks to smoke test`
- **COMPLETE:** acg-status on-demand health check — k3d-manager commit `b3bacf13` on `k3d-manager-v1.6.2`; spec: `docs/plans/v1.6.2-acg-status-health-check.md`; `_run_cluster_status()` now runs `_smoke_test_services(retries=1)` and Gemini triage on demand, `bin/k3dm-webhook` exposes `GET /api/v1/health` for the JSON smoke report, and `bin/acg-status` appends a Service Health section that curls the webhook endpoint with the bearer token from Keychain; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck -S warning bin/acg-status`, and `make restart-webhook`; commit message: `feat(webhook,acg-status): on-demand smoke test + Gemini triage in acg-status and /acg-status slash command`
- **COMPLETE:** Prometheus port-forward LaunchAgent — k3d-manager commit `498dde9e` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-prometheus-port-forward-launchagent.md`; added `scripts/etc/launchd/com.k3d-manager.prometheus-port-forward.plist.tmpl` plus `Makefile` targets `install-prometheus-port-forward` and `uninstall-prometheus-port-forward` so `kubectl port-forward svc/prometheus-operated 19090:9090 -n monitoring --context ubuntu-k3s` stays alive across SSH reconnects; validation used `make -n install-prometheus-port-forward`; commit message: `feat(launchd): add Prometheus port-forward LaunchAgent (ubuntu-k3s:19090)`
- **COMPLETE:** post-provision service smoke test — k3d-manager commit `8f64cccf` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-smoke-test-post-provision.md`; `bin/k3dm-webhook` now adds `_smoke_test_services()` with retrying HTTP checks for ArgoCD, Frontend, Prometheus, Grafana, and product images, appends the smoke results to the post-provision Slack message, and keeps the existing degraded-app Gemini triage loop unchanged while adding a follow-up Gemini triage pass for failed smoke checks; validation used `python3 -m py_compile bin/k3dm-webhook` and `./scripts/k3d-manager _agent_audit`; commit message: `feat(webhook): post-provision service smoke test — HTTP checks for ArgoCD, Frontend, Prometheus, Grafana, product images`
- **COMPLETE:** lib-acg v0.1.2 viewport fix — PR #37 merged (`4d5aa477`); subtree pulled into k3d-manager ✓
- **COMPLETE:** lib-acg v0.1.3 restart-flow viewport fix — lib-acg commit `d04e70d` on `feat/v0.1.3`; spec: `docs/bugs/2026-06-05-acg-restart-start-sandbox-outside-viewport.md`; `playwright/acg_restart.js` now scrolls `_startBtnEarly` and `_startBtnPanel` into view before clicking in the restart flow, and `CHANGELOG.md` adds an `[Unreleased]` fix entry; validation used `node --check playwright/acg_restart.js`; commit message: `fix(acg_restart): scrollIntoViewIfNeeded before Start Sandbox clicks in restart flow`
- **COMPLETE:** lib-acg v0.1.1 navLink fix — PR #36 merged (`f63b7ca3`); subtree pulled ✓
- **COMPLETE:** Gemini duplicate analysis — k3d-manager commits `cb7cc9bb` and `35c66881` on `k3d-manager-v1.6.2`; spec: `docs/bugs/v1.6.2-bugfix-gemini-duplicate-analysis-output.md`; `bin/k3dm-webhook` now removes `GEMINI_CLI_TRUST_WORKSPACE` from the Gemini subprocess env and rewrites the screenshot prompt to avoid file reads, while `bin/k3dm-cleanup` prunes old failure PNGs and Chromium cache dirs, `scripts/etc/launchd/com.k3d-manager.cleanup.plist.tmpl` schedules the daily cleanup LaunchAgent, `Makefile` adds `install-cleanup`, and `bin/acg-down` now prunes Docker build cache/unused images after k3d teardown; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck -S warning bin/k3dm-cleanup`, and `shellcheck -S warning bin/acg-down`; commit message: `feat(cleanup): add state dir cleanup script + LaunchAgent + docker prune on acg-down`
- **COMPLETE:** lib-acg provider pattern refactor — lib-acg PR #35 merged (SHA `2f0a39c0`); 8 Copilot findings fixed (`145bee4`); subtree pulled into k3d-manager (`8742438a` on `k3d-manager-v1.6.2`) ✓
- **COMPLETE:** System daemon plists not reinstalled by acg-refresh — k3d-manager commit on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-system-daemon-plists-not-reinstalled-by-acg-refresh.md`; `bin/acg-refresh` now calls `_system_daemon_install_if_missing` for each of argocd-browser-https, keycloak-browser-http, frontend-browser-http before `_launchd_ensure`, checking plist existence and regenerating from wrapper script when missing; validation used `shellcheck -S warning bin/acg-refresh`
- **COMPLETE:** Prometheus ACG web.config.file conflict — commit `c21b7cc9` on `k3d-manager-v1.6.1`; removed `additionalArgs` + `secrets` from `kube-prometheus-stack-acg-values.yaml`; prometheus-operator v0.79.2 blocks `web.config.file` in `additionalArgs`; all 18 ArgoCD apps now Synced/Healthy; bug doc: `docs/bugs/v1.6.1-bugfix-prometheus-acg-web-config-arg.md`
- **COMPLETE:** Vault port-forward LaunchAgent — k3d-manager commit `8046537f` on `k3d-manager-v1.6.1`; spec: `docs/plans/v1.6.1-vault-port-forward-launchagent.md`; added `scripts/etc/launchd/com.k3d-manager.vault-port-forward.plist.tmpl` plus `Makefile` targets `install-vault-port-forward` and `uninstall-vault-port-forward` so `kubectl port-forward vault-0 18200:8200 -n secrets --context k3d-k3d-cluster` stays alive on mac; validation used `make -n install-vault-port-forward`, `./scripts/k3d-manager _agent_checkpoint`, `./scripts/k3d-manager _agent_lint`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(launchd): add Vault port-forward LaunchAgent to keep port 18200 alive`
- **COMPLETE:** bin/acg-up LaunchAgent RunAtLoad fix — k3d-manager commit `3678b8ef` on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-launchagent-missing-runatload.md`; added `RunAtLoad` to the ArgoCD and Keycloak LaunchAgent heredocs and added a new frontend-port-forward LaunchAgent generator with `RunAtLoad`, `KeepAlive`, and `EnvironmentVariables` `KUBECONFIG`; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): add RunAtLoad and frontend-port-forward generator to LaunchAgent plists`
- **COMPLETE:** /acg-status stale ArgoCD display — k3d-manager commit `6651a192` on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-acg-status-stale-argocd-display.md`; `bin/k3dm-webhook` now softens the unreachable message to say the cluster may be rebuilding or the sandbox may have expired, and adds `_(cached — ACG cluster was unreachable)_` to the ArgoCD apps header when `acg_reachable` is false so stale hub data is clearly labeled; validation used `python3 -m py_compile bin/k3dm-webhook` and the running webhook was restarted with `make restart-webhook`; commit message: `fix(webhook): add stale caveat to ArgoCD apps when ACG cluster unreachable`
- **COMPLETE:** Keycloak port-forward LaunchAgent not reinstalled by acg-refresh — k3d-manager commit `67614ab9` on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-keycloak-launchagent-not-reinstalled-by-acg-refresh.md`; `bin/acg-refresh` now regenerates `com.k3d-manager.keycloak-port-forward` from the wrapper script when the plist is missing before `_launchd_ensure`, using either `${_ACG_STATE_DIR}/bin/keycloak-port-forward.sh` or `${_ACG_STATE_DIR}/keycloak-port-forward.sh`; validation used `shellcheck -S warning bin/acg-refresh`; commit message: `fix(acg-refresh): regenerate Keycloak port-forward plist when missing on refresh`
- **COMPLETE:** Vault port-forward LaunchAgent not installed by acg-up — k3d-manager commit `8769dfde` on `k3d-manager-v1.6.1`; spec: `docs/bugs/v1.6.1-bugfix-vault-port-forward-launchagent-not-installed-in-acg-up.md`; `bin/acg-up` now installs `com.k3d-manager.vault-port-forward` immediately after Vault is confirmed reachable via `sed` substitution into `~/Library/LaunchAgents/`, and `bin/acg-down` unloads and removes the plist after the PID-based stop so the port-forward is managed as a persistent LaunchAgent during macOS provisioning; validation used `shellcheck -S warning bin/acg-up` and `shellcheck -S warning bin/acg-down`; commit message: `fix(acg-up): install Vault port-forward LaunchAgent during provisioning`

## Status (v1.6.0 — RELEASED 2026-06-04)
- **COMPLETE:** Slack slash commands + Worker secrets fixed — Slack app slash command Request URLs updated from `webhook.3ai-talk.org/slack/command` to `https://k3dm-slack-relay.k3dm.workers.dev`; `SLACK_SIGNING_SECRET` and `WEBHOOK_TOKEN` re-synced in Cloudflare Worker; Worker HMAC verify now succeeds; issue doc: `docs/issues/2026-06-04-slack-slash-commands-wrong-url.md`
- **COMPLETE:** webhook plist Python 3.13 fix — changed `~/Library/LaunchAgents/com.k3d-manager.webhook.plist` interpreter from `/usr/bin/python3` (Python 3.9, SIGSEGV on macOS 26.5.1 Beta) to `/opt/homebrew/bin/python3` (Python 3.13); updated `scripts/etc/launchd/com.k3d-manager.webhook.plist.tmpl` to match; bug doc: `docs/bugs/v1.6.0-bugfix-webhook-python39-sigsegv.md`
- **COMPLETE:** webhook restart-orphan Slack notify — k3d-manager commit `1549f4c4` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-webhook-restart-orphan.md`; `_clear_stale_jobs()` now calls `_notify_job` after marking an orphaned job `failed`, reading the `action` file to name the job in the message; validation: `python3 -m py_compile` clean, BATS pass; commit message: `fix(webhook): notify Slack when restart orphans a running job`
- **COMPLETE:** Slack thread commands — k3d-manager commit `1726f1a8` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-slack-thread-commands.md`; plain-text replies in job threads now trigger kill/status/logs/diagnosis actions via the `message.groups` Events API subscription; Worker `/slack/events` pass-through added in earlier commit; commit message: `feat(webhook): Slack thread commands — kill/diagnosis/status/logs from job thread`
- **COMPLETE:** Playwright screenshot diagnosis Phase B — k3d-manager commit `a1087e66` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-playwright-screenshot-diagnosis.md`; `bin/k3dm-webhook` now adds `base64` import, `ANTHROPIC_API_KEY` constant, `_SCREENSHOT_RE` regex, `_call_claude_vision()` (Haiku vision via Anthropic API), and updated `_analyze_failure()` to scan job lines for screenshot path, encode PNG as base64, call Claude vision for browser state description, and append to Gemini prompt; `Makefile` adds `update-webhook-env` target to inject `ANTHROPIC_API_KEY` into LaunchAgent plist; falls back silently when key absent or screenshot missing; validation: `python3 -m py_compile` clean, 391/391 BATS pass; commit message: `feat(webhook): visual diagnosis — Claude vision API on Playwright failure screenshots`
- **COMPLETE:** Playwright screenshot diagnosis Phase A — lib-acg PR #32 merged SHA `9a7c008`; Copilot fix `ae94605` (mode 0o600); subtree pulled `698d351e`; Phase B pending (webhook `_call_claude_vision` + `update-webhook-env` Makefile target on `k3d-manager-v1.6.0`).
- **COMPLETE:** Slack threading for webhook job notifications — k3d-manager commit `ba2e7974` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-slack-threading.md`; `bin/k3dm-webhook` now adds `SLACK_BOT_TOKEN`/`SLACK_CHANNEL_ID`, posts thread-root notifications through `chat.postMessage`, saves `JOB_DIR/<job_id>/thread_ts`, and threads all subsequent job updates, stall alerts, failure analyses, cleanup notices, and post-provision health checks through `_notify_job()` while keeping the incoming-webhook fallback; `Makefile` now exposes `update-webhook-slack` to inject the Slack env vars into the LaunchAgent plist and restart the webhook; validation used `python3 -m py_compile bin/k3dm-webhook`, `bats scripts/tests/lib/webhook.bats`, and `live threading confirmed in #acg-automation (parent ts `1780575482.977489`, threaded reply `1780575483.237519`); bot token injected 2026-06-04 via `make update-webhook-slack` (keychain source, channel `C0B7ZHG2LR4`); commit message: `feat(webhook): Slack threading — group all job notifications into a single thread`
- **COMPLETE:** k3s-aws idempotent re-run guard — k3d-manager commit `d86b9ca6` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-acg-up-idempotency.md`; `scripts/lib/providers/k3s-aws.sh` now checks `kubectl get nodes --context ubuntu-k3s --no-headers` before `deploy_app_cluster --confirm` and skips the k3s deploy when all three nodes are already Ready, preserving idempotent re-runs; validation used `shellcheck -S warning scripts/lib/providers/k3s-aws.sh` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(k3s-aws): skip deploy_app_cluster when nodes already Ready — idempotent re-run`
- **COMPLETE:** Gemini timeout prompt-leak bugfix — commit `bd253426` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-gemini-timeout-leak.md`; `_call_gemini` catches `subprocess.TimeoutExpired` before `Exception` so the full prompt no longer leaks into Slack; timeout raised 60s→120s; webhook restarted.
- **COMPLETE:** acg_watch disown bugfix — k3d-manager commit `d0191b07` on `k3d-manager-v1.6.0`; spec: `docs/bugs/v1.6.0-bugfix-acg-watch-disown.md`; `scripts/lib/providers/k3s-aws.sh` now disowns the watcher immediately after `acg_watch &` so the webhook stdout pipe can close; validation used `shellcheck -S warning scripts/lib/providers/k3s-aws.sh` and `./scripts/k3d-manager _agent_audit` both passed. \n- **COMPLETE:** post-provision health check — k3d-manager commit `864130895cb4f3a69314f418ac87e05a25983ea6` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-post-provision-health-check.md`; `bin/k3dm-webhook` now launches `_run_post_provision_check(job_id, provider)` after successful `make up`, waits 90 seconds, checks the SSH tunnel, triages degraded ArgoCD apps with Claude Haiku, and posts the consolidated report to Slack; validation used `python3 -m py_compile bin/k3dm-webhook`, `bats scripts/tests/lib/webhook.bats`, and `./scripts/k3d-manager _agent_audit`; commit message: `feat(webhook): post-provision health check — AI triage of degraded apps after acg-up`
- **BUG DOC:** stale sudoers install path — `docs/bugs/v1.6.0-bugfix-stale-sudoers-install-path.md`; deployed sudoers uses `/opt/homebrew/opt/coreutils/libexec/gnubin/install` (old); current `bin/install-sudoers.sh` generates `/usr/bin/install`; fix: `sudo bin/install-sudoers.sh`; no code change.
- **COMPLETE:** data-layer ArgoCD ordering bug — k3d-manager commit `deb4c3b24a88cc950d5458d1f0b0a02e5a75ba42` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-data-layer-argocd-ordering.md`; `bin/acg-up` now applies `shopping-cart-infra/argocd/applications/data-layer.yaml` immediately before `deploy_shopping_cart_data` so the ArgoCD sync starts before the wait loop; validation used `shellcheck -S warning bin/acg-up`; commit message: `fix(acg-up): apply data-layer ArgoCD Application before deploy_shopping_cart_data wait loop`
- **COMPLETE:** webhook auto-cleanup after acg-up failure — k3d-manager commit `1eee5c95ae5a2ea73f4ffd347603104ffddc13f8` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-webhook-acg-up-cleanup.md`; `bin/k3dm-webhook` now starts `_run_cleanup(provider)` in a daemon thread after `_finish("failed")` for `action == "up"` failures so AWS teardown runs in the background and posts start/completion Slack notifications; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): auto-cleanup AWS resources after acg-up failure`
- **COMPLETE:** data-layer ArgoCD sync-wait — k3d-manager commit `5a59a8b13834db7cdad0e008ad3cf75843822e49` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-data-layer-argocd-sync-wait.md`; `bin/acg-up` now waits for `shopping-cart-infra/argocd/applications/data-layer.yaml` to report `Synced` before entering `deploy_shopping_cart_data`, and `scripts/plugins/shopping_cart.sh` now fails fast if the `shopping-cart-data` StatefulSet is still missing after ArgoCD sync; validation used `shellcheck -S warning bin/acg-up` and `shellcheck -S warning scripts/plugins/shopping_cart.sh`; commit message: `fix(acg-up): wait for data-layer ArgoCD sync before StatefulSet readiness check`
- **COMPLETE:** webhook failure analysis node state — k3d-manager commit `54ba9c18` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-webhook-failure-analysis-node-state.md`; `bin/k3dm-webhook` now prepends remote cluster node reachability from `kubectl get nodes --context ubuntu-k3s --no-headers` to `_collect_cluster_state()`, keeps pod/app state collection, and updates the Gemini prompt to return `CLUSTER_FAILURE|APP_FAILURE` before `TRANSIENT|PERMANENT` so node-unreachable failures are distinguished from app-only failures; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): add node state to failure analysis; distinguish cluster vs app failure`
- **COMPLETE:** webhook token auto-rotation — k3d-manager commit `91193d5f` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-webhook-token-autorotate.md`; added `bin/rotate-webhook-token` to generate a new 32-byte token, update the `k3dm-webhook-token` Keychain item, push `WEBHOOK_TOKEN` to `workers/slack-relay` via `wrangler secret put`, and restart the LaunchAgent; added `scripts/etc/com.k3d-manager.webhook-token-rotate.plist` with `StartInterval=21600` and `RunAtLoad=false`, plus `Makefile` targets for `rotate-webhook-token` and `install-token-rotator`; validation used `shellcheck -S warning bin/rotate-webhook-token` and `make -n rotate-webhook-token install-token-rotator`; commit message: `feat(webhook): auto-rotate bearer token every 6 hours via LaunchAgent`
- **COMPLETE:** webhook failure analysis — Claude fixes `b6cacaa5`, `d51f6228`, `4712215d`, `85b5337e` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-webhook-failure-analysis.md`; `_analyze_failure` in `_run_cluster` except block guarded with inner try/except so `_finish("failed")` always runs; stdout drained in background thread so `proc.wait()` + `os.killpg` fires immediately when make exits (prevents `sleep 12600` orphan from blocking pipe forever); AI analysis switched from Claude API (paid) to `gemini` CLI free tier — `_call_gemini` shells out to `gemini --model gemini-2.5-flash`; pending live validation of Gemini triage output in Slack.
- **COMPLETE:** webhook failure analysis cluster state enrichment — k3d-manager commit `b0a2ba356dd2c9fd6f6d856b84f8ce18f517fe42` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-webhook-failure-analysis-cluster-state.md`; `bin/k3dm-webhook` now adds `_collect_cluster_state()` before `_analyze_failure`, captures unhealthy pod state from `kubectl get pods -A --context ubuntu-k3s --no-headers` plus ArgoCD app health/sync state from `kubectl get applications -A --context k3d-k3d-cluster -o custom-columns=... --no-headers`, and injects the live cluster section into the Gemini failure prompt only when available; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): enrich failure analysis with live pod and ArgoCD app state`
- **COMPLETE:** webhook startup cleanup — k3d-manager commit `c7c06273bba11ff556f75acf5f1941d38b3f8905` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-webhook-startup-cleanup.md`; `bin/k3dm-webhook` now adds `_clear_stale_jobs()` before `_MAX_JOB_AGE_SECS` and calls it in `__main__` so orphaned `running` jobs are marked `failed` on restart and the concurrent job guard no longer blocks a fresh provision; validation used `python3 -m py_compile bin/k3dm-webhook`; commit message: `fix(webhook): clear stale running jobs on startup`
- **COMPLETE:** webhook stall detection — k3d-manager commit `892cc66e` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-webhook-stall-detection.md`; `bin/k3dm-webhook` now tracks running cluster jobs, performs stall analysis after 6 minutes of silence, and exposes `/api/v1/cluster` `kill`; validation used `python3 -m py_compile bin/k3dm-webhook`.

- **COMPLETE:** failure analysis missing tunnel check bugfix — k3d-manager commit `1609f465` on `k3d-manager-v1.6.0`; spec: `docs/bugs/v1.6.0-bugfix-failure-analysis-missing-tunnel-check.md`; `_collect_cluster_state()` now probes `localhost:6443` (curl 3s timeout) before any kubectl call; tunnel DOWN short-circuits remote calls and returns tunnel status only so Gemini diagnoses CLUSTER_FAILURE/TRANSIENT instead of APP_FAILURE/PERMANENT; tunnel UP proceeds as before; validation: `python3 -m py_compile` clean, 391/391 BATS pass; commit message: `fix(webhook): check SSH tunnel before remote kubectl in failure analysis`
- **COMPLETE:** webhook cleanup stale-connections-only — k3d-manager commit `8b572dac` on `k3d-manager-v1.6.0`; spec: `docs/bugs/v1.6.0-bugfix-webhook-cleanup-stale-connections-only.md`; `_run_cleanup()` now kills `autossh` and removes `ubuntu-k3s` kubeconfig context instead of running `make down KEEP_LOCAL=1`; Slack notification says "stale connections removed" (not "AWS resources removed"); webhook restarted; validation: `python3 -m py_compile` clean.
- **COMPLETE:** lib-acg Playwright sign-in click regression — lib-acg PR #33 merged (SHA `1e32e15`); Copilot found `force:true` insufficient (overlay still intercepts pointer events); switched to `evaluate(el => el.click())` (DOM-triggered, overlay-immune); subtree pulled into k3d-manager as commit `456d4e4c` on `k3d-manager-v1.6.0`; conflict resolved (HEAD had newer three-case handler; applied evaluate fix to the sign-in link branch within it); spec: `docs/bugs/2026-06-04-acg-credentials-signin-click-outside-viewport.md`.
## Status (shopping-cart-infra PR #83 — MERGED 2026-06-03)
- **MERGED:** shopping-cart-infra PR #83 — `fix/product-images-keycloak-login` → `main` (SHA `28a58358347be5a229fdaa1b579f6696830ef4b8`). Fixed minio image upload (python:3.12-slim) and LDAP test user passwords (SSHA hashes in bootstrap.yaml). Two files changed, one follow-up commit (plaintext credential hygiene). Copilot caught credential exposure in comments; fixed in commit `9aba540`. enforce_admins restored ✓; docs/next-improvements branch created ✓; retrospective committed (`54b26a1`) ✓; memory-bank updated ✓

## Status (v1.5.3 — RELEASED 2026-06-01)
- **RELEASED:** k3d-manager v1.5.3 — PR #88 merged to main (SHA `6e6663027b598b6f392601313165c65a6a608d1a`). Four infrastructure reliability fixes + observability ACG stack (Alertmanager + Grafana). Copilot PR #88 review caught 4 valid findings (curl header injection, context scope leak, credential exposure in logs, Grafana password handling); all addressed in follow-up commits. enforce_admins restored ✓; next branch: `k3d-manager-v1.5.4` created ✓; v1.5.3 tag + GitHub release created ✓; retrospective committed (`011ddd5d`) ✓; memory-bank updated ✓

## Status (v1.5.2 — RELEASED 2026-06-01)
- **RELEASED:** k3d-manager v1.5.2 — PR #87 merged to main (SHA `d11e7e78882c41278fd22b76420c98a619a44533`). Docs-only release: releases table accuracy fixes (v1.4.11 missing), memory-bank cleanup, v1.5.1 retrospective added. enforce_admins restored ✓; next branch: `k3d-manager-v1.5.3`; v1.5.2 retrospective committed (`2791d011`) ✓; memory-bank updated ✓
- **COMPLETE:** v1.5.3 observability ACG Alertmanager + Grafana — k3d-manager commit `5cd6ea53` on `k3d-manager-v1.5.3`; spec: `docs/plans/v1.5.3-observability-acg-alertmanager-grafana.md`; pruned noisy default rules in both kube-prometheus-stack values files, enabled ACG Alertmanager on NodePort `30093` with Vault-backed `alertmanager-smtp-secret`, enabled ACG Grafana on NodePort `30030`, and extended `deploy_observability_acg()` to read Vault credentials and create the secret on `ubuntu-k3s`; validation used `shellcheck scripts/plugins/observability.sh`, Ruby YAML parsing for both values files, and the spec-required PyYAML validation failed because `yaml` is not installed in this environment (issue note: `docs/issues/2026-06-01-observability-values-pyyaml-missing.md`).
- **COMPLETE:** Vault CA trust idempotency fix — k3d-manager commit `e99ffcc9` on `k3d-manager-v1.5.3`; spec: `docs/bugs/v1.5.3-bugfix-acg-up-vault-ca-trust-no-soft.md`; `bin/acg-up` now checks `security verify-cert` first, then uses `--interactive-sudo --soft` for `security add-trusted-cert` so repeat runs skip cleanly and warning fallback can fire; validation used `shellcheck -S warning bin/acg-up`, `bats scripts/tests/bin/acg_up.bats`, and `./scripts/k3d-manager _agent_audit`.
- **COMPLETE:** ClusterSecretStore timeout retry fix — k3d-manager commit `ec29ef3b` on `k3d-manager-v1.5.3`; spec: `docs/bugs/v1.5.3-bugfix-cluster-secret-store-timeout.md`; `scripts/plugins/shopping_cart.sh` now retries `kubectl wait` for `clustersecretstore/vault-backend` in three 90s windows and annotates `k3d-manager/reconcile-at` after each timeout to force ESO re-validation; validation used `shellcheck -S warning scripts/plugins/shopping_cart.sh` and `./scripts/k3d-manager _agent_audit`.
- **COMPLETE:** ArgoCD app registration + acg-down sudo routing fix — k3d-manager commit `410ec1e1` on `k3d-manager-v1.5.3`; spec: `docs/bugs/v1.5.3-bugfix-acg-up-register-apps-and-acg-down-sudo.md`; `bin/acg-up` now calls `register_shopping_cart_apps` at Step 11b and `bin/acg-down` routes macOS sudo pre-authorization through `_run_command --interactive-sudo --soft -- true`; validation used `shellcheck -S warning bin/acg-up`, `shellcheck -S warning bin/acg-down`, and `./scripts/k3d-manager _agent_audit`.

## Status (v1.4.12 — RELEASED 2026-05-29)
- **RELEASED:** k3d-manager v1.4.12 — PR #83 merged to main (SHA `c5949cf47097d3a89ff485e01f68c2725644d327`). imagePullSecrets patch, sync-branch/sync-main targets, ArgoCD ApplicationSets in make status. Copilot caught shell injection pattern + incomplete app list. enforce_admins restored ✓; next branch: `k3d-manager-v1.5.0`; v1.4.12 retrospective committed (`3c99fc85`) ✓

## Status (v1.5.1 — RELEASED 2026-05-31)
- **RELEASED:** k3d-manager v1.5.1 — PR #86 merged to main (SHA `a33971244b6ee1414db872c44fa029399ae0daa9`). OCI object storage backup/restore; ACG credential automation improvements; SSH fail-fast and snapshot validation fixes; BATS error-path coverage. Copilot review addressed (7 findings). enforce_admins restored ✓; next branch: `k3d-manager-v1.5.2`; v1.5.1 retrospective committed ✓; memory-bank updated ✓
- **COMPLETE:** shopping-cart-infra PR #81 post-merge — RespectIgnoreDifferences syncOptions addition; merge SHA `1eaf1e47f266f8fc5e1b6d34b8e6ec7234e6c61d`; enforce_admins restored ✓; docs/next-improvements branch recreated ✓; retrospective committed (`4d487c4`) ✓; memory-bank updated ✓
- **COMPLETE:** shopping-cart-infra PR #80 post-merge — ArgoCD data-layer ignoreDifferences fix for StatefulSet volumeClaimTemplates; merge SHA `7c1e64ea18e2f5af967a3797d4138a90fac233cb`; enforce_admins restored ✓; docs/next-improvements branch updated ✓; retrospective committed (`b732fdb`) ✓; memory-bank updated ✓

## Status (v1.5.0 — RELEASED 2026-05-31)
- **RELEASED:** k3d-manager v1.5.0 — PR #84 merged to main (SHA `faa00331214626982fd99d65b3764bca9f4c63ce`). OCI provider (single-node + two-node), observability stack (Prometheus+Grafana+Trivy+Alertmanager), multi-env ArgoCD, passwordless sudo, state-dir refactor, 12 bug fixes, 25 new BATS tests. Copilot review addressed (6 findings). enforce_admins restored ✓; next branch: `k3d-manager-v1.5.1`; v1.5.0 retrospective committed (`cf94879f`) ✓
- **COMPLETE:** ArgoCD persistent OutOfSync fix — shopping-cart-infra PR #79 merged to main (SHA `d5be9712cfb0dbe7d6a9667ba6ec8289dd16e5a9`); enforce_admins restored ✓; docs/next-improvements branch updated with retrospective (SHA `1f63eec`) ✓
- **COMPLETE:** OCI object storage backup/restore — k3d-manager commits `9b8d6a78` and `f5d2ce0e` on `k3d-manager-v1.5.1`; spec: `docs/plans/v1.5.1-oci-object-storage.md`; added `scripts/lib/providers/k3s-oci-storage.sh`, wired `scripts/lib/providers/k3s-oci.sh` to source it and auto-backup after deploy, and added `backup`/`restore` Makefile targets plus `oci_backup`/`oci_restore` CLI dispatch support; validation used `shellcheck scripts/lib/providers/k3s-oci-storage.sh scripts/lib/providers/k3s-oci.sh scripts/k3d-manager scripts/tests/lib/k3s_oci_provider.bats`, `bash -n` on the touched shell files, `bats scripts/tests/lib/k3s_oci_provider.bats`, and `./scripts/k3d-manager oci_backup --help` / `oci_restore --help`; live OCI backup/restore was not run in this workspace (see `docs/issues/2026-05-31-oci-object-storage-live-backup-restore-not-run.md`).
- **COMPLETE:** v1.5.5 observability rules + alertmanager — k3d-manager commit `5fd1f4fd` on `k3d-manager-v1.5.0`; shopping-cart-infra commit `41f00da` on `docs/next-improvements`; spec: `docs/plans/v1.5.5-observability-rules-alertmanager.md`; enabled Alertmanager with Vault-backed `alertmanager-smtp-secret` creation in `scripts/plugins/observability.sh`, updated `scripts/etc/helm/observability/kube-prometheus-stack-values.yaml`, added `Makefile` `alertmanager-secret`, and created `shopping-cart-infra/monitoring/rules/shopping-cart-apps.yaml` plus `shopping-cart-infra/monitoring/rules/argocd-application.yaml`; the ArgoCD Application was applied to the Hub cluster with `kubectl apply --validate=false -f monitoring/rules/argocd-application.yaml --context k3d-k3d-cluster -n cicd`; validation used `shellcheck scripts/plugins/observability.sh`, Ruby YAML parsing for both files, and the spec-required PyYAML command failed because `yaml` is not installed in this environment (issue note: `docs/issues/2026-05-31-shopping-cart-prometheusrules-pyyaml-missing.md`).
- **COMPLETE:** v1.5.6 observability template refactor — k3d-manager commit `001f37ba` on `k3d-manager-v1.5.0`; spec: `docs/plans/v1.5.6-observability-prometheus-templates.md`; moved PrometheusRule content into `scripts/etc/prometheus/rules/shopping-cart-apps.yaml` and Alertmanager config into `scripts/etc/prometheus/alertmanager.yaml.tmpl`; `scripts/plugins/observability.sh` now renders the Alertmanager config with `envsubst`, applies the rules directory directly, and removes the stale `shopping-cart-rules` ArgoCD Application from the Hub cluster; validation used `shellcheck scripts/plugins/observability.sh`, Ruby YAML parsing for both new files, and `kubectl delete application shopping-cart-rules -n cicd --context k3d-k3d-cluster --ignore-not-found` succeeded.
- **ASSIGNED TO CODEX:** State-dir layout reorganization — spec: `docs/plans/v1.5.2-state-dir-layout.md` (commit `e66b3659` on `k3d-manager-v1.5.0`); 7 target files; moves runtime *.sh to `bin/`, *.pid/*.plist to `run/`, *.log to `logs/`; commit message: `refactor(state-dir): reorganize runtime files into bin/ run/ logs/ subdirectories`
- **COMPLETE:** v1.5.4 observability + vuln scan — k3d-manager commit `44f4c92b` on `k3d-manager-v1.5.0`; spec: `docs/plans/v1.5.4-observability-vuln-scan.md`; adds Hub/ACG observability ApplicationSets, Prometheus/Grafana/Trivy Helm values, `scripts/plugins/observability.sh`, `scripts/tests/lib/observability.bats`, observability Makefile targets, and ACG Prometheus port-forward lifecycle in `bin/acg-up`/`bin/acg-down`; validation used `shellcheck -S warning bin/acg-up bin/acg-down scripts/k3d-manager scripts/plugins/observability.sh`, `shellcheck -S warning scripts/tests/lib/observability.bats`, `bats scripts/tests/lib/observability.bats`, and `./scripts/k3d-manager _agent_audit`.
- **COMPLETE:** state-dir layout reorganization — k3d-manager commit `af16d821` on `k3d-manager-v1.5.0`; spec: `docs/plans/v1.5.2-state-dir-layout.md`; updated `bin/acg-up`, `bin/acg-down`, `bin/acg-sync-apps`, `scripts/plugins/argocd.sh`, `scripts/plugins/ssm.sh`, `scripts/lib/acg/scripts/plugins/acg.sh`, and `scripts/lib/acg/scripts/lib/cdp.sh` so runtime wrappers live under `bin/`, PID/plist files under `run/`, and logs under `logs/`.
- **COMPLETE:** lib-foundation interactive sudo no-TTY fix — lib-foundation `00309de` on `feat/v0.3.20`; k3d-manager subtree merge `2501ba97` on `k3d-manager-v1.5.0`; spec: `docs/bugs/v1.5.0-bugfix-run-command-interactive-sudo-no-tty.md`
- **COMPLETE:** Vault CA PTY fix — k3d-manager commit `7b7a8ea2` on `k3d-manager-v1.5.0`; spec: `docs/bugs/v1.5.0-bugfix-acg-up-vault-ca-pty-sudo.md`; `bin/acg-up` now skips `security add-trusted-cert` when the Vault CA is already trusted.
- **COMPLETE:** redis-cart-secret race fix — k3d-manager commit `780930ac` on `k3d-manager-v1.5.0`; spec: `docs/bugs/v1.5.0-bugfix-redis-cart-secret-copy-race.md`; `scripts/plugins/shopping_cart.sh` now waits for `redis-cart-secret` before copying it into `shopping-cart-apps`.
- **COMPLETE:** platform-helm multi-env ArgoCD — k3d-manager commit `0e4268a5` on `k3d-manager-v1.5.0`; spec: `docs/plans/v1.5.0-platform-helm-argocd-multi-env.md`; validation used `shellcheck scripts/plugins/argocd.sh` and Ruby YAML parsing because PyYAML was unavailable in this environment.
- **COMPLETE:** services-git ExternalSecret refreshInterval drift ignore — k3d-manager commit `fd947a59` on `k3d-manager-v1.5.0`; spec: `docs/bugs/v1.5.0-bugfix-services-git-externalsecret-refreshinterval-drift.md`; validation used Ruby YAML parsing because PyYAML was unavailable in this environment.
- **COMPLETE:** shopping-cart ALTER USER cleanup / platform project destination / ESO force-refresh — k3d-manager commits `3ad44199`, `3fee4d61`, and `bbb4d008` on `k3d-manager-v1.5.0`; specs: `docs/bugs/v1.5.0-bugfix-shopping-cart-alter-user-change-me.md`, `docs/bugs/v1.5.0-bugfix-platform-project-missing-cicd-ubuntu-k3s.md`, and `docs/bugs/v1.5.0-bugfix-eso-stale-secrets-force-refresh.md`; validation used `shellcheck -S warning scripts/plugins/shopping_cart.sh` after each `shopping_cart.sh` commit, plus Ruby YAML parsing because PyYAML was unavailable for `platform.yaml.tmpl`.
- **NOTE:** `scripts/tests/bin/acg_up.bats` has one stale assertion mismatch on the ArgoCD browser login URL string; see `docs/issues/2026-05-30-acg-up-bats-argocd-browser-login-url-test-drift.md`.
- **NOTE:** `scripts/tests/test_install_sudoers.bats` needed a repo-root path correction (`../../..` → `../..`) to match this checkout layout; see `docs/issues/2026-05-30-sudo-passwordless-bats-repo-root-mismatch.md`.
- **COMPLETE:** ArgoCD sync before redis deploy — k3d-manager commit `2a37829d` on `k3d-manager-v1.5.0`; spec: `docs/bugs/v1.5.0-bugfix-deploy-data-layer-argocd-sync-before-redis.md`; `deploy_shopping_cart_data()` now runs `argocd app sync data-layer --server localhost:8080 --insecure --wait --timeout 120 2>/dev/null || true` before `kubectl apply -f redis/cart/`.
- **COMPLETE:** OCI provider vars-location correction — k3d-manager commit `72a5d38e` on `k3d-manager-v1.5.0`; spec: `docs/bugs/2026-05-30-oci-vars-wrong-location.md`; moved OCI env defaults to `scripts/etc/oci/vars.sh`, deleted the unconditional `scripts/etc/vars.sh` include from `scripts/k3d-manager`, and sourced the provider-scoped vars file from `scripts/lib/providers/k3s-oci.sh`; validation used `shellcheck scripts/lib/providers/k3s-oci.sh` and `shellcheck scripts/k3d-manager`.
- **COMPLETE:** k3s-oci provider BATS coverage — k3d-manager commit `30cd7adb` on `k3d-manager-v1.5.0`; spec: `docs/plans/v1.5.0-bats-k3s-oci-provider.md`; added `scripts/tests/lib/k3s_oci_provider.bats` with BATS coverage for deploy/destroy/validate/reconfigure flows; validation used `bats scripts/tests/lib/k3s_oci_provider.bats`, `shellcheck scripts/tests/lib/k3s_oci_provider.bats`, and `./scripts/k3d-manager _agent_audit`.
- **COMPLETE:** k3s-oci two-node cluster — k3d-manager commit `2ccbff18` on `k3d-manager-v1.5.0`; spec: `docs/plans/v1.5.0-k3s-oci-two-node.md`; updated `scripts/lib/providers/k3s-oci.sh` to provision server + agent nodes, install Cilium, and smoke-test two Ready nodes; validation used `shellcheck scripts/lib/providers/k3s-oci.sh`, `bash -n scripts/lib/providers/k3s-oci.sh`, and `./scripts/k3d-manager _agent_audit`.
- **COMPLETE:** k3s-oci two-node BATS + agent wait loop fix — k3d-manager commit `823f0349` on `k3d-manager-v1.5.0`; fixed `grep -v master` bug in `_oci_install_k3s_agent` (k3s has no master label; replaced with 2-node Ready count); 25 BATS tests covering step sequencing, idempotency, Cilium flags, private-IP join, smoke-test thresholds, destroy; 25/25 green; shellcheck clean; pushed to origin.
- **SPEC WRITTEN:** platform-helm multi-env ArgoCD — `docs/plans/v1.5.0-platform-helm-argocd-multi-env.md`; 2 files: `platform-helm.yaml` (dev/infra/prod generators + per-cluster labels), `register_app_cluster` (add `argocd-chart-version`/`argocd-replicas` labels); prerequisite for OCI provider

## Milestone: v1.9.0 (Blue/Green + Stress Testing)
- **SPEC WRITTEN:** Argo Rollouts + vCluster blue/green on OCI — `docs/plans/v1.9.0-blue-green-argo-rollouts.md`; two-layer design: vCluster (ephemeral full-stack isolated test env) + Argo Rollouts (production traffic selector swap); stress-runner custom image (vcluster CLI + kubectl + k6 + kustomize, ARM64); AnalysisTemplate creates vCluster → deploys full shopping-cart stack → k6 (20 VUs, 120s) → `trap` cleanup; `autoPromotionEnabled: false`; gates on v1.5.0 + v1.7.0

## Milestone: v1.8.0 (Self-Healing)
- **SPEC WRITTEN:** Alertmanager → k3dm-webhook self-healing — `docs/plans/v1.8.0-self-healing-alertmanager-webhook.md`; 5 handlers (VaultSealed, ArgoCDAppOutOfSync, ESOSecretStale, PodCrashLooping, ACGClusterUnreachable); cooldown per (alert, cluster); Alertmanager webhook receiver config; PrometheusRule CRDs; gates on v1.6.0 (webhook) + v1.7.0 (monitoring)

## Milestone: v1.7.0 (Observability — OCI)
- **SPEC WRITTEN:** Prometheus + Grafana on OCI — `docs/plans/v1.7.0-observability-oci.md`; kube-prometheus-stack on OCI (ARM64, 1 replica); Prometheus agent mode on laptop + ACG with remote_write to OCI; Grafana via `grafana.3ai-talk.org` Cloudflare tunnel; gates on OCI cluster live (v1.5.0). Decision: monitoring on OCI outlives laptop reboots.

## Milestone: v1.6.0 (ArgoCD Automated Upgrade Pipeline)
- **SPEC WRITTEN:** k3dm-webhook server — `docs/plans/v1.6.0-k3dm-webhook-server.md`; Python stdlib HTTP server on laptop; launchd LaunchAgent; exposed via Cloudflare tunnel; stage=acg triggers make up; stage=infra patches infra label; Bearer token auth
- **COMPLETE:** k3dm-webhook BATS suite — `scripts/tests/lib/webhook.bats`; `shellcheck scripts/tests/lib/webhook.bats` passed and `bats scripts/tests/lib/webhook.bats` reported 11 unit tests passing with Level 1/2/3 skipped; commit `a51ff8ff` on `k3d-manager-v1.6.0`; mismatch note recorded in `docs/issues/2026-06-01-webhook-bats-mismatch-path-normalization.md`.
- **COMPLETE:** k3dm-webhook cluster command endpoint — k3d-manager commit `bd02d2d3` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-webhook-cluster-commands.md`; added `_run_cluster` to `bin/k3dm-webhook`, restructured `do_POST` to share auth + size-cap guards, and hardcoded `KEEP_LOCAL=1` for `make down`; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck scripts/tests/lib/webhook.bats`, and `bats scripts/tests/lib/webhook.bats` with 16 unit tests passing and Level 1/2/3 skipped.
- **COMPLETE:** webhook concurrent job TOCTOU race bugfix — k3d-manager commit `c5cb0cbc` on `k3d-manager-v1.6.0`; spec: `docs/bugs/v1.6.0-bugfix-webhook-concurrent-job-race.md`; `bin/k3dm-webhook` now writes `running` in the `/api/v1/cluster` HTTP handler before starting the worker thread and removes the duplicate `running` write from `_run_cluster`; validation used `python3 -m py_compile bin/k3dm-webhook` and `bats scripts/tests/lib/webhook.bats` with 29 tests passing.
- **COMPLETE:** ArgoCD upgrade notification scaffolding — k3d-manager commit `3b290b45` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-argocd-upgrade-notification.md`; added `scripts/etc/argocd/platform-ops/notify.sh`, `scripts/etc/argocd/platform-ops/notification-secret.yaml.tmpl`, and `deploy_argocd_platform_ops` in `scripts/plugins/argocd.sh`; validation used `shellcheck scripts/etc/argocd/platform-ops/notify.sh` and `shellcheck scripts/plugins/argocd.sh` with no warnings.
- **COMPLETE:** ArgoCD CVE scan promotion — k3d-manager commit `775ecaf1` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-argocd-cve-scan-promotion.md`; added `scripts/etc/argocd/platform-ops/rbac.yaml`, `scripts/etc/argocd/platform-ops/cve-scan-cronjob.yaml`, and `scripts/etc/argocd/platform-ops/cve-scan.sh`, and replaced `deploy_argocd_platform_ops` in `scripts/plugins/argocd.sh`; validation used `shellcheck --shell=sh scripts/etc/argocd/platform-ops/cve-scan.sh` and `shellcheck scripts/plugins/argocd.sh` with no warnings.
- **COMPLETE:** ArgoCD CVE scan promotion moved to Hub cluster — k3d-manager commit `89201efb` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-argocd-cve-scan-promotion.md`; updated `scripts/etc/argocd/platform-ops/cve-scan-cronjob.yaml` to mount optional `oci-kubeconfig`, rewrote `scripts/etc/argocd/platform-ops/cve-scan.sh` for direct kubectl stage patching, and updated `deploy_argocd_platform_ops` help text to the Hub/OCI topology; validation used `shellcheck --shell=sh scripts/etc/argocd/platform-ops/cve-scan.sh` and `shellcheck scripts/plugins/argocd.sh` with no warnings.
- **COMPLETE:** Prometheus basic auth — k3d-manager commit `3b3ef58a` on `k3d-manager-v1.6.0`; spec: `docs/bugs/v1.6.0-bugfix-prometheus-basic-auth.md`; added `_prometheus_acg_web_config_secret` to `scripts/plugins/observability.sh` and mounted `prometheus-web-config` plus `web.config.file` in `scripts/etc/helm/observability/kube-prometheus-stack-acg-values.yaml`; validation used `shellcheck -S warning scripts/plugins/observability.sh` and Ruby YAML parsing for the values file, both passing with no output.
- **COMPLETE:** Shopping-cart images + Keycloak login — shopping-cart-infra commit `3710ad5` on `fix/product-images-keycloak-login`; spec: `docs/bugs/v1.6.0-bugfix-shopping-cart-images-keycloak.md`; changed `data-layer/minio/image-upload-job.yaml` from `python:3.12-alpine` to `python:3.12-slim` and documented the LDAP test-user passwords in `identity/ldap/bootstrap.yaml`; validation used Ruby YAML parsing for both files and the branch push to origin succeeded.
- **COMPLETE:** v1.6.0 Worker deploy workflow — k3d-manager commit `d7ebb11f` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-worker-deploy-workflow.md`; created `.github/workflows/deploy-worker.yml` and `bin/k3dm-worker-setup` (0755); validation used `yamllint .github/workflows/deploy-worker.yml`, `shellcheck bin/k3dm-worker-setup`, and `bash -n bin/k3dm-worker-setup` with no errors.
- **COMPLETE:** v1.6.0 acg-down sudoers cleanup — k3d-manager commit `ab4f6e39` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-acg-down-sudoers.md`; removed the redundant macOS pre-auth block from `bin/acg-down` and added a `deploy-worker` target to `Makefile`; validation used `grep -n "Pre-authoriz\|sudo -n true" bin/acg-down` and it returned no matches.
- **COMPLETE:** acg-down orphaned daemons bugfix — k3d-manager commit `81122796` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-acg-down-orphaned-daemons.md`; `bin/acg-down` now unloads the Cloudflare named tunnel, frontend browser HTTP listener, and Keycloak port-forward daemons during teardown; validation used `shellcheck -S warning bin/acg-down`, `./scripts/k3d-manager _agent_audit`, and `grep -n "Pre-authoriz\|sudo -n true" bin/acg-down` returned no output.
- **COMPLETE:** Hub cluster health check bugfix — k3d-manager commit `4bc87455` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-hub-cluster-health-check.md`; `bin/acg-up` Step 3.5 now checks for the `k3d` binary first, creates the Hub cluster when missing, and auto-starts the cluster when listed-but-stopped before retrying API reachability; validation used `grep -n "k3d binary not found\|cluster start\|Waiting for Hub cluster" bin/acg-up`, `shellcheck -S warning bin/acg-up`, and `./scripts/k3d-manager _agent_audit` all passed.
- **COMPLETE:** headless LaunchDaemon / cloudflared privilege escalation bugfix — k3d-manager commit `fa6011d5` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-bugfix-headless-launchd.md`; `bin/acg-up` now skips the three browser LaunchDaemon installs when running headless, moves the Cloudflare named tunnel from `/Library/LaunchDaemons` to `~/Library/LaunchAgents`, and `bin/acg-down` handles the Cloudflare tunnel as a user LaunchAgent while the browser listener teardowns skip in headless context; validation used `shellcheck -S warning bin/acg-up bin/acg-down`, `./scripts/k3d-manager _agent_audit`, and the required grep checks on both files passed.
- **COMPLETE:** v1.6.0 Slack relay + ACG expiry + ArgoCD AI analysis — k3d-manager commit `695a04d1` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-slack-ai-analysis.md`; created `workers/slack-relay/index.js` and `workers/slack-relay/wrangler.toml`, added `scripts/etc/argocd/platform-ops/acg-expiry-cronjob.yaml`, `scripts/etc/argocd/platform-ops/acg-expiry.sh`, `scripts/etc/argocd/platform-ops/prometheusrule.yaml`, and `scripts/etc/argocd/platform-ops/alertmanager-config.yaml`, plus updates to `bin/k3dm-webhook`, `bin/acg-up`, `scripts/etc/argocd/platform-ops/rbac.yaml`, `scripts/tests/lib/webhook.bats`, and `scripts/plugins/argocd.sh`; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck scripts/etc/argocd/platform-ops/acg-expiry.sh`, `shellcheck scripts/plugins/argocd.sh`, `bats scripts/tests/lib/webhook.bats` with 29 tests passing, and `shellcheck bin/acg-up` showed only pre-existing SC2015/SC2029 info notes.
- **SPEC WRITTEN:** ArgoCD upgrade notifications — `docs/plans/v1.6.0-argocd-upgrade-notification.md`; SendGrid email + PagerDuty; notify.sh bundled in ConfigMap; credentials in platform-ops-notifications Secret
- **SPEC WRITTEN:** ArgoCD CVE scan pipeline — `docs/plans/v1.6.0-argocd-cve-scan-promotion.md`; bi-weekly CronJob on OCI; Trivy scan → webhook ACG → webhook infra → OCI in-cluster; idempotent; skips unreachable stages with notification
- **COMPLETE:** k3dm-webhook server — k3d-manager commit `5084ef9b` on `k3d-manager-v1.6.0`; spec: `docs/plans/v1.6.0-k3dm-webhook-server.md`; created `bin/k3dm-webhook`, `bin/k3dm-webhook-setup`, and `scripts/etc/launchd/com.k3d-manager.webhook.plist.tmpl`, plus the `webhook.3ai-talk.org` ingress in `scripts/etc/cloudflared-config.yml.tmpl`; validation used `python3 -m py_compile bin/k3dm-webhook`, `shellcheck bin/k3dm-webhook-setup`, and a localhost auth probe that returned HTTP 401 for a wrong Bearer token.
- **ASSIGNED TO CODEX:** Passwordless sudo — spec `docs/plans/v1.5.0-sudo-passwordless.md`; 7 files: `bin/install-sudoers.sh` (new), prewarm in 3 bin scripts, Makefile `sudoers` target, howto doc, BATS test; commit message: `feat(sudo): passwordless sudo rules + prewarm for macOS host operations`
- **COMPLETE:** OIDC issuer URLs now use the external Keycloak domain in `shopping-cart-infra` (`1fff014`) and `shopping-cart-payment` (`24606e6`), and the Node.js 20 → 22 upgrade landed in `shopping-cart-frontend` (`908ce07`) plus `shopping-cart-e2e-tests` (`0be83fb`); all commits were pushed to their requested branches on origin. Validation used YAML parse checks for the edited manifests/workflows.
- **COMPLETE:** OIDC issuer URLs now use the external Keycloak domain in `shopping-cart-infra` (`identity/keycloak/kustomization.yaml`, `argocd/config/argocd-cm.yaml`) and `shopping-cart-payment` (`k8s/base/configmap.yaml`); committed as `1fff014` (`fix(keycloak): update OIDC issuer URLs to external domain for ArgoCD and Keycloak config`) and `24606e6` (`fix(auth): update OIDC issuer and JWK URIs to external Keycloak domain`) and pushed to `origin/shopping-cart-infra-v0.5.0` and `origin/docs/next-improvements`.
- **COMPLETE:** `/etc/hosts` updates now go through the scoped helper `bin/update-hosts-entry.sh`; `bin/install-sudoers.sh` installs/removes `/usr/local/bin/k3d-manager-update-hosts` with a matching NOPASSWD rule, and `bin/acg-up` no longer prewarms sudo with `sudo -v`; committed as `3c290cb6` (`fix(sudo): extract /etc/hosts update to scoped helper for pipeline compatibility`) and pushed to `origin/k3d-manager-v1.5.0`. Validation used `shellcheck -S warning -x bin/update-hosts-entry.sh bin/acg-up bin/install-sudoers.sh` and `bats scripts/tests/test_install_sudoers.bats`.
- **COMPLETE:** Passwordless sudo rules + macOS host-operation prewarm implemented on `k3d-manager-v1.5.0`; committed as `26a75bcb` (`feat(sudo): passwordless sudo rules + prewarm for macOS host operations`) and pushed to `origin/k3d-manager-v1.5.0`. Validation used `shellcheck -S warning bin/install-sudoers.sh`, `shellcheck -S warning bin/acg-up bin/acg-down bin/acg-refresh`, `bats scripts/tests/test_install_sudoers.bats`, and the staged repo audit.
- **COMPLETE:** `/etc/hosts` pipeline-compatible helper extraction — `bin/acg-up` now shells out to `/usr/local/bin/k3d-manager-update-hosts`, `bin/install-sudoers.sh` installs/removes that helper, and `bin/update-hosts-entry.sh` owns the actual `/etc/hosts` rewrite; committed as `8c9095c0` (`fix(sudo): extract /etc/hosts update to scoped helper for pipeline compatibility`) and pushed to `origin/k3d-manager-v1.5.0`. Validation used `shellcheck -S warning bin/update-hosts-entry.sh bin/acg-up bin/install-sudoers.sh`, `bats scripts/tests/test_install_sudoers.bats`, and the staged repo audit.
- **COMPLETE:** `acg-down` cached-credentials guard — `bin/acg-down` now skips `sudo -v` pre-auth when credentials are already cached; commit `b8cdaad7` pushed to `origin/k3d-manager-v1.5.0`. Validation used `shellcheck -S warning bin/acg-down` and the staged repo audit.

## Status (v1.4.9 — RELEASED 2026-05-22)
- **RELEASED:** k3d-manager v1.4.9 — PR #79 merged to main (SHA `160a5edbac565ffc2a76a00b89bc7d1a3ac8735e`); enforce_admins restoration deferred (API endpoint pending); next branch: `k3d-manager-v1.5.0`; v1.4.9 retrospective committed `e1bf78fb` on k3d-manager-v1.5.0; v1.4.9 release row added to README + docs/releases.md
- **COMPLETE:** shopping-cart-e2e-tests PR #2 post-merge — fix/e2e-workflow-push-trigger merged to main (SHA `a37a9d9801d85508d5e3ca74691b3ff96f24d262`); enforce_admins restored ✓; `docs/next-improvements` branch checked out ✓; retrospective committed (`7b7e9c7`) ✓; pushed to origin ✓; memory-bank updated ✓
- **PENDING (next release cycle):** Node.js 20 → 22 upgrade across all shopping-cart CI workflows — GitHub Actions deprecated Node.js 20 runner; `node-version: '20'` must be bumped to `'22'` in all jobs in all shopping-cart repos (`frontend`, `basket`, `order`, `payment`, `product-catalog`); issue doc at `shopping-cart-frontend/docs/issues/2026-05-22-nodejs20-deprecation.md` (commit `e6cde03` on `docs/next-improvements`); also consider adding `NODE_VERSION` to workflow-level `env:` block and referencing everywhere.
- **COMPLETE:** shopping-cart-frontend PR #17 post-merge — fix/vite-keycloak-url merged to main (SHA `41fb0751`); enforce_admins restored; `docs/next-improvements` branch created; retrospective committed `31f8c4e`; README releases table + `docs/releases.md` added (`2e11026`); all pushed to origin.
- **COMPLETE:** shopping-cart-frontend PR #18 post-merge — docs/next-improvements merged to main (SHA `128659675aadd88aac7d7561e064b4c1ae670e7c`); enforce_admins restored ✓; `docs/next-improvements` branch checked out; retrospective committed `2828c18` (`docs(retro): PR #18 E2E mock format fixes retrospective`); pushed to origin.
- **COMPLETE:** Ghost State Delete Sandbox — `--force-restart` flag added to `acg_extend.js` + arg pass-through in `acg.sh` + `elif` branch in `bin/acg-up` detects invalid creds on running sandbox and calls force-restart; lib-acg commit `f5bc18d` on `fix/ghost-state-waitForSelector` + k3d-manager commit `92c438ca` on `k3d-manager-v1.4.9`; both pushed to origin.
- **COMPLETE:** Ghost State SPA render timing fix (partial) — lib-acg PR #18 (`0b40f42`) merged; timeout extended 5s→30s + aria-busy wait added; but aria-busy unreliable — follow-up fix in progress.
- **COMPLETE:** lib-acg Ghost State nav fix + acg_check_ttl regression — lib-acg PR #17 (`6a69232`) merged; subtree pull (`936fea39`) restored `acg_check_ttl` in `scripts/lib/acg/scripts/plugins/acg.sh`. enforce_admins restored on lib-acg.
- **COMPLETE:** expired sandbox auto-restart — `docs/bugs/2026-05-20-acg-up-expired-sandbox-auto-restart.md`; `bin/acg-up` calls `acg_extend_playwright` (Ghost State: Delete→Start) + re-extracts credentials when TTL ≤ 0; commit `0395cf8e`. Supersedes `5962c514` (abort-only was wrong).
- **COMPLETE:** sandbox TTL check before provision — `docs/bugs/2026-05-19-acg-up-sandbox-ttl-check-before-provision.md`; `bin/acg-up` now checks sandbox TTL in the AWS skip-extraction path and extends when below threshold; commit `a89cc2e5` pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** Cloudflare tunnel missing Keycloak — `docs/bugs/2026-05-19-acg-up-cloudflare-tunnel-missing-keycloak.md`; `scripts/etc/cloudflared-config.yml.tmpl` added and Step 10h now generates `~/.cloudflared/config.yml` when missing or missing the keycloak ingress; commit `d8f2ca47` pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** CoreDNS duplicate `hosts` plugin crash — `docs/bugs/2026-05-19-acg-up-coredns-duplicate-hosts-plugin.md`; `bin/acg-up` now patches `data.NodeHosts` instead of injecting a second `hosts {}` block into Corefile; commit `5efe45e8` pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** CloudFormation IAM race condition — `docs/bugs/2026-05-19-acg-up-cloudformation-race-after-credential-extraction.md`; `bin/acg-up` polls `aws cloudformation list-stacks` after fresh credential extraction (up to 12×15s); commit `2e46a4a2` pushed to `origin/k3d-manager-v1.4.9`.
- **COMPLETE:** Ghost State Delete Sandbox selector fix — `lib-acg/playwright/acg_extend.js` now waits for sandbox card buttons after Ghost State re-navigation and shortens `deleteBtn.isVisible` to 10s; commit `0248736` pushed to `origin/fix/ghost-state-waitForSelector`; live `make credential-test` failed with `InvalidClientTokenId` and the failure was recorded in `docs/issues/2026-05-20-lib-acg-credential-test-invalid-client-token.md`.
- **COMPLETE:** e2e-tests CI push/PR triggers removed — `shopping-cart-e2e-tests/.github/workflows/e2e-tests.yml` now keeps only `workflow_dispatch` and the disabled schedule comment; `push` and `pull_request` blocks were replaced with the spec comment because `api-tests` and `flow-tests` require live services on `localhost:8083`, `localhost:8000`, and `localhost:8080`; commit `99499d3` pushed to `origin/fix/e2e-workflow-push-trigger`.

## Status (v1.4.8 — PR #77 merged 2026-05-19 + post-merge complete)
- **RELEASED:** k3d-manager v1.4.8 — PR #77 merged to main (SHA `8f08bd35`); enforce_admins restored; next branch: `k3d-manager-v1.4.9`; v1.4.8 retrospective committed `9946fa66` on k3d-manager-v1.4.9
- **MERGED:** k3d-manager PR #77 — vault temp file leaks, CDP hang, Keycloak frontendUrl, Cloudflare tunnel; merge SHA `8f08bd35`; v1.4.8 retrospective committed on k3d-manager-v1.4.9
- **POST-MERGE:** k3d-manager PR #77 housekeeping — enforce_admins restored ✓; next branch created ✓; retrospective committed ✓; memory-bank updated (in progress)
- **COMPLETE:** Keycloak public URL follow-up landed across all four repos: `k3d-manager` commit `ea9977b4` sets Keycloak `frontendUrl` to `https://keycloak.3ai-talk.org` after realm import; `shopping-cart-basket` commit `cb5a294` sets `OAUTH2_ISSUER_URI` to the Cloudflare public issuer; `shopping-cart-order` commit `fb578ca` sets both `OAUTH2_ISSUER_URI` and `OAUTH2_JWK_SET_URI` to the Cloudflare public issuer; `shopping-cart-frontend` commit `cb348c4` sets both `VITE_KEYCLOAK_URL` occurrences to `https://keycloak.3ai-talk.org`; explicit feature refs were pushed to `origin/fix/keycloak-public-url` in each shopping-cart repo, and the upstream-push quirk was documented in `docs/issues/2026-05-19-git-push-upstream-default-targeted-main.md`.
- **RELEASED:** k3d-manager v1.4.7 — tag `v1.4.7` pushed, GitHub release created, enforce_admins restored, next branch: `k3d-manager-v1.4.8`
- **MERGED:** k3d-manager PR #76 — Keycloak cross-cluster tunnel + CoreDNS on k3s-aws; merge SHA `0278e8d7`; v1.4.7 retrospective committed on k3d-manager-v1.4.8
- **POST-MERGE:** lib-foundation PR #28 — fix: Copilot CLI auth + rigor scanner improvements; merge SHA `fee313ed`; branch `feat/v0.3.20` cut, retrospective committed `a64e2ad`; enforce_admins restored (pending)
- **POST-MERGE:** lib-acg PR #13 — docs: PR #12 retrospective and Phase 3 completion; merge SHA `1bdc663`; branch `docs/next-improvements` created, retrospective committed `4d93147`; enforce_admins restored (pending)
- **RELEASED:** shopping-cart-infra v0.5.0 — tag `v0.5.0` pushed, GitHub release created, enforce_admins restored, next branch: `shopping-cart-infra-v0.5.5`
- **MERGED:** k3d-manager PR #75 — Keycloak cross-cluster reachability fix (SSH tunnel + iptables DNAT + CoreDNS patch); merge SHA `8cb5709b`; v1.4.6 retrospective committed on v1.4.7
- **MERGED:** shopping-cart-order PR #30 — Keycloak JWT issuer URI fix; merge SHA `16640fd5`; `docs/next-improvements` branch created
- **MERGED:** shopping-cart-basket PR #11 — Keycloak JWT issuer URI fix; merge SHA `c718c1cd`; `docs/next-improvements` branch created
- **MERGED:** shopping-cart-infra PR #59 — products table DDL removed (SQLAlchemy now owns schema); merge SHA `475794c0`; `docs/next-improvements` branch created
- **MERGED:** shopping-cart-frontend PR #16 — API response field mapping + customerId from Keycloak; merge SHA `674116b2`; `docs/next-improvements` branch created
- **COMPLETE:** products ConfigMap schema mismatch fix — `shopping-cart-infra/data-layer/postgresql/products/configmap.yaml` now matches `init-db.sql` by keeping only categories schema + seed data; commit `eb05a3f`; branch `fix/products-db-schema` pushed to `origin`.
- **NO-OP:** ArgoCD SSO `invalid_scope: groups` already absent from `shopping-cart-infra/argocd/config/argocd-cm.yaml` on `origin/main`; issue doc recorded the stale spec state at `docs/issues/2026-05-18-argocd-groups-scope-already-fixed.md`.
- **acg-up Keycloak LDAP user passwords seeding completed** — `bin/acg-up` now seeds `keycloak/users/admin`, `keycloak/users/developer`, and `keycloak/users/operator` in Vault and applies the passwords to LDAP on every `make up`; committed as `34c6f8a9` (`fix(acg-up): seed Keycloak LDAP user passwords in Vault on every make up`) and pushed to `origin/k3d-manager-v1.4.6`; validated with `shellcheck -S warning bin/acg-up` and `bats scripts/tests/bin/acg_up.bats`; PR URL: not created (per repository instruction).
- **shopping-cart PRs #20 + #29 post-merge completed** — `shopping-cart-product-catalog` PR #20 (SHA `12dfe79`) + `shopping-cart-order` PR #29 (SHA `8a2739d`) merged to main; enforce_admins restored on both; `docs/next-improvements` branch created on both repos; k3d-manager memory-bank updated
- **acg-up credential extraction skip completed** — `bin/acg-up` now checks `_acg_check_credentials` before calling `acg_get_credentials` for `k3s-aws`, skipping Playwright extraction when the existing AWS credentials are valid and falling back to extraction otherwise; committed as `7d2be2e6` and pushed to `origin/k3d-manager-v1.4.6`; PR URL: not created (per repository instruction).
- **frontend 504 fix for shared namespace services completed** — `shopping-cart-product-catalog/k8s/base/service.yaml` now sets the ClusterIP port to `8082`, and `shopping-cart-order/k8s/base/service.yaml` now sets the ClusterIP port to `8081`, matching the frontend nginx upstream config; committed as `345d89a` and `0edba5b` on `fix/argocd-shared-namespace` and pushed to origin in both repos; PR URL: not created (per repository instruction).
- **k3s-aws CloudFormation stack status-aware provisioning completed** — `scripts/lib/providers/k3s-aws.sh` now checks the existing CloudFormation stack status before provisioning, reuses healthy stacks without `--recreate`, recreates only broken stacks, and creates missing/unknown stacks without forcing recreation; committed as `25f867f0` and pushed to `origin/k3d-manager-v1.4.6`; PR URL: not created (per repository instruction).
- **127.0.0.2 loopback alias launchd agent completed** — `bin/acg-up` now ensures `127.0.0.2` exists on `lo0` with a best-effort `ifconfig lo0 alias 127.0.0.2` call, then installs a persistent `com.k3d-manager.loopback-alias` launchd daemon at `/Library/LaunchDaemons` so the alias survives reboots before the frontend-browser-http wrapper starts; committed as `8066f35e` and pushed to `origin/k3d-manager-v1.4.6`; PR URL: not created (per repository instruction).
- **frontend-browser-http wrapper kubectl path fix completed** — `bin/acg-up` now captures `kubectl` with `command -v` before writing the Step 10g frontend-browser-http wrapper heredoc, then expands that absolute path inside the launchd script so the root daemon does not depend on `PATH`; committed as `4b0509ff` and pushed to `origin/k3d-manager-v1.4.6`; PR URL: not created (per repository instruction).
- **Cloudflare Tunnel launchd agents completed** — `bin/acg-up` now installs macOS launchd agents for `cloudflared tunnel --url http://localhost:8080` and `cloudflared tunnel --url http://127.0.0.2:80`, logs the random `trycloudflare.com` URLs to `~/.local/share/k3d-manager/tunnel-urls.txt`, and prints the public ArgoCD/frontend URLs at the end of `acg-up`; committed as `e45a6b22` and pushed to `origin/k3d-manager-v1.4.6`; PR URL: not created (per repository instruction).
- **frontend.shopping-cart.local direct port-forward fix completed** — `bin/acg-up` now binds `frontend.shopping-cart.local` to `127.0.0.2` and starts a launchd-managed `kubectl port-forward --address=127.0.0.2 svc/frontend 80:80` listener instead of the blocked ServiceEntry path; committed as `84cb5a21` and pushed to `origin/k3d-manager-v1.4.6`; PR URL: not created (per repository instruction).
- **Cloudflare Tunnel task completed** — the pending tunnel spec is now implemented in `bin/acg-up` with launchd agents and `trycloudflare.com` URL logging; commit `e45a6b22` supersedes the pending note.
- **frontend.shopping-cart.local NodePort/ServiceEntry/VirtualService wiring completed** — `bin/acg-up` now creates the `frontend-nodeport` NodePort 30080 service on ubuntu-k3s, derives the ubuntu-k3s IP from kubeconfig, applies the `frontend-ubuntu-k3s` ServiceEntry and `frontend` VirtualService in k3d, and updates the Step 13 URL text; committed as `14e7b8b0` and pushed to `origin/k3d-manager-v1.4.6`; PR URL: not created (per repository instruction).
- **Keycloak empty-admin-secret hard-fail fix completed** — `scripts/plugins/keycloak.sh` now hard-fails if the admin ExternalSecret never becomes Ready or the decoded secret is empty, and `bin/acg-up` now fails fast when Vault returns an empty `keycloak/admin` password field; committed as `1ab96ed1` and pushed to `origin/k3d-manager-v1.4.6`; PR URL: not created (per repository instruction).
- **lib-acg extend dialog AppleScript native click fix completed** — `playwright/acg_credentials.js` now computes screen coordinates in `page.evaluate()` and uses `osascript` via `child_process.execSync` to native-click the close button; committed as `7874e9d` and pushed to `origin/fix/acg-credentials-extend-dialog`; PR URL: not created (per repository instruction).
- **lib-acg extend dialog bringToFront/Enter attempt superseded** — `1e7f2ff` used `page.bringToFront()` plus `Enter`, but that dismissed iTerm2 hotkey focus and reset the page before the key event arrived; superseded by the AppleScript native-click fix.
- **lib-acg extend dialog selector fix completed** — `playwright/acg_credentials.js` now uses `getByRole('button', { name: 'Cancel' })` and logs click errors instead of swallowing them; committed as `787cab8` and pushed to `origin/fix/acg-credentials-extend-dialog`; PR URL: not created (per repository instruction).
- **lib-acg extend dialog locator-click attempt superseded** — the earlier `locator('button', { hasText: 'Cancel' })` fix was superseded by the new `getByRole('button', { name: 'Cancel' })` selector and error logging; prior commit `98bbf59` is no longer the branch tip.
- **k3d-manager validation follow-up docs committed** — recorded the `.git/index.lock` permission note and the unrelated-suite `./scripts/k3d-manager test all` failures in `docs/issues/2026-05-16-git-commit-index-lock-permission-error.md` and `docs/issues/2026-05-17-repo-test-all-existing-failures.md`; committed as `65582d0c` (`docs: record validation follow-up notes`); PR URL: not created (per repository instruction).
- **lib-acg extend dialog and credential write fixes completed** — the prior escape-key fallback spec is superseded by the committed locator-click and credential-write fixes on `fix/acg-credentials-extend-dialog`; remote SHAs are `98bbf59` and `217609a`; PR URL: not created (per repository instruction).
- **k3d-manager v1.4.6 addLocatorHandler post-handler wait loop (acg_extend.js + acg_credentials.js) — completed** — spec: `docs/bugs/2026-05-16-acg-extend-locator-handler-no-wait-after.md` (updated to cover both files); implemented `{ noWaitAfter: true }` in `acg_extend.js` + `{ times: 1, noWaitAfter: true }` in `acg_credentials.js`; committed as `5a638912` (`fix(acg): add noWaitAfter: true to both addLocatorHandler calls`) and pushed to `origin/k3d-manager-v1.4.6`; `node --check scripts/lib/acg/playwright/acg_extend.js`, `node --check scripts/lib/acg/playwright/acg_credentials.js`, `_agent_audit`, and `_agent_lint` passed; `./scripts/k3d-manager test all` failed in unrelated existing suites (`scripts/tests/plugins/eso.bats`, `scripts/tests/plugins/gcp.bats`, `scripts/tests/plugins/keycloak.bats`), recorded in `docs/issues/2026-05-17-repo-test-all-existing-failures.md`
- **k3d-manager v1.4.6 acg "Session extended" toast × — mouse.click bounding-rect + addLocatorHandler — STILL FAILING (new issue)** — spec: `docs/bugs/2026-05-16-session-extended-mouse-click-and-locator-handler.md`; committed as `718562b9`; × click works but addLocatorHandler fires in infinite loop; superseded by noWaitAfter spec
- **k3d-manager v1.4.6 acg "Session extended" toast × — page.evaluate DOM traversal — STILL FAILING** — spec: `docs/bugs/2026-05-16-session-extended-evaluate-close-button.md`; element.click() in evaluate does not trigger React synthetic event handler; committed as `ebd3a985`; superseded by mouse-click-bounding-rect spec
- **k3d-manager v1.4.6 acg "Session extended" toast never dismissed — STILL FAILING** — spec: `docs/bugs/2026-05-16-session-extended-text-locator-page-level-close.md`; `[role="alert"]` fallback hangs 30s (no role on toast container); committed as `70ce5ca9`; superseded by evaluate spec
- **k3d-manager v1.4.6 acg-credentials Start Sandbox outside viewport — completed** — spec: `docs/bugs/2026-05-16-acg-credentials-start-sandbox-outside-viewport.md`; added 3× retry loop to `_clickStartSandbox` scroll+click; committed as `7b828419` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg "Session extended" × close fallback — completed** — spec: `docs/bugs/2026-05-16-session-extended-close-fallback-button-last.md`; added count-check + `button.last()` fallback at all 7 dismiss sites in `acg_credentials.js` + `acg_extend.js`; committed as `e464bafa` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg "Session extended" × button[aria-label="Close"] — STILL FAILING** — spec: `docs/bugs/2026-05-16-session-extended-button-aria-label.md`; `button[aria-label="Close"]` silently finds 0 elements inside container (.catch swallows), card stays visible; committed as `fd9b2fe7` but still fails
- **k3d-manager v1.4.6 acg-up return outside function — completed** — spec: `docs/bugs/2026-05-16-acg-up-return-outside-function.md`; changed `bin/acg-up` `|| return 1` to `|| exit 1`; committed as `fd9b2fe7`
- **k3d-manager v1.4.6 acg-credentials _clickStartSandbox blocked by Session extended — completed** — spec: `docs/bugs/2026-05-16-acg-credentials-click-start-sandbox-blocked-by-session-extended.md`; `_clickStartSandbox` now dismisses the "Your sandbox has been extended." card before scrolling/clicking Start Sandbox; committed as `644ec725` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials isEnabled() gate skips Start Sandbox — completed** — spec: `docs/bugs/2026-05-16-acg-credentials-start-sandbox-isenabled-gate.md`; `scripts/lib/acg/playwright/acg_credentials.js` Pattern 1 now always clicks Start Sandbox when visible; committed as `ee8be3ca` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-up credential shortcut skips extraction — completed** — spec: `docs/bugs/2026-05-16-acg-up-credential-shortcut-skips-extraction.md`; `bin/acg-up` now always calls `acg_get_credentials ${sandbox_url:+"$sandbox_url"} || return 1` for `k3s-aws`; committed as `b1da7ef4` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials Escape does not dismiss Extend Your Session dialog — completed** — spec: `docs/bugs/2026-05-16-acg-credentials-escape-does-not-dismiss-extend-session-dialog.md`; `scripts/lib/acg/playwright/acg_credentials.js` line 392 now clicks `_extendSessionPrompt.locator('button[aria-label="Close"]').first().click({ force: true })`; committed as `c66e8bc5` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg Session extended modal wrong-button completed** — spec: `docs/bugs/2026-05-16-acg-session-extended-modal-wrong-button.md`; 6 single-line locator changes in acg_extend.js (3) + acg_credentials.js (3); committed as `8eb6fd02` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 k3s-aws CloudFormation rollback-state completed** — spec: `docs/bugs/2026-05-16-k3s-aws-cloudformation-rollback-state.md`; `scripts/lib/providers/k3s-aws.sh` now calls `acg_provision --confirm --recreate`; committed as `bc4485d8` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg Session extended modal escape-unreliable completed** — spec: `docs/bugs/2026-05-16-acg-session-extended-modal-escape-unreliable.md`; `scripts/lib/acg/playwright/acg_extend.js` and `scripts/lib/acg/playwright/acg_credentials.js` now click the dialog's scoped button instead of Escape; implementation committed as `f39adc25`; validation note committed as `43dc422a`; both pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 agent rigor helper validation note** — direct `_agent_checkpoint` / `_agent_lint` / `_agent_audit` invocation in this shell emitted `.git/index.lock` permission errors and missing `_err` helper output; recorded in `docs/issues/2026-05-16-git-commit-index-lock-permission-error.md`
- **k3d-manager v1.4.6 acg-up step-number grep drift — completed** — updated `scripts/tests/bin/acg_up.bats` to expect `Step 10e/14 — Installing Istio ingress HTTP listener`, matching current `bin/acg-up`
- **k3d-manager v1.4.6 acg-credentials credential-wait-escape-closes-panel completed** — spec: `docs/bugs/2026-05-16-acg-credentials-credential-wait-escape-closes-panel.md`; `scripts/lib/acg/playwright/acg_credentials.js` now removes fallback Escape from the handler callback and `_waitForCredentials` inline dismiss, only presses Escape when "Session extended" is confirmed visible, waits for each dialog to hide, and was committed as `b7f0d0a5` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials handler-times-1-click-helper completed** — `scripts/lib/acg/playwright/acg_credentials.js` now adds `{ times: 1 }` to the "Extend Your Session" handler, waits 1000ms to settle after dismiss, adds `_clickStartSandbox(page, buttonLocator)` with hidden-prompt wait + scroll + force-click, and routes both Start Sandbox call sites through the helper; committed as `982d551e` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials handler-times-1-click-helper ASSIGNED** — spec: `docs/bugs/2026-05-16-acg-credentials-handler-times-1-click-helper.md`; adds `{ times: 1 }` + 1000ms settle to handler; adds `_clickStartSandbox` helper with waitFor(hidden)+scrollIntoView+force-click; replaces both Start Sandbox `.click()` calls
- **k3d-manager v1.4.6 acg-credentials addlocatorhandler-extend-session-button STILL FAILING** — spec: `docs/bugs/2026-05-16-acg-credentials-addlocatorhandler-extend-session-button.md`; committed as `6154b0df`; handler fires 3× due to SPA re-render flicker on `data-open`; superseded by handler-times-1-click-helper spec
- **k3d-manager v1.4.6 acg-credentials remove-addlocatorhandler SUPERSEDED** — `02d792a4`; superseded — live dialog screenshot confirmed button text is "Extend Session"; re-adding handler with exact label
- **k3d-manager v1.4.6 acg-credentials extend-session-via-filled-button SUPERSEDED** — `452fee93`; pando-button--usage_filled also doesn't extend session; Extend button selector unknown after 5+ attempts
- **k3d-manager v1.4.6 acg-credentials start-sandbox-detached SUPERSEDED** — `7d2faaba`; still failing — Cancel in handler causes dialog to loop; button hidden behind reappearing modal; force:true doesn't bypass true visibility block
- **k3d-manager v1.4.6 acg-credentials handler-nonblocking-250ms completed** — `e2d4bb1b`; handler now ~350ms/cycle; still failing post-dismiss due to React re-render detaching button — new bug filed
- **k3d-manager v1.4.6 acg-credentials handler-dismiss-waitfor SUPERSEDED** — `73660e6a`; waitFor still burns entire 30s in one cycle (redundant with Playwright built-in); replaced by nonblocking-250ms spec
- **k3d-manager v1.4.6 acg-credentials extend-not-cancel completed** — `scripts/lib/acg/playwright/acg_credentials.js` now clicks `Extend` first in the `addLocatorHandler` callback, falls back to `Cancel`/Escape, and keeps the 500ms Escape plus 1s settle wait; committed as `a07c3c10` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials handler animation settle completed** — `scripts/lib/acg/playwright/acg_credentials.js` now waits 1s at the end of the `addLocatorHandler` callback so the modal animation and React re-render settle before Playwright resumes the intercepted click; committed as `0fbdba3079b8c1e183e8cb1b72fdbf1429a3f3ff` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials addLocatorHandler animation wait loop completed** — `scripts/lib/acg/playwright/acg_credentials.js` now scopes `page.addLocatorHandler()` to `[data-open="true"]` so the post-handler wait exits immediately when Cancel sets `data-open="false"`; committed as `1026389cdbb4353ec4a4dba6b402f0ec5e9bcb88` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials Extend Modal addLocatorHandler completed** — `scripts/lib/acg/playwright/acg_credentials.js` now registers `page.addLocatorHandler()` immediately after page establishment to auto-dismiss the modal at any point; committed as `4cbe3b7e26e41eee1a2e05897a75c1d030557ef8` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 frontend.shopping-cart.local automation completed** — `bin/acg-up` now adds `frontend.shopping-cart.local` to `_HOSTS_LIST`, points Step 10e at `svc/istio-ingressgateway` in `istio-system`, removes the localhost:3000 frontend port-forward, and prints the frontend URL in the final summary; committed as `e701139a17eb4acb19bb84976db5e4a14e795a8f` and pushed to `origin/k3d-manager-v1.4.6`
- Current branch: `k3d-manager-v1.4.6` (created from k3d-manager PR #74 merge SHA `8f93df25`)
- **k3d-manager v1.4.6 acg-credentials Extend Your Session modal fix completed** — `scripts/lib/acg/playwright/acg_credentials.js` now dismisses the lingering "Extend Your Session" prompt before the Start/Open flow; committed as `69204c91e5e060df6e2ca230c1180d6055f1d91d` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials GCP reads AWS inputs fix completed** — `scripts/lib/acg/playwright/acg_credentials.js` now waits for GCP JSON content and extracts by content instead of position; committed as `1ba5c5ac66d5493cdfa8ca661c0372ca518c6882` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager PR #74 merged** — k3d-manager-v1.4.5 → main (SHA: `8f93df25`); ACG extend navigation fix + frontend SSO wiring; v1.4.6 branch created with retrospective
- **k3d-manager v1.4.6 acg-credentials "Extend Your Session" modal fix ASSIGNED** — spec: `docs/plans/v1.4.6-bugfix-acg-credentials-extend-session-modal.md`; apply first
- **k3d-manager v1.4.6 acg-credentials GCP reads AWS inputs fix ASSIGNED** — spec: `docs/plans/v1.4.6-bugfix-acg-credentials-gcp-reads-aws-inputs.md`; apply after extend-session-modal fix
- **k3d-manager v1.4.6 acg-credentials Session extended modal fix completed** — `scripts/lib/acg/playwright/acg_credentials.js` now dismisses the lingering "Session extended" modal before the Start/Open flow; committed as `3444c171d4797e4cc305b4fc9c1bc707b6a9fb5c` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-credentials wrong sandbox provider fix completed** — `scripts/lib/acg/playwright/acg_credentials.js` now selects the provider-specific "Open Sandbox" card by walking ancestors before clicking; committed as `5dfbdf44c2f3dce49e53f1e3f245b5c8e3a470fd` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 acg-extend Start Sandbox race fix completed** — `scripts/lib/acg/playwright/acg_extend.js` now exits 0 when the fallback button text is `Start Sandbox` instead of waiting for an extend button that never appears; committed as `bae89aafae42aeea7b7d71cd65eed4c24ca84962` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 auto-trust Vault CA fix completed** — `bin/acg-up` now trusts the Vault CA cert in the macOS System Keychain via `_run_command --interactive-sudo --soft` after `_argocd_issue_browser_tls_material`, and `scripts/tests/bin/acg_up.bats` asserts the `security add-trusted-cert -d -r trustRoot` call; committed as `42d9941306658c259f367916bc68ea69573a3085` and pushed to `origin/k3d-manager-v1.4.6`
- **k3d-manager v1.4.6 follow-up completed** — `scripts/lib/acg/playwright/acg_extend.js` commit `34c41ced492f37525f170feed6664156b5647634`; `bin/acg-up` final fix commit `0ea4cc9f89649a3a5ca7d8a2da4df4f6b39fb4b9` (interactive sudo + 120s listener timeout); both pushed to `origin/k3d-manager-v1.4.6`
- **shopping-cart-infra PR #58 merged** — shopping-cart-infra-v0.5.3 → main (SHA: `67f8d228`); ArgoCD SSO OIDC callback fix (`url: http://` → `url: https://`); v0.5.4 next branch created; retrospective added and pushed
- **shopping-cart-frontend PR #15 merged** — shopping-cart-frontend-v0.5.1 → main (SHA: `19e47118`); Keycloak SSO wiring (CI build-args + nginx CSP); v0.5.1 branch created with retrospective
- **shopping-cart-infra PR #57 merged** — shopping-cart-infra-v0.5.2 → main (SHA: `adbaec8`); networking + mTLS fixes; v0.5.3 branch created with retrospective
- **shopping-cart-infra PR #56 merged** — shopping-cart-infra-v0.5.1 → main (SHA: `79c42b7`); python3 → kcadm.sh bugfix + retrospective on v0.5.2
- **shopping-cart-infra PR #55 merged** — bug/keycloak-ldap-mappers-missing → main; v0.5.1 next branch created with retrospective
- **rigor-cli PR #10 merged** — rigor-cli-v0.1.6 → main; v0.1.6 tagged and released; v0.1.7 next branch created with retrospective
- Branch protection (`enforce_admins=true`) restored on all repos after merge
- v1.4.4, v1.4.3, and v1.4.2 remain shipped; branch protection was restored after each merge.

## Completed (v1.4.6 partial — lib-acg PR #12)
- [x] **lib-acg PR #12 merged** — feat/acg-multi-provider → main; exit 0 on stale "Session extended" toast at startup; SHA `8fe35fa1`
- [x] enforce_admins + required_approving_review_count=1 restored on lib-acg
- [x] feat/acg-gcp-credentials branch created from merge SHA
- [x] Retrospective: `docs/retro/2026-05-17-pr12-retrospective.md` (on lib-acg feat/acg-gcp-credentials, commit `0b9c224`)
- [x] k3d-manager memory-bank updated (activeContext.md + progress.md)

## Completed (v1.4.6 partial — lib-acg PR #11)
- [x] **lib-acg PR #11 merged** — fix/acg-credentials-extend-dialog → main; AWS credential extraction + dialog handling; SHA `feeb8e80`
- [x] enforce_admins restored on lib-acg (required_approving_review_count=1)
- [x] feat/acg-multi-provider branch created from merge SHA
- [x] Retrospective: `docs/retro/2026-05-17-pr11-retrospective.md` (on lib-acg feat/acg-multi-provider, commit `d2e3e48`)

## Completed (v1.4.5 partial — shopping-cart-frontend #15)
- [x] **shopping-cart-frontend PR #15 merged** — Keycloak SSO wiring (CI build-args + nginx CSP); SHA `19e47118`
- [x] enforce_admins restored on shopping-cart-frontend
- [x] v0.5.1 branch created from merge SHA
- [x] Retrospective: `docs/retro/2026-05-15-v0.5.1-retrospective.md` (on shopping-cart-frontend-v0.5.1, commit `60e660f`)


## Completed (v1.4.5 — k3d-manager PR #74)
- [x] **k3d-manager PR #74 merged** — merge SHA `8f93df25`; branch `k3d-manager-v1.4.5`; next branch `k3d-manager-v1.4.6` cut
- [x] enforce_admins restored on k3d-manager main
- [x] Retrospective: `docs/retro/2026-05-15-v1.4.5-retrospective.md` (on k3d-manager-v1.4.6, commit `b6b9f15e`)

## Completed (v1.4.5 partial — shopping-cart-infra #57)
- [x] **shopping-cart-infra PR #57 merged** — networking + mTLS fixes (appproject + keycloak-destinationrule); SHA `adbaec8`
- [x] enforce_admins restored on shopping-cart-infra
- [x] v0.5.3 branch created from merge SHA
- [x] Retrospective: `docs/retro/2026-05-15-v0.5.2-retrospective.md` (on shopping-cart-infra-v0.5.3)
- [x] Branch cleanup — deleted v0.4.0, v0.5.0, v0.5.2; retained v0.5.3

## Completed (v1.4.4)
- [x] **Keycloak ExternalSecret files missing** — spec: `docs/bugs/2026-05-08-keycloak-externalsecret-files-missing.md`
- [x] **Identity SSO fixes** — spec: `docs/plans/v1.4.4-identity-sso-fixes.md`
- [x] PR #73 merged, v1.4.4 tagged + released
- [x] enforce_admins restored on both repos
- [x] Retrospective: `docs/retro/2026-05-08-v1.4.4-retrospective.md`
- [x] Next branch created: `k3d-manager-v1.4.5`

## Completed (v1.4.3)
- [x] **Keycloak Vault KV seeding** — bin/acg-up provisions keycloak/admin and keycloak/clients KV paths
- [x] **shopping-cart-identity ArgoCD app** — services/shopping-cart-identity kustomization wires Keycloak stack from shopping-cart-infra
- [x] **shopping-cart-infra PR #36** — Keycloak frontend OIDC client + ESO migration merged
- [x] `enforce_admins` restored on both repos (shopping-cart-infra + k3d-manager)
- [x] Retrospective: `docs/retro/2026-05-08-v1.4.3-retrospective.md`
- [x] Next branch created: `k3d-manager-v1.4.4`

## Completed (v1.4.2)
- [x] `_ai_agent_review` dispatch layer in lib-foundation
- [x] `K3DM_ENABLE_AI` gate removed from lib-foundation backend
- [x] `ARGOCD_SERVER_WAIT_TIMEOUT` configurable (default 600s)
- [x] `bin/acg-up` bootstrap refresh on existing Hub
- [x] `bin/acg-up` `--confirm` flag
- [x] lib-acg PR #10 subtree pull (`launchctl bootout`, dead else-block)
- [x] Copilot plugin BATS suite (`scripts/tests/plugins/copilot.bats`)
- [x] All 8 Copilot PR #71 review threads resolved
- [x] `enforce_admins` restored on `main`
- [x] v1.4.2 tag + GitHub release created
- [x] Retrospective: `docs/retro/2026-05-07-v1.4.2-retrospective.md`

## Next Steps (v1.4.5)
- [x] **multi-repo SSO wiring follow-up** — COMPLETE. `k3d-manager` ACG sandbox navigation fix: `3bb7aa817983948cd426bc6a96525a811b2bd7ff`; `shopping-cart-infra` SSO ingress/workflow update: `8c581840b904f21459fa225c80ddfe54f93ed9aa`; `shopping-cart-frontend` CI/CSP update: `6120783b8af124abebca7f242f5b905a29734836`; frontend issue-doc commit: `64c943e5b39ec1468c1de8f0a7e6768a1d4cb6b1`; frontend `main` reverted to `2872a4cd9b120b55673b0c2633ab8e6270cb6b8c` after an accidental remote update. Issue doc: `docs/issues/2026-05-15-frontend-main-push-reverted.md`.
- [x] **networking ArgoCD project + Keycloak mTLS fix** — COMPLETE (`6c70fee99baa2c2f29330113e4f1c96b2b94cf75`). `argocd/applications/networking.yaml` now uses `project: platform`; added `networking/istio/keycloak-destinationrule.yaml` with `tls.mode: DISABLE`. Spec: `docs/bugs/2026-05-15-networking-app-project-and-mtls.md`.
- [x] **ArgoCD SSO via Keycloak** — COMPLETE. shopping-cart-infra PR #38 (`d314c34`, feat: ArgoCD SSO) + PR #39 (`afaf109`, fix: ldap duplicate) merged to main. enforce_admins restored. spec: `docs/plans/v1.4.5-argocd-sso-keycloak.md`; retro: `docs/retro/2026-05-09-pr38-pr39-argocd-sso-kustomize-fix.md` (on `docs/next-improvements`)
- [x] **pyjenkinsapi rigor-cli v0.1.6 subtree pull** — COMPLETE (subtree: `7d8a894b`, ci-fix: `0c479c99`); branch `docs/next-improvements`; BATS re-enabled
- [x] **acg-up /etc/hosts --soft** — COMPLETE (`3c096f6`). `_run_command --prefer-sudo` missing `--soft` causes `exit 1` instead of warning when EPERM. spec: `docs/bugs/v1.4.5-bugfix-etc-hosts-soft.md`.
- [x] **acg-up Step 10e bugs** — COMPLETE (`d6a31c5`). Fixed CoreDNS awk pattern + /etc/hosts sudo hard-fail. spec: `docs/plans/v1.4.5-bugfix-gemini.md`
- [x] **eso-ldap-directory Vault policy missing keycloak/* paths** — COMPLETE (`48938dea`). Extended `LDAP_VAULT_POLICY_PREFIX` default to `ldap,keycloak`. spec: `docs/bugs/v1.4.5-bugfix-eso-ldap-policy-missing-keycloak.md`
- [x] **shopping-cart-identity AppProject missing** — COMPLETE (`771ba5cf`). `project: shopping-cart` → `project: platform`; removed dead kustomization. spec: `docs/bugs/v1.4.5-bugfix-identity-appproject-missing.md`
- [x] **identity ESO bootstrap Bug 1** — COMPLETE (`f969b299`). `_eso_apply_vault_cluster_store` added to `eso.sh`; `ldap.sh` source guard + `deploy_ldap()` call added. Verified by Claude.
- [x] **identity ESO bootstrap Bugs 2+3** — spec: `docs/bugs/v1.4.5-bugfix-identity-externalsecret-bootstrap.md`; shopping-cart-infra PR #40 merged (`dfe00df1`); enforce_admins restored
- [x] **identity ESO bootstrap Bugs 4+5** — spec: `docs/bugs/2026-05-08-pr41-keycloak-postgresql-driver-ldap-ldif-chown-fixes.md`; shopping-cart-infra PR #41 merged (`180f5f89`); Bug 4: Keycloak `--db=postgres` in args, KC_DB removed from ConfigMap; Bug 5: LDAP LDIF chown fix via initContainer + emptyDir; enforce_admins restored
- [x] **LDAP CrashLoopBackOff Bug 6** — PR #42 merged (`11aa8d7d`); enforce_admins restored. Spec: `docs/bugs/2026-05-09-ldap-emptydir-mountpoint-rm-fails.md`
- [x] **LDAP CrashLoopBackOff Bug 7** — PR #43 merged (`dcd18af7`). enforce_admins restored. Fix: `enableServiceLinks: false` in pod spec. Retro: `docs/retro/2026-05-09-pr43-ldap-service-links-retrospective.md`
- [x] **deploy_eso unpinned chart version** — spec: `docs/bugs/v1.4.5-bugfix-deploy-eso-no-version-pin.md`; pinned to `1.0.0` via `ESO_HELM_CHART_VERSION` (SHA: `50851f0e`)
- [x] **Bug 8 — CoreDNS keycloak.shopping-cart.local → Keycloak ClusterIP** — `34e0101f`. Corefile patched with `hosts` block; NodeHosts/IngressGateway approach removed.
- [x] **Bug 9 — acg-up frontend port-forward launchd agent** — `fb56a443`. Step 13 launchd plist added (`localhost:3000 → ubuntu-k3s/shopping-cart-apps/frontend:80`).
- [x] **Bug 10 — acg-up ArgoCD localhost:8080 readiness gate** — `bin/acg-up` now waits for the Argo CD launchd listener to answer `localhost:8080/healthz` and fails fast with the log tail when it never becomes reachable.
- [x] **Bug 11 — acg-down keep-hub preservation trust gap** — `bin/acg-down` now logs the resolved Hub cluster and the `Makefile` wrapper exposes `KEEP_LOCAL` directly, making `--keep-hub` unambiguous to operators.
- [x] **Bug 12 — acg-up missing Argo CD plugin source** — `bin/acg-up` now sources `scripts/plugins/argocd.sh` before Step 4b calls `_argocd_wait_for_local_port_forward` (SHA: `6052786a`).
- [x] **Bug 13 — acg-up PLUGINS_DIR unbound before plugin sourcing** — `bin/acg-up` now initializes `PLUGINS_DIR="${SCRIPT_DIR}/plugins"` before sourcing `scripts/plugins/argocd.sh` (SHA: `5d4b9b1e`).
- [x] **Bug 14 — Keycloak `argocd` client redirect reconciliation** — `bin/acg-up` now reconciles the existing `argocd` client redirect URIs even when the realm already exists, so rebuilds do not preserve stale OIDC callbacks. Spec: `docs/bugs/2026-05-11-keycloak-argocd-client-redirect-reconciliation.md`.
- [x] **Bug 15 — Argo CD port-forward flaps after sandbox rebuild** — `bin/acg-up` now renders a self-healing launchd wrapper that restarts the Argo CD port-forward whenever `localhost:8080/healthz` stops responding. Spec: `docs/issues/2026-05-11-acg-up-argocd-port-forward-flapping.md`; commit `18f8d884`.
- [ ] **Follow-up — `scripts/plugins/argocd.sh:_argocd_write_port_forward_wrapper` Agent Audit mismatch** — the helper is temporarily allowlisted because the audit counter reports 11 `if` blocks even though the rendered function body only contains one. Spec: `docs/issues/2026-05-11-argocd-port-forward-wrapper-if-count-mismatch.md`.
- [ ] **Follow-up — `deploy_keycloak` if-count refactor** — temporary allowlist entry added for `scripts/plugins/keycloak.sh:deploy_keycloak`; separate refactor needed. Spec: `docs/issues/2026-05-11-keycloak-deploy_keycloak-if-count.md`.
- [x] **Keycloak admin token mismatch after rebuild** — `bin/acg-up` now preserves existing Vault identity secrets on rebuild, the live Vault `secret/keycloak/admin` secret was restored to the historical working password version, and admin token requests return `200` again. Specs: `docs/bugs/2026-05-11-keycloak-admin-password-reseed-on-rebuild.md` and `docs/issues/2026-05-11-keycloak-admin-token-reconciliation-failure.md`.
- [ ] **Argo CD rejects localhost return URLs during SSO login** — `/auth/login` rejects `return_url=http://localhost:8080/...` even though the Keycloak client redirect URIs are present. Spec: `docs/issues/2026-05-11-argocd-localhost-return-url-rejected.md`.
- [x] **Argo CD canonical HTTPS hostname is backed by a Vault PKI-issued local TLS listener** — `bin/acg-up` now installs a root-owned `launchd` daemon that terminates TLS for `argocd.shopping-cart.local:443` and proxies to the existing `localhost:8080` Argo CD port-forward, and `bin/acg-down` removes it. Spec: `docs/issues/2026-05-11-argocd-canonical-https-listener-missing.md` (fixed).
- [x] **Argo CD browser TLS role creation now uses a parent-domain allowed_domains shape** — `scripts/plugins/argocd.sh` derives `allowed_domains=shopping-cart.local` for `argocd.shopping-cart.local`, allowing Vault to accept the PKI role write on rebuild. Spec: `docs/issues/2026-05-11-argocd-browser-tls-role-allowed-domains.md` (fixed).
- [x] **Argo CD browser TLS role write now authenticates to Vault before upserting the PKI role** — `scripts/plugins/argocd.sh` now calls `_vault_login` before the browser TLS role write, avoiding `403 permission denied` on rebuild. Spec: `docs/issues/2026-05-11-argocd-browser-tls-vault-login-permission-denied.md`.
- [x] **Argo CD browser TLS cleanup skips revoke when prior cert is already gone** — `scripts/plugins/vault.sh` now treats a missing browser TLS serial as best-effort cleanup only, so stale certs no longer abort `make up`. Fixed in `f2942bf9`. Issue: `docs/issues/2026-05-11-argocd-browser-tls-revoke-missing-cert.md`.
- [ ] **Planned — `make up TRUST_CA=1` host CA trust bootstrap** — spec `docs/plans/v1.4.5-trust-ca-auto-import.md`. This should keep the trust-store flow opt-in while letting the helper select macOS or Linux behavior automatically.
- [x] **Argo CD browser `launchctl` stderr visibility** — `bin/acg-up` now captures stderr from the macOS browser listener install/bootstrap path in `argocd-browser-https-launchctl.log` and prints it on failure. Commit `8d18e18c`.
- [x] **Argo CD browser `launchctl bootout` best-effort** — `bin/acg-up` now ignores the expected `launchctl bootout system` failure when no listener is loaded, preventing the missing-listener case from aborting bootstrap. Commit `8d18e18c`.
- [x] **Recorded launchctl bootout edge case** — `docs/issues/2026-05-12-acg-up-argocd-browser-bootout-fails-when-listener-missing.md` captures the `Boot-out failed: 5: Input/output error` rebuild case.
- [x] **Stopped advertising localhost as browser entrypoint** — `bin/acg-up` now labels `http://localhost:8080` as terminal-only and `docs/howto/argocd.md` directs browser users to `https://argocd.shopping-cart.local`. Commit `b086aef2`. Issue doc: `docs/issues/2026-05-12-argocd-localhost-browser-entrypoint-misleading.md`.
- [x] **Argo CD browser HTTPS preflight skips sudo when already healthy** — `bin/acg-up` now checks `https://argocd.shopping-cart.local:443/healthz` before touching launchd, so repeated `make up` runs avoid the sudo prompt when the listener is already up. Commit `d5be3d0e`.
- [x] **Keycloak browser HTTP listener on localhost:80** — `bin/acg-up` now installs a local launchd-managed HTTP listener for `keycloak.shopping-cart.local:80` and keeps `/etc/hosts` on `127.0.0.1`, so Safari can reach the Keycloak auth endpoint without a certificate prompt. Commit `bca8a30c`. Issue doc: `docs/issues/2026-05-12-keycloak-browser-http-listener-missing.md`. Supersedes the earlier ingress-gateway mapping (`99581cc7`).
- [x] **Argo CD port-forward context fallback** — the generated wrapper now prefers the requested `k3d-k3d-cluster` context only when it exists, otherwise it falls back to `kubectl config current-context` and then the kubeconfig default. Commit `3f8d5150`. Issue doc: `docs/issues/2026-05-12-argocd-port-forward-context-fallback.md`.
- [x] **Argo CD browser auto-open removed from bootstrap** — `bin/acg-up` no longer opens Safari at the end of bootstrap, so `make up` finishes cleanly on both macOS and Linux without a GUI dependency, then prints `https://argocd.shopping-cart.local` for manual navigation. Issue doc: `docs/issues/2026-05-12-acg-up-browser-auto-open-removed-for-noninteractive-flows.md`.
- [ ] **Argo CD Safari stale localhost session follow-up** — old browser tabs can still exist if opened manually, but `make up` no longer drives Safari. Issue doc: `docs/issues/2026-05-12-argocd-safari-stale-localhost-tab-keeps-reopening.md`.
- [ ] **Argo CD port-forward wrapper scalar context follow-up** — the first fallback patch still rendered `_kubectl_context_args[@]` under `set -u` and quoted an empty kubeconfig value; current fix switches to a scalar context argument and leaves kubeconfig empty when unset. Issue doc: `docs/issues/2026-05-12-argocd-port-forward-wrapper-empty-array-unbound.md`.
- [ ] **Keycloak browser launchd plist path fix** — the Keycloak browser listener was staged under `~/Library/LaunchDaemons`, which does not exist on this machine; current fix moves it to `/Library/LaunchDaemons` and keeps teardown aligned. Issue doc: `docs/issues/2026-05-12-keycloak-browser-launchd-plist-installed-under-home-dir.md`.
- [x] **Keycloak realm import is mandatory** — `bin/acg-up` now fails if the Keycloak realm import does not happen, so bootstrap cannot silently report success with stale SSO state. Issue doc: `docs/issues/2026-05-12-acg-up-silent-keycloak-realm-import-skip.md`.
- [x] **Keycloak realm import status capture no longer concatenates fallback output** — a real `409` from Keycloak is now preserved instead of becoming `409000`. Issue doc: `docs/issues/2026-05-12-acg-up-keycloak-realm-import-status-concatenated-fallback.md`.
- [x] **Keycloak readiness gate reports deployment availability instead of a dead tunnel** — `bin/acg-up` now waits on `deployment/keycloak` becoming `Available` and prints deployment/pod status on timeout, so the loop no longer counts against a stale port-forward. Issue doc: `docs/issues/2026-05-12-keycloak-readiness-loop-watched-dead-port-forward.md`.
- [ ] **Live LDAP bind credentials still mismatch the running directory** — `bin/ldap-search` against the live `openldap-admin` secret returns `ldap_bind: Invalid credentials (49)` even though the cluster is up and Keycloak can reach the LDAP service. Issue doc: `docs/issues/2026-05-12-live-ldap-bind-secret-invalid-credentials.md`.
- [ ] **LDAP username attribute re-aligned to `uid`** — shopping-cart-infra bugfix branch `fix/keycloak-ldap-username-attribute-uid` now sets `LDAP_USERNAME_ATTRIBUTE` and `usernameLDAPAttribute` to `uid` to match the stable directory identifier. Issue doc: `docs/issues/2026-05-13-keycloak-ldap-username-attribute-uid.md`.
- [x] **shopping-cart-infra realm sync moved out of startup** — the Keycloak deployment no longer imports `shopping-cart` on pod boot; `acg-up` owns the live realm sync after Keycloak is healthy. Issue doc: `docs/issues/2026-05-14-keycloak-realm-sync-moved-out-of-startup.md`.
- [x] **shopping-cart-infra realm import placeholders were an intermediate step** — historical notes kept for the old initContainer renderer work and Copilot follow-ups. Issue docs: `docs/issues/2026-05-13-keycloak-realm-import-placeholders-not-rendered.md`, `docs/issues/2026-05-13-kustomize-cross-directory-realm-file-disallowed.md`, `docs/issues/2026-05-13-keycloak-realm-import-invalid-dn-literal-binddn.md`.
- [x] **shopping-cart-infra PR #51 Copilot review addressed** — wired `keycloak-client-secrets` into the Keycloak initContainer env so the renderer sees the required client secret variables, aligned the bug doc to the actual mounted realm import source, and resolved both Copilot review threads via `gh api graphql`. Commit: `1ffaf46`.
- [x] **shopping-cart-infra PR #52 Copilot review addressed** — kept `identity/keycloak/realm-shopping-cart.json` as the single realm import source, added `KC_DB_USERNAME` to the generated `keycloak-config`, removed the stale `identity/keycloak/configmap.yaml`, rendered the realm import with `--db=postgres --override=true`, and resolved the Copilot review threads via `gh api graphql`. Commit: `17e965c`.
- [x] **shopping-cart-infra main/v0.4.0 sync completed** — merged `origin/main` into `shopping-cart-infra-v0.4.0` while preserving the existing branch tip, pushed merge commit `9d1778c`, and restored `enforce_admins=true` on `main`.
- [x] **bin helper filename normalization** — renamed the `bin/*.sh` helpers to extensionless `bin/*` scripts and updated the live caller paths plus active docs to match.
- [x] **make down preserves Hub only when explicitly requested** — default `KEEP_LOCAL` is now `0`, so local Hub deletion is the default teardown and preservation is opt-in via `KEEP_LOCAL=1`. Issue doc: `docs/issues/2026-05-12-make-down-default-preserved-local-hub-was-counterintuitive.md`.
- [ ] **Follow-up — Agent Audit if-count budget in `scripts/lib/test.sh`** — `test_jenkins` and `test_cert_rotation` still exceed the threshold; temporary allowlist entries were added and the failure was recorded in `docs/issues/2026-05-12-scripts-lib-test-if-count-allowlist.md`.
- [x] **Vault troubleshooting helper auth** — `bin/vault-exec` now auto-authenticates `vault ...` commands with the live root token before running them. Issue doc: `docs/issues/2026-05-12-vault-exec-403-unauthenticated-root-token.md`.
- [x] **acg-down browser launchd bootout is best-effort** — already-removed Keycloak and ArgoCD browser listeners now warn instead of failing `make down`. Issue doc: `docs/issues/2026-05-12-acg-down-browser-launchd-bootout-missing-listener-aborted.md`.
- [x] **Argo CD invalid scope `groups`** — Argo CD OIDC config requested `groups` in `requestedScopes`, but Keycloak rejected it with `invalid_scope: Invalid scopes: openid profile email groups`. Fixed by removing `groups` from `shopping-cart-infra/argocd/config/argocd-cm.yaml` while keeping the groups claim mapper. Shopping-cart-infra commit: `09fbb7da14b447ccdbed17bd4a422eebeaa2811d`. Issue doc: `docs/issues/2026-05-12-argocd-invalid-scope-groups-requested.md`.
- [x] **Keycloak LDAP login now keys on email** — shopping-cart-infra Keycloak LDAP federation was aligned to use `mail` as the username attribute, matching the email-style login the browser flow presents. Shopping-cart-infra PR #47 (`https://github.com/wilddog64/shopping-cart-infra/pull/47`) opened from commit `eed0b01`. Issue doc: `docs/issues/2026-05-12-keycloak-ldap-login-username-mail.md`.
- [x] **Shopping-cart-infra Copilot review thread resolved** — clarified the issue-doc heading on PR #47, pushed commit `ea491dd`, and resolved the Copilot review thread with `gh api graphql`.
- [x] **Shopping-cart-infra admin override enabled** — disabled `enforce_admins` on `main` so admins can bypass branch protection when needed. Confirmed the LDAP username change is realm-wide shopping-cart identity config, not Argo CD-only.
- [x] **Keycloak client attribute merge preserves stale PKCE** — live admin `PUT` left `pkce.code.challenge.method` behind in `client_attributes`; `bin/acg-up` now deletes the stale row directly after client reconciliation. Issue doc: `docs/issues/2026-05-12-keycloak-client-attributes-merge-preserves-pkce.md`.
- [ ] **New finding — Keycloak LDAP null username with mail mapping** — live Keycloak log shows `User returned from LDAP has null username!` when `usernameLDAPAttribute=mail`; the live LDAP entry does contain `mail: admin@shopping-cart.local`, so the runtime mapping/import path is still broken. Issue doc: `docs/issues/2026-05-12-keycloak-ldap-null-username-mail-mapping.md`.
- [x] **shopping-cart-infra PR #49 merged and synced** — branch `fix/keycloak-ldap-username-attribute-uid` merged to `shopping-cart-infra-v0.4.0`, `main` updated to merge commit `1fb43a4`, and `enforce_admins=true` restored on `main`.
- [x] **shopping-cart-infra main/v0.4.0 sync no-op** — `origin/shopping-cart-infra-v0.4.0` and `origin/main` are already aligned at `1fb43a4` (`rev-list --left-right --count` returned `0 0`), so there was nothing to merge. Branch protection on `main` is already enabled.
- [x] **shopping-cart-infra v0.3.0 already contains the realm-import fix** — branch `shopping-cart-v0.3.0` includes `0dd3b55` (`fix(keycloak): import realm on startup`) and `5da2560` (`fix(keycloak): restore ldap root dn admin`); `main` has no additional commits to merge into that branch.
- [x] **shopping-cart-infra realm import removed from startup path** — `identity/keycloak/deployment.yaml` no longer imports the `shopping-cart` realm on pod boot; the live `acg-up` reconciliation path owns realm sync after Keycloak is healthy. Issue docs: `docs/issues/2026-05-13-keycloak-realm-import-should-skip-existing-shopping-cart-realm.md` and `docs/issues/2026-05-14-keycloak-realm-sync-moved-out-of-startup.md`.
- [x] **shopping-cart-infra v0.4.0 release note / PR follow-up** — added a short README release-notes bullet linking the idempotent realm-import bug doc, tagged the branch tip as `v0.4.0`, opened PR #53, and requested Copilot review via `gh api`.
- [x] **shopping-cart-infra v0.4.0 release material refresh** — consolidated the README release-note entry into the existing CI/CD Integration `### Release Notes` section and removed the duplicate top-level section; PR #54 (`https://github.com/wilddog64/shopping-cart-infra/pull/54`) still carries the release-note material. Release note commit: `2e97ec6`; release tag `v0.4.0` already exists on `128ab3f`.
- [x] **shopping-cart-infra v0.4.0 tag correction** — `v0.4.0` had pointed at `128ab3f` before the Keycloak fix; it now points at `674b7b1` so the release tag includes the functional fix.
- [x] **shopping-cart-infra PR #53 YAML lint fix** — wrapped the long `sed` command in `identity/keycloak/deployment.yaml` so the CI line-length check passes; pushed commit `272f5d2`.
- [x] **shopping-cart-infra PR #53 fresh-DB precheck fix** — guarded the realm existence lookup with `to_regclass('public.realm')` before querying `realm`, so a new Keycloak database falls through to import without a false startup error.
- [x] **shopping-cart-infra main admin enforcement disabled temporarily** — set `enforce_admins=false` on `main` so PR #53 can be merged; verified the change.
- [x] **shopping-cart-infra v0.5.0 branch cut** — advanced `shopping-cart-infra-v0.5.0` to the current `main` tip (`4a6c14f`) after PR #54 merged, then restored `enforce_admins=true` on `main`.
- [x] **shopping-cart-infra v0.5.0 branch cleanup** — deleted and re-created the local `shopping-cart-infra-v0.5.0` ref from the current `main` tip (`4a6c14f`) so the release branch is clean again.
- [x] **shopping-cart-infra v0.4.0 release published** — created the GitHub release object on the verified `v0.4.0` tag with notes describing the Argo CD PostSync live-reconcile fix. Release URL: `https://github.com/wilddog64/shopping-cart-infra/releases/tag/v0.4.0`.
- [x] **Identity troubleshooting helpers** — added `bin/vault-exec`, `bin/ldap-search`, `bin/keycloak-logs`, and `bin/ldap-logs` with shared `_run_command`-backed wrappers in `scripts/lib/identity_tools.sh`. Added `docs/howto/identity-troubleshooting.md` for quick operator reference.
- [x] **Historical branch prune recommendation documented** — `docs/next-improvements` and `k3d-manager-v1.1.0` through `k3d-manager-v1.4.4` are not ancestors of current `main` or `k3d-manager-v1.4.5`. Recommendation: keep for auditability unless the team explicitly decides to prune branch refs. Issue doc: `docs/issues/2026-05-13-k3d-manager-historical-branches-prune-recommendation.md`.
- [ ] **acg-up known_hosts sync** — managed AWS host IPs in `~/.ssh/config` are now tracked so stale public IP entries can be pruned from `~/.ssh/known_hosts` while current entries remain. Issue doc: `docs/issues/2026-05-13-acg-up-maintain-known-hosts-for-managed-aws-ips.md`.
- [x] **Keycloak intermittent startup timeout documented** — `make up` still occasionally times out at the Keycloak readiness gate because the live deployment remains in `ContainerCreating`/unavailable. Current manifest shows `startupProbe.failureThreshold: 30` with 10s periods. Issue doc: `docs/issues/2026-05-13-keycloak-intermittent-startup-never-reaches-available.md`.
- [x] **Keycloak deployment NotFound readiness gate fix** — `bin/acg-up` now polls for `deployment/keycloak` to appear before it waits for `Available`, so an Argo CD sync delay no longer fails immediately with `NotFound`. Issue doc: `docs/issues/2026-05-13-keycloak-deployment-notfound-should-poll-for-creation.md`.
- [x] **Argo CD VirtualService host default moved to canonical browser URL** — `scripts/etc/argocd/vars.sh` now defaults `ARGOCD_VIRTUALSERVICE_HOST` to `argocd.shopping-cart.local` instead of `argocd.dev.local.me`, so fresh deployments stop writing a stale browser URL into `argocd-cm`. Issue doc: `docs/issues/2026-05-13-argocd-virtualservice-host-default-still-dev-local-me.md`.
- [x] **acg-up stale shopping-cart-infra realm JSON path fixed** — `bin/acg-up` now reads `shopping-cart-infra/identity/keycloak/realm-shopping-cart.json` instead of the deleted `identity/config/realm-shopping-cart.json`, and the Keycloak fixture test was updated to match. Issue doc: `docs/issues/2026-05-13-acg-up-uses-stale-realm-json-path-after-shopping-cart-infra-layout-change.md`. Commit: `c0566cb4`.
- [ ] **Refactor shopping_cart.sh → k3s_remote.sh** — spec: `docs/plans/v1.4.3-refactor-k3s-remote-plugin.md`; assign to Codex (spec updated to also rename source line in `k3s-aws.sh` and `k3s-gcp.sh`)
- [ ] **Service mesh + LB for k3s-aws and k3s-gcp** — spec: `docs/plans/v1.4.3-service-mesh-lb-k3s-remote.md`; assign to Codex AFTER refactor is done
- [ ] **chisel HTTPS tunnel** — spec: `docs/plans/v1.4.3-chisel-tunnel.md`; replaces autossh+socat with HTTPS WebSocket; `TUNNEL_PROVIDER=chisel` gate; depends on refactor spec
- [ ] Restore `deploy_argocd_bootstrap "$@"` passthrough — lib-foundation flag-filtering approach; fix callers that depended on it (esp. `provision-tomcat`)
- [ ] lib-foundation upstream: remove `K3DM_ENABLE_AI=1` from `_copilot_review` usage snippet in `docs/api/functions.md`
- **COMPLETE:** post-provision smoke always runs + /acg-status reconcile — k3d-manager commit `619d47cf` on `k3d-manager-v1.6.3`; spec: `docs/bugs/v1.6.3-bugfix-webhook-post-provision-smoke-and-status-reconcile.md`; `bin/k3dm-webhook` now always runs the post-provision smoke test even when the SSH tunnel is down by replacing the early return with a warning line, and `_run_cluster_status` adds a reconciliation note when smoke passes but hub/tunnel checks are down so `/acg-status` explains that service endpoints are still responding and recommends `/acg-refresh`; validation used `python3 -m py_compile bin/k3dm-webhook` and `make restart-webhook`; commit message: `fix(webhook): always run post-provision smoke test + add reconciliation note when hub down but services up`
- **COMPLETE:** acg-up Vault port-forward stale process + orbctl GUI dialog hint — k3d-manager commit `a5b87701` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-vault-pf-stale-port.md`; `bin/acg-up` now kills any stale `kubectl port-forward` holders on port `18200` via `lsof -ti` before re-binding the Vault port-forward, and logs an OrbStack GUI password dialog warning before `orbctl start` so the pause is explained; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): kill stale vault port-forward by lsof before rebind — warn on orbctl GUI dialog`
- **COMPLETE:** azure.sh top-level ESO vars source removal — k3d-manager commit `5b3c1c6b` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-azure-sh-toplevel-source-eso-vars.md`; `scripts/plugins/azure.sh` no longer sources `scripts/etc/azure/azure-vars.sh` at plugin load time, so `az ad app create` only runs inside `_create_azure_eso_store` when ESO setup actually needs it; validation used `shellcheck -S warning scripts/plugins/azure.sh` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(azure): remove top-level azure-vars.sh source from azure.sh — az ad app create ran on every plugin load`
- **COMPLETE:** acg-up Step 1 Azure fast-path skips extraction on stale sandbox creds — k3d-manager commit `1f7e3ef3` on `k3d-manager-v1.6.5`; spec: `docs/bugs/v1.6.5-bugfix-acg-up-azure-stale-credentials-skip.md`; `bin/acg-up` now probes `az group list` before taking the Azure fast-path, only skips credential extraction when `_az_ok` and a non-empty resource group are both present, and aborts with `_err` if `AZ_RESOURCE_GROUP` is still empty after extraction; validation used `shellcheck -S warning bin/acg-up` and `./scripts/k3d-manager _agent_audit`; commit message: `fix(acg-up): require non-empty resource group in Azure fast-path — stale sandbox creds no longer skip extraction`
- **COMPLETE:** _waitForCredentials single reopen attempt timing fix — lib-acg commit `209d77d` on `feat/v0.1.6`; spec: `docs/bugs/2026-06-11-waitforcredentials-single-reopen-too-aggressive.md`; `playwright/lib/sandbox.js` now uses a `reopenCount` counter with up to 3 reopen attempts and an 8s post-reopen wait, so Azure panels that stay closed briefly during render no longer abort after a single 3s retry; validation used `node --check playwright/lib/sandbox.js`; commit message: `fix(sandbox): allow 3 reopen attempts with 8s wait — single 3s attempt too tight for Azure panel render`
- **PREFLIGHT HARNESS TESTED LIVE (Claude, 2026-06-16):** Ran `make preflight NAME=pr33 MODE=--keep` against Hostinger to test the v1.1.1 vcluster preflight. **All risky plumbing PASSED:** vcluster `pr33` created on `ubuntu-hostinger` (pod `pr33-0` Ready ~60s, syncer coredns healthy); `argocd-manager` RBAC + token minted inside vcluster; API URL derived from NodePort; **`https://srv1754834.hstgr.cloud:30430/healthz` reachable from the Mac** (the feared VPS-firewall hard-stop is already open); registered with hub ArgoCD (`secret/cluster-pr33` in `cicd` — ArgoCD now lists `cluster-pr33`, `cluster-ubuntu-hostinger`, `cluster-ubuntu-k3s`). **Two findings:** (1) committed `bin/cluster-preflight` is a **Phase 1a spike** — `main` stops at `exit 0` (line 569) and **never calls** the already-written `_cluster_preflight_apply_argocd_resources` (503) or `_cluster_preflight_wait_for_apps` (98), so no app deploy yet (prefixed-clone render code exists but is unwired/dead). (2) the spike **exits non-zero** (`make: Error 1`) despite success — will break the P3 commit-status gate. **pr33 left RUNNING** as the live test target for Phase 1b. Next: spec `docs/plans/v1.1.1-vcluster-preflight-phase1b-deploy.md` (wire apply+wait, add `--reuse <name>` skip-create path, fix exit code) → Codex.
- **COMPLETE:** preflight appset-environment isolation — k3d-manager commit `ceffbdb5` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.1.1-bugfix-preflight-appset-environment-isolation.md`; `bin/cluster-preflight` now defaults `APP_CLUSTER_ENVIRONMENT=preflight`, passes `ARGOCD_APP_CLUSTER_ENVIRONMENT` through `register_app_cluster`, and rewrites the cloned platform-helm selector values from `[dev, infra, prod]` to `[${APP_CLUSTER_ENVIRONMENT}]` so the live `platform-helm` appset stops selecting the throwaway vCluster while the preflight clone still converges; validation used `shellcheck -S warning bin/cluster-preflight`, `bash -n bin/cluster-preflight`, and `./scripts/k3d-manager _agent_audit`; commit message: `fix(preflight): isolate vcluster from production appsets via environment label`
- **PREFLIGHT PHASE 1b VERIFIED (Claude, 2026-06-16):** Codex commit `644d3d6d` on `k3d-manager-v1.7.1` (msg `feat(preflight): wire deploy path + --reuse mode + fix success exit code`), confirmed on `origin`. **Static PASS:** diff faithful to spec, only `bin/cluster-preflight` touched (58+/28-); `bash -n` clean, `shellcheck -S warning` clean, `_agent_audit` exit 0. One extra defensive line `PREFLIGHT_REUSE="${PREFLIGHT_REUSE:-0}"` in vars block — harmless. **Live `--reuse` PASS for the wiring:** `make preflight NAME=pr33 MODE=--reuse` skipped create/register, re-derived NodePort, switched to hub, and **applied the prefixed-clone resources** (AppProject clone, data-layer Application, ApplicationSet clones) — the deploy the Phase 1a spike never did. Exit-code fix confirmed: wait timeout → non-zero exit → `make Error 1` (correct — non-converging deploy must fail the gate). **Deploy convergence findings (NEXT increment, NOT 644d3d6d defects):** (1) `pr33-platform` rejected — points at the live `platform` AppProject not the prefixed clone (`destination ... do not match any of the allowed destinations in project 'platform'`); isolation HELD (rejected, did not re-target live). (2) `pr33-preflight-shopping-cart-order` NodePort collision `30081 ... already allocated` — preflight clones pin static NodePorts. (3) `payment`/`product-catalog` fail on missing-namespace ordering. Healthy: `shopping-cart-namespace`; basket+frontend Synced/Progressing. **pr33 KEPT for investigation** (failed → keep, per delete-on-success/keep-on-failure policy). **Lifecycle gap:** current `--destroy` trap destroys unconditionally — does NOT keep-on-failure; needs `rc`-gated teardown for the P3 CI policy.
- [x] **VERIFIED → preflight `--destroy` keep-on-failure** — Codex commit `75c8103c` on `k3d-manager-v1.7.1` (msg `fix(preflight): --destroy keeps vCluster on failure, deletes only on success`), confirmed on `origin`. Static PASS: diff = exactly the two spec blocks (rc-gated teardown + usage line), teardown body/closing `fi`/`return "${rc}"` untouched, only `bin/cluster-preflight` in the fix commit (memory-bank in separate `84a1de62`); `bash -n` + `shellcheck -S warning` + `_agent_audit` all clean. Live test still pending (Claude runs it). Spec: `docs/bugs/v1.1.1-bugfix-preflight-destroy-on-success-keep-on-failure.md`.
- **COMPLETE:** preflight `--auto` default mode — k3d-manager commit `97178adc` on `k3d-manager-v1.7.1`; spec: `docs/bugs/v1.1.1-bugfix-preflight-auto-default-mode.md`; `bin/cluster-preflight` now defaults to `--auto`, accepts `--auto|--keep|--reuse`, renames the smart teardown guard from `--destroy` to `--auto`, and keeps the teardown body itself unchanged; `Makefile` now documents `MODE=--auto|--keep, default --auto`; validation used `shellcheck -S warning bin/cluster-preflight`, `bash -n bin/cluster-preflight`, `./scripts/k3d-manager _agent_audit`, and `grep -n -- '--destroy' bin/cluster-preflight Makefile` (no output); commit message: `feat(preflight): --auto smart teardown is the default; --keep is the keep-always override`. **VERIFIED (Claude, 2026-06-16):** static PASS (SHA on origin, diff = exactly the 5 spec blocks, no `--destroy` left, `rc` capture + `return "${rc}"` intact, shellcheck/bash -n/_agent_audit clean); CLI: `--destroy`+bad-mode both exit 1, usage shows `--auto|--keep|--reuse`. **Live run** `bin/cluster-preflight vauto` (no flag) on `ubuntu-hostinger`: default resolved to `--auto` and on failure (rc=1) logged `[preflight] --auto: run FAILED (rc=1) — keeping vauto ...` with the exact manual-cleanup hint → **keep-on-failure proven live + default=--auto proven live**. Failure was environmental (vcluster refuses 2nd vcluster in `vclusters` ns already held by pr33), so vcluster-create aborted early → no vauto debris (confirmed none on host/hub), pr33 untouched. **delete-on-success live attempt (Claude, 2026-06-16):** tore down pr33 (helm uninstall + hub prune + finalizer strip → ns free, NodePort 30430 freed), then ran clean `bin/cluster-preflight vok` (no flag) → vcluster created+registered+stack deployed, but the 300s app-wait timed out → `rc=1` → `--auto` correctly KEPT vok. **So delete-on-success still NOT proven** — the smart trap is correct, but no converging deploy is achievable until the convergence bugs are fixed. **CONVERGENCE BUG ROOT-CAUSED on clean vok repro — DEFINITIVE (full evidence: `docs/issues/2026-06-16-vcluster-preflight-production-appset-leak.md`):** it is a **cluster-isolation leak, NOT AppProject authorization** (earlier authz hypothesis was WRONG). The preflight registers `cluster-vok` with label `environment: dev`; the LIVE production `platform-helm` ApplicationSet selects clusters by `environment In [dev,infra,prod]` ONLY (no cluster-name guard) → production grabs the throwaway vcluster and deploys `vok-platform` (project `platform`, owned by LIVE `platform-helm` appset — confirmed via ownerReferences) onto it → `InvalidSpecError` (live `platform` project has zero allow entries for the vok server `:30360`). Production owning the platform slot blocks the preflight CLONE `vok-preflight-platform` (rendered correctly but never generated) → platform prereqs (namespaces, ESO/CRDs) never install → cascade: `data-layer` SyncError "tasks not valid", `shopping-cart-data` ns never created. The dead `_cluster_preflight_allow_platform_destination` (defined, never called) is a RED HERRING — patching the live project is the wrong remedy. **Fix direction:** register preflight cluster with a non-production `environment` value (e.g. `preflight`) so live appsets don't select it + update clone appsets to select that value alongside `cluster-name` + drop the dead allow/remove pair + re-verify ns ordering. Needs new spec → Codex (the "deeper diagnosis first" path the user chose). **vok KEPT as the clean triage repro** (replaces pr33, torn down 2026-06-16). Re-run preflight for delete-on-success AFTER the isolation fix lands.
