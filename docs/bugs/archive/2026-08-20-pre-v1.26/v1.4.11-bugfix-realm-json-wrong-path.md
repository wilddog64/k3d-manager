# Bugfix: v1.4.11 — realm-shopping-cart.json path stale after infra repo reorganization

**Status: RESOLVED — `c2c6227a` on `k3d-manager-v1.4.11`**
**Branch:** `k3d-manager-v1.4.11`
**Files:** `bin/acg-up`

---

## Problem

Step 10d/14 fails with `sed: .../identity/config/realm-shopping-cart.json: No such file or directory`.

**Root cause:** `bin/acg-up` referenced `identity/config/realm-shopping-cart.json` but the file
lives at `identity/keycloak/realm-shopping-cart.json` in shopping-cart-infra.

**WARNING — this doc was previously written with the before/after INVERTED. That caused Codex
to keep re-introducing the wrong path. The authoritative location is `identity/keycloak/`.**

---

## Fix (already applied)

```bash
# Before (wrong):
"${REPO_ROOT}/../shopping-carts/shopping-cart-infra/identity/config/realm-shopping-cart.json"

# After (correct):
"${REPO_ROOT}/../shopping-carts/shopping-cart-infra/identity/keycloak/realm-shopping-cart.json"
```

Do NOT change this back to `identity/config/` — that path does not exist.
Verify: `find ~/src/gitrepo/personal/shopping-carts/shopping-cart-infra -name realm-shopping-cart.json`
Expected: `identity/keycloak/realm-shopping-cart.json`

---

## Files Changed

| File | Change |
|------|--------|
| `bin/acg-up` | Corrected realm JSON path from `identity/config/` to `identity/keycloak/` |
