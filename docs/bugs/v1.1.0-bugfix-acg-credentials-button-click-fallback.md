# Bugfix: v1.1.0 — acg_credentials.js skips button click when data-heap-id doesn't match

**Branch:** `k3d-manager-v1.1.0`
**Files:** `scripts/playwright/acg_credentials.js`

---

## Before You Start

1. `git pull origin k3d-manager-v1.1.0`
2. Read `memory-bank/activeContext.md` and `memory-bank/progress.md`
3. Read `scripts/playwright/acg_credentials.js` lines 337–371

---

## Problem

The `waitForFunction` at line 306 detects sandbox buttons by **text content**
(`Start Sandbox`, `Open Sandbox`, `Resume`). It succeeds when any of those
texts appear in the DOM.

The actual click logic at lines 349–369 uses **`data-heap-id` attribute selectors**:

```js
button[data-heap-id*="GCP Sandbox - Open Sandbox"]
button[data-heap-id*="GCP Sandbox - Start Sandbox"]
button[data-heap-id*="GCP Sandbox - Resume"]
```

If the Pluralsight page renders those buttons **without the expected `data-heap-id`**
attribute (e.g. DOM structure changed, or attribute is missing), then:

- `waitForFunction` succeeds (text is present)
- All three `isVisible()` checks return `false` (no data-heap-id match)
- The `if / else if / else if` chain exits without clicking anything
- `_waitForCredentials()` is never called
- `_extractGcpCredentials` immediately fails on `text=Username` (15s timeout)

This is a regression introduced in `132421ad`. Before that commit, text-based
selectors were used for clicking too.

---

## Fix

Add an `else` fallback block after the data-heap-id chain. When none of the
`data-heap-id` buttons are visible, fall back to text-based selectors with the
same click sequence.

**Exact old block (lines 349–370 of `acg_credentials.js`):**

```js
      if (await openButton.isVisible({ timeout: 5000 }).catch(() => false)) {
        console.error('INFO: Clicking Open Sandbox...');
        await openButton.click();
        await page.waitForTimeout(3000);

        // After Open, there might be a Start Sandbox button in the slide-over
        const startButton2 = page.locator(`button[data-heap-id*="${_providerLabel} Sandbox - Start Sandbox"]`).first();
        if (await startButton2.isVisible({ timeout: 5000 }).catch(() => false)) {
          console.error('INFO: Clicking Start Sandbox (Step 2)...');
          await startButton2.click();
        }
        await _waitForCredentials();
      } else if (await startButton.isVisible({ timeout: 5000 }).catch(() => false)) {
        console.error('INFO: Clicking Start Sandbox...');
        await startButton.click();
        await _waitForCredentials();
      } else if (await resumeButton.isVisible({ timeout: 5000 }).catch(() => false)) {
        console.error('INFO: Clicking Resume Sandbox...');
        await resumeButton.click();
        await _waitForCredentials();
      }
    }
```

**Exact new block:**

```js
      if (await openButton.isVisible({ timeout: 5000 }).catch(() => false)) {
        console.error('INFO: Clicking Open Sandbox...');
        await openButton.click();
        await page.waitForTimeout(3000);

        // After Open, there might be a Start Sandbox button in the slide-over
        const startButton2 = page.locator(`button[data-heap-id*="${_providerLabel} Sandbox - Start Sandbox"]`).first();
        if (await startButton2.isVisible({ timeout: 5000 }).catch(() => false)) {
          console.error('INFO: Clicking Start Sandbox (Step 2)...');
          await startButton2.click();
        }
        await _waitForCredentials();
      } else if (await startButton.isVisible({ timeout: 5000 }).catch(() => false)) {
        console.error('INFO: Clicking Start Sandbox...');
        await startButton.click();
        await _waitForCredentials();
      } else if (await resumeButton.isVisible({ timeout: 5000 }).catch(() => false)) {
        console.error('INFO: Clicking Resume Sandbox...');
        await resumeButton.click();
        await _waitForCredentials();
      } else {
        console.error('WARN: No data-heap-id button matched — falling back to text-based selectors');
        const _fbOpen = page.locator('button:has-text("Open Sandbox")').first();
        const _fbStart = page.locator('button:has-text("Start Sandbox")').first();
        const _fbResume = page.locator('button:has-text("Resume")').first();
        if (await _fbOpen.isVisible({ timeout: 2000 }).catch(() => false)) {
          console.error('INFO: Fallback: Clicking Open Sandbox...');
          await _fbOpen.click();
          await page.waitForTimeout(3000);
          const _fbStart2 = page.locator('button:has-text("Start Sandbox")').first();
          if (await _fbStart2.isVisible({ timeout: 5000 }).catch(() => false)) {
            console.error('INFO: Fallback: Clicking Start Sandbox (Step 2)...');
            await _fbStart2.click();
          }
          await _waitForCredentials();
        } else if (await _fbStart.isVisible({ timeout: 2000 }).catch(() => false)) {
          console.error('INFO: Fallback: Clicking Start Sandbox...');
          await _fbStart.click();
          await _waitForCredentials();
        } else if (await _fbResume.isVisible({ timeout: 2000 }).catch(() => false)) {
          console.error('INFO: Fallback: Clicking Resume...');
          await _fbResume.click();
          await _waitForCredentials();
        }
      }
    }
```

---

## Files Changed

| File | Change |
|------|--------|
| `scripts/playwright/acg_credentials.js` | Add text-based fallback after data-heap-id button chain |

---

## Rules

- `bats scripts/tests/providers/k3s_gcp.bats` — must pass
- Only `scripts/playwright/acg_credentials.js` may be touched

---

## Definition of Done

- [ ] `else` fallback block added after the third `else if (await resumeButton...)` branch
- [ ] Fallback logs `WARN: No data-heap-id button matched — falling back to text-based selectors`
- [ ] Fallback tries Open Sandbox → Start Sandbox → Resume in order (text-based)
- [ ] Each fallback branch calls `_waitForCredentials()` after clicking
- [ ] Existing data-heap-id chain is unchanged
- [ ] `bats scripts/tests/providers/k3s_gcp.bats` passes
- [ ] Committed and pushed to `k3d-manager-v1.1.0`
- [ ] `memory-bank/activeContext.md` and `memory-bank/progress.md` updated with commit SHA and task status

**Commit message (exact):**
```
fix(acg-credentials): add text-based button fallback when data-heap-id selectors don't match
```

---

## What NOT to Do

- Do NOT create a PR
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT modify any file other than `scripts/playwright/acg_credentials.js`
- Do NOT commit to `main` — work on `k3d-manager-v1.1.0`
