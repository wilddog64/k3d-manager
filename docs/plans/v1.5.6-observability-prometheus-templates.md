# v1.5.6 — Observability: Move Rules + Alertmanager Config to Template Files

**Branch:** `k3d-manager-v1.5.0`
**Repos touched:** `k3d-manager` only

## Context

v1.5.5 put the PrometheusRule CRD in `shopping-cart-infra` (ArgoCD-managed) and
the Alertmanager config as a heredoc inside `scripts/plugins/observability.sh`.
Both should instead be static template files under `scripts/etc/prometheus/` — the
same pattern as `scripts/etc/observability/istio.yaml`. This keeps all observability
config co-located, readable, and diffable without reading shell code.

The live `shopping-cart-rules` ArgoCD Application (created by v1.5.5) must be removed
from the Hub cluster since `deploy_observability` will apply the rule directly.

---

## Before You Start

- `git pull origin k3d-manager-v1.5.0` in k3d-manager
- Read this spec in full before touching any file
- Branch: `k3d-manager-v1.5.0`

---

## Part 1 — Create template files

### File to create: `scripts/etc/prometheus/rules/shopping-cart-apps.yaml`

```yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: shopping-cart-apps
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  groups:
    - name: shopping-cart.apps
      interval: 60s
      rules:

        - alert: ServiceDown
          expr: |
            sum by (namespace) (
              kube_pod_status_ready{condition="true", namespace=~"shopping-cart.*"}
            ) == 0
          for: 5m
          labels:
            severity: critical
            cluster: hub
          annotations:
            summary: "Service down in {{ $labels.namespace }}"
            description: "No ready pods in namespace {{ $labels.namespace }} for > 5 min."

        - alert: PodCrashLooping
          expr: |
            increase(
              kube_pod_container_status_restarts_total{
                namespace=~"shopping-cart.*"
              }[10m]
            ) > 3
          for: 0m
          labels:
            severity: warning
            cluster: hub
          annotations:
            summary: "Pod crash-looping: {{ $labels.pod }}"
            description: "Container {{ $labels.container }} in {{ $labels.namespace }}/{{ $labels.pod }} restarted > 3 times in 10 min."

        - alert: HighErrorRate
          expr: |
            (
              sum by (destination_service_name, destination_service_namespace) (
                rate(istio_requests_total{
                  destination_service_namespace=~"shopping-cart.*",
                  response_code=~"5.."
                }[5m])
              )
            ) /
            (
              sum by (destination_service_name, destination_service_namespace) (
                rate(istio_requests_total{
                  destination_service_namespace=~"shopping-cart.*"
                }[5m])
              )
            ) > 0.1
          for: 5m
          labels:
            severity: warning
            cluster: hub
          annotations:
            summary: "High error rate: {{ $labels.destination_service_name }}"
            description: "Service {{ $labels.destination_service_name }} in {{ $labels.destination_service_namespace }} has > 10% 5xx errors for 5 min."
```

### File to create: `scripts/etc/prometheus/alertmanager.yaml.tmpl`

Uses `${ALERTMANAGER_GMAIL_FROM}`, `${ALERTMANAGER_GMAIL_APP_PW}`, and
`${ALERTMANAGER_SMS_GATEWAY}` as `envsubst` placeholders — same as
`scripts/etc/cloudflared/config.yml` uses `${CF_DOMAIN}`.

```yaml
global:
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: '${ALERTMANAGER_GMAIL_FROM}'
  smtp_auth_username: '${ALERTMANAGER_GMAIL_FROM}'
  smtp_auth_password: '${ALERTMANAGER_GMAIL_APP_PW}'
  smtp_require_tls: true

route:
  receiver: 'null'
  group_wait: 5m
  group_interval: 2h
  repeat_interval: 24h
  routes:
    - matchers:
        - severity = critical
      receiver: sms-critical

receivers:
  - name: 'null'
  - name: sms-critical
    email_configs:
      - to: '${ALERTMANAGER_SMS_GATEWAY}'
        send_resolved: false
        headers:
          Subject: '[ALERT] {{ .GroupLabels.alertname }}'
        text: '{{ range .Alerts }}{{ .Annotations.summary }}{{ end }}'

inhibit_rules:
  - source_matchers:
      - alertname = ServiceDown
    target_matchers:
      - alertname =~ "PodCrashLooping|HighErrorRate|TargetDown"
    equal: [namespace]
```

---

## Part 2 — Update `scripts/plugins/observability.sh`

### Change 1 — Replace heredoc with template file for Alertmanager secret

Find this block (the `_am_config` heredoc + `_kubectl create secret`):

```bash
    local _am_config
    _am_config=$(cat <<AMEOF
global:
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: '${ALERTMANAGER_GMAIL_FROM}'
  smtp_auth_username: '${ALERTMANAGER_GMAIL_FROM}'
  smtp_auth_password: '${ALERTMANAGER_GMAIL_APP_PW}'
  smtp_require_tls: true

route:
  receiver: 'null'
  group_wait: 5m
  group_interval: 2h
  repeat_interval: 24h
  routes:
    - matchers:
        - severity = critical
      receiver: sms-critical

receivers:
  - name: 'null'
  - name: sms-critical
    email_configs:
      - to: '${ALERTMANAGER_SMS_GATEWAY}'
        send_resolved: false
        headers:
          Subject: '[ALERT] {{ .GroupLabels.alertname }}'
        text: '{{ range .Alerts }}{{ .Annotations.summary }}{{ end }}'

inhibit_rules:
  - source_matchers:
      - alertname = ServiceDown
    target_matchers:
      - alertname =~ "PodCrashLooping|HighErrorRate|TargetDown"
    equal: [namespace]
AMEOF
)
    _kubectl create secret generic alertmanager-smtp-secret \
      --context k3d-k3d-cluster \
      -n monitoring \
      --from-literal=alertmanager.yaml="${_am_config}" \
      --dry-run=client -o yaml | _kubectl apply -f -
    _info "[observability] Alertmanager config secret created"
```

Replace with:

```bash
    local _am_tmpl="${SCRIPT_DIR}/etc/prometheus/alertmanager.yaml.tmpl"
    local _am_config
    # shellcheck disable=SC2016
    _am_config=$(envsubst '${ALERTMANAGER_GMAIL_FROM} ${ALERTMANAGER_GMAIL_APP_PW} ${ALERTMANAGER_SMS_GATEWAY}' \
      < "${_am_tmpl}")
    _kubectl create secret generic alertmanager-smtp-secret \
      -n monitoring \
      --from-literal=alertmanager.yaml="${_am_config}" \
      --dry-run=client -o yaml | _kubectl apply -f -
    _info "[observability] Alertmanager config secret created"
```

### Change 2 — Apply PrometheusRule from file + remove stale ArgoCD Application

Add these two blocks **after** the Alertmanager secret block and **before** the Istio manifest block:

```bash
  local _rules_dir="${SCRIPT_DIR}/etc/prometheus/rules"
  if [[ -d "${_rules_dir}" ]]; then
    _kubectl apply -f "${_rules_dir}/" >/dev/null \
      && _info "[observability] PrometheusRules applied from ${_rules_dir}/"
  fi

  if _kubectl get application shopping-cart-rules -n cicd >/dev/null 2>&1; then
    _kubectl delete application shopping-cart-rules -n cicd >/dev/null \
      && _info "[observability] Removed stale shopping-cart-rules ArgoCD Application"
  fi
```

---

## Rules

- Run `shellcheck scripts/plugins/observability.sh` — zero new warnings
- Validate the new YAML files:
  ```bash
  ruby -e "require 'yaml'; YAML.load_file('scripts/etc/prometheus/rules/shopping-cart-apps.yaml')" && echo ok
  ruby -e "require 'yaml'; YAML.load_file('scripts/etc/prometheus/alertmanager.yaml.tmpl')" && echo ok
  ```

---

## Definition of Done

- [ ] `scripts/etc/prometheus/rules/shopping-cart-apps.yaml` created (exact content above)
- [ ] `scripts/etc/prometheus/alertmanager.yaml.tmpl` created (exact content above)
- [ ] `scripts/plugins/observability.sh` — heredoc removed, template file used, rules applied from dir, stale ArgoCD Application removed
- [ ] shellcheck passes with zero new warnings
- [ ] Ruby YAML validation passes on both new files
- [ ] Commit message: `"refactor(observability): move PrometheusRules + Alertmanager config to template files"`
- [ ] Push `k3d-manager-v1.5.0` to origin before reporting done
- [ ] Update `memory-bank/activeContext.md` and `memory-bank/progress.md` with the commit SHA
- [ ] Report: commit SHA + paste the memory-bank lines updated

## What NOT to Do

- Do NOT create a PR
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT modify files outside the listed targets
- Do NOT commit to `main` — work on `k3d-manager-v1.5.0`
- Do NOT touch `shopping-cart-infra` — leave those files in place, just remove the live ArgoCD Application from the cluster via `_kubectl delete`
- Do NOT use `kubectl` directly — use `_kubectl` (the plugin wrapper)
- Do NOT remove the `--context k3d-k3d-cluster` flag from the `_kubectl create secret` call — confirm whether `_kubectl` already sets context or if it must be explicit (read the existing observability.sh to match the surrounding pattern exactly)
