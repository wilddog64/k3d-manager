# Spec: Restore scripts/tests/lib/system.bats (v0.9.7)

**Branch:** `k3d-manager-v0.9.7`
**Work repo:** `k3d-manager`
**Tracked in:** `docs/issues/2026-03-22-missing-system-bats.md`

---

## Context

`scripts/tests/lib/system.bats` was removed at some point and is now missing from k3d-manager.
The canonical version lives in the embedded lib-foundation subtree at:
`scripts/lib/foundation/scripts/tests/lib/system.bats`

The relative path inside that file (`${BATS_TEST_DIRNAME}/../../lib/system.sh`) resolves
correctly from `scripts/tests/lib/` in k3d-manager — no path changes needed.

---

## Before You Start

1. `git pull origin k3d-manager-v0.9.7`
2. Read this spec in full
3. Read the source file:
   `scripts/lib/foundation/scripts/tests/lib/system.bats`
4. Confirm `scripts/tests/lib/` directory does not exist yet (it must be created)

**Branch:** `k3d-manager-v0.9.7`

---

## The Change

Two steps — nothing else:

### Step 1 — create the directory and copy the file

```bash
mkdir -p scripts/tests/lib
cp scripts/lib/foundation/scripts/tests/lib/system.bats scripts/tests/lib/system.bats
```

### Step 2 — verify the suite runs against k3d-manager's system.sh

```bash
bats scripts/tests/lib/system.bats
```

All 8 tests must pass. If any fail, do NOT commit — report the failure here.

---

## Rules

- `bats scripts/tests/lib/system.bats` — all 8 tests pass
- `shellcheck -S warning scripts/tests/lib/system.bats` — zero warnings
- Do NOT edit the copied file — copy only
- Do NOT modify any other files
- Do NOT create a PR
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT commit to `main`

---

## Definition of Done

- [ ] `scripts/tests/lib/system.bats` exists and is byte-for-byte identical to `scripts/lib/foundation/scripts/tests/lib/system.bats`
- [ ] `bats scripts/tests/lib/system.bats` — all 8 tests pass
- [ ] `shellcheck -S warning scripts/tests/lib/system.bats` exits 0
- [ ] Committed on `k3d-manager-v0.9.7` with exact message:
      `test(lib): restore scripts/tests/lib/system.bats from lib-foundation`
- [ ] Pushed: `git push origin k3d-manager-v0.9.7`
- [ ] Report back: commit SHA + full `bats scripts/tests/lib/system.bats` output
