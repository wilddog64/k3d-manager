# Codex Task Spec: v1.2.0 Phase 1 — Rename antigravity → gemini

**Branch:** `k3d-manager-v1.2.0`
**Agent:** Codex
**Parent plan:** `docs/plans/v1.2.0-lib-acg-extraction.md`

---

## Before You Start

1. `git pull origin k3d-manager-v1.2.0`
2. Read this spec in full before touching any file.
3. Read the current state of every target file listed below.
4. Do NOT read or modify files in `scripts/lib/foundation/`.

---

## What This Task Does

Rename the Antigravity plugin to Gemini — file rename, function renames, and reference
updates across the repo. No logic changes. No files move to lib-acg yet (that is Phase 3).

---

## Target Files

| File | Change type |
|---|---|
| `scripts/plugins/antigravity.sh` | `git mv` → `scripts/plugins/gemini.sh` |
| `scripts/plugins/gemini.sh` | rename functions + variable (see table below) |
| `scripts/plugins/acg.sh` | update source path |
| `bin/acg-up` | update source path |
| `bin/acg-refresh` | update source path |
| `scripts/tests/plugins/aws.bats` | rename stubs |
| `docs/howto/antigravity.md` | `git mv` → `docs/howto/gemini.md` |
| `README.md` | update plugin table, diagram, howto link |

---

## Step 1 — git mv

```bash
git mv scripts/plugins/antigravity.sh scripts/plugins/gemini.sh
git mv docs/howto/antigravity.md docs/howto/gemini.md
```

---

## Step 2 — Rename inside `scripts/plugins/gemini.sh`

Apply these renames. Use `replace_all` — every occurrence in the file must change.

| Old string | New string | Notes |
|---|---|---|
| `_ANTIGRAVITY_GEMINI_MODELS` | `_GEMINI_MODELS` | array variable |
| `_antigravity_gemini_prompt` | `_gemini_prompt` | function name + all calls |
| `_ensure_antigravity` | `_ensure_gemini` | function name + all calls |
| `_antigravity_ensure_github_session` | `_gemini_ensure_github_session` | function name + all calls |
| `antigravity_install` | `gemini_install` | function name + usage strings |
| `antigravity_trigger_copilot_review` | `gemini_trigger_copilot_review` | function name + usage strings |
| `antigravity_poll_task` | `gemini_poll_task` | function name + usage strings |
| `Antigravity plugin` | `Gemini plugin` | header comment |
| `# Antigravity plugin` | `# Gemini plugin` | header comment |

**Do NOT rename these in `gemini.sh` — they belong to Phase 3:**
- `_antigravity_ensure_acg_session` (function stays, moves to lib-acg in Phase 3)
- `_browser_launch` (stays, moves to lib-acg in Phase 3)
- `ANTIGRAVITY_CHROME_NO_SANDBOX` (env var, renamed in Phase 3 alongside cdp.sh)
- `_antigravity_browser_ready` (lib-foundation function — never rename here)

### Key function signatures after rename (verify these exactly)

```bash
# OLD
function _antigravity_gemini_prompt() {
function _ensure_antigravity() {
function _antigravity_ensure_github_session() {
function antigravity_install() {
function antigravity_trigger_copilot_review() {
  local owner="${1:?usage: antigravity_trigger_copilot_review <owner> <repo> [prompt]}"
  local repo="${2:?usage: antigravity_trigger_copilot_review <owner> <repo> [prompt]}"
function antigravity_poll_task() {
  local owner="${1:?usage: antigravity_poll_task <owner> <repo> <task_uuid> [timeout]}"
  local repo="${2:?usage: antigravity_poll_task <owner> <repo> <task_uuid> [timeout]}"
  local task_uuid="${3:?usage: antigravity_poll_task <owner> <repo> <task_uuid> [timeout]}"
```

```bash
# NEW
function _gemini_prompt() {
function _ensure_gemini() {
function _gemini_ensure_github_session() {
function gemini_install() {
function gemini_trigger_copilot_review() {
  local owner="${1:?usage: gemini_trigger_copilot_review <owner> <repo> [prompt]}"
  local repo="${2:?usage: gemini_trigger_copilot_review <owner> <repo> [prompt]}"
function gemini_poll_task() {
  local owner="${1:?usage: gemini_poll_task <owner> <repo> <task_uuid> [timeout]}"
  local repo="${2:?usage: gemini_poll_task <owner> <repo> <task_uuid> [timeout]}"
  local task_uuid="${3:?usage: gemini_poll_task <owner> <repo> <task_uuid> [timeout]}"
```

Also update the `_info` message in `gemini_install`:

```bash
# OLD
  _info "Run 'antigravity_trigger_copilot_review <owner> <repo>' — Antigravity will launch and prompt for GitHub login if needed."
```

```bash
# NEW
  _info "Run 'gemini_trigger_copilot_review <owner> <repo>' — Gemini will launch and prompt for GitHub login if needed."
```

---

## Step 3 — Update source paths

### `bin/acg-up` (line 27)

```bash
# OLD
source "${REPO_ROOT}/scripts/plugins/antigravity.sh"
```

```bash
# NEW
source "${REPO_ROOT}/scripts/plugins/gemini.sh"
```

### `bin/acg-refresh` (line 20)

```bash
# OLD
source "${REPO_ROOT}/scripts/plugins/antigravity.sh"
```

```bash
# NEW
source "${REPO_ROOT}/scripts/plugins/gemini.sh"
```

### `scripts/plugins/acg.sh` (line 14)

```bash
# OLD
source "${SCRIPT_DIR}/plugins/antigravity.sh"
```

```bash
# NEW
source "${SCRIPT_DIR}/plugins/gemini.sh"
```

**Do NOT change** `scripts/plugins/acg.sh` line 301 (`_antigravity_browser_ready 30`) —
that is a lib-foundation function, not an antigravity function.

---

## Step 4 — Update stubs in `scripts/tests/plugins/aws.bats`

Rename only these stubs (use `replace_all`):

| Old | New |
|---|---|
| `_ensure_antigravity` | `_ensure_gemini` |
| `_antigravity_gemini_prompt` | `_gemini_prompt` |

**Do NOT rename** in aws.bats:
- `_ensure_antigravity_ide` (lib-foundation — stays)
- `_ensure_antigravity_mcp_playwright` (lib-foundation — stays)
- `_antigravity_launch` (stale stub — leave in place)
- `_antigravity_ensure_acg_session` (Phase 3 — stays)

---

## Step 5 — Update `README.md`

### Plugin table (line ~203)

```markdown
# OLD
| **Antigravity** | `antigravity_install`, `antigravity_trigger_copilot_review`, `antigravity_poll_task` | Browser automation via gemini CLI + Playwright over CDP (port 9222) — Copilot coding agent trigger |
```

```markdown
# NEW
| **Gemini** | `gemini_install`, `gemini_trigger_copilot_review`, `gemini_poll_task` | Browser automation via gemini CLI + Playwright over CDP (port 9222) — Copilot coding agent trigger |
```

### Architecture diagram (line ~129)

```
# OLD (inside the Mermaid diagram)
  KM --|lazy-load|--> PLUG["plugins/ — acg · aws · antigravity · tunnel · ..."]
```

```
# NEW
  KM --|lazy-load|--> PLUG["plugins/ — acg · aws · gemini · tunnel · ..."]
```

### Howto link (line ~248)

```markdown
# OLD
- **[Antigravity Browser Automation](docs/howto/antigravity.md)** — First-run setup, ACG extend, Copilot agent trigger
```

```markdown
# NEW
- **[Gemini Browser Automation](docs/howto/gemini.md)** — First-run setup, ACG extend, Copilot agent trigger
```

---

## Step 6 — Update `docs/howto/gemini.md` (renamed file)

Inside the renamed `docs/howto/gemini.md`, update any references to the old public
function names:
- `antigravity_install` → `gemini_install`
- `antigravity_trigger_copilot_review` → `gemini_trigger_copilot_review`
- `antigravity_poll_task` → `gemini_poll_task`
- Title/heading "Antigravity" → "Gemini"

Do NOT change internal technical descriptions or env var names like
`ANTIGRAVITY_CHROME_NO_SANDBOX` — those are renamed in Phase 3.

---

## Rules

1. `shellcheck -S warning scripts/plugins/gemini.sh scripts/plugins/acg.sh bin/acg-up bin/acg-refresh` — must pass with zero new warnings.
2. `bats scripts/tests/plugins/aws.bats` — must pass.
3. `grep -rn "antigravity" scripts/ bin/ docs/howto/ README.md | grep -v "lib/foundation\|ANTIGRAVITY_CHROME_NO_SANDBOX\|_antigravity_ensure_acg_session\|_antigravity_browser_ready\|_antigravity_launch"` — must return zero hits.
4. Do not run `--no-verify`.

---

## What NOT to Do

- Do NOT create a PR.
- Do NOT skip pre-commit hooks (`--no-verify`).
- Do NOT rename `_antigravity_ensure_acg_session`, `_browser_launch`, or `ANTIGRAVITY_CHROME_NO_SANDBOX` — those are Phase 3.
- Do NOT rename `_antigravity_browser_ready`, `_ensure_antigravity_ide`, `_ensure_antigravity_mcp_playwright` — those are lib-foundation.
- Do NOT modify files in `scripts/lib/foundation/`.
- Do NOT touch `CHANGE.md` — historical entries stay as-is.
- Do NOT commit to `main` — work on `k3d-manager-v1.2.0`.
- Do NOT modify files outside the listed targets.

---

## Definition of Done

- [ ] `git mv` completed for both files (antigravity.sh → gemini.sh, antigravity.md → gemini.md)
- [ ] All renames applied in gemini.sh, acg.sh, bin/acg-up, bin/acg-refresh, aws.bats, README.md, gemini.md
- [ ] `shellcheck -S warning` passes on all modified `.sh` files with zero new warnings
- [ ] `bats scripts/tests/plugins/aws.bats` passes
- [ ] `grep -rn "antigravity" scripts/ bin/ docs/howto/ README.md | grep -v "lib/foundation\|ANTIGRAVITY_CHROME_NO_SANDBOX\|_antigravity_ensure_acg_session\|_antigravity_browser_ready\|_antigravity_launch"` returns zero hits
- [ ] Committed to `k3d-manager-v1.2.0` with exact message:
      `refactor(gemini): rename antigravity plugin; antigravity_* → gemini_*`
- [ ] Pushed to `origin/k3d-manager-v1.2.0` — do NOT report done until push succeeds
- [ ] Updated `memory-bank/activeContext.md` and `memory-bank/progress.md` with commit SHA and Phase 1 status COMPLETE
- [ ] Report back: commit SHA + paste the memory-bank lines updated
