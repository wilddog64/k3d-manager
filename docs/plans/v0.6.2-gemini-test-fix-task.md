# v0.6.2 — Gemini Test Fix Task (Assertion Drift)

## Context

Two test files have assertions that no longer match production code after Codex fixes B and C.
CI is failing on test 52. Fix both files before Claude reviews.

Source: CI run 22775021421 (test 52 failure), Claude review of `safe_path.bats`.

## Critical Rules

1. **Fix only the assertions listed.** Do not restructure test logic.
2. **Do not modify production code.** Test files only.
3. **Do not modify `memory-bank/`.** Claude owns memory bank updates.
4. **Do not commit.** Claude reviews and commits.
5. Run `bats scripts/tests/lib/k3d_manager_copilot.bats scripts/tests/lib/safe_path.bats`
   after both fixes and report pass/fail per test.

---

## Fix 1 — `scripts/tests/lib/k3d_manager_copilot.bats` line 23

**Problem:** Fix C changed `_copilot_prompt_guard` to report the matched fragment
(`shell(cd`) instead of the original hardcoded string (`shell(cd ..)`).
The test still asserts on the old string — CI fails.

```
Before (line 23):
  [[ "$output" == *"shell(cd ..)"* ]]

After:
  [[ "$output" == *"shell(cd"* ]]
```

---

## Fix 2 — `scripts/tests/lib/safe_path.bats`

**Problem:** All tests were written against the old error message and old label.
Fix B changed both. Four message assertions and two label assertions need updating.

### Error message — all 4 failing tests

```
Before (appears in tests 1, 2, 3, 5):
  [[ "$output" == *"PATH contains world-writable directories"* ]]

After:
  [[ "$output" == *"PATH contains unsafe entries"* ]]
```

### Label — tests 2 and 3

```
Before (test 2):
  [[ "$output" == *"./bin (relative path)"* ]]

After:
  [[ "$output" == *"./bin (relative path entry)"* ]]
```

```
Before (test 3):
  [[ "$output" == *"(relative path)"* ]]

After:
  [[ "$output" == *"(relative path entry)"* ]]
```

---

## Verification

Run targeted suite and confirm all 7 tests pass:

```bash
bats scripts/tests/lib/k3d_manager_copilot.bats scripts/tests/lib/safe_path.bats
```

Expected: 7/7 pass — 2 from `k3d_manager_copilot.bats`, 5 from `safe_path.bats`.
