# Codex Task — v0.7.3 Shopping Cart CI/CD (Tasks 2–5)

**Branch:** `k3d-manager-v0.7.3`
**Assigned to:** Codex
**Machine:** macOS M4 Air (Claude's machine) — shopping-cart repos are on Ubuntu Parallels VM (SSH required)
**Full spec:** `docs/plans/v0.7.3-shopping-cart-cicd.md`

---

## Context

v0.7.3 wires the shopping cart services end-to-end: code push → Docker image built + scanned
→ ghcr.io → ArgoCD deploys to Ubuntu k3s at `10.211.55.14:6443`.

Gemini completed Task 1 (cluster rebuild + validation). You own Tasks 2–5 (pure code, no
cluster dependency). Gemini will run Task 6 (end-to-end verification) after your work is merged.

---

## Prerequisite — Verify SSH Access to Ubuntu

All shopping-cart repos live on the **Ubuntu Parallels VM** (`ssh ubuntu`).

**STOP FIRST — verify SSH access before any code changes:**

```bash
ssh ubuntu "ls ~/src/gitrepo/personal/shopping-carts/"
```

Expected output: list of shopping-cart repos (`shopping-cart-basket`, `shopping-cart-infra`, etc.)

If SSH fails, **do not proceed** — report the error back to Claude immediately.

---

## Repos you will touch

All paths are on the **Ubuntu Parallels VM** (access via `ssh ubuntu`).

| Repo | Location on Ubuntu |
|---|---|
| `k3d-manager` | `~/src/gitrepo/personal/k3d-manager` |
| `shopping-cart-infra` | `~/src/gitrepo/personal/shopping-carts/shopping-cart-infra` |
| `shopping-cart-basket` | `~/src/gitrepo/personal/shopping-carts/shopping-cart-basket` |
| `shopping-cart-order` | `~/src/gitrepo/personal/shopping-carts/shopping-cart-order` |
| `shopping-cart-payment` | `~/src/gitrepo/personal/shopping-carts/shopping-cart-payment` |
| `shopping-cart-product-catalog` | `~/src/gitrepo/personal/shopping-carts/shopping-cart-product-catalog` |
| `shopping-cart-frontend` | `~/src/gitrepo/personal/shopping-carts/shopping-cart-frontend` |

Before starting each task, `git pull origin main` in the relevant repo on M2 Air.

---

## Task 2 — Reusable Workflow (shopping-cart-infra)

**File to create:** `shopping-cart-infra/.github/workflows/build-push-deploy.yml`

The workflow must:
1. Accept `service-name` (string) and `image-name` (string) as `workflow_call` inputs
2. Build the Docker image (load locally, do NOT push yet)
3. Scan with Trivy (`aquasecurity/trivy-action@0.30.0`) — severity `CRITICAL,HIGH`, `exit-code: 1`, `ignore-unfixed: true`
4. Login to `ghcr.io` using `docker/login-action@v3` + `secrets.GITHUB_TOKEN`
5. Push image with tags: `sha-${{ github.sha }}` AND `latest`
6. Update `k8s/base/kustomization.yaml` — replace `newTag:` value with `sha-${{ github.sha }}`
7. Commit + push the kustomization change with actor `github-actions[bot]`

**Permissions required:** `contents: write`, `packages: write`

**All Actions must be pinned to version tags — never `@main` or `@latest`:**
- `actions/checkout@v4`
- `docker/setup-buildx-action@v3`
- `docker/build-push-action@v5`
- `aquasecurity/trivy-action@0.30.0`
- `docker/login-action@v3`

Use GHA cache (`type=gha`) for Docker layer cache.

Exact YAML is in the full spec (`docs/plans/v0.7.3-shopping-cart-cicd.md`) — follow it exactly.

Commit to `shopping-cart-infra` main branch with message: `ci: add reusable build-push-deploy workflow`

---

## Task 3 — Caller Workflow (each service repo)

**For each of the 5 service repos**, add a `publish` job to the existing CI workflow.

The job must:
- `needs: [build]` (or equivalent — check the existing workflow's last job name)
- Run only on: `github.ref == 'refs/heads/main' && github.event_name == 'push'`
- Call: `wilddog64/shopping-cart-infra/.github/workflows/build-push-deploy.yml@<COMMIT_SHA>`
  - Use the commit SHA from Task 2 after the reusable workflow merges to main
- Pass `service-name` and `image-name` per the table below:

| Service | `service-name` | `image-name` |
|---|---|---|
| Basket | `shopping-cart-basket` | `ghcr.io/wilddog64/shopping-cart-basket` |
| Order | `shopping-cart-order` | `ghcr.io/wilddog64/shopping-cart-order` |
| Payment | `shopping-cart-payment` | `ghcr.io/wilddog64/shopping-cart-payment` |
| Product Catalog | `shopping-cart-product-catalog` | `ghcr.io/wilddog64/shopping-cart-product-catalog` |
| Frontend | `shopping-cart-frontend` | `ghcr.io/wilddog64/shopping-cart-frontend` |

**DO NOT modify existing lint/test/build jobs — only add the `publish` job.**

**Frontend only — fix the Dockerfile before adding the publish job:**
- File: `shopping-cart-frontend/Dockerfile`
- Change `FROM nginx:alpine` → `FROM nginx:1.27-alpine`
- Verify `nginx:1.27-alpine` exists on Docker Hub before committing

Commit each service repo separately with message: `ci: add publish job for ghcr.io push`

---

## Task 4 — Fix ArgoCD Application CRs (shopping-cart-infra)

**Files:** `shopping-cart-infra/argocd/applications/*.yaml`

For each Application CR:

### 1. Fix `repoURL`

| File | Correct `repoURL` |
|---|---|
| `basket-service.yaml` | `https://github.com/wilddog64/shopping-cart-basket.git` |
| `order-service.yaml` | `https://github.com/wilddog64/shopping-cart-order.git` |
| `payment-service.yaml` | `https://github.com/wilddog64/shopping-cart-payment.git` |
| `product-catalog.yaml` | `https://github.com/wilddog64/shopping-cart-product-catalog.git` |

### 2. Fix `destination.server`

All Application CRs must have:

```yaml
destination:
  server: https://10.211.55.14:6443
  namespace: shopping-cart-apps
```

### 3. Reduce replicas for lab (CPU capacity)

Reduce replicas to 1 — either:
- Add a `kustomize.patches` block in the Application CR, OR
- Set `replicas: 1` directly in each service repo's `deployment.yaml` under `k8s/base/`

Choose the approach that requires fewer file changes. If you touch service repo deployment
files, do it in the same PR as Task 3.

Commit to `shopping-cart-infra` with message: `fix: correct ArgoCD Application CR repoURLs and destination server`

---

## Task 5 — k3d-manager: `shopping_cart.sh` plugin

**File to create:** `scripts/plugins/shopping_cart.sh` (in k3d-manager repo on Ubuntu)

Implement two public functions:

### `add_ubuntu_k3s_cluster`

- Reads kubeconfig path from `UBUNTU_K3S_KUBECONFIG` env var (default: `${HOME}/.kube/k3s-ubuntu.yaml`)
- If kubeconfig does not exist, print an error with the `scp` command to fix it and `return 1`
- Register with ArgoCD: `argocd cluster add k3s-automation --name ubuntu-k3s --kubeconfig "$kubeconfig"`
- Use `_run_command --` (not bare `argocd`)

### `register_shopping_cart_apps`

- Resolve repo root via `git rev-parse --show-toplevel`
- ArgoCD applications dir: `${repo_root}/../shopping-carts/shopping-cart-infra/argocd/applications`
- If dir does not exist, print error and `return 1`
- Apply: `kubectl apply -f "${argocd_dir}/"`
- Use `_run_command --`

**Dispatcher registration:** Add both function names to the dispatcher in `scripts/k3d-manager`.
Look at how existing functions like `deploy_cluster` are registered and follow the same pattern.

**Security rules (treat violations as bugs):**
- No bare `sudo`
- Route all external commands through `_run_command`
- `set -euo pipefail` at top of file
- Double-quote all variable expansions
- No hardcoded IPs or credentials

Run `shellcheck scripts/plugins/shopping_cart.sh` and fix all warnings before committing.
Report shellcheck output in memory-bank.

Commit with message: `feat: add shopping_cart.sh plugin (add_ubuntu_k3s_cluster + register_shopping_cart_apps)`

---

## Rules

- **Do not** modify `scripts/lib/foundation/` — that is a git subtree
- **Do not** run `git rebase`, `git reset --hard`, or `git push --force`
- **Do not** add changes beyond task scope
- Run `shellcheck` on every `.sh` file you touch and report output
- All GitHub Actions pins must use version tags (`@v4`, `@v3`, `@0.30.0`) — never `@main` or `@latest`
- Commit each repo separately — do not batch unrelated repos in one commit

---

## Completion Report

Update `memory-bank/activeContext.md` in the k3d-manager repo — mark Tasks 2–5 as done and add:

```
## v0.7.3 Tasks 2–5 Completion Report (Codex)

- Task 2 (reusable workflow): PASS/FAIL — commit <sha> in shopping-cart-infra
- Task 3 (caller workflows): PASS/FAIL — commits in each service repo
- Task 3 (frontend Dockerfile pin): PASS/FAIL
- Task 4 (ArgoCD CR fixes): PASS/FAIL — commit <sha> in shopping-cart-infra
- Task 5 (shopping_cart.sh): PASS/FAIL — shellcheck: <output or CLEAN>
- Dispatcher registration: PASS/FAIL
```

Commit and push `memory-bank/activeContext.md` to `k3d-manager-v0.7.3` last.
