# Codex Task: Foundation-managed vCluster CLI — Part B (k3d-manager rewire) (v1.27.0)

**Design spec:** `docs/plans/v1.27.0-foundation-managed-vcluster-cli.md` — read it first; this doc
is the implementation assignment, the spec is the *why* and the acceptance contract.
**Part A (done):** lib-foundation `foundation_ensure_vcluster_cli` shipped as **`v0.4.13`**
(PR #44 `0a3e4043`, tag + GitHub release live) and is **already subtree-pulled** into
`scripts/lib/foundation/scripts/lib/system.sh` (verified: function + 7 helpers present, curl fix
`-o` before `--` intact, `bash -n` clean). **Do not touch the subtree** — the contract is ready.
**Created:** 2026-08-21
**Author:** Claude (reviewed before Codex hand-off)
**Repo:** `k3d-manager` only. **This hand-off is Part B only.**

---

## Guardrails (read before touching anything)

1. **Repo + branch.** Work in `/Users/cliang/src/gitrepo/personal/k3d-manager` on the already-open
   milestone branch **`k3d-manager-v1.27.0`**. Do **not** create a new branch, do **not** commit to
   `main`.
2. **Do NOT edit the vendored subtree.** `scripts/lib/foundation/**` is upstream-owned and already
   carries the contract. Editing it is a **hard violation**. You consume `foundation_ensure_vcluster_cli`,
   you do not modify it.
3. **This is a consumer-rewire only.** No change to vCluster manifests, the Helm chart version
   (`VCLUSTER_VERSION` default stays `0.32.1`), the E2E substrate, the E2E image, the event schema,
   Grafana, or the pinned proxy port. Do **not** add any npm/Python/Homebrew/apt/dnf/pip dependency.
4. **No real downloads, no live cluster.** BATS must **stub** `foundation_ensure_vcluster_cli`. Never
   run a real `make e2e`, never fetch a real vCluster release — the live E2E gate is Claude's, after
   this lands.
5. **No arbitrary PATH `vcluster`.** After the rewire the plugin must obtain its executable **only**
   from the foundation contract's returned path. It must not fall back to a `vcluster` found on PATH,
   `brew`, `curl`, `mv`, `chmod`, or a system install dir.
6. **Style:** `set -euo pipefail` semantics; double-quote every expansion; `--` to separate options;
   LF line endings; no unsolicited refactors; match the surrounding `vcluster.sh` code. Keep using
   `_run_command`, `_info`/`_warn`/`_err`, and `_kubectl`. Keep each function within the
   `AGENT_AUDIT_MAX_IF=8` if-count budget (this rewire only removes branches, so it will not grow).

---

## Current state (real anchors — verify before editing)

`scripts/plugins/vcluster.sh`:

- **`VCLUSTER_INSTALL_DIR`** declared line 7, exported line 13 — **delete both.**
- **`vcluster_install_cli()`** lines 174–176 (public dispatcher wrapper) — **delete.**
- **`_vcluster_install_cli()`** lines 178–218 (brew on mac; curl+chmod+`--prefer-sudo` mv on
  linux; `_err` on unsupported) — **delete entirely.**
- **`_vcluster_check_prerequisites()`** lines 220–232 — currently: `if ! _command_exist vcluster`
  → `_vcluster_install_cli`, then host-context checks. **Rewire** (see B2).
- **Direct `vcluster` invocations** that must switch to the contract path:
  - line 49 `_run_command -- vcluster create …`
  - line 75 `_run_command --no-exit -- vcluster delete …`
  - line 171 `_run_command -- vcluster list …`
  - line 263 `config="$(_run_command -- vcluster connect …)"` (`_vcluster_export_kubeconfig`)
  - line 289 `_run_command --quiet -- vcluster connect …` (`_vcluster_refresh_connection`)
  - line 325 `list_output="$(_run_command --no-exit --quiet -- vcluster list …)"` (`_vcluster_ensure_exists`)
  - **Not invocations — leave as literal display text:** the `DRY_RUN:` `printf` strings at lines
    44 and 68.

The contract you call (already vendored): `foundation_ensure_vcluster_cli <version>` — validates a
`major.minor.patch` version, prints **only** the absolute managed executable path on **stdout**,
diagnostics on stderr, non-zero on failure.

---

## Part B — do all of it, then STOP

### B1 — Remove the local installer + its config

- Delete `VCLUSTER_INSTALL_DIR` (declaration line 7 **and** `export` line 13).
- Delete the public `vcluster_install_cli()` wrapper (174–176) and the private
  `_vcluster_install_cli()` (178–218) in full. No brew, no curl/chmod/mv, no `vcluster-linux-*`
  URL, no system-directory write remains anywhere in this file.

### B2 — Resolve the executable through the foundation contract

- Add a **module-scoped** executable variable near the other `VCLUSTER_*` declarations (e.g.
  `_VCLUSTER_BIN=""`) — **not** `local`, because helpers called *after* prerequisites
  (`_vcluster_export_kubeconfig`, `_vcluster_refresh_connection`, `_vcluster_ensure_exists`) must
  see it. Do **not** `export` it.
- In `_vcluster_check_prerequisites`, **before** the host-context checks, replace the
  `_command_exist`/`_vcluster_install_cli` block with a call to the contract:
  - `_VCLUSTER_BIN="$(foundation_ensure_vcluster_cli "$VCLUSTER_VERSION")"` (capture stdout only).
  - If the contract fails (non-zero) or returns an empty path, `_err` with an actionable message
    and **fail before any lifecycle work** — no fallback.
- Confirm `foundation_ensure_vcluster_cli` is in scope when this plugin runs (it is provided by the
  vendored `scripts/lib/foundation/scripts/lib/system.sh`). Do not source it directly from the
  plugin if the dispatcher already loads foundation; match how other foundation helpers
  (`_run_command`, `_detect_platform`) are already available here.

### B3 — Route every invocation through the resolved path

- Replace each direct `vcluster …` invocation listed above with `"$_VCLUSTER_BIN" …` (keep the
  exact `_run_command` flags, args, `-n "$VCLUSTER_NAMESPACE"`, `--chart-version`, `--wait`,
  `--connect=false`, quoting, and ordering unchanged).
- Leave the `DRY_RUN:` `printf` display strings (lines 44, 68) as literal text — they are messages,
  not executions.
- Namespace, chart version, pinned proxy port (`VCLUSTER_LOCAL_PORT`), cleanup, and dry-run
  semantics stay **unchanged**.

### B4 — Help + user-facing docs

- `scripts/lib/help/utils.sh` — the vCluster help must describe the **foundation-managed
  prerequisite** (the CLI is resolved automatically by `foundation_ensure_vcluster_cli` at the
  pinned `VCLUSTER_VERSION`; no manual install, no `vcluster_install_cli` command). Remove any
  wording that points at a local/self-install command.
- `docs/api/functions.md` line 106 — **delete** the `vcluster_install_cli` row (the command no
  longer exists).
- `docs/howto/vcluster.md` — remove the `vcluster_install_cli` section (lines ~92–101) and the
  `VCLUSTER_INSTALL_DIR` env-var row (~112). Replace with a one-line note that the vCluster CLI is
  provided automatically by the foundation contract at `VCLUSTER_VERSION`; no manual install.
- `docs/guides/vcluster-e2e-harness.md` (exists) — add/adjust a short **prerequisite** note: run
  `make e2e`; the foundation contract resolves the pinned vCluster CLI under
  `${XDG_DATA_HOME:-$HOME/.local/share}/lib-foundation/vcluster/<version>/`; no manual install is
  supported. Do not change any E2E behavior described elsewhere in the guide.

### B5 — Tests (`scripts/tests/plugins/vcluster.bats` + `scripts/tests/plugins/e2e.bats`)

Rework the installer-era tests to the contract model. Stub the contract, not a PATH binary:

- In `setup()` (currently puts a `vcluster` stub on PATH at lines 12–34), keep a stub **script**
  but have `foundation_ensure_vcluster_cli` **return that stub's path**. Add a default stub:
  `foundation_ensure_vcluster_cli() { printf '%s\n' "$VCLUSTER_STUB"; }` so lifecycle tests get a
  working executable path via the contract.
- **Delete/replace** these obsolete tests:
  - `@test "vcluster_create: installs CLI when binary missing"` (≈39–64) — replace with a test that
    stubs `foundation_ensure_vcluster_cli` and asserts `vcluster_create` invokes
    `"$VCLUSTER_STUB" create demo -n vclusters --chart-version 0.32.1 --connect=false -f …` (the
    RUN_LOG assertion must use the **stub path**, not bare `vcluster`).
  - `@test "_vcluster_install_cli: skips if binary already exists"` (≈130–137) — **delete** (function
    gone).
  - `@test "VCLUSTER_INSTALL_DIR defaults to /usr/local/bin"` (≈142–143) — **delete** (var gone).
  - `@test "vcluster_install_cli is a public function"` (≈195–196) — **delete** (command gone).
- **Add** contract tests:
  - `_vcluster_check_prerequisites` sets `_VCLUSTER_BIN` to the contract's returned path.
  - a **failing** `foundation_ensure_vcluster_cli` (non-zero) makes `_vcluster_check_prerequisites`
    (and thus `vcluster_create`) fail before any lifecycle command runs (RUN_LOG has no
    create/connect entry).
  - lifecycle commands (`create`/`list`/`connect`/`delete`) run through the contract-returned path.
- Guard-rail asserts (grep the source, must find nothing):
  - no `brew install loft-sh/tap/vcluster`
  - no `VCLUSTER_INSTALL_DIR`
  - no `vcluster-linux-` download URL
  - no bare `-- vcluster ` invocation (all go through `"$_VCLUSTER_BIN"`)
- `e2e.bats` — if it references `_vcluster_install_cli`/`vcluster_install_cli`/`VCLUSTER_INSTALL_DIR`
  or a PATH `vcluster`, update those to the contract stub the same way. If it does not, leave it
  unchanged.

### B6 — Gates, then STOP

```bash
cd /Users/cliang/src/gitrepo/personal/k3d-manager
bats scripts/tests/plugins/vcluster.bats scripts/tests/plugins/e2e.bats
shellcheck scripts/plugins/vcluster.sh scripts/plugins/e2e.sh
bash -n scripts/plugins/vcluster.sh
# disappearance checks — all must print NOTHING:
grep -nE 'brew install loft-sh/tap/vcluster|VCLUSTER_INSTALL_DIR|vcluster-linux-|vcluster_install_cli' scripts/plugins/vcluster.sh
grep -nE '(^|[^A-Za-z0-9_"])-- vcluster ' scripts/plugins/vcluster.sh
```

All BATS/shellcheck/`bash -n` clean and both greps empty before committing. **Commit on
`k3d-manager-v1.27.0`, push to `origin`, and report the real pushed SHA** (`git push`, then confirm
the SHA is on `origin/k3d-manager-v1.27.0` — do not report a local-only SHA). **Then STOP and hand
back.**

> **GATE (Claude, not Codex):** Claude reviews the diff, runs the disappearance checks
> independently, and — only when the hub is healthy — runs one real `make e2e` to confirm the
> foundation contract resolves the pinned CLI end-to-end (ConfigMap `k3dm.k3d.io/e2e-result=true` →
> exporter `e2e_last_run_pass` → Grafana). Do **not** run `make e2e` yourself.

---

## Out of scope for Codex (do NOT do)

- Editing the vendored `scripts/lib/foundation/**` subtree, or anything in the lib-foundation repo.
- Any **real** vCluster download, real `make e2e`, or live cluster/Grafana verification.
- Changing `VCLUSTER_VERSION`, the chart version, vCluster manifests/values, the pinned proxy port,
  the E2E image, the E2E event schema, or the dashboard.
- Adding any npm/Python/Homebrew/apt/dnf/pip dependency, or accepting an arbitrary `vcluster` on PATH.
- Touching shopping-cart repositories or any file outside the ones named above.
