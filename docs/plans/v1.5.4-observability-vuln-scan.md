# v1.5.4 — Observability + Vulnerability Scanning

## Goal

Deploy a lightweight observability stack across both clusters:

- **Hub k3d** (permanent) — Prometheus + Grafana + Trivy Operator, ArgoCD-managed
- **ACG ubuntu-k3s** (ephemeral) — Prometheus (minimal) + Trivy Operator, ArgoCD-managed

Grafana on Hub shows metrics from both clusters. ACG metrics survive cluster teardown
because Hub Prometheus scrapes them during the session — 7-day retention on Hub.

**M4 Air impact:** zero additional macOS RSS. Everything runs inside OrbStack's VM
(k3d) or on EC2 (ACG). M4 host only adds one `kubectl port-forward` process.

---

## Architecture

```
Hub k3d (permanent, M4 OrbStack VM)
├── monitoring/
│   ├── prometheus         ← scrapes Hub + federates ACG via host.internal:19090
│   └── grafana            ← two datasources: hub-prometheus + acg-prometheus
└── trivy-system/
    └── trivy-operator     ← scans Hub workloads (Vault, ArgoCD, Istio)

ACG ubuntu-k3s (ephemeral, EC2)
├── monitoring/
│   └── prometheus         ← NodePort 30090, port-forwarded to M4 host:19090
└── trivy-system/
    └── trivy-operator     ← scans shopping cart images

M4 host (managed by acg-up / acg-down)
└── kubectl port-forward svc/prometheus-operated 19090:9090 \
      -n monitoring --context ubuntu-k3s --address 0.0.0.0
    PID → ~/.local/share/k3d-manager/run/acg-prom-pf.pid
```

Hub Grafana reaches ACG Prometheus via `http://host.internal:19090` —
OrbStack's hostname for the M4 host, reachable from inside k3d pods.

Loki and Tempo are deferred. AlertManager is disabled.

---

## Target Files

```
# New files
scripts/etc/argocd/applicationsets/observability.yaml          # Hub ApplicationSet
scripts/etc/argocd/applicationsets/observability-acg.yaml      # ACG ApplicationSet
scripts/etc/helm/observability/kube-prometheus-stack-values.yaml       # Hub values
scripts/etc/helm/observability/kube-prometheus-stack-acg-values.yaml   # ACG values
scripts/plugins/observability.sh                               # new plugin
scripts/tests/lib/observability.bats                           # BATS tests

# Modified files
bin/acg-up     ← add Step 14: deploy ACG observability + start port-forward
bin/acg-down   ← add: kill acg-prom-pf.pid
Makefile       ← add observability target + .PHONY
```

---

## Helm Values — Hub Prometheus + Grafana

File: `scripts/etc/helm/observability/kube-prometheus-stack-values.yaml`

```yaml
prometheus:
  prometheusSpec:
    retention: 7d
    resources:
      requests:
        memory: 256Mi
        cpu: 100m
      limits:
        memory: 512Mi
        cpu: 500m
    additionalScrapeConfigs:
      - job_name: istiod
        kubernetes_sd_configs:
          - role: endpoints
            namespaces:
              names: [istio-system]
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name]
            action: keep
            regex: istiod
      - job_name: federate-acg
        scrape_interval: 30s
        honor_labels: true
        metrics_path: /federate
        params:
          match[]:
            - '{job!=""}'
        static_configs:
          - targets:
              - 'host.internal:19090'
            labels:
              cluster: acg

grafana:
  resources:
    requests:
      memory: 64Mi
      cpu: 50m
    limits:
      memory: 192Mi
      cpu: 200m
  additionalDataSources:
    - name: acg-prometheus
      type: prometheus
      url: http://host.internal:19090
      access: proxy
      jsonData:
        timeInterval: 30s

alertmanager:
  enabled: false

kubeEtcd:
  enabled: false
kubeControllerManager:
  enabled: false
kubeScheduler:
  enabled: false
```

## Helm Values — ACG Prometheus (minimal, no Grafana)

File: `scripts/etc/helm/observability/kube-prometheus-stack-acg-values.yaml`

```yaml
grafana:
  enabled: false

alertmanager:
  enabled: false

prometheus:
  prometheusSpec:
    retention: 2h
    resources:
      requests:
        memory: 128Mi
        cpu: 50m
      limits:
        memory: 256Mi
        cpu: 200m
    additionalScrapeConfigs:
      - job_name: istiod
        kubernetes_sd_configs:
          - role: endpoints
            namespaces:
              names: [istio-system]
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name]
            action: keep
            regex: istiod
  service:
    type: NodePort
    nodePort: 30090

kubeEtcd:
  enabled: false
kubeControllerManager:
  enabled: false
kubeScheduler:
  enabled: false
```

## Helm Values — Trivy Operator (shared for both clusters)

File: `scripts/etc/helm/observability/trivy-operator-values.yaml`

```yaml
trivy:
  ignoreUnfixed: true
  severity: CRITICAL,HIGH
  resources:
    requests:
      memory: 64Mi
      cpu: 50m
    limits:
      memory: 256Mi
      cpu: 500m

operator:
  scanJobTTL: 1h
  vulnerabilityReportsPlugin: Trivy
  configAuditScannerEnabled: true
  resources:
    requests:
      memory: 32Mi
      cpu: 20m
    limits:
      memory: 96Mi
      cpu: 100m
```

---

## ApplicationSet — Hub

File: `scripts/etc/argocd/applicationsets/observability.yaml`

```yaml
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: observability
  namespace: ${ARGOCD_NAMESPACE}
  labels:
    managed-by: k3d-manager
    app-type: observability
spec:
  generators:
    - list:
        elements:
          - name: kube-prometheus-stack
            namespace: monitoring
            chart: kube-prometheus-stack
            repoURL: https://prometheus-community.github.io/helm-charts
            targetRevision: 67.9.0
            valuesFile: scripts/etc/helm/observability/kube-prometheus-stack-values.yaml
          - name: trivy-operator
            namespace: trivy-system
            chart: trivy-operator
            repoURL: https://aquasecurity.github.io/helm-charts
            targetRevision: 0.24.1
            valuesFile: scripts/etc/helm/observability/trivy-operator-values.yaml
  goTemplate: true
  template:
    metadata:
      name: '{{.name}}'
    spec:
      project: platform
      destination:
        name: in-cluster
        namespace: '{{.namespace}}'
      sources:
        - repoURL: https://github.com/wilddog64/k3d-manager
          targetRevision: '${K3D_MANAGER_BRANCH}'
          ref: values
        - repoURL: '{{.repoURL}}'
          chart: '{{.chart}}'
          targetRevision: '{{.targetRevision}}'
          helm:
            valueFiles:
              - '$values/{{.valuesFile}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
```

## ApplicationSet — ACG

File: `scripts/etc/argocd/applicationsets/observability-acg.yaml`

```yaml
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: observability-acg
  namespace: ${ARGOCD_NAMESPACE}
  labels:
    managed-by: k3d-manager
    app-type: observability
spec:
  generators:
    - list:
        elements:
          - name: acg-kube-prometheus-stack
            namespace: monitoring
            chart: kube-prometheus-stack
            repoURL: https://prometheus-community.github.io/helm-charts
            targetRevision: 67.9.0
            valuesFile: scripts/etc/helm/observability/kube-prometheus-stack-acg-values.yaml
          - name: acg-trivy-operator
            namespace: trivy-system
            chart: trivy-operator
            repoURL: https://aquasecurity.github.io/helm-charts
            targetRevision: 0.24.1
            valuesFile: scripts/etc/helm/observability/trivy-operator-values.yaml
  goTemplate: true
  template:
    metadata:
      name: '{{.name}}'
    spec:
      project: platform
      destination:
        name: ubuntu-k3s
        namespace: '{{.namespace}}'
      sources:
        - repoURL: https://github.com/wilddog64/k3d-manager
          targetRevision: '${K3D_MANAGER_BRANCH}'
          ref: values
        - repoURL: '{{.repoURL}}'
          chart: '{{.chart}}'
          targetRevision: '{{.targetRevision}}'
          helm:
            valueFiles:
              - '$values/{{.valuesFile}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
```

---

## Plugin — `scripts/plugins/observability.sh`

```bash
#!/usr/bin/env bash
# scripts/plugins/observability.sh

function deploy_observability() {
  _info "[observability] Deploying Hub observability stack..."
  local _appset="${REPO_ROOT}/scripts/etc/argocd/applicationsets/observability.yaml"
  K3D_MANAGER_BRANCH="${K3D_MANAGER_BRANCH:-$(git -C "${REPO_ROOT}" rev-parse --abbrev-ref HEAD 2>/dev/null || echo main)}"
  export K3D_MANAGER_BRANCH
  if envsubst '$ARGOCD_NAMESPACE $K3D_MANAGER_BRANCH' < "${_appset}" | _kubectl apply -f -; then
    _info "[observability] Hub ApplicationSet applied — ArgoCD will sync monitoring/trivy-system"
  else
    _err "[observability] Failed to apply Hub observability ApplicationSet"
    return 1
  fi
}

function deploy_observability_acg() {
  _info "[observability] Deploying ACG observability stack..."
  local _appset="${REPO_ROOT}/scripts/etc/argocd/applicationsets/observability-acg.yaml"
  K3D_MANAGER_BRANCH="${K3D_MANAGER_BRANCH:-$(git -C "${REPO_ROOT}" rev-parse --abbrev-ref HEAD 2>/dev/null || echo main)}"
  export K3D_MANAGER_BRANCH
  if envsubst '$ARGOCD_NAMESPACE $K3D_MANAGER_BRANCH' < "${_appset}" | _kubectl apply -f -; then
    _info "[observability] ACG ApplicationSet applied — ArgoCD will sync monitoring/trivy-system on ubuntu-k3s"
  else
    _err "[observability] Failed to apply ACG observability ApplicationSet"
    return 1
  fi
}

function observability_status() {
  _info "[observability] === Hub (k3d-cluster) ==="
  for _ns in monitoring trivy-system; do
    _info "[observability] --- ${_ns} ---"
    _kubectl get pods -n "${_ns}" --no-headers 2>/dev/null || true
  done
  _info "[observability] === ACG (ubuntu-k3s) ==="
  for _ns in monitoring trivy-system; do
    _info "[observability] --- ${_ns} ---"
    kubectl get pods -n "${_ns}" --context ubuntu-k3s --no-headers 2>/dev/null || true
  done
}

function trivy_scan_report() {
  _info "[observability] VulnerabilityReport summary — Hub:"
  _kubectl get vulnerabilityreports -A --no-headers 2>/dev/null \
    | awk '{print $1, $2, $6, $7, $8}' | column -t | sort -k4 -rn || true
  _info "[observability] VulnerabilityReport summary — ACG:"
  kubectl get vulnerabilityreports -A --context ubuntu-k3s --no-headers 2>/dev/null \
    | awk '{print $1, $2, $6, $7, $8}' | column -t | sort -k4 -rn || true
}
```

---

## Exact Changes

### A — New ApplicationSet files (2)

Create:
- `scripts/etc/argocd/applicationsets/observability.yaml`
- `scripts/etc/argocd/applicationsets/observability-acg.yaml`

With content from the ApplicationSet sections above.

### B — New Helm values directory and files (3)

Create directory `scripts/etc/helm/observability/` and files:
- `kube-prometheus-stack-values.yaml`
- `kube-prometheus-stack-acg-values.yaml`
- `trivy-operator-values.yaml`

With content from the Helm Values sections above.

### C — New plugin `scripts/plugins/observability.sh`

With content from the Plugin section above.

### D — Register plugin in dispatcher

In `scripts/k3d-manager`, the dispatcher uses `_try_load_plugin` for lazy loading.
Add wrapper functions for all four public functions following the existing pattern.

Find the block of `function register_shopping_cart_apps()` and add after it:

```bash
function deploy_observability() {
  _try_load_plugin deploy_observability "$@"
}

function deploy_observability_acg() {
  _try_load_plugin deploy_observability_acg "$@"
}

function observability_status() {
  _try_load_plugin observability_status "$@"
}

function trivy_scan_report() {
  _try_load_plugin trivy_scan_report "$@"
}
```

### E — Makefile target

Add to `Makefile` after the existing targets:
```makefile
## Deploy observability stack (Prometheus+Grafana+Trivy) to Hub k3d
observability:
	./scripts/k3d-manager deploy_observability

## Deploy observability stack (Prometheus+Trivy) to ACG ubuntu-k3s
observability-acg:
	./scripts/k3d-manager deploy_observability_acg

## Show pod status for monitoring/trivy-system on both clusters
observability-status:
	./scripts/k3d-manager observability_status

## Print VulnerabilityReport summary for both clusters
vuln-scan:
	./scripts/k3d-manager trivy_scan_report
```

Add `observability observability-acg observability-status vuln-scan` to `.PHONY`.

### F — `bin/acg-up` Step 14

Add after the existing Step 13 block (around line 1391), before the final `kubectl get nodes`:

```bash
_info "[acg-up] Step 14/14 — Deploying ACG observability (Prometheus + Trivy)..."
source "${REPO_ROOT}/scripts/plugins/observability.sh"
deploy_observability_acg

_info "[acg-up] Step 14b/14 — Starting ACG Prometheus port-forward (ubuntu-k3s → localhost:19090)..."
_acg_prom_pf_pid_file="${_ACG_STATE_DIR}/run/acg-prom-pf.pid"
if [[ -f "${_acg_prom_pf_pid_file}" ]]; then
  kill "$(cat "${_acg_prom_pf_pid_file}")" 2>/dev/null || true
  rm -f "${_acg_prom_pf_pid_file}"
fi
kubectl port-forward svc/prometheus-operated 19090:9090 \
  -n monitoring --context ubuntu-k3s --address 0.0.0.0 \
  >>"${_ACG_STATE_DIR}/logs/acg-prom-pf.log" 2>&1 &
_acg_prom_pf_pid=$!
echo "${_acg_prom_pf_pid}" > "${_acg_prom_pf_pid_file}"
_info "[acg-up] ACG Prometheus port-forward PID: ${_acg_prom_pf_pid} (localhost:19090 → ubuntu-k3s, log: ${_ACG_STATE_DIR}/logs/acg-prom-pf.log)"
```

### G — `bin/acg-down` ACG Prometheus port-forward teardown

Add after the existing frontend port-forward stop block (after line ~103), before the ArgoCD launchd block:

```bash
_info "[acg-down] Stopping ACG Prometheus port-forward..."
_acg_prom_pf_pid_file="${_ACG_STATE_DIR}/run/acg-prom-pf.pid"
if [[ -f "${_acg_prom_pf_pid_file}" ]]; then
  kill "$(cat "${_acg_prom_pf_pid_file}")" 2>/dev/null || true
  rm -f "${_acg_prom_pf_pid_file}"
  _info "[acg-down] ACG Prometheus port-forward stopped"
fi
```

---

## BATS Tests

File: `scripts/tests/lib/observability.bats`

Tests:
1. `deploy_observability` calls `envsubst` with `$ARGOCD_NAMESPACE` and `$K3D_MANAGER_BRANCH`
2. `deploy_observability_acg` calls `envsubst` with `$ARGOCD_NAMESPACE` and `$K3D_MANAGER_BRANCH`
3. `trivy_scan_report` calls `kubectl get vulnerabilityreports -A` for Hub context
4. `trivy_scan_report` calls `kubectl get vulnerabilityreports -A --context ubuntu-k3s` for ACG
5. `observability_status` iterates over `monitoring trivy-system` for both contexts

---

## Before You Start

- `git pull origin k3d-manager-v1.5.0`
- Read this spec in full
- Read `scripts/plugins/argocd.sh` — follow the same plugin patterns
- Read `bin/acg-up` lines 178–191 — follow the same port-forward PID file pattern
- Read `bin/acg-down` lines 89–103 — follow the same teardown pattern
- Read `scripts/k3d-manager` lines around `register_shopping_cart_apps` — follow the same dispatcher wrapper pattern
- Branch: `k3d-manager-v1.5.0`

---

## What NOT to Do

- Do NOT create a PR
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT modify files outside the listed targets
- Do NOT commit to `main` — work on `k3d-manager-v1.5.0`
- Do NOT add AlertManager config — disabled intentionally
- Do NOT add Loki or Tempo — deferred
- Do NOT use `sudo` directly — route through `_run_command` if privileged ops needed
- Do NOT hardcode `host.internal` anywhere except the Helm values files

---

## Rules

- `shellcheck -S warning scripts/plugins/observability.sh` — zero warnings
- `bats scripts/tests/lib/observability.bats` — all pass

---

## Definition of Done

- [ ] `scripts/etc/argocd/applicationsets/observability.yaml` created
- [ ] `scripts/etc/argocd/applicationsets/observability-acg.yaml` created
- [ ] `scripts/etc/helm/observability/` dir with 3 values files created
- [ ] `scripts/plugins/observability.sh` created with 4 public functions
- [ ] Dispatcher wrappers added in `scripts/k3d-manager`
- [ ] `make observability`, `make observability-acg`, `make observability-status`, `make vuln-scan` targets added
- [ ] `bin/acg-up` Step 14 added (deploy ACG observability + port-forward)
- [ ] `bin/acg-down` ACG Prometheus port-forward teardown added
- [ ] BATS tests added and passing
- [ ] shellcheck clean
- [ ] Committed on `k3d-manager-v1.5.0` with message:
  `feat(observability): Prometheus+Grafana+Trivy on Hub+ACG, federated via port-forward`
- [ ] Pushed to `origin/k3d-manager-v1.5.0`
- [ ] memory-bank updated with commit SHA and task status
