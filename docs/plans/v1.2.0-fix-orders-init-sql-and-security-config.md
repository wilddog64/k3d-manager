# Fix: orders init SQL SERIAL→UUID + SecurityConfig probe gap + ArgoCD shared namespace

**Date:** 2026-04-27
**Repos:** `shopping-cart-infra`, `shopping-cart-order`, `shopping-cart-product-catalog`, `k3d-manager`
**Branch (shopping-cart-infra):** `fix/orders-init-sql-uuid`
**Branch (shopping-cart-order — Fix 2):** `fix/actuator-health-security`
**Branch (shopping-cart-order — Fix 3):** `fix/argocd-shared-namespace`
**Branch (shopping-cart-product-catalog):** `fix/argocd-shared-namespace`
**Branch (k3d-manager):** `k3d-manager-v1.3.0` (Fix 3a already done — `services/shopping-cart-namespace/` exists on main)

---

## Before You Start

1. Read memory-bank in k3d-manager:
   ```
   git -C ~/src/gitrepo/personal/k3d-manager pull origin k3d-manager-v1.3.0
   cat ~/src/gitrepo/personal/k3d-manager/memory-bank/activeContext.md
   cat ~/src/gitrepo/personal/k3d-manager/docs/bugs/2026-04-27-orders-init-sql-serial-vs-uuid.md
   ```

2. Read target files before touching anything:
   ```
   cat ~/src/gitrepo/personal/shopping-carts/shopping-cart-infra/data-layer/postgresql/orders/configmap.yaml
   cat ~/src/gitrepo/personal/shopping-carts/shopping-cart-order/src/main/java/com/shoppingcart/order/config/SecurityConfig.java
   cat ~/src/gitrepo/personal/shopping-carts/shopping-cart-order/src/main/java/com/shoppingcart/order/entity/Order.java
   cat ~/src/gitrepo/personal/shopping-carts/shopping-cart-order/src/main/java/com/shoppingcart/order/entity/OrderItem.java
   cat ~/src/gitrepo/personal/shopping-carts/shopping-cart-order/k8s/base/kustomization.yaml
   cat ~/src/gitrepo/personal/shopping-carts/shopping-cart-order/k8s/base/namespace.yaml
   cat ~/src/gitrepo/personal/shopping-carts/shopping-cart-product-catalog/k8s/base/kustomization.yaml
   cat ~/src/gitrepo/personal/shopping-carts/shopping-cart-product-catalog/k8s/base/namespace.yaml
   ls ~/src/gitrepo/personal/k3d-manager/services/
   ```

3. Create feature branches (k3d-manager Fix 3a is already done — skip):
   ```
   git -C ~/src/gitrepo/personal/shopping-carts/shopping-cart-infra checkout -b fix/orders-init-sql-uuid origin/main
   git -C ~/src/gitrepo/personal/shopping-carts/shopping-cart-order checkout -b fix/actuator-health-security origin/main
   git -C ~/src/gitrepo/personal/shopping-carts/shopping-cart-order checkout -b fix/argocd-shared-namespace origin/main
   git -C ~/src/gitrepo/personal/shopping-carts/shopping-cart-product-catalog checkout -b fix/argocd-shared-namespace origin/main
   ```
   Note: `shopping-cart-order` gets TWO branches — Fix 2 and Fix 3 are independent changes.
   Note: Do NOT create any k3d-manager branch — that work is already on main.

---

## Fix 1 — shopping-cart-infra: orders init SQL

**File:** `data-layer/postgresql/orders/configmap.yaml`

Replace the entire `01-init-schema.sql` value. The current SQL creates `SERIAL` primary keys,
but the `Order` and `OrderItem` entities use `@GeneratedValue(strategy = GenerationType.UUID)`.
Hibernate cannot automatically cast existing SERIAL columns to UUID, causing DDL failures on
every fresh cluster.

The new SQL must:
- Use `UUID DEFAULT gen_random_uuid()` for all `id` columns
- Use `UUID` (not `INTEGER`) for `order_id` foreign key in `order_items`
- Match the entity field names exactly: `customer_id`, `total_amount`, `currency`, `created_at`, `updated_at`
- Remove all sample `INSERT` statements (they reference integer IDs)
- Remove all functions that reference `orders_id_seq`
- Remove views that reference the old schema

### Exact old content (key lines — do NOT use SERIAL anywhere):
```yaml
  01-init-schema.sql: |
    -- Orders Database Schema
    ...
    CREATE TABLE IF NOT EXISTS orders (
        id SERIAL PRIMARY KEY,
        order_number VARCHAR(50) UNIQUE NOT NULL,
        user_id INTEGER NOT NULL,
        ...
    );

    CREATE TABLE IF NOT EXISTS order_items (
        id SERIAL PRIMARY KEY,
        order_id INTEGER NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
        ...
    );
```

### Exact new content (replace the entire `01-init-schema.sql` value with this):
```yaml
  01-init-schema.sql: |
    -- Orders Database Schema
    -- Aligned with Order and OrderItem JPA entities (UUID primary keys)

    CREATE EXTENSION IF NOT EXISTS "pgcrypto";

    CREATE TABLE IF NOT EXISTS orders (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        customer_id VARCHAR(255) NOT NULL,
        status VARCHAR(50) NOT NULL DEFAULT 'PENDING',
        total_amount NUMERIC(10, 2) NOT NULL,
        currency VARCHAR(3) NOT NULL DEFAULT 'USD',
        created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
    );

    CREATE TABLE IF NOT EXISTS order_items (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        order_id UUID NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
        product_id VARCHAR(255) NOT NULL,
        product_name VARCHAR(255) NOT NULL,
        quantity INTEGER NOT NULL CHECK (quantity > 0),
        unit_price NUMERIC(10, 2) NOT NULL CHECK (unit_price >= 0),
        total_price NUMERIC(10, 2) NOT NULL CHECK (total_price >= 0)
    );

    CREATE INDEX IF NOT EXISTS idx_orders_customer_id ON orders(customer_id);
    CREATE INDEX IF NOT EXISTS idx_orders_status ON orders(status);
    CREATE INDEX IF NOT EXISTS idx_orders_created_at ON orders(created_at DESC);
    CREATE INDEX IF NOT EXISTS idx_order_items_order_id ON order_items(order_id);

    -- Create function to update updated_at timestamp
    CREATE OR REPLACE FUNCTION update_updated_at_column()
    RETURNS TRIGGER AS $$
    BEGIN
        NEW.updated_at = CURRENT_TIMESTAMP;
        RETURN NEW;
    END;
    $$ LANGUAGE plpgsql;

    DROP TRIGGER IF EXISTS update_orders_updated_at ON orders;
    CREATE TRIGGER update_orders_updated_at
        BEFORE UPDATE ON orders
        FOR EACH ROW
        EXECUTE FUNCTION update_updated_at_column();
```

Everything else in the YAML file (metadata, labels) stays unchanged. Only `data.01-init-schema.sql` changes.

### Commit (shopping-cart-infra):
```
fix(postgresql): align orders init SQL to entity model — UUID primary keys

orders and order_items tables used SERIAL PRIMARY KEY but Order/OrderItem
entities use @GeneratedValue(strategy = GenerationType.UUID). Hibernate
ddl-auto=validate fails on fresh clusters; ddl-auto=update logs WARNINGs
and cannot cast existing SERIAL rows to UUID. Replace SERIAL with UUID
DEFAULT gen_random_uuid(); remove sample INSERTs and stale functions.
```

---

## Fix 2 — shopping-cart-order: SecurityConfig health probe gap

**File:** `src/main/java/com/shoppingcart/order/config/SecurityConfig.java`

The Kubernetes readiness and liveness probes hit `/actuator/health/readiness` and
`/actuator/health/liveness`. The current `permitAll()` only covers `/actuator/health`
(exact match), causing probes to get `401 Unauthorized` and the pod to stay `0/1`.

### Exact old line (line 67):
```java
                .requestMatchers("/actuator/health", "/actuator/info", "/actuator/prometheus").permitAll()
```

### Exact new line:
```java
                .requestMatchers("/actuator/health", "/actuator/health/**", "/actuator/info", "/actuator/prometheus").permitAll()
```

One line change. No other files in `shopping-cart-order` are touched.

### Commit (shopping-cart-order):
```
fix(security): permit /actuator/health/** for Kubernetes probe access

SecurityConfig only permitted /actuator/health (exact match), causing
/actuator/health/readiness and /actuator/health/liveness to return 401.
Kubernetes probes cannot pass authentication, so pods stayed 0/1 Ready.
```

---

## Fix 3 — ArgoCD SharedResourceWarning: Namespace/shopping-cart-apps

Both `shopping-cart-order` and `shopping-cart-product-catalog` define `Namespace/shopping-cart-apps`
in their `k8s/base/`. ArgoCD can only track a resource under one Application; the second app to
sync raises a `SharedResourceWarning` and stays OutOfSync permanently (livelock).

Additionally the two definitions have divergent labels (`istio-injection: enabled` in order vs
`app.kubernetes.io/managed-by: kustomize` in product-catalog), so each sync cycle overwrites
whatever the other app set on the live namespace.

Fix: remove `namespace.yaml` from both upstream repos and introduce a dedicated
`services/shopping-cart-namespace/` Application in k3d-manager that solely owns the namespace
with the merged correct labels.

### Fix 3a — k3d-manager: `services/shopping-cart-namespace/` — ALREADY DONE

This was completed in v1.2.0 and is already on `main`. Do NOT re-do this step.
`services/shopping-cart-namespace/namespace.yaml` and `kustomization.yaml` both exist.

---

### Fix 3b — shopping-cart-order: remove `k8s/base/namespace.yaml`

**Branch:** `fix/argocd-shared-namespace` (separate from `fix/actuator-health-security`)

**File 1: `k8s/base/kustomization.yaml`**

### Exact old resources block:
```yaml
resources:
- namespace.yaml
- serviceaccount.yaml
- configmap.yaml
- secret.yaml
- deployment.yaml
- service.yaml
```

### Exact new resources block (remove the `namespace.yaml` line only):
```yaml
resources:
- serviceaccount.yaml
- configmap.yaml
- secret.yaml
- deployment.yaml
- service.yaml
```

**File 2: `k8s/base/namespace.yaml`** — delete this file entirely:
```
git rm k8s/base/namespace.yaml
```

No other files in `shopping-cart-order` are touched for this fix.

### Commit (shopping-cart-order, branch fix/argocd-shared-namespace):
```
fix(k8s): remove duplicate Namespace/shopping-cart-apps definition

Namespace is now owned by the dedicated shopping-cart-namespace ArgoCD
Application in k3d-manager. Resolves SharedResourceWarning that caused
this Application to stay OutOfSync.
```

---

### Fix 3c — shopping-cart-product-catalog: remove `k8s/base/namespace.yaml`

**Branch:** `fix/argocd-shared-namespace`

**File 1: `k8s/base/kustomization.yaml`**

### Exact old resources block:
```yaml
resources:
- namespace.yaml
- serviceaccount.yaml
- configmap.yaml
- secret.yaml
- deployment.yaml
- service.yaml
```

### Exact new resources block (remove the `namespace.yaml` line only):
```yaml
resources:
- serviceaccount.yaml
- configmap.yaml
- secret.yaml
- deployment.yaml
- service.yaml
```

**File 2: `k8s/base/namespace.yaml`** — delete this file entirely:
```
git rm k8s/base/namespace.yaml
```

No other files in `shopping-cart-product-catalog` are touched.

### Commit (shopping-cart-product-catalog, branch fix/argocd-shared-namespace):
```
fix(k8s): remove duplicate Namespace/shopping-cart-apps definition

Namespace is now owned by the dedicated shopping-cart-namespace ArgoCD
Application in k3d-manager. Resolves SharedResourceWarning that caused
this Application to stay OutOfSync.
```

---

## Rules

- Run `shellcheck` on any shell files touched (there are none in these repos — skip)
- For `shopping-cart-order` Fix 2: run `./mvnw test -pl . --no-transfer-progress` after the change and confirm zero test failures before committing
- For `shopping-cart-infra`: no test suite — verify YAML is valid with `python3 -c "import yaml; yaml.safe_load(open('data-layer/postgresql/orders/configmap.yaml'))"` after the change
- For `k3d-manager` Fix 3: validate new YAML files with `python3 -c "import yaml; yaml.safe_load(open('services/shopping-cart-namespace/kustomization.yaml'))"` and `python3 -c "import yaml; yaml.safe_load(open('services/shopping-cart-namespace/namespace.yaml'))"`
- For `shopping-cart-order` Fix 3 and `shopping-cart-product-catalog`: no test suite — verify the file is deleted and kustomization.yaml no longer references `namespace.yaml`
- Do NOT run `--no-verify` on commits

---

## Definition of Done

### shopping-cart-infra
- [ ] `data-layer/postgresql/orders/configmap.yaml` — `01-init-schema.sql` uses UUID, no SERIAL, no sample INSERTs, no `orders_id_seq` references
- [ ] YAML validates with `python3 -c "import yaml; yaml.safe_load(open('data-layer/postgresql/orders/configmap.yaml'))"`
- [ ] Committed on branch `fix/orders-init-sql-uuid` with exact commit message above
- [ ] Pushed: `git push origin fix/orders-init-sql-uuid` — do NOT report done until push succeeds
- [ ] Report back: commit SHA

### shopping-cart-order
- [ ] `SecurityConfig.java` line 67 — `/actuator/health/**` added to the requestMatchers list
- [ ] `./mvnw test --no-transfer-progress` passes with zero failures
- [ ] Committed on branch `fix/actuator-health-security` with exact commit message above
- [ ] Pushed: `git push origin fix/actuator-health-security` — do NOT report done until push succeeds
- [ ] Report back: commit SHA

### k3d-manager (Fix 3a) — ALREADY DONE, skip

### shopping-cart-order (Fix 3b)
- [ ] `k8s/base/namespace.yaml` deleted (`git rm`)
- [ ] `k8s/base/kustomization.yaml` resources list no longer contains `- namespace.yaml`
- [ ] Committed on branch `fix/argocd-shared-namespace` with exact commit message above
- [ ] Pushed: `git push origin fix/argocd-shared-namespace` — do NOT report done until push succeeds
- [ ] Report back: commit SHA

### shopping-cart-product-catalog (Fix 3c)
- [ ] `k8s/base/namespace.yaml` deleted (`git rm`)
- [ ] `k8s/base/kustomization.yaml` resources list no longer contains `- namespace.yaml`
- [ ] Committed on branch `fix/argocd-shared-namespace` with exact commit message above
- [ ] Pushed: `git push origin fix/argocd-shared-namespace` — do NOT report done until push succeeds
- [ ] Report back: commit SHA

### After all pushes: update k3d-manager memory-bank
Update `memory-bank/activeContext.md` and `memory-bank/progress.md` in `k3d-manager` on branch `k3d-manager-v1.3.0`:
- Mark "orders init SQL SERIAL vs UUID" as COMPLETE with SHAs for shopping-cart-infra and shopping-cart-order (Fix 2)
- Mark "ArgoCD SharedResourceWarning — duplicate Namespace/shopping-cart-apps" as COMPLETE with SHAs for k3d-manager, shopping-cart-order, shopping-cart-product-catalog
- Note that the k3d-manager kustomize workarounds (`SPRING_JPA_HIBERNATE_DDL_AUTO`, TCP probes) can now be removed once Fix 1 and Fix 2 PRs are merged

---

## What NOT to Do
- Do NOT create a PR
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT modify files outside the listed targets (no other `.java` files, no `pom.xml`, no other configmaps, no other `k8s/base/` files)
- Do NOT commit to `main` — always work on the specified feature branches
- Do NOT edit `shopping-cart-infra` from the `shopping-cart-order` context or vice versa
- Do NOT remove the k3d-manager kustomize workarounds (`SPRING_JPA_HIBERNATE_DDL_AUTO`, TCP probes) — those stay in `services/shopping-cart-order/kustomization.yaml` until Fix 1 and Fix 2 PRs are merged and live
- Do NOT delete `namespace.yaml` from both repos without also committing the k3d-manager `services/shopping-cart-namespace/` in the same push batch — the namespace must always have an owner
- Do NOT modify any `ApplicationSet` YAML — the existing generator already picks up new `services/*` directories automatically
- Do NOT touch `shopping-cart-basket`, `shopping-cart-frontend`, or `shopping-cart-payment` — they have no `namespace.yaml` and are unaffected
