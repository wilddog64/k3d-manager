# Retrospective — v1.5.1

**Date:** 2026-05-31
**Milestone:** OCI object storage backup/restore + ACG credential automation improvements
**PR:** #86 — merged to main (SHA `a33971244b6ee1414db872c44fa029399ae0daa9`)
**Participants:** Claude, Codex, Gemini, Copilot

## What Went Well
- OCI object storage backup/restore implemented end-to-end (`oci_backup`, `oci_restore`, Makefile targets, BATS coverage)
- Copilot caught 7 real findings: shell injection via snapshot name (OWASP A03), SSH fail-fast gaps, empty download guard, step label mismatch, CHANGELOG accuracy
- All 7 Copilot findings fixed same session in single commit (commit on k3d-manager-v1.5.1)
- BATS error-path coverage added for all new error paths (30/30 pass)
- enforce_admins workflow functioned correctly — disabled for merge, re-enabled in post-merge

## What Went Wrong
- CHANGELOG initially claimed ACG automation "automates sign-in form submission" — inaccurate; CAPTCHA blocks full automation; caught by Copilot
- Step labels (10/10 vs 10/12) stale after adding steps 11/12 — minor but noticed by Copilot
- Initial oci_backup/oci_restore focused on happy path only; error paths required Copilot review to surface

## Process Rules Added
| Rule | Where |
|------|--------|
| All user-supplied values used in remote SSH command strings must be validated against an allowlist pattern | CLAUDE.md Security Rules |

## Decisions Made
- Snapshot name format pinned to `^k3s-etcd-[0-9]{8}-[0-9]{6}\.db$` — strict enough to block injection, matches auto-generated names
- CAPTCHA manual fallback documented in CHANGELOG rather than claiming full automation

## Theme
v1.5.1 closed the OCI cluster lifecycle loop: you can now take etcd snapshots, push them to OCI object storage, and restore from them. The session was tight — all 7 Copilot findings were addressed without needing a second review cycle, and BATS error coverage was added proactively. The main lesson: initial implementations focus on happy paths; Copilot's value is in surfacing the error paths systematically.
