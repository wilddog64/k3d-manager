# Retrospective — v1.4.9

**Date:** 2026-05-25
**Milestone:** v1.4.9 — ESO ownership fix, lib-acg v0.3.1 subtree, acg-down sudo pre-warm
**PR:** #80 — merged to main (`a294dfc250cb1e6eecf1e07f478fa7c70c6b60d9`)
**Participants:** Claude, Codex, Copilot

## What Went Well
- Root cause of `payment-db-credentials-eso` Degraded status found quickly: ESO v1 enforces single ownership per Secret; `postgres-payment-app` already owned the Secret with `creationPolicy: Owner`, making k3d-manager's ESO fully redundant
- lib-acg subtree discipline enforced: Copilot findings on subtree files were correctly routed upstream to lib-acg rather than fixed directly in the vendored copy
- All 9 Copilot PR #80 threads addressed and resolved; all 4 lib-acg PR #29 threads addressed and resolved
- Merge conflict resolution clean: subtree (`--ours`), memory-bank (`--ours`), CHANGELOG (manual) all resolved correctly

## What Went Wrong
- Claude initially started fixing lib-acg subtree bugs directly in k3d-manager — caught and corrected by user; fixed upstream in lib-acg instead
- Haiku post-merge subagent failed to execute steps (described instead of ran) — Claude executed post-merge steps directly
- CHANGELOG entry had wrong file path (`scripts/plugins/payment.sh` instead of `services/shopping-cart-payment/kustomization.yaml`) — caught by Copilot and fixed in merge conflict resolution

## Process Rules Added
| Rule | File | Detail |
|------|------|--------|
| lib-acg subtree edits upstream first | MEMORY.md | Edits to `scripts/lib/acg/` must go to lib-acg repo first; direct edits in k3d-manager subtree are overridden on next `git subtree pull` |
| `git grep -F` for fixed-string searches | lib-acg pre-commit | Filenames as grep patterns need `-F` to prevent regex metachar false positives |
| Bug spec Rules wording | lib-acg /bugfix template | "No other files touched" → "Code change limited to <file>; docs/memory-bank updates may also be required" |

## Decisions Made
- Payment ESO removed entirely from k3d-manager — `postgres-payment-app` in shopping-cart-infra is the authoritative owner
- lib-acg v0.3.1 fixes (pre-commit `-F`, acg_check_ttl sentinel, package versions) pulled into k3d-manager via subtree at `3af3a4e3`

## Theme
v1.4.9 was a cleanup sprint: fix the ESO ownership conflict causing payment credential Degraded status, enforce lib-acg subtree discipline by routing all subtree bug fixes upstream, and pull lib-acg v0.3.1 into the vendored copy. Nine Copilot findings were caught across two PRs — four in lib-acg (including a real `git grep` regex bug), nine in k3d-manager (including a CHANGELOG path error and stale docs). The subtree discipline correction mid-session was the key process moment.
