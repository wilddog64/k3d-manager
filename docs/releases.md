# Release History — k3d-manager

| Version | Date | Highlights |
|---|---|---|
| [v1.24.1](https://github.com/wilddog64/k3d-manager/releases/tag/v1.24.1) | 2026-08-13 | Make cluster status legible — a point release on v1.24.0. `make status` now defaults to a concise color-coded service-health summary (with `make status-full` for the detailed report and a stable `make status-json` for Slack/CI), follows the active cluster provider, and takes `SERVICE=<name>` for focused per-service diagnostics (`7ed82b89`). The same summary reaches Slack: `/cluster-status` renders an emoji-severity one-liner (`:x:`/`:warning:`/`:white_check_mark:` + `N ok / N warn / N fail`) instead of an ANSI wall (`5b9442cf`). The CVE remediation dashboard splits **Current CVE Remediation Status** from **Remediation History (audit)**, the exporter marks superseded / `deployment_advanced` failed events, and both dashboard and inventory tables hide repeated Prometheus scrape metadata behind concise headers (`d471d075`, `a119fdde`). Status fixes: follow the active provider in all modes + soften optional Pushgateway to a warning (`2758e626`, `7c000254`), read the webhook token from the keyed Keychain account (`15579e7a`), and use current Vault-sourced ArgoCD/Grafana + hub-scoped Keycloak credentials so logins stop false-failing after rotation (`a729e8ce`). Verified live — `make status-json` → healthy for k3s-hostinger; the Slack path renders the concise HEALTHY summary from 13 real services. |
| [v1.24.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.24.0) | 2026-08-11 | Platform hardening — reconcile live drift into git, automate credential rotation, and make CVE remediation durable. The internet-facing k3dm webhook now **fails closed** on malformed Slack signature input and enforces the Slack `user`→role allowlist as real code (dead until now), so an invalid identity resolves to reader, never admin (`3fddcf3e`). Istio-ambient + hostinger-CNI fixes that had only ever been applied live (committed to the archive branch) are reconciled onto the release branch so a rebuild reproduces them — `ServerSideDiff=true` compare-option + istiod `ValidatingWebhookConfiguration` `ignoreDifferences`, and the Istio CNI DaemonSet pointed at k3s/flannel paths not Cilium defaults (`357edf52`). Two persistent credentials gain recurring rotation: a least-privilege in-cluster **ArgoCD admin rotator** (monthly CronJob in `cicd`, bcrypt fed via stdin so the plaintext never hits argv, pinned `resourceNames` Role, `sidecar.istio.io/inject: "false"` so the Job isn't deadlocked by the mesh) and a host-side **Prometheus basic-auth** launchd timer (`3db193cb`), with the weak `admin/password` Prometheus default removed at the source (`e1256d0a`). Live verify caught four latent rotator defects the structural BATS missed — `argocd account bcrypt` `fatal EOF` from a missing stdin newline, the tool's `Password:` prompt captured into the hash, an istio-sidecar Job deadlock in the injected `cicd` namespace, and a launchd plist pointing at a nonexistent `bin/k3d-manager` (`84232cc0`). The CVE remediation **promoter persists to git** — `_git_persist_promotion` clones the app's tracked `targetRevision` branch, awk-pins the remediated `digest:` in `services/shopping-cart-<svc>/kustomization.yaml`, and commits+pushes as `k3dm-cve-promoter`, so a promotion survives a cluster recreate instead of being a live Application patch; `GIT_WRITE_TOKEN` is optional with a live-patch fallback (`3df62fbf`). The webhook's headless CVE analysis returns real text instead of auto-denying (`69e21e15`); `make show-service-passwords` reads the ArgoCD admin password from Vault so it survives rotation (`33e42905`). Verified live on the hub — one-shot ArgoCD rotation advanced `argocd-secret` mtime, wrote a clean bcrypt, and the new Vault password logged into `/api/v1/session` (wrong password rejected as control); the promoter git-persist path was dry-run end-to-end (clone via `x-access-token:` URL → real `set_image_digest.awk` → `git push --dry-run` authenticates) leaving the remote untouched |
| [v1.23.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.23.0) | 2026-08-09 | Make CVE remediation observable and self-verifying — v1.20.0 made the auto-patch loop run, this makes it trustworthy. A CVE inventory dashboard + a `vulnerability-inventory-exporter` show **what** is vulnerable and **where** (real namespace/image/resource) and self-GC the event ledger to the newest N terminal events per service so stale `manual_review` rows clear (`9168edd7`, 70→15 live). The remediation verifier confirms the **Deployment-pinned spec digest** + readiness instead of the pod runtime `imageID` — which containerd aliases to a different OCI index digest for multi-arch images, producing false `ready_pod_digest_mismatch` on images that were in fact patched (`33b45a41`) — and looks in each service's real namespace (`payment` → `shopping-cart-payment`, `8a8566e8`). A new `CVERemediationInFlight` alert inhibits `TrivyCriticalVulnerabilityDetected` for the same image during an active auto-patch and the analyze route caps re-notification at 12h (`ed52cf0c`), the source alert normalizing `image_repository` with `label_replace` so the host-qualified `cve_remediation_state` matches the bare Trivy labels (`72be9383`); TrivyCritical no longer pages on an empty `image_repository` (`5302ea54`). Two Bitnami-removal outages are closed by re-pinning the remediation verifier and LDAP rotator off the deleted `bitnami/kubectl` to `alpine/k8s:1.31.4` (`33b151ba`, `ddc68c90`), and the pulled-forward Grafana Vault-sourced monthly rotator (`5b418dd7`) runs end-to-end after four latent blockers (runAsUser `a66463e1`, openssl-free password `4557cdeb`, rollout-status RBAC `a0bb46c2`, DB-apply + hub-scope smoke `816835fd`). Also: webhook rate-limit moved after auth + `Content-Length` guard (`ee32837d`), and the alert-analysis / `gemini.sh` model ids retired off the removed `gemini-2.5-flash` (`8e7a5c79`, `612ca86d`). Verified live end-to-end on the hub — the verifier flips matching-digest payment events `promotion_requested → applied` and Alertmanager suppresses the paired TrivyCritical during an in-flight remediation, lifting on completion |
| [v1.22.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.22.0) | 2026-08-07 | Migrate OpenLDAP off the retired Bitnami image to the Symas chart — Bitnami's `bitnamilegacy` OpenLDAP images are unmaintained, so the directory service moves to the community `jp-gouin/openldap-stack-ha` chart (4.3.3) with every consumer reconciled so the cutover is transparent: admin/config passwords are generated delimiter-safe (hex) so the chart's `sed`-based value templating can't corrupt them (`7fb1ad28`), the platform `admin`/`developer`/`operator` users survive the swap via a durable Vault-seeded bootstrap instead of a one-shot (`c6195bb2`), and Keycloak LDAP federation + the password-rotator labels reconcile to the new `openldap.identity.svc.cluster.local` service (`0b23884b`, `e9fa00cc`); verified live — the Symas chart is running, `developer` login succeeds through Keycloak, and Jenkins LDAP auth is reconciled; `ldap_chart_passwords` 2/2 BATS green, shellcheck clean |
| [v1.21.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.21.0) | 2026-08-03 | Harden the k3dm webhook auth surface. A security review of the internet-facing webhook (Cloudflare Worker → tunnel → `127.0.0.1:7443`) found the bearer token was the only real boundary, and it grants full admin — so this release layers defense-in-depth. The **Slack command path** now enforces the same RBAC as the HTTP path: a `user`→role allowlist (`K3DM_SLACK_ROLE_MAP` env / Keychain `k3dm-slack-role-map`, unknown user → reader) plus a minimum-role check in `_handle_thread_command`, so Slack no longer bypasses the role gate (`490756e1`). Roles **fail closed** — a present-but-invalid `X-K3DM-Role` header resolves to reader instead of admin; an absent header is still the direct-token admin path, unchanged. The `/ask` read-only bash sandbox blocks interpreters, nested shells, and redirection in `k3dm-ask-bash`, closing the denylist-bypass escapes. The server gains a fixed-window **rate limiter** (`429` on sustained bursts), guarded POST `json.loads` (`400` on malformed bodies instead of a traceback), a bounded Slack `_seen_event_ids` dedup cache (OrderedDict cap 2048), a `TOKEN_FILE` over-permissive-mode rejection, and drops the `GEMINI_CLI_TRUST_WORKSPACE` workspace-trust escape hatch. `make show-service-passwords` shows the current Keycloak user passwords (`21cf3a38`). Verified live on a restarted `:7443` instance — health 200, no-token → 401, invalid role → 403 (fail-closed to reader), malformed JSON → 400, sustained GETs trip the 429 — plus `bin/smoke-test-webhook` 13/13 and 49/49 BATS green. **Owner deploy note:** populate `K3DM_SLACK_ROLE_MAP` before deploy or every Slack user is treated as reader-only. |
| [v1.20.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.20.0) | 2026-08-01 | Make the CVE auto-patch loop actually run. v1.18.0 wired up the Trivy→webhook→`app-cve-scan` detect→patch loop; this release is the hardening pass that fixes the many ways it broke in practice. `app-cve-scan` now patches ArgoCD Applications by their **cluster-prefixed** name (`ubuntu-hostinger-<svc>`, …) instead of the bare service name — the root cause of 26 remediation jobs that ran to completion but patched nothing (`9f7cdfe0`). Candidate resolution accepts **OCI image-index** media types in the manifest `Accept` header, so multi-arch `latest`/`sha-*` digests resolve instead of silently failing (`17f5f0e0`), and registry reads authenticate with the scan pod's **BusyBox** `wget --header` rather than the GNU-only `--config` flag BusyBox does not support (`47c2e2d7`). Exit semantics fixed: a completed scan **exits 0** — a per-service unresolved-digest or failed-rebuild-dispatch state now emits a warning notification instead of poisoning the Job exit code, so the CronJob is no longer marked Failed while a triage bot loops on a misleading "Trivy exits 1 on CRITICAL" diagnosis; only a run that matched **zero** VulnerabilityReports fails the Job. Both scan CronJob pod templates carry `app` / `app.kubernetes.io/name` labels so alerts render the workload name instead of `app ''` (`03fe5684`). CVE scan no longer retries a service whose immutable candidate cannot be resolved this run (`0136571f`), CronJobs bound `backoffLimit` + `ttlSecondsAfterFinished` so failed scan pods stop accumulating in `platform-ops` (`bda65d5c`), the Hub CVE scan reads its target chart from live deployment metadata instead of a hard-coded path (`699da11b`), LDAP password seeds are verified during cluster bring-up so identity login self-heals (`bb5b5653`), and `make up` reconciles the platform-ops stack after the cluster comes up so the CVE scan + dashboards are present on a cold bring-up (`0a316fb6`). (v1.19.0 was a shopping-cart-only Dependabot milestone with no k3d-manager changes and no tag.) |
| [v1.18.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.18.0) | 2026-07-28 | Close the first-mile CVE gap. A Trivy vulnerability report on a running app now drives an automatic patch end-to-end: the Trivy alert fires the k3dm webhook, which re-runs `app-cve-scan` for the affected workload (`1684190c`), closing the detect→patch loop without manual intervention. For the app dependencies Trivy can't rebuild, **Dependabot** version + security updates are enabled on all five shopping-cart repos (`basket`, `frontend`, `order`, `payment`, `product-catalog`) so dependency CVEs self-heal into PRs; branch protection on each was made Dependabot-mergeable — the phantom required `Go CI` context (emitted by no workflow, so every merge blocked forever) replaced with the checks each repo actually produces, and `required_approving_review_count` set to `0` for the solo maintainer, so a green Dependabot PR merges without an admin bypass while `enforce_admins` stays `true`. The platform-ops stack (CVE scan + Grafana dashboards + webhook-token sync) now deploys from the ArgoCD **bootstrap** path (`8537f27e`) so it survives a cold rebuild instead of needing a manual reapply. A hub Grafana dashboard visualizes the auto-patch loop — Trivy reconcile health, alert dispatch, scan outcomes (`29a087cf`). Webhook-token disaster recovery — the `k3dm-webhook-token` Secret auto-syncs from the macOS Keychain on every platform-ops deploy, and the Keychain→Secret sync is generalized so the app-rebuild `gh-token` follows the same DR path (`b0bc5a21`, `907f6259`). `make status` reports ApplicationSet values-branch drift (`3e847b26`, `c704d669`) so a set frozen on a stale release branch is visible instead of silently inert; Alertmanager `matcherStrategy: None` (`5af5e3a7`) routes a cluster-wide CVE alert to the webhook receiver regardless of namespace labels; every Trivy version pinned explicitly (`f428225f`). Fixes: `app-cve-scan` authenticates ghcr reads with `GH_TOKEN` for private packages (`191d9f4a`) and matches Trivy reports on the registry-less repository path (`43e63b53`); Keycloak LDAP federation component is created when absent so login self-heals on rebuild (`989dc8e4`); `make show-service-passwords` shows the correct dev users (`ad96c028`); the Trivy reconcile Grafana panel — a query matching zero lines, `timeFrom`, and the render — is now a per-container bar gauge on an instant Loki query. Ships lib-foundation **v0.4.8** via subtree (brace-expansion 1.1.16 CVE fix, `79294e89`) |
| [v1.17.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.17.0) | 2026-07-24 | The smoke test proves real logins. `make status` used to false-green: the health smoke only fetched health URLs, and a Keycloak stale-session page returns HTTP 200, so a broken login counted as a pass. The smoke now performs **credentialed logins** — a token mint for Keycloak, an authed request for the frontend, and real logins for ArgoCD and Grafana. Because the app-owned `frontend` Keycloak client has `directAccessGrantsEnabled=false` (a password grant can never succeed against it), k3d-manager seeds its **own** `k3dm-smoke` public client + local user via `keycloak_seed_smoke_user`, storing the generated credential in the `identity/k3dm-smoke-user` Secret; the seed is idempotent, so re-running restores it after an app-owned realm reconcile. ArgoCD and Grafana admin credentials are auto-discovered from their in-cluster Secrets instead of requiring exported env vars. Fixes: the seeded smoke user now receives the Keycloak 24+ declarative User Profile attributes that are required by default (`email`, `firstName`, `lastName`, `emailVerified`) and repairs them idempotently on re-run, so the direct-access-grant mint no longer fails `invalid_grant "Account is not fully set up"` on a fresh cluster; the frontend login check attempts the authed `/api/cart` request when falling back to the smoke client instead of unconditionally skipping it — 2xx passes, a smoke-client `401`/`403` skips (audience-strict deployment), anything else fails; `_ambient_install_cilium` builds `ssh_cmd` as an array rather than a string so all three remote invocations quote the key path and user@host correctly. Dead `_argocd_configure_post_deploy` removed along with the `enable_vault` local it was the only consumer of |
| [v1.16.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.16.0) | 2026-07-23 | Ambient mesh comes to the app tier. The hostinger `shopping-cart-apps` namespace migrated from sidecar injection to Istio **ambient** (`istio.io/dataplane-mode=ambient`) — all app pods run `1/1` with zero `istio-proxy` containers while ztunnel carries HBONE + mTLS between them (verified live: `dst.hbone_addr`, spiffe identities, inbound+outbound on `frontend → basket-service`). Ambient made durable: hostinger `make refresh` reapplies the `istio-ambient` ApplicationSet instead of dropping it, and cold `k3s-aws` rebuilds now install Cilium for ambient (`K3S_AMBIENT_MESH=true` default) with `set -e`-safe node-ready/SSM wait loops and a pinned `k3sup --k3s-version` to survive `update.k3s.io` channel outages; istio-cni conf/bin dirs resolve per CNI substrate (Cilium `/etc/cni/net.d` + `/opt/cni/bin` vs k3s/rancher paths), defaulted in `scripts/etc/argocd/vars.sh`. Multi-cluster ArgoCD hardened — appsets derive `APP_CLUSTER_NAME` from the active cluster (not hardcoded hostinger) and key `services-git` Application names by cluster so a second app cluster no longer collides with the first; a shopping-cart `AppProject` (incl. the `secrets` namespace for the vault-bridge) lets data-layer + services sync; appset `envsubst` fails loudly on an unset var instead of silently rendering an empty value. `cluster-status` reports service-mesh mode, CNI substrate, and ambient-namespace enrollment. 2-CPU hostinger right-sizing — `istiod`/`ztunnel` CPU requests trimmed and the built-in Trivy server scheduling reservation cut `200m`→`50m` on both the hub and app-cluster observability values (limits kept so real scans still burst) so `product-catalog` leaves `Pending` and schedules. App-image CVE scan dispatches via `wget` (with bats coverage) and fails loudly when the infra cluster secret is absent; bare `mktemp` sites in `shopping_cart.sh`/`argocd.sh` are trap-guarded. Ships lib-foundation **v0.4.7** via subtree — `acg_restart` shell entrypoint for the orphaned recovery script, stale `playwright-artifacts-*` temp-dir sweep, and `acg_check_ttl`/wrapper node-exit captures made `set -e`-safe |
| [v1.15.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.15.0) | 2026-07-13 | Security & multi-cluster provider hardening. Trivy Operator migrated Standalone→ClientServer via the chart's built-in shared Trivy server (`operator.builtInTrivyServer: true`), which deploys a shared `trivy-server` StatefulSet + `trivy-service:4954` and auto-wires `trivy.mode: ClientServer`, eliminating the per-job vuln-DB `cache may be in use by another process: timeout` failures under concurrent Standalone scans (hub + ACG values). js-yaml DoS advisory GHSA-h67p-54hq-rp68 (medium) closed via the lib-foundation v0.4.4 subtree — dev-only transitive `js-yaml` bumped `3.14.2`→`3.15.0`; the same pull carries the ACG Extend sandbox-tab routing fix (`_isSandboxPageUrl` tightened to `cloud-sandboxes`). Per-context hub-Vault ClusterSecretStore connectivity overrides keyed per app context so registering a second app cluster no longer clobbers the first cluster's connectivity profile. Default cluster provider demoted off `k3s-aws` with an added reachability preflight, so a bare invocation no longer defaults to an AWS cluster and an unreachable target fails fast instead of hanging |
| [v1.14.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.14.0) | 2026-07-12 | Observability & multi-cluster reliability hardening — a reactive sprint from operating the observability stack and the laptop/Hostinger/OCI/ACG fleet under load (21 bug specs, 19 shipped). Vault multi-cluster portability Workstream 3 Phase 1 — `configure_vault_app_auth_for_context` derives a per-cluster auth mount `kubernetes-<sanitized-context>` via `_vault_app_auth_mount` and threads it through all three env-driven mount sites (`configure_vault_app_auth`, the shopping-cart ClusterSecretStore auth block, ESO remote-Vault SecretStore), ending the last-cluster-wins clobber of the single shared `kubernetes-app` mount; `APP_K8S_AUTH_MOUNT` pins the legacy path for migration. Observability fidelity — Grafana memory raise (stop restarts/liveness kills), hub + app-cluster Prometheus PVCs (15d retention), hub Grafana public-route fix, Trivy hub-dashboard drilldown/alerts/finding-explanations/ownership-classification/dedupe, Image Updater processing-log counter parse, Loki logs panels filtered + formatted, Trivy scan-job OOMKill fix (scanner limit 512Mi→1Gi, request 64Mi→256Mi; `operator.resources` untouched). ACG sandbox lifecycle — `exit-22` seed-addr freeze resolved at call time, `_vault_kv_put` surfaces KV write HTTP status, absent-sandbox vs tunnel-loss classification, `k3s-oci` mapped to its own kube context, tunnel mode auto-selects SSM vs SSH by probing `iam:CreateRole`. Hub-Vault profile state scoped by app context; shopping-cart rerun Vault-helper scope fix for LDAP seeding; lib-foundation subtree synced to v0.4.3 |
| [v1.13.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.13.0) | 2026-07-05 | Webhook modularization Phase 1 — pure helpers extracted verbatim from the `bin/k3dm-webhook` monolith into `scripts/lib/webhook/` (`config.py` constants/paths/`_safe_job_dir`, `render.py` Slack output, `proc.py` fork-safe `os.posix_spawn` capture, `auth.py` Keychain/bearer-token/Slack-signature), no command-behavior or Slack/Worker-contract change; isolated webhook-server smoke gate (`bin/smoke-test-webhook`) boots a throwaway instance on an overridden `K3DM_JOB_DIR`/`K3DM_RUN_DIR` and hits `/api/v1/health`; post-refactor architecture doc (`docs/architecture/webhook-server.md`); fixes — Vault token delivered via stdin instead of process argv, Trivy Operator RBAC chart-pin realigned to the `0.31.2` image + own ServiceMonitor `release` label, webhook BATS isolation (`make`/`K3DM_GEMINI_BIN` stubs + `K3DM_JOB_DIR`/`K3DM_RUN_DIR` isolation to stop live `make up`/Chrome/`agy` launches and false Slack orphan-job alerts) |
| [v1.12.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.12.0) | 2026-07-03 | App-image CVE auto-update pipeline — ArgoCD Image Updater watches shopping-cart images, gates promotion on Trivy reports, writes back immutable SHAs, dispatches rebuilds (installed during bootstrap, `ghcr-pull-secret` in `cicd`); app CVE visibility in `make status` + a hub Grafana dashboard (Image Updater readiness, watched-app churn, Loki logs); remote operator access over Slack/Cloudflare — relay assigns per-command roles and forwards `X-K3DM-Role`/`X-K3DM-Actor`/`X-K3DM-Source-Command`, webhook enforces minimum roles (`403`), writes a JSONL audit trail, adds a read-only `/cluster-diagnose`; public Alertmanager access via a credential-rereading basic-auth proxy; Trivy Operator observability (scan-job failure + infra-security alert rules routed to the analyzer webhook); fixes — Hostinger `refresh` hardening, Vault token hygiene (no inline tokens), Slack `/cluster-status` ack-and-relay reliability, Trivy chart-pin rendering on k8s 1.36, Loki/Grafana wiring, debug artifacts moved from `/tmp` to `~/.local/share/k3d-manager/run` |
| [v1.11.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.11.0) | 2026-06-28 | Tier 3 P3 canonical-source Vault seeding — `vault_seed_hub_into_context` + idempotent `redis/*` and `rabbitmq/default` seeding (re-runnable without drift); Tier 3 P4 assisted-failover watchdog — `vault_failover_hub_into_context` (probe→profile-flip→re-seed→CSS reconcile), `vault_install_failover_watchdog` LaunchAgent (300s) + `bin/k3dm-vault-failover` wrapper + plist template; failover watchdog hardening (wrapper prepends Homebrew PATH for launchd, re-seed isolated in a subshell so an unreachable laptop source degrades to warn-and-continue, active-profile sourced before read); app-cluster Vault auth targets the in-cluster context under the `hostinger` profile + audience bound on the `eso-app-cluster` k8s-auth role; `deploy_eso` skips the Helm install when ESO is ArgoCD-managed; `k3s-hostinger` `refresh` runs `launchctl enable` before `bootstrap` to heal disabled agents; `.gitguardian.yaml` v2 schema |
| [v1.10.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.10.0) | 2026-06-27 | Provider-agnostic app-cluster Vault auth keyed on kube-context (`configure_vault_app_auth_for_context`) — portable across EKS/AKS/ACG/Azure/OCI/Hostinger; `HUB_VAULT_PROFILE` endpoint seam (`laptop`/`hostinger`) selecting the hub-Vault `server:` URL and whether the reverse-tunnel/socat bridge is stood up (Tier 3 P1, default `laptop` byte-for-byte); in-cluster auto-unseal watchdog CronJob replaying the Shamir shard on a `vault status` trigger (Tier 3 P2a, pinned image, namespace-scoped, no RBAC); `vault_deploy_hub_into_context` + least-privilege `app-cluster-reader` policy + kubernetes-auth ClusterSecretStore variant selected by `HUB_VAULT_CSS_AUTH` (Tier 3 P2b); Hostinger `refresh` ensures the `vault-port-forward` LaunchAgent; ArgoCD `data-layer` ApplicationSet ignores controller-injected `volumeClaimTemplates[]` fields |
| [v1.8.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.8.0) | 2026-06-26 | ESO operator install on app clusters via an ArgoCD ApplicationSet cluster generator (`external-secrets` chart, server-side apply, arch-portable); lib-foundation subtree synced to v0.4.0 (lib-acg absorbed, `_ensure_agy_cli`, extensible cluster-provider hook); ACG module now sourced from lib-foundation — `acg.sh`/`gcp.sh`/`gemini.sh` stubs + `bin/cluster-up`/`bin/cluster-refresh` repointed (absorption Phase 2); `gemini.sh` browser automation retargeted from the retired `@google/gemini-cli` to the Antigravity CLI (`agy`); Hostinger fixes (ghcr-pull-secret reconcile, CA-verified TLS + argocd-manager SA, provider-aware refresh/status, provider-state precedence, vcluster deregister, observability namespace race); standalone `lib-acg` subtree + git remote removed |
| [v1.7.1](https://github.com/wilddog64/k3d-manager/releases/tag/v1.7.1) | 2026-06-20 | vCluster preflight runner (`bin/cluster-preflight`) — webhook-as-runner, ArgoCD-registered throwaway vCluster on the Hostinger host (`--auto`/`--keep`/`--reuse`/`--destroy`); ArgoCD app-cluster label generator for shopping-cart + data-layer StatefulSet routing (finalizer-aware cutover); `bin/acg-*`→`bin/cluster-*` and Slack `/acg-*`→`/cluster-*` hard rename; `/cluster-*` default to Hostinger + provider-aware `cluster-status`; pre-push/pre-commit hooks under `.githooks/`; tightened pre-push main-guard; `/codex` `--output-last-message` fix |
| [v1.7.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.7.0) | 2026-06-13 | `k3s-hostinger` cluster provider — single-node k3s app cluster on a permanent Hostinger KVM VPS via SSH/k3sup (no VM lifecycle); env-overridable `HOSTINGER_HOST`; app-cluster registration with the hub ArgoCD via mTLS cluster secret (`cluster-ubuntu-hostinger`); `bin/hostinger-status` read-only full status report wired into `make status CLUSTER_PROVIDER=k3s-hostinger`; `APP_CLUSTER_NAME` ApplicationSet destination parameter |
| [v1.6.5](https://github.com/wilddog64/k3d-manager/releases/tag/v1.6.5) | 2026-06-13 | `k3s-az` Azure provider (VM provision + k3sup install + shopping-cart deploy), wired into `bin/acg-up`/`bin/acg-down`; optional `provider` argument for `/acg-up`/`/acg-down`/`/acg-resume`; provider-aware `make refresh`/`make status`/`acg-refresh`/`acg-status` via recorded active-provider state; Azure credential-path + SSH host-key hardening; `azure.sh` no longer sources `azure-vars.sh` at load; lib-acg subtree v0.1.7; lib-foundation extensible cluster-provider hook |
| [v1.6.4](https://github.com/wilddog64/k3d-manager/releases/tag/v1.6.4) | 2026-06-10 | Slack Events API text commands (`acg-status`/`acg-refresh`/`ask`/`claude`/`gemini`/`codex` from thread replies + channel messages); Slack thread context for text commands; Prometheus deploy metrics + Grafana dashboard + non-interactive auth bootstrap; `make show-service-passwords`; ACG restart-failure screenshot archival; webhook `posix_spawn` NEF-safety; lib-acg subtree v0.1.4 |
| [v1.6.3](https://github.com/wilddog64/k3d-manager/releases/tag/v1.6.3) | 2026-06-07 | `/acg-resume` checkpoint re-entry; `/ask` multi-agent troubleshooting; `make fix-*` agent fix targets; Slack thread context injection for agents; Gemini side-observation bug filing; Webhook read-only bash sandbox + prompt-injection guard for `/ask` agents; Webhook NEF SIGSEGV fix (`posix_spawn`); Keycloak group-ldap-mapper reconciliation |
| [v1.6.2](https://github.com/wilddog64/k3d-manager/releases/tag/v1.6.2) | 2026-06-05 | `/acg-refresh` Slack slash command via Cloudflare Worker relay; Keycloak admin token retry on slow startup; headless webhook fixes (no TTY, non-fatal kubectl) |
| [v1.6.1](https://github.com/wilddog64/k3d-manager/releases/tag/v1.6.1) | 2026-06-05 | ACG LaunchAgent self-healing — `acg-refresh` auto-reinstalls missing system daemon plists (argocd/keycloak/frontend port-forwards); Vault port-forward LaunchAgent install during `acg-up`; Prometheus web.config.file conflict fix; acg-status stale display caveat labels |
| [v1.6.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.6.0) | 2026-06-04 | Webhook Slack threading + thread commands; ArgoCD CVE scan CronJob; AI-powered failure analysis (Gemini triage + Claude vision); webhook token auto-rotation; Python 3.13 SIGSEGV fix; ArgoCD/Keycloak OIDC hardening; ACG observability stack (Prometheus + Grafana + Alertmanager) |
| [v1.5.1](https://github.com/wilddog64/k3d-manager/releases/tag/v1.5.1) | 2026-05-31 | OCI object storage backup/restore for k3s-oci (etcd snapshot lifecycle); ACG credential automation improvements; SSH fail-fast and snapshot validation fixes; BATS error-path coverage |
| [v1.5.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.5.0) | 2026-05-31 | OCI Always Free provider (k3s-oci single/two-node ARM64) with Cilium CNI; observability stack (Prometheus+Grafana+Trivy+Alertmanager with email-to-SMS) via ArgoCD ApplicationSet; observability.sh subshell guard fix; ACG memory limit fixes; credential wait loop + Playwright CDP improvements |
| [v1.4.12](https://github.com/wilddog64/k3d-manager/releases/tag/v1.4.12) | 2026-05-29 | imagePullSecrets patch for named ServiceAccounts (ghcr.io 401 fix), Makefile sync-branch/sync-main targets, ArgoCD ApplicationSets in make status, shopping-cart-payment ExternalSecret SharedResourceWarning fix |
| [v1.4.11](https://github.com/wilddog64/k3d-manager/releases/tag/v1.4.11) | 2026-05-29 | ESO sync saturation fix, acg-down macOS Tahoe password prompt fix, data-layer StatefulSet race fix, Keycloak group-ldap-mapper reconciliation, ArgoCD RBAC product-catalog reference fix, legacy ArgoCD app definition removal |
| [v1.4.9](https://github.com/wilddog64/k3d-manager/releases/tag/v1.4.9) | 2026-05-22 | ACG credential wait + extraction visibility fixes — sandbox TTL polling before provision, auto-extend before expiry, auto-restart expired; Cloudflare tunnel named config generation; CoreDNS duplicate `hosts` block patching; CloudFormation race condition polling after credential extraction |
| [v1.4.8](https://github.com/wilddog64/k3d-manager/releases/tag/v1.4.8) | 2026-05-19 | vault.sh temp file leak fixes, acg_extend.js CDP disconnect hang, Keycloak frontendUrl + named Cloudflare tunnel in acg-up |
| [v1.4.7](https://github.com/wilddog64/k3d-manager/releases/tag/v1.4.7) | 2026-05-18 | Keycloak public URL via Cloudflare + CoreDNS — `frontendUrl` set to Cloudflare domain after realm import; SSH tunnel + iptables DNAT + CoreDNS patch for cross-cluster Keycloak reachability |
| [v1.4.4](https://github.com/wilddog64/k3d-manager/releases/tag/v1.4.4) | 2026-05-08 | Identity SSO fixes — Keycloak ExternalSecret bootstrapping, Keycloak KV seeding, shopping-cart-identity ArgoCD app, shopping-cart-infra OIDC realm; lib-foundation + lib-acg subtree pulls |
| [v1.4.2](https://github.com/wilddog64/k3d-manager/releases/tag/v1.4.2) | 2026-05-07 | `_ai_agent_review` generic dispatch + ArgoCD bootstrap hardening + lib-acg cdp.sh fixes — `launchctl bootout`, dead Linux else-block removal, configurable `ARGOCD_SERVER_WAIT_TIMEOUT`, Hub bootstrap refresh |
| [v1.4.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.4.0) | 2026-05-01 | Copilot CLI plugin — `copilot_triage_pod` + `copilot_draft_spec`; `_copilot_review` in lib-foundation; pre-commit `AGENT_LINT_AI_FUNC` wiring; how-to doc |
| [v1.3.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.3.0) | 2026-05-01 | Sandbox rebuild hardening — GHCR PAT validation in `acg-up` + `rotate-ghcr-pat`; Makefile OAuth fallback removed; payment ESO postgres creds from Vault; `cdp.sh` subtree path fix; lib-acg extend timing sync |
| [v1.2.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.2.0) | 2026-04-30 | lib-acg extraction + shopping-cart bootstrap + GHCR hardening — ACG/GCP automation extracted to `scripts/lib/acg/` subtree; `deploy_shopping_cart_data()` in `acg-up`; Vault-first GHCR fail-closed; ArgoCD launchd port-forward; ApplicationSet branch var; Vault sealed-state recovery |
| [v1.1.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.1.0) | 2026-04-24 | Unified ACG automation AWS + GCP — GCP provider (`k3s-gcp`), OAuth automation, CDP headless Linux, `bin/acg-sync-apps` port-forward hardening, Hub auto-create + bootstrap, provider-aware teardown |
| [v1.0.6](https://github.com/wilddog64/k3d-manager/releases/tag/v1.0.6) | 2026-04-11 | AWS SSM support — `ssm_wait`/`ssm_exec`/`ssm_tunnel` helpers; `K3S_AWS_SSM_ENABLED` opt-in; IAM role + instance profile in CloudFormation; `--capabilities CAPABILITY_NAMED_IAM` fix; `make ssm`/`provision` targets |
| [v1.0.5](https://github.com/wilddog64/k3d-manager/releases/tag/v1.0.5) | 2026-04-10 | antigravity decoupling + LDAP Vault KV seeding — `antigravity_acg_extend` renamed/moved to `acg_extend_playwright` in `acg.sh`; `bin/acg-up` seeds `secret/data/ldap/admin` for ESO ExternalSecret |
| [v1.0.4](https://github.com/wilddog64/k3d-manager/releases/tag/v1.0.4) | 2026-04-10 | ACG extend hardening — button-first search; midnight date-wrap fix; random passwords in `bin/acg-up`; sandbox-expired guidance in `_acg_check_credentials`; Pluralsight URL standardization |
| [v1.0.3](https://github.com/wilddog64/k3d-manager/releases/tag/v1.0.3) | 2026-04-05 | ACG full stack fixes — ESO 1.0.0; ClusterSecretStore `v1`; ArgoCD context + server URL fix; `GHCR_PAT` masking; Chrome CDP launchd agent; `make sync-apps` + `make argocd-registration` |
| [v1.0.2](https://github.com/wilddog64/k3d-manager/releases/tag/v1.0.2) | 2026-04-03 | full stack automation — `make up` 12-step provision; Vault port-forward; vault-bridge Service; argocd-manager bootstrap; helm + ESO install; `bin/` SCRIPT_DIR fix |
| [v1.0.1](https://github.com/wilddog64/k3d-manager/releases/tag/v1.0.1) | 2026-03-31 | multi-node k3s-aws + CloudFormation + Playwright hardening — 3-node CF stack; auto sign-in; remove Antigravity pre-calls from `acg_get_credentials`; `AGENT_IP_ALLOWLIST` in pre-commit hook |
| [v1.0.0](https://github.com/wilddog64/k3d-manager/releases/tag/v1.0.0) | 2026-03-29 | k3s-aws provider foundation — `CLUSTER_PROVIDER=k3s-aws` end-to-end deploy; `aws_import_credentials`; `acg_provision --recreate`; `acg_watch` background TTL watcher; keypair idempotency + `page.goto()` fix |
| [v0.9.21](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.21) | 2026-03-29 | `_ensure_k3sup` auto-install helper — `deploy_app_cluster` now auto-installs k3sup via brew or curl; consistent with `_ensure_node`/`_ensure_copilot_cli` pattern |
| [v0.9.20](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.20) | 2026-03-29 | ACG Chrome launch fix — `_antigravity_launch` now opens Chrome (not Antigravity IDE) with `--password-store=basic`; `acg_credentials.js` SPA nav guard avoids hard reload |
| [v0.9.19](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.19) | 2026-03-28 | ACG automated credential extraction — `acg_get_credentials` (Playwright CDP auto-start), `acg_import_credentials` (stdin fallback), static `acg_credentials.js`; live-verified |
| [v0.9.18](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.18) | 2026-03-28 | Pluralsight URL migration — `_ACG_SANDBOX_URL` + `_antigravity_ensure_acg_session` updated to `app.pluralsight.com` |
| [v0.9.17](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.17) | 2026-03-27 | Antigravity model fallback (`gemini-2.5-flash` first), ACG session check, nested agent fix (`--approval-mode yolo` + workspace temp path) |
| [v0.9.16](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.16) | 2026-03-26 | Antigravity IDE + CDP browser automation — gemini CLI + Playwright engine; `antigravity_install`, `antigravity_trigger_copilot_review`, `antigravity_acg_extend`; ldap stdin hardening |
| [v0.9.13](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.13) | 2026-03-23 | v0.9.12 retro, `/create-pr` `mergeable_state` check, CHANGE.md backfill for v0.9.12 |
| [v0.9.11](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.11) | 2026-03-22 | dynamic plugin CI — `detect` job skips cluster tests for docs-only PRs; maps plugin changes to targeted smoke tests |
| [v0.9.10](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.10) | 2026-03-22 | if-count allowlist elimination (jenkins) — 8 helpers extracted; allowlist now `system.sh` only |
| [v0.9.9](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.9) | 2026-03-22 | if-count allowlist elimination — 11 ldap helpers + 6 vault helpers extracted; allowlist down to `system.sh` only |
| [v0.9.7](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.7) | 2026-03-22 | lib-foundation sync (`--interactive-sudo`, `_run_command_resolve_sudo`), `deploy_cluster` no-args guard, `bin/` `_kubectl` wrapper, BATS stub fixes, Copilot PR #41 findings |
| [v0.9.6](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.6) | 2026-03-22 | ACG sandbox plugin (`acg_provision/status/extend/teardown`), VPC/SG idempotency, `ACG_ALLOWED_CIDR` security, kops-for-k3s reframe |
| [v0.9.5](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.5) | 2026-03-21 | `deploy_app_cluster` — EC2 k3sup install + kubeconfig merge + ArgoCD registration; replaces manual rebuild |
| [v0.9.4](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.4) | 2026-03-21 | autossh tunnel plugin, ArgoCD cluster registration, smoke-test gate, `_run_command` TTY fallback, lib-foundation v0.3.3 |
| [v0.9.3](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.3) | 2026-03-16 | TTY fix (`_DCRS_PROVIDER` global), lib-foundation v0.3.2 subtree, cluster rebuild smoke test |
| [v0.9.2](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.2) | 2026-03-15 | vCluster E2E composite actions, 11-finding Copilot hardening (curl safety, mktemp, sudo -n, input validation) |
| [v0.9.1](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.1) | 2026-03-15 | vCluster plugin (`create/destroy/use/list`), two-tier `--help`, `function test()` refactor, 11 Copilot findings fixed |
| [v0.9.0](https://github.com/wilddog64/k3d-manager/releases/tag/v0.9.0) | 2026-03-15 | k3dm-mcp planning, agent workflow lessons, roadmap restructure |
| [v0.8.0](https://github.com/wilddog64/k3d-manager/releases/tag/v0.8.0) | 2026-03-13 | Vault-managed ArgoCD deploy keys, `deploy_cert_manager` (ACME/Let's Encrypt), Istio IngressClass, `install-hooks.sh` |
| [v0.7.3](https://github.com/wilddog64/k3d-manager/releases/tag/v0.7.3) | 2026-03-11 | `shopping_cart.sh` plugin, reusable GitHub Actions CI/CD workflow, ArgoCD ubuntu-k3s, infra rebuilt on M2 Air |
| [v0.7.2](https://github.com/wilddog64/k3d-manager/releases/tag/v0.7.2) | 2026-03-08 | dotfiles/hooks integration, lib-foundation v0.2.0, Ubuntu ESO + shopping-cart-data |
| [v0.7.0](https://github.com/wilddog64/k3d-manager/releases/tag/v0.7.0) | 2026-03-07 | lib-foundation subtree, `deploy_cluster` refactored 12→5 if-blocks |
| [v0.6.5](https://github.com/wilddog64/k3d-manager/releases/tag/v0.6.5) | 2026-03-07 | Agent Rigor BATS coverage, lib-foundation extracted |
| [v0.6.4](https://github.com/wilddog64/k3d-manager/releases/tag/v0.6.4) | 2026-03-07 | Linux k3s validation, `_agent_audit`, provider contract BATS (30 tests) |
| [v0.6.3](https://github.com/wilddog64/k3d-manager/releases/tag/v0.6.3) | 2026-03-06 | `_agent_lint` + `_agent_audit`, permission cascade refactor |
| [v0.6.2](https://github.com/wilddog64/k3d-manager/releases/tag/v0.6.2) | 2026-03-06 | Agent Rigor Protocol, `_k3d_manager_copilot` wrapper, PATH hardening |
| [v0.1.0](https://github.com/wilddog64/k3d-manager/releases/tag/v0.1.0) | 2026-02-27 | Initial release — k3d/k3s/OrbStack providers, Vault PKI, Jenkins, ESO, LDAP/AD |
